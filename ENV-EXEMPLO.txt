# Copie este arquivo para um arquivo chamado .env (na raiz do projeto) e preencha.
# O .env está no .gitignore e nunca é commitado — o token não sai da sua máquina.
#
# Onde achar cada coisa no Chatwoot:
#   CHATWOOT_URL     — o endereço do seu Chatwoot na VPS (sem barra no final)
#   CHATWOOT_ACCOUNT — aparece na própria URL quando você navega:
#                      https://seu-chatwoot/app/accounts/1/dashboard  ->  o "1"
#   CHATWOOT_TOKEN   — Perfil (canto inferior esquerdo) > Configurações do perfil
#                      > Token de acesso. É o "access token" do seu usuário.

CHATWOOT_URL=https://seu-chatwoot.exemplo.com
CHATWOOT_ACCOUNT=1
CHATWOOT_TOKEN=cole_seu_token_aqui

# Para onde os alertas vão. Preencha depois de rodar:
#     python tools/check_whatsapp.py conversas
# que lista as conversas com seus IDs.
# Aceita vários IDs separados por vírgula (ex.: 12,34,56).
CHATWOOT_CONVERSAS=

# ---------------------------------------------------------------------------
# OPCIONAL: de onde o scanner aceita COMANDO pelo WhatsApp.
#
# Vazio = o scanner so FALA, nunca ouve. Deixe assim se voce nao precisa
# mandar comando — e uma superficie a menos.
#
# Com uma conversa aqui, qualquer pessoa que escreva nela pode mandar no
# scanner. Use so uma conversa SUA ou da party.
#
# Comandos aceitos (sempre com ponto na frente):
#   .cancelar   tira o silencio de TvT/Prime que estiver rolando
#   .status     diz se esta vigiando ou em silencio, e qual o proximo evento
#
# Para descobrir qual id usar:
#   .venv\Scripts\python tools\check_whatsapp.py entrada
#
# ATENCAO: mensagens de GRUPO podem nao chegar ao Chatwoot. A ponte Baileys
# costuma vir com a ingestao de grupo desligada — medido nesta conta: os 11
# grupos nao recebem, e so as conversas privadas recebem. Se for o seu caso,
# mande o comando no PRIVADO do numero do bot; a resposta continua indo para
# o grupo normalmente.
CHATWOOT_CONVERSAS_COMANDO=

# QUEM pode mandar comando. Numeros separados por virgula.
#
# Vazio = QUALQUER PESSOA que escreva nas conversas acima manda no scanner.
# Num privado seu isso da na mesma; num GRUPO, libera todo mundo.
#
# A comparacao usa os ULTIMOS 8 DIGITOS, entao qualquer formato serve e o
# nono digito nao atrapalha: +5544997077000 e 554497077000 sao a mesma pessoa
# para o scanner. Escreva do jeito que preferir.
CHATWOOT_TELEFONES_COMANDO=

# ---------------------------------------------------------------------------
# PONTE DO DISCORD — so para quem vai rodar o ponte-discord.bat.
#
# O scanner (vigiar-party.bat) NAO usa esta chave e sobe normalmente sem ela.
#
# Onde achar o token:
#   1. Abra https://discord.com/developers/applications e entre na aplicacao
#      do bot (ou crie uma, em "New Application").
#   2. No menu da esquerda, clique em "Bot".
#   3. Clique em "Reset Token" e depois em "Copy". O Discord SO MOSTRA O TOKEN
#      UMA VEZ: se voce fechar a pagina sem copiar, tera que resetar de novo.
#   4. Cole na linha DISCORD_TOKEN= aqui embaixo, sem aspas e sem espaco.
#
# ESTE TOKEN E O BOT INTEIRO. Quem tem ele pode ler e escrever em todo servidor
# onde o bot esta, com o nome do bot. Por isso ele mora aqui no .env, que esta
# no .gitignore e nunca e commitado — e nunca no config.toml, que e versionado
# e e o arquivo que voce fotografa quando pede ajuda.
#
# Se voce colar o token em qualquer lugar por engano (grupo, print, issue),
# volte na pagina Bot e clique em "Reset Token": o antigo morre na hora.
#
# Antes do primeiro uso faltam mais 3 passos alem deste. Estao todos no
# PORTAO-DISCORD.txt, na raiz do projeto — inclusive o aviso da armadilha de
# teste, que e o erro mais caro que da para cometer aqui.
#
# A ponte RECUSA SUBIR se esta linha faltar, estiver vazia ou ainda tiver o
# texto de exemplo. Ela diz qual e o problema dos tres, porque os consertos sao
# diferentes: copiar este arquivo, preencher a linha, ou trocar o placeholder.
# Nenhuma dessas mensagens mostra o valor do token — nem truncado.
#
# CUIDADO COM A CONVERSA DE DESTINO. Ela mora no config.toml, na secao
# [discord], e NAO pode ser uma das conversas listadas em
# CHATWOOT_CONVERSAS_COMANDO aqui em cima: o arranque recusa se forem a mesma.
# O motivo e o sentido de VOLTA — quem RESPONDE numa conversa compartilhada
# manda uma mensagem de entrada, e numa conversa de comando essa resposta seria
# lida como comando do scanner.
DISCORD_TOKEN=

# OPCIONAL: etiqueta do Chatwoot que liga o canal de comando.
#
# Toda conversa marcada com esta etiqueta no painel do Chatwoot vira canal de
# comando, alem das listadas em CHATWOOT_CONVERSAS_COMANDO. Serve de
# interruptor: marcar e desmarcar no painel vale NA HORA, sem editar arquivo e
# sem reiniciar o scanner.
#
# A etiqueta sozinha nao restringe QUEM manda — para isso e o telefone acima.
CHATWOOT_ETIQUETA_COMANDO=
