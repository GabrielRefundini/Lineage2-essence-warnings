---
phase: 10-lista-de-presenca-do-solo-boss-pelo-whatsapp
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - l2scanner/agenda.py
  - l2scanner/presenca.py
  - tests/test_agenda.py
  - tests/test_presenca.py
autonomous: true
requirements: [PRES-05, PRES-06, PRES-07, PRES-08, PRES-09, PRES-10, PRES-11, PRES-13, PRES-15]

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um `.join` no privado poe o nick configurado na lista da proxima ocorrencia e o grupo recebe a confirmacao com esse NICK (D-05, D-10)"
    - "A resposta do `.join` diz QUAL horario de boss pegou, mesmo dado tres horas antes (D-07)"
    - "`.join` repetido responde no privado que a pessoa ja esta na lista e NAO manda nada ao grupo (D-08)"
    - "A confirmacao sai nos dois lugares com redacoes DIFERENTES: uma linha curta no privado e a confirmacao no grupo (D-09)"
    - "`.leave` tira da lista e o grupo fica sabendo; `.leave` de quem nunca joinou responde isso e nao anuncia nada"
    - "`.leave` depois do fechamento e recusado dizendo que o boss ja comecou (D-14)"
    - "Dezesseis threads competindo pelo mesmo `.join` produzem exatamente uma confirmacao de grupo (D-11)"
    - "Um marcador de presenca de quatro dias atras e podado; um de hoje sobrevive (D-11)"
  artifacts:
    - l2scanner/agenda.py
    - l2scanner/presenca.py
    - tests/test_presenca.py
  key_links:
    - "RegistroEmDisco.entrar -> marcar (O_CREAT|O_EXCL) — a atomicidade entre as duas instancias vem de graca"
    - "RegistroEmDisco.podar -> tupla de prefixos conhecidos, senao presenca_* fica em disco para sempre"
    - "presenca.py importa de agenda e loot; loot NUNCA importa presenca"
    - "MensagemDeComando.nick (plano 10-01) -> responder_join -> o slug do arquivo de presenca"
    - "RespostaDePresenca(privado, grupo|None) -> consumido pelo plano 10-03b no despacho"
---

<objective>
A lista de presenca em si: onde ela mora, quem entra, quem sai, e o que o bot
responde. E o plano com mais superficie da fase, e tres coisas nele sao
trabalho de verdade e nao detalhe:

1. **A poda nao alcanca o prefixo novo.** `RegistroEmDisco.podar` retira UM
   prefixo (`cancelado_`) antes de ler a data; qualquer outro cai no `except`
   e fica em disco para sempre. Isso contradiz frontalmente D-11 ("a poda de 3
   dias e CORRETA aqui: a lista morre quando o boss passa"). O conserto e uma
   tupla de prefixos conhecidos, e o teste da poda nasce junto.

2. **Nao existe analogo de resposta com DUAS redacoes.** Hoje `__main__.py`
   despacha o MESMO texto para o privado e para o grupo. D-09 pede textos
   diferentes. E desenho novo, pequeno e real: a resposta vira um par. Aqui
   nasce so o PAR (`RespostaDePresenca`); liga-lo ao despacho e generalizar o
   bloco que serve os oito ramos antigos e o plano **10-03b**, separado de
   proposito — aquela mudanca toca `__main__.py`, que tem 19-20% de cobertura
   e onde os 3 de 3 warnings do ultimo code review moravam.

3. **O tri-estado do disco e informacao de produto.** `ja_existia` e o que
   distingue "entrou" de "ja estava" (D-08), entao o analogo certo e o
   `RegistroDeLoot._criar` (criado | ja_existia | falhou), nao o `marcar` do
   `RegistroEmDisco`, que colapsa falha de disco em True.

Purpose: a pergunta que a chamada faz passa a ter resposta registrada.
Output: `l2scanner/presenca.py`, o namespace `presenca_` no `.agenda/`, a poda
consertada, e os ramos `.join`/`.leave` no despacho.

## Legenda das decisoes
Ver a tabela D-01..D-14 em `10-01-PLAN.md`. Este plano implementa D-05
(consumindo a autorizacao ja pronta), D-07, D-08, D-09, D-10, D-11 e D-14.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-CONTEXT.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-PATTERNS.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-01-SUMMARY.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-02-SUMMARY.md
</context>

<interface_context>
**Onde a lista mora, e por que:** dentro de `agenda.RegistroEmDisco`, e nao num
registro proprio. A pasta `.agenda/` ja e dele, a atomicidade `O_CREAT|O_EXCL`
ja esta escrita ali, a poda ja e dele, e o precedente literal (`PREFIXO_CANCELADO`
+ `cancelar()` + `cancelados()`) e um segundo namespace na mesma pasta. Um
registro novo duplicaria as tres coisas.

**Onde a DECISAO mora:** num modulo novo `l2scanner/presenca.py`, puro, que
importa `agenda` (ocorrencias, chave, tolerancia) e `loot` (`apelido`,
`exibir`). A direcao fica `presenca -> loot -> agenda`, e `loot` NUNCA importa
`presenca` — a restricao dura escrita no fim da docstring de `loot.py`. No
plano 10-05, quem precisa da lista no lado do loot a recebe por PARAMETRO,
lido na borda.

Assinaturas que este plano cria:

```
agenda.PREFIXO_PRESENCA = "presenca_"
agenda.PREFIXO_FECHADO  = "fechado_"
agenda.RegistroEmDisco.entrar(chave_da_ocorrencia, slug) -> str   # criado|ja_existia|falhou
agenda.RegistroEmDisco.sair(chave_da_ocorrencia, slug) -> bool    # True se removeu
agenda.RegistroEmDisco.presentes(chave_da_ocorrencia) -> frozenset[str]
agenda.RegistroEmDisco.fechar(chave_da_ocorrencia) -> bool        # sobre marcar()

presenca.RespostaDePresenca(privado: str, grupo: str | None)
presenca.ocorrencia_da_chamada(agora, eventos) -> tuple[str, datetime] | None
presenca.ocorrencia_recem_fechada(agora, eventos) -> tuple[str, datetime] | None
presenca.responder_join(registro, eventos, agora, nick) -> RespostaDePresenca
presenca.responder_leave(registro, eventos, agora, nick) -> RespostaDePresenca
presenca.nomes_dos_membros(membros) -> dict[str, str]   # slug -> nick configurado
```

`ocorrencia_da_chamada` e `proxima_ocorrencia` filtrada por
`chamar_minutos_antes > 0` — mecanismo generico, configuracao especifica
(D-03). Nada em `presenca.py` escreve o nome de um evento.

O `nick` que entra em `responder_join` e o `MensagemDeComando.nick` produzido
pelo plano 10-01 a partir do mapa `[[membro]]` — nunca `sender.name` (D-10).
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O namespace `presenca_` no `.agenda/`, e a poda que o alcanca</name>
  <files>l2scanner/agenda.py, tests/test_agenda.py</files>
  <read_first>
    - `l2scanner/agenda.py` linhas 238-354 — `PREFIXO_CANCELADO`, a docstring de `RegistroEmDisco` (as duas exigencias que sao a mesma), `marcar`, `cancelar`, `cancelados` e `podar`. A linha 342-347 e o defeito a consertar.
    - `l2scanner/loot.py` linhas 197-215 — `_criar`, o tri-estado, e a docstring que explica por que ele NAO e o `marcar`.
    - `l2scanner/loot.py` linhas 226-249 — `registros()`, o padrao de leitura por prefixo com `partition`.
    - `tests/test_agenda.py` linhas 426-560 — `TestRegistroEmDisco`, com `test_competicao_de_verdade_com_threads` e os dois testes de poda.
  </read_first>
  <behavior>
    - `entrar("2026-08-24_solo-boss-2000", "j4guar")` devolve `"criado"` na primeira vez e `"ja_existia"` na segunda.
    - Duas instancias apontando para a mesma pasta: exatamente uma recebe `"criado"`.
    - Dezesseis threads competindo pelo mesmo par (chave, slug): exatamente uma recebe `"criado"`.
    - `presentes` devolve so os slugs daquela ocorrencia; um `presenca_` de OUTRA ocorrencia nao vaza.
    - `sair` devolve True quando removeu e False quando nao havia nada — idempotente, sem levantar.
    - `podar` apaga `presenca_<data de 4 dias atras>_...` e preserva `presenca_<hoje>_...`.
    - `podar` apaga `fechado_<data velha>_...` e preserva o de hoje.
    - `podar` continua apagando `cancelado_*` velho e continua NAO tocando em `leiame.txt`.
    - `fechar` devolve True para o primeiro chamador e False para o segundo.
  </behavior>
  <action>
Em `l2scanner/agenda.py`, ao lado de `PREFIXO_CANCELADO`:

1. `PREFIXO_PRESENCA = "presenca_"` e `PREFIXO_FECHADO = "fechado_"`, com o
   mesmo comentario de namespace que o `PREFIXO_CANCELADO` ja tem. O nome
   completo de um marcador de presenca e
   `presenca_<chave-da-ocorrencia>_<slug>`, e o do fechamento e
   `fechado_<chave-da-ocorrencia>`. Como `chave_da_ocorrencia` comeca com
   `YYYY-MM-DD`, os dois sao podaveis pela mesma regra de data — desde que a
   poda saiba retirar o prefixo.

2. `_PREFIXOS_CONHECIDOS = (PREFIXO_CANCELADO, PREFIXO_PRESENCA, PREFIXO_FECHADO)`
   e, em `podar`, trocar o `if` unico por um laco sobre essa tupla. Comentario
   explicando o defeito que isso conserta: retirar so um prefixo fazia todo
   marcador de outro namespace cair no `except` e ficar em disco para sempre —
   o oposto do que D-11 decide para a lista de presenca. Registrar tambem, no
   mesmo comentario, o fato observado de que os marcadores `comando_<id>` do
   `chave_da_mensagem` tambem nunca sao podados hoje; nao mexer neles nesta
   fase, porque nao ha data no nome e resolve-los e outro desenho.

3. `entrar(chave_da_ocorrencia, slug) -> str`: cria
   `PREFIXO_PRESENCA + chave + "_" + slug` com `O_CREAT | os.O_EXCL |
   os.O_WRONLY` e devolve `"criado" | "ja_existia" | "falhou"`. Docstring
   dizendo POR QUE o tri-estado e nao o booleano do `marcar`: aqui
   `ja_existia` e informacao de PRODUTO — e o que faz o `.join` repetido
   responder "voce ja esta na lista" sem repetir no grupo (D-08) — e `"falhou"`
   nao pode virar True, porque anunciar no grupo uma entrada que nao foi
   gravada faria a lista fechar sem essa pessoa. Citar o `RegistroDeLoot._criar`
   como o analogo, e o `marcar` como o analogo que NAO serve aqui e por que.

4. `sair(chave_da_ocorrencia, slug) -> bool`: `unlink(missing_ok=True)`,
   devolvendo se o arquivo existia antes. `OSError` vira False, com a mesma
   exposicao deliberada de `RegistroDeLoot.cancelar` — um disco travado nao
   pode virar excecao no meio do farm.

5. `presentes(chave_da_ocorrencia) -> frozenset[str]`: filtra `enviados()` pelo
   prefixo mais a chave mais `_`, e devolve o sufixo. Mesma tecnica de
   `cancelados()`, com o cuidado de `partition` que `loot.registros()` ja usa
   para nao quebrar em nome malformado.

6. `fechar(chave)` sobre `marcar(PREFIXO_FECHADO + chave)`. Aqui o `marcar` E
   o certo: o fechamento e um ANUNCIO, e a regra do `marcar` (falha de disco
   prefere o duplicado ao perdido) e a regra dos anuncios do projeto.

   **NAO criar um `esta_fechada(chave)` ao lado.** Ele nao tem consumidor e
   nao deve ter, por duas razoes concretas:

   - `responder_leave` decide pela JANELA DE TEMPO (`ocorrencia_recem_fechada`),
     e essa e a predicao CERTA, nao uma aproximacao. Amarrar a recusa ao
     marcador faria a resposta depender de o tick ter rodado — detalhe de
     implementacao que quem digita nao enxerga. Caso concreto: scanner fora do
     ar as 20:00 e subindo as 20:03; um `.leave` as 20:02 nao seria recusado,
     cairia na ocorrencia das 22:00 e responderia "voce nao estava na lista",
     que e confuso e errado. A janela recusa certo nos dois estados.
   - Um `esta_fechada` morando ao lado do `fechar` e um convite para alguem
     transforma-lo na CHECAGEM ANTERIOR que a docstring do proprio
     `RegistroEmDisco.marcar` proibe por escrito ("A decisao de despachar tem
     que ser esta chamada, nunca uma checagem anterior"). Seria reabrir, de
     graca, o anuncio em dobro entre as duas instancias.

   O prefixo `PREFIXO_FECHADO` continua existindo: e o marcador que o `fechar`
   escreve e que a poda do item 2 precisa conhecer.

Em `tests/test_agenda.py`, dentro de `TestRegistroEmDisco`: replicar
`test_competicao_de_verdade_com_threads` para `entrar`, contando quantas
threads receberam `"criado"`. Esse e o teste que prova PRES-10 e PRES-15, e a
mensagem de falha tem que dizer quantas confirmacoes o grupo receberia.
  </action>
  <verify>
    <automated>python -m pytest tests/test_agenda.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Dezesseis threads competindo por `entrar` produzem exatamente um `"criado"`.
    - `podar` apaga marcadores `presenca_*` e `fechado_*` com mais de 3 dias e preserva os de hoje — teste dedicado por prefixo.
    - `podar` continua sem tocar em arquivo que nao segue nenhum prefixo conhecido.
    - `entrar` devolve os tres estados; ha teste para `"falhou"` (pasta inexistente ou permissao negada, simulado).
    - `presentes` de uma ocorrencia nao devolve slug de outra ocorrencia, mesmo com o nome do evento parecido.
    - Nenhuma chamada existente de `marcar`, `cancelar`, `cancelados` ou `enviados` mudou de assinatura.
  </acceptance_criteria>
  <done>A lista de presenca tem casa em disco, e a poda de 3 dias a alcanca — provado por teste, nao por leitura.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: `presenca.py` — a decisao e as duas redacoes</name>
  <files>l2scanner/presenca.py, tests/test_presenca.py</files>
  <read_first>
    - `l2scanner/loot.py` linhas 802-838 (`responder_designacao`) e 840-885 (`responder_cancelamento`) — o padrao de "funcao pura que recebe registro, eventos e agora, e devolve o TEXTO", incluindo a assimetria deliberada entre gravar e apagar.
    - `l2scanner/agenda.py` linhas 123-140 (`ocorrencias_do_dia`), 190-210 (`proxima_ocorrencia`), 371-378 (`chave_da_ocorrencia`) e o `TOLERANCIA_MINUTOS` da linha 43.
    - `l2scanner/loot.py` linhas 80-105 (`apelido`, `exibir`).
    - `tests/test_loot.py` linhas 30-90 — o helper `em(hora, minuto)` e a convencao de fixar a data.
  </read_first>
  <behavior>
    - `.join` as 18:15, boss as 20:00: privado diz que anotou e cita 20:00; grupo recebe uma confirmacao nomeando o nick e 20:00.
    - `.join` as 09:00, boss as 20:00: aceito do mesmo jeito, e a resposta cita 20:00 (D-07) — quem lembrou cedo nao e punido.
    - `.join` repetido: privado diz que a pessoa ja esta na lista, `grupo is None` (D-08).
    - `.join` com o disco falhando: privado diz que nao gravou e pede para tentar de novo, `grupo is None` — nunca anunciar entrada nao gravada.
    - `.join` sem nick (o remetente e dono mas nao esta em `[[membro]]`): privado explica que nao sabe que nick por na lista e aponta o `config.toml`; `grupo is None`.
    - `.join` sem nenhum evento com `chamar_minutos_antes > 0` na agenda: privado explica isso; `grupo is None`.
    - `.leave` de quem estava: privado confirma a saida citando o horario; grupo recebe a saida com o nick.
    - `.leave` de quem nunca joinou: privado diz isso; `grupo is None`.
    - `.leave` as 20:01 com o boss das 20:00 recem-fechado e a pessoa na lista dele: recusa citando o horario do boss que ja comecou; `grupo is None` (D-14).
    - `.leave` as 20:01 com o boss das 20:00 recem-fechado e a pessoa NAO na lista dele: opera normalmente sobre a proxima ocorrencia (22:00).
    - Todo texto sai sem acento e sem quebra de linha.
  </behavior>
  <action>
Criar `l2scanner/presenca.py`. Docstring de topo no tom do projeto, dizendo em
tres pontos: (a) por que a lista mora no `.agenda/` e nao numa pasta propria —
ela morre quando o boss passa, ao contrario da estatistica de loot; (b) por
que este modulo NAO importa `comandos` nem `sessao`, e por que `loot` nunca
importara este — a direcao e `presenca -> loot -> agenda` e um ciclo mataria
os dois; (c) que o tempo entra por parametro, sem `datetime.now()`, que e o
que permite testar o fechamento atrasado em milissegundos.

Conteudo:

1. `RespostaDePresenca`, `@dataclass(frozen=True)` com `privado: str` e
   `grupo: str | None`. Docstring explicando que ele existe porque hoje
   `__main__.py` despacha o MESMO texto nos dois destinos e D-09 pede
   redacoes diferentes: no privado a pessoa precisa saber que CHEGOU (sem o
   eco, um `.join` recusado por autorizacao e um que funcionou sao
   indistinguiveis para quem digitou); no grupo o que importa e o NICK e o
   horario. `grupo is None` e o jeito estruturado de dizer "isto nao merece
   mensagem no grupo" — nao uma string vazia, que o despachante enviaria.

2. `ocorrencia_da_chamada(agora, eventos)`: `proxima_ocorrencia` sobre os
   eventos com `chamar_minutos_antes > 0`. Mecanismo generico (D-03).

3. `ocorrencia_recem_fechada(agora, eventos)`: a ocorrencia com chamada cujo
   alvo satisfaz `alvo <= agora < alvo + TOLERANCIA_MINUTOS`, varrendo ontem e
   hoje pelo mesmo motivo que `silencio_ativo` varre — a virada da meia-noite.
   Reusar `TOLERANCIA_MINUTOS` da agenda em vez de criar constante nova: e a
   mesma propriedade do laco.

4. `responder_join(registro, eventos, agora, nick)`: sem ocorrencia, resposta
   so no privado explicando. Com ocorrencia, `chave_da_ocorrencia(nome, alvo)`,
   `registro.entrar(chave, apelido(nick))`, e um ramo por estado do tri-estado.
   A resposta SEMPRE cita o horario do alvo (D-07). O nome exibido sai de
   `exibir(nick)` sobre o nick CONFIGURADO que chegou por parametro, nunca de
   um slug relido do disco (D-10).

5. `responder_leave(registro, eventos, agora, nick)`: PRIMEIRO consulta
   `ocorrencia_recem_fechada`; se houver uma e o slug estiver em
   `presentes` dela, recusa citando o horario (D-14) — reabrir historico e a
   categoria de bug que o `.corrigir` do `loot.py` ja documenta como cara.
   Senao, opera sobre `ocorrencia_da_chamada` com `registro.sair`.

6. `nomes_dos_membros(membros) -> dict[str, str]`: slug -> nick como
   configurado, para o plano 10-04 exibir a lista fechada com a grafia do
   `config.toml` em vez da caixa do slug. Recebe qualquer iteravel de objetos
   com `.nick`, para nao precisar importar `comandos`.

Criar `tests/test_presenca.py` seguindo a convencao observada: uma classe por
comportamento, sem `unittest`, `tmp_path` para tudo que toca disco, helper
local `def em(hora, minuto)` fixando a data. Os textos sao afirmados por
SUBSTRING relevante (o nick, o horario), nunca por igualdade da frase inteira
— a redacao muda e a estrutura nao.
  </action>
  <verify>
    <automated>python -m pytest tests/test_presenca.py -q</automated>
  </verify>
  <acceptance_criteria>
    - **Direcao de importacao, afirmada por AST e nunca por grep de texto:** um teste faz `ast.parse` de `l2scanner/presenca.py` e coleta os nomes de modulo dos nos `Import`/`ImportFrom`; o conjunto nao contem `comandos`, `sessao` nem `__main__`. O mesmo teste faz `ast.parse` de `l2scanner/loot.py` e afirma que `presenca` nao esta la. Grep textual nao serve: as docstrings destes dois arquivos citam esses nomes de proposito, para explicar a restricao.
    - **Sem relogio proprio, afirmado por AST e PARAMETRIZADO sobre os TRES modulos** — `l2scanner/agenda.py`, `l2scanner/loot.py` e `l2scanner/presenca.py`: nenhuma chamada de atributo `now` sobre `datetime` aparece na arvore de nenhum dos tres. A disciplina travada no CONTEXT ("o tempo entra por parametro em tudo") sempre valeu para os tres, e os tres ganham codigo novo nesta fase — cobrir so o arquivo novo deixaria a proibicao como politica em vez de estrutura. Uma lista no topo do teste, um `for` sobre ela; a mensagem de falha NOMEIA o modulo. Grep textual nao serve: as docstrings de `agenda.py` e `loot.py` ja citam esse nome ao explicar a disciplina.
    - **Mecanismo generico (D-03), afirmado por comportamento:** um evento com nome inventado e `chamar_minutos_antes > 0` e escolhido por `ocorrencia_da_chamada` normalmente.
    - Toda resposta que nao gravou nada em disco tem `grupo is None`.
    - `responder_join` com o disco falhando devolve `grupo is None` e um privado que pede para tentar de novo.
    - O horario do alvo aparece em TODA resposta de `.join` e de `.leave` que tem alvo (D-07).
    - Nenhum texto produzido tem `\n`, e nenhum tem caractere acentuado.
  </acceptance_criteria>
  <reversibility rating="reversible">
    O formato do nome do arquivo (`presenca_<chave>_<slug>`) e duravel, mas a
    pasta e podada em 3 dias — mudar depois custa, no maximo, tres dias de
    marcadores orfaos, que a poda da Tarefa 1 limpa sozinha.
  </reversibility>
  <done>`.join` e `.leave` tem decisao e texto, com duas redacoes, testados sem rede e sem jogo.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — parte do plano 10-03

**`l2scanner/agenda.py`**
| Simbolo | Tipo |
|---|---|
| `PREFIXO_PRESENCA` | constante, `"presenca_"` |
| `PREFIXO_FECHADO` | constante, `"fechado_"` |
| `_PREFIXOS_CONHECIDOS` | tupla usada pela poda |
| `RegistroEmDisco.entrar(chave, slug)` | metodo -> `"criado" \| "ja_existia" \| "falhou"` |
| `RegistroEmDisco.sair(chave, slug)` | metodo -> `bool` |
| `RegistroEmDisco.presentes(chave)` | metodo -> `frozenset[str]` |
| `RegistroEmDisco.fechar(chave)` | metodo -> `bool` |
| `RegistroEmDisco.podar` | comportamento novo: retira qualquer prefixo conhecido |

**Nomes de arquivo novos em `.agenda/`**
| Forma | Significado |
|---|---|
| `presenca_<chave-da-ocorrencia>_<slug>` | um `.join` confirmado |
| `fechado_<chave-da-ocorrencia>` | a lista daquela ocorrencia ja foi anunciada |

**`l2scanner/presenca.py` (ARQUIVO NOVO)**
| Simbolo | Tipo |
|---|---|
| `RespostaDePresenca` | dataclass frozen (`privado: str`, `grupo: str \| None`) |
| `ocorrencia_da_chamada(agora, eventos)` | funcao -> `tuple[str, datetime] \| None` |
| `ocorrencia_recem_fechada(agora, eventos)` | funcao -> `tuple[str, datetime] \| None` |
| `responder_join(registro, eventos, agora, nick)` | funcao -> `RespostaDePresenca` |
| `responder_leave(registro, eventos, agora, nick)` | funcao -> `RespostaDePresenca` |
| `nomes_dos_membros(membros)` | funcao -> `dict[str, str]` (slug -> nick configurado) |

**`tests/test_presenca.py` (ARQUIVO NOVO)**

Os ramos `Comando.JOIN`/`Comando.LEAVE` em `atender_comandos` e a
generalizacao do bloco de despacho sao do plano **10-03b**.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.agenda/` compartilhado pelas DUAS instancias do usuario | Duas escritas concorrentes no mesmo par (ocorrencia, nick) |
| `atender_comandos` -> `despachante` | Onde o texto do bot vira mensagem no grupo de 4-8 pessoas |
| `MensagemDeComando.nick` -> nome de arquivo | Texto de config vira caminho em disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | marcadores `presenca_*` em `.agenda/` | low | accept | Arquivo vazio, sem estatistica derivada, podado em 3 dias. Quem alcanca a pasta ja alcanca a maquina do usuario |
| T-10-11 | Denial of Service | `.join` repetido em laco enchendo o grupo | medium | mitigate | A TRAVA 4 (`comando_<id>`) ja impede reobediencia da MESMA mensagem; o tri-estado `ja_existia` impede que mensagens novas repitam no grupo (D-08). Criterio de aceite conta os despachos |
| T-10-12 | Tampering | travessia de caminho pelo nick indo para nome de arquivo | medium | mitigate | O slug vem de `loot.apelido`, que reduz a `[a-z0-9-]`, e o nick ja passou por `NICK_VALIDO` na leitura do `[[membro]]` (plano 10-01). Duas barreiras independentes; teste com nick contendo separador de caminho |
| T-10-13 | Repudiation | entrada anunciada no grupo sem ter sido gravada | medium | mitigate | O tri-estado nunca colapsa `"falhou"` em sucesso; `grupo is None` em toda resposta que nao gravou. Criterio de aceite dedicado |
| T-10-14 | Information Disclosure | marcadores de presenca acumulando indefinidamente | low | mitigate | A poda multi-prefixo da Tarefa 1, com teste por prefixo (D-11) |
| T-10-SC | Tampering | instalacao de pacote | high | mitigate | Zero dependencia nova; `presenca.py` usa so stdlib e modulos do pacote |
</threat_model>

<verification>
1. `python -m pytest tests/ -q` — suite inteira passa.
2. `python -c "import l2scanner.presenca, l2scanner.loot, l2scanner.__main__"` — o pacote importa; prova de que a direcao `presenca -> loot -> agenda` nao fechou ciclo.
3. `python -m pytest tests/test_presenca.py -q` — o arquivo novo roda sozinho.
4. `git diff --stat` nao toca arquivo de dependencia e nao toca `l2scanner/__main__.py` (o despacho e do plano 10-03b).
</verification>

<success_criteria>
- PRES-07, PRES-10, PRES-11, PRES-13 e PRES-15 demonstrados por teste automatico, com o jogo fechado e sem rede; PRES-05, PRES-06, PRES-08 e PRES-09 tem aqui a DECISAO e o TEXTO, e chegam ao despacho no plano 10-03b.
- A corrida entre as duas instancias tem teste com threads, como `test_competicao_de_verdade_com_threads` ja tem para os avisos.
- A poda de 3 dias alcanca o prefixo novo — provado, nao presumido.
</success_criteria>

<output>
Create `.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-03-SUMMARY.md` when done
</output>
