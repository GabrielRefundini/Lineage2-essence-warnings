---
phase: 01-reconhecimento-preciso-e-lista-de-bosses-no-config
workstream: tiat
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_bosses.py
  - l2scanner/bosses.py
  - l2scanner/config.py
  - tests/test_presenca.py
autonomous: true
requirements: [RECO-01, RECO-02, RECO-03, RECO-04, RECO-05, VIGI-01, VIGI-02, VIGI-03, VIGI-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A frase degradada como o OCR degrada (`T1a7 Nor7h [Lv. 8O] has spawned!`) produz o MESMO alerta e o MESMO boss que a frase limpa (critério 3 / RECO-04)."
    - "A precisão contra chat digitado e o reconhecimento do anúncio real são afirmados no MESMO arquivo, um teste por direção (critério 3)."
    - "A detecção pelo ALVO é um caminho próprio e não depende de o chat ter anunciado nada (critério 2 / RECO-03)."
    - "Um `[[boss]]` sem `nome`, com `max` menor que `min`, com horas negativas ou com booleano no lugar de número derruba o arranque nomeando o boss e o campo (critério 6 / VIGI-03)."
    - "Um `nome` de `[[boss]]` com metacaractere de regex não vira curinga (T-01-03)."
    - "O texto lido pelo OCR nunca atravessa para a mensagem do WhatsApp (T-01-04)."
    - "`bosses.py` entra na tupla de módulos do portão AST de relógio e o portão continua verde."
  artifacts:
    - "tests/test_bosses.py :: a matriz completa dos 8 critérios"
    - "tests/test_presenca.py :: bosses.py dentro de TestSemRelogioProprio.MODULOS"
  key_links:
    - "`padrao_do_anuncio` <-> os dois sentidos do critério 3: o positivo degradado e o negativo digitado"
    - "`_boss_de_dict` <-> a mensagem de erro que cita boss e campo, no formato de `_evento_de_dict`"
    - "`bosses.VigiaDeBosses.avaliar(agora)` <-> o portão AST que impede um relógio próprio de nascer ali na Fase 2"
---

<objective>
A matriz de asserções que torna os oito critérios da fase VERDADEIROS, e não só
plausíveis — mais as duas correções de segurança que ela vai encontrar se
faltarem, e a adoção de `bosses.py` pelo portão AST de relógio.

Purpose: o tracer de `01-01` prova que a arquitetura funciona num caminho. Este
plano prova que ela funciona nos caminhos que o usuário vai encontrar em campo:
a frase que o OCR estragou, o alvo sem anúncio, dois bosses no mesmo tick, e o
bloco de config torto às três da manhã.

Output: `tests/test_bosses.py` completo, as correções que a matriz exigir em
`bosses.py` / `config.py`, e `bosses.py` dentro do portão de relógio.
</objective>

<decision_ids>
Ver a tabela D-09..D-14 em `01-01-PLAN.md`. Este plano afirma D-09 (folga na
parte fixa, `!` opcional), D-10 (identidade do config), D-11 (nível ignorado) e
D-12 (o contrato de migração dos 7 testes) por medição, não por leitura.
</decision_ids>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/tiat/ROADMAP.md
@.planning/workstreams/tiat/REQUIREMENTS.md
@.planning/workstreams/tiat/phases/01-reconhecimento-preciso-e-lista-de-bosses-no-config/01-CONTEXT.md
@.planning/workstreams/tiat/phases/01-reconhecimento-preciso-e-lista-de-bosses-no-config/01-01-SUMMARY.md
@.claude/CLAUDE.md

@l2scanner/bosses.py
@tests/test_bosses.py
@l2scanner/config.py
</context>

<parallel_workstreams>
Nenhum arquivo deste plano pertence a `discord` (`ponte_discord.py`,
`ponte_nucleo.py`) nem a `mercado` (`mercado_*.py`, `calibrar_mercado.py`).

`tests/test_presenca.py` é grande e não é de outro workstream, mas a Task 3
altera **uma tupla e a docstring ao lado dela**. Não reformate o arquivo, não
mexa em nenhuma outra classe.
</parallel_workstreams>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A matriz de reconhecimento — os dois sentidos, no mesmo arquivo</name>

  <read_first>
    - `l2scanner/bosses.py` inteiro, como ficou depois de `01-01`: `_FOLGA_DE_OCR`, `_DIGITO_COM_FOLGA`, `padrao_do_anuncio`, `padrao_do_nome`, `VigiaDeBosses.avaliar`.
    - `tests/test_bosses.py` inteiro — os 7 migrados e o contrato na docstring de módulo. A matriz é acrescentada ao lado deles, nunca por cima.
    - `01-01-SUMMARY.md` — a tabela final de `_FOLGA_DE_OCR` e a classe `_DIGITO_COM_FOLGA` como ficaram. É contra elas que os casos degradados são escritos.
    - `.planning/workstreams/tiat/ROADMAP.md`, critérios 1 a 4 da Fase 1 — a redação de cada asserção sai dali.
    - `.planning/workstreams/tiat/phases/.../01-CONTEXT.md`, bloco `<specifics>` — a frase real medida do print do usuário é `Tiat North [Lv. 60] has spawned!`, com nível 60 e não 80.
  </read_first>

  <files>tests/test_bosses.py, l2scanner/bosses.py</files>

  <behavior>
    Cada linha abaixo é um teste, e cada teste nomeia o critério do ROADMAP que
    ele torna verdadeiro. Os dois sentidos ficam no MESMO arquivo, e é
    deliberado: apertar o padrão para matar um falso positivo e afrouxá-lo para
    salvar um falso negativo são a mesma alavanca, e quem a puxa tem que ver os
    dois lados quebrarem.

    SENTIDO POSITIVO (o anúncio real tem que passar):
    - `Tiat North [Lv. 60] has spawned!` -> um aviso, boss `Tiat North`.
    - `Tiat South [Lv. 60] has spawned!` -> um aviso, boss `Tiat South`.
    - `T1a7 Nor7h [Lv. 8O] has spawned!` -> um aviso, boss `Tiat North`. O nível traz a LETRA O no lugar do zero; é o caso literal do critério 3.
    - A mesma frase SEM o `!` final -> um aviso. Pontuação é o que o OCR mais perde (D-09).
    - A mesma frase sem os colchetes em volta do nível -> um aviso.
    - A mesma frase com o nível trocado (`[Lv. 80]`, `[Lv. 1]`) -> um aviso, o MESMO boss. O número é casado e descartado (D-11).
    - A frase precedida de lixo, como o OCR de um ícone produziria -> um aviso.

    SENTIDO NEGATIVO (o que não é anúncio do servidor não pode passar):
    - `Fulano: tiat ja nasceu?` -> zero avisos. É o critério 1 e a razão da fase.
    - `alguem viu o Tiat North hoje` -> zero avisos. O nome sozinho não basta (RECO-01).
    - `Tiat North [Lv. 60]` sem `has spawned` -> zero avisos.
    - `[Lv. 60] has spawned!` sem nome de boss configurado -> zero avisos.
    - Um anúncio de um boss que NÃO está no `config.toml` -> zero avisos.

    ALVO (RECO-03, critério 2):
    - Chat mudo e alvo lendo `Tiat South` -> um aviso, boss `Tiat South`, origem ALVO.
    - Alvo lendo o nome degradado (`T1a7 Sou7h`) -> um aviso, mesmo boss.
    - Alvo lendo `Tiat` sozinho, com `Tiat North` e `Tiat South` na lista -> zero avisos. Não dá para escolher entre dois, e escolher errado manda a party para o lugar errado.

    REARME POR BOSS (RECO-05, critério 4):
    - Chat anunciando `Tiat North` e alvo mostrando `Tiat North` no mesmo tick -> UM aviso, origem `CHAT_E_ALVO`.
    - Chat anunciando `Tiat North` e alvo mostrando `Tiat South` no mesmo tick -> DOIS avisos, um por boss.
    - `Tiat North` persistindo no chat por vários ticks enquanto `Tiat South` é alvejado no terceiro -> o aviso de `Tiat South` SAI. É o caso que o flag global engolia e a razão inteira da mudança de forma.
    - Cada boss rearma pelo seu próprio contador: `Tiat North` sumindo do chat por duas leituras rearma só ele.
  </behavior>

  <action>
Escreva os testes acima em `tests/test_bosses.py`, agrupados em classes por
critério do ROADMAP, e com o número do critério e o ID do requisito na docstring
de cada classe. A suíte deste projeto é lida como documentação; um teste que não
diz qual afirmação do roadmap ele sustenta obriga o próximo leitor a
engenharia reversa.

Use o helper `vigia(...)` que já existe no arquivo, estendido para receber o
roster. Onde a asserção for sobre a MENSAGEM (e não sobre a estrutura), afirme
que o texto COMEÇA com o nome do boss (D-14) — não case a frase inteira, porque
melhorar a redação não pode quebrar teste.

Acrescente o teste de T-01-04, que é o mais valioso da lista e não está em
nenhum critério do ROADMAP: alimente o recorte do chat com o anúncio CERCADO de
texto arbitrário — inclusive algo que pareça um comando ou um endereço — e
afirme que a mensagem despachada contém o nome configurado e NADA do texto lido.
O nome vem sempre do `config.toml`; o texto do OCR é predicado booleano e é
descartado. Sem este teste, a regra é política; com ele, é estrutura.

Acrescente o teste de linha, que é a mitigação de T-01-02: um recorte cujo OCR
devolve DUAS linhas, a primeira terminando com um nome de boss e a segunda
começando com o resto da frase fixa, produz ZERO avisos. O casamento é linha a
linha justamente para que o padrão não emende texto de dois autores.

**Se algum caso falhar, a correção vai em `bosses.py`, e a asserção não se
mexe.** A matriz é a régua. Os pontos onde ela mais provavelmente vai apontar
defeito, e o que fazer em cada um:

- O caso do nível com letra O falha se `_DIGITO_COM_FOLGA` não cobrir a letra. Amplie a classe, nunca troque a asserção por `[Lv. 80]`.
- O caso do alvo lendo `Tiat` sozinho falha (produzindo DOIS avisos) se o padrão do nome for tratado como subcadeia sem desempate. A correção é recusar quando mais de um boss configurado casa o MESMO trecho do texto do alvo: ambíguo não é sinal, e mandar a party para o lugar errado é pior do que não mandar. Escreva a razão no código.
- O caso de duas linhas falha se o casamento for sobre o blob. A correção é `splitlines()`.
- O caso do texto arbitrário falha se o nome na mensagem vier do trecho casado em vez do config. A correção é interpolar o `Boss.nome`.

Ao fim, verifique que a docstring de módulo com o contrato de migração de D-12
continua no topo do arquivo e que os 7 nomes migrados continuam lá, intactos.
Este plano ACRESCENTA; ele não reescreve o que `01-01` entregou.
  </action>

  <verify>
    <automated>python -m pytest tests/test_bosses.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Com o texto `Fulano: tiat ja nasceu?` entrando pelo OCR do chat, nenhum aviso sai; com `Tiat North [Lv. 60] has spawned!` no mesmo lugar, sai um aviso e a mensagem começa com `Tiat North`; trocando `North` por `South`, a mensagem começa com `Tiat South`.
    - `T1a7 Nor7h [Lv. 8O] has spawned!` produz o mesmo aviso e o mesmo boss que a frase limpa — o nível com letra O no lugar do zero não derruba o reconhecimento.
    - A mesma frase sem o `!` final e sem os colchetes continua produzindo o aviso.
    - Com o chat mudo e o alvo lendo `Tiat South`, sai um aviso identificando `Tiat South` pelo alvo, sem o chat ter anunciado nada.
    - Chat e alvo mostrando o mesmo boss no mesmo tick produzem UM aviso; mostrando bosses diferentes produzem DOIS, um por boss.
    - Um `Tiat South` alvejado enquanto o anúncio de `Tiat North` ainda persiste no chat NÃO é engolido.
    - O recorte cujo OCR devolve duas linhas que só juntas formariam a frase produz zero avisos.
    - A mensagem despachada contém o nome do `[[boss]]` e nenhum trecho do texto lido pelo OCR.
    - Os 7 testes migrados de `01-01` continuam presentes, com os mesmos nomes e as mesmas asserções de debounce.
    - Nenhuma asserção desta matriz foi enfraquecida para fazer o código passar; toda correção foi em `l2scanner/bosses.py`.
  </acceptance_criteria>

  <done>
    Os critérios 1, 2, 3 e 4 do ROADMAP são afirmados por teste, nos dois
    sentidos, no mesmo arquivo — e o arquivo diz qual critério cada classe
    sustenta.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A matriz de configuração — o bloco torto derruba o arranque nomeando o campo</name>

  <read_first>
    - `l2scanner/config.py` — `ler_bosses` e `_boss_de_dict` como ficaram depois de `01-01`.
    - `l2scanner/config.py` linhas 177-268 — `_evento_de_dict`, e em especial o comentário sobre `isinstance(True, int)` em `chamar_minutos_antes`. É o precedente escrito da rejeição explícita de booleano, e a mensagem de erro segue aquela forma.
    - `tests/test_agenda.py` linhas 490-600 — `TestAgendaRealDoUsuario` e o `test_as_duracoes_de_silencio_estao_no_esquema_para_a_fase_7`. Os dois moldes: afirmar sobre o `config.toml` DO REPOSITÓRIO, e afirmar que um campo lido-e-ignorado está no esquema para a fase seguinte.
    - `l2scanner/config.py` linhas 402-430 — `_recusar_nicks_repetidos`, o molde da recusa de duplicata.
  </read_first>

  <files>tests/test_bosses.py, l2scanner/config.py, l2scanner/bosses.py</files>

  <behavior>
    VIGI-03 (critério 6) — cada caso levanta, e a mensagem cita o boss e o campo:
    - bloco sem `nome`; bloco com `nome` vazio ou só espaços;
    - `respawn_horas_min` ausente; `respawn_horas_max` ausente;
    - `respawn_horas_min = true` (booleano onde se espera número);
    - `respawn_horas_min = "seis"` (texto onde se espera número);
    - `respawn_horas_min = -1`; `respawn_horas_min = 0`;
    - `respawn_horas_max = 4` com `respawn_horas_min = 6` — a mensagem cita os DOIS valores;
    - dois blocos com o mesmo `nome`, inclusive diferindo só na caixa.

    Quando o `nome` existe, a mensagem o cita; quando não existe, ela cita a
    posição do bloco. Nenhum caso sobe vigiando errado em silêncio.

    VIGI-04 (critério 7):
    - `config.toml` sem nenhum `[[boss]]` -> `ler_bosses` devolve lista vazia, sem levantar;
    - arquivo inexistente -> lista vazia, sem levantar;
    - TOML malformado -> levanta, citando o arquivo.

    VIGI-01 / VIGI-02 (critério 5), sobre o `config.toml` DO REPOSITÓRIO:
    - os dois blocos do Tiat são lidos, com 6 e 8;
    - o cabeçalho do arquivo documenta os três campos do bloco;
    - um boss INVENTADO, acrescentado a um `config.toml` temporário, passa a ser vigiado sem que nenhum arquivo `.py` mude — este é o teste que prova VIGI-01 de verdade, e ele monta o vigia a partir do config lido, não de um roster escrito à mão.

    SEGURANÇA (T-01-03):
    - `nome = "Tiat.*"` NÃO casa `Tiatzzz [Lv. 1] has spawned`;
    - `nome = "Tiat("` não levanta erro de compilação de regex;
    - `nome = "Tiat[a-z]"` casa literalmente e não como classe.
  </behavior>

  <action>
Escreva a matriz acima em `tests/test_bosses.py`, em classes separadas por
requisito, escrevendo `config.toml` temporários com `tmp_path` no molde de
`TestAgendaRealDoUsuario` e do helper `escrever` que aquele arquivo já usa.

Afirme sobre o CONTEÚDO da mensagem de erro, não só sobre o tipo da exceção:
cada teste checa que o nome do boss (quando existe) e o nome do campo aparecem
no texto. É o que o critério 6 pede — "uma mensagem que cita o boss e o campo" —
e é o que separa uma recusa útil de um `raise` genérico.

Para VIGI-01, o teste do boss inventado é o mais importante da tarefa e precisa
fechar o circuito inteiro: escreva um `config.toml` temporário com um mob que
não existe no jogo, leia com `ler_bosses`, monte um `VigiaDeBosses` com o
resultado, e afirme que o anúncio DAQUELE mob dispara. Se ele passar com um
roster escrito à mão no teste, ele não prova VIGI-01 — prova só o vigia.

Para VIGI-02, espelhe literalmente o teste precedente da agenda: um teste
chamado no mesmo espírito de `test_as_duracoes_de_silencio_estao_no_esquema_para_a_fase_7`,
afirmando que os dois Tiat do repositório têm 6 e 8, com docstring dizendo que
os campos são lidos e IGNORADOS nesta fase e moram no arquivo desde já para o
usuário não editar o mesmo config duas vezes quando a Fase 2 chegar. Nomeie o
precedente na docstring: é o mesmo movimento, e citá-lo é o que faz a fase
seguinte encontrar o padrão em vez de reinventá-lo.

Para o cabeçalho, espelhe `test_o_cabecalho_do_arquivo_documenta_o_campo`: leia
o texto do `config.toml` do repositório e afirme que a seção nova documenta os
três campos. Um campo que existe e não está documentado é mentira por omissão —
quem lê o arquivo conclui que a regra de respawn não é editável.

Para os casos de segurança, o teste de `nome = "Tiat.*"` é o que fecha T-01-03.
Ele não é hipotético: um nome de mob com pontuação é plausível, e um padrão
montado por concatenação crua transformaria o vigia num curinga que dispara com
qualquer linha do chat — o oposto exato do que a fase inteira existe para fazer.

**Se algum caso falhar, a correção vai em `config.py` ou em `bosses.py`.** Não
enfraqueça a matriz. Em particular: se a recusa de duplicata não existir,
escreva-a em `ler_bosses` no molde de `_recusar_nicks_repetidos`, comparando os
nomes sem diferenciar maiúsculas, porque o padrão de OCR é `IGNORECASE` e dois
nomes que só diferem na caixa são indistinguíveis para o vigia — e na Fase 2
dividiriam a mesma âncora em disco.
  </action>

  <verify>
    <automated>python -m pytest tests/test_bosses.py tests/test_agenda.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Cada um dos nove casos de bloco torto levanta, e o texto da exceção contém o nome do boss (quando ele existe) e o nome do campo defeituoso.
    - O caso de `max` menor que `min` cita os dois valores na mensagem.
    - Dois blocos com o mesmo nome, mesmo diferindo só na caixa, são recusados.
    - `config.toml` sem seção de boss e caminho inexistente devolvem lista vazia sem levantar; TOML malformado levanta citando o arquivo.
    - Um mob inventado, escrito num `config.toml` temporário e lido por `ler_bosses`, passa a ser vigiado — sem nenhuma alteração em arquivo `.py`.
    - O `config.toml` do repositório produz os dois Tiat com `respawn_horas_min == 6` e `respawn_horas_max == 8`, e o cabeçalho da seção documenta os três campos.
    - `nome = "Tiat.*"` não casa `Tiatzzz [Lv. 1] has spawned`; `nome = "Tiat("` não levanta erro de compilação.
    - `tests/test_agenda.py` continua verde — a seção nova do `config.toml` está abaixo do primeiro `[[evento]]` e não moveu o corte do guarda de cabeçalho.
  </acceptance_criteria>

  <done>
    Os critérios 5, 6 e 7 do ROADMAP são afirmados por teste, e o vetor de
    metacaractere no nome do boss está fechado com prova.
  </done>
</task>

<task type="auto">
  <name>Task 3: O portão AST de relógio adota `bosses.py`</name>

  <read_first>
    - `tests/test_presenca.py` linhas 1173-1216 — `TestSemRelogioProprio` inteira: a docstring que explica a disciplina, a tupla `MODULOS`, o teste parametrizado, e o teste de prova-vazia.
    - `l2scanner/bosses.py` — confirme que `avaliar` recebe `agora` por parâmetro e que não há nenhuma chamada de relógio no módulo.
    - `.planning/workstreams/tiat/ROADMAP.md`, critério 7 da Fase 2 — o requisito que fala de MODULOS é de lá; esta tarefa antecipa a adoção para o módulo que a Fase 2 vai usar como âncora.
  </read_first>

  <files>tests/test_presenca.py</files>

  <action>
Acrescente `"bosses.py"` à tupla `MODULOS` de `TestSemRelogioProprio` e estenda a
docstring da classe com o motivo — em duas ou três linhas, no tom que já está
ali.

O motivo, e ele não é cerimônia: o instante que `VigiaDeBosses.avaliar` recebe
por parâmetro é exatamente o instante que a Fase 2 vai gravar em disco como
âncora do ciclo de respawn. Um `datetime.now()` enfiado em `bosses.py` faria a
âncora registrar o momento em que o código rodou, e não o momento do frame — e
num `--replay`, ou nos dois laços que a Fase 2 precisa costurar, os testes
continuariam verdes passando um `agora` que o código já não usaria. É a mesma
família de defeito que a docstring existente descreve para `agenda.py`, com a
diferença de que aqui o defeito escreve em disco.

Adotar agora, e não na Fase 2: o módulo já cumpre a regra (o `agora` já entra
por parâmetro desde a versão anterior), então a adoção custa uma linha e fecha a
porta ANTES de existir código com lógica de tempo. Adotar depois seria adotar um
módulo que já poderia ter violado a regra, e aí o portão nasce vermelho ou nasce
afrouxado.

Não mexa em mais nada de `tests/test_presenca.py`. O arquivo tem 2090 linhas e
nenhuma outra parte dele é desta fase.
  </action>

  <verify>
    <automated>python -m pytest tests/test_presenca.py -q -k SemRelogioProprio</automated>
  </verify>

  <acceptance_criteria>
    - A tupla de módulos do portão contém quatro entradas e o teste parametrizado roda quatro vezes, todas verdes.
    - Inserir um `datetime.now()` em `l2scanner/bosses.py` faz o portão ficar VERMELHO — verificável fazendo a edição, rodando, e desfazendo.
    - O teste de prova-vazia da classe continua passando, e a docstring diz por que `bosses.py` entrou.
    - `python -m pytest tests/test_presenca.py -q` inteiro continua verde: nenhuma outra parte do arquivo foi tocada.
  </acceptance_criteria>

  <done>
    Nenhum `datetime.now()` pode nascer em `bosses.py` sem alguém ver — e a
    razão está escrita onde a Fase 2 vai ler.
  </done>

  <reversibility rating="reversible">
    Uma entrada numa tupla de teste. Reverter é apagá-la.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorte do chat -> OCR -> `padrao_do_anuncio` | Entrada não-confiável: qualquer jogador escreve ali. Este plano é onde a fronteira ganha prova, e não só implementação. |
| `config.toml` (`nome` do `[[boss]]`) -> `re.compile` | Texto escrito à mão vira fonte de expressão regular. |
| `AvisoDeBoss.texto` -> WhatsApp | Texto que sai da máquina para terceiros. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `nome` do `[[boss]]` com metacaractere de regex | high | mitigate | Task 2 escreve os três casos que fecham o vetor: `Tiat.*` não vira curinga, `Tiat(` não levanta erro de compilação, `Tiat[a-z]` casa literalmente. Sem estes testes a mitigação de `01-01` é implementação sem prova, e a primeira "simplificação" para concatenação a apaga em silêncio. |
| T-03-02 | Information disclosure | texto do OCR atravessando para o grupo | medium | mitigate | Task 1 escreve o teste que alimenta o recorte com o anúncio cercado de texto arbitrário e afirma que a mensagem contém o nome do config e nada do texto lido. |
| T-03-03 | Tampering | costura de duas linhas do chat num único casamento | low | mitigate | Task 1 escreve o teste de duas linhas: nome no fim de uma, resto da frase no começo da outra, zero avisos. |
| T-03-04 | Denial of Service (falso negativo silencioso) | padrão apertado por uma correção futura | high | mitigate | Os dois sentidos moram no MESMO arquivo, um teste por direção, com o critério do ROADMAP nomeado na docstring de cada classe. Quem apertar o padrão para matar um falso positivo vê o falso negativo quebrar no mesmo `pytest`. É a mitigação estrutural de R-02. |
| T-03-05 | Spoofing | jogador digitando a frase exata do anúncio | medium | accept | Continua aceito e continua escrito. A matriz NÃO tenta fechar este vetor, e é deliberado: a única defesa seria ancorar o padrão em algo que o jogador não controla (a cor de sistema da linha), e o CONTEXT difere isso até um falso positivo real aparecer em campo. Registre no SUMMARY que a Fase 2 herda este risco com severidade MAIOR, porque lá o sinal vira âncora durável. |
| T-03-06 | Repudiation (portão que para de guardar) | `TestSemRelogioProprio` | medium | mitigate | O portão já tem teste de prova-vazia; a Task 3 confirma que ele continua passando depois da adoção, e o critério de aceite exige a verificação manual de que inserir um relógio próprio em `bosses.py` deixa o portão vermelho. |
| T-03-SC | Tampering | instalação de pacotes (npm/pip/cargo) | n/a | accept | Nenhuma dependência nova. Nenhuma tarefa deste plano executa gerenciador de pacotes. |

**Nível ASVS: 1. `security_block_on: high`** — T-03-01 e T-03-04 são `high` e os
dois viram teste nesta fase, com o caso concreto nomeado.
</threat_model>

<verification>
- `python -m pytest -q` — a suíte inteira.
- `python -m pytest tests/test_bosses.py -q` — a matriz completa.
- `python -m pytest tests/test_presenca.py -q` — o portão de relógio com quatro módulos.
- Verificação manual de que os guardas guardam: inserir um `datetime.now()` em `l2scanner/bosses.py`, rodar o portão, confirmar vermelho, desfazer.
</verification>

<success_criteria>
- Critérios 1 a 7 do ROADMAP afirmados por teste, com o número do critério na docstring da classe que o sustenta.
- Os dois sentidos do critério 3 no mesmo arquivo — a precisão contra chat digitado não custou o reconhecimento do anúncio real, e a prova disso é local.
- `bosses.py` dentro do portão AST de relógio, verde.
- Nenhuma asserção enfraquecida: toda falha da matriz virou correção em `bosses.py` ou `config.py`.
</success_criteria>

## Artifacts this phase produces

**Nenhum módulo novo.** Este plano acrescenta asserções e as correções que elas exigirem.

**`tests/test_bosses.py`** ganha classes por critério do ROADMAP: os dois sentidos de RECO-01/RECO-04 (positivo degradado e negativo digitado), RECO-02 (North vs South), RECO-03 (caminho do alvo), RECO-05 (rearme por boss, as duas direções), VIGI-01 (boss inventado lido do config), VIGI-02 (as horas no esquema para a Fase 2), VIGI-03 (nove casos de bloco torto), VIGI-04 (lista vazia e arquivo ausente), mais T-01-02 (casamento linha a linha), T-01-03 (nome com metacaractere) e T-01-04 (o texto do OCR não atravessa para a mensagem).

**`tests/test_presenca.py`:** `"bosses.py"` entra em `TestSemRelogioProprio.MODULOS`, que passa a ter quatro entradas.

**Correções possíveis em `l2scanner/bosses.py` / `l2scanner/config.py`**, se a matriz apontar: ampliação de `_DIGITO_COM_FOLGA`; regra de desempate quando mais de um boss casa o mesmo trecho do texto do alvo; `splitlines()` no casamento do chat; recusa de `nome` duplicado em `ler_bosses` comparado sem diferenciar maiúsculas.

<output>
Criar `.planning/workstreams/tiat/phases/01-reconhecimento-preciso-e-lista-de-bosses-no-config/01-03-SUMMARY.md` ao terminar, registrando:
1. Quais casos da matriz FALHARAM na primeira rodada e qual correção cada um exigiu — é o dado que diz onde o padrão estava frágil e alimenta o plano 01-04.
2. A regra final de desempate quando mais de um boss casa o mesmo trecho do texto do alvo.
3. O risco T-03-05 reafirmado com a nota de que a Fase 2 o herda com severidade maior.
</output>
