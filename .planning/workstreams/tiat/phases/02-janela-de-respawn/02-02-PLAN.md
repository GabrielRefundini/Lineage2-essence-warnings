---
phase: 02-janela-de-respawn
workstream: tiat
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - l2scanner/respawn.py
  - l2scanner/__main__.py
  - tests/test_respawn.py
  - tests/test_janela_no_relogio.py
autonomous: true
requirements: [JANE-02, JANE-05, OPER-02, OPER-03]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uma volta do `laco_da_agenda` (sem jogo, sem calibracao, sem captura, sem rede) com o relogio em `ancora + respawn_horas_min` produz UMA mensagem de que a janela abriu; em `ancora + respawn_horas_max`, UMA mensagem de que o limite passou (criterio 2 / JANE-02 / JANE-05)."
    - "A mesma volta com o relogio tres horas depois do vencimento nao produz mensagem nenhuma: um aviso vencido nao ressuscita ao subir o scanner (criterio 2 / D-22)."
    - "A mesma volta em `--dry-run` mostra a mensagem no console e NAO grava nada na `.agenda/` compartilhada (criterio 5 / JANE-06 / D-21)."
    - "Com `[[boss]]` no config e NENHUM `[[evento]]`, o `--so-agenda` SOBE em vez de recusar: quem so vigia boss e justamente quem mais precisa do modo do relogio (JANE-05)."
    - "No arranque, os dois lacos listam cada boss vigiado e, para os que tem ancora, quando a janela abre e quando o limite passa; para os que nao tem, dizem que ainda nao viram nascimento nenhum daquele boss (criterio 6 / OPER-02)."
    - "A linha de quem nao tem ancora NAO inventa previsao nenhuma (criterio 6 / OPER-02)."
    - "Os DOIS lacos chegam aos avisos de janela pela MESMA funcao (`respawn.anunciar_janelas`); nenhum dos dois calcula vencimento nem escreve texto por conta propria, e um portao AST impede que voltem a divergir (criterio 2 / JANE-05)."
    - "Toda a fase continua demonstravel com o jogo fechado e sem rede (criterio 7 / OPER-03)."
  artifacts:
    - "l2scanner/respawn.py :: linhas_de_previsao"
    - "l2scanner/__main__.py :: _avisar_janelas_de_respawn"
    - "l2scanner/__main__.py :: _anunciar_previsao_de_janelas"
    - tests/test_janela_no_relogio.py
  key_links:
    - "`__main__.laco_da_agenda` -> `ler_bosses()` -> `_avisar_janelas_de_respawn` -> `respawn.anunciar_janelas` -> `despachante.despachar(Categoria.SEMPRE)`"
    - "`__main__.laco_principal` e `__main__.laco_da_agenda` -> `_anunciar_previsao_de_janelas` -> `respawn.ancoras_mais_recentes` -> `respawn.linhas_de_previsao` -> console"
    - "`sessao._processar_janelas` e `__main__._avisar_janelas_de_respawn` -> a MESMA `respawn.anunciar_janelas` (portao AST contra a divergencia)"
---

<objective>
A janela passa a valer com o jogo FECHADO, e o console passa a dizer o que ela e.

Purpose: JANE-05 exige que o aviso funcione nos DOIS lacos, e o `--so-agenda` e o modo
de quem esta longe do jogo — que e exatamente quem precisa do lembrete. O plano 02-01
provou a fatia inteira dentro do laco principal; este plano leva o mesmo caminho ao
segundo sitio de fiacao, pela mesma funcao, e poe o portao que impede os dois de
divergirem depois. O ROADMAP registra por escrito que um conserto aplicado de um lado
so faz os dois anunciarem coisas diferentes sobre o mesmo boss, e `presenca` ja pagou
esse preco uma vez (WR-08).

Output: `linhas_de_previsao` em `respawn.py` (pura); `_avisar_janelas_de_respawn` e
`_anunciar_previsao_de_janelas` em `__main__.py`, no molde literal de
`_fechar_listas_de_presenca` e de `_anunciar_proximo`; e `tests/test_janela_no_relogio.py`,
que exercita o laco de verdade com o relogio parado e afirma que os dois sitios passam
pela mesma implementacao.
</objective>

<decision_ids>
Os mesmos D-15..D-23 atribuidos em `02-01-PLAN.md`, mais as resolucoes de discricao
R-1..R-4 registradas la. Este plano nao cria decisao nova.

| ID | O que este plano faz com ela |
|----|-----------------------------|
| D-15 | Os dois sinais ja ancoram (02-01); aqui as ancoras das duas origens produzem aviso e linha de console pelo mesmo caminho. |
| D-16 | A citacao da origem aparece tambem na linha de console de previsao — quem le o console julga com a mesma informacao de quem le o grupo. |
| D-17 | Dois avisos por ciclo, tambem no `--so-agenda`. Nenhum terceiro aviso, nenhum lembrete de antecedencia. |
| D-21 | O `marcar` continua sendo a decisao, e continua dentro de `anunciar_janelas` — o shell novo nao ganha checagem propria. |
| D-22 | A tolerancia de 5 minutos afirmada no LACO, e nao so na funcao pura. |
| D-23 | `Categoria.SEMPRE` no despacho do `--so-agenda`, como ja e no laco principal. |
</decision_ids>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/tiat/ROADMAP.md
@.planning/workstreams/tiat/REQUIREMENTS.md
@.planning/workstreams/tiat/phases/02-janela-de-respawn/02-CONTEXT.md
@.planning/workstreams/tiat/phases/02-janela-de-respawn/02-01-PLAN.md
@.planning/workstreams/tiat/phases/02-janela-de-respawn/02-01-SUMMARY.md
@.claude/CLAUDE.md

@l2scanner/respawn.py
@l2scanner/presenca.py
</context>

<parallel_workstreams>
RODANDO AO MESMO TEMPO, EM OUTROS WORKSTREAMS. Nenhuma tarefa deste plano pode
tocar, criar ou renomear estes arquivos:

- `l2scanner/ponte_discord.py`, `l2scanner/ponte_nucleo.py` — workstream `discord`
- `l2scanner/mercado_*.py`, `l2scanner/calibrar_mercado.py` — workstream `mercado`
- `tools/medir_*.py`, `tests/test_medir_*.py`, `tests/test_mercado_*.py` — workstream `mercado`

`__main__.py` e deste plano. Conferido: nenhum arquivo das listas acima define funcao
dentro de `__main__.py` nem le `.agenda/`.

**Consequencia operacional:** as falhas de `tests/test_mercado_*.py` nao sao suas.
Atribua por `git log`, liste nominalmente no SUMMARY, nao conserte e nao silencie.
</parallel_workstreams>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O segundo sitio de fiacao — a janela valendo com o jogo fechado</name>

  <read_first>
    - `l2scanner/__main__.py` linhas 1272-1320 — `_fechar_listas_de_presenca`. **E o molde literal desta tarefa**: extraida do corpo do laco de proposito porque o `--so-agenda` esta em 19-20% de cobertura e um fechamento escrito la dentro nasceria sem teste; loga SEMPRE e despacha se houver para onde; a decisao e o texto moram no modulo, e aqui fica so o log e o despacho.
    - `l2scanner/__main__.py` linhas 1322-1440 — `laco_da_agenda` inteiro: a recusa por agenda vazia no comeco, a construcao do `RegistroEmDisco` com `simulando`, a ordem do corpo do `while` e o bloco horario do fim.
    - `l2scanner/presenca.py` linhas 493-555 — `fechar_e_narrar`, e a docstring que explica por que a sequencia mora no modulo e nao nos dois lacos (WR-08). `respawn.anunciar_janelas` e o gemeo dela e ja existe desde 02-01.
    - `l2scanner/respawn.py` — `anunciar_janelas`, sua assinatura e o que ela devolve. **Nao reimplemente nada dela.**
    - `l2scanner/sessao.py` — `_processar_janelas`, escrito em 02-01. O shell desta tarefa e o irmao dele: mesma chamada, saida diferente (log e despachante em vez de `ResultadoDoTick`).
    - `tests/test_agenda.py` linhas 1095-1190 — `TestSimulacaoNoLacoDaAgenda._tick`. **E o instrumento do criterio 2**: uma volta so do `laco_da_agenda`, com relogio parado, `ler_agenda` trocado e `time.sleep` levantando `KeyboardInterrupt`. Esta tarefa reusa a FORMA desse helper, acrescentando a troca de `ler_bosses` e a semeadura da ancora.
  </read_first>

  <files>l2scanner/__main__.py, tests/test_janela_no_relogio.py</files>

  <behavior>
    - Com uma ancora de `2026-08-30 14:30` semeada na pasta e o relogio parado em `20:30`, uma volta do `laco_da_agenda` produz exatamente um despacho, com `Categoria.SEMPRE`, cujo texto nomeia o boss e cita `14:30`.
    - A mesma volta em `22:30` (com `respawn_horas_max = 8`) produz exatamente um despacho, o do limite.
    - A mesma volta em `20:29` produz zero despachos.
    - A mesma volta em `23:30` (tres horas depois do vencimento da abertura) produz zero despachos.
    - Uma segunda volta em `20:31`, sobre a mesma pasta, produz zero despachos.
    - Em `--dry-run`, a volta de `20:30` mostra a mensagem no console e a pasta continua sem existir.
    - Com `[[boss]]` no config e nenhum `[[evento]]`, o laco SOBE em vez de devolver 2.
  </behavior>

  <action>
Quatro movimentos.

**1. `_avisar_janelas_de_respawn(registro, bosses, agora, despachante) -> list`**, funcao
de modulo em `__main__.py`, colocada ao lado de `_fechar_listas_de_presenca` e escrita
no molde dela.

Chama `anunciar_janelas(registro, bosses, agora)` e, para cada par devolvido, LOGA
SEMPRE e DESPACHA SE HOUVER PARA ONDE — a mesma separacao que o laco ja faz com o
encerramento de silencio e com a lista fechada, pela razao ja escrita ali: quem roda
sem `.env` e sem `--dry-run` perdia a mensagem ate no console. Loga com `destacar` e a
hora, despacha com `moldurar` e `Categoria.SEMPRE` (D-23). Devolve a lista de avisos,
para o teste poder afirmar sem vasculhar log.

**A docstring precisa carregar as tres razoes desta funcao existir**, porque as tres
ja foram pagas neste repositorio:

- **Extraida do corpo do laco** (a razao de `_fechar_listas_de_presenca`): o
  `--so-agenda` e o modo de menor cobertura do projeto, e foi onde o incidente de
  2026-08-26 19:30 aconteceu. Codigo escrito dentro do `while` nasce sem teste
  justamente no modo que faz o recurso valer.
- **A decisao e o texto NAO moram aqui** (a razao de `fechar_e_narrar`): eles moram em
  `respawn.anunciar_janelas`, e os dois lacos chamam a mesma funcao. As duas copias de
  `presenca` escreviam na MESMA `.agenda/` e falavam no MESMO grupo, e o usuario roda
  os dois modos — um conserto aplicado so de um lado fazia os dois anunciarem coisas
  diferentes sobre o mesmo boss. Aqui fica so o log e o despacho.
- **Nenhuma checagem anterior ao `marcar`** (D-21): esta funcao nao pergunta se o aviso
  ja saiu. Quem decide e o `marcar` dentro de `anunciar_janelas`, e a docstring de
  `RegistroEmDisco.marcar` proibe por escrito qualquer outra forma.

**2. A chamada dentro de `laco_da_agenda`.**

Leia `ler_bosses()` uma vez, junto de `ler_agenda()`, no comeco da funcao — ao lado e
pelo mesmo motivo: sao a mesma coisa, dado que o usuario escreve a mao e que o arranque
tem que conferir antes de subir. `ler_bosses` levanta `BossInvalido` para bloco torto,
e o `main()` ja trata isso e devolve 2; nao capture aqui.

Chame `_avisar_janelas_de_respawn` dentro do `while`, logo depois de
`_fechar_listas_de_presenca`. A ordem nao muda resultado — os dois namespaces em disco
sao disjuntos e nao ha estado compartilhado — mas fixa-la torna o tick deterministico
para o teste, que e a mesma razao ja escrita para a posicao do consumo de loot e do
fechamento. Diga isso no comentario, e diga tambem que ESTE e o laco que faz JANE-05
valer, pela mesma razao que AGEN-05 existe: quem mais precisa saber que a janela abriu
e justamente quem nao esta com o jogo aberto.

**3. A recusa por agenda vazia deixa de valer quando ha boss.**

Hoje o laco recusa subir e devolve 2 se `ler_agenda()` vier vazio. A partir desta fase
isso esta errado: um usuario que so vigia boss e nao configurou nenhum `[[evento]]`
ficaria sem o unico modo que entrega a janela com o jogo fechado — e ele e o publico
inteiro de JANE-05.

Passe a recusar apenas quando NAO houver evento E NAO houver boss, e reescreva a
mensagem de erro para citar as duas portas de entrada: um bloco de evento ou um bloco
de boss, com o exemplo comentado do repositorio. Quando houver so boss, a linha de
arranque que hoje anuncia os eventos precisa dizer o que esta acontecendo em vez de
mentir sobre uma agenda vazia — o modo esta vigiando o relogio para janela de boss, e
nao para evento.

Conferido antes de planejar: nenhum teste da suite afirma o codigo 2 dessa recusa, e
`git grep` pela mensagem atual so acha o proprio fonte. Acrescente o teste que faltava,
nos dois sentidos — sem evento e sem boss ainda devolve 2; sem evento e com boss sobe.

**4. `tests/test_janela_no_relogio.py`.**

Arquivo novo, para os testes de LACO. A docstring de abertura diz por que ele existe
separado de `tests/test_respawn.py`: aquele afirma funcoes puras, este afirma que o
laco as ALCANCA — que e o modo de falha mais caro deste projeto, codigo com teste verde
que o laco nunca chama, e que ja custou o nivel de membro e o elo do `.pegou`.

Reuse a forma de `TestSimulacaoNoLacoDaAgenda._tick`: `RelogioParado`, `PASTA_AGENDA` e
`PASTA_LOOT` para dentro do `tmp_path`, `ler_agenda` trocado, `montar_relogio` trocado
(o de verdade pergunta a hora ao Chatwoot e o teste nao pode depender do `.env` nem da
rede), `time.sleep` levantando `KeyboardInterrupt` para uma volta so. Acrescente a troca
de `ler_bosses` e um despachante que grava os despachos em memoria, para as afirmacoes
serem sobre estrutura e nao sobre log.

Semeie a ancora escrevendo o arquivo vazio direto na pasta, com o nome exato registrado
no `02-01-SUMMARY.md` — e nao chamando o produtor de chave. E o mesmo principio que faz
os valores esperados desta fase nunca virem da saida do proprio sistema: se o formato
mudar, este teste tem que ficar VERMELHO, e um teste que gera a propria chave ficaria
verde para sempre.
  </action>

  <verify>
    <automated>python -m pytest tests/test_janela_no_relogio.py tests/test_agenda.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Uma volta do `laco_da_agenda` com ancora de `2026-08-30 14:30` semeada e relogio em `20:30` produz exatamente um despacho, `Categoria.SEMPRE`, com o nome do boss e `14:30` no texto.
    - A mesma volta em `22:30` produz exatamente um despacho, e o texto dele e diferente do de `20:30`.
    - A mesma volta em `20:29` produz zero despachos; em `23:30`, zero despachos.
    - Uma segunda volta em `20:31` sobre a mesma pasta produz zero despachos, e a pasta tem exatamente um arquivo de aviso de abertura.
    - Em `--dry-run`, a volta de `20:30` deixa o texto no console e a pasta NAO existe ao fim.
    - Com `ler_agenda` devolvendo vazio e `ler_bosses` devolvendo dois bosses, o laco devolve 0 e nao 2.
    - Com os dois vazios, o laco devolve 2 e a mensagem de erro cita as duas portas de entrada.
    - O nome de arquivo da ancora usado pelo teste esta escrito literalmente no arquivo de teste, e nao gerado por `chave_do_nascimento`.
  </acceptance_criteria>

  <done>
    O usuario com o jogo fechado, o PC ligado e `run-agenda.bat` rodando recebe no
    WhatsApp que a janela do Tiat abriu, seis horas depois de um nascimento que ele
    talvez nem tenha visto.
  </done>

  <reversibility rating="costly">
    Afrouxar a recusa por agenda vazia muda o contrato de arranque do `--so-agenda`: um
    config que antes derrubava o modo agora o faz subir. E RUIDOSO nos dois sentidos (o
    console diz o que esta vigiando) e reversivel por uma condicao, mas mexe num caminho
    de erro que existia desde a Fase 6.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OPER-02 — o console diz quando a janela abre, e cala quando nao sabe</name>

  <read_first>
    - `l2scanner/__main__.py` linhas 1453-1477 — `_anunciar_proximo`. E o molde desta tarefa: funcao de modulo, relogio por PARAMETRO (com a razao escrita de por que ele nao e lido la dentro), chamada no arranque e de hora em hora.
    - `l2scanner/__main__.py` linhas 343-400 — `montar_vigia_de_bosses`, e o comentario que diz literalmente que a linha NOMEIA os bosses e nao os conta, porque "a Fase 2 completa a outra metade acrescentando a proxima janela prevista de cada um — e para isso o nome precisa ja ser a ancora do texto". Esta tarefa e a outra metade.
    - `l2scanner/__main__.py` linhas 1789-1815 — o bloco de linhas de arranque do `laco_principal` (agenda, proximo evento, monitorados) e a construcao do `registro_da_agenda`. As linhas novas entram nesse bloco, depois do registro existir.
    - `l2scanner/__main__.py` linhas 1425-1445 — o bloco horario do `laco_da_agenda`, que repete `_anunciar_proximo` a cada 3600 segundos com a razao escrita ("um scanner que nao diz quando vai falar de novo e indistinguivel de um scanner travado").
    - `l2scanner/respawn.py` — `Ancora`, `ancoras_mais_recentes`, `AvisoDeJanela` e as quatro frases da 02-01 Task 2. As linhas de previsao usam a MESMA citacao de origem.
  </read_first>

  <files>l2scanner/respawn.py, l2scanner/__main__.py, tests/test_respawn.py, tests/test_janela_no_relogio.py</files>

  <behavior>
    - Com dois bosses configurados e ancora so para um, saem DUAS linhas: a do que tem ancora diz quando a janela abre e quando o limite passa; a do outro diz que ainda nao houve nascimento visto.
    - A linha de quem nao tem ancora nao contem horario nenhum.
    - A linha de quem tem ancora cita a origem do nascimento, do mesmo jeito que a mensagem do WhatsApp cita.
    - Com nenhum boss configurado, sai lista vazia — nao uma linha dizendo que nao ha bosses (isso `montar_vigia_de_bosses` ja diz, e repetir treinaria o usuario a ignorar).
    - Os dois lacos imprimem as linhas no arranque.
  </behavior>

  <action>
Dois movimentos.

**1. `linhas_de_previsao(agora, bosses, ancoras) -> list[str]` em `respawn.py`, PURA.**

Uma linha por `Boss`, na ordem do config. Sem disco, sem relogio proprio, sem log —
devolve texto e quem imprime e o chamador, pela mesma disciplina que faz
`texto_de_fechamento` so formatar e `texto_do_aviso` nao conhecer designacao nenhuma.

Com ancora, a linha traz: o nome do boss, quando a janela abre e quando o limite passa
(data curta e hora, no formato de `_anunciar_proximo`), e a citacao da ancora com o
horario e a ORIGEM — anuncio do servidor ou alvo, com a mesma distincao das quatro
frases da 02-01. Quem le o console tem que poder julgar o numero com a mesma informacao
de quem le o grupo (D-16).

Sem ancora, a linha diz que ainda nao houve nascimento visto DAQUELE boss e que por
isso nao ha previsao. **Ela nao pode conter horario nenhum**, e a docstring precisa
dizer por que: o console dizendo "ainda nao vi nascimento deste boss" e a RESPOSTA
CORRETA, e nao uma degradacao — e um horario inventado ali seria a mesma familia de
defeito que a poda de 3 dias existe para impedir, so que na tela em vez de no grupo.

O `agora` entra por parametro e nao e lido la dentro, pela razao ja escrita em
`_anunciar_proximo`: e o que impede esta funcao de ser a ultima do arquivo a perguntar
as horas ao Windows. O modulo esta no portao AST desde 02-01 Task 3.

Acrescente as linhas de previsao ao teste parametrizado do portao de D-19 escrito em
02-01 Task 2 — a lista de tokens proibidos vale para todo texto que esta fase produz,
e nao so para as quatro frases do WhatsApp.

**2. `_anunciar_previsao_de_janelas(registro, bosses, agora)` em `__main__.py`**, no
molde de `_anunciar_proximo`: le `registro.nascimentos()`, monta as ancoras com
`ancoras_mais_recentes`, chama `linhas_de_previsao` e loga cada linha em `INFO`.
Nada mais.

Chame nos DOIS lacos, no arranque:

- No `laco_da_agenda`, ao lado da chamada existente de `_anunciar_proximo`, e tambem
  dentro do bloco horario que a repete. A razao e a mesma ja escrita ali e vale ainda
  mais aqui: o `--so-agenda` roda por dias, e um scanner que nao diz quando vai falar de
  novo e indistinguivel de um travado. O ROADMAP e explicito em que este e "o modo de
  quem mais precisa da linha".
- No `laco_principal`, no bloco de linhas de arranque, depois de `registro_da_agenda`
  existir. **Deliberadamente FORA de `montar_vigia_de_bosses`**, e a razao vai no
  comentario: a previsao depende so da ancora e do relogio, entao ela tem que sair
  mesmo quando o vigia esta desligado por falta de calibracao ou de OCR. Amarra-la ao
  vigia faria uma calibracao quebrada apagar, em silencio, a previsao de uma ancora que
  continua correta em disco — que e a mesma razao pela qual `Sessao` recebe
  `regras_de_respawn` separado de `bosses`.

A linha de `montar_vigia_de_bosses` que hoje nomeia os bosses vigiados NAO muda: ela e
a metade "quem esta sendo vigiado" que a Fase 1 entregou, e estas linhas sao a metade
"qual a proxima janela". As duas juntas sao OPER-02 inteiro.
  </action>

  <verify>
    <automated>python -m pytest tests/test_respawn.py tests/test_janela_no_relogio.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `linhas_de_previsao` com dois `Boss` e ancora so para o primeiro devolve exatamente duas linhas.
    - A linha do boss COM ancora contem o nome do boss, um horario de abertura, um horario de limite, e a citacao da origem do nascimento.
    - A linha do boss SEM ancora contem o nome do boss e nao contem nenhum digito de horario; ela afirma que ainda nao houve nascimento visto daquele boss.
    - Trocando a origem da ancora de anuncio para alvo, a linha muda a citacao — a mesma distincao das quatro frases do WhatsApp.
    - `linhas_de_previsao` com lista de bosses vazia devolve lista vazia.
    - `linhas_de_previsao` nao contem nenhum dos tokens proibidos de D-19, e o detector continua acusando o texto de controle.
    - Uma volta do `laco_da_agenda` com dois bosses e uma ancora emite as duas linhas no console em `INFO`, antes de qualquer despacho.
    - Uma volta do `laco_principal` com a mesma configuracao emite as mesmas duas linhas, inclusive quando `montar_vigia_de_bosses` devolveu `None` por falta de calibracao.
    - `linhas_de_previsao` nao le relogio: chamada duas vezes com o mesmo `agora` devolve exatamente as mesmas strings.
  </acceptance_criteria>

  <done>
    Quem sobe o scanner ve, na primeira tela, quais bosses estao sendo vigiados e
    quando a janela de cada um abre — e ve escrito, sem previsao inventada, de quais
    deles o scanner ainda nao viu nascimento nenhum.
  </done>

  <reversibility rating="reversible">
    Sao linhas de console. A redacao muda a qualquer momento sem custo; nada aqui vira
    nome de arquivo nem chave duravel.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: O portao contra a divergencia dos dois lacos</name>

  <read_first>
    - `tests/test_presenca.py` linhas 1988-2030 — `test_o_laco_da_agenda_chama_o_fechamento` e `test_a_sessao_do_laco_principal_recebe_membros`. **Sao o molde exato desta tarefa**: AST sobre `__main__.py`, procurando a chamada dentro da funcao nomeada, com a docstring que diz por que ("a funcao pode existir e nunca ser chamada... o modo de falha mais caro desta fase").
    - `tests/test_presenca.py` linhas 2063-2100 — `test_os_dois_lacos_entregam_um_leitor_que_carrega_membros`, parametrizado sobre os dois lacos, com a razao escrita de por que os DOIS e nao so o principal.
    - `tests/test_agenda.py` linhas 1216-1250 — `test_os_dois_lacos_constroem_o_registro_com_simulando`, e a razao escrita de AST em vez de grep (as docstrings desta fase escrevem os nomes varias vezes, e uma busca textual acusaria a propria documentacao que protege a regra).
    - `l2scanner/__main__.py` e `l2scanner/sessao.py` — como ficaram depois das Tasks 1 e 2 e do plano 02-01.
  </read_first>

  <files>tests/test_janela_no_relogio.py</files>

  <action>
Quatro afirmacoes, todas por AST e nenhuma por busca textual, pela razao que este
projeto ja escreveu tres vezes: as docstrings desta fase citam os nomes proibidos de
proposito, para explicar a regra, e um grep daria positivo justamente na documentacao
que a protege.

**1. Os dois sitios chamam a MESMA funcao.** `sessao._processar_janelas` contem uma
chamada a `anunciar_janelas`; `__main__._avisar_janelas_de_respawn` contem uma chamada
a `anunciar_janelas`; e `__main__.laco_da_agenda` contem uma chamada a
`_avisar_janelas_de_respawn`. As tres, parametrizadas, cada uma dizendo QUAL elo
quebrou quando quebrar.

**2. Nenhum dos dois sitios calcula por conta propria.** Nem `sessao.py` nem
`__main__.py` podem conter chamada a `janelas_devidas` nem a `texto_da_janela` — essas
duas sao propriedade exclusiva de `anunciar_janelas`. Escreva na docstring o que este
portao esta comprando: uma copia da sequencia num dos lacos ficaria VERDE em todos os
outros testes desta fase, porque cada laco tem os seus, e a divergencia so apareceria
no dia em que alguem consertasse um lado. E literalmente o defeito WR-08 que `presenca`
pagou, e o ROADMAP mandou encara-lo.

`chave_do_nascimento` fica FORA da proibicao e a excecao e deliberada: ela e chamada em
`sessao._processar_bosses`, que e o unico sitio de ESCRITA de ancora do projeto, porque
ancorar exige pixels e so o laco principal tem pixels. Escreva a excecao e a razao no
teste, para que quem a encontrar nao a tome por esquecimento.

**2b. E nenhum dos dois shells pode CHECAR antes de marcar (D-21).** Proibir so o
calculo deixa um furo que importa: nada impediria alguem de escrever, dentro de
`_avisar_janelas_de_respawn` ou de `sessao._processar_janelas`, uma consulta a
`registro.enviados()` para "evitar mandar de novo". Essa e exatamente a forma que a
docstring de `RegistroEmDisco.marcar` proibe por escrito — a decisao de despachar tem
que ser AQUELA chamada, nunca uma checagem anterior — e ela reintroduz a janela de
corrida entre ler e escrever que o `O_CREAT|O_EXCL` existe para fechar, bem no caminho
das duas instancias do usuario. O sintoma seria um aviso PERDIDO, nao duplicado, e
nenhum teste desta fase o veria: cada instancia ficaria verde sozinha.

Acrescente `enviados` a lista de chamadas proibidas DENTRO dos dois shells (o corpo de
`_avisar_janelas_de_respawn` e o de `sessao._processar_janelas`), e nao no arquivo
inteiro — `laco_da_agenda` chama `registro.enviados()` para os avisos de agenda desde a
Fase 6 e continua podendo. A unica leitura de disco que os caminhos de janela fazem
fora de `anunciar_janelas` e `registro.nascimentos()`, em
`_anunciar_previsao_de_janelas`, que so escreve no console e nao despacha nada; nomeie
essa excecao no teste com essa razao.

**3. Os dois lacos leem a lista de bosses do config.** `laco_principal` e
`laco_da_agenda` contem, cada um, uma chamada a `ler_bosses` — no molde de
`test_os_dois_lacos_constroem_o_registro_com_simulando`. Sem isso, um dos lacos poderia
passar uma lista vazia e ficar mudo com todos os testes verdes.

**4. Os dois lacos chamam `_anunciar_previsao_de_janelas`**, parametrizado sobre os
dois nomes, pela mesma razao de OPER-02 valer "tambem no `--so-agenda`, que e o modo de
quem mais precisa da linha".

**Guarda contra prova vazia, obrigatoria.** No molde de
`test_a_prova_pega_de_verdade_um_relogio_proprio`: o detector aplicado a uma arvore
sintatica fabricada, que CONTEM a chamada proibida, tem que acusar. Sem ela, um
detector que procurasse o nome errado ficaria verde para sempre — e este portao inteiro
seria decoracao.
  </action>

  <verify>
    <automated>python -m pytest tests/test_janela_no_relogio.py -q && python -m pytest -q --ignore=tests/test_mercado_leitura.py --ignore=tests/test_mercado_pagina.py</automated>
  </verify>

  <acceptance_criteria>
    - Um teste falha, nomeando o elo, se `sessao._processar_janelas` deixar de chamar `anunciar_janelas`.
    - Um teste falha, nomeando o elo, se `__main__._avisar_janelas_de_respawn` deixar de chamar `anunciar_janelas`.
    - Um teste falha se `laco_da_agenda` deixar de chamar `_avisar_janelas_de_respawn`.
    - Um teste falha se `janelas_devidas` ou `texto_da_janela` for chamada em `sessao.py` ou em `__main__.py`, e a excecao de `chave_do_nascimento` esta escrita com a razao.
    - Um teste falha se `enviados` for chamada dentro do corpo de `_avisar_janelas_de_respawn` ou de `sessao._processar_janelas` — a checagem anterior ao `marcar` que D-21 proibe. O teste NAO proibe `enviados` no resto de `__main__.py` (os avisos de agenda a usam desde a Fase 6), e nomeia `_anunciar_previsao_de_janelas` como o unico leitor de disco permitido fora de `anunciar_janelas`, porque ele so imprime no console.
    - O detector do item acima ACUSA uma arvore fabricada contendo `registro.enviados()` dentro de um dos dois shells.
    - Testes parametrizados falham se `laco_principal` ou `laco_da_agenda` deixar de chamar `ler_bosses` ou `_anunciar_previsao_de_janelas`.
    - O detector, aplicado a uma arvore fabricada que contem a chamada proibida, ACUSA — a prova nao e vazia.
    - A suite inteira, fora dos arquivos de mercado, esta verde.
  </acceptance_criteria>

  <done>
    Um conserto aplicado a um dos dois lacos deixa de ser possivel: ou ele vai para
    `respawn.py` e vale para os dois, ou um teste nomeado quebra dizendo qual elo
    divergiu.
  </done>

  <reversibility rating="reversible">
    Portoes de teste. Retira-los custa apagar o arquivo; o que eles compram e que a
    retirada seja um ato deliberado e visivel no diff, em vez de uma divergencia que
    aparece meses depois no grupo do WhatsApp.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.agenda/` -> `laco_da_agenda` (segundo sitio de leitura) | O mesmo disco compartilhado, agora lido e marcado por um segundo processo que nao tem tela nenhuma. |
| `config.toml` -> `ler_bosses` no `--so-agenda` | Um bloco `[[boss]]` torto passa a poder derrubar o arranque de um modo que antes nem lia bosses. |
| `linhas_de_previsao` -> console | Afirmacao sobre o futuro, agora tambem na tela do usuario. |
| `_avisar_janelas_de_respawn` -> Chatwoot -> grupo | O mesmo texto do laco principal, saindo por um segundo caminho de despacho. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | os dois lacos divergindo na logica de janela | **high** | mitigate | E o defeito WR-08 repetido, e ele ja aconteceu neste repositorio com `presenca`. Mitigado por ESTRUTURA e nao por politica: a decisao e o texto moram so em `respawn.anunciar_janelas`, os dois lacos ficam com log e despacho, e a Task 3 poe um portao AST que quebra se um dos sitios deixar de passar por ela ou passar a calcular por conta propria. Com prova-nao-vazia, para o portao nao ser decoracao. |
| T-02-11 | Repudiation | `--so-agenda` e laco principal rodando juntos e anunciando em dobro | medium | mitigate | Herdada e ja resolvida pela forma: os dois passam por `anunciar_janelas`, que chama `registro.marcar(aviso.chave)` sobre a MESMA pasta — `O_CREAT|O_EXCL` deixa exatamente um vencer (D-21). O shell novo nao ganha checagem propria, e a Task 3 impede que ganhe. Afirmado no LACO, e nao so na funcao pura: uma segunda volta sobre a mesma pasta produz zero despachos. |
| T-02-12 | Denial of Service | `--dry-run` queimando o marcador de janela do scanner de verdade | medium | mitigate | E o incidente de 2026-08-26 19:30, e a guarda ja existe no lugar certo: `RegistroEmDisco(PASTA_AGENDA, simulando=args.dry_run)` no arranque do laco, e `marcar` devolvendo `True` sem encostar no disco. O shell novo nao conhece `dry_run` e nao pode conhecer. Afirmado: em `--dry-run` a volta mostra a mensagem e a pasta nao existe ao fim. |
| T-02-13 | Information disclosure | previsao inventada no console para um boss sem ancora | medium | mitigate | `linhas_de_previsao` produz, para quem nao tem ancora, uma linha SEM horario nenhum, afirmando que nao houve nascimento visto. Afirmado por teste que exige ausencia de digito de horario naquela linha. O console dizendo que nao sabe e a resposta correta, e nao uma degradacao. |
| T-02-14 | Denial of Service | `--so-agenda` recusando subir para quem so configurou boss | medium | mitigate | A recusa passa a exigir ausencia de evento E de boss (Task 1), com teste nos dois sentidos. Sem isso, o publico inteiro de JANE-05 — quem vigia boss com o jogo fechado — ficaria sem o unico modo que entrega o aviso. |
| T-02-15 | Tampering | bloco `[[boss]]` torto derrubando um modo que antes subia | low | accept | `ler_bosses` no `--so-agenda` passa a poder levantar `BossInvalido` e o processo devolve 2. E o comportamento CORRETO e ja e o do laco principal desde a Fase 1: derrubar o arranque enquanto o usuario olha o console, em vez de subir vigiando errado em silencio. Aceito com a razao escrita, e nao capturado localmente. |
| T-02-SC | Tampering | instalacao de pacotes (npm/pip/cargo) | n/a | accept | Nenhuma dependencia nova. Nenhuma tarefa executa gerenciador de pacotes; o portao de legitimidade nao tem alvo. Se uma tarefa vier a precisar de um pacote, ela para e o portao se aplica antes do install. |

**Herdadas de `02-01-PLAN.md` e nao re-litigadas aqui:** T-02-01 a T-02-09 e T-01-02.
Este plano nao muda a superficie de nenhuma delas — ele acrescenta um segundo LEITOR da
mesma pasta, e nenhum escritor de ancora novo.

**Nivel ASVS: 1. `security_block_on: high`.** A unica `high` (T-02-10) e `mitigate` com
implementacao nomeada em tarefa e teste.
</threat_model>

<verification>
- `python -m pytest tests/test_janela_no_relogio.py tests/test_respawn.py tests/test_agenda.py tests/test_presenca.py tests/test_sessao.py -q` — os arquivos das duas fases, verdes.
- `python -m pytest -q` — a suite inteira, com as falhas de `tests/test_mercado_*.py` atribuidas por `git log` e listadas nominalmente no SUMMARY.
- `python -c "import l2scanner.__main__"` — nenhum ciclo se fechou.
- Demonstracao sem jogo e sem rede, ponta a ponta, a partir de uma pasta limpa: semear uma ancora, rodar uma volta do laco com o relogio parado em `ancora + min` e ver a mensagem; rodar de novo e nao ver nada.
</verification>

<success_criteria>
- Criterio 2 do ROADMAP (JANE-02, JANE-05) verdadeiro INTEIRO: o modo de relogio, sem jogo, sem calibracao, sem captura e sem rede, entrega uma mensagem em `min` e uma em `max`, nada antes de `min`, e nada para um aviso vencido ha muito tempo.
- Criterio 6 (OPER-02) verdadeiro: os dois lacos listam os bosses vigiados e, para os que tem ancora, quando a janela abre e quando o limite passa; para os que nao tem, dizem isso sem inventar previsao.
- Criterio 7 (OPER-03) verdadeiro na metade que e deste plano: toda a fase demonstravel com o jogo fechado e sem rede.
- O risco 1 do ROADMAP ("dois lugares de fiacao, nao um") fica fechado por estrutura e nao por politica, com portao AST e prova-nao-vazia.
</success_criteria>

## Artifacts this phase produces

Inventario completo dos simbolos, chaves, prefixos e caminhos criados pela FASE 2
(os dois planos somados). A verificacao da fase e qualquer fase futura dependem destes
nomes exatos.

### Modulo novo — `l2scanner/respawn.py` (plano 02-01, expandido em 02-02)

| Simbolo | Forma | Nota |
|---------|-------|------|
| `TipoDeJanela` | `Enum` | `ABRE = "abre"`, `LIMITE = "limite"`. O `.value` e sufixo de nome de arquivo duravel. Nao ha membro com verbo de encerramento, por D-19. |
| `Ancora` | dataclass congelado | `boss: str` (APELIDO), `instante: datetime`, `origem: OrigemDoAviso` |
| `AvisoDeJanela` | dataclass congelado | `boss: str` (NOME do config), `tipo: TipoDeJanela`, `ancora: Ancora`, `alvo: datetime`, `horas: float`; property `chave` |
| `chave_do_nascimento` | funcao | `(nome, instante, origem) -> str`, sem prefixo |
| `ancora_de_chave` | funcao | `(chave: str) -> Ancora \| None` — nunca levanta |
| `ancoras_mais_recentes` | funcao pura | `(chaves) -> dict[apelido, Ancora]` — JANE-04 inteiro (D-20) |
| `janelas_devidas` | funcao pura | `(agora, bosses, ancoras, ja_enviados, tolerancia_minutos=TOLERANCIA_MINUTOS) -> list[AvisoDeJanela]` |
| `texto_da_janela` | funcao | `(aviso) -> str` — a matriz `{ABRE, LIMITE} x {anuncio, alvo}` |
| `anunciar_janelas` | funcao | `(registro, bosses, agora) -> list[tuple[AvisoDeJanela, str]]` — **a UMA implementacao que os dois lacos chamam** |
| `linhas_de_previsao` | funcao pura | `(agora, bosses, ancoras) -> list[str]` — OPER-02 |

### `l2scanner/agenda.py` (plano 02-01)

| Simbolo | Forma | Nota |
|---------|-------|------|
| `PREFIXO_NASCIMENTO` | `str` = `"nascimento_"` | Declarado AQUI de proposito, para o guarda de introspecao de `vars(agenda)` o alcancar. Entra em `_PREFIXOS_CONHECIDOS`. |
| `RegistroEmDisco.registrar_nascimento` | metodo | `(chave: str) -> bool` — delega a `marcar` com o prefixo, molde de `cancelar` |
| `RegistroEmDisco.nascimentos` | metodo | `() -> set[str]` — nomes sem o prefixo, molde de `cancelados` |

`apelido_do_evento` ganha um QUARTO consumidor citado na docstring. Nao muda de nome
nem de comportamento: o valor e duravel.

### `l2scanner/sessao.py` (plano 02-01)

| Simbolo | Forma | Nota |
|---------|-------|------|
| `Sessao(regras_de_respawn=())` | kwarg | a lista de `Boss`; SEPARADO de `bosses` (o `VigiaDeBosses`), para a previsao nao morrer com uma calibracao quebrada |
| `Sessao._processar_janelas` | metodo | `(agora, resultado) -> None` |
| `ResultadoDoTick.ancoras_gravadas` | `list` | pares `(boss, origem)` das ancoras que ESTE tick criou |
| `ResultadoDoTick.avisos_de_janela` | `list` | pares `(boss, tipo)` dos avisos que ESTE tick despachou |

`Sessao._processar_bosses` passa a escrever a ancora — **o unico sitio de escrita de
ancora do projeto**, porque ancorar exige pixels.

### `l2scanner/config.py` (plano 02-01)

`_recusar_bosses_repetidos` passa a recusar tambem APELIDOS repetidos e apelido vazio,
porque o `nome` do `[[boss]]` virou nome de arquivo duravel.

### `l2scanner/__main__.py` (planos 02-01 e 02-02)

| Simbolo | Forma | Nota |
|---------|-------|------|
| `_avisar_janelas_de_respawn` | funcao | `(registro, bosses, agora, despachante) -> list` — o shell do `--so-agenda`, molde de `_fechar_listas_de_presenca` |
| `_anunciar_previsao_de_janelas` | funcao | `(registro, bosses, agora) -> None` — OPER-02, molde de `_anunciar_proximo`, chamada nos DOIS lacos |

`laco_principal` passa a ler `ler_bosses()` para uma variavel e a passa-la tambem a
`Sessao(regras_de_respawn=...)`. `laco_da_agenda` passa a ler `ler_bosses()`, a chamar
o shell novo e a so recusar o arranque quando NAO ha evento E NAO ha boss.

### Nomes de arquivo em `.agenda/` (DURAVEIS — mudar reenvia aviso)

| Namespace | Forma | Podado por |
|-----------|-------|------------|
| ancora de nascimento | `nascimento_<YYYY-MM-DD>_<boss-slug>-<HHMM>_<origem>` | `podar`, via `PREFIXO_NASCIMENTO` em `_PREFIXOS_CONHECIDOS`, em `DIAS_DE_MARCADOR` = 3 dias |
| aviso de janela | `<YYYY-MM-DD>_<boss-slug>-<HHMM>_<tipo>` (sem prefixo, data/hora DA ANCORA) | `podar`, pelo mesmo caminho dos avisos de agenda |

`<origem>` e `chat`, `alvo` ou `chat_e_alvo` (o `.value` de `bosses.OrigemDoAviso`).
`<tipo>` e `abre` ou `limite`.

### Chaves de `config.toml`

Nenhuma chave nova. Esta fase e quem finalmente USA `respawn_horas_min` e
`respawn_horas_max`, lidos, validados e ignorados desde a Fase 1.

### Testes

`tests/test_respawn.py` (novo, 02-01); `tests/test_janela_no_relogio.py` (novo, 02-02);
`"respawn.py"` na tupla `MODULOS` de `tests/test_presenca.py::TestSemRelogioProprio`; o
guarda de prefixos de `tests/test_agenda.py` passa a varrer `agenda` E `respawn`.

### NAO mudam, deliberadamente

`Calibracao.tiat_chat` / `tiat_alvo`, as chaves de mesmo nome no `calibration.json`, as
chaves `frame.extras['tiat_chat'|'tiat_alvo']`, a flag `--tiat`, o `calibrar-tiat.bat`,
`agenda.TipoDeAviso` (nao ganha membro — ver a justificativa escrita em 02-01), e a
linha de `montar_vigia_de_bosses` que nomeia os bosses vigiados.

## Multi-Source Coverage Audit (fase inteira)

### GOAL — o objetivo da Fase 2 no ROADMAP

| Clausula do objetivo | Coberta por |
|---|---|
| "A party sabe pelo WhatsApp que a janela do boss abriu" | 02-01 T1 (fatia inteira), 02-01 T2 (a frase) |
| "e que o limite otimista passou" | 02-01 T2 |
| "com o jogo fechado" | 02-02 T1 |
| "a partir do ultimo nascimento que o scanner realmente viu" | 02-01 T1 (`ancoras_mais_recentes`), 02-01 T3 (a poda que impede ancora velha de mentir) |
| "com a mensagem dizendo de onde o numero veio" | 02-01 T2 (a citacao da origem), 02-02 T2 (a mesma citacao no console) |

### REQ — os 8 requisitos que o ROADMAP mapeia para esta fase

| Req | Plano(s) | Criterio do ROADMAP |
|---|---|---|
| JANE-01 | 02-01 T1, 02-01 T3 | 1 |
| JANE-02 | 02-01 T1, 02-01 T2, 02-02 T1 | 2 |
| JANE-03 | 02-01 T2 | 3 |
| JANE-04 | 02-01 T1 (`ancoras_mais_recentes`), 02-01 T2 | 4 |
| JANE-05 | 02-02 T1, 02-02 T3 | 2 |
| JANE-06 | 02-01 T1, 02-02 T1 | 5 |
| OPER-02 | 02-02 T2 | 6 |
| OPER-03 | 02-01 T3, 02-02 T1, 02-02 T3 | 7 |

Criterio 8 do ROADMAP (`Categoria.SEMPRE`) nao tem requisito proprio e e coberto por
02-01 T1 (laco principal) e 02-02 T1 (`--so-agenda`).

### RESEARCH — nao houve RESEARCH.md; o `<specifics>` do CONTEXT e o `01-VERIFICATION.md` ocupam o lugar

| Item medido | Coberto por |
|---|---|
| Regra do servidor: 6h fixas + 0 a 2h aleatorias, contadas da MORTE | 02-01 T1 (`janelas_devidas` le do `[[boss]]`), 02-01 T2 (a frase cita as horas do config) |
| Dois Tiats vigiados, com relogios independentes; o volume de mensagem dobra | 02-01 T1 (ancora por boss), 02-01 T2 (dois bosses e uma ancora), 02-02 T2 (uma linha por boss) |
| Medido em campo 2026-08-30 14h30: `Tiat South` no alvo sem ter acabado de nascer | 02-01 T1 (o comentario da escrita da ancora), 02-01 T2 (a ressalva no texto do alvo), T-02-02 |
| O erro nao acumula entre ciclos: cada nascimento reancora | 02-01 T1 (`ancoras_mais_recentes`), 02-01 T2 (a aresta das duas ancoras) |
| `splitlines()` inerte em producao; exposicao medida como LOW | Carregado como medido no `<threat_model>` de 02-01 (T-01-02, `accept`), sem re-litigar |
| Origem ALVO afirma nascimento sem prova — a Fase 2 tem de decidir explicitamente | DECIDIDO: D-15 (ancora) + D-16 (a citacao e a ressalva). 02-01 T1 e T2. |
| Gap G-01 da Fase 1 (`regiao_da_calibracao` na ferramenta de diagnostico) | **FORA DESTA FASE, deliberadamente.** E `tools/` puro, nao afeta o scanner, nao afeta nenhum criterio da Fase 2, e a propria verificacao o escopou como "`tools/` apenas". Continua aberto no `01-VERIFICATION.md`. Nao e lacuna desta fase. |
| Os 2 itens de `human_verification` da Fase 1 (calibrar com o jogo aberto) | **FORA DESTA FASE.** Sao da Fase 1 e exigem o jogo aberto; esta fase inteira e demonstravel com o jogo fechado (OPER-03). |

### CONTEXT — decisoes travadas

| ID | Plano(s) |
|---|---|
| D-15 (os dois sinais ancoram) | 02-01 T1, 02-01 T2 |
| D-16 (a mensagem cita qual sinal ancorou) | 02-01 T2, 02-02 T2 |
| D-17 (dois avisos por ciclo, sem antecedencia) | 02-01 T1, 02-01 T2, 02-02 T1 |
| D-18 (a ancora em `.agenda/`, prefixo proprio, poda de 3 dias) | 02-01 T1, 02-01 T3 |
| D-19 (a mensagem de `max` nao afirma fechamento) | 02-01 T2, 02-02 T2 |
| D-20 (JANE-04 estrutural, so a ancora mais recente) | 02-01 T1, 02-01 T2 |
| D-21 (`marcar` E a decisao de despacho) | 02-01 T1, 02-02 T1, 02-02 T3 |
| D-22 (tolerancia de 5 minutos) | 02-01 T2, 02-02 T1 |
| D-23 (`Categoria.SEMPRE`) | 02-01 T1, 02-02 T1 |
| R-1 (nome do modulo) | 02-01 T1 |
| R-2 (tipo proprio, com justificativa escrita) | 02-01 T1, secao `<justificativa_do_tipo_proprio>` |
| R-3 (redacao das quatro mensagens) | 02-01 T2 |
| R-4 (onde mora a funcao compartilhada) | 02-01 T1, 02-02 T1, 02-02 T3 |

### Riscos que o ROADMAP mandou encarar

| Risco | Onde e encarado |
|---|---|
| 1. Dois lugares de fiacao, nao um | 02-02 T1 (o segundo sitio pela mesma funcao) e 02-02 T3 (o portao AST que impede a divergencia). T-02-10. |
| 2. A chave estruturada e derivada da ancora | 02-01 T1 (`AvisoDeJanela.chave`, com as tres razoes escritas), sobre a forma travada em D-18 pelo usuario em 2026-08-30 (`02-CONTEXT.md` linhas 53-71) |
| 3. Nao estender `TipoDeAviso` sem medir o preco | 02-01 T1, `<justificativa_do_tipo_proprio>` — quatro razoes e o custo da separacao dito em voz alta |
| 4. A tolerancia de 5 minutos vale aqui | 02-01 T2 (cinco arestas na funcao pura) e 02-02 T1 (afirmada no laco) |
| 5. O ponto cego de `_PREFIXOS_CONHECIDOS` | 02-01 T1 (declarado em `agenda.py`, o guarda existente alcanca) **e** 02-01 T3 (o guarda estendido ao modulo novo). As DUAS opcoes do ROADMAP, e nao uma — a primeira resolve hoje, a segunda fecha o buraco para o proximo modulo. |

### Exclusoes declaradas (nao sao lacunas)

- `<deferred>` do CONTEXT: `/morreu <boss>` (MORT-01, v2); aprender o tempo-de-vida (APRE-01, v2); lembrete de antecedencia (recusado por volume); a cor ciano do anuncio como segunda confirmacao.
- `Out of Scope` do REQUIREMENTS: ler memoria ou pacotes; prever o horario exato; detectar a morte pela tela; avisar sobre mobs comuns.
- Escopo da Fase 1, fechada e verificada: qualquer mudanca no reconhecimento.
- Gap G-01 e os 2 itens de verificacao humana da Fase 1: `tools/` e jogo aberto, ambos fora de OPER-03.
- Arquivos dos workstreams `discord` e `mercado`.

**Resultado: 0 itens sem plano.**

<output>
Criar `.planning/workstreams/tiat/phases/02-janela-de-respawn/02-02-SUMMARY.md` ao terminar.

O SUMMARY precisa registrar, obrigatoriamente:

1. **A demonstracao ponta a ponta sem jogo e sem rede**, com os comandos exatos e a
   saida: pasta limpa, ancora semeada, relogio em `ancora + min`, a mensagem; relogio
   em `ancora + max`, a segunda mensagem; relogio tres horas depois, silencio.
2. **As linhas de console como ficaram**, para os dois casos (com ancora e sem), porque
   a verificacao da fase compara o criterio 6 contra elas.
3. **Se a recusa por agenda vazia mudou de comportamento para algum config real** do
   usuario, e qual.
4. **O que o portao AST da Task 3 acusaria hoje se um dos lacos fosse copiado** — rodar
   o detector contra uma copia deliberada e registrar a acusacao, para provar que ele
   nao e decoracao.
5. As falhas de `tests/test_mercado_*.py` no momento da execucao, nominalmente, com a
   atribuicao por `git log`.
</output>
