---
phase: quick-260829-rd9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/mercado/REQUIREMENTS.md
  - .planning/workstreams/mercado/ROADMAP.md
autonomous: true
requirements: [QUICK-260829-rd9]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LEIT-01 DEIXA DE SER UM PORTÃO. Hoje ele diz que o que é reconhecido são os itens de uma lista do `config.toml`; passa a dizer que o nome vem por OCR sobre o recorte da coluna do nome, é agrupado por similaridade contra os nomes já vistos, e que um nome desconhecido entra como SÉRIE NOVA sem intervenção do usuário. O que é registrado deixa de depender de configuração prévia."
    - "LEIT-02 SAI BYTE A BYTE IDÊNTICO, e isso é PROVADO, não prometido: `grep -qxF` da linha inteira contra o texto de hoje. A medição da spike diz por quê — o motor devolve `1650` onde a tela diz `16,50`, erro de 100x com aparência plausível, que é exatamente a família de defeito que LEIT-02 existe para impedir. O OCR é a metade que NÃO pode tocar no número."
    - "LEIT-05 nasce e é o ÚNICO item desta mudança que toca a calibração: a leitura do nome usa o recorte da COLUNA DO NOME, nunca a linha inteira, e essa coluna é calibrada e persistida em `calibration.json`. Justificativa medida, não suposta: no frame `20260828-061253-mercado-tooltip/frame_000010.png`, lendo a linha inteira, `Can be stored in the private warehouse` entra na leitura como se fosse nome de item."
    - "O RACIOCÍNIO SUPERADO É PRESERVADO, NÃO APAGADO. A linha da tabela Out of Scope sai, mas em seu lugar fica um bloco em prosa que reconstrói o argumento antigo, diz o que o derrubou, e ancora nas duas datas: a exclusão nasceu em `db751c7` (2026-08-27) e a primeira gravação do World Exchange é de 2026-08-28 05:31 — o OCR foi descartado um dia antes de existir um frame do mercado no disco."
    - "A ARITMÉTICA FECHA EM QUATRO LUGARES OU A TAREFA FALHOU: o bloco de requisitos v1 conta 18 bullets, a tabela Traceability conta 18 linhas, o Coverage do REQUIREMENTS.md diz 18, e o Coverage do ROADMAP.md diz 18 com `LEIT 5`. Uma contagem que não bate é a assinatura clássica de um requisito órfão."
    - "A FASE 3 DO ROADMAP SAI BYTE A BYTE IDÊNTICA, e a verificação foi feita ANTES de planejar: o bloco `### Phase 3` tem ZERO ocorrências de `item`, `nome` ou `watchlist`. A identidade do item não aparece nos critérios da persistência, então não há o que ajustar — e a tentação de 'aproveitar e melhorar' é recusada explicitamente."
    - "AS FASES 1 e 4 SAEM BYTE A BYTE IDÊNTICAS. O critério 2 da Fase 1 menciona a lista no contexto das variantes de encanto, e é registro histórico de uma spike já respondida e validada; ANAL-01 e WAPP-02 mencionam a lista no papel que ela CONTINUA tendo. Nenhum dos três muda."
    - "É DOCS-ONLY E ISSO É PROVADO PELO DIFF: `git diff --name-only HEAD` não contém nenhum `.py`, nada sob `tests/`, e nada sob `l2scanner/`. Zero arquivo de produção, zero teste."
  artifacts:
    - ".planning/workstreams/mercado/REQUIREMENTS.md — LEIT-01 reescrito, LEIT-05 novo, bloco `#### Por que o OCR de nomes voltou ao escopo`, tabela Out of Scope sem a exclusão do OCR e com a nota de remoção, Traceability com LEIT-05, Coverage em 18"
    - ".planning/workstreams/mercado/ROADMAP.md — resumo, Goal, critérios 1 e 2 e Requirements da Fase 2 atualizados; tabela Coverage com LEIT-05 e contagem 18"
    - ".planning/workstreams/mercado/quick/260829-rd9-ocr-de-nomes-de-item-entra-no-escopo-lei/260829-rd9-SUMMARY.md"
  key_links:
    - "LEIT-05 no REQUIREMENTS.md ↔ linha `**Requirements**` da Fase 2 no ROADMAP.md ↔ linha `| Leitura |` da tabela Coverage ↔ linha `| LEIT-05 |` da Traceability — um requisito que existe em três dos quatro é um requisito órfão"
    - "LEIT-01 (o nome vem por OCR) ↔ critério 1 da Fase 2 (a forma observável do mesmo fato) — requisito reescrito sem critério reescrito é uma promessa sem prova"
    - "LEIT-05 (recorte da coluna do nome) ↔ critério 1 da Fase 2 (o texto da tooltip nunca vira nome de item) — a defesa medida contra o vazamento do Resultado 3 da evidência"
---

<objective>
Mover a fronteira de escopo do milestone v1-mercado: a leitura de nomes de item por OCR
volta a ser escopo, e a lista pré-configurada deixa de ser o portão do que é registrado.

O usuário declarou em 2026-08-29 que quer acompanhar TODOS os itens do mercado, e que item
novo postado precisa ser reconhecido SEM mapeamento prévio. Isso derruba a premissa sobre a
qual LEIT-01 foi escrito — e a exclusão que o sustentava nunca teve medição por baixo: ela
nasceu em `db751c7` (2026-08-27) e a primeira gravação do World Exchange é de
2026-08-28 05:31.

Purpose: Deixar os documentos de escopo dizendo a verdade ANTES de a Fase 2 ser planejada.
Planejar a Fase 2 contra um LEIT-01 que já morreu produziria uma fase inteira construindo
o portão errado.

Output: `REQUIREMENTS.md` e `ROADMAP.md` do workstream mercado, e nada mais.

**Esta tarefa NÃO implementa o OCR.** Isso é a Fase 2, e será planejada por
`/gsd-plan-phase` depois. Aqui só a fronteira se move.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/REQUIREMENTS.md
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/quick/260829-rd9-ocr-de-nomes-de-item-entra-no-escopo-lei/260829-rd9-EVIDENCIA-SPIKE-OCR.md
</context>

<verificacoes_ja_feitas>
Feitas no planejamento, com o comando ao lado. NÃO refaça — use o resultado.

1. **A Fase 3 do ROADMAP não depende da identidade do item.**
   `sed -n '/### Phase 3/,/### Phase 4/p' ROADMAP.md | grep -i "item\|watchlist\|nome"` → **zero linhas.**
   O critério 1 da Fase 3 fala de carimbo do relógio, preço total, quantidade e centésimos —
   nunca de nome nem de chave de lista. **Portanto a Fase 3 NÃO é editada.** Está no plano
   como gate negativo, não como tarefa.

2. **Onde a lista aparece hoje, e o veredito de cada ocorrência:**
   - `REQUIREMENTS.md:27` (LEIT-01) → **MUDA** (é o objeto da tarefa)
   - `REQUIREMENTS.md:111` (linha da tabela Out of Scope) → **SAI**
   - `REQUIREMENTS.md:89` (ANAL-01) → **NÃO MUDA** — é o papel que a lista CONTINUA tendo
   - `REQUIREMENTS.md:101` (WAPP-02, v2) → **NÃO MUDA** — mesma razão
   - `ROADMAP.md:24` (resumo da Fase 2) → **MUDA** — ver nota de julgamento abaixo
   - `ROADMAP.md:38` (critério 2 da Fase 1) → **NÃO MUDA** — registro histórico de uma
     spike já respondida e validada, sobre variantes de encanto
   - `ROADMAP.md:72` (Goal da Fase 2) → **MUDA**
   - `ROADMAP.md:77` (critério 1 da Fase 2) → **MUDA**
   - `ROADMAP.md:115` (critério 3 da Fase 4 / ANAL-01) → **NÃO MUDA**

3. **Nota de julgamento do planejador sobre `ROADMAP.md:24`.** A instrução da tarefa lista
   Goal, critérios 1 e 2, Requirements e Coverage. Não lista o resumo de uma linha da Fase 2
   na lista `## Phases`. Ele **é** incluído, porque é o eco em uma linha da Goal e hoje abre
   anunciando a lista pré-configurada como a matéria da fase — deixá-lo intacto contraria
   diretamente a exigência de que o texto não sugira mais que a lista é o portão. Se o
   revisor discordar, é o único item removível sem quebrar nenhum outro.

4. **A recusa por colisão de nomes é fato registrado, não suposição.**
   `01-04-PLAN.md:30` (must_have): *"A calibracao recusa ALTO quando dois templates da
   watchlist colidem: a matriz de confusao e medida na hora e o pior score inter-classe fica
   registrado"*. É a âncora do argumento estrutural — com o mercado inteiro, o conjunto
   pré-configurado não fica só trabalhoso: ele PARA, por causa da salvaguarda funcionando
   corretamente.

5. **Ambos os arquivos estão limpos em HEAD** (`git diff --stat HEAD -- .planning/workstreams/mercado/` → vazio),
   o que é o que torna os gates de deriva por `git show HEAD:` confiáveis.
</verificacoes_ja_feitas>

<numeros_permitidos>
Todo número no texto novo sai daqui. **Nenhum outro é autorizado.** Precisou de um que não
está nesta lista? Escreva que não está, não invente.

| Número / literal | Fonte |
|---|---|
| `db751c7`, 2026-08-27 | evidência, cabeçalho do bloco de citação |
| 2026-08-28 05:31 (primeira gravação do World Exchange) | evidência, mesmo bloco |
| 2026-08-29 (a declaração do usuário; a medição) | evidência, `**Medido em:**` |
| `16,50` lido como `1650` | evidência, Resultado 2 |
| `Common Aztac` e `Common Aztac M. Def. +200` | evidência, Resultado 1 |
| painel localizado em 6 de 6 frames | evidência, Metodologia |
| `20260828-061253-mercado-tooltip/frame_000010.png` | evidência, Resultado 3 |
| `Can be stored in the private warehouse` | evidência, Resultado 3 |
| 18 requisitos v1 (FIRE 1, FUND 3, DETC 2, LEIT 5, PERS 3, ANAL 4) | aritmética desta mudança |

**Proibido** citar `0.7110`, `0.8554` ou o número de moldes de dígito: eles pertencem ao
argumento de LEIT-02, que não muda, e arrastá-los para cá só cria superfície de erro.
</numeros_permitidos>

<redacoes_alvo>
Redações alvo. O executor pode refinar o português, desde que os gates continuem passando e
a substância não encolha.

**R1 — LEIT-01 (substitui `REQUIREMENTS.md:27`)**

```
- [ ] **LEIT-01**: Nome do item lido por OCR sobre o recorte da coluna do nome e agrupado por similaridade contra os nomes já vistos; um nome que não casa com nenhum conhecido entra como SÉRIE NOVA, sem intervenção do usuário — o que é registrado não depende de lista prévia
```

**R2 — LEIT-05 (novo, logo depois de LEIT-04 na seção Leitura)**

```
- [ ] **LEIT-05**: A leitura do nome usa o recorte da COLUNA DO NOME, nunca a linha inteira — a coluna é calibrada e persistida em `calibration.json`. Medido: com a tooltip aberta, o texto dela vaza para dentro da linha e viraria nome de item
```

**R3 — bloco em prosa (fim da seção Leitura, antes de `### Persistência`)**

Mesma disciplina de `#### Por que CSV, e por que a decisão mudou duas vezes`: preserva o
raciocínio derrubado em vez de apagá-lo, e ancora na medição. Cinco coisas, nesta ordem:

1. **De premissa.** A exclusão nasceu em `db751c7` (2026-08-27); a primeira gravação do
   World Exchange é de 2026-08-28 05:31. O OCR foi descartado um dia antes de existir um
   frame do mercado no disco. A justificativa escrita era uma decisão de PRODUTO registrada
   como se fosse um achado TÉCNICO. Cite a linha removida como a evidência a cita — com
   reticências no lugar do motivo, e **nunca** reproduzida como linha de tabela começando a
   coluna no início da linha.
2. **O que caiu.** Não foi uma medição: foi a premissa. Em 2026-08-29 o usuário declarou que
   quer acompanhar TODOS os itens do mercado, e que item novo postado precisa ser
   reconhecido sem mapeamento prévio.
3. **Estrutural.** A calibração recusa ALTO quando dois moldes de nome colidem — proteção
   deliberada, registrada como must_have em `01-04-PLAN.md`. Nomes do L2 compartilham
   prefixo em profusão, e isso foi lido na própria página do usuário: `Common Aztac` e
   `Common Aztac M. Def. +200`. Com o mercado inteiro, o conjunto pré-configurado não fica
   só trabalhoso — ele para de funcionar, e por causa da salvaguarda funcionando
   corretamente.
4. **O que a medição mostrou, e o que ela recusou.** Nomes são lidos de forma estável;
   números não (`16,50` vira `1650`). Aponte para
   `260829-rd9-EVIDENCIA-SPIKE-OCR.md` e diga a conclusão travada em uma linha: nome por
   OCR, preço e quantidade por molde — LEIT-02 não muda.
5. **O que sobra da lista configurada.** Ela deixa de decidir O QUE É REGISTRADO. No máximo
   vira um filtro de DESTAQUE no console, que é território da Fase 4 (ANAL-01, ANAL-02).
   Diga isso explicitamente e **não crie requisito novo para isso aqui**.

**R4 — nota sob a tabela Out of Scope (substitui a linha removida, fora da tabela)**

```
> A entrada que excluía a leitura de nomes por OCR saiu desta tabela em 2026-08-29. Ela não foi apagada: o argumento inteiro, e o que o derrubou, está em "Por que o OCR de nomes voltou ao escopo", na seção Leitura. Uma exclusão que cai também não pode cair em silêncio.
```

**R5 — Goal da Fase 2 (`ROADMAP.md:72`)**

```
**Goal**: Contra as fixtures gravadas na Fase 1, o scanner lê por OCR o nome de cada linha visível, agrupa por similaridade e abre série nova para o desconhecido — e lê preços/quantidades por molde de dígito, sem nunca inventar um número.
```

**R6 — critério 1 da Fase 2 (`ROADMAP.md:77`)**

```
  1. Rodando o replay contra as fixtures, cada linha visível tem seu nome lido por OCR sobre o recorte da COLUNA DO NOME — nunca a linha inteira — e agrupado por similaridade contra os nomes já vistos; um item que o usuário nunca viu antes aparece como SÉRIE NOVA sem que ele configure nada, e com a tooltip aberta o texto dela não vira nome de item nem cria série fantasma
```

**R7 — critério 2 da Fase 2 (`ROADMAP.md:78`)**

Mantém a exigência de hoje inteira, palavra por palavra, e ACRESCENTA a barreira medida:

```
  2. Os preços e quantidades lidos batem dígito a dígito com o que o usuário vê no frame — separador de milhar tratado como glifo de primeira classe; quando o frame está ilegível, a linha aparece como descartada, nunca como um número plausível. O número NUNCA vem do motor de OCR que lê o nome: medido contra as gravações da Fase 1, ele perde a vírgula decimal e devolve `1650` onde a tela diz `16,50` — erro de 100x com aparência plausível, exatamente o que LEIT-02 existe para impedir
```

**R8 — resumo da Fase 2 (`ROADMAP.md:24`)**

```
- [ ] **Phase 2: Leitura de página** - Nome por OCR agrupado por similaridade, dígitos por molde e estabilizador de página contra as fixtures da Fase 1 — falha sempre fechada, preço nunca inventado
```

**R9 — Requirements da Fase 2 (`ROADMAP.md:74`)**

```
**Requirements**: LEIT-01, LEIT-02, LEIT-03, LEIT-05
```

**R10 — Coverage do ROADMAP (`ROADMAP.md:135` e a linha `| Leitura |`)**

```
Todas as 18 exigências v1 mapeadas (a definição dizia 16; a recontagem na criação do roadmap achou 17; LEIT-05 entrou em 2026-08-29, quando o OCR de nomes voltou ao escopo — FIRE 1, FUND 3, DETC 2, LEIT 5, PERS 3, ANAL 4), cada uma em exatamente uma fase:
```

```
| Leitura | LEIT-01, LEIT-02, LEIT-03, LEIT-05 | 2 |
```

**R11 — Traceability e Coverage do REQUIREMENTS**

```
| LEIT-05 | Phase 2 | Pending |
```

```
- v1 requirements: 18 total (a contagem "16" da definição inicial estava errada — a recontagem na criação do roadmap deu 17; LEIT-05 entrou em 2026-08-29)
- Mapped to phases: 18
- Unmapped: 0 ✓
```
</redacoes_alvo>

<tasks>

<task type="tracer">
  <name>Task 1: A fronteira se move — o nome passa a vir por OCR, nos dois documentos</name>
  <files>.planning/workstreams/mercado/REQUIREMENTS.md, .planning/workstreams/mercado/ROADMAP.md</files>
  <precondition>Os dois documentos estão sem modificações pendentes em relação a HEAD — `git diff --quiet HEAD -- .planning/workstreams/mercado/REQUIREMENTS.md .planning/workstreams/mercado/ROADMAP.md` sai com código 0. Se sair diferente de 0, PARE: há trabalho de outra pessoa no meio, e os gates de deriva desta tarefa vão medir a coisa errada.</precondition>
  <reversibility rating="reversible">Edição de texto em dois documentos versionados; `git revert` do commit desfaz por completo. A decisão de produto por baixo já foi tomada pelo usuário em 2026-08-29 — o que esta tarefa faz é registrá-la.</reversibility>
  <action>
Esta é a fatia fina que atravessa tudo: depois dela, quem abrir QUALQUER um dos dois
documentos já lê a fronteira nova. A escrituração (bloco em prosa, tabela Out of Scope,
contagens) é a Task 2.

No `REQUIREMENTS.md`, seção Leitura: aplique R1 no lugar da linha 27, e insira R2 logo
depois de LEIT-04. LEIT-02 (linha 28) e LEIT-03 (linha 29) NÃO são tocados — nem uma
vírgula, nem um espaço; o gate compara a linha inteira com a de hoje. A razão de LEIT-02
ser intocável está medida na evidência, Resultado 2: o motor perde a vírgula decimal, então
o número é justamente a metade que ele não pode ler.

No `ROADMAP.md`, bloco `### Phase 2`: aplique R5 na Goal, R6 no critério 1, R7 no critério 2
e R9 na linha `**Requirements**`. O critério 3 fica intocado. R7 preserva o texto de hoje
inteiro e só acrescenta — se alguma exigência de hoje sumir ou enfraquecer, a tarefa falhou.
A Goal e o critério 1 deixam de prometer que o reconhecimento é restrito à lista do
`config.toml`; passam a descrever leitura por OCR mais agrupamento por similaridade, com o
desconhecido virando série nova.

Não toque em `### Phase 1`, `### Phase 3` nem `### Phase 4`. A Fase 3 já foi verificada e
não depende da identidade do item — está em `<verificacoes_ja_feitas>`, item 1.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
R=.planning/workstreams/mercado/REQUIREMENTS.md
M=.planning/workstreams/mercado/ROADMAP.md
set -e
# o repo tem autocrlf ligado: um \r no fim da linha quebraria todo grep -x e todo diff
# abaixo, com falha que parece defeito de conteudo e nao e. Normalizado em cada leitura.

# --- LEIT-01: reescrito (positivo) e sem a redacao antiga (negativo, escopado A LINHA) ---
L1=$(grep -n '^- \[ \] \*\*LEIT-01\*\*' "$R" | cut -d: -f1); test -n "$L1"
LINE1=$(sed -n "${L1}p" "$R" | tr -d '\r')
printf '%s' "$LINE1" | grep -qi 'OCR'
printf '%s' "$LINE1" | grep -qi 'similaridade'
printf '%s' "$LINE1" | grep -qi 'nova'
test "$(printf '%s' "$LINE1" | grep -cF 'template de conjunto fechado')" = 0

# --- LEIT-05: existe, fala de coluna, tooltip e calibration.json ---
L5=$(grep -n '^- \[ \] \*\*LEIT-05\*\*' "$R" | cut -d: -f1); test -n "$L5"
LINE5=$(sed -n "${L5}p" "$R" | tr -d '\r')
printf '%s' "$LINE5" | grep -qi 'coluna'
printf '%s' "$LINE5" | grep -qi 'tooltip'
printf '%s' "$LINE5" | grep -qF 'calibration.json'

# --- LEIT-02 e LEIT-03: byte a byte identicos ---
# (o -e e obrigatorio: o padrao comeca com '-' e sem ele o grep o le como opcao)
tr -d '\r' < "$R" | grep -qxF -e '- [ ] **LEIT-02**: Preços e quantidades lidos por template-por-dígito com falha FECHADA: frame ilegível é descartado, preço nunca é inventado'
tr -d '\r' < "$R" | grep -qxF -e '- [ ] **LEIT-03**: Página só é aceita quando dois frames consecutivos concordam nas linhas PARSEADAS (nunca em pixels — frames bit a bit idênticos são o sinal de captura congelada)'

# --- Fase 2 do ROADMAP ---
F2=$(tr -d '\r' < "$M" | sed -n '/^### Phase 2:/,/^### Phase 3:/p')

GOAL=$(printf '%s\n' "$F2" | grep '^\*\*Goal\*\*')
printf '%s' "$GOAL" | grep -qi 'OCR'
printf '%s' "$GOAL" | grep -qi 'similaridade'
test "$(printf '%s' "$GOAL" | grep -cF 'itens da watchlist')" = 0

printf '%s\n' "$F2" | grep '^\*\*Requirements\*\*' | grep -qF 'LEIT-05'

C1=$(printf '%s\n' "$F2" | grep '^  1\.')
printf '%s' "$C1" | grep -qi 'OCR'
printf '%s' "$C1" | grep -qi 'similaridade'
printf '%s' "$C1" | grep -qi 'coluna'
printf '%s' "$C1" | grep -qi 'nova'
test "$(printf '%s' "$C1" | grep -ciF 'conjunto fechado')" = 0

C2=$(printf '%s\n' "$F2" | grep '^  2\.')
printf '%s' "$C2" | grep -qF 'dígito a dígito'
printf '%s' "$C2" | grep -qF 'separador de milhar tratado como glifo de primeira classe'
printf '%s' "$C2" | grep -qF 'nunca como um número plausível'
printf '%s' "$C2" | grep -qi 'OCR'
printf '%s' "$C2" | grep -qF '16,50'
printf '%s' "$C2" | grep -qF '1650'

printf '%s\n' "$F2" | grep -qxF '  3. Uma página só é aceita quando dois frames consecutivos concordam nas linhas PARSEADAS; frames bit a bit idênticos são reportados como captura congelada, não aceitos como acordo'

echo "TASK 1 OK"
    </automated>
  </verify>
  <done>
LEIT-01 descreve leitura de nome por OCR, agrupamento por similaridade e série nova para o
desconhecido, e não contém mais a redação antiga de casamento restrito. LEIT-05 existe e
prende a leitura ao recorte da coluna do nome, calibrado em `calibration.json`. LEIT-02 e
LEIT-03 saem idênticos ao que eram, provado por comparação de linha inteira. A Fase 2 do
ROADMAP tem Goal, critério 1 e linha de Requirements alinhados com o desenho novo; o
critério 2 mantém intacta cada exigência de hoje e ganha a barreira medida contra o número
vindo do OCR; o critério 3 sai idêntico.
  </done>
</task>

<task type="auto">
  <name>Task 2: A escrituração — o argumento derrubado fica registrado, e a aritmética fecha</name>
  <files>.planning/workstreams/mercado/REQUIREMENTS.md, .planning/workstreams/mercado/ROADMAP.md</files>
  <action>
No `REQUIREMENTS.md`:

Remova da tabela Out of Scope a entrada que excluía a leitura de nomes de item (linha 111
hoje). As outras quatro entradas ficam. Logo abaixo da tabela, acrescente R4 — a remoção
tem de aparecer, porque a própria seção existe para impedir movimento silencioso de escopo,
e isso vale nas duas direções.

No fim da seção Leitura, antes de `### Persistência`, escreva o bloco R3 com o título
`#### Por que o OCR de nomes voltou ao escopo`. Ele fica ao lado dos requisitos que governa,
que é exatamente onde `#### Por que CSV, e por que a decisão mudou duas vezes` vive nesta
mesma seção — e adota a mesma disciplina: guarda o argumento superado em vez de apagá-lo.
Todo número sai de `<numeros_permitidos>`. Ao citar a entrada removida, faça como a
evidência faz — com reticências no lugar do motivo — e nunca reproduza a linha de tabela
começando a coluna no começo da linha, ou você invalida o gate desta própria tarefa.

Acrescente R11 à tabela Traceability, logo depois da linha de LEIT-04, e atualize o bloco
Coverage para 18.

No `ROADMAP.md`: aplique R8 no resumo da Fase 2 na lista `## Phases`, e R10 na seção
Coverage — a prosa da contagem e a linha `| Leitura |` da tabela. A prosa preserva o
histórico das recontagens anteriores; ela só ganha o motivo novo.

Nada de ANAL-01, WAPP-02, do critério 2 da Fase 1 nem da Fase 3. Já verificado — a Fase 3
não menciona identidade de item em lugar nenhum.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
R=.planning/workstreams/mercado/REQUIREMENTS.md
M=.planning/workstreams/mercado/ROADMAP.md
set -e

# --- a entrada saiu da tabela Out of Scope (negativo, escopado A SECAO e ancorado no inicio da linha) ---
OOS=$(tr -d '\r' < "$R" | sed -n '/^## Out of Scope/,/^## Traceability/p')
test "$(printf '%s\n' "$OOS" | grep -c '^| OCR aberto')" = 0
# as outras quatro continuam
printf '%s\n' "$OOS" | grep -qF 'Paginação, refresh ou busca automática no mercado'
printf '%s\n' "$OOS" | grep -qF 'Ler o "preço médio" do jogo como fonte primária'
printf '%s\n' "$OOS" | grep -qF 'Histórico de VENDAS'
printf '%s\n' "$OOS" | grep -qF 'Varredura do mercado inteiro'
# a remocao nao foi silenciosa
printf '%s\n' "$OOS" | grep -qi 'voltou ao escopo'

# --- o bloco em prosa existe, esta DENTRO da secao Leitura, e cita o que tem de citar ---
LEIT=$(tr -d '\r' < "$R" | awk '/^### Leitura/{p=1} /^### Persistência/{p=0} p')
printf '%s\n' "$LEIT" | grep -qxF '#### Por que o OCR de nomes voltou ao escopo'
printf '%s\n' "$LEIT" | grep -qF 'db751c7'
printf '%s\n' "$LEIT" | grep -qF '2026-08-27'
printf '%s\n' "$LEIT" | grep -qF '2026-08-28 05:31'
printf '%s\n' "$LEIT" | grep -qF '260829-rd9-EVIDENCIA-SPIKE-OCR.md'
printf '%s\n' "$LEIT" | grep -qF 'Common Aztac M. Def. +200'
printf '%s\n' "$LEIT" | grep -qF '01-04-PLAN.md'
printf '%s\n' "$LEIT" | grep -qF 'LEIT-02'
printf '%s\n' "$LEIT" | grep -qi 'destaque'
printf '%s\n' "$LEIT" | grep -qi 'Fase 4'
# numeros proibidos nao vazaram para o bloco novo
test "$(printf '%s\n' "$LEIT" | grep -cE '0\.7110|0\.8554')" = 0

# --- traceability e contagens do REQUIREMENTS ---
grep -qE '^\| LEIT-05 \| Phase 2 \| Pending \|' "$R"
grep -qF 'v1 requirements: 18 total' "$R"
grep -qF 'Mapped to phases: 18' "$R"
grep -qF 'Unmapped: 0' "$R"

# --- ROADMAP: resumo da Fase 2, coverage e contagem ---
B2=$(grep '^- \[ \] \*\*Phase 2:' "$M")
printf '%s' "$B2" | grep -qi 'OCR'
test "$(printf '%s' "$B2" | grep -cF 'Watchlist, dígitos e estabilizador')" = 0
grep -qF '18 exigências v1' "$M"
grep -qF 'LEIT 5' "$M"
grep -qE '^\| Leitura \| LEIT-01, LEIT-02, LEIT-03, LEIT-05 \| 2 \|' "$M"
grep -qE '^\| Leitura \| LEIT-04 \| 4 \|' "$M"

echo "TASK 2 OK"
    </automated>
  </verify>
  <done>
A tabela Out of Scope não exclui mais a leitura de nomes por OCR, mantém as outras quatro
entradas, e a remoção está anotada logo abaixo dela apontando para o bloco novo. O bloco
`#### Por que o OCR de nomes voltou ao escopo` vive na seção Leitura, reconstrói o argumento
derrubado, ancora nas duas datas (`db751c7` 2026-08-27 contra a primeira gravação em
2026-08-28 05:31), aponta para o arquivo de evidência, carrega o argumento estrutural da
colisão de nomes com o par medido, e diz explicitamente que a lista configurada deixa de ser
o portão e vira no máximo filtro de destaque na Fase 4. A contagem de requisitos v1 diz 18
nos quatro lugares onde ela aparece.
  </done>
</task>

<task type="auto">
  <name>Task 3: O portão — provar que foi docs-only e que nada mais derivou</name>
  <files>.planning/workstreams/mercado/REQUIREMENTS.md, .planning/workstreams/mercado/ROADMAP.md</files>
  <action>
Nenhuma edição de conteúdo aqui, a menos que um gate falhe. Esta tarefa é o portão que a
mudança de escopo merece: uma edição de documento que passa sem prova é exatamente como um
requisito órfão entra no projeto.

Rode os gates. Se algum falhar, conserte a causa nos dois documentos — nunca afrouxando o
gate. Depois escreva o SUMMARY em
`.planning/workstreams/mercado/quick/260829-rd9-ocr-de-nomes-de-item-entra-no-escopo-lei/260829-rd9-SUMMARY.md`,
registrando: o que mudou, o que deliberadamente NÃO mudou (Fase 3, Fase 1, ANAL-01, WAPP-02,
LEIT-02, LEIT-03) e por quê, e a pendência que esta tarefa deixa aberta de propósito — o
todo do `STATE.md` que manda definir a lista no `config.toml` "porque a Fase 2 depende dela"
ficou desatualizado com esta mudança, e o `STATE.md` está fora do escopo desta tarefa.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
R=.planning/workstreams/mercado/REQUIREMENTS.md
M=.planning/workstreams/mercado/ROADMAP.md
set -e

# --- FIREWALL DOCS-ONLY: nenhum codigo, nenhum teste ---
CHANGED=$(git diff --name-only HEAD)
test "$(printf '%s\n' "$CHANGED" | grep -c '\.py$')" = 0
test "$(printf '%s\n' "$CHANGED" | grep -c '^tests/')" = 0
test "$(printf '%s\n' "$CHANGED" | grep -c '^l2scanner/')" = 0
# so os dois documentos e os artefatos da propria quick task
test "$(printf '%s\n' "$CHANGED" | grep -v '^$' | grep -vE '^\.planning/workstreams/mercado/(REQUIREMENTS|ROADMAP)\.md$' | grep -vcE '^\.planning/workstreams/mercado/quick/260829-rd9-' || true)" = 0

# --- DERIVA: fases 1, 3 e 4 do ROADMAP byte a byte identicas a HEAD ---
# tr -d '\r' nos DOIS lados: git show entrega o blob (LF) e a copia de trabalho pode estar
# em CRLF por autocrlf — sem isso o diff acusa a ARQUIVO INTEIRO como alterado.
for N in 1 3 4; do
  diff <(git show HEAD:.planning/workstreams/mercado/ROADMAP.md | tr -d '\r' | awk -v n="$N" '/^## /{p=0} /^### Phase /{p=($0 ~ "^### Phase " n ":")} p') \
       <(tr -d '\r' < "$M" | awk -v n="$N" '/^## /{p=0} /^### Phase /{p=($0 ~ "^### Phase " n ":")} p')
done

# --- DERIVA: no REQUIREMENTS, tudo antes da secao Leitura e tudo de Persistencia ate Out of Scope, identicos ---
diff <(git show HEAD:.planning/workstreams/mercado/REQUIREMENTS.md | tr -d '\r' | awk '/^### Firewall de escopo/{p=1} /^### Leitura/{p=0} p') \
     <(tr -d '\r' < "$R" | awk '/^### Firewall de escopo/{p=1} /^### Leitura/{p=0} p')
diff <(git show HEAD:.planning/workstreams/mercado/REQUIREMENTS.md | tr -d '\r' | awk '/^### Persistência/{p=1} /^## Out of Scope/{p=0} p') \
     <(tr -d '\r' < "$R" | awk '/^### Persistência/{p=1} /^## Out of Scope/{p=0} p')

# --- ARITMETICA: 18 bullets v1, 18 linhas de traceability ---
BULLETS=$(awk '/^## v1 Requirements/{p=1} /^## v2 Requirements/{p=0} p' "$R" | grep -cE '^- \[[ x]\] \*\*[A-Z]+-[0-9]+\*\*')
test "$BULLETS" = 18
ROWS=$(grep -cE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| (Complete|Pending) \|' "$R")
test "$ROWS" = 18

# --- todo ID do bloco v1 aparece na traceability, e vice-versa ---
diff <(awk '/^## v1 Requirements/{p=1} /^## v2 Requirements/{p=0} p' "$R" | grep -oE '\*\*[A-Z]+-[0-9]+\*\*' | tr -d '*' | sort) \
     <(grep -oE '^\| [A-Z]+-[0-9]+ \|' "$R" | tr -d '| ' | sort)

# --- todo requisito de LEIT esta em exatamente uma fase do coverage do ROADMAP ---
grep -qE '^\| Leitura \| LEIT-01, LEIT-02, LEIT-03, LEIT-05 \| 2 \|' "$M"
grep -qE '^\| Leitura \| LEIT-04 \| 4 \|' "$M"

echo "TASK 3 OK — docs-only, sem deriva, aritmetica fechada"
    </automated>
  </verify>
  <done>
`git diff --name-only HEAD` lista exatamente os dois documentos mais os artefatos desta quick
task — nenhum `.py`, nada em `tests/`, nada em `l2scanner/`. As Fases 1, 3 e 4 do ROADMAP e
as seções Firewall/Fundação/Detecção e Persistência/Análise do REQUIREMENTS saem byte a byte
idênticas a HEAD. O bloco v1 tem 18 requisitos, a Traceability tem 18 linhas, e os dois
conjuntos de IDs são iguais. O SUMMARY existe e registra o `STATE.md` desatualizado como
pendência deixada de propósito.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Mudança docs-only: nenhum código executa, nenhuma entrada é analisada, nenhum dado atravessa processo. As fronteiras do sistema saem idênticas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-rd9-01 | Tampering | fronteira de escopo em REQUIREMENTS.md | medium | mitigate | Um escopo que se alarga sem registro é a forma mais barata de o projeto passar a construir outra coisa. Mitigado pelo bloco em prosa obrigatório (gate da Task 2 exige as duas datas, o arquivo de evidência e o par de nomes medido) e pela nota sob a tabela Out of Scope. |
| T-rd9-02 | Repudiation | contagem de requisitos | medium | mitigate | Um requisito novo sem linha na Traceability é indistinguível de um requisito esquecido. Mitigado pelo gate da Task 3, que compara os conjuntos de IDs dos dois lados e exige 18 nas duas contagens. |
| T-rd9-03 | Tampering | LEIT-02 (falha fechada do número) | high | mitigate | Se a volta do OCR arrastar o número junto, o projeto ganha um erro de 100x com aparência plausível (`16,50` → `1650`). Mitigado por `grep -qxF` da linha inteira de LEIT-02 e por gates positivos que exigem que o critério 2 da Fase 2 preserve cada exigência de hoje. |
| T-rd9-04 | Elevation of Privilege | firewall docs-only | low | mitigate | Gate `git diff --name-only HEAD` sem nenhum `.py`, `tests/` ou `l2scanner/`. |
| T-rd9-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalação de pacote nesta tarefa — não há árvore de dependências tocada, então a auditoria de legitimidade não se aplica. |
</threat_model>

<verification>
Rode os três blocos `<automated>` em ordem. Nenhum leva mais que segundos e nenhum depende
do `.venv` — são só `git`, `grep`, `sed`, `awk` e `diff`.

Sinal de "a tarefa foi bem feita" que nenhum gate mede sozinho: abra o `REQUIREMENTS.md` e
leia a seção Leitura de cima a baixo. Ela tem de contar uma história completa — o que o
sistema lê, como, e por que a decisão anterior caiu — sem que o leitor precise abrir o
arquivo de evidência para entender. O arquivo de evidência é a prova, não o enredo.
</verification>

<success_criteria>
- LEIT-01 reescrito: nome por OCR, agrupamento por similaridade, série nova para o desconhecido
- LEIT-05 criado, prendendo a leitura ao recorte da coluna do nome, calibrado e persistido
- LEIT-02 e LEIT-03 idênticos, comparados por linha inteira
- Bloco `#### Por que o OCR de nomes voltou ao escopo` na seção Leitura, com as duas datas, o arquivo de evidência, o argumento estrutural da colisão e o novo papel da lista configurada
- Tabela Out of Scope sem a exclusão do OCR, com a remoção anotada e as outras quatro entradas intactas
- Fase 2 do ROADMAP alinhada: resumo, Goal, critérios 1 e 2, Requirements
- 18 requisitos v1 batendo em quatro lugares; LEIT-05 na Traceability e no Coverage
- Fases 1, 3 e 4 do ROADMAP e as seções não-Leitura do REQUIREMENTS byte a byte idênticas
- Zero `.py`, zero `tests/`, zero `l2scanner/` no diff
</success_criteria>

<output>
Create `.planning/workstreams/mercado/quick/260829-rd9-ocr-de-nomes-de-item-entra-no-escopo-lei/260829-rd9-SUMMARY.md` when done
</output>
