---
phase: 04-modo-mercado-an-lise-e-console
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - vigiar-mercado.bat
  - tests/test_vigiar_mercado_bat.py
  - config.toml
autonomous: true
requirements: [DETC-02, ANAL-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O usuario abre a terceira invocacao com dois cliques, sem digitar linha de comando"
    - "O `.bat` do mercado nao anuncia sucesso que nao conferiu — o precedente do `calibrar-mercado.bat` esta preso por teste"
    - "O `config.toml` ganha o `[[receita]]` COMENTADO no FIM do arquivo, sem uma linha existente ser tocada"
    - "Os tres testes que leem o `config.toml` REAL seguem verdes depois da edicao"
  artifacts:
    - vigiar-mercado.bat
    - tests/test_vigiar_mercado_bat.py
    - "bloco `[[receita]]` comentado no fim de config.toml"
  key_links:
    - "A edicao do `config.toml` e a ULTIMA task da fase, isolada e sequenciada por ultimo — o arquivo e disputado com o agente que trabalha em `tiat` nesta mesma arvore"
    - "Comentario nao altera o parse: o diff e aditivo e no fim do arquivo, onde conflito textual e improvavel"
---

<objective>
Fechar a fase: a terceira invocacao que o usuario abre com dois cliques, e o `[[receita]]`
comentado no `config.toml` — a unica edicao desta fase num arquivo disputado, feita por
ultimo e com o menor diff possivel.

Purpose: DETC-02 diz "terceira invocacao"; sem um `.bat` o usuario teria de digitar uma
linha de comando com `--janela` e `--mercado`, o que contradiz a doutrina de zero-ritual
que o `vigiar-party.bat` estabeleceu. E o `04-CONTEXT.md` exige o bloco `[[receita]]`
comentado no `config.toml`, num arquivo que outro agente esta editando nesta mesma arvore
agora — por isso ele e a ultima coisa que esta fase toca.

Output: `vigiar-mercado.bat`, seu teste, e o bloco comentado no fim do `config.toml`.

Requisitos cobertos: **DETC-02** (a terceira invocacao usavel) e **ANAL-04** (o formato do
`[[receita]]` documentado onde o usuario o escreve).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-CONTEXT.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-RESEARCH.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-04-SUMMARY.md
@./.claude/CLAUDE.md
</context>

<restricoes_da_fase>
1. **Nenhuma dependencia nova.** `requirements.txt` nao muda.
2. **NAO tocar** `rastreador.py`, `visao.py`, `respawn.py`, `bosses.py`, `agenda.py`,
   `sessao.py`, `tests/test_bosses.py`, `l2scanner/__main__.py`, `vigiar-party.bat`.
3. **`config.toml` e DISPUTADO com outro agente nesta arvore.** A Task 2 e a UNICA que o
   toca, ela e a ULTIMA da fase, e o diff e **aditivo, comentado, no FIM do arquivo**.
   Nenhuma linha existente muda.
4. **NUNCA commitar `calibration.json`.**
5. **Nenhuma comprehension nao-afirmante em bloco de verificacao** — `assert` de verdade.
6. **Sem travessao e sem acento em texto de erro.**
7. Pytest no **Python GLOBAL**. **FLAKE conhecido:** `tests/test_agenda.py:1141` levanta
   `KeyboardInterrupt` de proposito — **abortar nao e falhar**.
</restricoes_da_fase>

<tasks>

<task type="auto">
  <name>Task 1: `vigiar-mercado.bat` — a terceira invocacao com dois cliques</name>

  <read_first>
    - `vigiar-party.bat` INTEIRO — a busca do Python pelo CAMINHO COMPLETO (nunca o nome do
      comando), a recusa do stub da Microsoft Store, a montagem do `.venv` na primeira
      execucao, a conferencia de dependencias novas por import, e o `%*` que repassa
      argumentos. **Leia, nao edite: e o lancador da party e nao pode mudar.**
    - `calibrar-mercado.bat` — o outro lancador de mercado, e a forma dos blocos de erro.
    - `tests/test_calibrar_mercado_bat.py:1-30` — o precedente do teste de `.bat` e a razao
      dele existir: **o `.bat` nao pode anunciar sucesso que nao conferiu.** Foi medido em
      campo em 2026-08-28, com a ferramenta saindo em codigo 1 e o `.bat` mandando o usuario
      conferir um arquivo que nunca foi gerado.
    - `l2scanner/mercado_modo.py` — os codigos de saida do modo (2 para recusa de
      configuracao).
  </read_first>

  <files>vigiar-mercado.bat, tests/test_vigiar_mercado_bat.py</files>

  <action>
Crie `vigiar-mercado.bat` copiando a estrutura de `vigiar-party.bat`: a busca do Python
pelo caminho completo, a recusa do stub da Microsoft Store, a montagem do `.venv` na
primeira execucao, a conferencia de dependencias novas por import, e o `%*` no fim. **Nao
edite `vigiar-party.bat`** — ele e o lancador da party e nao pode ganhar risco por causa
desta fase.

A linha de execucao chama o modulo com `--janela` e `--mercado`, na mesma forma que o
lancador da party usa `--janela`. O cabecalho de comentario explica, para quem der dois
cliques, o que este programa E e o que ele NAO e:

- ele e a TERCEIRA invocacao, que roda **ao lado** das duas de party, nao no lugar delas;
- ele NAO vigia a party e NAO envia alerta nenhum;
- ele precisa da calibracao de mercado — sem ela o modo nao sobe, e diz o que falta e como
  calibrar;
- ele so coleta enquanto o painel do World Exchange estiver ABERTO na tela.

**O `.bat` nao pode anunciar o que nao conferiu.** Se ele imprimir qualquer bloco de
encerramento, esse bloco fica atras de uma checagem de `errorlevel` — o defeito medido em
campo no `calibrar-mercado.bat` foi exatamente imprimir o bloco de sucesso depois de a
ferramenta ter saido em codigo 1. O mais simples e nao imprimir bloco de sucesso nenhum: o
resumo da sessao ja sai do proprio programa, no `finally`.

Escreva `tests/test_vigiar_mercado_bat.py` no molde de `tests/test_calibrar_mercado_bat.py`
(leitura do texto do `.bat`, sem executa-lo): que o arquivo existe; que a linha de execucao
carrega as duas flags; que qualquer bloco impresso depois da execucao esta atras de uma
checagem de `errorlevel`; e que o `.bat` nao contem nenhuma flag de destino de arquivo — o
modo nao pode ganhar essa porta.
  </action>

  <verify>
    <automated>python -m pytest tests/test_vigiar_mercado_bat.py tests/test_calibrar_mercado_bat.py -x -q</automated>
    <automated>test -z "$(git diff --stat -- vigiar-party.bat)" || { echo "REPROVADO: vigiar-party.bat foi tocado"; exit 1; }</automated>
  </verify>

  <acceptance_criteria>
    - `test -f vigiar-mercado.bat` sai com codigo 0.
    - Um teste afirma com `assert` que o texto do `.bat` contem a chamada do modulo com as
      duas flags do modo mercado na MESMA linha.
    - Um teste afirma com `assert` que nenhuma linha de `echo` posterior a linha de execucao
      esta fora de um bloco condicionado por `errorlevel`. Se nao houver `echo` posterior, o
      teste afirma isso explicitamente com `assert` e passa por essa via.
    - Um teste afirma com `assert` que o texto do `.bat` nao contem nenhuma flag de destino
      de arquivo.
    - `test -z "$(git diff --stat -- vigiar-party.bat)" || { echo "REPROVADO: vigiar-party.bat foi tocado"; exit 1; }` sai com codigo 0 — o comando reprova sozinho.
    - `test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }` sai com codigo 0.
  </acceptance_criteria>

  <done>
    O usuario abre a terceira invocacao com dois cliques, o lancador da party continua
    intocado, e o `.bat` novo nao herda o defeito de anunciar sucesso nao conferido.
  </done>
</task>

<task type="auto">
  <name>Task 2 (A ULTIMA DA FASE): o bloco `[[receita]]` comentado, no FIM do `config.toml`</name>

  <precondition>
    Todas as tasks dos planos 04-01 a 04-04 e a Task 1 deste plano estao commitadas. Esta e
    a UNICA task da fase que toca `config.toml`, e ela roda por ultimo de proposito: o
    arquivo esta sendo editado por outro agente, no workstream `tiat`, nesta mesma arvore
    agora. Antes de editar, confira com `git status --porcelain config.toml` que nao ha
    modificacao pendente de terceiro no arquivo; se houver, PARE e reporte em vez de
    escrever por cima.
  </precondition>

  <read_first>
    - `config.toml:180-204` — a secao `[mercado] watchlist` COMENTADA. E o padrao literal
      que este bloco segue: um bloco de comentario explicativo seguido das linhas de exemplo
      tambem comentadas, formando uma unidade que se descomenta inteira.
    - `config.toml` ULTIMAS 25 LINHAS — o fim do arquivo hoje, para o bloco novo ser
      acrescentado DEPOIS e nao no meio.
    - `l2scanner/config.py` `ler_receitas` — escrito pelo plano 04-04; o exemplo comentado
      precisa parsear por essa funcao quando descomentado.
    - `04-CONTEXT.md` > "A margem de craft (ANAL-04)" — a secao nasce nesta fase, como
      `[[receita]]`, COMENTADA, no mesmo padrao da watchlist e dos blocos de evento.
    - `04-RESEARCH.md` secao 4.2 (a forma de tabela inline, verificada no `tomllib`) e 4.5
      (as limitacoes que vao no texto do comentario).
  </read_first>

  <files>config.toml</files>

  <action>
Acrescente ao **FIM** do `config.toml` um bloco novo, inteiramente COMENTADO, com a secao
`[[receita]]`. **Nenhuma linha existente do arquivo muda** — o diff e puramente aditivo, no
fim, onde conflito textual com o outro agente e improvavel. Comentario nao altera o parse,
entao o arquivo continua valendo exatamente o mesmo para todo mundo que o le hoje.

O bloco tem duas partes, no padrao da secao da watchlist:

**(a) O cabecalho explicativo, em comentario**, dizendo:
- o que a margem calcula: a diferenca entre o menor pedido visivel do produto (vezes o que
  a receita rende) e a soma dos menores pedidos visiveis dos componentes (vezes a
  quantidade de cada um);
- que os DOIS lados sao PEDIDOS visiveis no quadro, e nao transacoes — o scanner ve
  ofertas;
- que o numero e BRUTO: ele ignora a comissao do World Exchange e a chance de falha do
  craft;
- que a conta usa o unitario, entao ela nao lida com lote minimo — se o menor pedido de um
  componente for um lote de 100 e a receita pedir 20, quem compra leva 100;
- que a oferta pode ja ter sido comprada: o programa nao registra desaparecimento, entao
  nenhuma margem e acionavel sem conferir na tela;
- que o programa NAO conhece o crafting do jogo, entao uma receita errada produz um numero
  bem formatado e falso — a unica defesa e o usuario ter escrito a receita certo;
- que o nome de cada item precisa ser **exatamente** o que aparece na tela, com o prefixo
  de encanto quando houver, porque cada variante e uma serie de preco independente — e que
  um nome que casa com duas series quebra a margem listando as candidatas, em vez de
  escolher;
- que **sem nenhum bloco a margem simplesmente nao aparece**, e isso nao e erro nem aviso;
- que `rende` e OBRIGATORIO, e por que: um padrao silencioso produziria margem errada para
  quem crafta varios de cada vez.

**(b) O exemplo, comentado**, na forma de TABELA INLINE: o bloco `[[receita]]` com
`produto`, `rende` e `componentes` como lista de tabelas inline, cada uma com `item` e
`quantidade`. Use nomes de item do universo do jogo que ja aparecem no `config.toml` ou no
material do projeto, para o usuario reconhecer a forma. A forma alternativa com sub-blocos
tambem parseia, mas comentada ela vira quatro pedacos soltos que o usuario descomenta pela
metade sem perceber — por isso a inline.

**Depois de editar, rode os tres testes que leem o `config.toml` REAL**:
`tests/test_bosses.py`, `tests/test_mira_da_janela.py` e
`tests/test_calibracao_generica.py`. Eles sao a rede que prova que o arquivo continua
parseando e que nada existente foi tocado.
  </action>

  <reversibility rating="costly">
    O arquivo e disputado com outro agente trabalhando nesta mesma arvore agora. Um conflito
    aqui custa trabalho dos DOIS lados. A mitigacao e a forma do diff — aditivo, comentado,
    no fim — e o sequenciamento por ultimo, alem da checagem de `git status` na
    `<precondition>`.
  </reversibility>

  <verify>
    <automated>python -m pytest tests/test_bosses.py tests/test_mira_da_janela.py tests/test_calibracao_generica.py -x -q</automated>
    <automated>python -c "import tomllib, pathlib; tomllib.loads(pathlib.Path('config.toml').read_text(encoding='utf-8')); print('config.toml parseia')"</automated>
    <automated>test "$(git diff --numstat -- config.toml | cut -f2)" = "0" || { echo "REPROVADO: config.toml teve linhas removidas"; exit 1; }</automated>
  </verify>

  <acceptance_criteria>
    - `test "$(git diff --numstat -- config.toml | cut -f2)" = "0"` sai com codigo 0. O
      comando REPROVA sozinho, com codigo diferente de zero e mensagem, quando qualquer
      linha foi removida — a promessa de "zero remocoes" nao pode depender de o agente ler
      a saida de um `git diff --numstat` cru, que sai com codigo 0 tendo removido 0 ou 400
      linhas. **Este e o unico check da task que enxerga remocao:** o check de linhas
      acrescentadas logo abaixo e cego a remocoes por construcao, e nem o `tomllib.loads`
      nem os tres testes de config real pegariam a remocao de uma linha que eles nao leem.
      O arquivo esta sendo escrito por outro agente nesta arvore agora, e uma remocao
      silenciosa destroi trabalho de terceiro.
    - `python -c "import subprocess; d = subprocess.run(['git','diff','-U0','--','config.toml'], capture_output=True, text=True).stdout; ruins = [l for l in d.splitlines() if l.startswith('+') and not l.startswith('+++') and l[1:].strip() and not l[1:].lstrip().startswith('#')]; assert not ruins, ruins"` sai com codigo 0 — toda linha acrescentada esta comentada ou em branco.
    - `python -c "import tomllib, pathlib; tomllib.loads(pathlib.Path('config.toml').read_text(encoding='utf-8'))"` sai com codigo 0.
    - Um comando extrai o bloco de exemplo do fim do arquivo, remove o `#` de cada linha,
      passa o resultado por `tomllib.loads` e depois por `ler_receitas`, e afirma com
      `assert` que devolve exatamente uma receita com o `produto`, o `rende` e os
      componentes esperados. **O exemplo comentado precisa parsear quando descomentado** —
      um exemplo que nao roda e pior que nenhum exemplo.
    - `grep -c 'receita' config.toml` devolve numero maior que zero.
    - Os tres testes que leem o `config.toml` real seguem verdes. **Se
      `tests/test_bosses.py` falhar, confira primeiro se a falha e desta edicao ou do
      agente que trabalha em `tiat` nesta arvore:** aquele arquivo e dele, e `git diff --
      tests/test_bosses.py` responde em um comando. Ele esta nesta linha porque esta e a
      UNICA task da fase que edita o `config.toml`, e portanto a unica em que ele e rede de
      verdade.
    - `test -z "$(git diff --stat -- l2scanner/)" || { echo "REPROVADO: esta task tocou l2scanner/, e ela toca UM arquivo so"; exit 1; }` sai com codigo 0.
  </acceptance_criteria>

  <done>
    O `config.toml` ganhou o formato do `[[receita]]` documentado onde o usuario o escreve,
    com todas as limitacoes por escrito, sem uma unica linha existente tocada e sem quebrar
    os tres testes que leem o arquivo real.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.bat` -> interpretador | o `.bat` escolhe QUAL Python roda; um stub da Microsoft Store ou um `PATH` velho levariam ao interpretador errado |
| agente -> `config.toml` | arquivo compartilhado com outro agente trabalhando na mesma arvore agora |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Spoofing | stub da Microsoft Store respondendo por `python` | medium | mitigate | Copia da recusa ja escrita no `vigiar-party.bat`: substituicao de string nativa do cmd, sem depender do `find.exe`, que pode estar sombreado (Task 1) |
| T-04-24 | Repudiation | `.bat` anunciando sucesso que nao conferiu | medium | mitigate | Nenhum bloco de sucesso, ou bloco atras de checagem de `errorlevel`, com teste sobre o texto do `.bat` — o defeito foi MEDIDO em campo no `calibrar-mercado.bat` (Task 1) |
| T-04-25 | Tampering | edicao concorrente do `config.toml` por outro agente | medium | mitigate | Task isolada, sequenciada por ULTIMO na fase; diff aditivo e comentado no fim do arquivo; `<precondition>` confere `git status` antes de escrever; criterio de zero linhas removidas (Task 2) |
| T-04-26 | Tampering | exemplo comentado que nao parseia quando descomentado | medium | mitigate | Criterio que descomenta o bloco, passa por `tomllib.loads` e por `ler_receitas`, e afirma o conteudo (Task 2) |
| T-04-27 | Tampering | flag de destino de arquivo entrando pelo `.bat` | low | mitigate | Teste afirmando que o `.bat` nao contem flag de destino — a guarda que impede replay de contaminar `.mercado/` nao pode ser furada por esta porta (Task 1) |
| T-04-SC | Tampering | instalacao de pacote | low | accept | Esta fase nao instala nada; o `.bat` novo instala as MESMAS dependencias que o da party ja instala, a partir do `requirements.txt` inalterado. Ver `04-RESEARCH.md` > Package Legitimacy Audit |
</threat_model>

<verification>
1. `python -m pytest tests/test_vigiar_mercado_bat.py tests/test_bosses.py tests/test_mira_da_janela.py tests/test_calibracao_generica.py -x -q` verde.
2. `python -m pytest tests/ --ignore=tests/test_agenda.py -q` mantem o verde de referencia
   mais todos os testes acrescentados pela fase. `KeyboardInterrupt` vindo de
   `tests/test_agenda.py` **nao e falha**.
3. `test "$(git diff --numstat -- config.toml | cut -f2)" = "0" || { echo "REPROVADO: config.toml teve linhas removidas"; exit 1; }` sai com codigo 0.
4. `test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py vigiar-party.bat)" || { echo "REPROVADO: um arquivo proibido foi tocado"; exit 1; }` sai com codigo 0.
5. `test -z "$(git status --porcelain calibration.json)" || { echo "REPROVADO: calibration.json foi escrito"; exit 1; }` sai com codigo 0.
6. `test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }` sai com codigo 0.

   **Os quatro REPROVAM sozinhos**, com codigo diferente de zero e mensagem. Nesta fase
   isso importa mais que em qualquer outra: o `config.toml` esta sendo escrito por outro
   agente nesta arvore agora.

<human-check>
**O fecho da fase, e ele e do USUARIO.** Tres conferencias ao vivo, todas com o jogo aberto:

1. **DETC-02, o criterio 1 do ROADMAP.** Duas instancias de party mais `vigiar-mercado.bat`
   por 10 minutos com o World Exchange aberto. Criterio objetivo: o total de CPU dos tres
   processos fica dentro do aceitavel (o arranque do modo imprime o orcamento esperado);
   o `scanner.log` das duas instancias de party nao ganha nenhuma linha nova de falha de
   captura nem de imagem congelada; e nenhuma borda amarela aparece em volta da janela do
   jogo. **Contencao entre tres processos e propriedade do SISTEMA e nao do programa:
   nenhum teste automatizado a alcanca, e por isso este portao existe.**
2. **As duas conferencias humanas da Fase 2, que fecham naturalmente aqui:** o OCR real
   rodando DENTRO do tick (nao no replay), e o congelamento provocado — cobrir ou pausar a
   janela e ver o aviso aparecer, e sumir quando a janela volta.
3. **A margem de craft com uma receita de verdade**, descomentada do `config.toml`, sobre
   uma sessao ja coletada.

**E uma auditoria de decisao, nao de codigo.** As 20 decisoes do `04-CONTEXT.md` foram
tomadas por Claude, nao pelo usuario, sob autorizacao de execucao autonoma. Duas merecem
uma segunda olhada ao acordar: (a) **a watchlist virou filtro de DESTAQUE e nao porta de
entrada, o que contraria a LETRA do criterio 3 do ROADMAP** ("para cada item da
watchlist"); (b) **os pisos de evidencia `1`, `5` e `8` sao escolhas declaradas, nunca
medidas** — nenhum piso desse tipo foi medido neste projeto.
</human-check>
</verification>

<success_criteria>
- A terceira invocacao abre com dois cliques e nao anuncia nada que nao conferiu.
- O `config.toml` documenta o `[[receita]]` onde o usuario o escreve, com as limitacoes por
  escrito, e o exemplo parseia quando descomentado.
- Nenhuma linha existente do `config.toml` foi tocada, e o lancador da party ficou intocado.
- A suite inteira segue verde no Python global.
</success_criteria>

## Artifacts this phase produces

| Artefato | Novo? | O que e |
|---|---|---|
| `vigiar-mercado.bat` | novo | A terceira invocacao com dois cliques, no molde do lancador da party, sem anunciar sucesso nao conferido |
| `tests/test_vigiar_mercado_bat.py` | novo | Prende o defeito medido em campo: bloco de encerramento nao pode sair fora de checagem de `errorlevel` |
| Bloco `[[receita]]` comentado no fim de `config.toml` | novo | O formato documentado onde o usuario o escreve, com as cinco limitacoes da margem por escrito; diff aditivo, zero remocoes |

<output>
Create `.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-05-SUMMARY.md` when done
</output>
