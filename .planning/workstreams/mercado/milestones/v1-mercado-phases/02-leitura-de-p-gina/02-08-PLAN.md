---
phase: 02-leitura-de-p-gina
plan: 08
type: execute
wave: 7
depends_on: ["02-04", "02-06", "02-07"]
files_modified:
  - tools/medir_largura_de_run.py
  - tests/test_medir_largura_de_run.py
  - l2scanner/mercado_leitura.py
  - l2scanner/mercado_pagina.py
  - l2scanner/calibracao.py
  - tests/test_mercado_leitura.py
  - tests/test_mercado_pagina.py
  - tests/test_calibracao_mercado.py
  - tests/fixtures/mercado/calibracao_de_fixture.json
  - tests/fixtures/mercado/glifos_colados_quantidade_f078.png
  - tests/fixtures/mercado/glifos_colados_total_f105.png
  - tests/fixtures/mercado/glifos_colados_total_f054.png
autonomous: true
requirements: [LEIT-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um run mais largo que o MAIOR molde de um caractere NAO pode ser um glifo so, e hoje nada pergunta isso: `pontuar_glifos` casa o run inteiro contra um molde e devolve UM rotulo, com score e margem que atravessam as duas peneiras. O resultado e falha ABERTA — numero errado e plausivel — e nao a falha FECHADA que LEIT-02 exige. Este plano fecha essa porta"
    - "O LIMITE nao e um numero novo: ele e `max(largura)` sobre os moldes de UM caractere que ja moram no `calibration.json`, e por isso ele nao vai para o calibration.json. Gravar uma copia criaria DUAS VERDADES para uma so geometria — o mesmo argumento que `layout_confere` ja escreve sobre o `dx` do cabecalho — e na recalibracao seguinte a copia envelheceria contra os moldes que ela descreve"
    - "O limite cai dentro de um VALE MEDIDO E VAZIO de quatro niveis. Sondado pelo planejador no censo inteiro: das **7.105 celulas que a producao ACEITA hoje** nas tres colunas, **ZERO** tem algum run de 7, 8, 9 ou 10 px. As larguras aceitas sao {4, 5, 6} de um lado e {11, 12} do outro. Nao ha zona cinzenta a dividir, ha um vale — exatamente a forma do platô que ja justificou `VALOR_MINIMO_DO_SUFIXO = 120`"
    - "A CAUSA e geometrica e esta vista no pixel, nao deduzida: a barra horizontal do molde `4` ocupa as SEIS colunas da caixa dele. Dois glifos vizinhos passam a compartilhar a coluna de fronteira, a projecao de coluna nao acha coluna vazia, e `segmentar_glifos` — cuja regra e QUALQUER COLUNA VAZIA SEPARA — nao tem o que separar. Baixar o piso de brilho NAO resolve: sondado nos pisos 180, 177, 170 e 160, o `44` continua sendo um run so"
    - "A GUARDA sozinha ja converte falha ABERTA em FECHADA, e o custo dela esta medido e e pequeno: 69 de 1.135 linhas completas (6,08%) e **ZERO paginas perdidas** (155 paginas com pelo menos uma linha antes e depois). Mas ela e ESTRITAMENTE DOMINADA pela particao, e por isso ela nao e o produto deste plano — ela e o ramo de fallback DENTRO dele, o que a producao faz quando a particao nao acha corte que passe"
    - "A PARTICAO e viavel e foi medida contra DOIS rotulos nao circulares. Na coluna Quantity, contra a quantidade derivada de `Total` e `Unit price`: hoje 463 CERTO / **14 ERRADO** / 1.693 NAO LE; com a particao **477 CERTO / 0 ERRADO** / 1.693 NAO LE. Nas colunas de moeda, contra o intervalo que `Unit price x Quantity` implica quando as duas leem LIMPO: hoje **55 numeros INVENTADOS** de 59; com a particao **58 DENTRO do intervalo e 0 inventados**"
    - "A LARGURA DE CORTE DECIDE ENTRE CONSERTO E INVENCAO, e essa e a medicao mais importante do plano. O run de 11 px de `frame_000105` L6 admite DOIS cortes que passam nas duas peneiras: `6+5` da `4`(0,915) + `4`(0,470) e produz `144,44`; `7+4` da `4`(0,884) + `9`(0,791) e produz `149,44`. A aritmetica independente da linha (unitario `2,99`, quantidade `50`) exige o total no intervalo [149,25; 150,00): **`149,44` cabe e `144,44` nao**. Permitir toda largura de 1 a 6 escolhe o corte errado em **54 de 59 celulas**; permitir as larguras de molde MAIS a coluna de cola escolhe o certo em 58 de 59"
    - "A FOLGA DE COLA e o UNICO numero livre deste plano, e por isso ela e a unica que vai para o `calibration.json` — medida pela ferramenta, nunca escolhida pelo planejador. Ela conta quantas colunas a barra anti-serrilhada compartilha com o vizinho, e a ferramenta a varre com passo 1 e a propoe pelos MESMOS tres baldes do 02-07, recusando propor quando existe UMA leitura fora do rotulo"
    - "A AUSENCIA da chave e o comportamento SEGURO, e nao o de hoje: sem `mercado_folga_de_cola_do_glifo` a producao aplica a GUARDA e a celula com run largo cai FECHADA. So a presenca da chave medida liga a particao. Um recurso que degrada para MAIS seguro e o unico default aceitavel num caminho que decide preco"
    - "A guarda e a particao moram em `ler_glifos`, e a razao e de raio de alcance medido: `ler_glifos` tem **UM ponto de chamada de producao** (`mercado_leitura.py:563`, dentro de `ler_celula`) e ja possui as QUATRO coisas que a particao precisa — a mascara, os moldes, o piso e a margem — alem de ja ser dona da regra de TUDO OU NADA. `segmentar_glifos` tem **35 pontos de chamada** e nao tem molde nenhum: ela nao poderia decidir isto nem se quisesse"
    - "`segmentar_glifos` e `segmentar_glifos_no_brilho` NAO mudam de assinatura, e isso e afirmado por `inspect.signature` e nao por leitura de codigo — os 35 pontos de chamada em producao, ferramentas e testes continuam recebendo os mesmos runs"
    - "As celulas que hoje leem SEM run largo leem EXATAMENTE o mesmo depois, byte a byte, e isso e estrutural e nao estatistico: quando todo run cabe no limite, o caminho novo E o caminho antigo. Preso por valor sobre as fixturas versionadas que ja existem"
    - "O algoritmo e LINEAR e nao combinatorio, e a alternativa foi medida e descartada: a enumeracao de particoes passou de 10 MINUTOS no censo e foi abortada, enquanto a programacao dinamica sobre as posicoes de corte custa **11,7 ms por linha larga** — dentro de um tick de 1 Hz com folga de tres ordens de grandeza. Uma peneira que estoura o tick para o scanner de olhar a party, que e o unico defeito inaceitavel deste projeto"
    - "`VERSAO_DO_ESQUEMA` SEGUE EM 2 e a chave nova entra por `.get` opcional: os 13 moldes e as 3 ancoras do `calibration.json` so a mao do usuario produz, e eles ja se perderam uma vez"
    - "O conjunto de medicao sao as 8 gravacoes NOMEADAS do censo, IMPORTADAS da constante que ja existe — `recordings/` tem 16 pastas e a `pre-voo` sozinha tem 1.502 PNGs, que dominariam qualquer distribuicao"
  artifacts:
    - path: "tools/medir_largura_de_run.py"
      provides: "a varredura que mede a distribuicao de largura de run por classe de acerto, imprime o vale, quantifica o custo da guarda por celula/linha/pagina, varre a folga de cola nos tres baldes contra DOIS rotulos nao circulares, e grava a chave por load-mutate-save"
      contains: "os.replace"
    - path: "l2scanner/mercado_leitura.py"
      provides: "`larguras_de_molde`, `particionar_run` e a guarda dentro de `ler_glifos`, com `largura_maxima_de_glifo` e `folga_de_cola` como parametros somente-nomeados sem valor de fabrica"
      contains: "folga_de_cola"
    - path: "l2scanner/calibracao.py"
      provides: "`mercado_folga_de_cola_do_glifo` como campo opcional lido por `.get`, com faixa conferida no arranque"
      contains: "mercado_folga_de_cola_do_glifo"
    - path: "tests/fixtures/mercado/glifos_colados_total_f105.png"
      provides: "o caso que separa conserto de invencao: o run de 11 px cujo corte certo e 7+4 e cujo corte plausivel-e-errado e 6+5"
      contains: "PNG"
  key_links:
    - from: "tools/medir_largura_de_run.py"
      to: "tools/medir_oclusao.py"
      via: "`GRAVACOES_DO_CENSO` IMPORTADA pelo mesmo `importlib` de `medir_brilho_da_quantidade._carregar_o_censo`, nunca redeclarada"
      pattern: "GRAVACOES_DO_CENSO"
    - from: "tools/medir_largura_de_run.py"
      to: "l2scanner/mercado_leitura.py"
      via: "`quantidade_derivada` do 02-07 e a aritmetica de `limite_derivado_do_cruzamento` IMPORTADAS — a seta aponta ferramenta para puro, e o rotulo de intervalo das colunas de moeda e a INVERSA da mesma aritmetica"
      pattern: "quantidade_derivada"
    - from: "l2scanner/mercado_pagina.py"
      to: "l2scanner/calibracao.py"
      via: "`LeitorDePagina` le `mercado_folga_de_cola_do_glifo` no arranque; a AUSENCIA dela e a guarda ligada e a particao desligada, com aviso alto e nunca `raise`"
      pattern: "_calibrado"
    - from: "tests/fixtures/mercado/calibracao_de_fixture.json"
      to: "calibration.json"
      via: "a chave nova e copiada VERBATIM da calibracao de producao, como as outras `mercado_*` — a fixtura nunca inventa um valor proprio"
      pattern: "mercado_folga_de_cola_do_glifo"
---

<objective>
Fechar a falha ABERTA que a leitura de numero tem hoje: **um run mais largo que qualquer molde
e casado contra UM molde e vira UM digito**, com score e margem que atravessam as duas peneiras.
Medido: `44` e lido como `4`, `149,44` e lido como `14,44`, e nada nisso reclama.

Purpose: este e o unico defeito conhecido da Fase 2 que produz **numero errado e plausivel** em
vez de descartar a linha. Todos os outros — o `1` escuro do 02-07, a trava de digitos do 02-03,
a sonda de oclusao do 02-02 — falham FECHADO. Este falha ABERTO, viola `LEIT-02` diretamente
("preco nunca e inventado") e e a familia do incidente 27x. E ele contamina o 02-05: o replay
contaria as 14 quantidades erradas e os 55 totais inventados como ACERTO.

Output: `tools/medir_largura_de_run.py` com o vale, o custo e a folga de cola medidos; a chave
`mercado_folga_de_cola_do_glifo` no `calibration.json` da maquina do usuario; a guarda e a
particao em `ler_glifos` por parametro obrigatorio; e tres fixturas VERSIONADAS que prendem os
tres casos.
</objective>

<as_cinco_perguntas_respondidas_por_escrito>

> **STATUS DESTES NUMEROS.** Tudo abaixo foi medido pelo planejador sobre o **censo inteiro** —
> as 8 gravacoes nomeadas, 347 paginas com painel aberto e layout conferido, **3.458 linhas de
> grade** —, e nao sobre uma sonda de 3 frames. Ainda assim: **e SONDAGEM DE PLANEJAMENTO**, uma
> passagem so, nao reproduzida por terceiro, e **nao entra em docstring nem em teste como se
> fosse medicao da fase**. A Task 1 re-responde tudo com ferramenta versionada e testada. **Onde
> a medicao da fase contradisser esta sondagem, a medicao manda**, e a contradicao vira registro
> no SUMMARY com o numero que a derrubou.

## 0. Antes das cinco: dois erros do enunciado, corrigidos por medida

O script descartavel que levantou o alarme calcula a largura como `fim - inicio + 1`. **A
convencao de `segmentar_glifos` e SEMI-ABERTA** — `runs.append((inicio, coluna))` onde `coluna`
e a primeira coluna VAZIA. Conferido contra os moldes: `glifos_unitario_f010.png` le `6,00` com
larguras `[4, 1, 4, 4]` por `fim - inicio`, e os moldes valem 4 px para o digito e 1 px para a
virgula. Sob `+1` daria `[5, 2, 5, 5]`, que nao casa com molde nenhum.

Consequencias, as duas vinculantes:

1. **Todas as larguras do enunciado estao infladas em 1.** O run do `44` tem **12 px**, nao 13.
   A faixa suspeita e **7..19**, nao 7..20.
2. **A faixa `> 6` daquele script incluia TODO `4` legitimo**, porque ele testava `b-a+1 > 6`,
   ou seja `b-a >= 6`, e 6 e exatamente a largura do molde `4`. Por isso os 7,1% / 9,1% / 7,4%.

Remedido com a convencao certa, sobre TODOS os runs do censo:

| coluna | runs | runs > 6 px | % |
|---|---|---|---|
| quantidade | 5.793 | 270 | 4,66% |
| total | 19.225 | 802 | 4,17% |
| unitario | 16.583 | 393 | 2,37% |

E o numero que de fato importa nao e esse — e quantas celulas **que a producao ACEITA hoje**
carregam um run largo: **14 de 1.216** na quantidade, **75 de 2.910** no total, **19 de 2.979**
no unitario. As outras ja caem por outra peneira.

## 1. A guarda basta? Sim — e mesmo assim ela nao e o produto, porque e DOMINADA.

A guarda (todo run maior que o maior molde derruba a celula) converte falha ABERTA em FECHADA.
Custo medido, com a gramatica aplicada, que e o que a producao aceita:

| | hoje | com a guarda | delta |
|---|---|---|---|
| celulas de quantidade aceitas | 1.216 | 1.202 | −14 (−1,15%) |
| celulas de total aceitas | 2.910 | 2.835 | −75 (−2,58%) |
| celulas de unitario aceitas | 2.979 | 2.960 | −19 (−0,64%) |
| **linhas com as 3 colunas aceitas** | **1.135** | **1.066** | **−69 (−6,08%)** |
| paginas com pelo menos 1 linha | 155 | 155 | **0** |
| paginas que ficam com ZERO linha | — | **0** | — |

Entao a resposta a pergunta como ela foi feita: **o custo por PAGINA nao e maior que o custo por
celula — ele e MENOR.** A perda de 6,08% em linha nao derruba uma unica pagina, porque as linhas
com run largo estao espalhadas e nunca ocupam uma pagina inteira. E das 69 linhas perdidas,
**todas as 14 da quantidade sao comprovadamente mentira** (leem `4` onde o rotulo derivado diz
`44`) e **55 das 59 celulas de total conferiveis tambem sao** (ver pergunta 2). A guarda quase
nao troca leitura boa por seguranca; ela quase so remove leitura errada.

**Mas ela e estritamente dominada**, e por isso ela entra como o RAMO DE FALLBACK da particao e
nao como o produto. O que ela descarta, a particao recupera — de graca e mais barato.

## 2. Separar os glifos grudados e viavel? SIM, e melhor que descartar em toda medida.

**A causa, vista no pixel e nao deduzida.** O molde `4` do `calibration.json`:

```
...##.
...##.
....#.
....#.
######     <- a barra horizontal ocupa as SEIS colunas da caixa
....#.
....#.
....#.
```

A barra chega ate a ultima coluna do proprio molde. Colado o vizinho, a coluna de fronteira fica
com tinta, `mascara.any(axis=0)` nao ve coluna vazia, e a regra de `segmentar_glifos` — *QUALQUER
COLUNA VAZIA SEPARA, sem tolerancia de lacuna* — nao tem o que separar. O run do `44` em
`053105-mercado-aberto/frame_000078.png` L3, com a mascara impressa:

```
....#.....#.
....#.....#.
....#.....#.
....#.....#.
############   <- as duas barras viraram uma linha continua
....#.....#.
....#.....#.
....#.....#.
```

**Confirmado que NAO e brilho:** nos pisos 200, 210 e 220 as colunas cheias voltam a ser
`.####..####.` — os dois glifos reaparecem —, mas 220 apaga a coluna inteira em outros frames.
Subir o piso troca um defeito por outro; o defeito e de GEOMETRIA.

**O algoritmo.** Programacao dinamica sobre as posicoes de corte, nao enumeracao de particoes:
`melhor[j]` = o melhor par (pior score, pior margem) para as colunas `[0, j)` do run, com
transicao sobre as larguras permitidas e **poda pelo piso e pela margem em cada segmento** — que
o tudo-ou-nada ja exigiria de qualquer jeito. Custo `O(W x |larguras|)` chamadas de pontuacao.
**A enumeracao foi medida e descartada: ela passou de 10 minutos no censo e foi abortada**, porque
runs de ate 42 px com larguras de 1 a 6 dao ate 6^8 composicoes. A DP custa **11,7 ms por linha
larga**, e linhas largas sao 362 de 3.458 (10,5%).

**O rotulo NAO CIRCULAR, em duas formas.** Para a Quantity, o do 02-07: `quantidade_derivada`,
que recebe dois inteiros e nao toca um pixel da coluna. Para as colunas de MOEDA existe a
**INVERSA da mesma aritmetica**, e ela e nova neste plano: quando `Unit price` e `Quantity` leem
LIMPO (aceitas e sem nenhum run largo), o total esta preso ao intervalo
`[(u - 0,5)*q, (u + 1)*q)` — a uniao das hipoteses de truncamento e de arredondamento que o 02-07
ja derivou. Ela nao FIXA o total, mas **REJEITA um total inventado**, que e exatamente o que
precisa ser medido. Ela existe para 59 das celulas de total com run largo.

**O veredito, medido nas duas populacoes:**

| variante | Quantity: CERTO / **ERRADO** / NAO LE | Total: DENTRO / **INVENTADO** / NAO LE |
|---|---|---|
| A — hoje | 463 / **14** / 1.693 | 0 / **55** / 4 |
| B — so a guarda | 463 / **0** / 1.707 | 0 / **0** / 59 |
| C — partir com as larguras de molde `(1,4,6)` | 477 / **0** / 1.693 | 4 / **0** / 55 |
| D — partir com toda largura de 1 a 6 | 477 / **0** / 1.693 | 4 / **54** / 1 |
| **E — partir com a folga de cola de 1 coluna** | **477 / 0 / 1.693** | **58 / 0 / 1** |

E o rendimento: linhas completas **1.135 -> 1.148 (+1,15%)**, paginas com pelo menos uma linha
**155 -> 156**. A particao nao custa rendimento; ela **rende**, e ainda zera as duas populacoes de
erro aberto.

**A linha D e o achado que dimensiona o risco, e ela precisa ser lida com atencao:** afrouxar as
larguras permitidas nao degrada devagar — ela **INVENTA 54 numeros**. O run de 11 px de
`frame_000105` L6 admite dois cortes que passam nas duas peneiras:

| corte | esquerda | direita | texto da celula |
|---|---|---|---|
| `6+5` | `4` score 0,915 | `4` score **0,470** | `144,44` |
| `7+4` | `4` score 0,884 | `9` score **0,791** | `149,44` |

O `0,470` do corte errado esta a **0,0002 acima** do piso de leitura `0,4698`. A aritmetica
independente da linha (unitario `2,99`, quantidade `50`, as duas lidas sem run largo) exige o
total em `[149,25; 150,00)`: **`149,44` cabe, `144,44` nao.** O corte certo e `7+4` — sete, um a
mais que o molde —, e e por isso que a folga de cola existe: **a barra do `4` compartilha uma
coluna com o vizinho, entao o glifo colado ocupa a largura do molde MAIS a coluna partilhada.**

**Nao e mera taxa: e a diferenca entre conserto e invencao**, e por isso a folga NAO e escolhida
por este plano. A Task 1 a varre com passo 1 e a propoe pelos tres baldes do 02-07, recusando
quando existe UMA leitura fora do rotulo.

## 3. Onde a guarda mora? Em `ler_glifos`. Por raio de alcance E por posse dos ingredientes.

| candidato | pontos de chamada | tem molde? | tem piso/margem? | veredito |
|---|---|---|---|---|
| `segmentar_glifos` | **35** (60 mencoes, 11 arquivos) | nao | nao | impossivel: nao poderia nem pontuar um corte |
| `segmentar_glifos_no_brilho` | 3 | nao | nao | idem |
| `pontuar_glifos` | 2 em producao, 7 em ferramenta | sim | **nao, de proposito** | nao: aplicar corte ali tornaria a medicao circular, que e a razao escrita dela nao ter piso |
| **`ler_glifos`** | **1 em producao** (`mercado_leitura.py:563`) + 2 mencoes de teste | **sim** | **sim** | **e o lugar** |
| `ler_celula_de_numero` | 2 | sim | sim | tarde demais: o texto ja foi montado, e a decisao e por RUN |

`ler_glifos` ja e dona da regra de TUDO OU NADA e ja aplica piso e margem run a run — a guarda e
a particao sao a mesma decisao, no mesmo lugar, sobre o mesmo laco. E a assinatura de
`segmentar_glifos` fica intacta, como o 02-07 exigiu.

## 4. O limite e 6 ou e por molde? E DERIVADO dos moldes, e ele cai num vale VAZIO de 4 niveis.

Medido: das **7.105 celulas que a producao aceita hoje** nas tres colunas, **ZERO** tem algum run
de 7, 8, 9 ou 10 px. A distribuicao da largura MAXIMA por celula aceita:

| coluna | 4 | 5 | 6 | 7..10 | 11 | 12 |
|---|---|---|---|---|---|---|
| quantidade | 1.053 | 107 | 42 | **0** | 0 | 14 |
| total | 2.015 | 66 | 754 | **0** | 65 | 10 |
| unitario | 2.117 | 64 | 779 | **0** | 11 | 8 |

Duas populacoes, e um vale de quatro niveis entre elas. Por isso o custo da guarda e **identico**
nos limites 6, 7, 8, 9 e 10 — a escolha dentro do vale nao muda nada, que e a definicao de um
limite bem posto. E por isso ele **nao vai para o `calibration.json`**: ele e
`max(largura dos moldes de UM caractere)`, uma funcao dos moldes que ja estao la. Gravar uma
copia criaria **duas verdades para uma so geometria** — o argumento literal que
`layout_confere` ja escreve sobre o `dx` do cabecalho — e na proxima recalibracao a copia
envelheceria contra os moldes que ela descreve.

**O numero LIVRE deste plano e outro, e ele SIM vai para o `calibration.json`:** a folga de cola.
Ela nao se deriva dos moldes, ela e uma propriedade do anti-serrilhado, e a sondagem diz `1` sem
provar. A Task 1 prova ou reprova.

## 5. Onde a wave encaixa — e o 02-05 anda mais uma casa.

Hoje: **02-04 (w4) -> 02-06 (w5) -> 02-07 (w6, pousado no commit `e513219`) -> 02-05 (w7)**.

Este plano e a **wave 7**, com `depends_on: ["02-04", "02-06", "02-07"]`, e o **02-05 passa para a
wave 8** com `02-08` somado ao `depends_on` dele. Tres razoes, todas vinculantes:

1. **Conflito de arquivo com o 02-07.** Aquele plano modifica `l2scanner/mercado_leitura.py`,
   `l2scanner/calibracao.py`, `tests/test_mercado_leitura.py` e a calibracao de fixtura; este
   tambem. A wave 6 esta fechada. E ir DEPOIS e melhor que ir antes: `ler_celula` ja recebe
   `valor_minimo` sem valor de fabrica, entao o parametro novo entra ao lado de um irmao com a
   mesma regra, numa passagem so.
2. **O 02-05 mede rendimento, e antes deste plano ele mediria um rendimento FALSO — para cima.**
   O replay contaria as **14 quantidades erradas** e os **55 totais inventados** como acerto,
   porque os dois passam na gramatica. O criterio 2 da fase ("os precos e quantidades lidos batem
   digito a digito com o que o usuario ve no frame") passaria justamente por nao olhar as celulas
   que o derrubam.
3. **`files_modified` nao conflita com o 02-05** (ele toca `mercado_catalogo.py`,
   `mercado_pagina.py` e `tests/test_mercado_replay.py`; a interseccao e `mercado_pagina.py` e
   `tests/test_mercado_pagina.py`, que este plano tambem toca) — entao a dependencia explicita e
   necessaria pelos dois criterios, e nao apenas pela ordem.

**E ha uma consequencia que este plano NAO executa e precisa ficar escrita.** O 02-07 REPROVOU no
passo ZERO da proposta: o balde LE ERRADO do **piso compartilhado** nao estava vazio (as 14
celulas de `44`), e sobre um conjunto assim nao ha piso proponivel. Medido aqui: **com a particao,
esse balde fica VAZIO no piso 180**. Ou seja, este plano **remove a causa da REPROVA do 02-07**, e
a varredura de piso daquele plano volta a ser proponivel — e ela pega o benefício de graca, porque
`tools/medir_brilho_da_quantidade.py` chama `ler_celula`. **Re-rodar aquela varredura NAO e task
deste plano** (ela passa de 10 minutos e ja matou um executor por limite de sessao); e um item de
SUMMARY e de ledger, para quem for fechar o 02-07.

</as_cinco_perguntas_respondidas_por_escrito>

<artifacts_this_phase_produces>

| artefato | novo/mod | o que ele passa a garantir |
|---|---|---|
| `tools/medir_largura_de_run.py` | NOVO | a distribuicao de largura por classe de acerto, o vale, o custo da guarda por celula/linha/pagina, e a folga de cola proposta ou REPROVADA nos tres baldes |
| `tests/test_medir_largura_de_run.py` | NOVO | o rotulo de intervalo nos casos de borda, a recusa quando as populacoes se tocam, a parada quando falta pasta do censo, e a prova de que o censo foi IMPORTADO |
| `mercado_folga_de_cola_do_glifo` no `calibration.json` | NOVO (nao versionado) | o unico numero livre do plano, MEDIDO, na maquina do usuario |
| `l2scanner/mercado_leitura.py` | MOD | `larguras_de_molde`, `particionar_run`, e a guarda com a particao dentro de `ler_glifos`, por parametro sem valor de fabrica |
| `l2scanner/calibracao.py` | MOD | a chave nova por `.get`, com a faixa conferida no arranque e sem bump de esquema |
| `l2scanner/mercado_pagina.py` | MOD | o portao por AUSENCIA: sem a chave, a guarda liga e a particao fica desligada, com aviso alto |
| 3 fixturas `glifos_colados_*.png` | NOVAS | os tres casos presos por VALOR: `44`, o corte 7+4 contra o 6+5, e o `44,00` |
| `tests/fixtures/mercado/calibracao_de_fixture.json` | MOD | a chave nova copiada VERBATIM da producao |

</artifacts_this_phase_produces>

<nota_de_orcamento_para_o_executor>
**13 arquivos em 2 tasks — acima do alvo de 5 a 8, e de proposito.** As duas tasks nao se dividem
mais: entre elas ha um PORTAO DE DADO (a ultima linha do relatorio, `PROPOSTO` ou `REPROVADO`, que
decide qual criterio de rendimento a Task 2 cobra), e essa e a fronteira natural. Separar em tres
criaria conflito de `files_modified` em `l2scanner/mercado_leitura.py` sem ganho nenhum.

O que isso significa na pratica: **o orcamento esta apertado e nao ha folga para escopo extra.**
Nao aproveitar a passagem para arrumar outra coisa no arquivo, nao renomear o que ja funciona, nao
re-rodar a varredura de piso do 02-07 (ela passa de 10 minutos e e item de SUMMARY, nao task).
Se algo fora do plano parecer necessario, PARAR e registrar em vez de fazer.
</nota_de_orcamento_para_o_executor>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-04-SUMMARY.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-06-SUMMARY.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A varredura que MEDE o vale, o custo da guarda e a folga de cola — com DOIS rotulos nao circulares</name>

  <files>tools/medir_largura_de_run.py, tests/test_medir_largura_de_run.py, tests/fixtures/mercado/glifos_colados_quantidade_f078.png, tests/fixtures/mercado/glifos_colados_total_f105.png, tests/fixtures/mercado/glifos_colados_total_f054.png</files>

  <read_first>
    - `tools/medir_brilho_da_quantidade.py` linhas 100–230 — o cabecalho de `RAIZ`/`sys.path`, o `_carregar_o_censo` por `importlib.util` apontando para `tools/medir_oclusao.py`, e `quantidade_derivada` com a docstring inteira: ela ja explica por que o criterio e de INTERVALO e nao de residuo, e este plano usa a INVERSA dessa mesma aritmetica como rotulo das colunas de moeda
    - `tools/medir_brilho_da_quantidade.py` linhas 346–470 — `varrer_uma_janela` e `varrer`: o laco gravacao/frame/linha/coluna com `RastreioDoPainel`, o PORTAO DE LAYOUT ligado antes de fatiar, e `_recorte`
    - `tools/medir_brilho_da_quantidade.py` linhas 550–700 — `Baldes`, `classificar`, `regiao_segura`, `ha_seguro_abaixo_do_primeiro_que_erra` e `propor_o_piso`: a disciplina dos tres baldes, o passo 1, o ULTIMO SEGURO e as quatro causas de REPROVA. Esta task reproduz essa disciplina sobre outra grandeza
    - `tools/medir_brilho_da_quantidade.py` linhas 860–900 — `gravar`, o load-mutate-save por `NamedTemporaryFile` + `os.replace`
    - `l2scanner/mercado_leitura.py` linhas 84–215 — `segmentar_glifos`, `mascara_de_numero` e `segmentar_glifos_no_brilho`: a convencao de faixa compartilhada e, em especial, que o run e SEMI-ABERTO (`runs.append((inicio, coluna))`, com `coluna` sendo a primeira VAZIA)
    - `l2scanner/mercado_leitura.py` linhas 452–530 — `pontuar_glifos` (sem piso, de proposito) e `ler_glifos`
    - `l2scanner/mercado_leitura.py` linhas 619–670 — `LIMITE_DERIVADO_POR_UNIDADE`, `limite_derivado_do_cruzamento` e `residuo_do_cruzamento`: a aritmetica do arredondamento mora aqui e e daqui que ela e importada
    - `tests/test_medir_leitura_de_glifo.py` linhas 1–40 — o cabecalho que DECLARA a proveniencia de cada fixtura e o helper `_bandas`, que e a convencao de fixtura em bandas empilhadas
    - `tools/medir_oclusao.py` linhas 75–115 — `GRAVACOES_DO_CENSO` e `MOTIVO_PARA_IGNORAR`
  </read_first>

  <precondition>`calibration.json` existe no checkout PRINCIPAL com `mercado_grade.layout` valendo `negociacao`, os 13 moldes de glifo e as 3 ancoras — sem eles a varredura nao tem material nem rotulo. Conferir antes de comecar imprimindo `len(mercado_templates_de_digito)`, `len(mercado_ancoras)` e o layout; qualquer um fora do esperado e PARADA, nao improviso.</precondition>

  <behavior>
    - `larguras_de_molde(moldes)` devolve a tupla ORDENADA das larguras dos moldes de UM caractere.
      Sobre os 13 moldes de producao ela devolve `(1, 4, 6)` — a virgula, os digitos, e o `4`. Os
      moldes de PALAVRA (`Adena` 35 px, `XM Coin` 44 px) ficam de fora, pela mesma regra de
      `pontuar_glifos`, que ja filtra por `len(rotulo) == 1`.
    - `limite_de_glifo_unico(moldes)` devolve o MAIOR desses valores. Sobre os moldes de producao
      ele devolve 6. Ele NAO le o `calibration.json` procurando uma chave: ele deriva dos moldes,
      porque uma copia gravada envelheceria contra os moldes que ela descreve.
    - `total_no_intervalo(total, unitario, quantidade)` e o rotulo das colunas de MOEDA e devolve
      `True`/`False`/`None`. Ele e a INVERSA de `quantidade_derivada`: aceita o total quando
      `(2*u - 1)*q // 2 <= total < (u + 1)*q`, que e a uniao das hipoteses de truncamento e de
      arredondamento — a mesma uniao que `quantidade_derivada` ja usa no outro sentido. Devolve
      `None` quando `unitario` ou `quantidade` faltam, e NUNCA levanta.
      Caso conferido a mao pelo planejador, que vira teste: com `unitario = 299` e
      `quantidade = 50`, o total `14944` esta DENTRO e o total `14444` esta FORA.
    - `classificar_por_largura(amostras, limite)` separa as celulas ACEITAS de hoje em duas
      populacoes — as que tem todo run dentro do limite e as que tem algum run acima — e devolve a
      distribuicao de largura MAXIMA por celula de cada uma. O relatorio precisa mostrar as DUAS,
      porque a afirmacao central do plano e que existe um VALE entre elas.
    - `particionar_por_dp(mascara, faixa, inicio, fim, larguras, piso, margem)` devolve
      `(pior_score, pior_margem, rotulos)` ou `None`. Ela e programacao dinamica sobre as posicoes
      de corte: `melhor[j]` cobre as colunas `[inicio, inicio+j)`, a transicao percorre as larguras
      permitidas, e cada segmento e PODADO na hora pelo piso e pela margem — o tudo-ou-nada exigiria
      isso de qualquer jeito, e a poda e o que mantem o custo linear. Ela NAO enumera particoes: a
      enumeracao foi medida e passou de 10 minutos no censo.
    - `larguras_com_folga(larguras, folga)` devolve as larguras permitidas na particao: cada largura
      de molde, e ela acrescida de 1 ate `folga` colunas. Com `folga = 0` ela devolve as larguras de
      molde puras; com `folga = 1` ela devolve `(1, 2, 4, 5, 6, 7)`.
    - `propor_a_folga(amostras)` varre `folga` de 0 ate um teto declarado por constante do modulo,
      com passo 1, e classifica cada celula em TRES baldes por folga. Ela recebe a COLHEITA ja feita
      — mascara, faixa e runs por celula — e NAO reabre PNG nenhum: o censo e lido uma vez so, e so
      as celulas com run largo sao reclassificadas a cada folga — LE CERTO (bate com o rotulo,
      derivado na Quantity ou de intervalo nas colunas de moeda), NAO LE (cai por piso, margem ou
      gramatica: falha fechada) e LE ERRADO (produz numero que o rotulo REJEITA). Propoe a folga com
      o maior CERTO entre as que tem o balde LE ERRADO **vazio**, e REPROVA quando nenhuma tem. O
      passo e 1 e a proposta e sempre uma folga MEDIDA, nunca um ponto entre duas.
    - Antes de propor, imprimir EM SEPARADO o balde LE ERRADO da folga 0 — que e a GUARDA pura — e o
      de HOJE. Sem separar as tres causas, o numero que for para o ledger fica atribuido a coisa
      errada, que foi a licao do 02-07.
    - `GRAVACOES_DO_CENSO` do modulo e o MESMO OBJETO de `medir_oclusao.GRAVACOES_DO_CENSO` —
      identidade, nao igualdade. O teste obtem o modulo de referencia por `sys.modules["medir_oclusao"]`,
      que o proprio `_carregar_o_censo` registra, e NAO por um `importlib` proprio: um segundo
      carregamento produz outro objeto e a identidade falharia sobre codigo CORRETO.
    - `main` devolve 2 quando o diretorio de gravacoes nao existe, 3 quando falta pasta do censo
      (nomeando as que faltam), e 0 nos dois vereditos.
  </behavior>

  <action>
Criar `tools/medir_largura_de_run.py` no molde estrutural de `tools/medir_brilho_da_quantidade.py`: mesmo cabecalho de `RAIZ` + `sys.path`, mesmo `_carregar_o_censo` por `importlib.util` apontando para `tools/medir_oclusao.py`, e as primitivas IMPORTADAS de `l2scanner.mercado_leitura` (`segmentar_glifos_no_brilho`, `mascara_de_numero`, `pontuar_glifos`, `numero_valido`, `inteiro_de_quantidade`, `centesimos_de_moeda`, `ler_celula`, `layout_confere`) e de `tools/medir_brilho_da_quantidade` (`quantidade_derivada`) em vez de reescritas. A seta aponta ferramenta para puro; reescrever faria a ferramenta medir com uma convencao e a producao decidir com outra.

A CONVENCAO DE LARGURA, declarada no cabecalho do modulo e conferida por teste. O run de `segmentar_glifos` e SEMI-ABERTO: `runs.append((inicio, coluna))` guarda a primeira coluna VAZIA como `fim`, logo a largura e `fim - inicio` e nunca `fim - inicio + 1`. Um erro de uma unidade aqui inflaria toda largura em 1 e faria a faixa suspeita engolir o molde `4`, que tem exatamente 6 px — que foi o erro do script descartavel que levantou o alarme. Prender isso com uma afirmacao sobre `glifos_unitario_f010.png`, que le `6,00` com larguras `4, 1, 4, 4` contra moldes de 4 px e uma virgula de 1 px.

A VARREDURA. Reproduzir o laco de `medir_brilho_da_quantidade.varrer`: para cada uma das 8 gravacoes NOMEADAS, `RastreioDoPainel` com as ancoras da calibracao; para cada frame com painel aberto, `layout_confere` ANTES de fatiar (medir a coluna Quantity sobre a aba Adena seria medir uma coluna que nem existe la), e entao as dez linhas da grade e os recortes das tres colunas pelo `dx`/`largura` da calibracao. Por celula, guardar: os runs e as larguras, o texto lido HOJE, e o piso de brilho da coluna (o compartilhado nas colunas de moeda, `mercado_limiar_de_brilho_da_quantidade` na Quantity). Por LINHA, guardar os dois rotulos: a quantidade derivada de `Total` e `Unit price`, e — para cada coluna de moeda que tenha run largo — o veredito de intervalo, calculado somente quando as OUTRAS duas colunas leem LIMPO, isto e, aceitas pela gramatica E sem nenhum run acima do limite. A condicao de limpeza e o que torna o rotulo nao circular: um rotulo calculado a partir de uma coluna que tambem esta mentindo mediria a leitura contra ela mesma.

**O CENSO E LIDO UMA VEZ SO, E ISSO E DESENHO E NAO OTIMIZACAO.** Guardar, por celula, a mascara e a faixa ja calculadas alem dos runs, e reclassificar as folgas candidatas EM MEMORIA sobre essa colheita — nunca reabrir os PNGs por folga. A varredura irma (`tools/medir_brilho_da_quantidade.py`) varre o censo uma vez POR PISO CANDIDATO, e e por isso que ela passa de 10 minutos e ja matou um executor por limite de sessao. Aqui a estrutura do problema permite o contrario: a segmentacao no piso da coluna NAO depende da folga — so a particao depende —, entao a folga so precisa reclassificar as celulas que tem run largo, que sao ~10% das linhas. Reabrir o censo por folga seria pagar o custo alheio sem a razao alheia.

RELATORIO 1, O VALE. A distribuicao da largura MAXIMA por celula, nas tres colunas, separada entre celulas ACEITAS e RECUSADAS hoje. Marcar explicitamente quantas celulas aceitas caem em cada nivel entre o maior molde e o menor run largo observado. Se esse intervalo NAO estiver vazio, imprimir alto que o limite derivado deixou de servir e que a resposta da pergunta 4 mudou — e nesse caso a Task 2 NAO comeca, o plano volta a mesa.

RELATORIO 2, O CUSTO DA GUARDA. Quantas celulas ACEITAS hoje cada coluna perde sob a guarda pura; quantas LINHAS perdem as tres colunas; quantas PAGINAS perdem alguma linha e quantas ficam com zero. Imprimir os textos que a guarda descarta, agrupados por valor e com a contagem, para que o custo seja inspecionavel a olho e nao so um percentual.

RELATORIO 3, A FOLGA DE COLA, que e a resposta da pergunta 2. A tabela de TODAS as folgas candidatas com os tres baldes de cada uma, nas duas populacoes rotuladas (Quantity contra o rotulo derivado; colunas de moeda contra o rotulo de intervalo), no molde da tabela de candidatos do 02-07, com a escolhida marcada. A folga proposta e RECLASSIFICADA nos tres baldes e a proposta so sai se o balde LE ERRADO DELA estiver vazio — uma folga proposta e uma folga medida. Reportar a folga imediatamente seguinte tambem, com os baldes dela, para que se veja o que acontece ao afrouxar: a sondagem do planejador achou que afrouxar nao degrada devagar, e sim inventa numero em bloco, e essa afirmacao precisa ser conferida no censo.

O ORCAMENTO DE RELOGIO DA PROPRIA FERRAMENTA, declarado antes de escrever o laco. A sondagem do planejador mede as duas metades separadamente: a passagem unica pelo censo — 8 gravacoes, 347 paginas, 3.458 linhas, tres colunas, abrindo cada PNG uma vez — custou **43 s**; a particao sobre as **362 linhas largas** custou **~4 s por folga candidata**. Com a colheita reaproveitada e um teto de folga pequeno, a ferramenta inteira fica **abaixo de 2 minutos**. Re-varrer o censo por folga a levaria a casa dos 10 minutos, que e onde a varredura do 02-07 vive e onde um executor ja morreu. Imprimir o relogio de cada metade no fim da execucao, para que o proximo saiba onde o tempo foi.

RELATORIO 4, O CUSTO DE TEMPO DENTRO DO TICK — que e outra grandeza e nao se confunde com a de cima: aquela e o relogio da FERRAMENTA, esta e o orcamento da PRODUCAO. O tempo medio por linha LARGA e por linha comum, e a fracao de linhas largas. Comparar com o orcamento de um tick de 1 Hz e dizer a folga em ordens de grandeza. Imprimir tambem o maior run visto e o custo do pior caso, porque e o pior caso que estoura o tick e nao a media.

O RAMO REPROVADO. Quando nenhuma folga tem o balde LE ERRADO vazio, gravar mesmo assim a folga `0` na chave nova e imprimir o veredito com os numeros e a causa. A folga `0` E a guarda pura: ela falha FECHADA, e grava-la e o que permite a producao nao carregar nenhum ramo de fallback nem nenhum numero de fabrica. **Reprovando, a Task 2 CONTINUA e muda de criterio, e isso e instrucao e nao interpretacao:** ela entrega a guarda e o encanamento, o criterio de rendimento dela deixa de ser "estritamente maior" e passa a ser "as duas populacoes de erro aberto ficam vazias", e a refutacao entra em `.planning/WINDOWS.md` com o numero que a derrubou. Escrever `PROPOSTO` ou `REPROVADO` em maiusculas na ULTIMA linha do relatorio, porque e essa linha que a Task 2 le para saber qual criterio cobrar.

A GRAVACAO. `gravar(caminho, folga)` faz load-mutate-save: `json.loads` do arquivo, mutacao da unica chave `mercado_folga_de_cola_do_glifo`, `NamedTemporaryFile` no mesmo diretorio e `os.replace`. Montar uma `Calibracao` do zero apagaria os 13 moldes e as 3 ancoras — foi o modo de falha REAL que ja custou uma recuperacao a mao. `--gravar` e opcional e o padrao e so relatar.

AS TRES FIXTURAS. Recortar de `recordings/` e gravar em `tests/fixtures/mercado/`, na convencao de bandas de `tests/test_medir_leitura_de_glifo.py`, com a proveniencia DECLARADA no cabecalho do arquivo de teste: `glifos_colados_quantidade_f078.png` da coluna Quantity de `20260828-053105-mercado-aberto/frame_000078.png` linha 3 (o run unico onde ha dois glifos); `glifos_colados_total_f105.png` da coluna Total de `frame_000105.png` linha 6 (o caso que separa conserto de invencao); `glifos_colados_total_f054.png` da coluna Total de `frame_000054.png` linha 8. `recordings/` e SOMENTE LEITURA e as 8 pastas do censo sao nomeadas, nunca alcancadas por glob.

Os testes de `tests/test_medir_largura_de_run.py` correm sobre as fixturas VERSIONADAS e a `calibracao_de_fixture.json`, nunca sobre `recordings/`. Afirmar: a convencao semi-aberta contra as larguras de molde; `larguras_de_molde` e `limite_de_glifo_unico` sobre os moldes da fixtura; `total_no_intervalo` nos dois casos conferidos a mao; `larguras_com_folga` nas folgas 0 e 1; a proposta sobre uma populacao construida COM balde vazio (propoe) e sobre uma SEM (REPROVA e devolve `None`); a identidade do censo importado; e que a DP encontra o corte de sete-mais-quatro em `glifos_colados_total_f105.png` com pior score acima do piso de leitura.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -m pytest tests/test_medir_largura_de_run.py -x -q</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('mlr','tools/medir_largura_de_run.py'); m=importlib.util.module_from_spec(s); sys.modules['mlr']=m; s.loader.exec_module(m); o=sys.modules['medir_oclusao']; assert m.GRAVACOES_DO_CENSO is o.GRAVACOES_DO_CENSO, 'o censo foi COPIADO, nao importado'; print('censo importado ok', len(m.GRAVACOES_DO_CENSO))"</automated>
  </verify>

  <acceptance_criteria>
    - GATE DE PRODUCAO DE DADO, no checkout PRINCIPAL, com caminhos absolutos:
      `C:/Users/refun/Desktop/Lineage2-warnings/.venv/Scripts/python.exe C:/Users/refun/Desktop/Lineage2-warnings/tools/medir_largura_de_run.py --gravacoes C:/Users/refun/Desktop/Lineage2-warnings/recordings --calibracao C:/Users/refun/Desktop/Lineage2-warnings/calibration.json`
      sai com codigo 0 e imprime os QUATRO relatorios, as 8 gravacoes do censo com a contagem de
      PNGs, e as pastas ignoradas com o motivo de cada uma
    - A mesma ferramenta apontada para um diretorio com apenas UMA das 8 pastas sai com codigo 3 e
      nomeia as 7 que faltam
    - O RELATORIO 1 mostra a distribuicao de largura maxima por celula ACEITA nas tres colunas, e
      declara quantas celulas aceitas caem entre o limite derivado e o menor run largo. Se esse
      intervalo NAO estiver vazio, a Task 2 NAO comeca — o limite derivado deixou de servir e o
      plano volta a mesa
    - O RELATORIO 3 tem uma linha por folga candidata, sem saltos, com `PASSO_DA_VARREDURA` igual a
      1 declarado no cabecalho, e a folga ESCOLHIDA aparece com os SEUS PROPRIOS tres baldes
      preenchidos e o balde LE ERRADO dela VAZIO. Nao basta que as vizinhas estejam: uma folga
      proposta sem a propria linha na tabela e interpolacao disfarcada de medicao
    - Os baldes das colunas de MOEDA sao contados contra o rotulo de INTERVALO, e ele so e calculado
      quando as outras duas colunas da linha leem LIMPO (aceitas pela gramatica e sem run acima do
      limite). O relatorio imprime quantas celulas tiveram rotulo disponivel — um rotulo que nao
      existe para a populacao nao mede nada
    - O balde LE ERRADO de HOJE e o da folga 0 (a guarda pura) aparecem EM SEPARADO, cada um com a
      sua contagem: as tres causas sao distintas e o numero do ledger nao pode ser atribuido a
      errada
    - A ULTIMA linha do relatorio e `PROPOSTO` ou `REPROVADO` em maiusculas, com a causa quando
      REPROVADO — e essa linha que a Task 2 le para saber qual criterio de rendimento cobrar
    - O RELATORIO 4 declara o custo por linha larga e o do PIOR caso, e a folga contra o tick de
      1 Hz em ordens de grandeza. Custo de pior caso acima de 200 ms e PARADA: a peneira que
      estoura o tick para o scanner de olhar a party
    - O RELOGIO DA FERRAMENTA e impresso em DUAS metades — a passagem unica pelo censo e a
      reclassificacao das folgas — e o total fica **abaixo de 5 minutos**. Acima disso, PARAR e
      conferir se o censo esta sendo reaberto por folga: a sondagem mediu 43 s para a passagem e
      ~4 s por folga candidata sobre as linhas largas, e so re-abrir os PNGs explica uma ordem de
      grandeza a mais. A varredura irma do 02-07 passa de 10 minutos e ja custou uma sessao a um
      executor — este plano NAO herda esse custo, e o criterio existe para provar que nao herdou
    - A gravacao roda com `--gravar` e o PORTAO e conferido em DUAS partes, porque IGUALDADE NAO E
      PRESENCA — um `calibration.json` que ja tivesse perdido os moldes passaria trivialmente num
      teste so de digest, e os 13 moldes ja se perderam uma vez:
      (a) PRESENCA, antes E depois: `len(mercado_templates_de_digito) == 13` e
      `len(mercado_ancoras) == 3`;
      (b) IDENTIDADE, antes E depois: md5 dos 13 moldes e md5 das 3 ancoras, iguais nas duas pontas.
      Registrar as quatro conferencias e os dois digests no SUMMARY
    - `git status --short` nao lista `calibration.json` — ele e gitignored e NUNCA e commitado
    - As tres fixturas novas estao em `tests/fixtures/mercado/`, com a proveniencia (pasta, frame,
      linha, coluna) declarada no cabecalho de `tests/test_medir_largura_de_run.py`
    - O veredito (PROPOSTO com a folga e os tres baldes, ou REPROVADO com o numero que o derrubou)
      fica no SUMMARY, e uma linha entra em `.planning/WINDOWS.md`
  </acceptance_criteria>

  <done>
`tools/medir_largura_de_run.py` existe, importa o censo em vez de copia-lo, varre as 8 gravacoes
com o portao de layout ligado, produz os quatro relatorios, e gravou
`mercado_folga_de_cola_do_glifo` no `calibration.json` da maquina do usuario com o veredito por
escrito — ou REPROVOU com o numero que o derrubou, tambem por escrito. As tres fixturas estao
versionadas com proveniencia declarada. Os 13 moldes e as 3 ancoras estao intactos, conferidos por
presenca E por md5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A guarda e a particao chegam a producao por `ler_glifos`, e as celulas estreitas nao mudam um pixel</name>

  <files>l2scanner/mercado_leitura.py, l2scanner/mercado_pagina.py, l2scanner/calibracao.py, tests/test_mercado_leitura.py, tests/test_mercado_pagina.py, tests/test_calibracao_mercado.py, tests/fixtures/mercado/calibracao_de_fixture.json</files>

  <read_first>
    - `l2scanner/mercado_leitura.py` linhas 1–83 — o charter, e em especial o bloco que exige que todo limiar chegue por parametro sem valor de fabrica: esta task acrescenta dois parametros a essa lista e a regra ja esta escrita
    - `l2scanner/mercado_leitura.py` linhas 452–530 — `pontuar_glifos` e `ler_glifos`: o laco run a run com piso e margem, que e onde a guarda e a particao entram
    - `l2scanner/mercado_leitura.py` linhas 530–575 — `ler_celula`, que e o UNICO ponto de chamada de producao de `ler_glifos` e quem ja possui os `moldes` de onde as larguras sao derivadas
    - `l2scanner/mercado_leitura.py` linhas 575–660 — `ler_celula_de_numero` e `ler_celula_de_quantidade`, e as docstrings que ja descrevem o defeito do `1` e o piso proprio: a docstring nova vive ao lado, no mesmo padrao
    - `l2scanner/mercado_leitura.py` linhas 723–810 — `ler_linha` e a ordem do pipeline; os pontos de chamada das tres colunas ficam aqui
    - `l2scanner/mercado_pagina.py` linhas 113–170 e 300–435 — o `__init__` do `LeitorDePagina`, o ponto de chamada de `ler_linha`, e o `_calibrado()` que confere as chaves por AUSENCIA
    - `l2scanner/calibracao.py` linhas 1190–1235 — `_conferir_o_piso_de_brilho_da_quantidade`, a chave irma que o 02-07 acabou de acrescentar: a forma exata do comentario, da faixa valida e do que um valor errado custa
    - `l2scanner/calibracao.py` linhas 605–760 — `carregar` com os `.get`, e `_conferir_as_chaves_de_mercado`
    - `tests/test_mercado_leitura.py` linhas 200–235 — `test_nenhum_limiar_tem_valor_de_fabrica` e `test_o_PISO_DE_BRILHO_tambem_chega_sem_valor_de_fabrica`: os testes de `inspect.signature` que esta task estende
    - `tests/fixtures/mercado/glifos_precos_f010.png` e `glifos_unitario_f010.png` — as fixturas de controle, cujas leituras precisam ficar IDENTICAS
  </read_first>

  <precondition>`mercado_folga_de_cola_do_glifo` existe em `C:/Users/refun/Desktop/Lineage2-warnings/calibration.json`, gravado pelo gate da Task 1, e a ULTIMA linha do relatorio daquela task diz `PROPOSTO` ou `REPROVADO` — esta task copia o valor VERBATIM para a calibracao de fixtura e nao o escolhe, e le aquela linha para saber qual criterio de rendimento cobrar. Rodar `python -m pytest tests/test_mercado_leitura.py tests/test_mercado_pagina.py -q` antes de comecar, porque sao os mesmos pontos de chamada que esta task vai tocar.</precondition>

  <reversibility rating="costly">Trocar "um run largo vira um digito" por "um run largo vira varios digitos" e mexer na peneira que decide preco. Voltar atras e reescrever uma funcao, mas as observacoes que a Fase 3 gravar sob uma folga errada ficam no CSV com aparencia plausivel — e a sondagem mediu exatamente isso acontecendo com a folga errada (54 totais inventados). Por isso a Task 1 recusa propor com o balde LE ERRADO nao vazio, e por isso o criterio central desta task e a IGUALDADE das celulas estreitas somada ao VAZIO das duas populacoes de erro aberto — nunca so o ganho.</reversibility>

  <behavior>
    - `larguras_de_molde(moldes)` e `limite_de_glifo_unico(moldes)` sao PROMOVIDAS da ferramenta para
      `l2scanner/mercado_leitura.py`, com as docstrings inteiras, e a ferramenta passa a IMPORTA-LAS
      — a seta aponta ferramenta para puro, e duas copias envelheceriam separadas. Sobre os 13
      moldes de producao elas devolvem `(1, 4, 6)` e `6`.
    - `particionar_run(mascara, faixa, inicio, fim, moldes, piso, margem, larguras)` devolve a lista
      de `(rotulo, score, margem)` do melhor corte, ou `None` quando nenhum corte tem TODOS os
      segmentos acima do piso E da margem. Ela e a DP promovida da Task 1, com a mesma poda.
    - `ler_glifos` ganha `largura_maxima_de_glifo: int` e `folga_de_cola: int | None`, os dois
      somente-nomeados e SEM valor de fabrica. Um run dentro do limite segue exatamente o caminho de
      hoje. Um run acima do limite: com `folga_de_cola` valendo `None` a funcao devolve `None` (a
      GUARDA — falha fechada); com um inteiro ela tenta `particionar_run` com
      `larguras_com_folga(larguras_de_molde(moldes), folga_de_cola)` e devolve `None` se a particao
      nao achar corte que passe. Nos dois ramos a falha e FECHADA: a diferenca e so quantas celulas
      chegam a ler.
    - `inspect.signature(ler_glifos).parameters["largura_maxima_de_glifo"].default` e
      `inspect.Parameter.empty`, e o mesmo para `folga_de_cola`. Chamar `ler_glifos` sem eles levanta
      `TypeError`.
    - `ler_celula` DERIVA `largura_maxima_de_glifo` dos `moldes` que ja recebe e repassa, e ganha
      `folga_de_cola` como somente-nomeado sem valor de fabrica, que ela apenas repassa. O limite nao
      entra na assinatura de `ler_celula`: derivar no unico lugar que tem os moldes e o que impede
      duas verdades.
    - `ler_celula_de_numero`, `ler_celula_de_quantidade` e `ler_linha` recebem e repassam
      `folga_de_cola` pela mesma regra de somente-nomeado sem valor de fabrica.
    - `segmentar_glifos` e `segmentar_glifos_no_brilho` NAO mudam de assinatura: afirmado por
      `inspect.signature` comparando a lista de parametros com a lista esperada, e nao por leitura.
    - AS CELULAS ESTREITAS NAO MUDAM. `glifos_precos_f010.png` e `glifos_unitario_f010.png` produzem
      EXATAMENTE o mesmo texto de antes, com os mesmos runs — preso por VALOR e nao por "nao
      levantou". Esta e a afirmacao central: quando todo run cabe no limite, o caminho novo E o
      caminho antigo.
    - AS TRES FIXTURAS COLADAS. `glifos_colados_quantidade_f078.png` le o par de digitos repetidos
      com a folga medida e devolve `None` com `folga_de_cola` valendo `None`.
      `glifos_colados_total_f105.png` le o valor cujo corte certo e o de sete-mais-quatro, e o teste
      afirma o VALOR lido — nao apenas que leu —, porque o corte errado tambem produz um numero que a
      gramatica aceita. `glifos_colados_total_f054.png` le o valor de quatro glifos.
    - `mercado_folga_de_cola_do_glifo` e OPCIONAL em `calibracao.py`, lida por `.get`, com a faixa
      conferida no arranque (inteiro, nao negativo, e nao maior que o teto declarado). Ausente, ela
      vale `None` e a producao usa a GUARDA. `VERSAO_DO_ESQUEMA` segue em 2.
    - `LeitorDePagina` le a chave no arranque; a AUSENCIA e a guarda ligada com aviso alto, e NUNCA
      um `raise` — o mesmo portao por ausencia das outras chaves.
  </behavior>

  <action>
Promover `larguras_de_molde` e `limite_de_glifo_unico` da ferramenta para `l2scanner/mercado_leitura.py`, com as docstrings inteiras, e trocar a definicao da ferramenta por um import — o precedente e literal e esta escrito no charter do proprio modulo, que registra quais primitivas foram MOVIDAS e por que copiar teria produzido duas versoes envelhecendo separadas. Promover tambem a DP como `particionar_run`.

O LIMITE E DERIVADO, E ISSO PRECISA ESTAR ESCRITO ONDE ALGUEM VAI PROCURAR. Na docstring de `limite_de_glifo_unico`, registrar as tres coisas que sustentam a decisao. Primeira, o que ele e: o maior valor de `largura` entre os moldes de UM caractere do proprio `calibration.json`, e nao uma chave. Segunda, por que ele nao vira chave: uma copia gravada seria uma segunda verdade sobre uma geometria que ja tem dona, e na recalibracao seguinte ela envelheceria contra os moldes que descreve — o mesmo argumento que `layout_confere` ja escreve sobre o `dx` do cabecalho nao ser gravado duas vezes. Terceira, a medicao que mostra que ele cai num vale e nao numa zona cinzenta, com o numero da Task 1 e a data.

A GUARDA E A PARTICAO EM `ler_glifos`, E A RAZAO DO LUGAR NA DOCSTRING. Registrar que `ler_glifos` foi escolhida por possuir as quatro coisas que a decisao precisa — a mascara, os moldes, o piso e a margem — e por ter UM ponto de chamada de producao, contra os 35 de `segmentar_glifos`, que alem disso nao tem molde nenhum e nao poderia pontuar um corte. Registrar tambem por que `pontuar_glifos` NAO e o lugar: ela nao tem piso de proposito, para que a medicao nao seja circular, e a razao ja esta escrita na docstring dela.

A CAUSA, NO FONTE, COM O PIXEL. Na docstring da guarda, registrar o mecanismo medido: a barra horizontal do molde `4` ocupa as seis colunas da caixa dele, o vizinho colado compartilha a coluna de fronteira, e a projecao nao acha coluna vazia — a regra de `segmentar_glifos` nao tem o que separar. Registrar que baixar o piso de brilho NAO resolve (sondado em quatro pisos, o run continua unico) e que subir demais apaga a coluna: o defeito e de GEOMETRIA e nao de brilho, e essa frase evita que a proxima pessoa gaste uma tarde no piso.

A FOLGA DE COLA, E POR QUE ELA E A UNICA CHAVE NOVA. Na docstring de `larguras_com_folga`, registrar que ela e o unico numero LIVRE do mecanismo — as larguras vem dos moldes, o limite vem das larguras, e so a folga nao se deriva de nada. Registrar o achado que dimensiona o risco, com os numeros da Task 1: afrouxar as larguras permitidas nao degrada devagar, ela passa a INVENTAR numero em bloco, e a sondagem do planejador mediu o caso concreto — um run onde o corte com o score mais alto no primeiro segmento produz o numero ERRADO e o corte certo e um pixel mais largo. Registrar o par de scores dos dois cortes e o intervalo aritmetico que decide entre eles.

`ler_celula` DERIVA E REPASSA. Ela ja recebe `moldes`; derivar o limite ali e o que garante uma verdade so. `folga_de_cola` ela apenas repassa, somente-nomeado e sem valor de fabrica, no mesmo formato de `valor_minimo` que o 02-07 acabou de acrescentar ao lado. Propagar por `ler_celula_de_numero`, `ler_celula_de_quantidade` e `ler_linha`, e atualizar o ponto de chamada em `mercado_pagina.py` na mesma passagem.

A CHAVE EM `calibracao.py`, no molde exato da irma que o 02-07 acrescentou: `.get` opcional em `carregar`, uma funcao propria de conferencia de faixa chamada por `_conferir_as_chaves_de_mercado`, e o comentario que diz a faixa valida e o que um valor errado custa. **Sem bump de esquema** — os 13 moldes e as 3 ancoras do usuario custariam uma tarde dele.

O PORTAO POR AUSENCIA em `LeitorDePagina`, no mesmo formato das outras chaves: a ausencia liga a GUARDA, avisa alto, e o scanner sobe. Registrar no comentario que aqui a ausencia degrada para MAIS seguro e nao para o comportamento de hoje — a celula com run largo cai FECHADA —, e que e por isso que essa e a unica ausencia da fase que nao precisa desligar o recurso inteiro.

OS TESTES. Estender `test_nenhum_limiar_tem_valor_de_fabrica` para os dois parametros novos, por `inspect.signature`. Acrescentar a afirmacao de que `segmentar_glifos` e `segmentar_glifos_no_brilho` mantem a lista de parametros esperada, tambem por `inspect.signature`. Prender por VALOR as leituras das duas fixturas de controle e das tres fixturas coladas, com a de `f105` afirmando o numero lido e nao so que leu. Copiar a chave nova VERBATIM da calibracao de producao para `calibracao_de_fixture.json`, como as outras chaves de mercado — a fixtura nunca inventa valor proprio, e o teste de `tests/test_calibracao_mercado.py` que afirma que as chaves novas sao OPCIONAIS ganha mais uma.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -m pytest tests/test_mercado_leitura.py tests/test_mercado_pagina.py tests/test_calibracao_mercado.py tests/test_mercado_glifos.py -q</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -c 'import inspect; from l2scanner.mercado_leitura import ler_glifos, ler_celula, ler_celula_de_numero, ler_celula_de_quantidade, ler_linha, segmentar_glifos, segmentar_glifos_no_brilho; E=inspect.Parameter.empty
EXIGIDOS=[(ler_glifos,"largura_maxima_de_glifo"),(ler_glifos,"folga_de_cola"),(ler_celula,"folga_de_cola"),(ler_celula_de_numero,"folga_de_cola"),(ler_celula_de_quantidade,"folga_de_cola"),(ler_linha,"folga_de_cola")]
assert len(EXIGIDOS)==6, "a lista de exigidos foi encolhida"
for f,p in EXIGIDOS: assert inspect.signature(f).parameters[p].default is E, f.__name__+"."+p+" TEM valor de fabrica"
assert list(inspect.signature(segmentar_glifos).parameters)==["recorte"], "assinatura de segmentar_glifos MUDOU"
assert list(inspect.signature(segmentar_glifos_no_brilho).parameters)==["recorte","valor_minimo"], "assinatura de segmentar_glifos_no_brilho MUDOU"
print("6 de 6 sem valor de fabrica; 2 assinaturas intactas")'</automated>
  </verify>

  <acceptance_criteria>
    - `inspect.signature(ler_glifos)` mostra `largura_maxima_de_glifo` e `folga_de_cola` com
      `default is inspect.Parameter.empty`, e o mesmo para `folga_de_cola` em `ler_celula`,
      `ler_celula_de_numero`, `ler_celula_de_quantidade` e `ler_linha`. Afirmado por
      `inspect.signature`, nunca por leitura de codigo: um valor de fabrica aqui e uma constante
      magica no caminho que decide preco, e a quantidade multiplica o preco no CSV da Fase 3.
      **A CONFERENCIA TEM DE AFIRMAR, e nao apenas imprimir.** Uma list-comprehension com `if`
      sobre as seis assinaturas NAO serve: sobre o defeito ela produz lista vazia, nao imprime
      nada e **sai com codigo 0** — ela passaria exatamente sobre o que existe para pegar. O
      `<automated>` desta task usa `assert` com mensagem por parametro, e afirma tambem que a
      lista tem os SEIS itens, para que encolher a lista nao seja um jeito silencioso de passar
    - `inspect.signature(segmentar_glifos)` e `inspect.signature(segmentar_glifos_no_brilho)` tem
      exatamente a lista de parametros de hoje. Os 35 pontos de chamada continuam recebendo os
      mesmos runs, e nenhum deles foi tocado
    - AS CELULAS ESTREITAS SAO IDENTICAS, preso por VALOR: `glifos_precos_f010.png` e
      `glifos_unitario_f010.png` produzem os mesmos runs e o mesmo texto de antes desta onda. Se um
      deles mudar, o mecanismo vazou para quem nao pediu — e este e o criterio central da task,
      acima de qualquer ganho
    - AS DUAS POPULACOES DE ERRO ABERTO FICAM VAZIAS. Rodar a ferramenta da Task 1 sem `--gravar`
      depois desta task — uma passagem unica pelo censo, com a colheita reaproveitada, dentro do
      mesmo orcamento de relogio da Task 1 — e conferir no RELATORIO 3 que o balde LE ERRADO da
      folga gravada esta VAZIO
      nas DUAS populacoes rotuladas — a Quantity contra o rotulo derivado, e as colunas de moeda
      contra o rotulo de intervalo. Este e o criterio que vale nos DOIS vereditos da Task 1
    - Se a Task 1 disse `PROPOSTO`: o rendimento de linhas com as tres colunas aceitas e
      ESTRITAMENTE MAIOR que o de antes, e o numero das duas pontas fica no SUMMARY
    - Se a Task 1 disse `REPROVADO`: a chave gravada e a folga `0`, a producao roda a GUARDA pura, e
      o criterio de rendimento passa a ser o VAZIO das duas populacoes de erro aberto — nao o ganho.
      A queda de rendimento medida entra no SUMMARY e uma linha entra em `.planning/WINDOWS.md` com
      o numero que derrubou a folga
    - As tres fixturas coladas leem os valores esperados com a folga gravada, e a de `f105` afirma o
      NUMERO lido e nao apenas que leu — o corte errado tambem produz numero que a gramatica aceita,
      entao um teste de "leu" passaria sobre o bug
    - As tres fixturas coladas devolvem `None` com `folga_de_cola` valendo `None`: a guarda pura
      falha FECHADA, e isso e afirmado e nao suposto
    - A chave e OPCIONAL: `Calibracao.carregar` sobre um JSON sem ela devolve `None` no campo e nao
      levanta, e `VERSAO_DO_ESQUEMA` segue em 2. Preso pelo teste de chaves opcionais que ja existe
    - A calibracao de fixtura carrega a chave copiada VERBATIM da calibracao de producao
    - A bateria completa passa: `2122 passed, 2 skipped` sem `tests/test_agenda.py` e `132 passed`
      so com ele, mais os testes novos. **Abortar nao e falhar** — `tests/test_agenda.py:1141`
      levanta `KeyboardInterrupt` de proposito e as vezes derruba a sessao; rodar de novo
    - `git status --short` nao lista `calibration.json`
  </acceptance_criteria>

  <done>
`ler_glifos` recusa tratar um run mais largo que o maior molde como um glifo so, e a folga MEDIDA
chega ate ela por parametro sem valor de fabrica, atravessando `ler_celula`, as duas leituras de
coluna, `ler_linha` e `LeitorDePagina`. As celulas estreitas leem identico, preso por valor. As
duas populacoes de erro aberto estao vazias, conferidas pela ferramenta da Task 1. As assinaturas
de `segmentar_glifos` e `segmentar_glifos_no_brilho` estao intactas, conferidas por
`inspect.signature`. `VERSAO_DO_ESQUEMA` segue em 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela do jogo -> `mercado_leitura` | pixels NAO CONFIAVEIS cruzam aqui: tooltip semitransparente, marcacao de alvo, glifos colados, e o mundo atras do painel. Nada aqui e validado por origem — so por peneira |
| `calibration.json` -> producao | dado de maquina, produzido por ferramenta e editavel a mao. Um numero fora de faixa vira decisao errada dentro do tick |
| `recordings/` -> ferramenta de medicao | somente leitura, 16 pastas das quais 8 sao o censo. Um glob traria a `pre-voo` com 1.502 PNGs e dominaria a distribuicao |
| leitura -> CSV da Fase 3 | a fronteira que este plano existe para proteger: o que atravessa vira dado permanente que o usuario entrega a outra IA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Tampering | `ler_glifos` / `pontuar_glifos` | **high** | mitigate | Um run mais largo que qualquer molde e casado contra UM molde e vira UM digito, com score e margem que atravessam as duas peneiras — falha ABERTA, medida em 14 quantidades e 55 totais. A guarda em `ler_glifos` recusa tratar esse run como glifo unico, e a particao so entrega texto quando TODOS os segmentos passam no piso E na margem |
| T-02-08-02 | Tampering | `particionar_run` | **high** | mitigate | A propria particao pode inventar: medido, com as larguras afrouxadas ela produz 54 numeros errados e plausiveis. Mitigado por tres coisas juntas — as larguras permitidas sao DERIVADAS dos moldes mais a folga MEDIDA, a Task 1 recusa propor com o balde LE ERRADO nao vazio, e o rotulo das colunas de moeda e um intervalo aritmetico que nao toca um pixel |
| T-02-08-03 | Spoofing | rotulo de medicao | **high** | mitigate | Um rotulo circular (medir a leitura contra ela mesma) faria a ferramenta aprovar o proprio defeito — o modo de falha que o 02-02 ja refutou. Mitigado exigindo que o rotulo de intervalo so exista quando as OUTRAS duas colunas leem LIMPO (aceitas E sem run acima do limite), e reportando quantas celulas tiveram rotulo disponivel |
| T-02-08-04 | Denial of Service | `particionar_run` dentro do tick | medium | mitigate | A enumeracao de particoes foi medida e passou de 10 minutos no censo. Mitigado pela DP com poda por piso e margem, custo linear em `W x larguras`, com o pior caso medido e reportado no RELATORIO 4 e uma PARADA acima de 200 ms. Uma peneira que estoura o tick para o scanner de olhar a party |
| T-02-08-05 | Information Disclosure | `calibration.json` | medium | mitigate | 13 moldes e 3 ancoras que so a mao do usuario produz, ja perdidos uma vez. Gitignored, gravado por load-mutate-save com `os.replace`, e portao de PRESENCA (contagens) somada a IDENTIDADE (md5) antes E depois — igualdade sozinha nao e presenca |
| T-02-08-06 | Elevation of Privilege | fronteira mercado/detector de morte | low | accept | Este plano nao toca `l2scanner/rastreador.py` nem o gate de brilho da barra propria em `l2scanner/visao.py`. Acoplar o sinal de mercado ao detector de morte e a manobra que causou o incidente 27x; o consumidor de oclusao e DETC-02, na Fase 4 |
| T-02-08-07 | Repudiation | veredito da medicao | low | mitigate | A ULTIMA linha do relatorio diz `PROPOSTO` ou `REPROVADO` em maiusculas com a causa, e o veredito com os tres baldes vai para o SUMMARY e para `.planning/WINDOWS.md`. Um numero que caiu precisa dizer que caiu |
| T-02-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | **Este plano nao instala pacote nenhum.** `numpy`, `opencv-python` e o resto ja estao na arvore desde a Fase 1, e nenhuma dependencia nova entra. A auditoria de legitimidade nao tem objeto aqui. FIRE-01 segue valendo: nenhuma biblioteca de sintese de input entra na arvore |
</threat_model>

<verification>
1. **A convencao de largura esta certa e presa.** `glifos_unitario_f010.png` le `6,00` com larguras
   `4, 1, 4, 4` por `fim - inicio`, contra moldes de 4 px e uma virgula de 1 px. Sob a convencao
   inclusiva daria `5, 2, 5, 5`, que nao casa com molde nenhum — e foi esse erro de uma unidade que
   fez a faixa suspeita engolir todo `4` legitimo no alarme original.
2. **O vale existe.** O RELATORIO 1 mostra ZERO celulas ACEITAS com run entre o maior molde e o
   menor run largo. Se nao estiver vazio, a Task 2 nao comeca.
3. **As duas populacoes de erro aberto ficam vazias.** A ferramenta da Task 1, rodada depois da
   Task 2, mostra o balde LE ERRADO da folga gravada VAZIO na Quantity (contra o rotulo derivado) e
   nas colunas de moeda (contra o rotulo de intervalo).
4. **As celulas estreitas nao mudaram.** As duas fixturas de controle produzem os mesmos runs e o
   mesmo texto, preso por valor.
5. **As assinaturas de segmentacao estao intactas**, por `inspect.signature`, e nenhum dos 35 pontos
   de chamada foi tocado.
6. **Nenhum limiar tem valor de fabrica**, por `inspect.signature`, nas seis funcoes da cadeia.
7. **A calibracao do usuario esta intacta:** 13 moldes e 3 ancoras, conferidos por presenca E por
   md5, antes e depois, e `calibration.json` nao aparece no `git status`.
8. **A bateria completa passa** — `2122 passed, 2 skipped` sem `tests/test_agenda.py`, `132 passed`
   so com ele, mais os testes novos. Abortar por `KeyboardInterrupt` nao e falhar.
</verification>

<success_criteria>
- A ferramenta mede o vale, o custo da guarda por celula/linha/pagina, e a folga de cola nos tres
  baldes contra DOIS rotulos nao circulares, e emite `PROPOSTO` ou `REPROVADO` na ultima linha.
- `mercado_folga_de_cola_do_glifo` esta no `calibration.json` da maquina do usuario, MEDIDA, e
  chega a `ler_glifos` por parametro sem valor de fabrica atravessando toda a cadeia.
- Um run mais largo que o maior molde nunca mais vira UM digito: ou ele se parte em glifos que
  todos passam no piso e na margem, ou a celula cai FECHADA.
- As celulas que hoje leem sem run largo leem exatamente o mesmo, preso por valor.
- As tres fixturas coladas estao versionadas com proveniencia declarada e presas por VALOR.
- O veredito, os tres baldes e as duas populacoes rotuladas estao no SUMMARY, e a linha
  correspondente esta em `.planning/WINDOWS.md`.
- Fica registrado no SUMMARY que este plano **remove a causa da REPROVA do 02-07** (o balde LE
  ERRADO do piso compartilhado deixa de existir) e que a varredura de piso daquele plano volta a ser
  proponivel — sem re-roda-la aqui, porque ela passa de 10 minutos e ja matou um executor.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-08-SUMMARY.md` when done
</output>