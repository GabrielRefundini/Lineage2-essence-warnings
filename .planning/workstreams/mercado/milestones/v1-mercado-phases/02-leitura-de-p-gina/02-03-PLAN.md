---
phase: 02-leitura-de-p-gina
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - calibration.json
  - l2scanner/mercado_catalogo.py
  - tools/medir_agrupamento_de_nome.py
  - tests/test_mercado_catalogo.py
  - tests/test_medir_agrupamento_de_nome.py
  - tests/fixtures/mercado/nome_linha0_f000.png
  - tests/fixtures/mercado/nome_linha0_f010.png
  - tests/fixtures/mercado/nome_sob_tooltip_f015.png
  - tests/fixtures/mercado/leituras_de_nome.json
autonomous: false
requirements: [LEIT-01]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Empate EXATAMENTE no corte agrupa e empate exatamente no piso descarta; e um empate de similaridade entre DUAS series diferentes e desempatado de forma deterministica (maior score, depois ordem lexicografica da chave), nunca pela ordem de insercao do dicionario"
    - "Leitura vazia nunca produz chave nem serie: `chave_da_serie` devolve None para nome vazio, e um catalogo vazio na primeira execucao e estado legitimo — a primeira serie nasce dele"
    - "O corte de similaridade e REMEDIDO com a metrica que vai ser usada (`difflib.SequenceMatcher`), sobre as leituras reais das 8 gravacoes — o 88 foi escolhido pensando no `WRatio`, que e um scorer composto, e virou heranca de outro contexto (D-05)"
    - "A trava de digitos separa `+6 X` de `+4 X` e `Lv. 1` de `Lv. 3` POR CONSTRUCAO, nao por limiar — medido: `WRatio` da 96,30 para `Lv. 1`x`Lv. 3` (precisa separar) e 96,30 para `Lv. I`x`Lv. 1` (precisa agrupar), e o mesmo numero teria de decidir coisas opostas (D-03)"
    - "A FONTE da assinatura de digitos foi MEDIDA contra o gabarito conhecido `+6/+4/+2/+7/(nenhum)/+5/+7/+5/+7/+6` antes de virar decisao — a pesquisa marcou isso como suposicao A8 de risco ALTO, e ela decide se D-02 e D-03 convivem"
    - "A faixa cinzenta existe: entre o piso e o corte a linha nao agrupa NEM cria serie, e cai com aviso — fusao no CSV e irreversivel, descarte nao e (D-06)"
    - "Nenhuma dependencia nova entra na arvore: o agrupamento e `difflib` da stdlib, e `rapidfuzz` foi recusado por decisao travada (D-04). A prova e sobre o GRAFO DE IMPORT e nao sobre o texto do fonte — a docstring cita o nome de proposito, para registrar por que ele caiu"
    - "As TRES rotas do portao de decisao chegam a mesa com numero medido, inclusive a contagem de series duplicadas que `ocr-igualdade` criaria: uma opcao que chega sem medicao nao e opcao, e armadilha"
    - "A varredura roda sobre as 8 gravacoes NOMEADAS do censo e ignora as outras 8 pastas de `recordings/`, dizendo quais pulou e por que"
    - "Este plano espera o 02-02 gravar antes de gravar: `os.replace` escreve o `calibration.json` INTEIRO, e duas gravacoes cruzadas apagariam chaves uma da outra em silencio"
    - "A ferramenta importa os predicados de PRODUCAO em vez de reescreve-los — medir com uma implementacao e decidir com outra seria comparar convencoes"
  artifacts:
    - path: "l2scanner/mercado_catalogo.py"
      provides: "os predicados PUROS do agrupamento: assinatura de digitos (as duas fontes candidatas), similaridade por difflib e a chave da serie"
      contains: "SequenceMatcher"
    - path: "tools/medir_agrupamento_de_nome.py"
      provides: "varredura de OCR 2x/3x sobre a coluna do nome calibrada nas 8 gravacoes nomeadas; histograma, censo do conflito entre as escalas, rendimento das TRES rotas com series duplicadas projetadas; propoe e grava corte e piso"
      contains: "mercado_corte_de_similaridade"
    - path: "tests/test_mercado_catalogo.py"
      provides: "a trava de digitos e a similaridade afirmadas contra os pares medidos na pesquisa"
      contains: "assinatura"
    - path: "tests/fixtures/mercado/leituras_de_nome.json"
      provides: "as leituras 2x/3x resgatadas das gravacoes, versionadas, para o teste rodar num clone limpo sem OCR"
      min_lines: 10
  key_links:
    - from: "tools/medir_agrupamento_de_nome.py"
      to: "l2scanner/mercado_catalogo.py"
      via: "a ferramenta importa `similaridade` e as duas assinaturas de producao; ela nao tem copia propria de nenhuma"
      pattern: "from l2scanner.mercado_catalogo import"
    - from: "tools/medir_agrupamento_de_nome.py"
      to: "l2scanner/ocr.py"
      via: "le pelo caminho publico que NUNCA levanta, nunca por `_reconhecer` cru"
      pattern: "ocr."
---

<objective>
Remedir o corte de agrupamento com a metrica que vai ser usada, e fechar por medicao a
suposicao de risco ALTO que decide se as duas decisoes travadas de LEIT-01 conseguem conviver.

O contexto: a pesquisa derrubou `rapidfuzz.WRatio` com corte 88 contra a implementacao de
referencia do proprio rapidfuzz. `Common Aztac` x `Common Aztac M. Def. +200` da **90,00** —
acima do corte, as duas series se fundem, e esse e o par que a decisao citava como motivo de
existir. Entraram no lugar `difflib` da stdlib (D-04) mais uma trava de digitos (D-03). Mas o
88 nasceu de um scorer composto; trocada a metrica, ele e heranca (D-05). E o corte tem de sair
de varredura, nao de escolha.

**E ha um conflito entre duas decisoes travadas que so a medicao resolve.** D-02 diz que as
escalas 2x e 3x concordam quando caem na MESMA SERIE, e cita `Lv. I` contra `Lv. 1` como
exatamente o ruido que o agrupamento tem de absorver. D-03 diz que a sequencia ordenada de
digitos tem de bater EXATAMENTE antes de qualquer similaridade. Aplicadas juntas sobre a
leitura de OCR: `Lv. I` tem assinatura vazia e `Lv. 1` tem assinatura `1`, elas nao batem, as
duas escalas caem em series diferentes e a linha morre — em
`scroll-transicao/frame_000016` isso e 10 de 10 linhas, a pagina inteira, que e precisamente a
perda que D-02 foi escrito para evitar. Os `Hardin's Soul Crystal Lv. N` ficariam
permanentemente ilegiveis.

A saida que a pesquisa aponta e ler o digito do nome com os MOLDES, nao com o OCR — a fonte e a
mesma fonte do jogo, e o motor de molde leu 25 de 25 precos sem erro. Mas isso e a suposicao A8,
marcada como risco ALTO e nunca medida.

Purpose: essa medicao decide a forma da CHAVE DA SERIE, que vai para o disco e que a Fase 3
referencia no CSV de observacoes. Chave errada nao se conserta depois sem orfanar serie — e
uma porta de mao unica, e por isso ela tem portao.

Output: `mercado_catalogo.py` nascido com os predicados puros, uma ferramenta de varredura, dois
numeros medidos no `calibration.json`, e uma decisao humana registrada sobre a fonte da
assinatura de digitos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-RESEARCH.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Os predicados puros do agrupamento, e a varredura que mede o corte e o conflito das escalas</name>
  <precondition>O 02-02 JA GRAVOU as chaves dele. Este plano tambem escreve `calibration.json` por load-mutate-save, e as duas gravacoes nao podem se cruzar: `os.replace` grava o arquivo INTEIRO, entao a segunda apagaria as chaves da primeira, calada. Por isso `depends_on` inclui `02-02` e `calibration.json` esta em `files_modified` — o guarda de wave so enxerga a corrida se o arquivo estiver declarado, e ele e gitignored. `calibration.json` traz `mercado_coluna_do_nome` e `mercado_grade.layout == "negociacao"` — o portao humano do 02-01 ja rodou. `recordings/` so existe no checkout principal; num worktree, ler pelo caminho ABSOLUTO, somente leitura. O motor de OCR so responde no `.venv` (`ocr.disponivel()` e `False` no Python global, que e onde o pytest roda).</precondition>
  <files>l2scanner/mercado_catalogo.py, tools/medir_agrupamento_de_nome.py, tests/test_mercado_catalogo.py, tests/test_medir_agrupamento_de_nome.py, tests/fixtures/mercado/leituras_de_nome.json, tests/fixtures/mercado/nome_linha0_f000.png, tests/fixtures/mercado/nome_linha0_f010.png, tests/fixtures/mercado/nome_sob_tooltip_f015.png</files>
  <read_first>
    - `l2scanner/ocr.py` inteiro, com atencao em: a tabela de escalas medida (:28-79), o
      comentario de :36-39 sobre registrar uma medicao refutada, `_ler`/`_reconhecer` (:195-260)
      e a docstring que promete que `_ler` NUNCA levanta
    - `l2scanner/manutencao.py:384-402` (a injecao de leitora no `__init__`) e `:466-527`
      (`_ler_com_as_duas_escalas` e `_registrar_desacordo`) — o acordo entre duas escalas ja
      escrito, com o desacordo tratado como estado proprio e o log com delimitadores `>>><<<`
    - `l2scanner/manutencao.py:131-143` (`_normalizar_digitos`) — o parente mais proximo da
      trava de digitos, e a razao de ele NAO servir aqui: ele so troca `O/l/I/S` em token que ja
      tem digito de verdade, entao `Lv. I` sai intacto
    - `l2scanner/calibrar_mercado.py:359-455` (`segmentar_glifos`) e `:1596-1643`
      (`propor_rotulo`) — o caminho de molde, para a assinatura por molde
    - `l2scanner/loot.py:189` — `# NAO tem poda, de proposito`
    - `02-RESEARCH.md`, pergunta 3 inteira (a tabela WRatio/ratio/partial_ratio, o fonte do
      `WRatio` que explica o 90,00, a comparacao difflib x rapidfuzz) e Pitfall 3 (o acordo
      estrito rendendo 30/60, e 0/10 em `scroll-transicao/frame_000016`)
    - `02-RESEARCH.md`, Assumptions Log A6 e A8, e Open Question 2 — o gabarito de encanto
    - `02-CONTEXT.md`, secao "Leitura do nome por OCR" — as sete decisoes travadas
  </read_first>
  <behavior>
    - `assinatura_por_ocr("+6 Agathion Alpha Hunter Sealed")` devolve `"6"`; para
      `"+4 Agathion Alpha Hunter Sealed"` devolve `"4"`; as duas nao batem, entao os dois nomes
      nunca agrupam por mais alta que seja a similaridade
    - `assinatura_por_ocr("Hardin's Soul Crystal Lv. 1")` e `... Lv. 3` nao batem
    - `assinatura_por_ocr("Hardin's Soul Crystal Lv. I")` devolve `""` e NAO bate com `"1"` —
      este e o conflito que a Task 2 resolve, e o teste o prende por escrito em vez de esconder
    - `similaridade("Earth Spirit Evolution Stone", "Earth Spirit Ewlution Stone")` fica acima
      de 0,90; `similaridade("Common Aztac", "Common Aztac M. Def. +200")` fica abaixo de 0,70
    - `similaridade` de duas strings vazias, ou de uma vazia contra qualquer outra, devolve 0,0
      e nunca levanta
    - Nome vazio ou so com espaco nunca produz chave: `chave_da_serie` devolve `None`
    - Duas entradas de catalogo que empatam na similaridade sao desempatadas de forma
      DETERMINISTICA (maior score, depois ordem lexicografica da chave) — nunca pela ordem de
      insercao do dicionario
    - Empate EXATAMENTE no corte agrupa; empate exatamente no piso descarta
  </behavior>
  <action>
Criar `l2scanner/mercado_catalogo.py` com o charter de modulo puro nesta primeira etapa: sem
I/O de arquivo ainda (isso e o 02-05), so os predicados. Ele ganha:

- `assinatura_por_ocr(nome: str) -> str` — a sequencia ORDENADA dos digitos do nome lido, a
  trava de D-03. Ela e comparada por igualdade exata antes de qualquer similaridade.
- `assinatura_por_molde(recorte_bgr, moldes, piso, margem) -> str | None` — a segunda fonte
  candidata: os digitos do nome lidos pelo mesmo motor de molde que le o preco, via
  `segmentar_glifos` + argmax com margem. Devolve `None` quando qualquer run reprova (tudo ou
  nada, como `propor_rotulo`).
- `similaridade(a: str, b: str) -> float` — `difflib.SequenceMatcher(None, a, b).ratio()`, com
  a docstring registrando por que NAO e `rapidfuzz`: ele nao esta instalado nem no
  `requirements.txt`, e sobre estes nomes (curtos, sem tokens reordenados, diferencas de 1-2
  caracteres) as duas metricas dao praticamente o mesmo numero — medido, `Evolution`x`Ewlution`
  da 94,5 e 94,55. E registrar por que NAO e `WRatio`: ele cai no ramo do `partial_ratio` quando
  as strings tem razao de tamanho >= 1,5, e `Common Aztac` sendo substring perfeita de
  `Common Aztac M. Def. +200` produz `100 x 0,9 = 90,00`. O corte de 88 fundiria as duas.
- `chave_da_serie(nome: str, assinatura: str) -> str | None` — slug estavel sem espaco, com a
  assinatura ANEXADA de forma explicita, para que a fusao de series seja impossivel na propria
  chave. `None` para nome vazio.
- `agrupar(leitura: str, assinatura: str, catalogo, corte, piso) -> ResultadoDoAgrupamento` —
  candidatos filtrados pela assinatura; a similaridade decide so o resto; `>= corte` agrupa,
  `piso <= s < corte` cai na faixa cinzenta e DESCARTA com aviso, `< piso` e serie nova.
  Desempate deterministico e documentado.
- `ResultadoDoAgrupamento` dataclass: `chave: str | None`, `motivo: str`, `nova: bool`.

`chave_da_serie` recebe a assinatura JA CALCULADA em vez de escolher a fonte sozinha — a fonte
e a decisao da Task 2, e deixar a escolha no chamador e o que permite decidi-la sem reescrever
este modulo.

Escrever `tools/medir_agrupamento_de_nome.py`, com `--gravacoes`, `--calibracao` e `--gravar`.
Ela IMPORTA os predicados acima de producao; nao tem copia de nenhum. Para cada frame com
painel aberto das 8 gravacoes: localiza o painel, recorta a COLUNA DO NOME calibrada de cada
linha, le com OCR 2x e 3x pelo caminho publico de `ocr.py` (nunca `_reconhecer` cru — ele estoura
onde `_ler` devolve `None` limpo), e produz QUATRO relatorios, todos no SUMMARY:

1. HISTOGRAMA DA SIMILARIDADE, separado em duas populacoes: pares que PRECISAM agrupar (as
   discordancias de 1-2 caracteres entre as escalas sobre a mesma linha do mesmo frame) e pares
   que PRECISAM separar (leituras de linhas diferentes do mesmo frame). Propoe `corte` e `piso`
   como os pontos que separam as duas populacoes com a maior folga, e declara a folga. Se elas
   se sobrepuserem, NAO propoe: imprime os pares que se sobrepoem e sai com codigo diferente de
   zero. Rodar o histograma com e sem a trava de digitos, para mostrar quanto a trava sozinha
   ja resolve.

2. CENSO DO CONFLITO ENTRE AS ESCALAS. Quantas linhas, de quantos frames, as duas escalas
   produzem assinaturas de digito DIFERENTES — isto e, quantas linhas D-02 e D-03 matam juntas
   quando a assinatura vem do OCR. Nomear os frames e as linhas. A pesquisa mediu 10 de 10 em
   `scroll-transicao/frame_000016`; conferir se o numero se sustenta na varredura inteira.

3. A FONTE DA ASSINATURA, medida (A8). Rodar `assinatura_por_molde` sobre o recorte do prefixo
   de encanto das dez linhas de `20260828-063752-mercado-aberto/frame_000000`, cujo gabarito
   conhecido e `+6 / +4 / +2 / +7 / (nenhum) / +5 / +7 / +5 / +7 / +6`. Reportar acerto,
   descarte e erro, e a distribuicao de score e margem — e comparar com a assinatura por OCR
   sobre as mesmas dez linhas. Reportar tambem o caso `Lv. N`, que tem o digito no meio do nome
   e nao no prefixo.

4. RENDIMENTO DAS TRES ROTAS. A Task 2 poe TRES saidas na frente do usuario, entao as tres
   precisam de numero — uma opcao que chega a mesa sem medicao nao e uma opcao, e uma armadilha.
   Para cada uma, a taxa de linhas que sobrevivem ao acordo entre escalas nas 8 gravacoes,
   como "li N, perdi M":

   - **`molde`** — assinatura pelos moldes de digito.
   - **`ocr-estrito`** — assinatura por OCR, com a linha caindo quando as escalas discordam no
     digito.
   - **`ocr-igualdade`** — assinatura por OCR, mas nome COM digito sai da rota de similaridade e
     exige igualdade exata de string entre as escalas. Esta muda o PREDICADO, entao alem do
     rendimento ela precisa do numero que decide contra ela: **quantas SERIES DUPLICADAS ela
     criaria** ao longo das 8 gravacoes — series que sao o mesmo item com o digito lido de dois
     jeitos. Reportar a contagem, e listar as duplicatas projetadas com as duas grafias lado a
     lado, porque o argumento contra essa rota e lixo no catalogo que a Fase 3 vai referenciar em
     cada observacao, e lixo se mede contando.

Com `--gravar`, escreve `mercado_corte_de_similaridade` e `mercado_piso_de_similaridade` por
load-mutate-save.

Resgatar para `tests/fixtures/mercado/` o material que os testes precisam: os recortes de coluna
do nome `nome_linha0_f000.png` (de `063752-mercado-aberto/frame_000000`, com o `+6`),
`nome_linha0_f010.png` (de `pagina-cheia/frame_000010`) e `nome_sob_tooltip_f015.png` (de
`tooltip/frame_000015`, o vazamento medido); e `leituras_de_nome.json`, com as leituras 2x/3x
que a varredura produziu, para que o teste dos predicados rode num clone limpo SEM motor de
OCR. Todo teste que exercite OCR de verdade pula com `pytest.skip` quando `ocr.disponivel()` for
`False`, e diz a razao — o pytest roda no Python GLOBAL, que nao tem as bindings WinRT.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_catalogo.py tests/test_medir_agrupamento_de_nome.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_catalogo.py -q` passa, com testes que afirmam cada
      par medido da pesquisa: `Common Aztac`x`Common Aztac M. Def. +200` abaixo de 0,70;
      `Evolution`x`Ewlution` acima de 0,90; `+6 X`x`+4 X` recusado pela assinatura; `Lv. 1`x
      `Lv. 3` recusado pela assinatura; `Lv. I`x`Lv. 1` com assinaturas diferentes (o conflito,
      preso por escrito)
    - Existe teste que afirma o desempate deterministico e as duas bordas: empate no corte
      agrupa, empate no piso descarta
    - `python -c "import sys, l2scanner.mercado_catalogo; assert 'rapidfuzz' not in sys.modules; print('sem rapidfuzz ok')"` imprime `sem rapidfuzz ok` — importar o modulo nao puxa a
      dependencia recusada. A asserção e sobre o GRAFO DE IMPORT, e nao sobre o texto do fonte:
      a docstring do modulo cita `rapidfuzz` de proposito, para registrar por que ele caiu, entao
      um grep negativo sobre o fonte se auto-invalidaria
    - `python -c "import inspect, l2scanner.mercado_catalogo as m; s=inspect.getsource(m); assert 'SequenceMatcher' in s; print('difflib ok')"` imprime `difflib ok` — a asserção positiva de
      que a metrica em uso e a da stdlib
    - `requirements.txt` NAO esta em `files_modified` deste plano: nenhuma dependencia nova entra
    - `python -m pytest tests/test_firewall_escopo.py -q` verde — nenhuma dependencia nova
    - **GATE, no checkout principal e no `.venv`:** a varredura roda sobre as 8 gravacoes nomeadas
      do conjunto de medicao e sai com codigo 0 —
      `.venv/Scripts/python.exe tools/medir_agrupamento_de_nome.py --gravacoes <abs>/recordings --calibracao <abs>/calibration.json`
      — imprimindo os quatro relatorios e a lista das pastas que ignorou. Ela PARA com codigo
      diferente de zero quando alguma das 8 esperadas nao existe
    - Rodada com `--gravar`, `calibration.json` ganha `mercado_corte_de_similaridade` e
      `mercado_piso_de_similaridade`
    - O SUMMARY registra os quatro relatorios com numeros, em especial o rendimento "li N, perdi
      M" das DUAS fontes candidatas de assinatura
  </acceptance_criteria>
  <done>Os predicados puros existem e estao presos por teste contra os pares medidos; o corte e o piso sao numeros de varredura no calibration.json; e o conflito entre D-02 e D-03 esta medido, quantificado e com as duas saidas na mesa.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: DECISAO — de onde vem a assinatura de digitos da chave da serie</name>
  <reversibility rating="one-way">A assinatura entra na CHAVE DA SERIE, que vai para o `catalogo-de-nomes.csv` e que a Fase 3 referencia em cada linha do CSV de observacoes. Trocar a fonte depois orfana toda serie ja gravada: as observacoes antigas apontam para uma chave que a leitura nova nunca mais produz, e nao ha como saber quais eram o mesmo item sem a tela de volta. Reagrupar a posteriori seria adivinhar.</reversibility>
  <read_first>
    - Os relatorios 3 e 4 do `02-03-SUMMARY.md` produzidos pela Task 1 — o relatorio 4 traz o
      rendimento das TRES rotas, e para `ocr-igualdade` traz tambem a contagem de series
      duplicadas projetadas
    - `02-CONTEXT.md`, os bullets D-02 (o predicado do acordo) e D-03 (a trava de digitos)
    - `02-RESEARCH.md`, pergunta 3, propostas 2, 3 e 4
  </read_first>
  <decision>De onde vem a sequencia de digitos que trava o agrupamento de nome — do OCR ou dos moldes de digito — e o que acontece quando as duas escalas de OCR discordam so no digito.</decision>
  <context>
Duas decisoes travadas do usuario colidem, e a colisao e medida.

D-02 diz que as escalas 2x e 3x concordam quando caem na MESMA SERIE, e cita `Lv. I` contra
`Lv. 1` como exatamente o ruido que o agrupamento tem de absorver. D-03 diz que a sequencia
ordenada de digitos do nome tem de bater EXATAMENTE antes de qualquer similaridade — e ela existe
porque `WRatio` da 96,30 tanto para `Lv. 1` x `Lv. 3` (que precisa separar) quanto para
`Lv. I` x `Lv. 1` (que precisa agrupar), e nenhum corte escalar decide coisas opostas com o mesmo
numero.

Aplicadas juntas sobre a leitura de OCR elas se anulam: `Lv. I` tem assinatura vazia e `Lv. 1`
tem assinatura `1`; elas nao batem; as duas escalas caem em series diferentes; a linha morre. Em
`scroll-transicao/frame_000016` isso e 10 de 10 linhas — a pagina inteira, que e precisamente a
perda que D-02 foi escrito para evitar. Os `Hardin's Soul Crystal Lv. N` ficariam permanentemente
ilegiveis.

A Task 1 mediu as saidas. Ponha na frente do usuario, sem arredondar para o lado que agrada: o
acerto de `assinatura_por_molde` contra o gabarito conhecido `+6/+4/+2/+7/(nenhum)/+5/+7/+5/+7/+6`;
o comportamento dela no caso `Lv. N`, cujo digito fica no meio do nome e nao no prefixo; e o
rendimento "li N, perdi M" das TRES rotas sobre as 8 gravacoes — mais, para `ocr-igualdade`,
quantas series duplicadas ela criaria e QUAIS sao, com as duas grafias lado a lado.

E diga o que e irreversivel: a chave da serie vai para o disco e a Fase 3 a referencia em cada
observacao. Mudar de ideia depois nao e um refactor — as series ja gravadas ficam orfas.
  </context>
  <options>
    <option id="molde">
      <name>Assinatura por MOLDE de digito</name>
      <pros>O digito do nome vem do mesmo motor que leu 25 de 25 precos sem erro, e a fonte e a mesma fonte do jogo. A ambiguidade `I`/`1` do OCR deixa de existir na parte que decide a serie, e D-02 e D-03 passam a conviver: `Lv. I` e `Lv. 1` caem na mesma serie e `Lv. 1` e `Lv. 3` continuam separados por construcao.</pros>
      <cons>Depende do acerto medido pela Task 1: brilho, tamanho e antialias do texto do NOME podem diferir dos do preco, e a pesquisa marcou isso como suposicao A8 de risco ALTO. Se os moldes nao casarem o texto do nome, a linha cai por outro motivo. E o `Lv. N` tem o digito no meio do nome, nao no prefixo — a segmentacao precisa alcanca-lo.</cons>
    </option>
    <option id="ocr-estrito">
      <name>Assinatura por OCR, com o custo aceito</name>
      <pros>D-03 fica literal como foi escrito, sem nenhum mecanismo novo. Fusao de serie e impossivel. E a aplicacao direta do principio do usuario: fusao no CSV e irreversivel, descarte nao e.</pros>
      <cons>Os itens cujo digito o OCR le de forma instavel ficam PERMANENTEMENTE ilegiveis — medido, pelo menos os `Hardin's Soul Crystal Lv. N`, e uma pagina inteira perdida num frame real. O usuario ve "perdi 10" no console para uma pagina que os olhos dele leem sem esforco.</cons>
    </option>
    <option id="ocr-igualdade">
      <name>Assinatura por OCR, e igualdade exata de string como acordo para nome COM digito</name>
      <pros>Nome que contem digito sai da rota de similaridade e exige as duas escalas identicas; nome sem digito segue agrupando normalmente. Fusao continua impossivel E a linha nao morre: ela vira uma serie que o usuario VE no catalogo e pode corrigir no Sheets. E o fallback que a pesquisa registrou.</pros>
      <cons>Series duplicadas quando o OCR oscila: `Lv. I` cria uma serie a mais, com contagem de avistamentos baixa. **O numero exato esta no relatorio 4 da Task 1** — quantas duplicatas as 8 gravacoes produziriam, e quais, com as duas grafias lado a lado. O catalogo fica com lixo visivel que alguem precisa limpar, e a Fase 3 grava observacoes contra a chave duplicada enquanto isso.</cons>
    </option>
  </options>
  <resume-signal>Escolha: molde, ocr-estrito ou ocr-igualdade.</resume-signal>
  <verify>
    <human-check>O usuario escolheu a fonte da assinatura de digitos, com o numero medido na frente, e entendeu que a chave da serie e uma porta de mao unica.</human-check>
  </verify>
  <acceptance_criteria>
    - O `02-03-SUMMARY.md` nomeia a fonte escolhida, a razao, e as duas alternativas recusadas com
      o custo de cada uma — um numero que caiu precisa dizer que caiu
    - `02-CONTEXT.md` ganha a anotacao da revisao, no formato riscado que o arquivo ja usa para as
      tres decisoes substituidas em 2026-08-29
    - Nenhuma linha de codigo de `chave_da_serie` foi ligada a uma fonte antes desta decisao:
      `agrupar` e `chave_da_serie` continuam recebendo a assinatura pronta do chamador
  </acceptance_criteria>
  <done>A fonte da assinatura de digitos esta decidida pelo usuario com o numero medido na mesa, e o registro diz o que caiu e por que.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela -> processo (OCR) | Texto lido de pixels que a UI do jogo desenha; uma tooltip semitransparente e conteudo hostil do ponto de vista do leitor |
| ferramenta -> arquivo (`calibration.json`) | A varredura ESCREVE dois numeros no arquivo insubstituivel do usuario |
| arvore de dependencias | Uma biblioteca nova entrando pela porta do agrupamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Spoofing | vazamento de tooltip virando nome de item | high | mitigate | Recorte da COLUNA do nome (LEIT-05) reduz a superficie, e a fixture `nome_sob_tooltip_f015.png` prende por teste que o recorte de coluna SOZINHO nao basta — a sonda de oclusao do 02-04 e que fecha. Medido: com a tooltip por cima a coluna devolve `'In case of success, you will get a high Common Kng F doll...'` |
| T-02-12 | Tampering | fusao de duas series diferentes numa so | high | mitigate | Trava de digitos por igualdade exata ANTES da similaridade, mais faixa cinzenta que descarta em vez de decidir. Fusao no CSV e irreversivel; descarte nao e |
| T-02-13 | Elevation of Privilege (de escopo) | dependencia nova pela porta do agrupamento | high | mitigate | `difflib` da stdlib por decisao travada (D-04); `tests/test_firewall_escopo.py` varre o venv INSTALADO com banlist de 9 nomes e normalizacao PEP 503 |
| T-02-14 | Tampering | `--gravar` sobrescrevendo `calibration.json` | high | mitigate | Load-mutate-save; nunca montar uma `Calibracao` do zero |
| T-02-35 | Tampering | corrida de escrita no `calibration.json` entre este plano e o 02-02 | high | mitigate | `os.replace` grava o arquivo INTEIRO: duas gravacoes cruzadas apagam as chaves uma da outra, em silencio e sem sintoma ate a leitura falhar. Serializado por `depends_on: ["02-01", "02-02"]` E por `calibration.json` declarado em `files_modified` dos dois — o arquivo e gitignored, entao sem a declaracao o guarda de wave nao enxerga a corrida |
| T-02-15 | Denial of Service | `_reconhecer` cru estourando dentro da varredura | medium | mitigate | Ler so pelo caminho publico de `ocr.py`, que promete nunca levantar; testes que exercitem OCR pulam quando `ocr.disponivel()` e `False` |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install nesta fase**, por D-04. `rapidfuzz` foi auditado na pesquisa (veredito `SUS` por `unknown-downloads`, limitacao da API do PyPI) e RECUSADO por decisao travada — a categoria inteira fica fechada |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Modulo novo `l2scanner/mercado_catalogo.py`** (primeira etapa, so predicados puros):
- `assinatura_por_ocr(nome: str) -> str`
- `assinatura_por_molde(recorte_bgr, moldes, piso, margem) -> str | None`
- `similaridade(a: str, b: str) -> float`
- `chave_da_serie(nome: str, assinatura: str) -> str | None`
- `agrupar(leitura, assinatura, catalogo, corte, piso) -> ResultadoDoAgrupamento`
- `ResultadoDoAgrupamento` dataclass: `chave: str | None`, `motivo: str`, `nova: bool`

**Ferramenta nova `tools/medir_agrupamento_de_nome.py`:** `main(argv)`,
`varrer(gravacoes, cal)`, `histograma(pares)`, `censo_do_conflito(...)`,
`medir_a_fonte_da_assinatura(...)`, `rendimento_das_tres_rotas(...)`,
`series_duplicadas_projetadas(...)`, `propor_corte_e_piso(...)`,
`GRAVACOES_DO_CENSO` (a lista das 8, como constante do modulo)

**Flags de CLI novas:** `--gravacoes <caminho> --calibracao <caminho> --gravar`

**Chaves de `calibration.json` PREENCHIDAS** (declaradas no 02-01):
`mercado_corte_de_similaridade`, `mercado_piso_de_similaridade`

**Fixtures versionadas novas:** `tests/fixtures/mercado/nome_linha0_f000.png`,
`nome_linha0_f010.png`, `nome_sob_tooltip_f015.png`, `leituras_de_nome.json`

**Arquivos de teste novos:** `tests/test_mercado_catalogo.py`,
`tests/test_medir_agrupamento_de_nome.py`

**Decisao registrada:** a fonte da assinatura de digitos da chave da serie (uma de tres),
gravada no SUMMARY e anotada em `02-CONTEXT.md`
</artifacts_this_phase_produces>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

O `02-CONTEXT.md` registra as decisoes do usuario em prosa, sem numera-las. Os IDs abaixo
sao estaveis e seguem a ORDEM DE LEITURA da secao `<decisions>` daquele arquivo — eles nao
inventam decisao nenhuma, so dao nome ao que ja esta escrito la.

| ID | Decisao (do `02-CONTEXT.md`) |
|---|---|
| D-02 | O PREDICADO do acordo e "as duas leituras caem na MESMA SERIE do catalogo" (REVISADO 2026-08-29 por medicao: igualdade estrita rendia 30/60, e 0/10 num frame). |
| D-03 | Agrupamento por difflib.SequenceMatcher E uma trava de digitos: a sequencia ordenada de digitos do nome tem de bater EXATAMENTE antes de qualquer similaridade (REVISADO; WRatio + corte 88 caiu). |
| D-04 | difflib da stdlib, e NAO rapidfuzz — nenhuma dependencia nova entra na arvore. |
| D-05 | O CORTE precisa ser REMEDIDO, e com ferramenta. Nenhum corte entra no codigo antes dessa medicao. |
| D-06 | A faixa cinzenta continua existindo: abaixo do corte e acima do piso a linha nao agrupa NEM cria serie — e descartada com aviso. |
</indice_de_decisoes>

<conjunto_de_medicao>
## O conjunto de medicao, nomeado gravacao por gravacao

`recordings/` tem **16 pastas**. O censo de 335 frames da pesquisa cobriu **8**, e sao SO essas
que entram em qualquer varredura desta fase. As ferramentas `tools/medir_*.py` carregam esta lista
como constante e IGNORAM o resto, imprimindo o que pularam e por que — apontar a ferramenta para
a pasta e deixar ela iterar subdiretorios mediria sobre material que o censo nunca viu, e os
limiares mudariam calados.

```
20260828-053105-mercado-aberto
20260828-055323-mercado-scroll
20260828-060622-mercado-pagina-cheia
20260828-061253-mercado-tooltip
20260828-061409-mercado-alvo-sobreposto
20260828-063240-mercado-farm-com-party
20260828-063409-mercado-scroll-transicao
20260828-063752-mercado-aberto
```

**As 8 que ficam de fora, e o motivo de cada uma** (contagem de PNG conferida no disco):

| pasta | PNGs | por que nao entra |
|---|---|---|
| `20260827-225521-pre-voo` | 1502 | anterior ao spike e de outro dia. E a MAIOR pasta do diretorio: um glob que a arrastasse dominaria qualquer distribuicao medida, sozinho |
| `20260828-053003-mercado-fechado` | 33 | e a gravacao do painel FECHADO: nenhum frame com painel aberto, entao nao contribui linha nenhuma para limiar de leitura, de oclusao ou de agrupamento. O censo da pesquisa nao a lista |
| `20260828-115559-calibragem` | 34 | sessao de calibragem, nao de mercado |
| `20260828-115700-calibragem` | 24 | idem |
| `hp_baixo` | 0 | **vazia** — zero PNGs |
| `inv2` | 90 | material de inventario, de outro recurso |
| `inv3` | 54 | idem — e a base do replay do incidente 27x, nao do mercado |
| `inventario` | 30 | idem |

**Se a lista divergir do que existe no disco, a ferramenta PARA** e diz qual pasta esperada nao
achou. Medir sobre um conjunto diferente do censo produz um numero que nao se compara com nenhum
outro numero deste projeto.
</conjunto_de_medicao>

<verification>
- **A varredura E GATE desta task, e roda no checkout PRINCIPAL.** Isso nao contradiz a regra da
  fixture versionada — sao duas coisas diferentes, e a Fase 1 ja enfrentou a distincao:
  - **Produzir o numero e PRODUCAO DE DADO.** A varredura le `recordings/`, que e gitignored e nao
    se materializa num worktree, entao ela roda onde o material existe: no checkout principal, com
    `--gravacoes` e `--calibracao` por caminho ABSOLUTO. Sem ela as chaves ficam nulas e os planos
    seguintes nao tem numero para ler — um plano declarado completo sem a varredura entrega uma
    fase que nao le digito nenhum.
  - **O TESTE de regressao e outra coisa** e roda em qualquer lugar, sobre fixture versionada em
    `tests/fixtures/mercado/`. E ele que fica verde num worktree e num clone limpo, e e por isso
    que nenhum criterio de teste depende de `recordings/`.
- `python -m pytest tests/test_mercado_catalogo.py tests/test_medir_agrupamento_de_nome.py -q` verde
- `python -m pytest tests/test_firewall_escopo.py -q` verde
- Suite completa: `python -m pytest -q`. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um
  `KeyboardInterrupt` que aborta a sessao perto de ~88 testes (5 abortos em 60 rodadas).
  **Abortar nao e falhar — rode de novo.** Baseline verde: **1704 passed, 2 skipped**
</verification>

<success_criteria>
O corte e o piso de similaridade sao numeros de varredura no `calibration.json`; a trava de
digitos esta presa por teste contra os pares medidos; e a fonte da assinatura — a decisao de mao
unica que forma a chave da serie — foi escolhida pelo usuario com o rendimento medido das duas
alternativas na frente dele.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-03-SUMMARY.md` when done
</output>
