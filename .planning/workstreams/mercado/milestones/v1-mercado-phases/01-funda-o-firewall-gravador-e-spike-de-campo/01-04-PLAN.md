---
phase: 01-funda-o-firewall-gravador-e-spike-de-campo
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - l2scanner/calibrar.py
  - l2scanner/calibrar_mercado.py
  - l2scanner/calibracao.py
  - l2scanner/mercado_visao.py
  - l2scanner/visao.py
  - l2scanner/__main__.py
  - calibrar-mercado.bat
  - tests/test_calibrar_mercado.py
  - tests/test_mercado_27x.py
  - tests/fixtures/mercado/
autonomous: false
requirements: [FUND-03, DETC-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O usuario roda a ferramenta de calibracao sobre um frame GRAVADO e ve as regioes da janela, a ancora do painel e os templates de digito persistidos em calibration.json — sem editar JSON a mao"
    - "A calibracao recusa ALTO quando dois templates da watchlist colidem: a matriz de confusao e medida na hora e o pior score inter-classe fica registrado"
    - "No replay da gravacao do incidente 27x o painel de mercado e reconhecido como aberto E zero alertas de morte sao disparados — as duas metades presas pelo mesmo teste"
    - "O sinal 'mercado aberto' entra no caminho da party como extra OPCIONAL e campo exibicional; nenhuma decisao de alerta sai dele nesta fase"
    - "A mecanica de selecao de regiao e COMPARTILHADA com calibrar.py por um auxiliar extraido, nao duplicada — e o tripwire de imwrite dos dois modulos segue verde"
  artifacts:
    - path: "l2scanner/calibrar_mercado.py"
      provides: "ferramenta de calibracao de mercado: regioes, ancora, templates de nome e digito, matriz de confusao, conferencia visual"
      contains: "_selecionar_regiao"
    - path: "l2scanner/calibrar.py"
      provides: "auxiliar _selecionar_regiao extraido de calibrar_selecionando, agora compartilhado"
      contains: "_selecionar_regiao"
    - path: "calibrar-mercado.bat"
      provides: "lancador no precedente de calibrar-solo.bat"
      contains: "l2scanner.calibrar_mercado"
    - path: "tests/test_mercado_27x.py"
      provides: "regressao do incidente 27x afirmando as DUAS metades"
      min_lines: 70
    - path: "tests/test_calibrar_mercado.py"
      provides: "tripwire estrutural do modulo novo + testes da matriz de confusao"
      contains: "imwrite"
    - path: "l2scanner/visao.py"
      provides: "campo exibicional mercado_aberto_aparente em Observacao"
      contains: "mercado_aberto_aparente"
  key_links:
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/calibrar.py"
      via: "import de _gravar_conferencia e do auxiliar _selecionar_regiao — jamais um imwrite proprio, jamais mecanica duplicada"
      pattern: "from .calibrar import"
    - from: "l2scanner/__main__.py"
      to: "l2scanner/mercado_visao.py"
      via: "extra opcional da regiao da ancora, no trilho literal do banner_manutencao"
      pattern: "mercado_ancora"
    - from: "tests/test_mercado_27x.py"
      to: "l2scanner/rastreador.py"
      via: "sequencia de frames reais do 27x alimentando extrair + Rastreador.observar, afirmando zero eventos de morte"
      pattern: "Rastreador"
  prohibitions:
    - "MUST NOT estender o gate de brilho da barra propria (barra_propria_legivel, _moldura_da_barra_propria, _bordas_da_barra_intactas) para detectar o mercado — a licao do consumidor unico esta medida em visao.py:85-111"
    - "MUST NOT deixar rastreador.py ler o campo exibicional de mercado aberto — nenhuma decisao de alerta pode sair dele, e existe tripwire de arquitetura que quebra se passar a ler"
    - "MUST NOT deixar a feature de mercado produzir ou suprimir um evento de morte nesta fase — um sinal, e o consumidor de oclusao so chega na Fase 4, por desenho"
    - "MUST NOT escrever imagem em calibrar_mercado.py, nem acrescentar escrita de imagem em calibrar.py fora de _gravar_conferencia — os tripwires estruturais contam ocorrencias de imwrite no fonte de cada modulo"
    - "MUST NOT pedir que o usuario edite calibration.json a mao, nem gravar um calibration.json parcial — a ferramenta carrega o arquivo existente, muta os campos de mercado e regrava o arquivo INTEIRO"
    - "MUST NOT subir VERSAO_DO_ESQUEMA — a calibracao medida a mao pelo usuario nao pode ser invalidada por campos opcionais"
    - "MUST NOT duplicar a mecanica de selectROI no modulo novo — D-06 diz reusar o codigo de calibracao atual"
    - "MUST NOT commitar gravacoes do usuario alem dos recortes minimos resgatados para tests/fixtures/"
---

<objective>
Construir a ferramenta de calibracao do mercado e fechar a deteccao positiva do painel —
o sinal unico que tambem protege o detector de morte.

Este plano e a segunda metade do antigo `01-03`, desmembrado na costura do checkpoint de
validacao. Ele so comeca depois que o usuario validou as respostas do spike no `01-03`,
porque tres coisas aqui saem diretamente daquelas respostas: a grade de linhas, o conjunto
de glifos, e a forma da ancora (retangulo fixo ou busca em faixa, conforme a resposta de A1).

Purpose: o criterio 4 do ROADMAP — painel reconhecido E zero alertas de morte no replay do
27x — e a prova de que o projeto aprendeu com o incidente das 27 mortes falsas.

Output: `calibrar-mercado.bat` + `l2scanner/calibrar_mercado.py`, as chaves de grade e
templates em `calibration.json`, o campo exibicional em `Observacao`, e o teste de
regressao do 27x.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-RESEARCH.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-PATTERNS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-CONTEXT.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-02-SUMMARY.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-03-SUMMARY.md
</context>

<decision_map>
IDs D-NN das decisoes travadas de `01-CONTEXT.md` > `<decisions>` (mesma tabela dos demais planos):
D-01 roteiro como checklist numerado | D-02 os 8 cenarios | D-03 uma sessao por cenario com rotulo fixo |
D-04 SPIKE-RESPOSTAS.md validado pelo usuario | D-05 variantes de encanto, template com prefixo `+N`, watchlist conjunto fechado puro |
D-06 calibracao em modo proprio (`calibrar-mercado.bat`), estilo `selectROI`, **reusando o codigo de calibracao atual**, fonte = frame GRAVADO, matriz de confusao, nunca editar JSON a mao |
D-07 chaves novas opcionais via `.get`, versao 2 congelada | D-08 firewall com banlist ampla |
D-09 imwrite checado, erro alto | D-10 ancora positiva propria, jamais o gate de brilho, verificada no replay do 27x com ZERO alertas de morte.
</decision_map>

<detc01_reconciliation>
## DETC-01: o que esta fase entrega, e o que fica para a Fase 4 (por desenho)

`REQUIREMENTS.md` descreve DETC-01 como: *"'World Exchange aberto' detectado por
ancora/template positivo, e o sinal e compartilhado com a logica de oclusao do detector
de morte — um sinal, dois consumidores, nunca duplicado."*

**Esta fase entrega o SINAL e a superficie exibicional dele. NAO entrega um consumidor de
DECISAO no detector de morte, e isso e deliberado.**

| Metade de DETC-01 | Onde | Estado ao fim da Fase 1 |
|---|---|---|
| Ancora positiva propria, medida, em modulo puro | `l2scanner/mercado_visao.py` (Plano 02 + este) | ENTREGUE |
| Sinal unico, nunca duplicado (uma so implementacao de deteccao) | `mercado_visao.mercado_aberto` | ENTREGUE |
| Superficie no caminho da party: extra opcional + campo exibicional | `__main__.py` + `Observacao.mercado_aberto_aparente` | ENTREGUE |
| Consumidor 1 — laco `--mercado` como interruptor idle/ativo | Fase 4 (DETC-02) | POR DESENHO, FORA DESTA FASE |
| Consumidor 2 — oclusao CONHECIDA influenciando o detector de morte | Fase 4 | POR DESENHO, FORA DESTA FASE |

**Por que os consumidores nao chegam agora.** Promover este sinal a um segundo consumidor
de decisao dentro do detector de morte e exatamente a manobra que causou o incidente 27x.
A licao esta MEDIDA e escrita em `visao.py:85-111`: o campo `hp_proprio_aparente` existe
separado justamente porque promover um sinal a um segundo uso produziu 2 classes de alerta
falso e atraso de morte. Ligar a oclusao de mercado ao rastreador na mesma fase em que a
ancora nasce significaria confiar num limiar recem-medido para SUPRIMIR alertas de morte —
o tipo de decisao que so se toma depois de a ancora ter rodado em campo.

Por isso o campo e exibicional, ha um tripwire de arquitetura que QUEBRA se `rastreador.py`
passar a le-lo, e a regressao do 27x afirma zero eventos de morte — nao "menos eventos".

**Nota para o `/gsd-verify-work`:** DETC-01 deve ser pontuado como ENTREGUE nesta fase no
escopo acima. A ausencia de consumidor de oclusao nao e entrega parcial; e a decisao
arquitetural registrada aqui, com o consumidor agendado para a Fase 4 junto de DETC-02,
que e onde o laco `--mercado` nasce.
</detc01_reconciliation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calibrar-mercado — auxiliar de selecao compartilhado, templates cortados da gravacao e matriz de confusao medida (D-06, D-07, FUND-03)</name>
  <precondition>`calibration.json` existe na raiz do repo, na versao 2, com `janela` e `party_window` ja calibrados — a ferramenta carrega a calibracao existente e regrava o arquivo inteiro, e recusa se nao houver calibracao anterior</precondition>
  <reversibility rating="costly">As chaves de grade e de templates em `calibration.json` sao formato em disco que as Fases 2, 3 e 4 vao ler. Nao e porta de mao unica porque a propria ferramenta REGRAVA o arquivo inteiro (D-06) — desfazer custa uma recalibragem, nao uma migracao — mas renomear uma chave depois obriga toda instalacao a recalibrar.</reversibility>
  <files>l2scanner/calibrar.py, l2scanner/calibrar_mercado.py, l2scanner/calibracao.py, calibrar-mercado.bat, tests/test_calibrar_mercado.py</files>
  <read_first>
    - `l2scanner/calibrar.py` linhas 355-435 — a assinatura REAL e `def calibrar_selecionando(pixels: np.ndarray, ox: int, oy: int) -> Calibracao | None:` (linha 363): ela faz UMA `selectROI` e devolve uma `Calibracao` inteira. Ler tambem `_gravar_conferencia` (388-434) e o auxiliar `tentar` (407-415)
    - `l2scanner/calibrar.py` linhas 610-670 — o modo solo: carregar -> mutar -> `cal.salvar()` regrava o arquivo INTEIRO, e a recusa explicada quando nao ha calibracao anterior
    - `tests/test_conferencia_gravada.py` linhas 200-239 — `test_existe_um_unico_ponto_de_escrita_no_modulo`, o tripwire que conta `imwrite` no FONTE de `l2scanner.calibrar`. E a razao de `calibrar_mercado.py` ser modulo NOVO, e a razao de a extracao do auxiliar nao poder trazer escrita de imagem junto
    - `l2scanner/identidade.py` linhas 147-215 — `Assinatura`, `criar_assinatura` e `_correlacionar`: a tecnica de conjunto fechado ja MEDIDA (1.000 nos acertos contra 0.454 no melhor erro)
    - `l2scanner/calibracao.py` linhas 190-220, 285-315, 340-380 — o trilho `banner_manutencao` e `conferir_geometria`
    - `calibrar-solo.bat` (inteiro) — o esqueleto a copiar
    - `l2scanner/mercado_visao.py` — `molde_para_hex` / `molde_de_hex` e `CASAMENTO_MINIMO_DA_ANCORA` entregues no Plano 02
    - `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` e `01-03-SUMMARY.md` — quantas linhas por pagina, quais colunas, qual o conjunto de glifos, e **quais respostas o usuario corrigiu no checkpoint**
    - `config.toml` — onde a watchlist de itens vive (a ferramenta corta um template por item listado, D-05)
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_calibrar_mercado.py`:
    - Tripwire estrutural do modulo novo (replica de `test_conferencia_gravada.py:217-227`):
      o total de ocorrencias de `imwrite` no fonte de `l2scanner.calibrar_mercado` e ZERO —
      toda escrita de imagem passa por `_gravar_conferencia` importado de `l2scanner.calibrar`.
    - Regressao do tripwire existente: o teste de `l2scanner.calibrar` continua verde depois
      da extracao do auxiliar (a extracao move selecao de regiao, nunca escrita de imagem).
    - `_selecionar_regiao` extraido: com `cv2.selectROI` monkeypatchado devolvendo uma caixa
      conhecida sobre uma imagem larga (que forca `escala < 1.0`), o auxiliar devolve a caixa
      convertida de volta para as coordenadas ORIGINAIS; caixa de largura ou altura zero
      devolve `None`.
    - Equivalencia de comportamento: `calibrar_selecionando` refatorada para usar o auxiliar
      produz, para a mesma caixa monkeypatchada, o mesmo resultado de antes.
    - Matriz de confusao: com templates sinteticos onde dois sao quase identicos, a funcao
      devolve o par colidente e o pior score inter-classe, e o veredito RECUSA com erro
      nomeado; com templates bem separados, aprova e devolve o limiar sugerido.
    - Load-mutate-save: partindo de `tests/fixtures/calibracao_de_referencia.json` copiado
      para `tmp_path`, mutar so os campos de mercado preserva todos os demais campos.
    - Recusa explicada: sem `calibration.json` anterior, a ferramenta recusa dizendo o que
      rodar antes, em vez de criar uma calibracao pela metade.
    - Recusa por geometria: `mercado_geometria_da_captura` com dimensoes de janela diferentes
      das atuais faz a leitura de mercado recusar com "recalibre o mercado" (Pitfall 5).
  </behavior>
  <action>
Quatro entregas. A ordem importa: o auxiliar compartilhado vem primeiro, porque e ele que
torna o resto reuso em vez de copia.

**(a) Extrair o auxiliar de selecao de `l2scanner/calibrar.py` (D-06: "reusando o codigo de
calibracao atual").** A funcao existente nao e reutilizavel como esta: `calibrar_selecionando`
faz UMA selecao e devolve uma `Calibracao` inteira, enquanto a calibracao de mercado precisa
de tres ou mais selecoes independentes e nao devolve `Calibracao` nenhuma. Extrair, portanto,
a mecanica comum:
`def _selecionar_regiao(pixels: np.ndarray, titulo: str, instrucao: str) -> tuple[int, int, int, int] | None:`
contendo o que hoje esta embutido em `calibrar.py:363-385` — o calculo de `escala` com o
teto de 1600 px de largura, o `cv2.resize` condicional, o `cv2.selectROI` com
`showCrosshair=False`, o `cv2.destroyAllWindows`, a rejeicao de caixa degenerada
(largura ou altura zero) devolvendo `None`, e a conversao de volta pela escala.
Refatorar `calibrar_selecionando` para chamar o auxiliar — **refatoracao de comportamento
preservado**, coberta pelo teste de equivalencia. `l2scanner/calibrar.py` entra no diff SO
por esta extracao: **nenhuma escrita de imagem nova entra nele**, porque o tripwire
`test_existe_um_unico_ponto_de_escrita_no_modulo` conta `imwrite` no fonte do modulo e
quebraria. Essa restricao e tambem a razao de `calibrar_mercado.py` continuar sendo modulo
NOVO em vez de crescer dentro de `calibrar.py` (que ja tem 943 linhas).

**(b) Chaves restantes em `l2scanner/calibracao.py`** — mesmo trilho `.get` do Plano 02
(D-07), `VERSAO_DO_ESQUEMA` FICA EM 2:
- `mercado_grade: dict | None = None` — geometria da lista (origem, altura de linha,
  linhas por pagina, retangulos das colunas relevantes), com os numeros vindos de
  `SPIKE-RESPOSTAS.md` ja validado
- `mercado_templates_de_nome: list[dict] | None = None` — um por item da watchlist,
  nome-como-renderizado INCLUINDO o prefixo `+N` no recorte (D-05)
- `mercado_templates_de_digito: list[dict] | None = None` — um por glifo (0-9 mais os
  separadores confirmados em `SPIKE-RESPOSTAS.md`)
- `mercado_limiar_de_template: float | None = None` — o limiar SUGERIDO pela matriz de
  confusao, com margem sobre o pior score inter-classe medido

**(c) `l2scanner/calibrar_mercado.py` — modulo NOVO.** Importar de `l2scanner.calibrar`
tanto `_gravar_conferencia` quanto `_selecionar_regiao` — jamais um `imwrite` proprio,
jamais mecanica de ROI duplicada. Fluxo de `python -m l2scanner.calibrar_mercado`:
1. `--frame <caminho>` aponta um PNG gravado, ou `--gravacao <pasta>` com escolha de frame
   (padrao: o frame do meio). **Fonte = frame GRAVADO, nunca captura ao vivo** (D-06).
2. Carrega a `Calibracao` existente. Sem ela, recusa com explicacao no tom de
   `calibrar.py:643-652`.
3. Confere a geometria: dimensoes do frame gravado contra a janela da calibracao. Divergiu,
   recusa alto em vez de medir a regiao errada calada.
4. Tres chamadas a `_selecionar_regiao`: (i) o retangulo da ANCORA — pre-preenchido com o
   retangulo ja medido no Plano 02, que o usuario so confirma ou ajusta; (ii) o retangulo
   da grade de linhas; (iii) a primeira linha, de onde a altura de linha e as colunas sao
   derivadas.
5. Corte dos templates a partir da propria gravacao: para cada item da watchlist do
   `config.toml`, o usuario marca o nome-como-renderizado (com o prefixo `+N` quando houver,
   D-05); para os glifos, um exemplar de cada digito e dos separadores. Cada recorte e
   mostrado ampliado para confirmacao visual antes de entrar.
6. **Matriz de confusao medida na hora**: todo template contra todo template com a mesma
   correlacao em posicao unica de `identidade.py::_correlacionar`. Imprimir a matriz,
   registrar o PIOR score inter-classe, sugerir o limiar com margem acima dele. **Recusar
   ALTO** se dois itens da watchlist colidirem — um `+3 Bota X` confundido com `+4 Bota X`
   corromperia a serie inteira. A mensagem nomeia o par colidente e sugere o conserto.
7. Imagem de conferencia com TODOS os retangulos desenhados (`cv2.rectangle`), gravada via
   `_gravar_conferencia` importado. Imprimir o caminho para o usuario abrir.
8. `cal.salvar()` regrava o `calibration.json` INTEIRO. Nunca imprimir valores para o
   usuario colar — o criterio 3 do ROADMAP e "sem editar JSON a mao" (D-06).

**(d) `calibrar-mercado.bat`** — copiar o esqueleto de `calibrar-solo.bat` inteiro:
`cd /d "%~dp0"`, a checagem de `.venv\Scripts\python.exe` com a instrucao de rodar
`vigiar-party.bat` uma vez primeiro, a chamada trocada para
`".venv\Scripts\python.exe" -m l2scanner.calibrar_mercado %*`, e o bloco final instruindo
a conferencia visual do PNG. Adaptar o cabecalho `REM`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrar_mercado.py tests/test_calibracao_mercado.py tests/test_conferencia_gravada.py tests/test_modo_solo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_calibrar_mercado.py -q` passa com todos os testes de `<behavior>`.
    - `inspect.getsource(l2scanner.calibrar_mercado).count("imwrite")` é 0, e o modulo importa `_gravar_conferencia` E `_selecionar_regiao` de `l2scanner.calibrar`.
    - `tests/test_conferencia_gravada.py::test_existe_um_unico_ponto_de_escrita_no_modulo` continua VERDE apos a extracao do auxiliar em `calibrar.py`.
    - `l2scanner/calibrar_mercado.py` nao contem `cv2.selectROI` proprio — a selecao chega pelo auxiliar compartilhado.
    - `l2scanner/calibracao.py` ainda declara `VERSAO_DO_ESQUEMA = 2`, e `tests/fixtures/calibracao_de_referencia.json` continua carregando sem excecao.
    - Salvar apos mutar so os campos de mercado preserva todos os demais campos.
    - Com dois templates quase identicos, a ferramenta RECUSA nomeando o par colidente; com templates separados, aprova e imprime o limiar sugerido e o pior score inter-classe.
    - `calibrar-mercado.bat` existe, checa o `.venv` e invoca `l2scanner.calibrar_mercado`.
    - `python -m pytest tests/ -q` sem regressao.
  </acceptance_criteria>
  <done>A ferramenta existe, compartilha a mecanica de selecao com o calibrador atual em vez de duplica-la, e os dois tripwires de escrita de imagem seguem verdes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: PORTAO DE CALIBRACAO — o usuario roda calibrar-mercado.bat e confere a imagem (criterio 3, D-06)</name>
  <action>Rodar a calibracao de mercado sobre uma gravacao do spike e conferir o resultado</action>
  <instructions>
    Este portao existe porque o criterio 3 do ROADMAP e literalmente sobre voce: *"O usuario
    roda a ferramenta de calibracao sobre um frame gravado e ve as regioes da janela, a
    ancora do painel e os templates de digito persistidos em calibration.json — sem editar
    JSON a mao."* A ferramenta e interativa por desenho (D-06): as regioes sao marcadas com
    o mouse em `cv2.selectROI`. Nao ha como eu marcar por voce, e o teste automatizado de
    load-mutate-save prova que o arquivo e regravado inteiro, mas nao prova que os
    retangulos caem no lugar certo — so o olho faz isso.

    Eu ja fiz tudo que era automatizavel: a ferramenta esta construida e testada, o
    retangulo da ancora ja vem pre-preenchido com o que foi medido no Plano 02, a matriz de
    confusao roda sozinha e recusa colisao entre itens, e a imagem de conferencia e gravada
    por um caminho que checa o retorno do `imwrite` e diz alto se falhar.

    O que fazer:
    1. Rode `calibrar-mercado.bat --gravacao recordings/{carimbo}-mercado-aberto`
       (troque `{carimbo}` pela pasta real da sua sessao `mercado-aberto`).
    2. Confirme ou ajuste o retangulo da ancora, marque a grade de linhas e a primeira linha.
    3. Marque o recorte de nome de cada item da watchlist do `config.toml` — incluindo o
       prefixo `+N` nos itens encantados — e um exemplar de cada digito e separador.
    4. Leia a matriz de confusao impressa no fim: nao pode haver par colidente. Se a
       ferramenta recusar, ela vai nomear o par — recorte mais largo, ou tire um dos itens
       da watchlist.
    5. Abra a imagem de conferencia cujo caminho foi impresso: os retangulos da ancora, da
       grade e das colunas devem cair exatamente onde voce espera.
    6. Confirme que `calibration.json` ganhou as chaves de mercado sem que voce tenha
       editado nada a mao.
  </instructions>
  <verification>Depois do seu "calibrado", eu confiro automaticamente: `calibration.json` carrega sem excecao, `VERSAO_DO_ESQUEMA` continua 2, as chaves `mercado_ancora`, `mercado_grade`, `mercado_templates_de_nome` e `mercado_templates_de_digito` estao presentes e nao vazias, todo molde empacotado decodifica com as dimensoes declaradas batendo com o tamanho real dos bytes, e todos os campos NAO relacionados a mercado continuam identicos aos de antes da calibracao.</verification>
  <resume-signal>Digite "calibrado" quando a imagem de conferencia estiver correta — ou descreva o problema (ex.: "a matriz recusou +3 Bota X contra +4 Bota X", "o retangulo da grade ficou torto").</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DETC-01 — o sinal, sua superficie exibicional, e a regressao do incidente 27x (D-10)</name>
  <precondition>`recordings/inv3/` existe no disco com os 45 recortes de party (`f0NN.png`) e os 10 frames de janela completa (`f0NN_JANELA.png`) do incidente 27x; `recordings/` e gitignored e nao vem de um clone limpo</precondition>
  <reversibility rating="costly">`Observacao` e um dataclass publico lido pelo console e pelos logs; acrescentar um campo opcional e barato de desfazer, mas o campo passa a ser citado pela Fase 4 (modo `--mercado`), entao renomea-lo depois toca mais de um consumidor.</reversibility>
  <files>l2scanner/visao.py, l2scanner/__main__.py, l2scanner/mercado_visao.py, tests/test_mercado_27x.py, tests/fixtures/mercado/</files>
  <read_first>
    - O bloco `<detc01_reconciliation>` no topo deste plano — o que esta fase entrega de DETC-01 e o que fica para a Fase 4, e por que
    - `l2scanner/visao.py` linhas 79-118 — os DOIS campos opcionais existentes de `Observacao` (`hp_proprio_aparente` e `estado_do_cliente`) e o comentario que registra a licao medida: promover um sinal a um segundo uso produziu 2 classes de alerta falso e atraso de morte. E o molde exato do campo novo
    - `l2scanner/__main__.py` linhas 1436-1442 — o trilho literal do extra opcional
    - `l2scanner/captura_janela.py` linhas 371-395 — `_extra_para_janela`, que devolve `None` quando a regiao cai fora da janela (falha fechada ja existente)
    - `tests/test_inventario_por_cima_da_barra_propria.py` linhas 510-545 (`frame_de_party()`: aquecimento do rastreador com pixels REAIS) e 855-939 (`TestOCasamentoNasFixturesVERSIONADAS` e o `pytest.skip` com a razao dita)
    - `l2scanner/frames.py` linhas 190-245 — `ReplaySource` faz glob de `frame_*.png`; `recordings/inv3/` usa `f0NN.png`, entao `--replay recordings/inv3` NAO funciona e a regressao tem de ser um harness pytest
    - `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` e `01-03-SUMMARY.md` — a resposta VALIDADA de A1 (painel fixo ou arrastavel) decide se a ancora e retangulo fixo ou precisa de busca em faixa
    - `.planning/workstreams/mercado/phases/01-.../01-02-SUMMARY.md` — o retangulo da ancora e os scores ja medidos
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_mercado_27x.py`. O teste afirma as DUAS metades juntas,
    porque a metade "zero mortes" ja passa hoje pelo portao de moldura do quick 260826-dxm —
    um teste que so afirmasse isso nao prenderia o DETC-01 (Pitfall 5 da RESEARCH):

    - Metade 1 (o que o DETC-01 ADICIONA): percorrendo os frames de janela do 27x,
      `mercado_aberto(...)` responde `True` em TODOS eles, com os scores parametrizados
      dentro de 0.001 dos valores medidos.
    - Metade 2 (o que nao pode regredir): alimentando `extrair` + `Rastreador.observar` com
      a sequencia de recortes de party do 27x, aquecidos antes com pixels reais no padrao
      `frame_de_party()`, o rastreador emite ZERO eventos de morte na sequencia inteira.
    - Metade 3 (o campo e exibicional): apos a sequencia, a `Observacao` carrega
      `mercado_aberto_aparente is True`, e o conjunto de eventos emitidos e vazio.
    - Tripwire de arquitetura: o fonte de `l2scanner.rastreador` NAO cita
      `mercado_aberto_aparente`. Se um dia citar, este teste quebra — a mesma protecao que
      `hp_proprio_aparente` ja tem.
    - Degenerado: sem a chave de mercado na calibracao, o extra nao entra, o campo fica
      `None`, e nada no caminho da party muda.
  </behavior>
  <action>
**(a) Resgatar as fixtures do caminho da party.** Estender `tests/fixtures/mercado/` com o
que a Metade 2 precisa: os recortes de party `f0NN.png` do `recordings/inv3/` que cobrem a
janela do desastre, e os recortes da barra propria derivados dos 10 frames `_JANELA`
aplicando a regiao `hp_proprio` do `calibration.json` vigente. **Conferir primeiro** que a
geometria da calibracao atual ainda corresponde aos frames do inv3 (janela 1720x1392) e
inspecionar visualmente um recorte derivado antes de confiar nele (A4 da RESEARCH) — um
recorte derivado errado faria o teste medir a regiao errada e passar por motivo errado.
Resgatar o MINIMO necessario.

**(b) Campo exibicional em `l2scanner/visao.py::Observacao`.** Acrescentar
`mercado_aberto_aparente: bool | None = None`, no molde LITERAL de `hp_proprio_aparente`
(`visao.py:79-118`), com o comentario no mesmo tom: e so para mostrar; nenhuma decisao pode
sair dele; `None` significa "ninguem perguntou" e nunca vira evento; a seguranca nao vem de
guardas no rastreador, vem de esta leitura NAO EXISTIR para ele. Acrescentar tambem, no
comentario, o ponteiro para o bloco `<detc01_reconciliation>`: o consumidor de oclusao chega
na Fase 4, por desenho. Preencher o campo quando o extra da ancora estiver presente,
chamando `mercado_visao.mercado_aberto` com o molde e o limiar vindos da calibracao. NAO
tocar em `barra_propria_legivel`, `_moldura_da_barra_propria` nem `_bordas_da_barra_intactas`
(D-10).

**(c) Extra opcional em `l2scanner/__main__.py`.** No bloco de montagem de `extras`
(linhas 1436-1442), acrescentar `extras["mercado_ancora"] = cal.mercado_ancora` SOMENTE
quando `cal.mercado_ancora` existir — trilho literal do `banner_manutencao`. Uma instalacao
sem calibracao de mercado nao paga nem uma captura a mais.

**(d) Ajustar o limiar com os negativos novos.** Rodar `casamento_da_ancora` tambem contra
os frames dos cenarios `mercado-fechado`, `mercado-tooltip` e `mercado-alvo-sobreposto`
gravados pelo usuario, e reconfirmar `CASAMENTO_MINIMO_DA_ANCORA` em
`l2scanner/mercado_visao.py` com a margem REMEDIDA escrita ao lado (pior positivo, melhor
negativo, margem). Se `SPIKE-RESPOSTAS.md` (na versao VALIDADA pelo usuario no `01-03`)
responder que o painel e ARRASTAVEL (A1), acrescentar o fallback de busca em faixa com
cadencia limitada — precedente `SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO = 5.0` em
`captura_janela.py:38-43`, onde a busca por `matchTemplate` na janela inteira custa ~45 ms
e por isso nao roda a cada tick.

**(e) Escrever `tests/test_mercado_27x.py`** com as tres metades e o tripwire de arquitetura
descritos em `<behavior>`. Usar `pytest.skip` com a razao dita para qualquer teste que
dependa de `recordings/` diretamente; os testes que rodam sempre sao os que usam as fixtures
resgatadas em `tests/fixtures/mercado/`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_27x.py tests/test_visao.py tests/test_rastreador.py tests/test_inventario_por_cima_da_barra_propria.py -q</automated>
    <human-check>Rode o harness de replay do 27x e observe a saida: o painel de mercado deve ser reconhecido como "World Exchange aberto" em todos os frames de janela, e a contagem de alertas de morte deve ser exatamente ZERO. Confirme tambem, com uma sessao normal `--janela` sem calibracao de mercado, que o modo party continua se comportando identicamente ao de antes.</human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_27x.py -q` passa com as tres metades e o tripwire.
    - Todo frame de janela do 27x tem `mercado_aberto(...) is True`; a sequencia de party do 27x produz uma lista de eventos de morte de comprimento 0.
    - O fonte de `l2scanner.rastreador` nao contem a expressao do campo exibicional (tripwire de arquitetura verde).
    - `git diff l2scanner/visao.py` nao toca `barra_propria_legivel`, `_moldura_da_barra_propria` nem `_bordas_da_barra_intactas`.
    - Sem `mercado_ancora` na calibracao, `extras` nao ganha a chave e `Observacao.mercado_aberto_aparente` fica `None`.
    - O comentario ao lado de `CASAMENTO_MINIMO_DA_ANCORA` cita a margem REMEDIDA incluindo os negativos das gravacoes novas.
    - `python -m pytest tests/ -q` sem regressao.
  </acceptance_criteria>
  <done>O painel e reconhecido positivamente, o sinal chega ao caminho da party como informacao exibicional, o detector de morte nao mudou, e um unico teste prende as duas metades para que nenhuma regressao futura desfaca uma sem a outra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` (templates e grade) -> processo do scanner | Moldes empacotados em hex sao decodificados e reformatados a partir de dimensoes DECLARADAS no proprio arquivo. |
| Sinal "mercado aberto" -> `Observacao` -> rastreador | O limite mais perigoso do projeto: promover um sinal a um segundo consumidor de DECISAO ja custou 27 alertas falsos. |
| Gravacoes do usuario -> `tests/fixtures/` (versionado) | Nomes de personagens e chat do jogo podem vazar para o repositorio via recorte mal escolhido. |
| `config.toml` (watchlist) -> ferramenta de calibracao | Lista escrita pelo usuario vira o conjunto fechado de templates; colisao entre itens corromperia series inteiras. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `Observacao.mercado_aberto_aparente` -> `rastreador.py` | high | mitigate | Campo exibicional no molde de `hp_proprio_aparente`, sem consumidor de decisao; tripwire de arquitetura quebra se `rastreador.py` passar a cita-lo; regressao do 27x afirma zero eventos de morte; o consumidor de oclusao fica para a Fase 4 por desenho (ver `<detc01_reconciliation>`) |
| T-04-02 | Tampering | `molde_de_hex` sobre `calibration.json` | medium | mitigate | Validar `altura`/`largura` declaradas contra o tamanho real dos bytes antes de reformatar; `CalibracaoInvalida` alta no trilho ja existente (`calibracao.py:316-366`) |
| T-04-03 | Spoofing | template de item da watchlist | high | mitigate | Matriz de confusao medida na calibracao; recusa ALTA quando o pior score inter-classe passa da margem, nomeando o par colidente — um `+3` lido como `+4` corromperia a serie de precos |
| T-04-04 | Information Disclosure | `tests/fixtures/mercado/` versionado | medium | mitigate | Resgatar o MINIMO; nenhuma gravacao nova do usuario e commitada; o SUMMARY lista arquivo por arquivo o que entrou no repo |
| T-04-05 | Repudiation | calibracao gravada sem conferencia | medium | mitigate | Imagem de conferencia obrigatoria via `_gravar_conferencia` (que ja checa o retorno do `imwrite`) mais o portao humano da Task 2, onde o usuario olha a imagem |
| T-04-06 | Denial of Service | leitura de mercado com janela redimensionada | low | mitigate | `mercado_geometria_da_captura` confere as dimensoes no arranque e recusa com "recalibre o mercado" em vez de ler degradado (Pitfall 5) |
| T-04-07 | Tampering | extracao de `_selecionar_regiao` em `calibrar.py` | medium | mitigate | Refatoracao de comportamento preservado com teste de equivalencia; nenhuma escrita de imagem entra em `calibrar.py`, e o tripwire `test_existe_um_unico_ponto_de_escrita_no_modulo` e criterio de aceite explicito |
| T-04-SC | Tampering | instalacoes npm/pip/cargo | high | mitigate | Zero instalacoes nesta fase; `requirements.txt` nao muda. FIRE-01 (Plano 01) e o portao permanente |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plano 04)

**Modulos e lancadores novos**
- `l2scanner/calibrar_mercado.py` — `python -m l2scanner.calibrar_mercado`, flags `--frame <png>` e `--gravacao <pasta>`; corta ancora, grade, templates de nome e de digito; mede a matriz de confusao; grava a conferencia via `_gravar_conferencia` importado
- `calibrar-mercado.bat` — lancador no precedente de `calibrar-solo.bat`

**Simbolo extraido (compartilhado)**
- `l2scanner/calibrar.py::_selecionar_regiao(pixels, titulo, instrucao) -> tuple[int,int,int,int] | None` — mecanica de `selectROI` com reescalonamento, extraida de `calibrar_selecionando` e agora usada pelos dois calibradores

**Chaves novas em `calibration.json` (opcionais, `VERSAO_DO_ESQUEMA` segue 2)**
- `mercado_grade`
- `mercado_templates_de_nome` (um por item da watchlist, com o prefixo `+N` no recorte)
- `mercado_templates_de_digito` (0-9 mais separadores)
- `mercado_limiar_de_template`

**Campos novos**
- `l2scanner/calibracao.py::Calibracao.mercado_grade`, `.mercado_templates_de_nome`, `.mercado_templates_de_digito`, `.mercado_limiar_de_template`
- `l2scanner/visao.py::Observacao.mercado_aberto_aparente: bool | None = None` — exibicional

**Alterado**
- `l2scanner/__main__.py` — chave `mercado_ancora` no dict de `extras`, so quando a calibracao a tem
- `l2scanner/mercado_visao.py` — `CASAMENTO_MINIMO_DA_ANCORA` REMEDIDO com os negativos das gravacoes novas; fallback de busca em faixa se o painel for arrastavel

**Arquivos novos**
- `tests/test_calibrar_mercado.py`
- `tests/test_mercado_27x.py`
- `tests/fixtures/mercado/` — recortes de party e de barra propria do incidente 27x, alem dos ja resgatados no Plano 02
</artifacts_this_phase_produces>

<verification>
1. `python -m pytest tests/ -q` — suite inteira verde no Python GLOBAL (o `.venv` nao tem pytest).
2. Os DOIS tripwires de escrita de imagem verdes: o de `l2scanner.calibrar` (existente, apos a extracao) e o de `l2scanner.calibrar_mercado` (novo).
3. `calibrar-mercado.bat` roda sobre uma gravacao do spike e produz um `calibration.json` completo mais a imagem de conferencia — sem edicao manual de JSON (portao humano da Task 2).
4. `tests/test_mercado_27x.py` afirma as duas metades do criterio 4 no mesmo teste.
5. `l2scanner/rastreador.py` nao aparece no diff da fase.
</verification>

<success_criteria>
- Criterio 3 do ROADMAP completo: a calibracao de mercado persiste em `calibration.json` por ferramenta propria, rodada pelo usuario, sem editar JSON a mao.
- Criterio 4 do ROADMAP completo: no replay do 27x o painel e reconhecido E zero alertas de morte disparam, presos pelo mesmo teste.
- DETC-01 entregue no escopo registrado em `<detc01_reconciliation>`: o sinal e sua superficie exibicional; consumidores na Fase 4, por desenho.
- Zero dependencias novas; `VERSAO_DO_ESQUEMA` segue 2; `rastreador.py` intocado; nenhuma mecanica de `selectROI` duplicada.
</success_criteria>

<output>
Criar `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-04-SUMMARY.md` ao terminar, incluindo: a matriz de confusao medida, o limiar final da ancora com a margem remedida, a lista arquivo por arquivo do que foi resgatado para `tests/fixtures/mercado/`, e **a reconciliacao de DETC-01 do bloco `<detc01_reconciliation>` reproduzida na integra**, para que o `/gsd-verify-work` nao pontue o requisito como parcialmente entregue.
</output>
