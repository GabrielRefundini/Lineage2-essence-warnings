---
phase: 01-funda-o-firewall-gravador-e-spike-de-campo
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md
  - tools/conferir_gravacoes_do_spike.py
  - l2scanner/calibracao.py
  - l2scanner/mercado_visao.py
  - tests/fixtures/mercado/
  - tests/test_mercado_ancora.py
  - tests/test_calibracao_mercado.py
autonomous: false
requirements: [FUND-02, DETC-01]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe um ROTEIRO-SPIKE.md com 8 cenarios numerados, cada um com o rotulo exato de gravacao a usar e o que fazer na tela — o usuario segue com o arquivo aberto no segundo monitor"
    - "O usuario gravou as 8 sessoes reais do World Exchange, e as pastas aparecem em recordings/ com os rotulos fixos do roteiro"
    - "Um script executavel confirma que as 8 gravacoes servem: sufixos presentes, JSONL sem orfaos, PNGs na dimensao da JANELA e nao do recorte da party, e zero falhas de escrita"
    - "A tecnica da ancora do painel esta MEDIDA contra evidencia real: nos frames de janela do incidente 27x o casamento fica acima do limiar, e nos frames sem painel fica abaixo, com a margem escrita ao lado da constante"
    - "calibration.json aceita as chaves de ancora de mercado sem subir a VERSAO_DO_ESQUEMA, e uma calibracao antiga sem essas chaves continua carregando sem uma linha de migracao"
    - "A deteccao do painel e uma ancora POSITIVA propria, num modulo puro — o gate de brilho da barra propria em visao.py nao foi tocado"
  artifacts:
    - path: "l2scanner/mercado_visao.py"
      provides: "modulo puro da ancora do mercado: correlacao em posicao calibrada, limiar medido, empacotamento hex do molde"
      contains: "mercado_aberto"
    - path: "l2scanner/calibracao.py"
      provides: "chaves opcionais mercado_ancora, mercado_molde_da_ancora, mercado_limiar_da_ancora, mercado_geometria_da_captura"
      contains: "mercado_ancora"
    - path: "tests/test_mercado_ancora.py"
      provides: "positivos (frames do 27x) e negativos parametrizados com os scores MEDIDOS"
      min_lines: 60
    - path: "tests/test_calibracao_mercado.py"
      provides: "round-trip das chaves novas e prova de que uma calibracao sem elas carrega"
      min_lines: 40
    - path: ".planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md"
      provides: "checklist de 8 cenarios com rotulos fixos, duracoes e pre-voo"
      contains: "mercado-aberto"
    - path: "tools/conferir_gravacoes_do_spike.py"
      provides: "portao executavel do checkpoint: confere sufixos, JSONL, dimensao dos PNGs e ausencia de falhas de escrita"
      contains: "imread"
    - path: "tests/fixtures/mercado/"
      provides: "recortes resgatados de recordings/inv3 (gitignored) para o teste permanente"
  key_links:
    - from: "tests/test_mercado_ancora.py"
      to: "l2scanner/mercado_visao.py"
      via: "casamento_da_ancora sobre os recortes resgatados"
      pattern: "casamento_da_ancora"
    - from: "l2scanner/calibracao.py"
      to: "calibration.json"
      via: "dados.get('mercado_ancora') no carregar e serializacao condicional no salvar — trilho literal do banner_manutencao"
      pattern: "mercado_ancora"
    - from: "ROTEIRO-SPIKE.md"
      to: "l2scanner/__main__.py"
      via: "os rotulos do roteiro sao os valores de --rotulo que viram nomes de pasta"
      pattern: "--record-janela"
  prohibitions:
    - "MUST NOT estender o gate de brilho da barra propria (barra_propria_legivel, _moldura_da_barra_propria, _bordas_da_barra_intactas) para detectar o mercado — a licao do consumidor unico esta medida em visao.py:85-111 e produziu 2 classes de alerta falso"
    - "MUST NOT subir VERSAO_DO_ESQUEMA — a calibracao medida a mao pelo usuario nao pode ser invalidada por campos opcionais (razao documentada em calibracao.py:196-206)"
    - "MUST NOT commitar gravacoes do usuario (nomes de personagens e chat do jogo) alem dos recortes minimos resgatados para tests/fixtures/"
    - "MUST NOT deixar um teste permanente apontando para recordings/ — a pasta e gitignored e um clone limpo ficaria vermelho; resgatar fixture ou pular com a razao dita"
    - "MUST NOT chutar o limiar da ancora — ele sai MEDIDO dos positivos e negativos reais, com os numeros escritos ao lado da constante"
---

<objective>
Escrever o roteiro que o usuario vai seguir para gravar o World Exchange, de-riscar a
tecnica da ancora do painel contra evidencia que JA ESTA no disco, e entao PARAR: a
fase bloqueia aqui esperando as gravacoes.

O bloqueio e estrutural, nao uma escolha de processo — so o USUARIO pode gravar o
World Exchange (conta, cliente e servidor dele). A ordem e travada no CONTEXT: gravador
consertado (Plano 01) -> roteiro -> **PAUSA: o usuario grava** -> analise (Plano 03) ->
calibracao e deteccao (Plano 04).

Purpose: as sessoes gravadas sao o recurso escasso da fase. Gastar as 8 sessoes com um
roteiro vago, ou descobrir depois que a tecnica da ancora nao tem margem, custaria uma
segunda rodada de gravacoes. O incidente 27x ja deixou 10 frames de janela completa com
o painel "XM Market" aberto em `recordings/inv3/` — isso permite medir a margem da
ancora com numeros reais ANTES de o usuario gravar, e ajustar o roteiro se a margem for
ruim.

Output: `ROTEIRO-SPIKE.md`, `l2scanner/mercado_visao.py` com o limiar medido, as chaves
de ancora em `calibration.json`, fixtures resgatadas, e as 8 gravacoes do usuario.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-RESEARCH.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-PATTERNS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-CONTEXT.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-01-SUMMARY.md
</context>

<decision_map>
IDs D-NN das decisoes travadas de `01-CONTEXT.md` > `<decisions>` (mesma tabela do Plano 01):
D-01 roteiro como checklist markdown numerado | D-02 os 8 cenarios, inventario fora |
D-03 uma sessao por cenario com rotulo fixo | D-04 SPIKE-RESPOSTAS.md por analise de frames |
D-05 variantes de encanto `+N` como prefixo no template | D-06 calibracao em modo proprio sobre frame gravado |
D-07 chaves novas opcionais via `.get`, versao 2 congelada | D-08 firewall com banlist ampla |
D-09 imwrite checado, erro alto | D-10 ancora positiva propria, nunca o gate de brilho.
</decision_map>

<tasks>

<task type="auto">
  <name>Task 1: ROTEIRO-SPIKE.md + o portao executavel que confere as gravacoes (D-01, D-02, D-03, D-05)</name>
  <files>.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md, tools/conferir_gravacoes_do_spike.py</files>
  <read_first>
    - `.planning/workstreams/mercado/phases/01-.../01-CONTEXT.md` secao `<decisions>` — os 8 cenarios e o fato de campo do inventario
    - `.planning/workstreams/mercado/phases/01-.../01-RESEARCH.md` secoes "Descoberta 2" (custo de disco medido, naming das pastas) e "Open Questions" (as 7 perguntas que o roteiro precisa deixar respondiveis)
    - `.planning/workstreams/mercado/phases/01-.../01-01-SUMMARY.md` — a forma EXATA da flag de gravacao de janela completa entregue no Plano 01, para o roteiro citar o comando certo
    - `l2scanner/__main__.py` linhas 1750-1775 — as flags reais (`--janela`, `--rotulo`), para os comandos do roteiro serem copiaveis e corretos
  </read_first>
  <action>
Escrever `ROTEIRO-SPIKE.md` no diretorio da fase, em portugues, como checklist markdown
com caixas `- [ ]` (D-01). O usuario abre este arquivo no segundo monitor e segue.

Estrutura obrigatoria:

1. **Cabecalho com o porque em duas linhas** e o aviso de custo de disco MEDIDO: a
   janela completa sai a cerca de 3,5 MB por PNG; a ~1 Hz isso e cerca de 210 MB por
   minuto. Por isso cada cenario e uma sessao CURTA de 30 a 60 segundos.
2. **Pre-voo (cenario 0, obrigatorio antes de tudo):** uma sessao de 5 segundos com o
   rotulo `pre-voo`, seguida da conferencia visual de um PNG. Criterio escrito: o PNG
   deve ter cerca de 3,5 MB e mostrar a janela INTEIRA do jogo. Se aparecer um PNG de
   ~170 KB com so a faixa da party window, PARE — e o modo errado, e gastar as 8 sessoes
   assim perderia o spike inteiro (Pitfall 2 da RESEARCH).
3. **Os 8 cenarios numerados** (D-02, D-03), cada um com: numero, nome, o **comando
   exato copiavel** com o rotulo fixo, a duracao alvo, e "o que fazer na tela":
   - `mercado-fechado` — jogo normal, mercado nunca aberto. E o negativo da ancora.
   - `mercado-aberto` — painel aberto e parado. Incluir explicitamente a aba **Adena**
     e a aba **Equipment**, alternando entre elas, porque o formato de numero pode
     diferir entre abas (A2 da RESEARCH).
   - `mercado-scroll` — painel aberto, rolando a lista devagar, com pausas.
   - `mercado-pagina-cheia` — uma pagina com o maximo de linhas preenchidas.
   - `mercado-tooltip` — deixar o mouse parado sobre uma linha ate a tooltip aparecer e
     cobrir linhas vizinhas. Gemeo do incidente 27x: um snapshot parcial e confiante.
   - `mercado-alvo-sobreposto` — com uma marcacao de alvo / barra de vida de alvo
     sobrepondo levemente o painel.
   - `mercado-farm-com-party` — mercado aberto durante o farm, com a party window
     visivel. E o caso real de uso e o material do sinal compartilhado.
   - `mercado-scroll-transicao` — capturar de proposito o momento do movimento, com a
     lista no meio da transicao. E o material para o estabilizador de pagina da Fase 2.
4. **Um bloco "arraste o painel" dentro do cenario `mercado-aberto`**: pedir que, se o
   painel puder ser arrastado, o usuario o mova para uma segunda posicao durante a
   sessao. Isso responde a pergunta aberta A1 da RESEARCH (ancora em retangulo fixo vs
   busca em faixa) — a decisao de arquitetura mais cara em aberto.
5. **Uma secao "o que estas gravacoes precisam deixar visivel"** listando as perguntas
   de campo que o Plano 03 vai responder por analise dos frames (D-04): linhas por
   pagina, separador de milhar, moeda, colunas, preco total vs unitario, onde fica o
   preco medio embutido, e a confirmacao de que o encanto renderiza como prefixo
   `+N <nome do item>` (D-05) — pedindo que pelo menos um item encantado esteja na tela
   em `mercado-aberto` ou `mercado-pagina-cheia`, e que um nome longo com prefixo
   apareca para expor truncamento.
6. **Uma nota de escopo:** abrir o inventario FECHA o painel do mercado (fato de campo
   do usuario), entao nao ha cenario de inventario sobre mercado — e nao e um esquecimento.
7. **Uma nota de privacidade:** as gravacoes contem nomes de personagens e chat do jogo;
   `recordings/` e gitignored e permanece local. So recortes minimos vao para
   `tests/fixtures/`.
8. **Checklist de encerramento:** rodar `python tools/conferir_gravacoes_do_spike.py` (o
   portao do item seguinte) e ler o relatorio; lembrar que o resumo final de cada sessao
   imprime frames confirmados, falhas e a contagem lida do disco — os tres numeros devem
   fechar.

**(b) `tools/conferir_gravacoes_do_spike.py`** — o portao executavel do checkpoint. O
checkpoint decide se as 8 sessoes do usuario foram bem gastas, e o sinal de alarme do
Pitfall 2 e um PNG de ~170 KB onde deveriam estar 3,5 MB — isso e verificavel por
programa, entao nao pode ficar so em prosa. Contrato do script (sai com codigo diferente
de zero e nomeia cada problema):
1. Para cada um dos 8 sufixos do roteiro, procura `recordings/*-{sufixo}` e exige pelo
   menos uma pasta. Nomeia os sufixos ausentes.
2. Em cada pasta: `observacoes.jsonl` existe e tem mais de zero linhas.
3. Todo `arquivo` citado no JSONL existe no disco (a mesma assertiva anti-orfao do
   FUND-01, agora aplicada as gravacoes de verdade).
4. **Zero falhas de escrita**, provado sem depender de contador em memoria: o numero de
   linhas do JSONL e igual a `len(list(pasta.glob("frame_*.png")))`. Depois do FUND-01 o
   JSONL so recebe frame confirmado, entao qualquer divergencia denuncia perda.
5. **Dimensao correta**: le o primeiro e o ultimo `frame_*.png` de cada pasta com
   `cv2.imread` e exige que `shape[:2]` corresponda a JANELA (compare com a geometria da
   janela em `calibration.json`), recusando explicitamente o formato do recorte da party
   window. Reprovar com a mensagem que nomeia o modo errado e manda regravar — descobrir
   isso agora custa uma sessao, descobrir depois custa as oito.
6. Imprimir no sucesso uma tabela: sufixo, pasta, frames, dimensao, tamanho medio do PNG.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; p=pathlib.Path('.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md'); t=p.read_text(encoding='utf-8'); faltando=[r for r in ['pre-voo','mercado-fechado','mercado-aberto','mercado-scroll','mercado-pagina-cheia','mercado-tooltip','mercado-alvo-sobreposto','mercado-farm-com-party','mercado-scroll-transicao'] if r not in t]; sys.exit('ROTULOS FALTANDO: '+repr(faltando)) if faltando else print('ok: 9 rotulos presentes')"</automated>
    <human-check>Leia o ROTEIRO-SPIKE.md de cima a baixo como se fosse executa-lo agora, sem contexto: cada cenario tem um comando copiavel, uma duracao e uma instrucao de tela que voce saberia seguir sem perguntar nada?</human-check>
  </verify>
  <acceptance_criteria>
    - `tools/conferir_gravacoes_do_spike.py` existe e, rodado antes das gravacoes, reprova nomeando os 8 sufixos ausentes (prova de que o portao tem dentes).
    - O script reprova uma pasta fabricada em `tmp` cujo PNG tem a dimensao do recorte da party window em vez da janela.
    - O script reprova uma pasta cujo `observacoes.jsonl` cita um `arquivo` que nao existe no disco.
    - O arquivo existe em `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md`.
    - Contem os 8 rotulos de cenario mais `pre-voo`, cada um dentro de um comando copiavel que usa a flag de gravacao de janela completa entregue no Plano 01.
    - Cada cenario tem duracao alvo escrita (30 a 60 segundos, exceto o pre-voo de 5 segundos).
    - O criterio de aprovacao do pre-voo esta escrito com os dois tamanhos de arquivo (o certo e o errado).
    - Ha uma secao com as 7 perguntas de campo e o pedido explicito de um item com prefixo de encanto na tela.
    - Ha a nota de que inventario e mercado sao mutuamente exclusivos e a nota de privacidade.
  </acceptance_criteria>
  <done>Um checklist que o usuario consegue seguir do inicio ao fim no segundo monitor sem fazer uma unica pergunta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trilho da ancora do mercado, MEDIDO sobre a evidencia do 27x que ja esta no disco (D-07, D-10, DETC-01)</name>
  <precondition>`recordings/inv3/f000_JANELA.png` existe no disco (gravacao do incidente 27x; `recordings/` e gitignored e nao vem de um clone limpo)</precondition>
  <reversibility rating="costly">As chaves de mercado em `calibration.json` sao formato em disco que os Planos 03/04 e as Fases 2-4 vao ler. Nao e uma porta de mao unica porque o arquivo inteiro e REGRAVADO pela ferramenta de calibracao (D-06) — desfazer custa uma recalibragem, nao uma migracao — mas renomear uma chave depois obriga a recalibrar toda instalacao. O trilho (`.get` opcional, versao 2 congelada) ja foi escolhido pelo usuario em D-07.</reversibility>
  <files>l2scanner/calibracao.py, l2scanner/mercado_visao.py, tests/fixtures/mercado/, tests/test_mercado_ancora.py, tests/test_calibracao_mercado.py</files>
  <read_first>
    - `l2scanner/visao.py` linhas 1-7 (o charter de pureza a copiar), 79-118 (os dois campos opcionais de `Observacao`) e 180-240 (o estilo de constante com a medicao escrita ao lado)
    - `l2scanner/identidade.py` linhas 147-215 — `Assinatura.como_dict`/`de_dict` (o precedente de empacotamento hex) e `_correlacionar` (a correlacao em POSICAO UNICA, com o comentario que registra por que deslizar e pior: +0.000 aos casamentos certos e ate +0.373 aos ERRADOS)
    - `l2scanner/calibracao.py` linhas 190-220, 285-315 e 340-380 — os TRES pontos do trilho `banner_manutencao` (campo `| None = None` com a doutrina de versao, serializacao condicional no `salvar`, `.get` no `carregar`) e `conferir_geometria`
    - `tests/test_inventario_por_cima_da_barra_propria.py` linhas 510-545 e 855-939 — o aquecimento com pixels reais e o padrao `TestOCasamentoNasFixturesVERSIONADAS` (fixtures resgatadas + valores medidos parametrizados + `pytest.skip` com a razao dita)
    - `.planning/workstreams/mercado/phases/01-.../01-RESEARCH.md` secoes "Descoberta 3" (o que ha em `recordings/inv3/`, verificado visualmente) e "Descoberta 5(b)" (armazenamento de templates)
    - `calibration.json` na raiz — a geometria vigente, para conferir se corresponde aos frames do inv3 (1720x1392) antes de confiar em qualquer recorte derivado (A4 da RESEARCH)
  </read_first>
  <behavior>
    Testes primeiro. `tests/test_calibracao_mercado.py`:
    - Uma `Calibracao` construida SEM nenhuma chave de mercado salva e recarrega sem erro,
      e as quatro chaves voltam como `None`.
    - Um `calibration.json` v2 gravado ANTES desta funcionalidade (usar
      `tests/fixtures/calibracao_de_referencia.json`) carrega sem excecao e sem migracao.
    - Com as quatro chaves preenchidas, `salvar` seguido de `carregar` devolve valores
      identicos (round-trip), inclusive o molde em hex byte a byte.
    - `VERSAO_DO_ESQUEMA` continua valendo 2.

    `tests/test_mercado_ancora.py`:
    - Positivos parametrizados: para cada recorte de ancora resgatado dos frames `_JANELA`
      do 27x, `casamento_da_ancora(recorte, molde)` fica acima do limiar E dentro de
      0.001 do valor MEDIDO gravado no parametro (mesmo formato de
      `test_as_livres_casam_alto`).
    - Negativos parametrizados: recortes do MESMO retangulo em frames de janela SEM o
      painel do mercado ficam abaixo do limiar, tambem com o valor medido.
    - Degenerados: recorte vazio, recorte uniforme (desvio ~0) e molde maior que o alvo
      devolvem 0.0 e portanto `mercado_aberto` devolve `False` — falha FECHADA.
    - Round-trip do empacotamento: `molde_de_hex(molde_para_hex(m))` devolve um array
      identico a `m`.
    - `pytest.skip` com a razao dita para qualquer teste que dependa de `recordings/`
      (populacao que nao existe num clone limpo).
  </behavior>
  <action>
Tres entregas, nesta ordem: resgatar as fixtures, criar o modulo puro, medir o limiar.

**(a) Resgatar fixtures de `recordings/inv3/` para `tests/fixtures/mercado/`.**
Escrever um script descartavel (nao commitado, ou em `tools/`) que:
1. Confere que `recordings/inv3/f000_JANELA.png` tem 1720x1392 e que a
   `geometria_da_tela` do `calibration.json` da raiz ainda corresponde. Se as dimensoes
   nao baterem, PARE e registre — todo recorte derivado mediria a regiao errada (A4).
2. Escolhe o retangulo da ancora. Dois candidatos verificados no frame real: a faixa de
   titulo "XM Market" e a linha de cabecalhos de coluna "Goods | Quantity | Total | Unit
   price". Preferir a faixa de TITULO: ela e visivelmente opaca (arte propria do painel,
   nao translucidez sobre terreno) e sobrevive melhor a tooltip e a marcacao de alvo,
   que caem sobre as LINHAS (A5). Registrar as coordenadas escolhidas no SUMMARY.
3. Salva o molde (`molde_da_ancora.png`, tons de cinza) e um recorte por frame `_JANELA`
   como positivo (`ancora_27x_f000.png` ... `ancora_27x_f040.png`).
4. Salva negativos do MESMO retangulo a partir de frames de janela completa SEM o painel
   do mercado. Fontes candidatas ja no disco: `recordings/base_janela.png`,
   `recordings/agora_janela.png`, `recordings/escuro_janela.png` e os frames de janela de
   `recordings/inventario/` (que trazem OUTRO painel aberto — o negativo mais valioso,
   porque e o que um limiar chutado confundiria). Usar somente os que tiverem a MESMA
   geometria de janela; descartar os demais e dizer no SUMMARY quais foram descartados.
   `tests/fixtures/**/*.png` e excecao no `.gitignore`, entao estes recortes sao
   versionaveis — e sao a unica coisa de `recordings/` que pode ser commitada.

**(b) Criar `l2scanner/mercado_visao.py` — modulo NOVO e PURO (D-10).**
Copiar o charter de pureza do cabecalho de `visao.py:1-7`: sem relogio, sem rede, sem
disco, sem decisao. Conteudo:
- `casamento_da_ancora(recorte, molde) -> float` — replicar `_correlacionar` de
  `identidade.py:187-212` LITERALMENTE, incluindo os guards (`size == 0`, molde maior que
  o alvo, desvio < 1e-6 devolvendo 0.0) e o comentario que registra a medicao do porque
  a posicao e UNICA (`[0, 0]` e o molde na origem do recorte — a posicao que a calibracao
  gravou).
- `mercado_aberto(recorte, molde, limiar) -> bool` — comparacao simples contra o limiar.
- `molde_para_hex(molde) -> dict` e `molde_de_hex(dados) -> np.ndarray` — o padrao
  `Assinatura.como_dict`/`de_dict` (`identidade.py:157-180`) adaptado para tons de cinza:
  bytes CRUS em hex com `altura` e `largura` declaradas, sem `packbits`. A ancora de
  ~40x20 sai a cerca de 1,6 KB de hex, tamanho trivial dentro do `calibration.json`.
- `CASAMENTO_MINIMO_DA_ANCORA` — a constante do limiar, com a medicao escrita ao lado no
  estilo de `visao.py:209-220`: o pior positivo, o melhor negativo, e a margem entre os
  dois. **Nao chutar este numero** — ele sai da medicao do item (c).
- Proibicao herdada, escrita como comentario no modulo: este e um sinal POSITIVO proprio;
  jamais estender `barra_propria_legivel` / `_moldura_da_barra_propria` /
  `_bordas_da_barra_intactas` de `visao.py`, cuja licao do consumidor unico esta medida em
  `visao.py:85-111`.

**(c) Medir o limiar.** Rodar `casamento_da_ancora` de todos os positivos e de todos os
negativos, imprimir a tabela, e escolher o limiar no meio da margem. Gravar os valores
medidos como parametros dos testes e a margem no comentario da constante. Se a margem
ficar apertada (menos de 0.15 entre o pior positivo e o melhor negativo), registrar isso
alto no SUMMARY e acrescentar ao `ROTEIRO-SPIKE.md` um cenario extra de negativo antes do
checkpoint — este de-risking existe justamente para que o roteiro ainda possa mudar.

**(d) Chaves de ancora em `l2scanner/calibracao.py` (D-07).** Replicar os TRES pontos do
trilho `banner_manutencao` para cada uma das quatro chaves:
- `mercado_ancora: Regiao | None = None` — o retangulo da ancora dentro da janela
- `mercado_molde_da_ancora: dict | None = None` — o molde empacotado (dict cru
  `{"altura", "largura", "bytes"}`; `calibracao.py` guarda sem decodificar, para nao
  passar a importar numpy — quem decodifica e `mercado_visao.molde_de_hex`)
- `mercado_limiar_da_ancora: float | None = None`
- `mercado_geometria_da_captura: dict | None = None` — carimbo de contexto de captura
  (dimensoes da janela no momento do corte), para o arranque recusar a leitura de mercado
  quando a janela tiver dimensao diferente, em vez de ler degradado (Pitfall 5)
`VERSAO_DO_ESQUEMA` FICA EM 2. Copiar tambem o comentario-doutrina de `calibracao.py:196-206`
explicando por que estes campos sao opcionais de proposito.

Nao tocar em `l2scanner/visao.py` nem em `l2scanner/rastreador.py` nesta task — a fiacao
do consumidor no caminho da party e do Plano 04.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_ancora.py tests/test_calibracao_mercado.py tests/test_visao.py tests/test_identidade.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_ancora.py tests/test_calibracao_mercado.py -q` passa.
    - `tests/fixtures/mercado/` contem `molde_da_ancora.png`, pelo menos 8 positivos do 27x e pelo menos 2 negativos, todos versionaveis (nao bloqueados pelo `.gitignore`).
    - Todo positivo tem casamento >= `CASAMENTO_MINIMO_DA_ANCORA`; todo negativo tem casamento < `CASAMENTO_MINIMO_DA_ANCORA`; o comentario ao lado da constante cita o pior positivo, o melhor negativo e a margem, com numeros.
    - `l2scanner/calibracao.py` ainda declara `VERSAO_DO_ESQUEMA = 2`, e `tests/fixtures/calibracao_de_referencia.json` (sem as chaves novas) carrega sem excecao.
    - `git diff --stat` NAO lista `l2scanner/visao.py` nem `l2scanner/rastreador.py`.
    - `python -m pytest tests/ -q` sem regressao.
  </acceptance_criteria>
  <done>A ancora do painel tem tecnica implementada, limiar medido contra positivos e negativos reais, fixtures versionadas, e um lugar no `calibration.json` — tudo antes de o usuario gastar uma unica sessao.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: PORTAO EXTERNO — o usuario grava as 8 sessoes do World Exchange (FUND-02)</name>
  <action>Gravar as 8 sessoes reais do World Exchange seguindo o ROTEIRO-SPIKE.md</action>
  <instructions>
    Este e o bloqueio externo da fase e nao ha como automatiza-lo: as gravacoes exigem a
    SUA conta, o SEU cliente e o SEU servidor. Tudo que podia ser feito antes ja foi:

    - O gravador foi consertado (Plano 01): o contador so conta escrita confirmada, uma
      falha aparece como erro alto, e o resumo final imprime frames confirmados, falhas e
      a contagem lida do disco.
    - O modo de gravacao de JANELA COMPLETA existe — sem ele nenhum pixel do painel do
      mercado entraria nos PNGs.
    - O `ROTEIRO-SPIKE.md` esta pronto no diretorio da fase, com os 8 cenarios numerados,
      o comando copiavel de cada um e o rotulo fixo a usar.
    - A tecnica da ancora do painel ja foi medida contra a gravacao do incidente 27x que
      estava no disco, entao a margem e conhecida antes de voce gravar.

    O que fazer:
    1. Abra `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md`
       no segundo monitor.
    2. Faca o **pre-voo** (cenario 0, 5 segundos) e confira o PNG: cerca de 3,5 MB e a
       janela inteira do jogo. Se vier um arquivo de ~170 KB com so a faixa da party
       window, PARE e avise — e o modo errado, e as 8 sessoes seriam desperdicadas.
    3. Grave os 8 cenarios, um por vez, 30 a 60 segundos cada, usando exatamente os
       rotulos do roteiro.
    4. Confira no fim: 8 pastas em `recordings/`, e em cada sessao os tres numeros do
       resumo final fechando (confirmados = disco, falhas = 0).

    Custo estimado: cerca de 210 MB por minuto de gravacao; as 8 sessoes juntas devem
    ficar na casa de 1,5 a 3 GB. Garanta espaco em disco antes de comecar.
  </instructions>
  <verification>Depois do seu "pronto", eu rodo `python tools/conferir_gravacoes_do_spike.py` — ele confere, pasta por pasta: os 8 sufixos do roteiro presentes, `observacoes.jsonl` nao vazio, todo `arquivo` citado no JSONL existindo no disco, linhas do JSONL igual a contagem de `frame_*.png` (prova de zero falhas de escrita), e `cv2.imread(...).shape[:2]` correspondendo a JANELA e nao ao recorte da party window. Se ele reprovar, ele nomeia a pasta e o problema, e eu te digo exatamente o que regravar antes de seguir.</verification>
  <resume-signal>Digite "pronto" quando as 8 sessoes estiverem gravadas — ou descreva o que deu errado (ex.: "o pre-voo saiu com 170 KB", "o painel nao aparece no cenario 3").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jogo (tela) -> `recordings/` -> `tests/fixtures/` | Dados pessoais (nomes de personagens, chat do jogo) atravessam do runtime para o disco e, potencialmente, para o repositorio versionado. |
| `calibration.json` -> processo do scanner | Arquivo de configuracao parseado no arranque; chaves novas sao entrada nao confiavel para o decodificador de molde. |
| Sinal "mercado aberto" -> detector de morte | O limite mais perigoso da fase: um sinal promovido a um segundo consumidor ja produziu 27 alertas falsos neste projeto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `tests/fixtures/mercado/` | medium | mitigate | So recortes MINIMOS da ancora sao resgatados (retangulo do titulo do painel, sem nomes de personagens nem chat); `recordings/` continua gitignored; o SUMMARY lista exatamente o que foi commitado |
| T-02-02 | Tampering | `Calibracao.carregar` com as chaves novas | medium | mitigate | Trilho `.get` com `VERSAO_DO_ESQUEMA` congelada em 2 e `CalibracaoInvalida` alta (`calibracao.py:316-366`); `molde_de_hex` valida `altura`/`largura` declaradas contra o tamanho real dos bytes antes de reformatar |
| T-02-03 | Elevation of Privilege | ancora do mercado -> logica de morte | high | mitigate | `mercado_visao.py` e modulo PURO e SEM consumidor de decisao nesta task; `visao.py` e `rastreador.py` ficam fora do diff, verificado por criterio de aceite |
| T-02-04 | Denial of Service | disco durante as 8 sessoes | medium | mitigate | Roteiro prescreve sessoes de 30-60s com o custo por minuto escrito; o gravador do Plano 01 degrada com erro alto em vez de derrubar o scanner |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | mitigate | Zero instalacoes nesta fase; `requirements.txt` nao muda. FIRE-01 (Plano 01) e o portao permanente |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plano 02)

**Modulos novos**
- `l2scanner/mercado_visao.py`
  - `casamento_da_ancora(recorte, molde) -> float`
  - `mercado_aberto(recorte, molde, limiar) -> bool`
  - `molde_para_hex(molde) -> dict` / `molde_de_hex(dados) -> np.ndarray`
  - `CASAMENTO_MINIMO_DA_ANCORA: float` (valor MEDIDO, com a margem no comentario)

**Chaves novas em `calibration.json` (todas opcionais, `VERSAO_DO_ESQUEMA` segue 2)**
- `mercado_ancora` (Regiao)
- `mercado_molde_da_ancora` (`{"altura", "largura", "bytes"}` em hex)
- `mercado_limiar_da_ancora` (float)
- `mercado_geometria_da_captura` (carimbo de contexto de captura)

**Campos novos em `l2scanner/calibracao.py::Calibracao`**
- `mercado_ancora`, `mercado_molde_da_ancora`, `mercado_limiar_da_ancora`, `mercado_geometria_da_captura` — todos `| None = None`

**Arquivos novos**
- `.planning/workstreams/mercado/phases/01-.../ROTEIRO-SPIKE.md`
- `tools/conferir_gravacoes_do_spike.py` — portao executavel do checkpoint (sufixos, JSONL sem orfaos, JSONL x glob como prova de zero falhas, dimensao de janela vs recorte da party)
- `tests/test_mercado_ancora.py`
- `tests/test_calibracao_mercado.py`
- `tests/fixtures/mercado/molde_da_ancora.png`
- `tests/fixtures/mercado/ancora_27x_f0NN.png` (positivos resgatados do incidente 27x)
- `tests/fixtures/mercado/negativo_*.png` (recortes do mesmo retangulo sem o painel)

**Produzido pelo usuario (checkpoint)**
- `recordings/{carimbo}-mercado-fechado/`, `-mercado-aberto/`, `-mercado-scroll/`,
  `-mercado-pagina-cheia/`, `-mercado-tooltip/`, `-mercado-alvo-sobreposto/`,
  `-mercado-farm-com-party/`, `-mercado-scroll-transicao/` — cada uma com
  `frame_*.png` de janela completa e `observacoes.jsonl` (gitignored)
</artifacts_this_phase_produces>

<verification>
1. `python -m pytest tests/ -q` — suite inteira verde no Python GLOBAL.
2. `ROTEIRO-SPIKE.md` contem os 9 rotulos e os comandos copiaveis (verificacao automatizada da Task 1).
3. A tabela de scores medidos (positivos e negativos) esta no `01-02-SUMMARY.md`, com a margem calculada.
4. `git diff --stat` do plano nao lista `l2scanner/visao.py` nem `l2scanner/rastreador.py`.
5. Apos o checkpoint: `python tools/conferir_gravacoes_do_spike.py` sai com codigo 0.
</verification>

<success_criteria>
- Criterio 2 do ROADMAP a meio caminho: as gravacoes existem e passam no portao executavel (as respostas escritas vem no Plano 03).
- Criterio 4 do ROADMAP de-riscado: a metade "o painel e reconhecido" ja tem tecnica e limiar medidos contra a gravacao do proprio incidente 27x.
- O `calibration.json` tem onde guardar a ancora, sem invalidar nenhuma calibracao existente.
- Nenhuma linha de `visao.py` ou `rastreador.py` mudou — a proibicao mais cara do projeto continua intacta.

**Escopo de DETC-01 nesta fase:** este plano entrega a TECNICA e o LIMITE MEDIDO da ancora;
o `01-04` entrega a superficie exibicional. Nenhum consumidor de oclusao e ligado ao
detector de morte na Fase 1 — isso e deliberado e esta registrado em `01-04-PLAN.md` >
`<detc01_reconciliation>`, com o consumidor agendado para a Fase 4 junto de DETC-02.
</success_criteria>

<output>
Criar `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-02-SUMMARY.md` ao terminar, incluindo a TABELA DE SCORES medidos (positivos e negativos), as coordenadas do retangulo da ancora escolhido, e a lista exata dos arquivos resgatados para `tests/fixtures/mercado/`.
</output>
