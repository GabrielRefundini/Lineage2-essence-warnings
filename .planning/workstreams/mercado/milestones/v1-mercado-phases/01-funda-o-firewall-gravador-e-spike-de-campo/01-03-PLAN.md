---
phase: 01-funda-o-firewall-gravador-e-spike-de-campo
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md
  - tools/conferir_spike_respostas.py
autonomous: false
requirements: [FUND-02]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "As perguntas de campo estao respondidas POR ESCRITO a partir das gravacoes: linhas por pagina, separador de milhar, moeda, colunas, preco total vs unitario, onde fica o preco medio embutido — cada resposta citando um frame que EXISTE no disco"
    - "A decisao sobre variantes de encanto (+3 vs +4) na watchlist esta registrada e validada contra frame real: o encanto renderiza como prefixo +N no nome e o template inclui o prefixo no recorte"
    - "Uma pergunta sem evidencia aparece como NAO RESPONDIDA, nunca preenchida com o plausivel — e o portao automatizado recusa uma resposta selada que cite frame inexistente"
    - "O usuario leu, validou ou corrigiu as respostas antes de qualquer codigo consumi-las — a validacao e um portao que bloqueia, nao uma sugestao"
  artifacts:
    - path: ".planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md"
      provides: "as 9 perguntas de campo respondidas, seladas e com citacao de frame verificavel"
      min_lines: 60
    - path: "tools/conferir_spike_respostas.py"
      provides: "portao que resolve no disco todo frame citado e recusa selo sem evidencia real"
      contains: "exists()"
  key_links:
    - from: "tools/conferir_spike_respostas.py"
      to: "recordings/"
      via: "resolucao de cada caminho citado no documento contra o sistema de arquivos"
      pattern: "exists\\(\\)"
    - from: "SPIKE-RESPOSTAS.md"
      to: "01-04-PLAN.md"
      via: "as respostas de grade, glifos e A1 alimentam a calibracao e a ancora do plano seguinte"
      pattern: "mercado_grade"
  prohibitions:
    - "MUST NOT aceitar como evidencia um caminho de frame que nao existe no disco — citar caminho com FORMATO valido nao e citar evidencia, e e exatamente o modo de falha que o FUND-01 consertou no gravador"
    - "MUST NOT preencher uma resposta com o plausivel quando o frame nao mostra — a resposta fica NAO RESPONDIDA, dizendo o que faltou gravar"
    - "MUST NOT deixar codigo consumir as respostas antes da validacao do usuario — D-04 exige que o usuario valide ou corrija"
    - "MUST NOT commitar gravacoes do usuario (nomes de personagens e chat do jogo) — so os caminhos sao citados; os PNGs continuam gitignored"
---

<objective>
Transformar as 8 gravacoes do usuario em respostas de campo escritas, verificaveis e
VALIDADAS — o insumo sem o qual as Fases 2 e 3 nao podem ser planejadas em detalhe.

Purpose: este documento vira a fonte de verdade de tres fases. Uma resposta errada aqui
e paga la na frente, quando ja houver codigo de leitura de preco construido em cima
dela. Por isso o plano faz duas coisas que um documento normal nao faz: um portao
automatizado que RESOLVE no disco cada frame citado (citar um caminho com formato certo
nao e citar evidencia), e um portao humano que bloqueia ate o usuario validar — D-04 e
uma decisao travada, nao uma sugestao.

Este plano nasceu do desmembramento do antigo 01-03 na costura do checkpoint: a
calibracao e o DETC-01 foram para o `01-04`, que so pode comecar depois que as respostas
daqui estiverem validadas.

Output: `SPIKE-RESPOSTAS.md` validado pelo usuario e `tools/conferir_spike_respostas.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-RESEARCH.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-CONTEXT.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-02-SUMMARY.md
</context>

<decision_map>
IDs D-NN das decisoes travadas de `01-CONTEXT.md` > `<decisions>` (mesma tabela dos demais planos):
D-01 roteiro como checklist numerado | D-02 os 8 cenarios | D-03 uma sessao por cenario com rotulo fixo |
D-04 SPIKE-RESPOSTAS.md: Claude analisa e propoe citando frames, **o usuario valida ou corrige** |
D-05 variantes de encanto no v1, template = nome-como-renderizado com o prefixo `+N`, watchlist conjunto fechado puro |
D-06 calibracao em modo proprio sobre frame gravado | D-07 chaves opcionais via `.get`, versao 2 congelada |
D-08 firewall com banlist ampla | D-09 imwrite checado, erro alto | D-10 ancora positiva propria, jamais o gate de brilho.
</decision_map>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE-RESPOSTAS.md + o portao que resolve a evidencia no disco (D-04, D-05, FUND-02)</name>
  <precondition>As 8 pastas de gravacao do ROTEIRO existem em `recordings/` com os sufixos `-mercado-fechado`, `-mercado-aberto`, `-mercado-scroll`, `-mercado-pagina-cheia`, `-mercado-tooltip`, `-mercado-alvo-sobreposto`, `-mercado-farm-com-party`, `-mercado-scroll-transicao`, cada uma com `observacoes.jsonl` nao vazio</precondition>
  <files>.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md, tools/conferir_spike_respostas.py</files>
  <read_first>
    - `.planning/workstreams/mercado/phases/01-.../ROTEIRO-SPIKE.md` — as perguntas que os cenarios foram gravados para responder
    - `.planning/workstreams/mercado/phases/01-.../01-RESEARCH.md` secoes "Descoberta 3" (o que ja foi verificado visualmente em `recordings/inv3/f000_JANELA.png`: colunas "Goods | Quantity | Total | Unit price", virgula decimal com 2 casas, moeda "XM Coin" na aba Equipment, ~10 slots de linha) e "Open Questions" (as 7 perguntas e as assuncoes A1/A2/A5 a fechar)
    - `.planning/workstreams/mercado/phases/01-.../01-CONTEXT.md` secao `<decisions>` — a decisao de encanto (D-05) que o spike so VALIDA, nao redecide
    - `tools/` — convencoes dos scripts auxiliares ja existentes no repo
    - Os frames das gravacoes: abrir visualmente pelo menos um frame representativo de CADA uma das 8 pastas
  </read_first>
  <action>
Duas entregas: o documento e o portao que o mantem honesto.

**(a) `tools/conferir_spike_respostas.py`** — escrever ANTES do documento, para que o
documento seja escrito ja sabendo que sera conferido. Contrato do script (sai com codigo
diferente de zero e mensagem nomeando cada problema):
1. Le `SPIKE-RESPOSTAS.md`.
2. Identifica as **secoes de resposta** por cabecalho numerado: linhas que casam
   `^#{2,3}\s+\d+\.` . Exige exatamente 9 secoes numeradas. Qualquer outro cabecalho
   (legenda, introducao, "Impacto no planejamento") fica FORA da contagem — a legenda
   contem as tres palavras de selo e nao pode satisfazer o portao sozinha.
3. Em cada secao numerada, exige EXATAMENTE UM selo entre `VERIFICADO`, `PARCIAL` e
   `NAO RESPONDIDO`.
4. Para cada secao selada `VERIFICADO` ou `PARCIAL`: extrai os caminhos citados com
   `recordings/[^\s)`]+\.png` e exige **pelo menos um** — e que **todos** existam no
   disco via `pathlib.Path(p).exists()`.
5. Globalmente: todo caminho citado no documento inteiro tem de existir no disco.
   Reprovar listando um a um os que nao existem.
6. Imprimir no sucesso um resumo por secao: numero, selo, quantidade de frames citados
   e confirmados.
Razao de existir, para escrever na docstring: citar um caminho com FORMATO valido nao e
citar evidencia. Um portao que so casa o formato aceitaria
`recordings/20260827-1200-mercado-aberto/frame_000012.png` sustentando uma resposta
inventada — a mesma classe de mentira que o `cv2.imwrite` sem retorno checado produzia no
gravador, e que o FUND-01 existe para consertar. Nesta fase, cujo proposito declarado e
"evidencia nao-confirmada e o pesadelo documentado deste projeto", o portao do spike nao
pode aceitar evidencia nao confirmada.

**(b) `SPIKE-RESPOSTAS.md`** — analisar os frames gravados e escrever o documento (D-04:
Claude analisa e PROPOE, citando frames especificos; o usuario valida ou corrige na Task 2).

Regra de honestidade: cada resposta carrega o caminho do frame que a sustenta e um dos
tres selos — **VERIFICADO** (visto no frame), **PARCIAL** (visto, mas so numa aba ou
condicao) ou **NAO RESPONDIDO** (o frame nao mostra; diz o que faltou gravar). Uma
pergunta sem evidencia fica NAO RESPONDIDA, nunca preenchida com o plausivel.

Uma secao numerada por pergunta, exatamente 9:
1. **Linhas por pagina e aparencia do slot vazio** — por aba. O frame do 27x mostra ~10
   slots com 3 preenchidos; confirmar com `mercado-pagina-cheia`.
2. **Separador de milhar, decimais e moeda** — na aba **Adena** E na aba Equipment. O
   frame ja verificado mostra "40,00 XM Coin" (virgula decimal, 2 casas) na aba
   Equipment; a aba Adena pode ter formato proprio (A2). Registrar o glifo do separador
   de milhar como cidadao de primeira classe — e o que a Fase 2 vira template.
3. **Colunas da grade e sua ordem** — confirmar "Goods | Quantity | Total | Unit price | Buy"
   e se ordenar por "Total" muda a ordem das linhas.
4. **Preco total vs unitario** — as duas colunas coexistem? Se sim, registrar que a Fase 3
   pode gravar as duas em colunas separadas (unitario derivado, nunca confundido).
5. **Preco medio embutido: onde fica?** — nao estava visivel no frame ja verificado.
6. **Idade do listing e exibida?**
7. **Renderizacao do encanto (D-05)** — validar com frame que o encanto aparece como
   prefixo `+N <nome do item>`, e capturar um nome LONGO com prefixo para expor
   truncamento. Registrar a decisao ja travada: cada variante e um template distinto
   (`+3 Bota X` e `+4 Bota X`), watchlist e conjunto fechado puro, cada variante e serie
   independente de ponta a ponta.
8. **Posicao do painel: fixa ou arrastavel? (A1)** — a pergunta de arquitetura mais cara
   em aberto. Se o painel se mover, a ancora em retangulo fixo NAO basta e o `01-04`
   precisa do fallback de busca em faixa com cadencia limitada (precedente
   `SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO = 5.0`). Responder isto ANTES de calibrar.
9. **Opacidade do painel (A5)** e **o que exatamente tooltip e marcacao de alvo cobrem** —
   alimentam os negativos do limiar da ancora e o estabilizador de pagina da Fase 2.

Fechar com uma secao NAO numerada **"Impacto no planejamento das Fases 2, 3 e 4"**, uma
linha por consequencia (ex.: "o separador de milhar e ponto, entao a Fase 2 precisa de um
12o glifo").
  </action>
  <verify>
    <automated>python tools/conferir_spike_respostas.py</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/conferir_spike_respostas.py` sai com codigo 0 e imprime as 9 secoes com selo e contagem de frames confirmados.
    - O script reprova (codigo diferente de zero) quando um caminho citado nao existe no disco — provar apontando-o para um documento de teste com um caminho inventado.
    - O script reprova quando uma secao selada VERIFICADO ou PARCIAL nao cita nenhum frame.
    - A linha de legenda do documento, que contem as tres palavras de selo, NAO conta como secao de resposta.
    - `SPIKE-RESPOSTAS.md` tem exatamente 9 secoes numeradas mais a secao de impacto.
    - A pergunta A1 (painel fixo ou arrastavel) tem resposta explicita, porque o `01-04` depende dela.
  </acceptance_criteria>
  <done>As respostas existem, cada selo positivo aponta para um frame que realmente esta no disco, e o portao que garante isso e executavel.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: PORTAO DE VALIDACAO — o usuario valida ou corrige as respostas do spike (D-04)</name>
  <action>Validar ou corrigir SPIKE-RESPOSTAS.md antes que qualquer codigo consuma as respostas</action>
  <instructions>
    D-04 e uma decisao travada: eu analiso os frames e PROPONHO as respostas; quem valida
    e voce. Este portao existe porque as tres coisas que o `01-04` constroi saem
    diretamente daqui, e nenhuma delas e barata de refazer:

    - a **grade** (`mercado_grade`) e os **glifos** (`mercado_templates_de_digito`) sao
      escritos no `calibration.json` a partir das respostas 1, 2 e 3;
    - a resposta 8 (painel fixo ou arrastavel) decide se a ancora e um retangulo fixo ou
      precisa de busca em faixa — uma diferenca de arquitetura, nao de ajuste;
    - a resposta 7 fixa como os templates de encanto sao recortados, e um `+3` confundido
      com um `+4` corromperia a serie de precos inteira.

    Eu ja fiz tudo que era automatizavel: as respostas estao escritas, cada selo positivo
    cita um frame, e `tools/conferir_spike_respostas.py` ja confirmou que todos esses
    frames existem no disco. O que a maquina NAO consegue e comparar as respostas com o
    que voce sabe do jogo.

    O que fazer:
    1. Abra `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md`.
    2. Leia secao por secao. Para cada uma, confirme contra o que voce ve no jogo — ou
       abra o frame citado, que esta no disco.
    3. Corrija direto no arquivo o que estiver errado. Rebaixe para NAO RESPONDIDA
       qualquer resposta que eu tenha selado com confianca demais.
    4. Preste atencao especial nas secoes 2 (separador de milhar da aba Adena), 7
       (renderizacao do encanto) e 8 (painel fixo ou arrastavel) — sao as tres que mais
       custam se estiverem erradas.
  </instructions>
  <verification>Depois do seu "validado", eu rodo `python tools/conferir_spike_respostas.py` de novo (suas correcoes nao podem ter quebrado a rastreabilidade da evidencia) e registro no `01-03-SUMMARY.md` exatamente quais respostas voce corrigiu e quais confirmou — a lista de correcoes e o que o `01-04` le antes de calibrar.</verification>
  <resume-signal>Digite "validado" quando tiver revisado as 9 secoes — ou descreva o que corrigiu (ex.: "a secao 8 esta errada, o painel ARRASTA").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frames gravados -> SPIKE-RESPOSTAS.md -> Fases 2/3/4 | Um documento de analise vira fonte de verdade de tres fases. O risco nao e um atacante: e uma afirmacao confiante sem lastro. |
| SPIKE-RESPOSTAS.md -> `tools/conferir_spike_respostas.py` | O portao le caminhos vindos de um documento escrito por um agente e os resolve no sistema de arquivos. |
| Gravacoes do usuario -> documento versionado | O documento CITA caminhos de gravacoes que contem nomes de personagens e chat do jogo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Repudiation | selo de evidencia em SPIKE-RESPOSTAS.md | high | mitigate | O portao RESOLVE cada caminho citado no disco e exige pelo menos um frame existente por selo positivo; casar formato de caminho nao basta. Selos contados so dentro das 9 secoes numeradas, entao a legenda nao satisfaz o portao |
| T-03-02 | Tampering | respostas consumidas sem validacao humana | high | mitigate | `checkpoint:human-action` com `gate="blocking-human"` entre este plano e o `01-04`; o gate nunca e auto-aprovado, nem com `auto_advance` |
| T-03-03 | Information Disclosure | caminhos citados no documento versionado | low | accept | So os CAMINHOS entram no documento; os PNGs continuam em `recordings/`, gitignored. Nenhum recorte de tela e commitado por este plano |
| T-03-04 | Denial of Service | `conferir_spike_respostas.py` sobre caminho hostil | low | mitigate | O script so chama `Path(p).exists()` — leitura de metadado, sem abrir nem executar nada; caminhos vem de um documento do proprio repo |
| T-03-SC | Tampering | instalacoes npm/pip/cargo | high | mitigate | Zero instalacoes nesta fase; `requirements.txt` nao muda. FIRE-01 (Plano 01) e o portao permanente |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plano 03)

**Arquivos novos**
- `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` — 9 secoes numeradas, cada uma com selo (`VERIFICADO` / `PARCIAL` / `NAO RESPONDIDO`) e citacao de frame, mais a secao "Impacto no planejamento das Fases 2, 3 e 4"
- `tools/conferir_spike_respostas.py` — portao executavel: resolve no disco todo caminho citado, exige um selo por secao numerada e pelo menos um frame existente por selo positivo

**Portao humano**
- `checkpoint:human-action` `gate="blocking-human"` — validacao das respostas por D-04; nada do `01-04` roda antes dele

**Nao produz codigo de runtime.** Nenhum arquivo em `l2scanner/` e tocado por este plano.
</artifacts_this_phase_produces>

<verification>
1. `python tools/conferir_spike_respostas.py` sai com codigo 0.
2. O portao reprova um documento com caminho inventado (prova por mutacao, no criterio de aceite da Task 1).
3. O usuario respondeu "validado" no checkpoint, e o `01-03-SUMMARY.md` lista o que foi corrigido e o que foi confirmado.
4. `python -m pytest tests/ -q` sem regressao (este plano nao toca codigo de runtime; roda a suite so para confirmar).
5. Nenhum PNG de `recordings/` foi commitado.
</verification>

<success_criteria>
- Criterio 2 do ROADMAP completo: as perguntas de campo estao respondidas por escrito a partir das gravacoes, com a decisao de encanto registrada — e VALIDADAS pelo usuario.
- Nenhuma resposta selada positiva aponta para um frame inexistente.
- O `01-04` pode comecar sabendo que a grade, os glifos e a resposta de A1 foram conferidos por quem conhece o jogo.
</success_criteria>

<output>
Criar `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-03-SUMMARY.md` ao terminar, incluindo: a tabela final de selos por secao, a **lista das respostas que o usuario corrigiu no checkpoint** (o `01-04` le esta lista antes de calibrar), e a resposta de A1 destacada, porque ela decide a forma da ancora.
</output>
