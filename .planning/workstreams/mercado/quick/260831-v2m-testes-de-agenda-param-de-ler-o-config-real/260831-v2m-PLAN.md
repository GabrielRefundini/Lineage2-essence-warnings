---
phase: quick-260831-v2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_agenda.py
  - tests/test_silenciamento.py
autonomous: true
requirements: [AGENDA-SILENCIO-07, CR-TESTE-ESQUEMA]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low   # .planning/estimation-calibration.json tem samples: [] — fator 1.0, sem historico

must_haves:
  truths:
    - "Trocar `silenciar_minutos` do `[[evento]]` TvT no `config.toml` para QUALQUER inteiro nao negativo deixa a suite inteira verde. Provado rodando a suite COMPLETA com dois valores diferentes (9 e 15), as duas verdes."
    - "As 7 falhas medidas hoje (5 em `tests/test_agenda.py`, 2 em `tests/test_silenciamento.py`) ficam verdes SEM que nenhum teste seja apagado: a contagem de `passed` da suite completa vai de 3797 para no minimo 3804."
    - "Os testes de REGRA (uniao de janelas sobrepostas, cancelamento por ocorrencia, a borda 22:00-22:05) continuam DISCRIMINANDO: baixar a janela do TvT nos dados PROPRIOS da fixtura de 15 para 9 deixa `TestJanelaDeSilencio` VERMELHO."
    - "`TestAgendaRealDoUsuario` continua lendo o `config.toml` do repositorio e continua cravando os `horarios` do TvT — esse acoplamento e DELIBERADO e esta escrito no cabecalho do proprio `config.toml`. So a assercao sobre `silenciar_minutos` vira ESQUEMA."
    - "O `config.toml` do usuario termina com `silenciar_minutos = 9` no bloco TvT, exatamente como ele o deixou, e nenhuma linha a mais ou a menos."
    - "Nenhum arquivo de `l2scanner/` e tocado, e nenhuma dependencia nova entra (FIRE-01)."
  artifacts:
    - "tests/test_agenda.py com `SILENCIO_DO_TVT`, `SILENCIO_DO_PRIME` e a fabrica `agenda_de_silencio()`"
    - "tests/test_silenciamento.py com as tres fixturas `agenda` montando dados proprios"
  key_links:
    - "A FABRICA E O SEAM UNICO: `agenda_de_silencio()` em `tests/test_agenda.py` alimenta as QUATRO fixturas acopladas (uma no proprio arquivo, tres em `tests/test_silenciamento.py`). `tests/test_silenciamento.py` ja importa `SEGUNDA` de `tests.test_agenda` DENTRO das funcoes — a fabrica viaja pelo mesmo caminho ja provado, e nenhum encanamento novo nasce."
    - "`SILENCIO_DO_TVT = 15` e `SILENCIO_DO_PRIME = 120` sao CONSTANTES NOMEADAS de modulo, e nao numeros soltos dentro da fabrica. E o que torna o mutante do criterio 3 uma linha de `sed` estavel em vez de uma regex adivinhando a forma do codigo do executor."
    - "Os 15 e 120 da fabrica NAO sao copia do `config.toml`: sao os dois numeros que FAZEM a borda 22:05 existir (TvT 21:50+15 = 22:05 depois do Prime 20:00+120 = 22:00). Sao dados do TESTE, escolhidos pela regra que o teste afirma."
---

<objective>
Os testes de agenda e de silencio afirmam hoje o VALOR que o usuario escolheu
para `silenciar_minutos`, em vez de afirmarem a REGRA que o codigo implementa.
Quando ele trocou o TvT de 15 para 9 minutos hoje, 7 testes ficaram vermelhos
sem uma linha de codigo de producao ter mudado.

E o MESMO defeito que `8b87eb3` consertou hoje de manha, quando o servidor
mudou o respawn do Tiat de 6h para 8h e dois testes de `tests/test_bosses.py`
afirmavam o valor do servidor em vez do esquema. Aquele commit e o PADRAO deste
plano, e a frase dele vale literalmente aqui:

> "ele provava a regra do servidor em vez de provar que ela e LIDA do arquivo"

Purpose: o `config.toml` e DADO do usuario. Um teste que trava o dado do usuario
transforma cada ajuste de preferencia dele num deploy — o oposto exato do
motivo de o valor morar num arquivo de configuracao.

Output: `tests/test_agenda.py` e `tests/test_silenciamento.py`, com a agenda das
regras montada de dados PROPRIOS e o arquivo do repositorio afirmado por ESQUEMA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@./.claude/CLAUDE.md

O PRECEDENTE, de leitura obrigatoria antes da Task 1: `git show 8b87eb3`
</context>

<medicoes>
Tudo abaixo foi MEDIDO nesta arvore durante o planejamento, nao estimado.

**A causa, literal.** Quatro fixturas montam a agenda lendo o arquivo do
usuario:

| Arquivo | Linha | Classe |
|---|---|---|
| `tests/test_silenciamento.py` | 119 | `TestControleDoSilencio` |
| `tests/test_silenciamento.py` | 470 | `TestCancelarOSilencio` |
| `tests/test_silenciamento.py` | 536 | `TestOQueDaParaCancelar` |
| `tests/test_agenda.py` | 1737 | `TestJanelaDeSilencio` |

Essas quatro fixturas alimentam **24 testes**. Sete estao vermelhos hoje; os
outros 17 estao verdes por sorte — o valor do usuario ainda nao os alcancou.

**As 7 falhas, na suite completa, com o `9` do usuario:**

```
7 failed, 3797 passed, 2 skipped in 170.08s
  test_agenda.py::TestAgendaRealDoUsuario::test_as_duracoes_de_silencio_estao_no_esquema_para_a_fase_7
  test_agenda.py::TestJanelaDeSilencio::test_dentro_do_tvt_ha_silencio
  test_agenda.py::TestJanelaDeSilencio::test_a_uniao_termina_as_2205_e_nao_as_2200
  test_agenda.py::TestJanelaDeSilencio::test_entre_2200_e_2205_ainda_ha_silencio
  test_agenda.py::TestJanelaDeSilencio::test_uma_segunda_inteira_minuto_a_minuto
  test_silenciamento.py::TestControleDoSilencio::test_a_mensagem_nomeia_quem_terminou_por_ultimo
  test_silenciamento.py::TestCancelarOSilencio::test_cancelar_o_prime_NAO_cala_o_tvt
```

**A prova de que e acoplamento puro, e nada mais:** com `silenciar_minutos = 15`
os mesmos tres arquivos dao **392 passed, 0 failed**. Com `9`, 7 vermelhos. O
`config.toml` foi restaurado byte a byte (sha256 conferido) depois da medicao.

**UM NUMERO DO BRIEFING QUE CAIU — `tests/test_comandos.py` NAO tem o defeito.**
O briefing pedia para incluir o arquivo como "defeito latente". Ele nao esta
latente: esta ausente, e isso foi medido, nao suposto.

- `tests/test_comandos.py` ja constroi `EventoAgendado(...)` INLINE com
  `silenciar_minutos=120` proprio, nas linhas 1306, 1409 e 1676.
- Suas duas unicas leituras do arquivo do repositorio (linhas 2116 e 2329) sao
  sobre `ler_membros` — a guarda de que o `config.toml` versionado nao carrega
  telefone de ninguem. Nao tocam a agenda.
- Rodado com um TERCEIRO valor (`silenciar_minutos = 37`): **212 passed**.

Ou seja: o arquivo que NAO tem o bug e justamente o que ja monta os proprios
objetos. Ele nao entra em `files_modified` porque nao ha o que consertar nele —
e o padrao que este plano copia para os outros dois.

**Escopo confirmado repo-wide.** Outros tres pontos leem a agenda do arquivo
real (`test_agenda.py:862`, `test_desativar_solo_boss.py:145`,
`test_desativar_lista_de_presenca.py:257`), e os tres ficam FORA: eles afirmam
contagem de avisos, dependem de `horarios` e `chamar_minutos_antes`, e nenhum
deles reage a `silenciar_minutos` — o baseline de 7 falhas na suite COMPLETA e
a prova disso.

**FLAKE CONHECIDO, pre-existente e documentado no STATE.md:**
`tests/test_agenda.py` vaza um `KeyboardInterrupt` que aborta a sessao do pytest
perto de ~88 testes (medido: 5 abortos em 60 rodadas). **Abortar nao e falhar.**
Ele apareceu uma vez durante este planejamento. Os criterios abaixo ja tratam o
aborto explicitamente — nao o confunda com vermelho, e nao "conserte" o flake
neste plano.
</medicoes>

<restricoes>
1. **So os arquivos de teste.** NAO tocar `l2scanner/agenda.py`,
   `l2scanner/sessao.py`, `l2scanner/respawn.py`, `l2scanner/bosses.py` nem
   `tests/test_bosses.py`. Outro agente trabalha nessa vizinhanca NESTA MESMA
   ARVORE.
2. **NAO alterar o `config.toml` do usuario.** O `9` e escolha dele e fica. As
   mutacoes dos criterios sao temporarias e restauradas na mesma execucao.
3. **Nenhuma dependencia nova** (FIRE-01). Tudo que a fabrica precisa
   (`EventoAgendado`, `TODOS_OS_DIAS`) ja esta importado em `tests/test_agenda.py`.
4. **NUNCA `git commit --amend`** — hoje um amend caiu sobre commit alheio nesta
   arvore.
5. O commit leva **apenas** `tests/test_agenda.py` e `tests/test_silenciamento.py`.
</restricoes>

<tasks>

<task type="tracer">
  <name>Task 1: a fabrica de agenda propria, e a primeira classe migrada ponta a ponta</name>
  <files>tests/test_agenda.py</files>
  <action>
Este e o fatiamento fino que prova o desenho inteiro antes de expandir: uma
fabrica nova + UMA classe migrada, verde ponta a ponta.

Em `tests/test_agenda.py`, ao lado dos ajudantes que ja existem (`SEGUNDA`,
`evento()`, `em()`, por volta da linha 29-42), acrescentar constantes de modulo
NOMEADAS e uma fabrica:

- `SILENCIO_DO_TVT = 15`
- `SILENCIO_DO_PRIME = 120`
- `agenda_de_silencio()` — devolve uma LISTA NOVA a cada chamada (nunca uma
  constante de modulo compartilhada) com os tres eventos que as regras de
  silencio precisam, montados por `EventoAgendado` direto:
  - TvT: horarios `((15,0),(17,0),(19,30),(21,50),(23,0))`, `dias=TODOS_OS_DIAS`,
    `avisar_minutos_antes=10`, `silenciar_minutos=SILENCIO_DO_TVT`
  - Prime: horarios `((20,0),)`, `dias=frozenset({0,1,2,3})`,
    `avisar_minutos_antes=10`, `silenciar_minutos=SILENCIO_DO_PRIME`
  - Solo Boss: os 12 horarios pares, `avisar_no_horario=False`,
    `chamar_minutos_antes=110`, `silenciar_minutos=0`

Escrever na docstring da fabrica POR QUE ela nao le o arquivo do repositorio,
citando `8b87eb3` e a quebra medida hoje (o usuario trocou o TvT de 15 para 9 e
7 testes ficaram vermelhos sem uma linha de producao mudar). Registrar tambem
que os 15 e 120 sao os numeros que FAZEM a borda 22:05 existir — sao dados do
teste, escolhidos pela regra afirmada, e nao copia da preferencia do usuario.

Trocar a fixtura `agenda` de `TestJanelaDeSilencio` (por volta da linha 1737)
para devolver `agenda_de_silencio()`.

`TestAgendaRealDoUsuario` NAO e tocada nesta task — ela e a Task 2.
  </action>
  <verify>
    <automated>python -m pytest tests/test_agenda.py::TestJanelaDeSilencio -q -p no:cacheprovider 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
Os 10 testes de `TestJanelaDeSilencio` passam, incluindo os 4 que estavam
vermelhos, com o `config.toml` do usuario intocado em `9`.
  </done>
</task>

<task type="auto">
  <name>Task 2: as tres fixturas de silenciamento e o esquema em TestAgendaRealDoUsuario</name>
  <files>tests/test_agenda.py, tests/test_silenciamento.py</files>
  <action>
**(a) `tests/test_silenciamento.py` — as tres fixturas.** Trocar as fixturas
`agenda` das linhas ~119 (`TestControleDoSilencio`), ~470 (`TestCancelarOSilencio`)
e ~536 (`TestOQueDaParaCancelar`) para devolverem `agenda_de_silencio()`.

Importar a fabrica **dentro da funcao**, seguindo o estilo que o arquivo ja usa
para `SEGUNDA` (`from tests.test_agenda import SEGUNDA`, sempre dentro do corpo).
Nao promover para import de topo: a forma em funcao ja esta provada neste
arquivo e o pacote `tests` nao tem `__init__.py`.

As tres classes somam 16 testes e TODOS tem de continuar passando — inclusive os
14 que ja estavam verdes. `test_cancelar_o_prime_NAO_cala_o_tvt` volta a valer
porque a fabrica fixa o TvT em 15 min: 21:50+15 = 22:05.

**(b) `tests/test_agenda.py` — o esquema, exatamente no molde de `8b87eb3`.**
Em `TestAgendaRealDoUsuario::test_as_duracoes_de_silencio_estao_no_esquema_para_a_fase_7`
(linha ~570): a fixtura dessa classe CONTINUA lendo o arquivo do repositorio, e
os testes de `horarios` e de `chamar_minutos_antes` continuam cravando valor —
esse acoplamento e deliberado e o cabecalho do proprio `config.toml` (linhas
140-142) explica por que existe.

O que muda e so a assercao sobre `silenciar_minutos`: em vez de `== 15` e
`== 120`, afirmar o ESQUEMA para os tres eventos — o campo existe, e inteiro, e
nao e negativo. Renomear o teste para dizer que a duracao e LIDA (o nome atual
promete "esquema" e entrega valor cravado).

Na docstring, escrever a razao no tom do precedente: a duracao do silencio e
PREFERENCIA do usuario, ela MUDA — em 2026-08-31 ele a trocou de 15 para 9 — e
cravar o numero aqui transforma um ajuste de preferencia dele em deploy. Citar
`8b87eb3`, que consertou o mesmo defeito na janela de respawn do Tiat.
  </action>
  <verify>
    <automated>python -m pytest tests/test_agenda.py tests/test_silenciamento.py tests/test_comandos.py -q -p no:cacheprovider 2>&amp;1 | tail -4</automated>
  </verify>
  <done>
Os tres arquivos ficam **399 passed, 0 failed** (392 do baseline verde + os 7
consertados) com o `config.toml` do usuario ainda em `9`.
  </done>
</task>

<task type="auto">
  <name>Task 3: a prova mecanica — dois valores, contagem que nao cai, e o mutante que morre</name>
  <files>tests/test_agenda.py, tests/test_silenciamento.py</files>
  <action>
Nenhum codigo novo. Esta task RODA os quatro criterios abaixo e so termina com
os quatro verdes. Se algum reprovar, o conserto volta para a Task 1 ou 2.

Rodar da raiz do repositorio, um bloco de cada vez. Os blocos 1 e 3 mexem em
arquivos e RESTAURAM na mesma execucao — nao interrompa no meio.
  </action>
  <verify>
    <automated>
# ---------------------------------------------------------------------------
# CRITERIO 1 — O CORACAO: a suite COMPLETA fica verde com DOIS valores de
# silenciar_minutos, e o valor do usuario e restaurado ao fim aconteca o que
# acontecer (o trap cobre inclusive Ctrl-C e falha no meio).
# ---------------------------------------------------------------------------
ORIG="$(mktemp)"; cp config.toml "$ORIG"
trap 'cp "$ORIG" config.toml' EXIT

por_valor() {
  python - "$1" <<'PY'
import pathlib, re, sys
p = pathlib.Path("config.toml"); t = p.read_text(encoding="utf-8")
novo, n = re.subn(r"(?m)^silenciar_minutos = \d+$",
                  "silenciar_minutos = " + sys.argv[1], t, count=1)
assert n == 1, "a linha do TvT nao foi encontrada"
p.write_text(novo, encoding="utf-8")
PY
  for _ in 1 2 3 4 5; do
    s="$(python -m pytest -q -p no:cacheprovider 2>&1)"
    # flake conhecido do test_agenda.py: abortar NAO e falhar, rode de novo
    if printf '%s' "$s" | grep -q 'KeyboardInterrupt'; then continue; fi
    if printf '%s' "$s" | grep -qE '[0-9]+ failed'; then
      echo "VERMELHO com silenciar_minutos = $1"; return 1
    fi
    if printf '%s' "$s" | grep -qE '[0-9]+ passed'; then
      echo "verde com silenciar_minutos = $1"; return 0
    fi
  done
  echo "5 abortos seguidos com $1 — nao da para afirmar verde"; return 1
}

por_valor 9  || exit 1
por_valor 15 || exit 1

# ---------------------------------------------------------------------------
# CRITERIO 2 — ANTI-APAGAMENTO: consertar nao pode ser deletar. O baseline
# medido hoje foi 3797 passed com 7 failed; consertados, tem de ser >= 3804.
# ---------------------------------------------------------------------------
cp "$ORIG" config.toml
n="$(python -m pytest -q -p no:cacheprovider 2>&1 | grep -oE '[0-9]+ passed' | grep -oE '^[0-9]+')"
test -n "$n" || { echo "nao consegui ler a contagem"; exit 1; }
test "$n" -ge 3804 || { echo "a contagem CAIU para $n (< 3804): teste apagado em vez de consertado"; exit 1; }
echo "contagem OK: $n"

# ---------------------------------------------------------------------------
# CRITERIO 3 — ANTI-VACUO: os testes ainda DISCRIMINAM. Baixando a janela do
# TvT nos dados PROPRIOS da fixtura de 15 para 9, a uniao com o Prime passaria
# a terminar 22:00 em vez de 22:05 — TestJanelaDeSilencio TEM de ficar vermelho.
# Um mutante que sobrevive significa que os testes pararam de ler a janela.
# ---------------------------------------------------------------------------
MUT="$(mktemp)"; cp tests/test_agenda.py "$MUT"
sed -i 's/^SILENCIO_DO_TVT = 15$/SILENCIO_DO_TVT = 9/' tests/test_agenda.py
grep -q '^SILENCIO_DO_TVT = 9$' tests/test_agenda.py || { cp "$MUT" tests/test_agenda.py; echo "a constante nomeada nao existe — criterio 3 nao pode nem rodar"; exit 1; }
mut="$(python -m pytest tests/test_agenda.py::TestJanelaDeSilencio -q -p no:cacheprovider 2>&1)"
cp "$MUT" tests/test_agenda.py
printf '%s' "$mut" | grep -qE '[0-9]+ failed' || { echo "MUTANTE SOBREVIVEU: os testes nao afirmam mais a borda 22:05"; exit 1; }
echo "mutante morto"
grep -q '^SILENCIO_DO_TVT = 15$' tests/test_agenda.py || { echo "a restauracao do mutante falhou"; exit 1; }

# ---------------------------------------------------------------------------
# CRITERIO 4 — O VALOR DO USUARIO VOLTOU. Grep no CONTEUDO do arquivo, e nao
# em `git diff`: a forma com git fica cega depois do commit (STATE.md, 04-05).
# ---------------------------------------------------------------------------
cp "$ORIG" config.toml
test "$(grep -c '^silenciar_minutos = 9$' config.toml)" -eq 1 || { echo "o 9 do usuario nao voltou"; exit 1; }
test "$(grep -c '^silenciar_minutos = 15$' config.toml)" -eq 0 || { echo "o config.toml ficou com o valor do teste"; exit 1; }
test "$(grep -c '^silenciar_minutos = 120$' config.toml)" -eq 1 || { echo "o Prime foi alterado"; exit 1; }
echo "config.toml do usuario intacto"

echo "OS QUATRO CRITERIOS PASSARAM"
    </automated>
  </verify>
  <done>
A saida termina em `OS QUATRO CRITERIOS PASSARAM`. Trocar `silenciar_minutos`
para qualquer valor deixou de quebrar teste; nenhum teste sumiu; o mutante
morreu; e o `9` do usuario esta de volta no arquivo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| criterio de aceitacao -> `config.toml` do usuario | O unico ponto deste plano que ESCREVE num arquivo que o usuario possui. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v2m-01 | Tampering | mutacao de `config.toml` no Criterio 1 | high | mitigate | Copia de `mktemp` feita ANTES da primeira escrita + `trap ... EXIT`, que restaura mesmo com falha ou Ctrl-C no meio; Criterio 4 confere o conteudo restaurado por grep ancorado. |
| T-v2m-02 | Tampering | mutacao de `tests/test_agenda.py` no Criterio 3 | medium | mitigate | Copia antes, restauracao IMEDIATAMENTE apos a rodada e ANTES do `grep` que julga o resultado — o julgamento nunca acontece com a arvore suja; conferido por grep da constante original. |
| T-v2m-03 | Tampering | commit arrastando `config.toml` | medium | mitigate | Restricao 5: o commit leva apenas os dois arquivos de teste, nomeados explicitamente. |
| T-v2m-04 | Repudiation | agente alheio na mesma arvore | medium | accept | Restricao 1 mantem o plano fora de `l2scanner/`; `--amend` proibido pela Restricao 4. Colisao em `tests/test_agenda.py` e improvavel (o outro agente esta em `tests/test_bosses.py`) e visivel no commit. |
| T-v2m-SC | Tampering | npm/pip/cargo installs | high | mitigate | Nao aplicavel: zero instalacao neste plano (FIRE-01). Nenhum pacote e adicionado. |
</threat_model>

<verification>
1. `python -m pytest -q` com `silenciar_minutos = 9` → 0 failed
2. `python -m pytest -q` com `silenciar_minutos = 15` → 0 failed
3. Contagem de `passed` ≥ 3804 (baseline medido: 3797 + 7 consertados)
4. Mutante `SILENCIO_DO_TVT` 15→9 deixa `TestJanelaDeSilencio` vermelho
5. `config.toml` volta com `silenciar_minutos = 9` no TvT e `120` no Prime
6. `git status` mostra apenas os dois arquivos de teste modificados por este plano
</verification>

<success_criteria>
Trocar `silenciar_minutos` no `config.toml` para qualquer valor nao quebra teste
nenhum — provado com DOIS valores na suite COMPLETA, sem apagar teste (contagem
≥ 3804) e sem esvaziar as assercoes (o mutante da borda 22:05 morre). O valor do
usuario volta intacto.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/quick/260831-v2m-testes-de-agenda-param-de-ler-o-config-real/260831-v2m-SUMMARY.md` when done
</output>
