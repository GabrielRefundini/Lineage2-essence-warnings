---
quick_id: 260901-p2r
workstream: mercado
type: execute
files_modified:
  - l2scanner/calibrar_mercado.py
  - tests/test_calibrar_mercado.py
  - tests/test_mercado_adena_pagina.py
autonomous: true
---

# A guarda de paridade avisa cedo, e o teste para de medir a propria copia

Duas dividas levantadas pelo verificador da Fase 5. Independentes, mas ambas do
mesmo tipo: **um mecanismo que afirma uma coisa e faz outra.**

## DEBT-06 — a guarda avisa TARDE, e duas frases dizem o contrario

**Medido pelo verificador:** `cortar_glifos` (os treze arrastos do usuario) roda em
`calibrar_mercado.py:2586`; `conferir_a_paridade_do_ciano` so em 2491, dentro de
`_gravar_os_glifos`, chamado em 2595. **Ela impede a ESCRITA, nao a sessao.**

**O que a torna divida e nao limitacao:** DUAS afirmacoes no repositorio dizem o
contrario.

- `calibrar_mercado.py:2489` — *"tem de custar uma mensagem, e nao uma sessao de farm"*
- o doc da sessao de debug — *"o que impede o esforco jogado fora"*

As duas sao FALSAS hoje. Em 2026-09-01 elas custaram ao usuario **TRES rodadas
completas**: ele cortou os treze glifos, a matriz de confusao APROVOU, e so entao a
paridade recusou. Duas vezes seguidas.

**Pela regra da casa, ou as frases se corrigem ou a guarda sobe.**

**O conserto barato existe, e o verificador o nomeou:**
`anel_do_zero_esta_partido` **ja e pura sobre UM molde**. Da para conferir no
primeiro `0` cortado, dentro do laco de `cortar_glifos`, e recusar ali — antes dos
outros doze.

**O que NAO pode mudar:** a guarda no momento da ESCRITA continua existindo. Ela e
a rede final; a nova e conveniencia, nao substituta. Se a de dentro do laco for
contornada por qualquer caminho, a de fora ainda pega.

## DEBT-07 — o teste mede a propria copia

`tests/test_mercado_adena_pagina.py:350` — `_vencedor_medido` REIMPLEMENTA
`_casamento_do_layout` dentro do teste: limiar, `max`, empate, `None`.
`test_o_vencedor_por_banda` mede a copia, **nao a producao**.

E contido — a funcao real e chamada em `TestOPortaoESCOLHEEmVezDeSoRecusar` e no
ponta a ponta — por isso e aviso e nao lacuna. Mas e a **decima aparicao do padrao
desta sessao**, em variante nova: o teste que passa mesmo se a producao mudar.

**Conserto de uma linha:** trocar o corpo de `_vencedor_medido` por
`leitor._casamento_do_layout(...)`.

**Controle negativo obrigatorio:** mostre que o teste consertado CAI se a producao
mudar de comportamento (mutacao medida), e que o antigo NAO caia. Sem isso, a troca
e cosmetica.

## Aceitacao

- **DEBT-06:** teste que corta um `0` de anel PARTIDO e afirma que a recusa sai
  **antes** do segundo glifo — nao depois dos treze. Mais o controle negativo: um
  `0` de anel FECHADO nao e recusado.
- **DEBT-06:** a guarda da ESCRITA continua existindo e continua pegando — teste
  que a exercita por um caminho que pule a do laco.
- **DEBT-06:** as duas frases falsas ficam verdadeiras, ou sao corrigidas para
  dizer o que o codigo faz. Nenhuma das duas pode continuar como esta.
- **DEBT-07:** o teste consertado CAI sob mutacao da producao; o antigo NAO caia.
- Suite completa verde (referencia: 4382 passed, 2 skipped).

## Restricoes

- **NAO escrever** em `.mercado/` nem em `calibration.json` — o usuario acabou de
  cortar 13 moldes cromaticos a mao, e perde-los seria destruir o trabalho da noite.
  Todo teste em `tmp_path`.
- **NAO tocar** `rastreador.py`, `visao.py`, `respawn.py`, `bosses.py`,
  `agenda.py`, `sessao.py`, `test_bosses.py` — outro agente trabalha nesta arvore.
- **NAO mexer** no conjunto de moldes nem nos limiares. Isto e conserto de
  MOMENTO da guarda e de honestidade de teste, nao de deteccao.
- Nenhuma dependencia nova. Nenhum `--amend`. Nunca `git stash`.
