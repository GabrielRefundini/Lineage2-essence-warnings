---
phase: 01-funda-o-firewall-gravador-e-spike-de-campo
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
gap_closure: true
files_modified:
  - l2scanner/calibrar_mercado.py
  - l2scanner/mercado_visao.py
  - l2scanner/calibracao.py
  - calibrar-mercado.bat
  - tests/test_mercado_glifos.py
  - tests/test_calibrar_mercado.py
  - tests/fixtures/mercado/
  - .planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-04-SUMMARY.md
autonomous: false
requirements: [FUND-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O usuario roda a ferramenta de calibracao sobre um frame GRAVADO e ve as regioes da janela, a ancora do painel E OS TEMPLATES DE DIGITO persistidos em calibration.json — sem editar JSON a mao (criterio 3 do ROADMAP, as TRES coisas que ele enumera)"
    - "mercado_templates_de_digito deixa de ser null: carrega os dez digitos, a virgula e os dois sufixos de moeda, cada um com molde que decodifica na forma declarada"
    - "O corte de glifos NAO pede dez retangulos ao usuario: ele marca UM numero, digita o que le, e a segmentacao por projecao de coluna devolve os glifos — a mesma economia de erro humano que ja deriva 10 linhas de uma linha marcada (D-06)"
    - "A ferramenta RECUSA ALTO quando dois glifos colidem, nomeando o par: `0` lido como `8` num preco corrompe a serie inteira, e a matriz de confusao dos glifos e MEDIDA na hora, nunca chutada"
    - "Um conjunto de glifos INCOMPLETO nunca e gravado calado: a ferramenta nomeia cada glifo que faltou, e uma rodada nova FUNDE com o que ja estava gravado em vez de apagar (precedente CR-04)"
    - "Uma mao humana desenha os retangulos de ponta a ponta e ve a mensagem de calibracao gravada — a metade do criterio 3 que grep nao alcanca"
  artifacts:
    - path: "l2scanner/calibrar_mercado.py"
      provides: "modo de corte de glifos: segmentacao por projecao de coluna, rotulo digitado com conferencia de contagem, matriz de confusao dos glifos, flag --so-digitos e montagem de conferencia visual"
      contains: "segmentar_glifos"
    - path: "l2scanner/mercado_visao.py"
      provides: "glifos_para_calibracao / glifos_de_calibracao — o par de empacotamento dos moldes de glifo, no trilho literal de ancoras_para_calibracao, passando forma_esperada a molde_de_hex"
      contains: "glifos_de_calibracao"
    - path: "l2scanner/calibracao.py"
      provides: "mercado_limiar_de_glifo como chave opcional via .get, com validacao de forma; VERSAO_DO_ESQUEMA segue 2"
      contains: "mercado_limiar_de_glifo"
    - path: "tests/test_mercado_glifos.py"
      provides: "a metade PURA: segmentacao afirmada contra pixels reais resgatados, matriz de confusao dos glifos, ida-e-volta do empacotamento"
      min_lines: 120
    - path: "tests/fixtures/mercado/glifos_precos_f010.png"
      provides: "240x45 px resgatados da coluna Total do frame_000010 de mercado-pagina-cheia — seis precos reais que somam nove dos dez digitos e a virgula"
    - path: "tests/fixtures/mercado/glifos_unitario_f010.png"
      provides: "30x40 px da coluna Unit price do mesmo frame — o unico `6` do frame"
  key_links:
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/identidade.py"
      via: "mascara_de_texto — a mesma mascara de brilho ja medida para nomes de party e a representacao que DOBRA a margem entre glifos (0.7831 contra 0.8648 em tons de cinza)"
      pattern: "mascara_de_texto"
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/mercado_visao.py"
      via: "glifos_para_calibracao — a ferramenta nao empacota molde a mao; o par de serializacao vive junto do das ancoras"
      pattern: "glifos_para_calibracao"
    - from: "l2scanner/mercado_visao.py"
      to: "calibration.json"
      via: "glifos_de_calibracao passa forma_esperada a molde_de_hex — o guard do WR-01/WR-04 finalmente com chamador"
      pattern: "forma_esperada"
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/calibrar.py"
      via: "UMA unica chamada a _gravar_conferencia por rodada, com a montagem dos glifos composta na mesma tela dos retangulos"
      pattern: "_gravar_conferencia"
  prohibitions:
    - "MUST NOT gravar imagem por conta propria em calibrar_mercado.py — o teste test_o_modulo_novo_nao_grava_imagem_por_conta_propria conta as ocorrencias no fonte e tem de seguir verde"
    - "MUST NOT chamar _gravar_conferencia duas vezes na mesma rodada — ela grava num nome FIXO, entao a segunda chamada apaga a primeira e o usuario confere metade da calibracao achando que confere tudo"
    - "MUST NOT reusar COLISAO_MAXIMA_ENTRE_TEMPLATES para os glifos — ele e politica para nomes LONGOS; sobre glifos de 4x11 px em tons de cinza o pior par real mede 0.8648 e a ferramenta recusaria o conjunto correto"
    - "MUST NOT alinhar a matriz dos glifos cortando ao menor comum — a virgula tem UMA coluna de largura, o corte reduz todo par a essa coluna e o guard de desvio devolve 0.0: uma colisao inventada como ausencia de colisao"
    - "MUST NOT alterar matriz_de_confusao nem _alinhar — sao a rota dos moldes de NOME, testadas e corrigidas em CR-03/CR-04; o caminho dos glifos nasce ao lado, nao por dentro"
    - "MUST NOT subir VERSAO_DO_ESQUEMA — as chaves novas entram opcionais via .get, no precedente banner_manutencao (D-07)"
    - "MUST NOT gravar lista de glifos vazia por cima de uma nao-vazia, nem trocar o conjunto anterior pelo desta rodada: a fusao e por rotulo e o corte novo vence, o ausente e preservado (CR-04)"
    - "MUST NOT cortar moldes de NOME neste plano — os moldes da watchlist estao classificados como `deferred` para a Fase 2 pela VERIFICATION, e nao sao lacuna"
    - "MUST NOT tocar rastreador.py, o gate de brilho da barra propria em visao.py, nem acrescentar escrita de imagem a calibrar.py"
    - "MUST NOT commitar gravacoes do usuario alem dos dois recortes minimos nomeados neste plano"
    - "MUST NOT acrescentar dependencia nova — FIRE-01 fica vermelho com qualquer biblioteca de sintese de input, e este plano nao instala nada"
---

<objective>
Fechar a lacuna G-01 da `01-VERIFICATION.md`: dar um PRODUTOR aos templates de digito.

O criterio 3 do ROADMAP enumera tres coisas — regioes da janela, ancora do painel e
**templates de digito**. Duas chegaram e estao medidas. A terceira nao existe no disco
(`mercado_templates_de_digito: null`) e, pior, nao existe como capacidade: `calibrar_mercado.py`
tem 940 linhas e nenhuma delas corta um glifo. A chave e um campo de passagem em
`calibracao.py` — declarada, serializada, desserializada, nunca produzida. O
`01-04-SUMMARY.md` a conta entre as "5 chaves opcionais novas", o que faz um leitor
concluir que ela e produzida.

Purpose: FUND-03 esta BLOQUEADO por esta metade. Enquanto ela nao existir, a Fase 2 (LEIT-02,
"precos lidos por template-por-digito com falha FECHADA") nao tem de onde ler os moldes — e a
alternativa registrada pela VERIFICATION (mover o corte para a Fase 2) foi descartada: cortar
glifos e um trabalho de CALIBRACAO sobre frame gravado, exatamente o que esta ferramenta e.

Output: modo de corte de glifos em `calibrar_mercado.py` com segmentacao por projecao de coluna
e conferencia de contagem contra o rotulo digitado, o par de empacotamento em `mercado_visao.py`,
a chave de limiar em `calibracao.py`, dois recortes de pixels reais resgatados para
`tests/fixtures/mercado/`, e o portao humano que finalmente exercita o fluxo inteiro com o mouse.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-VERIFICATION.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-CONTEXT.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-PATTERNS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-04-SUMMARY.md
</context>

<decision_map>
IDs D-NN das decisoes travadas de `01-CONTEXT.md` > `<decisions>`, mesma tabela dos planos 01-04.
As duas que mandam neste plano:

**D-06** — calibracao em modo proprio (`calibrar-mercado.bat`), no estilo `selectROI` da ferramenta
atual, **reusando o codigo de calibracao existente**, fonte = **frame GRAVADO** (nunca captura ao
vivo), **templates cortados pela propria ferramenta com confirmacao visual e matriz de confusao
medida**, criterio "nunca editar JSON a mao". Este plano entrega a metade de D-06 que a
VERIFICATION marcou como nao honrada: *"para os moldes de DIGITO nao ha ferramenta"*.

**D-07** — chaves novas em `calibration.json` sao opcionais via `.get` (precedente
`banner_manutencao`); `VERSAO_DO_ESQUEMA` congelado em 2. Instalacao sem calibracao de mercado
continua carregando.

Fora de escopo por decisao do verificador, nao por esquecimento: **D-05** (variantes de encanto,
moldes de NOME da watchlist) esta classificado como `deferred` para a Fase 2 na
`01-VERIFICATION.md` > `deferred[0]` — a capacidade existe, avisa alto quando nao corta nada, e o
que falta e a entrada do usuario. Nenhuma tarefa aqui toca moldes de nome.
</decision_map>

<source_audit>
## Auditoria de cobertura (escopo de fechamento de lacuna)

Este plano nao replaneja a fase: ele fecha UM item verificado como faltante. As quatro fontes,
restritas a esse item:

| Fonte | Item | Coberto por |
|---|---|---|
| GOAL (ROADMAP criterio 3) | "...e os templates de digito persistidos em `calibration.json`" | Tasks 1 e 2 |
| GOAL (ROADMAP criterio 3) | "O usuario roda a ferramenta ... sem editar JSON a mao" | Task 3 (portao humano) |
| REQ | FUND-03 — calibracao do mercado persiste via ferramenta propria | Tasks 1, 2, 3 |
| RESEARCH / SPIKE-RESPOSTAS 2 | "Glifos que a Fase 2 precisa cortar: os dez digitos, a virgula, e os sufixos `XM Coin` e `Adena` como palavras inteiras" | Task 2 (`GLIFOS_EXIGIDOS`) |
| SPIKE-RESPOSTAS 2 (validado) | agrupamento como TRAVA DE VALIDACAO: 3 digitos apos virgula = milhar, 2 = decimal | Fora deste plano por desenho — a trava e do LEITOR (Fase 2, LEIT-02). Aqui so nascem os moldes |
| SPIKE-RESPOSTAS 3 (validado) | tres layouts de coluna | Ja coberto: `mercado_grade.layout` gravado no 01-04. Glifos sao independentes de layout (mesma fonte) |
| CONTEXT | D-06 (metade dos digitos), D-07 | Tasks 1, 2 |
| VERIFICATION `missing[2]` | "Corrigir a linha 17 do 01-04-SUMMARY.md" | Task 2 |
| VERIFICATION `deferred[0]` | moldes de NOME da watchlist | EXCLUIDO — adiamento registrado, nao e lacuna |
| VERIFICATION WARNING-01 / WARNING-02 | dano residual no `calibration.json`, bookkeeping do ROADMAP | EXCLUIDOS — avisos, nao lacunas; WARNING-01 fecha sozinho na recalibracao da Task 3 |

**Nenhum item da lacuna fica sem plano.**
</source_audit>

<evidencia_medida>
## O que eu MEDI antes de escrever este plano — nao re-derive

Tudo abaixo saiu de execucao real sobre os frames gravados, nesta maquina, com o `cv2` do
projeto. Os numeros vao para docstrings; o executor CONFERE, nao redescobre.

### 1. A segmentacao por projecao de coluna funciona, e funciona limpa

Mascara de brilho `identidade.mascara_de_texto` (V > 180), projecao das colunas, **qualquer
coluna vazia separa** — sem tolerancia de lacuna. Sobre
`recordings/20260828-060622-mercado-pagina-cheia/frame_000010.png`, coluna `Total`, linhas 5 a 10:

    preco       glifos esperados   runs encontrados
    100,00            6                  6
    3,00              4                  4
    18,90             5                  5
    7,50              4                  4
    18,00             5                  5
    2,45              4                  4

E na coluna `Unit price` do mesmo frame: `6,00` -> 4 runs, `9,45` -> 4 runs. **8 de 8.**

Geometria dos glifos: **11 px de altura**; digitos com **4 px** de largura, com uma excecao — o
`4` mede **6 px**; a **virgula mede 1 px**. A menor lacuna observada entre dois glifos vizinhos e
de **1 coluna vazia**, o que e o que autoriza a regra "qualquer coluna vazia separa".

Os precos sao alinhados a DIREITA e as colunas de run repetem posicao entre linhas
(90, 96, 102, 108, 111, 117 dentro do recorte) — util para conferir a marcacao, inutil como
substituto dela.

### 2. A representacao decide se o conjunto e separavel — e a de cinza NAO e

Matriz de confusao completa (55 pares) sobre os 11 glifos (`0`-`9` e a virgula) cortados desse
frame, com `mercado_visao.casamento_da_ancora` e alinhamento por preenchimento ate a maior caixa:

    representacao              pior par inter-classe     margem ate 1.0
    tons de cinza nativo       0.8648   (`0` x `8`)          0.1352
    MASCARA BINARIA (V>180)    0.7831   (`0` x `8`)          0.2169
    cinza ampliado 4x cubico   0.8624   (`0` x `8`)          0.1376
    mascara ampliada 4x        0.7451   (`0` x `8`)          0.2549

Tres consequencias, todas duras:

- **A mascara binaria e a escolha.** Ela quase dobra a margem (0.2169 contra 0.1352) sobre a
  mesma evidencia, e e a mesma representacao que `identidade.py` ja usa para nomes de party. A
  ampliacao 4x nao paga: sobre cinza ela nao mexe no numero (0.8648 -> 0.8624) e sobre mascara
  ela mistura os vizinhos na interpolacao.
- **`COLISAO_MAXIMA_ENTRE_TEMPLATES = 0.85` recusaria o conjunto CORRETO** se aplicado sobre
  cinza: 0.8648 > 0.85. O glifo precisa da sua propria constante, e ela precisa da sua propria
  medicao ao lado.
- **Os vizinhos do pior par:** `3` x `5` em 0.6902 e `3` x `8` em 0.5633 (na mascara). O `0` x `8`
  esta sozinho la em cima — e e exatamente o par que corromperia um preco calado.

Nota de cor: os precos aparecem em **branco** (linha normal), **ciano** (a linha destacada
`100,00` do proprio frame) e **dourado** (a quantidade em Adena). Os numeros acima usam SO linhas
brancas. Com o `0` vindo da linha ciano, o pior par cai para 0.7219 — cruzar cores nao quebra a
correlacao normalizada, mas os moldes gravados devem sair de linhas normais.

### 3. O frame de calibracao designado NAO tem os dez digitos

`recordings/20260828-115700-calibragem/frame_000012.png` (aba Adena, painel limpo — o frame que
calibrou a grade atual) mostra `5,000,000 Adena` e os precos 59,00 / 61,99 / 62,00 / 63,00 /
64,00 / 65,00 / 67,50 / 67,95. Os digitos presentes sao `0 1 2 3 4 5 6 7 9`. **O `8` nao aparece.**
Conferi os frames 0, 6, 18 e 23 dessa gravacao: sao a mesma tela parada, entao nenhum outro frame
dela resolve.

`recordings/20260828-060622-mercado-pagina-cheia/frame_000010.png` tem **os dez digitos e a
virgula** entre as colunas `Total` (linhas 5-10 dao `0 1 2 3 4 5 7 8 9`) e `Unit price` (o `6,00`
da linha 10 da o `6`).

**Consequencia de desenho, e nao conveniencia:** o corte de glifos precisa poder rodar sobre um
frame DIFERENTE daquele que calibrou a grade, sem obrigar o usuario a refazer os cinco arrastos
de ancora e grade. Isto e o que justifica a flag da Task 2. Os moldes de glifo nao carregam
coordenada nenhuma — sao recortes de imagem — entao trocar de frame nao acopla nada, desde que a
janela tenha o mesmo tamanho (que `conferir_o_frame` ja exige).

### 4. Os recortes a resgatar, com tamanho ja medido

    tests/fixtures/mercado/glifos_precos_f010.png    <- frame_000010[665:905, 1120:1165]   240x45 px,  5354 bytes
    tests/fixtures/mercado/glifos_unitario_f010.png  <- frame_000010[840:870, 1410:1450]    30x40 px,   769 bytes

O primeiro contem, de cima para baixo e com passo de 45 px, exatamente: `100,00`, `3,00`,
`18,90`, `7,50`, `18,00`, `2,45`. O segundo contem `6,00`. Ambos sao so digitos: nenhum nome de
personagem, nenhuma linha de chat, nenhum nome de item.
</evidencia_medida>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: a metade PURA — segmentacao por projecao de coluna, matriz de confusao dos glifos e o par de empacotamento (D-06, FUND-03)</name>
  <precondition>`recordings/20260828-060622-mercado-pagina-cheia/frame_000010.png` existe no checkout PRINCIPAL (a pasta `recordings/` e gitignored e nao vem de clone limpo). Num worktree, leia-o por caminho absoluto, somente leitura — os recortes resgatados e que entram no repositorio.</precondition>
  <reversibility rating="reversible">Funcoes puras novas mais dois PNGs de fixture. Nenhum formato em disco muda nesta task; a chave em `calibration.json` so e escrita na Task 2.</reversibility>
  <files>l2scanner/calibrar_mercado.py, l2scanner/mercado_visao.py, tests/test_mercado_glifos.py, tests/fixtures/mercado/glifos_precos_f010.png, tests/fixtures/mercado/glifos_unitario_f010.png</files>
  <read_first>
    - `l2scanner/identidade.py` linhas 56, 134-145 — `VALOR_MINIMO_DO_TEXTO = 180` e `mascara_de_texto`: a mascara de BRILHO, sem filtro de saturacao, e a razao dita no docstring (o nome do lider e amarelo). E a mesma razao que faz ela servir para o dourado do `Adena` e o ciano da linha destacada
    - `l2scanner/identidade.py` linhas 228-250 — `_primeira_coluna_com_texto` e `_inicio_do_nome_apos_ornamento`: a mecanica de `np.flatnonzero(mascara.any(axis=0))` e `np.diff(colunas) > LACUNA` ja existe no projeto. A segmentacao de glifos e essa mesma leitura com a lacuna em zero
    - `l2scanner/identidade.py` linhas 187-214 — `_correlacionar` e a licao MEDIDA sobre posicao unica contra maximo deslizante (erros subiam ate +0.373). O mesmo argumento vale glifo a glifo, e com mais forca: um glifo tem 4 px de largura
    - `l2scanner/mercado_visao.py` linhas 102-170 — `casamento_da_ancora` e o guard de desvio (`std < 1e-6` devolve 0.0). E este guard que torna o alinhamento por CORTE inutilizavel quando a virgula de 1 px esta no conjunto
    - `l2scanner/mercado_visao.py` linhas 207-280 — `molde_para_hex` / `molde_de_hex`, incluindo o parametro `forma_esperada` e o comentario sobre o molde transposto (WR-04)
    - `l2scanner/mercado_visao.py` linhas 518-560 — `ancoras_para_calibracao` / `ancoras_de_calibracao`: o par de empacotamento a espelhar, e o unico lugar do projeto que hoje passa `forma_esperada` (a correcao do WR-01)
    - `l2scanner/calibrar_mercado.py` linhas 80-92 — `COLISAO_MAXIMA_ENTRE_TEMPLATES` e o paragrafo "HONESTIDADE SOBRE ESTE NUMERO": ele diz explicitamente que a constante e POLITICA e nao medicao, e por que. A constante dos glifos nasce com o paragrafo equivalente, mas com medicao de verdade ao lado
    - `l2scanner/calibrar_mercado.py` linhas 165-278 — `ResultadoDaConfusao`, `.rodou`, `.explicar`, `_alinhar` e `matriz_de_confusao`: o trilho a espelhar, incluindo a licao do CR-03 (com menos de dois moldes NAO se afirma score nem limiar)
    - `tests/test_calibrar_mercado.py` linhas 239-355 — `TestAMatrizDeConfusao`: a forma dos testes de matriz, incluindo `test_sem_par_para_comparar_NAO_afirma_score_nem_deriva_limiar`
    - `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` secao 2 — a virgula como separador de milhar E decimal na mesma linha, e a lista dos glifos que a Fase 2 precisa
    - O bloco `<evidencia_medida>` no topo deste plano — os numeros que vao para as docstrings
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_mercado_glifos.py`, sobre os PIXELS RESGATADOS (fixtures
    commitadas), nunca sobre `recordings/`:

    - Segmentacao sobre `glifos_precos_f010.png`: fatiando a fixture em seis faixas de 45 px, a
      contagem de runs de cada faixa e exatamente 6, 4, 5, 4, 5, 4 — os comprimentos de
      `100,00`, `3,00`, `18,90`, `7,50`, `18,00`, `2,45`. Este e o teste que prende a tecnica
      inteira: se a segmentacao regredir, ele fica vermelho contra pixels de jogo real.
    - Segmentacao sobre `glifos_unitario_f010.png`: 4 runs, os de `6,00`.
    - Geometria medida: cada run devolvido tem largura entre 1 e 6 px, e o run da virgula em
      `3,00` mede 1 px — a prova de que o glifo mais estreito do conjunto sobrevive a
      segmentacao em vez de ser absorvido pelo vizinho.
    - Recorte vazio e recorte sem nenhum pixel de texto devolvem lista VAZIA, e nao levantam.
    - Matriz dos glifos, caso REAL: montando o conjunto dos 11 glifos a partir das duas fixtures
      e dos rotulos conhecidos, a matriz APROVA e o pior par inter-classe fica em 0.7831 +/- 0.01,
      com o par nomeado sendo `0` x `8`. Assercao sobre o par, nao so sobre o numero.
    - Matriz dos glifos, caso de COLISAO: com dois glifos sinteticos quase identicos a matriz
      RECUSA, nomeia o par, e nao deriva limiar.
    - Alinhamento: um par formado por um glifo largo e a virgula de 1 px produz um score
      MEDIDO (a matriz `rodou` e o par aparece nela) em vez do 0.0 que o corte ao menor comum
      produziria — o teste que impede a regressao para o alinhamento errado.
    - A rota dos NOMES nao muda: `matriz_de_confusao` continua devolvendo os mesmos resultados
      dos testes existentes (a suite antiga e a assercao).
    - Ida-e-volta do empacotamento: um dicionario de glifos empacotado e desempacotado devolve
      arrays identicos, com os rotulos preservados; um molde com dimensao declarada trocada
      (transposto) e RECUSADO com mensagem que manda recalibrar.
    - Entrada nao confiavel: lista de glifos com item sem rotulo, com molde faltando, ou com
      `bytes` de tamanho errado levanta erro explicado em vez de devolver molde silenciosamente
      errado.
  </behavior>
  <action>
Tres entregas, todas de codigo PURO — nenhuma janela, nenhum mouse. A Task 2 e que costura isto
no fluxo interativo.

**(a) Resgatar os dois recortes para `tests/fixtures/mercado/`.** Ler
`recordings/20260828-060622-mercado-pagina-cheia/frame_000010.png` do checkout principal por
caminho absoluto, fatiar `[665:905, 1120:1165]` em `tests/fixtures/mercado/glifos_precos_f010.png`
e `[840:870, 1410:1450]` em `tests/fixtures/mercado/glifos_unitario_f010.png`, e conferir os
tamanhos contra os medidos (240x45 e 30x40; cerca de 5,4 KB e 0,8 KB). Faca isso com um script
descartavel executado uma vez — nao deixe codigo de recorte de gravacao no repositorio, e nao
use `l2scanner/calibrar.py` nem `l2scanner/calibrar_mercado.py` como veiculo, porque os dois tem
tripwire estrutural contando escrita de imagem no fonte. Confira que os PNGs contem SO digitos:
nenhum nome de personagem, nenhuma linha de chat.

**(b) `segmentar_glifos` em `l2scanner/calibrar_mercado.py`.** Assinatura sugerida:
`def segmentar_glifos(recorte: np.ndarray) -> list[tuple[int, int]]:`, devolvendo os pares
`(inicio, fim)` de coluna, na ordem da esquerda para a direita. Mecanica: `mascara_de_texto`
importada de `l2scanner.identidade` (nao reimplementada — ela ja carrega a razao medida de ser so
brilho), `mascara.any(axis=0)`, e uma passada que agrupa colunas contiguas. **Qualquer coluna
vazia separa** — sem tolerancia de lacuna, porque a menor lacuna real medida entre glifos vizinhos
e de uma coluna. A docstring registra: 8 de 8 celulas de preco reais devolveram exatamente o
numero de glifos do rotulo; glifos de 11 px de altura, 4 px de largura com o `4` em 6 px e a
virgula em 1 px. Recorte vazio devolve lista vazia, e nao levanta.

**(c) A matriz dos glifos, ao LADO da matriz dos nomes.** Nova constante
`COLISAO_MAXIMA_ENTRE_GLIFOS = 0.85`, com a docstring trazendo a tabela das quatro
representacoes medidas (cinza 0.8648, mascara 0.7831, cinza 4x 0.8624, mascara 4x 0.7451) e
dizendo em uma frase por que ela nao pode ser a constante dos nomes: sobre cinza, o conjunto
CORRETO mede 0.8648 e seria recusado. Nova funcao `matriz_de_confusao_de_glifos(moldes)`
devolvendo o mesmo `ResultadoDaConfusao` ja existente — reuso do dataclass, incluindo `.rodou` e
a licao do CR-03 de nao afirmar score nem limiar quando nao houve par. Duas diferencas em relacao
a `matriz_de_confusao`, ambas obrigatorias e ambas comentadas no fonte: os moldes sao comparados
na **mascara binaria** (a representacao que dobra a margem), e o alinhamento **preenche ate a
maior caixa comum com zeros** em vez de cortar ao menor — porque cortar ao menor com a virgula de
1 px no conjunto reduz todo par a uma coluna, o guard de desvio de `casamento_da_ancora` devolve
0.0, e a ferramenta imprimiria "sem colisao" sobre uma comparacao que nao aconteceu. Isso e o
mesmo defeito do CR-03 com outra roupa. `matriz_de_confusao`, `_alinhar` e
`COLISAO_MAXIMA_ENTRE_TEMPLATES` ficam **intocados**.

**(d) `glifos_para_calibracao` / `glifos_de_calibracao` em `l2scanner/mercado_visao.py`**, espelhando
linha a linha o par das ancoras. O formato de cada item e `{"glifo": <rotulo>, "molde": <dict de
molde_para_hex>}`, paralelo ao `{"nome": ..., "molde": ...}` dos templates de nome. O molde
gravado e a **mascara binaria escalada para 0/255** — `molde_para_hex` aceita array 2-D direto
(`_em_tons_de_cinza` deixa passar), e a correlacao normalizada e invariante a essa escala, entao
gravar em 0/255 mantem o arquivo legivel por qualquer visualizador sem mudar um decimal do
casamento. Na volta, `glifos_de_calibracao` **passa `forma_esperada` a `molde_de_hex`** a partir
da altura e largura declaradas ao lado de cada molde: e o guard do WR-04 que o WR-01 apontou como
sem chamador, e este e o segundo chamador de producao a usa-lo. Entrada malformada (item sem
rotulo, molde ausente, bytes de tamanho incompativel) levanta erro explicado que manda recalibrar,
no tom das mensagens vizinhas.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_glifos.py tests/test_calibrar_mercado.py tests/test_mercado_multiancora.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_glifos.py -q` passa, com todos os comportamentos de `<behavior>` presentes como testes nomeados.
    - `tests/fixtures/mercado/glifos_precos_f010.png` existe com forma `(240, 45, 3)` e `tests/fixtures/mercado/glifos_unitario_f010.png` com forma `(30, 40, 3)`; a soma dos dois fica abaixo de 8 KB.
    - Fatiando `glifos_precos_f010.png` em faixas de 45 px, `segmentar_glifos` devolve, na ordem, 6, 4, 5, 4, 5, 4 runs.
    - Sobre `glifos_unitario_f010.png`, `segmentar_glifos` devolve 4 runs, e o segundo deles tem largura 1 (a virgula).
    - `matriz_de_confusao_de_glifos` sobre os 11 glifos reais APROVA, `resultado.rodou` e verdadeiro, `resultado.pior_score` fica entre 0.77 e 0.80, e `resultado.par_colidente` nomeia `0` e `8` em alguma ordem.
    - Trocando a representacao para tons de cinza nativo, o pior par sobe acima de 0.86 — o teste que documenta por que a mascara foi escolhida. (Pode ser uma unica assercao dentro do teste da matriz; o valor precisa estar no fonte, nao so no plano.)
    - Um par (glifo largo, virgula de 1 px) aparece na matriz com score maior que 0.0.
    - `COLISAO_MAXIMA_ENTRE_TEMPLATES`, `_alinhar` e `matriz_de_confusao` seguem byte a byte iguais ao que estavam, e `tests/test_calibrar_mercado.py::TestAMatrizDeConfusao` continua verde inteiro.
    - `glifos_para_calibracao` seguido de `glifos_de_calibracao` devolve arrays identicos aos originais para os 11 glifos; um molde com altura e largura trocadas e recusado com mensagem citando recalibrar.
    - `l2scanner/mercado_visao.py` passa `forma_esperada` a `molde_de_hex` tambem no caminho dos glifos.
    - `tests/test_calibrar_mercado.py::TestOsDoisTripwiresDeEscritaDeImagem` inteiro continua verde.
    - `python -m pytest tests/ -q` sem regressao contra a linha de base de 1558 passed, 2 skipped. (Aborto por `KeyboardInterrupt` vazado de `tests/test_agenda.py` e flake conhecido e anterior a esta fase: rode de novo, nao e falha.)
  </acceptance_criteria>
  <done>A tecnica de corte de glifos existe, esta medida contra pixels reais commitados, e o formato em disco tem ida e volta com guard de forma — sem que uma janela tenha aberto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o modo de corte no fluxo interativo, a persistencia que FUNDE em vez de apagar, e a correcao do SUMMARY (D-06, D-07, FUND-03)</name>
  <precondition>A Task 1 esta commitada: `segmentar_glifos`, `matriz_de_confusao_de_glifos` e o par `glifos_para_calibracao`/`glifos_de_calibracao` existem e estao verdes.</precondition>
  <reversibility rating="costly">`mercado_templates_de_digito` e `mercado_limiar_de_glifo` viram formato em disco que a Fase 2 (LEIT-02) vai ler. Nao e porta de mao unica — a ferramenta regrava o arquivo inteiro e refazer custa uma recalibragem —, mas renomear a chave depois obriga toda instalacao a recortar glifos de novo.</reversibility>
  <files>l2scanner/calibrar_mercado.py, l2scanner/calibracao.py, calibrar-mercado.bat, tests/test_calibrar_mercado.py, .planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-04-SUMMARY.md</files>
  <read_first>
    - `l2scanner/calibrar_mercado.py` linhas 735-900 — a funcao `calibrar` inteira: onde a watchlist e lida ANTES das janelas (a licao do WR-06), a ordem dos cinco arrastos, a guarda `if moldes_de_nome:` que preserva o trabalho anterior (CR-04), a escrita unica de `cal.salvar` com o `OSError` traduzido, e o resumo final impresso
    - `l2scanner/calibrar_mercado.py` linhas 680-735 — `_marcar` (que traduz cancelamento em recusa explicada) e `_texto_final_da_conferencia`, com a honestidade extra de admitir que o arquivo JA foi gravado quando a conferencia falha
    - `l2scanner/calibrar_mercado.py` linhas 634-680 — `desenhar_conferencia` e `montar_ancoras`: o estilo de desenho sobre copia e a recusa por recorte vazio
    - `l2scanner/calibrar_mercado.py` linhas 554-590 e 425-543 — `escolher_frame` e `navegar_e_escolher`: como `--frame`, `--gravacao` e `--indice` ja se combinam, e as teclas MEDIDAS do navegador (WR-03)
    - `l2scanner/calibrar_mercado.py` linhas 901-940 — `main`, o parser e o aviso de DPI (WR-07). A flag nova entra aqui
    - `l2scanner/calibrar.py` — `_gravar_conferencia` inteira: ela grava num nome FIXO, cai para um nome alternativo quando o arquivo esta travado, e devolve `None` quando nao gravou nada. E o motivo de a rodada ter de chamar essa funcao UMA vez so
    - `l2scanner/calibracao.py` linhas 282-300 — a docstring ja commitada de `mercado_templates_de_digito`, que define o conteudo da chave: os dez digitos e a virgula, mais as palavras `XM Coin` e `Adena`, com a razao (e o sufixo que desambigua a convencao da virgula, nao o numero)
    - `l2scanner/calibracao.py` linhas 396-410 e 486-500 — serializacao e desserializacao das chaves de mercado, e o `.get` do D-07
    - `l2scanner/calibracao.py` linhas 590-630 — `_conferir_as_chaves_de_mercado`: a validacao de `mercado_limiar_de_template` (numero, faixa `(0, 1]`, com a razao escrita) e o laco de forma das tres listas. A chave nova entra nos dois lugares
    - `tests/test_calibrar_mercado.py` linhas 356-400 — `TestCarregarMutarRegravar`, incluindo `test_mutar_so_o_mercado_preserva_todos_os_demais_campos` e `test_a_VERSAO_DO_ESQUEMA_nao_sobe`
    - `.planning/workstreams/mercado/phases/01-.../01-VERIFICATION.md` — a lacuna G-01 inteira, incluindo o item 3 de `missing` (a linha do SUMMARY a corrigir) e o paragrafo "Agravante de auditoria"
    - `.planning/workstreams/mercado/phases/01-.../01-04-SUMMARY.md` linha 17 — o texto exato a corrigir
    - O bloco `<evidencia_medida>` secao 3 deste plano — por que a flag de corte isolado existe
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_calibrar_mercado.py` (o arquivo dos testes de fluxo), com
    `_selecionar_regiao` e a leitura do teclado monkeypatchados — sem mouse e sem janela:

    - Conferencia de contagem: marcando um retangulo cujo recorte segmenta em 5 glifos e digitando
      um rotulo de 5 caracteres, os cinco glifos entram rotulados na ordem. Digitando um rotulo de
      4 caracteres para o mesmo recorte, a marcacao e RECUSADA com mensagem que cita os dois
      numeros (quantos glifos foram vistos, quantos caracteres foram digitados) e o usuario e
      convidado a marcar de novo — nada entra no conjunto.
    - Rotulo invalido: rotulo com letra, espaco ou vazio e recusado dizendo quais caracteres sao
      aceitos, sem consumir a marcacao.
    - Acumulacao: dois numeros marcados em sequencia somam seus glifos; um glifo que aparece nos
      dois e gravado uma vez so.
    - Cobertura anunciada: com um conjunto incompleto, a ferramenta imprime a lista NOMEADA do que
      faltou (por exemplo o `8`), e essa lista aparece tambem no resumo final, depois da linha de
      calibracao gravada — nao so no meio da sessao, onde rola para fora da tela.
    - Fusao, nao substituicao: partindo de uma calibracao que ja tem glifos gravados, uma rodada
      que corta um subconjunto PRESERVA os ausentes e substitui os recortados. Uma rodada que
      corta ZERO glifos nao apaga nada e diz que manteve os anteriores — o mesmo comportamento e a
      mesma mensagem do caminho dos moldes de nome (CR-04).
    - Recusa por colisao: quando a matriz dos glifos reprova, NADA e gravado e a mensagem nomeia o
      par colidente — mesmo contrato da watchlist.
    - Limiar: `mercado_limiar_de_glifo` so e escrito quando a matriz o derivou; com menos de dois
      glifos ele nao e inventado (a licao do CR-03 aplicada a chave nova).
    - Carregar-mutar-regravar: a rodada de glifos preserva todo campo nao relacionado a mercado, e
      `VERSAO_DO_ESQUEMA` segue 2.
    - Validacao de entrada: `mercado_limiar_de_glifo` com tipo errado, ou fora de `(0, 1]`, faz o
      carregamento recusar com mensagem que manda recalibrar — espelho do teste ja existente para
      `mercado_limiar_de_template`.
    - Modo isolado: com a flag de corte isolado, nenhuma selecao de ancora ou de grade e pedida
      (contagem de chamadas ao seletor prova isso), e as chaves de ancora e de grade saem do
      arquivo identicas as que entraram.
    - Conferencia visual unica: a rodada chama a gravacao de conferencia exatamente UMA vez, e a
      imagem entregue a ela contem a montagem dos glifos (altura maior que a do frame no fluxo
      completo; so a montagem no modo isolado).
    - Os dois tripwires estruturais de escrita de imagem seguem verdes.
  </behavior>
  <action>
Cinco entregas.

**(a) `GLIFOS_EXIGIDOS` — o conjunto que a Fase 2 vai precisar, escrito uma vez.** Os dez digitos
`0`-`9`, a virgula, e as duas palavras inteiras `XM Coin` e `Adena`. Essa lista nao e invencao
deste plano: e literalmente o conteudo que a docstring ja commitada de
`Calibracao.mercado_templates_de_digito` declara, e e a resposta validada da secao 2 do
`SPIKE-RESPOSTAS.md` — as palavras entram porque a virgula e separador de milhar E de decimal na
mesma linha, e quem desambigua e o sufixo, nao o numero. A constante vive perto de
`LINHAS_ESPERADAS`, com a mesma disciplina: o que foi medido em campo, com a citacao da secao.

**(b) O laco de corte, no mesmo trilho dos moldes de nome.** Uma funcao proxima de
`def cortar_glifos(pixels: np.ndarray, ja_gravados: dict[str, np.ndarray]) -> dict[str, np.ndarray]:`,
chamada de dentro de `calibrar` depois da grade. A cada volta: uma chamada a `_marcar` pedindo
**UM numero inteiro da coluna de preco, sem o sufixo de moeda e sem o icone**; `segmentar_glifos`
sobre o recorte; a ferramenta imprime quantos glifos viu e pede que o usuario **digite o numero
como ele o le na tela, com a virgula**; se o comprimento do que foi digitado nao bater com a
contagem de glifos, a marcacao e descartada com as duas contagens na tela e o laco repete. Esta
conferencia e o coracao da task: ela transforma um erro de marcacao — o retangulo que cortou meio
digito, ou que pegou o `X` de `XM Coin` — em recusa imediata, em vez de num molde errado gravado
com a mesma confianca de um certo. Aceite apenas digitos e virgula no rotulo. As duas palavras de
sufixo sao marcadas a parte, uma cada, como molde inteiro sem segmentacao — a ferramenta pergunta
qual das duas esta sendo marcada em vez de deduzir. Entre uma marcacao e a proxima, imprima a
cobertura corrente: quais de `GLIFOS_EXIGIDOS` ja existem e quais faltam, nomeados. O laco
termina quando o usuario disser que terminou; terminar com o conjunto incompleto e PERMITIDO e
esperado (o frame que calibra a grade nao tem os dez digitos — ver `<evidencia_medida>` 3), mas
sai com um bloco alto listando os que faltam e dizendo que a leitura da Fase 2 vai descartar toda
linha que contenha um glifo nao gravado.

**(c) Persistencia que funde.** Depois do laco, rodar `matriz_de_confusao_de_glifos` sobre o
conjunto FUNDIDO (os ja gravados mais os recortados agora), imprimir a matriz ordenada e o
veredito no mesmo formato ja usado para os nomes, e recusar tudo — sem gravar nada — quando ela
reprova. Aprovando: `cal.mercado_templates_de_digito = glifos_para_calibracao(fundido)`, e
`cal.mercado_limiar_de_glifo` so quando a matriz derivou um limiar. A fusao e por rotulo, o corte
desta rodada vence, e o glifo ausente desta rodada e PRESERVADO — a mesma guarda que o CR-04
instalou para os moldes de nome, pela mesma razao: cada molde custou um arrasto de mouse sobre um
frame gravado, e este e o unico caminho do projeto capaz de apagar trabalho de calibracao sem
perguntar. Quando nada foi recortado e ja havia glifos, diga quantos foram mantidos, no molde
exato da mensagem vizinha.

**(d) A chave do limiar e a flag de corte isolado.** Em `l2scanner/calibracao.py`, acrescentar
`mercado_limiar_de_glifo: float | None = None` no trilho `.get` (D-07), serializado e
desserializado ao lado das demais chaves de mercado, com validacao em
`_conferir_as_chaves_de_mercado` espelhando a de `mercado_limiar_de_template` — numero, faixa
`(0, 1]`, e a razao escrita na mensagem: um limiar frouxo faz todo recorte casar com todo glifo, e
um digito lido errado corrompe o preco calado. **`VERSAO_DO_ESQUEMA` fica em 2.** Chave separada da
de nome de proposito: sao dois conjuntos fechados diferentes, com matrizes diferentes e piores
pares diferentes; um numero servindo aos dois seria derivado de uma medicao e aplicado a outra.
Em `main`, uma flag do tipo `--so-digitos` que pula ancoras, grade e watchlist e roda so o laco de
glifos sobre o frame apontado por `--frame` / `--gravacao`. Ela existe por um fato medido, nao por
conveniencia: o frame que calibrou a grade atual nao contem o digito `8`, e sem esta flag o
usuario teria de refazer cinco arrastos por frame visitado. `conferir_o_frame` continua valendo no
modo isolado — o tamanho da fonte depende do tamanho da janela. Atualizar o cabecalho `REM` de
`calibrar-mercado.bat` e o bloco de instrucao final para citar as duas invocacoes; o `.bat` ja
repassa `%*`, entao a flag chega sem mudanca de mecanica.

**(e) Corrigir o `01-04-SUMMARY.md`.** A linha que lista "5 chaves opcionais novas em
calibration.json" conta `mercado_templates_de_digito` entre as entregues, e a
`01-VERIFICATION.md` registra isso como agravante de auditoria — um leitor do SUMMARY conclui que
a chave e produzida. Corrija a linha para distinguir chave DECLARADA de chave PRODUZIDA, nomeando
qual estava sem produtor, e acrescente uma linha na secao "O que NAO ficou, e por que" apontando
para este plano. Nao reescreva o SUMMARY: e registro historico, e a correcao e cirurgica.

**Uma unica conferencia visual por rodada.** `_gravar_conferencia` grava num nome FIXO — chamar
duas vezes na mesma rodada apaga a primeira imagem e o usuario conferiria metade da calibracao
achando que conferiu tudo. Monte, portanto, UMA tela: no fluxo completo, o frame com os
retangulos verdes de sempre e, abaixo dele, a montagem dos glifos (cada um ampliado com o seu
rotulo desenhado ao lado, para o olho conferir que o `8` e mesmo um `8`); no modo isolado, so a
montagem. Uma chamada, no fim, e o `_texto_final_da_conferencia` ja existente continua sendo quem
diz a verdade sobre o que foi gravado. Nenhuma escrita de imagem propria entra neste modulo — a
gravacao continua sendo a funcao importada de `l2scanner.calibrar`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrar_mercado.py tests/test_mercado_glifos.py tests/test_calibracao_mercado.py tests/test_conferencia_gravada.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_calibrar_mercado.py -q` passa, com todos os comportamentos de `<behavior>` presentes como testes nomeados.
    - Rotulo com comprimento diferente da contagem de glifos: a marcacao e descartada, a mensagem traz os DOIS numeros, e o conjunto acumulado nao muda de tamanho.
    - Uma rodada que corta um subconjunto sobre uma calibracao que ja tinha glifos deixa o total maior ou igual ao anterior, nunca menor; uma rodada que corta zero glifos deixa a chave byte a byte identica.
    - Com o conjunto incompleto, a saida final contem a lista nomeada dos glifos faltantes de `GLIFOS_EXIGIDOS`.
    - Com a flag de corte isolado, o seletor de regiao e chamado apenas para glifos: nenhuma chamada de ancora ou de grade, e `mercado_ancoras` e `mercado_grade` saem do arquivo identicos aos que entraram.
    - A gravacao de conferencia e chamada exatamente uma vez por rodada (contagem afirmada no teste), e a imagem passada a ela contem a montagem rotulada dos glifos.
    - `l2scanner/calibracao.py` declara `VERSAO_DO_ESQUEMA = 2`, e `tests/fixtures/calibracao_de_referencia.json` continua carregando sem excecao.
    - `mercado_limiar_de_glifo` com tipo errado ou fora de `(0, 1]` faz `Calibracao.carregar` recusar com mensagem que manda recalibrar.
    - `tests/test_calibrar_mercado.py::TestOsDoisTripwiresDeEscritaDeImagem` e `tests/test_conferencia_gravada.py::test_existe_um_unico_ponto_de_escrita_no_modulo` seguem verdes.
    - `git diff --stat` NAO mostra `l2scanner/rastreador.py`, `l2scanner/visao.py` nem `l2scanner/sessao.py`.
    - `01-04-SUMMARY.md` nao afirma mais que `mercado_templates_de_digito` foi entregue, e aponta para o `01-05`.
    - `python -m pytest tests/ -q` sem regressao contra 1558 passed, 2 skipped.
  </acceptance_criteria>
  <done>A ferramenta corta, confere, funde e grava os glifos; `mercado_templates_de_digito` tem produtor; e o SUMMARY parou de contar como entregue algo que nao era.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: PORTAO — a primeira mao humana a desenhar os retangulos, e os glifos gravados de verdade (criterio 3 do ROADMAP, D-06)</name>
  <precondition>Tasks 1 e 2 commitadas e a suite verde. As gravacoes `20260828-115700-calibragem` e `20260828-060622-mercado-pagina-cheia` existem em `recordings/` no checkout principal.</precondition>
  <files>calibration.json (maquina do usuario — gitignored, nunca commitado)</files>
  <read_first>
    - `.planning/workstreams/mercado/phases/01-.../01-VERIFICATION.md` secao "Verificacao Humana Necessaria", item 1 — por que este portao sobrevive a lacuna: o criterio 3 comeca com "O usuario roda a ferramenta", e ele nunca rodou ate o fim. Tres tentativas abortaram num defeito real (o ENTER vazando, resolvido em `03b3e26`), o CR-01 (janela de selecao em escala errada, que corromperia qualquer retangulo desenhado a mao) foi corrigido e coberto por teste mas NUNCA exercitado por uma mao humana, e as regioes atuais foram medidas por um agente
    - `l2scanner/calibrar_mercado.py` — o bloco de instrucoes impresso antes das janelas (as cinco janelas, o aviso de que fechar no X cancela) e as teclas do navegador de frames
    - O bloco `<evidencia_medida>` secoes 3 e 4 deste plano — qual frame tem quais digitos
  </read_first>
  <action>Rodar a calibracao de mercado de ponta a ponta com o mouse, primeiro a grade e depois os glifos, e conferir a imagem</action>
  <instructions>
    Este portao existe por duas razoes, e a segunda e a que sobrevive a todo o resto.

    A primeira: os templates de digito nunca existiram. Agora existe ferramenta para corta-los, e
    e voce que corta.

    A segunda: **nenhuma mao humana jamais completou este fluxo.** O criterio 3 do ROADMAP e
    literalmente sobre voce — *"O usuario roda a ferramenta de calibracao sobre um frame gravado e
    ve as regioes da janela, a ancora do painel e os templates de digito persistidos em
    calibration.json — sem editar JSON a mao."* Suas tres tentativas anteriores abortaram num
    defeito real (o ENTER do navegador vazando para a selecao seguinte), e depois voce delegou; as
    regioes que estao la hoje foram MEDIDAS por um agente, nunca desenhadas. O defeito da janela
    de selecao em escala errada foi corrigido e tem teste — mas "corrigido e testado" nao e a
    mesma afirmacao que "um humano arrastou um retangulo e funcionou".

    Ja fiz tudo que era automatizavel: a segmentacao esta medida contra pixels reais do seu jogo
    (8 celulas de preco, 8 acertos), a matriz de confusao dos glifos roda sozinha e recusa
    colisao nomeando o par, e a conferencia de contagem recusa a marcacao errada antes de ela
    virar molde.

    **Passo 1 — a grade (o fluxo completo).**
    Rode `.\calibrar-mercado.bat --gravacao recordings\20260828-115700-calibragem --layout adena`.
    Marque as tres ancoras, a area da lista e a primeira linha. Ao chegar nos glifos, marque os
    numeros da coluna de preco que aparecerem — sem o `XM Coin` ao lado, sem o icone da moeda — e
    digite cada numero como voce o le, com a virgula. **Aviso util:** este frame nao tem o digito
    `8` em lugar nenhum (conferi, e as 24 capturas dessa gravacao sao a mesma tela). A ferramenta
    vai terminar dizendo que faltam glifos. Isso e esperado; o passo 2 resolve.

    **Passo 2 — os glifos que faltaram.**
    Rode `.\calibrar-mercado.bat --so-digitos --frame recordings\20260828-060622-mercado-pagina-cheia\frame_000010.png`.
    Este frame tem os dez digitos e a virgula: `18,90` e `18,00` na coluna `Total` trazem o `8`, e
    o `6,00` da coluna `Unit price` (a da direita) traz o `6`. Marque numeros ate a ferramenta
    dizer que o conjunto esta completo. Marque tambem, quando ela pedir, o `XM Coin` e o `Adena`
    como palavras inteiras. Prefira linhas de texto BRANCO — a linha em ciano tambem funciona, mas
    a branca e o caso comum.

    **Passo 3 — conferir com o olho.**
    Abra a imagem de conferencia cujo caminho foi impresso. Na montagem de glifos, cada recorte
    aparece ampliado com o rotulo ao lado: confira que o que esta escrito `8` e mesmo um `8`, e
    que nenhum recorte cortou meio digito ou pegou um pedaco do vizinho. Se a matriz recusar, ela
    vai nomear o par — remarque os dois com mais folga vertical.

    Voce nao vai editar `calibration.json` em momento nenhum. Se em algum ponto a ferramenta pedir
    isso, e defeito, e eu quero saber.
  </instructions>
  <verification>
    Depois do seu "calibrado", eu confiro automaticamente e sem tocar no arquivo:
    `calibration.json` carrega sem excecao; `VERSAO_DO_ESQUEMA` continua 2;
    `mercado_templates_de_digito` deixou de ser nulo e traz pelo menos os dez digitos e a virgula,
    com rotulo unico por entrada; cada molde decodifica por `glifos_de_calibracao` com a forma
    declarada batendo com o tamanho real dos bytes; `mercado_limiar_de_glifo` esta presente,
    dentro de `(0, 1]`, e foi derivado de uma matriz que RODOU (nao do `0.5` sem significado);
    `mercado_ancoras` e `mercado_grade` continuam identicos aos de antes; e todo campo nao
    relacionado a mercado esta inalterado. Registro tambem, no SUMMARY, o pior par inter-classe
    que a SUA matriz mediu, ao lado do 0.7831 que eu medi no frame de referencia — se os dois
    numeros divergirem muito, e sinal de marcacao frouxa e eu digo.
  </verification>
  <acceptance_criteria>
    - O usuario viu a mensagem "Calibracao de mercado gravada em calibration.json" ao fim das DUAS invocacoes, com os retangulos desenhados por ele.
    - `mercado_templates_de_digito` contem no minimo 11 entradas de rotulo unico, cobrindo `0`-`9` e a virgula; as duas palavras de sufixo entram quando o usuario as marcou.
    - Cada molde da chave decodifica por `glifos_de_calibracao` sem excecao, com `forma_esperada` conferida.
    - `mercado_limiar_de_glifo` esta presente e em `(0, 1]`, e o pior par inter-classe medido na maquina do usuario esta registrado no SUMMARY com o par nomeado.
    - `mercado_ancoras`, `mercado_grade` e todos os campos nao relacionados a mercado saem identicos aos de antes do passo 2.
    - `calibration.json` NAO aparece em `git status` como arquivo rastreado — segue gitignored, e nenhum valor especifico da maquina do usuario entra em teste.
    - O usuario nao editou o JSON a mao em nenhum momento.
  </acceptance_criteria>
  <resume-signal>Digite "calibrado" quando a imagem de conferencia estiver correta e a ferramenta tiver dito que o conjunto de glifos esta completo — ou descreva o problema (por exemplo: "a matriz recusou 0 contra 8", "a contagem nunca bate no 100,00", "a janela de selecao abriu minuscula", "o ENTER pulou uma selecao").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` (`mercado_templates_de_digito`) -> processo do scanner | Moldes em hex sao decodificados e reformatados a partir de dimensoes DECLARADAS no proprio arquivo, que o usuario pode editar. |
| Rotulo digitado pelo usuario -> molde gravado | O que o humano digita vira a IDENTIDADE do glifo. Um erro de digitacao aqui vira um `8` gravado como `0`, e nenhuma camada posterior o descobre. |
| Frame gravado -> conjunto fechado de glifos | Pixels de uma sessao real viram o vocabulario com que todo preco futuro sera lido. |
| `recordings/` (gitignored, local) -> `tests/fixtures/` (versionado) | Recorte mal escolhido leva nome de personagem ou linha de chat para o repositorio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | colisao entre glifos (`0` lido como `8`) | high | mitigate | Task 1: matriz de confusao dos glifos MEDIDA (pior par real 0.7831 na mascara binaria, `0` x `8`), constante propria `COLISAO_MAXIMA_ENTRE_GLIFOS`, recusa alta nomeando o par. A representacao foi escolhida por medicao, nao por gosto: em tons de cinza o mesmo conjunto mede 0.8648 e seria recusado |
| T-05-02 | Tampering | `molde_de_hex` sobre `mercado_templates_de_digito` | medium | mitigate | Task 1: `glifos_de_calibracao` passa `forma_esperada` — o guard do WR-04 que o WR-01 apontou como sem chamador de producao. Dimensao declarada conferida contra o tamanho real dos bytes antes do reformat; item malformado levanta erro que manda recalibrar |
| T-05-03 | Repudiation | conjunto de glifos incompleto ou mal rotulado gravado calado | high | mitigate | Task 2: conferencia de contagem (glifos segmentados contra caracteres digitados) recusa a marcacao antes de virar molde; cobertura de `GLIFOS_EXIGIDOS` impressa a cada volta e repetida no resumo final com os faltantes nomeados; montagem de conferencia visual com cada glifo ampliado ao lado do seu rotulo; Task 3 poe um olho humano nessa montagem |
| T-05-04 | Denial of Service | rodada nova apagando glifos ja calibrados | medium | mitigate | Task 2: fusao por rotulo, nunca substituicao; lista vazia jamais sobrescreve lista nao-vazia; mensagem dizendo quantos foram mantidos. Precedente direto do CR-04, que consertou exatamente este defeito no caminho dos moldes de nome |
| T-05-05 | Information Disclosure | `tests/fixtures/mercado/` versionado | low | mitigate | Dois recortes, 240x45 e 30x40, somando menos de 8 KB, contendo apenas digitos de preco — sem nome de personagem, sem chat, sem nome de item. Conferido antes do commit e listado arquivo a arquivo no SUMMARY |
| T-05-06 | Tampering | escrita do `calibration.json` no fim da rodada | low | accept | A escrita atomica ja foi instalada pelo WR-12 e nao muda aqui; a ferramenta segue regravando o arquivo inteiro a partir do carregado, no caminho ja testado por `TestCarregarMutarRegravar` |
| T-05-SC | Tampering | instalacoes npm/pip/cargo | high | mitigate | **Zero instalacoes neste plano** — `requirements.txt` nao muda; toda a tecnica sai de `cv2` e `numpy` ja presentes. FIRE-01 e o portao permanente e fica vermelho com qualquer biblioteca de sintese de input |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plano 05)

**Simbolos novos em `l2scanner/calibrar_mercado.py`**
- `segmentar_glifos(recorte) -> list[tuple[int, int]]` — projecao de coluna sobre a mascara de brilho; qualquer coluna vazia separa
- `COLISAO_MAXIMA_ENTRE_GLIFOS` — constante de politica com a tabela das quatro representacoes medidas na docstring
- `matriz_de_confusao_de_glifos(moldes) -> ResultadoDaConfusao` — mascara binaria, alinhamento por preenchimento ate a maior caixa
- `GLIFOS_EXIGIDOS` — os dez digitos, a virgula e as duas palavras de sufixo, com a citacao da secao 2 do `SPIKE-RESPOSTAS.md`
- `cortar_glifos(pixels, ja_gravados) -> dict[str, np.ndarray]` — o laco interativo com conferencia de contagem
- a montagem rotulada dos glifos para a imagem de conferencia (uma unica chamada de gravacao por rodada)
- a flag `--so-digitos` no parser de `main`

**Simbolos novos em `l2scanner/mercado_visao.py`**
- `glifos_para_calibracao(moldes) -> list[dict]` — itens `{"glifo", "molde"}`, molde = mascara binaria em 0/255 via `molde_para_hex`
- `glifos_de_calibracao(dados) -> dict[str, np.ndarray]` — volta passando `forma_esperada` a `molde_de_hex`

**Simbolos novos em `l2scanner/calibracao.py`**
- `Calibracao.mercado_limiar_de_glifo: float | None` — chave opcional via `.get`, serializada, desserializada e validada em `_conferir_as_chaves_de_mercado`. `VERSAO_DO_ESQUEMA` segue 2

**Arquivos novos**
- `tests/test_mercado_glifos.py` — a metade pura, afirmada contra pixels reais commitados
- `tests/fixtures/mercado/glifos_precos_f010.png` — 240x45 px, seis precos da coluna `Total`
- `tests/fixtures/mercado/glifos_unitario_f010.png` — 30x40 px, o `6,00` da coluna `Unit price`

**Arquivos modificados**
- `tests/test_calibrar_mercado.py` — testes do laco interativo, da fusao e do modo isolado
- `calibrar-mercado.bat` — cabecalho e instrucao final citando as duas invocacoes
- `01-04-SUMMARY.md` — correcao da linha que contava `mercado_templates_de_digito` como entregue

**Portao humano**
- `checkpoint:human-action` `gate="blocking-human"` — a primeira mao humana no fluxo, fechando a metade do criterio 3 que grep nao alcanca

**Nao produz:** nenhum molde de NOME (adiado para a Fase 2 pela VERIFICATION), nenhum leitor de preco (LEIT-02 e Fase 2), nenhuma dependencia nova, nenhuma alteracao em `rastreador.py`, `visao.py` ou `sessao.py`.
</artifacts_this_phase_produces>

<verification>
1. `python -m pytest tests/ -q` sem regressao contra a linha de base **1558 passed, 2 skipped**. Aborto por `KeyboardInterrupt` vazado de `tests/test_agenda.py` e flake conhecido e anterior a esta fase (5 em 60 execucoes): aborto nao e falha, rode de novo.
2. `python -m pytest tests/test_mercado_glifos.py tests/test_calibrar_mercado.py -q` verde, com a segmentacao afirmada contra as duas fixtures resgatadas.
3. Os dois tripwires estruturais de escrita de imagem seguem verdes — o do modulo de mercado e o de `l2scanner.calibrar`.
4. `git diff --stat` do plano nao toca `l2scanner/rastreador.py`, `l2scanner/visao.py` nem `l2scanner/sessao.py`, e `grep -in mercado l2scanner/rastreador.py` continua devolvendo zero ocorrencias.
5. `l2scanner/calibracao.py` declara `VERSAO_DO_ESQUEMA = 2`, e `tests/fixtures/calibracao_de_referencia.json` carrega sem excecao.
6. Nenhum PNG de `recordings/` commitado alem dos dois recortes nomeados; `calibration.json` segue gitignored.
7. O portao humano da Task 3 foi cumprido, e a conferencia automatica pos-checkpoint passou item a item.
</verification>

<success_criteria>
- **Criterio 3 do ROADMAP completo pelas TRES coisas que ele enumera**: o usuario roda a ferramenta sobre um frame gravado e ve as regioes da janela, a ancora do painel **e os templates de digito** persistidos em `calibration.json`, sem editar JSON a mao.
- **FUND-03 desbloqueado**: `mercado_templates_de_digito` deixa de ser um campo de passagem sem produtor e passa a ser produzido pela propria ferramenta, com conferencia visual e matriz de confusao medida — a metade de D-06 que a VERIFICATION marcou como nao honrada.
- A Fase 2 (LEIT-02) encontra os moldes de que precisa, com um limiar derivado de uma matriz que rodou de verdade, em vez de um numero fabricado.
- Nenhuma proibicao da fase foi violada, e o `01-04-SUMMARY.md` parou de afirmar como entregue o que nao estava.
</success_criteria>

<output>
Criar `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-05-SUMMARY.md` ao terminar, incluindo:
- a tabela final da matriz de confusao dos glifos MEDIDA na maquina do usuario (pior par nomeado e score), ao lado do 0.7831 medido no frame de referencia durante o planejamento;
- a lista completa dos glifos gravados e, se houver, dos que ficaram faltando;
- os dois arquivos resgatados para `tests/fixtures/mercado/`, com tamanho;
- o que o usuario relatou no portao humano — em especial qualquer atrito com o mouse, a escala da janela de selecao ou as teclas do navegador, porque este foi o primeiro fluxo completo por mao humana e o relato dele e evidencia nova, nao confirmacao.
</output>
