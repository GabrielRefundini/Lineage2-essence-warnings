---
phase: 01-funda-o-firewall-gravador-e-spike-de-campo
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md
  - l2scanner/calibrar_mercado.py
  - l2scanner/calibracao.py
  - l2scanner/mercado_visao.py
  - l2scanner/visao.py
  - l2scanner/__main__.py
  - calibrar-mercado.bat
  - tests/test_calibrar_mercado.py
  - tests/test_mercado_27x.py
  - tests/fixtures/mercado/
autonomous: false
requirements: [FUND-02, FUND-03, DETC-01]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As perguntas de campo estao respondidas POR ESCRITO a partir das gravacoes: linhas por pagina, separador de milhar, moeda, colunas, preco total vs unitario, onde fica o preco medio embutido — cada resposta citando o frame que a sustenta"
    - "A decisao sobre variantes de encanto (+3 vs +4) na watchlist esta registrada e validada contra frame real: o encanto renderiza como prefixo +N no nome e o template inclui o prefixo no recorte"
    - "O usuario roda a ferramenta de calibracao sobre um frame GRAVADO e ve as regioes da janela, a ancora do painel e os templates de digito persistidos em calibration.json — sem editar JSON a mao"
    - "A calibracao recusa ALTO quando dois templates da watchlist colidem: a matriz de confusao e medida na hora e o pior score inter-classe fica registrado"
    - "No replay da gravacao do incidente 27x o painel de mercado e reconhecido como aberto E zero alertas de morte sao disparados — as duas metades presas pelo mesmo teste"
    - "O sinal 'mercado aberto' entra no caminho da party como extra OPCIONAL e campo exibicional; nenhuma decisao de alerta sai dele"
  artifacts:
    - path: ".planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md"
      provides: "as 7 perguntas de campo respondidas com citacao de frame"
      min_lines: 60
    - path: "l2scanner/calibrar_mercado.py"
      provides: "ferramenta de calibracao de mercado: regioes, ancora, templates de nome e digito, matriz de confusao, conferencia visual"
      contains: "selectROI"
    - path: "calibrar-mercado.bat"
      provides: "lancador no precedente de calibrar-solo.bat"
      contains: "l2scanner.calibrar_mercado"
    - path: "tests/test_mercado_27x.py"
      provides: "regressao do incidente 27x afirmando as DUAS metades"
      min_lines: 70
    - path: "tests/test_calibrar_mercado.py"
      provides: "tripwire estrutural do modulo novo + testes da matriz de confusao"
      contains: "imwrite"
    - path: "l2scanner/visao.py"
      provides: "campo exibicional mercado_aberto_aparente em Observacao"
      contains: "mercado_aberto_aparente"
  key_links:
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/calibrar.py"
      via: "import de _gravar_conferencia — jamais um imwrite proprio"
      pattern: "_gravar_conferencia"
    - from: "l2scanner/__main__.py"
      to: "l2scanner/mercado_visao.py"
      via: "extra opcional da regiao da ancora, no trilho literal do banner_manutencao"
      pattern: "mercado_ancora"
    - from: "tests/test_mercado_27x.py"
      to: "l2scanner/rastreador.py"
      via: "sequencia de frames reais do 27x alimentando extrair + Rastreador.observar, afirmando zero eventos de morte"
      pattern: "Rastreador"
  prohibitions:
    - "MUST NOT estender o gate de brilho da barra propria (barra_propria_legivel, _moldura_da_barra_propria, _bordas_da_barra_intactas) para detectar o mercado — a licao do consumidor unico esta medida em visao.py:85-111"
    - "MUST NOT deixar rastreador.py ler o campo exibicional de mercado aberto — nenhuma decisao de alerta pode sair dele, e existe tripwire de arquitetura que quebra se passar a ler"
    - "MUST NOT deixar a feature de mercado produzir ou suprimir um evento de morte nesta fase — um sinal, dois consumidores, nunca duplicado"
    - "MUST NOT escrever imagem em calibrar_mercado.py fora de _gravar_conferencia importado de calibrar.py — o tripwire estrutural conta ocorrencias de imwrite no fonte do modulo"
    - "MUST NOT pedir que o usuario edite calibration.json a mao, nem gravar um calibration.json parcial — a ferramenta carrega o arquivo existente, muta os campos de mercado e regrava o arquivo INTEIRO"
    - "MUST NOT subir VERSAO_DO_ESQUEMA — a calibracao medida a mao pelo usuario nao pode ser invalidada por campos opcionais"
    - "MUST NOT commitar gravacoes do usuario alem dos recortes minimos resgatados para tests/fixtures/"
    - "MUST NOT deixar um teste permanente apontando para recordings/ — resgatar fixture ou pular com a razao dita"
---

<objective>
Transformar as gravacoes do usuario nas tres coisas que o resto do milestone depende:
as respostas de campo escritas, a ferramenta de calibracao do mercado, e a deteccao
positiva do painel — o sinal unico que tambem protege o detector de morte.

Purpose: a Fase 2 (leitura de pagina) e a Fase 3 (persistencia) nao podem ser planejadas
em detalhe sem estas respostas. E o criterio 4 do ROADMAP — painel reconhecido E zero
alertas de morte no replay do 27x — e a prova de que o projeto aprendeu com o incidente
das 27 mortes falsas: um sinal, dois consumidores, nunca duplicado.

Output: `SPIKE-RESPOSTAS.md` validado pelo usuario, `calibrar-mercado.bat` +
`l2scanner/calibrar_mercado.py`, as chaves de grade e templates em `calibration.json`,
o campo exibicional em `Observacao`, e o teste de regressao do 27x.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-RESEARCH.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-PATTERNS.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-CONTEXT.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/ROTEIRO-SPIKE.md
@.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-02-SUMMARY.md
</context>

<decision_map>
IDs D-NN das decisoes travadas de `01-CONTEXT.md` > `<decisions>` (mesma tabela dos Planos 01 e 02):
D-01 roteiro como checklist numerado | D-02 os 8 cenarios | D-03 uma sessao por cenario com rotulo fixo |
D-04 SPIKE-RESPOSTAS.md: Claude analisa e propoe citando frames, o usuario valida |
D-05 variantes de encanto no v1, template = nome-como-renderizado com o prefixo `+N`, watchlist conjunto fechado puro, cada variante e serie independente |
D-06 calibracao em modo proprio (`calibrar-mercado.bat`), estilo `selectROI`, fonte = frame GRAVADO, templates cortados pela ferramenta com conferencia visual e matriz de confusao, nunca editar JSON a mao |
D-07 chaves novas opcionais via `.get`, versao 2 congelada | D-08 firewall com banlist ampla |
D-09 imwrite checado, erro alto | D-10 ancora positiva propria, jamais o gate de brilho, verificada no replay do 27x com ZERO alertas de morte.
</decision_map>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE-RESPOSTAS.md — responder as perguntas de campo a partir dos frames gravados (D-04, D-05, FUND-02)</name>
  <precondition>As 8 pastas de gravacao do ROTEIRO existem em `recordings/` com os sufixos `-mercado-fechado`, `-mercado-aberto`, `-mercado-scroll`, `-mercado-pagina-cheia`, `-mercado-tooltip`, `-mercado-alvo-sobreposto`, `-mercado-farm-com-party`, `-mercado-scroll-transicao`, cada uma com `observacoes.jsonl` nao vazio</precondition>
  <files>.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md</files>
  <read_first>
    - `.planning/workstreams/mercado/phases/01-.../ROTEIRO-SPIKE.md` — as perguntas que os cenarios foram gravados para responder
    - `.planning/workstreams/mercado/phases/01-.../01-RESEARCH.md` secoes "Descoberta 3" (o que ja foi verificado visualmente em `recordings/inv3/f000_JANELA.png`: colunas "Goods | Quantity | Total | Unit price", virgula decimal com 2 casas, moeda "XM Coin" na aba Equipment, ~10 slots de linha) e "Open Questions" (as 7 perguntas e as assuncoes A1/A2/A5 a fechar)
    - `.planning/workstreams/mercado/phases/01-.../01-CONTEXT.md` secao `<decisions>` — a decisao de encanto (D-05) que o spike so VALIDA, nao redecide
    - Os frames das gravacoes: abrir visualmente pelo menos um frame representativo de CADA uma das 8 pastas
  </read_first>
  <action>
Analisar os frames gravados e escrever `SPIKE-RESPOSTAS.md` no diretorio da fase (D-04:
Claude analisa e PROPOE, citando frames especificos como evidencia; o usuario valida ou
corrige — nao inventar resposta que o frame nao mostra).

Regra de honestidade do documento: cada resposta carrega o caminho do frame que a
sustenta (`recordings/{pasta}/frame_000012.png`) e um dos tres selos — **VERIFICADO**
(visto no frame), **PARCIAL** (visto, mas so numa aba/condicao) ou **NAO RESPONDIDO**
(o frame nao mostra; diz o que faltou gravar). Uma pergunta sem evidencia fica NAO
RESPONDIDA, nunca preenchida com o plausivel — inventar aqui envenena as Fases 2 e 3.

Responder, uma secao por pergunta:
1. **Linhas por pagina e aparencia do slot vazio** — por aba. O frame do 27x mostra ~10
   slots com 3 preenchidos; confirmar com o cenario `mercado-pagina-cheia`.
2. **Separador de milhar, decimais e moeda** — na aba **Adena** E na aba Equipment. O
   frame ja verificado mostra "40,00 XM Coin" (virgula decimal, 2 casas) na aba
   Equipment; a aba Adena pode ter formato proprio (A2). Registrar o glifo do separador
   de milhar como cidadao de primeira classe — e o que a Fase 2 vai transformar em
   template.
3. **Colunas da grade e sua ordem** — confirmar "Goods | Quantity | Total | Unit price | Buy"
   e se a ordenacao por "Total" muda a ordem das linhas.
4. **Preco total vs unitario** — as duas colunas coexistem? Se sim, registrar que a Fase 3
   pode gravar as duas em colunas separadas (unitario derivado, nunca confundido) em vez
   de derivar uma da outra.
5. **Preco medio embutido: onde fica?** — nao estava visivel no frame ja verificado.
6. **Idade do listing e exibida?**
7. **Renderizacao do encanto (D-05)** — validar com frame que o encanto aparece como
   prefixo `+N <nome do item>`, e capturar o caso de nome LONGO com prefixo para expor
   truncamento. Registrar explicitamente a decisao ja travada: cada variante e um
   template distinto (`+3 Bota X` e `+4 Bota X`), a watchlist e conjunto fechado puro, e
   cada variante e uma serie independente de ponta a ponta.
8. **Posicao do painel: fixa ou arrastavel? (A1)** — a pergunta de arquitetura mais cara
   em aberto. Se o cenario `mercado-aberto` mostrar o painel em duas posicoes, a ancora
   em retangulo fixo NAO basta e a Task 3 precisa do fallback de busca em faixa com
   cadencia limitada (precedente `SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO = 5.0`). Responder
   isto ANTES de calibrar.
9. **Opacidade do painel (A5)** e **o que exatamente tooltip e marcacao de alvo cobrem** —
   alimentam os negativos do limiar da ancora e o estabilizador de pagina da Fase 2.

Fechar com uma secao **"Impacto no planejamento das Fases 2, 3 e 4"** listando, em uma
linha cada, o que muda por causa das respostas (ex.: "o separador de milhar e ponto, entao
a Fase 2 precisa de um 12o glifo").
  </action>
  <verify>
    <automated>python -c "import pathlib,re,sys; t=pathlib.Path('.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/SPIKE-RESPOSTAS.md').read_text(encoding='utf-8'); selos=len(re.findall(r'VERIFICADO|PARCIAL|NAO RESPONDIDO',t)); frames=len(re.findall(r'recordings/[^\s)]+\.png',t)); print(f'selos={selos} frames_citados={frames}'); sys.exit('poucas respostas seladas ou sem citacao de frame') if selos<9 or frames<5 else None"</automated>
    <human-check>Leia SPIKE-RESPOSTAS.md e confirme cada resposta contra o que voce ve no jogo. Corrija o que estiver errado — este documento vira a fonte de verdade das Fases 2 e 3, e uma resposta errada aqui e paga tres fases adiante.</human-check>
  </verify>
  <acceptance_criteria>
    - `SPIKE-RESPOSTAS.md` existe no diretorio da fase e responde as 9 secoes acima.
    - Toda resposta carrega um dos tres selos (VERIFICADO / PARCIAL / NAO RESPONDIDO) e, quando VERIFICADO ou PARCIAL, o caminho do frame que a sustenta.
    - A pergunta A1 (painel fixo ou arrastavel) tem resposta explicita, porque a Task 3 depende dela.
    - A renderizacao do encanto `+N` esta validada contra frame, e a decisao de template com prefixo esta registrada.
    - Existe a secao de impacto no planejamento das Fases 2, 3 e 4.
  </acceptance_criteria>
  <done>As perguntas de campo estao respondidas por escrito, com evidencia rastreavel, e o que nao pode ser respondido esta marcado como tal em vez de inventado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calibrar-mercado — ferramenta propria, templates cortados da gravacao e matriz de confusao medida (D-06, D-07, FUND-03)</name>
  <precondition>`calibration.json` existe na raiz do repo, na versao 2, com `janela` e `party_window` ja calibrados — a ferramenta carrega a calibracao existente e regrava o arquivo inteiro, e recusa se nao houver calibracao anterior</precondition>
  <reversibility rating="costly">As chaves de grade e de templates em `calibration.json` sao formato em disco que as Fases 2, 3 e 4 vao ler. Nao e porta de mao unica porque a propria ferramenta REGRAVA o arquivo inteiro (D-06) — desfazer custa uma recalibragem, nao uma migracao — mas renomear uma chave depois obriga toda instalacao a recalibrar.</reversibility>
  <files>l2scanner/calibrar_mercado.py, l2scanner/calibracao.py, calibrar-mercado.bat, tests/test_calibrar_mercado.py</files>
  <read_first>
    - `l2scanner/calibrar.py` linhas 355-435 (`calibrar_selecionando` com `selectROI` e o reescalonamento para 1600 px; `_gravar_conferencia` e o auxiliar `tentar`) e 610-670 (o modo solo: carregar -> mutar -> `cal.salvar()` regrava o arquivo INTEIRO, e a recusa explicada quando nao ha calibracao anterior)
    - `tests/test_conferencia_gravada.py` linhas 200-239 — `test_existe_um_unico_ponto_de_escrita_no_modulo`, o tripwire estrutural que conta `imwrite` no FONTE do modulo. E a razao de `calibrar_mercado.py` ser modulo NOVO
    - `l2scanner/identidade.py` linhas 147-215 — `Assinatura`, `criar_assinatura` e `_correlacionar`: a tecnica de conjunto fechado ja MEDIDA (1.000 nos acertos contra 0.454 no melhor erro)
    - `l2scanner/calibracao.py` linhas 190-220, 285-315, 340-380 — o trilho `banner_manutencao` (as chaves de ancora do Plano 02 ja seguem esse trilho; as novas seguem igual) e `conferir_geometria`
    - `calibrar-solo.bat` (inteiro) — o esqueleto a copiar
    - `l2scanner/mercado_visao.py` — `molde_para_hex` / `molde_de_hex` e `CASAMENTO_MINIMO_DA_ANCORA` entregues no Plano 02
    - `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` — quantas linhas por pagina, quais colunas e qual o conjunto de glifos, sai daqui
    - `config.toml` — onde a watchlist de itens vive (a ferramenta corta um template por item listado, D-05)
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_calibrar_mercado.py`:
    - Tripwire estrutural (replica de `test_conferencia_gravada.py:217-227` para o modulo
      novo): o total de ocorrencias de `imwrite` no fonte de `l2scanner.calibrar_mercado`
      e ZERO — toda escrita de imagem passa por `_gravar_conferencia` importado de
      `l2scanner.calibrar`.
    - Matriz de confusao: dada uma lista de templates sinteticos com dois quase-identicos,
      a funcao de matriz devolve o par colidente e o pior score inter-classe, e a funcao de
      veredito RECUSA (levanta/retorna erro nomeado) quando esse score fica acima da margem.
      Com templates bem separados, o veredito aprova e devolve o limiar sugerido.
    - Load-mutate-save: partindo de `tests/fixtures/calibracao_de_referencia.json` copiado
      para `tmp_path`, mutar so os campos de mercado e salvar preserva byte a byte todos
      os campos NAO relacionados a mercado (nada de JSON parcial).
    - Recusa explicada: sem `calibration.json` anterior, a ferramenta recusa com mensagem
      que diz o que rodar antes, em vez de criar uma calibracao pela metade.
    - Recusa por geometria: `mercado_geometria_da_captura` gravado com dimensoes de janela
      diferentes das atuais faz a leitura de mercado recusar com "recalibre o mercado"
      (Pitfall 5), nunca ler degradado.
  </behavior>
  <action>
**(a) Chaves restantes em `l2scanner/calibracao.py`** — mesmo trilho `.get` do Plano 02
(D-07), `VERSAO_DO_ESQUEMA` FICA EM 2:
- `mercado_grade: dict | None = None` — geometria da lista de linhas (origem, altura de
  linha, quantidade de linhas por pagina, retangulos das colunas relevantes), com os
  numeros vindos de `SPIKE-RESPOSTAS.md`
- `mercado_templates_de_nome: list[dict] | None = None` — um por item da watchlist,
  nome-como-renderizado INCLUINDO o prefixo `+N` no recorte (D-05); cada entrada guarda o
  rotulo do item e o molde empacotado
- `mercado_templates_de_digito: list[dict] | None = None` — um por glifo (0-9 mais o
  separador de milhar e o separador decimal, conforme `SPIKE-RESPOSTAS.md`)
- `mercado_limiar_de_template: float | None = None` — o limiar SUGERIDO pela matriz de
  confusao, com margem sobre o pior score inter-classe medido

**(b) `l2scanner/calibrar_mercado.py` — modulo NOVO.** Modulo novo por razao estrutural,
nao estetica: `test_existe_um_unico_ponto_de_escrita_no_modulo` conta `imwrite` no fonte
de `l2scanner.calibrar`, entao qualquer escrita de imagem nova la quebraria a suite; e
`calibrar.py` ja tem 943 linhas. Importar `_gravar_conferencia` de `l2scanner.calibrar` —
jamais um `imwrite` proprio.

Fluxo da ferramenta (`python -m l2scanner.calibrar_mercado`):
1. `--frame <caminho>` aponta um PNG gravado, ou `--gravacao <pasta>` com escolha de frame
   (padrao: o frame do meio). **Fonte = frame GRAVADO, nunca captura ao vivo** (D-06):
   calibra sobre a evidencia real e permite recalibrar sem abrir o jogo.
2. Carrega a `Calibracao` existente. Sem ela, recusa com explicacao no mesmo tom de
   `calibrar.py:643-652`.
3. Confere a geometria: dimensoes do frame gravado contra a janela da calibracao. Divergiu,
   recusa alto em vez de medir a regiao errada calada.
4. `selectROI` para: (i) o retangulo da ANCORA do painel — pre-preenchido com o retangulo
   ja medido no Plano 02, que o usuario so confirma ou ajusta; (ii) o retangulo da grade
   de linhas; (iii) a primeira linha, de onde a altura de linha e as colunas sao derivadas.
   Reusar a mecanica de `calibrar_selecionando` (`calibrar.py:363-385`), incluindo o
   reescalonamento para 1600 px de largura e a conversao de volta pela escala.
5. Corte dos templates a partir da propria gravacao: para cada item da watchlist do
   `config.toml`, o usuario marca o recorte do nome-como-renderizado (com o prefixo `+N`
   quando houver, D-05); para os glifos, o usuario marca um exemplar de cada digito e dos
   separadores. Cada recorte e mostrado ampliado para confirmacao visual antes de entrar.
6. **Matriz de confusao medida na hora**: todo template contra todo template usando a
   mesma correlacao em posicao unica de `identidade.py::_correlacionar`. Imprimir a matriz,
   registrar o PIOR score inter-classe, e sugerir o limiar com margem acima dele. **Recusar
   ALTO** se dois itens da watchlist colidirem (score inter-classe acima da margem) — e o
   Pitfall 7.3, e um `+3 Bota X` confundido com `+4 Bota X` corromperia a serie inteira.
   A mensagem de recusa nomeia o par colidente e sugere o conserto (recortar mais largo,
   incluir mais do prefixo, ou tirar um dos itens da watchlist).
7. Imagem de conferencia com TODOS os retangulos desenhados (`cv2.rectangle`), gravada via
   `_gravar_conferencia` importado. O caminho impresso e o que o usuario abre para conferir.
8. `cal.salvar()` regrava o `calibration.json` INTEIRO. Nunca imprimir valores para o
   usuario colar — o criterio 3 do ROADMAP e "sem editar JSON a mao" (D-06).

**(c) `calibrar-mercado.bat`** — copiar o esqueleto de `calibrar-solo.bat` inteiro:
`cd /d "%~dp0"`, a checagem de `.venv\Scripts\python.exe` com a instrucao de rodar
`vigiar-party.bat` uma vez primeiro, a chamada trocada para
`".venv\Scripts\python.exe" -m l2scanner.calibrar_mercado %*`, e o bloco final instruindo
a conferencia visual do PNG. Adaptar o cabecalho `REM` para o proposito de mercado.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrar_mercado.py tests/test_calibracao_mercado.py tests/test_conferencia_gravada.py -q</automated>
    <human-check>Rode `calibrar-mercado.bat --gravacao recordings/{carimbo}-mercado-aberto`, marque as regioes e os templates quando pedido, e abra a imagem de conferencia impressa no fim: os retangulos da ancora, da grade e das colunas devem cair exatamente onde voce espera, e a matriz de confusao impressa nao pode ter par colidente. Depois confira que `calibration.json` ganhou as chaves de mercado — sem que voce tenha editado nada a mao.</human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_calibrar_mercado.py -q` passa com todos os testes de `<behavior>`.
    - `inspect.getsource(l2scanner.calibrar_mercado).count("imwrite")` é 0, e o modulo importa `_gravar_conferencia` de `l2scanner.calibrar`.
    - `l2scanner/calibracao.py` ainda declara `VERSAO_DO_ESQUEMA = 2`, e `tests/fixtures/calibracao_de_referencia.json` continua carregando sem excecao.
    - Salvar apos mutar so os campos de mercado preserva todos os demais campos byte a byte.
    - Com dois templates quase identicos, a ferramenta RECUSA nomeando o par colidente; com templates separados, aprova e imprime o limiar sugerido e o pior score inter-classe.
    - `calibrar-mercado.bat` existe, checa o `.venv` e invoca `l2scanner.calibrar_mercado`.
    - `python -m pytest tests/ -q` sem regressao.
  </acceptance_criteria>
  <done>O usuario calibra o mercado sobre um frame gravado, ve a conferencia visual e a matriz de confusao, e o `calibration.json` sai completo — sem uma linha de JSON editada a mao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DETC-01 — um sinal, dois consumidores: fiacao exibicional e a regressao do incidente 27x (D-10)</name>
  <precondition>`recordings/inv3/` existe no disco com os 45 recortes de party (`f0NN.png`) e os 10 frames de janela completa (`f0NN_JANELA.png`) do incidente 27x; `recordings/` e gitignored e nao vem de um clone limpo</precondition>
  <reversibility rating="costly">`Observacao` e um dataclass publico lido pelo console e pelos logs; acrescentar um campo opcional e barato de desfazer, mas o campo passa a ser citado pela Fase 4 (modo `--mercado`), entao renomea-lo depois toca mais de um consumidor.</reversibility>
  <files>l2scanner/visao.py, l2scanner/__main__.py, l2scanner/mercado_visao.py, tests/test_mercado_27x.py, tests/fixtures/mercado/</files>
  <read_first>
    - `l2scanner/visao.py` linhas 79-118 — os DOIS campos opcionais existentes de `Observacao` (`hp_proprio_aparente` e `estado_do_cliente`) e o comentario que registra a licao medida: promover um sinal a um segundo uso produziu 2 classes de alerta falso e atraso de morte. E o molde exato do campo novo
    - `l2scanner/__main__.py` linhas 1436-1442 — o trilho literal do extra opcional: a regiao so entra no dict de extras quando a calibracao tem a chave
    - `l2scanner/captura_janela.py` linhas 371-395 — `_extra_para_janela`, que devolve `None` quando a regiao cai fora da janela (falha fechada ja existente)
    - `tests/test_inventario_por_cima_da_barra_propria.py` linhas 510-545 (`frame_de_party()`: aquecimento do rastreador com pixels REAIS) e 855-939 (`TestOCasamentoNasFixturesVERSIONADAS` e o `pytest.skip` com a razao dita) — os dois padroes que esta task replica
    - `l2scanner/frames.py` linhas 190-245 — `ReplaySource` faz glob de `frame_*.png`; `recordings/inv3/` usa `f0NN.png`, entao `--replay recordings/inv3` NAO funciona e a regressao tem de ser um harness pytest
    - `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md` — a resposta de A1 (painel fixo ou arrastavel) decide se a ancora e retangulo fixo ou precisa de busca em faixa
    - `.planning/workstreams/mercado/phases/01-.../01-02-SUMMARY.md` — o retangulo da ancora e os scores ja medidos
  </read_first>
  <behavior>
    Testes primeiro, em `tests/test_mercado_27x.py`. O teste afirma as DUAS metades juntas,
    porque a metade "zero mortes" ja passa hoje pelo portao de moldura do quick 260826-dxm —
    um teste que so afirmasse isso nao prenderia o DETC-01 (Pitfall 5 da RESEARCH):

    - Metade 1 (o que o DETC-01 ADICIONA): percorrendo os frames de janela do 27x,
      `mercado_aberto(...)` responde `True` em TODOS eles, com os scores parametrizados
      dentro de 0.001 dos valores medidos.
    - Metade 2 (o que nao pode regredir): alimentando `extrair` + `Rastreador.observar` com
      a sequencia de recortes de party do 27x, aquecidos antes com pixels reais no padrao
      `frame_de_party()`, o rastreador emite ZERO eventos de morte na sequencia inteira.
    - Metade 3 (o campo e exibicional): apos a sequencia, a `Observacao` carrega
      `mercado_aberto_aparente is True`, e o conjunto de eventos emitidos e vazio — o
      campo existiu e nao virou evento.
    - Tripwire de arquitetura: o fonte de `l2scanner.rastreador` NAO cita
      `mercado_aberto_aparente`. Se um dia citar, este teste quebra — a mesma protecao que
      `hp_proprio_aparente` ja tem.
    - Degenerado: sem a chave de mercado na calibracao, o extra nao entra, o campo fica
      `None`, e nada no caminho da party muda.
  </behavior>
  <action>
**(a) Resgatar as fixtures do caminho da party.** Estender `tests/fixtures/mercado/` com o
que a Metade 2 precisa: os recortes de party `f0NN.png` do `recordings/inv3/` que cobrem a
janela do desastre, e os recortes da barra propria derivados dos 10 frames `_JANELA`
aplicando a regiao `hp_proprio` do `calibration.json` vigente. **Conferir primeiro** que a
geometria da calibracao atual ainda corresponde aos frames do inv3 (janela 1720x1392) e
inspecionar visualmente um recorte derivado antes de confiar nele (A4 da RESEARCH) — um
recorte derivado errado faria o teste medir a regiao errada e passar por motivo errado.
Resgatar o MINIMO necessario; `recordings/` continua gitignored e as gravacoes do usuario
nao vao para o repo.

**(b) Campo exibicional em `l2scanner/visao.py::Observacao`.** Acrescentar
`mercado_aberto_aparente: bool | None = None`, no molde LITERAL de `hp_proprio_aparente`
(`visao.py:79-118`), com o comentario no mesmo tom: e so para mostrar; nenhuma decisao
pode sair dele; `None` significa "ninguem perguntou" e nunca vira evento; a seguranca nao
vem de guardas no rastreador, vem de esta leitura NAO EXISTIR para ele. Preencher o campo
quando o extra da ancora estiver presente, chamando `mercado_visao.mercado_aberto` com o
molde e o limiar vindos da calibracao. NAO tocar em `barra_propria_legivel`,
`_moldura_da_barra_propria` nem `_bordas_da_barra_intactas` (D-10) — a licao do consumidor
unico esta medida no proprio arquivo.

**(c) Extra opcional em `l2scanner/__main__.py`.** No bloco de montagem de `extras`
(linhas 1436-1442), acrescentar `extras["mercado_ancora"] = cal.mercado_ancora` SOMENTE
quando `cal.mercado_ancora` existir — trilho literal do `banner_manutencao`. Uma
instalacao sem calibracao de mercado nao paga nem uma captura a mais.

**(d) Ajustar o limiar com os negativos novos.** Rodar `casamento_da_ancora` tambem contra
os frames dos cenarios `mercado-fechado`, `mercado-tooltip` e `mercado-alvo-sobreposto`
gravados pelo usuario, e reconfirmar `CASAMENTO_MINIMO_DA_ANCORA` em
`l2scanner/mercado_visao.py` com a margem REMEDIDA escrita ao lado (pior positivo, melhor
negativo, margem). Se `SPIKE-RESPOSTAS.md` responder que o painel e ARRASTAVEL (A1),
acrescentar o fallback de busca em faixa com cadencia limitada — precedente
`SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO = 5.0` em `captura_janela.py:38-43`, onde a busca por
`matchTemplate` na janela inteira custa ~45 ms e por isso nao roda a cada tick.

**(e) Escrever `tests/test_mercado_27x.py`** com as tres metades e o tripwire de
arquitetura descritos em `<behavior>`. Usar `pytest.skip` com a razao dita para qualquer
teste que dependa de `recordings/` diretamente; os testes que rodam sempre sao os que
usam as fixtures resgatadas em `tests/fixtures/mercado/`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_27x.py tests/test_visao.py tests/test_rastreador.py tests/test_inventario_por_cima_da_barra_propria.py -q</automated>
    <human-check>Rode o harness de replay do 27x e observe a saida: o painel de mercado deve ser reconhecido como "World Exchange aberto" em todos os frames de janela, e a contagem de alertas de morte deve ser exatamente ZERO. Confirme tambem, com uma sessao normal `--janela` sem calibracao de mercado, que o modo party continua se comportando identicamente ao de antes.</human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_27x.py -q` passa com as tres metades e o tripwire.
    - Todo frame de janela do 27x tem `mercado_aberto(...) is True`; a sequencia de party do 27x produz uma lista de eventos de morte de comprimento 0.
    - O fonte de `l2scanner.rastreador` nao contem a expressao do campo exibicional (tripwire de arquitetura verde).
    - `git diff l2scanner/visao.py` nao toca `barra_propria_legivel`, `_moldura_da_barra_propria` nem `_bordas_da_barra_intactas`.
    - Sem `mercado_ancora` na calibracao, `extras` nao ganha a chave e `Observacao.mercado_aberto_aparente` fica `None`.
    - O comentario ao lado de `CASAMENTO_MINIMO_DA_ANCORA` cita a margem REMEDIDA incluindo os negativos das gravacoes novas.
    - `python -m pytest tests/ -q` sem regressao.
  </acceptance_criteria>
  <done>O painel e reconhecido positivamente, o sinal chega ao caminho da party como informacao exibicional, o detector de morte nao mudou, e um unico teste prende as duas metades para que nenhuma regressao futura desfaca uma sem a outra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` (templates e grade) -> processo do scanner | Moldes empacotados em hex sao decodificados e reformatados a partir de dimensoes DECLARADAS no proprio arquivo. |
| Sinal "mercado aberto" -> `Observacao` -> rastreador | O limite mais perigoso do projeto: promover um sinal a um segundo consumidor de DECISAO ja custou 27 alertas falsos. |
| Gravacoes do usuario -> `tests/fixtures/` (versionado) | Nomes de personagens e chat do jogo podem vazar para o repositorio via recorte mal escolhido. |
| `config.toml` (watchlist) -> ferramenta de calibracao | Lista escrita pelo usuario vira o conjunto fechado de templates; colisao entre itens corromperia series inteiras. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `Observacao.mercado_aberto_aparente` -> `rastreador.py` | high | mitigate | Campo exibicional no molde de `hp_proprio_aparente`, sem consumidor de decisao; tripwire de arquitetura na suite quebra se `rastreador.py` passar a cita-lo; regressao do 27x afirma zero eventos de morte |
| T-03-02 | Tampering | `molde_de_hex` sobre `calibration.json` | medium | mitigate | Validar `altura`/`largura` declaradas contra o tamanho real dos bytes antes de reformatar; `CalibracaoInvalida` alta no trilho ja existente (`calibracao.py:316-366`); versao do esquema recusa arquivo de outra versao |
| T-03-03 | Spoofing | template de item da watchlist | high | mitigate | Matriz de confusao medida na calibracao (todo template x todo template); recusa ALTA quando o pior score inter-classe passa da margem, nomeando o par colidente — um `+3` lido como `+4` corromperia a serie de precos |
| T-03-04 | Information Disclosure | `tests/fixtures/mercado/` versionado | medium | mitigate | Resgatar o MINIMO: recortes da ancora, da barra propria e da party window do incidente 27x; nenhuma gravacao nova do usuario e commitada; o SUMMARY lista arquivo por arquivo o que entrou no repo |
| T-03-05 | Repudiation | calibracao gravada sem conferencia | medium | mitigate | Imagem de conferencia obrigatoria via `_gravar_conferencia`, que ja checa o retorno do `imwrite` e diz alto quando nao consegue — o mesmo bug que o FUND-01 consertou no gravador |
| T-03-06 | Denial of Service | leitura de mercado com janela redimensionada | low | mitigate | `mercado_geometria_da_captura` confere as dimensoes no arranque e recusa com "recalibre o mercado" em vez de ler degradado (Pitfall 5) |
| T-03-SC | Tampering | instalacoes npm/pip/cargo | high | mitigate | Zero instalacoes nesta fase; `requirements.txt` nao muda. FIRE-01 (Plano 01) e o portao permanente |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plano 03)

**Modulos e lancadores novos**
- `l2scanner/calibrar_mercado.py` — `python -m l2scanner.calibrar_mercado`, com as flags `--frame <png>` e `--gravacao <pasta>`; corta ancora, grade, templates de nome e de digito; mede a matriz de confusao; grava a conferencia via `_gravar_conferencia` importado de `l2scanner.calibrar`
- `calibrar-mercado.bat` — lancador no precedente de `calibrar-solo.bat`

**Chaves novas em `calibration.json` (opcionais, `VERSAO_DO_ESQUEMA` segue 2)**
- `mercado_grade`
- `mercado_templates_de_nome` (um por item da watchlist, com o prefixo `+N` no recorte)
- `mercado_templates_de_digito` (0-9 mais separadores)
- `mercado_limiar_de_template`

**Campos novos**
- `l2scanner/calibracao.py::Calibracao.mercado_grade`, `.mercado_templates_de_nome`, `.mercado_templates_de_digito`, `.mercado_limiar_de_template`
- `l2scanner/visao.py::Observacao.mercado_aberto_aparente: bool | None = None` — exibicional

**Alterado**
- `l2scanner/__main__.py` — chave `mercado_ancora` no dict de `extras`, so quando a calibracao a tem
- `l2scanner/mercado_visao.py` — `CASAMENTO_MINIMO_DA_ANCORA` REMEDIDO com os negativos das gravacoes novas; fallback de busca em faixa se o painel for arrastavel

**Arquivos novos**
- `.planning/workstreams/mercado/phases/01-.../SPIKE-RESPOSTAS.md`
- `tests/test_calibrar_mercado.py`
- `tests/test_mercado_27x.py`
- `tests/fixtures/mercado/` — recortes de party e de barra propria do incidente 27x, alem dos ja resgatados no Plano 02
</artifacts_this_phase_produces>

<verification>
1. `python -m pytest tests/ -q` — suite inteira verde no Python GLOBAL (o `.venv` nao tem pytest).
2. `SPIKE-RESPOSTAS.md` responde as 9 secoes, com selo e citacao de frame em cada uma.
3. `calibrar-mercado.bat` roda sobre uma gravacao do spike e produz um `calibration.json` completo mais a imagem de conferencia — sem edicao manual de JSON.
4. `tests/test_mercado_27x.py` afirma as duas metades do criterio 4 no mesmo teste.
5. `l2scanner/rastreador.py` nao aparece no diff da fase.
</verification>

<success_criteria>
- Criterio 2 do ROADMAP completo: as perguntas de campo estao respondidas por escrito a partir das gravacoes, com a decisao de encanto registrada.
- Criterio 3 do ROADMAP completo: a calibracao de mercado persiste em `calibration.json` por ferramenta propria, sem editar JSON a mao.
- Criterio 4 do ROADMAP completo: no replay do 27x o painel e reconhecido E zero alertas de morte disparam, presos pelo mesmo teste.
- Zero dependencias novas; `VERSAO_DO_ESQUEMA` segue 2; `rastreador.py` intocado.
</success_criteria>

<output>
Criar `.planning/workstreams/mercado/phases/01-funda-o-firewall-gravador-e-spike-de-campo/01-03-SUMMARY.md` ao terminar, incluindo a matriz de confusao medida, o limiar final da ancora com a margem remedida, e a lista arquivo por arquivo do que foi resgatado para `tests/fixtures/mercado/`.
</output>
