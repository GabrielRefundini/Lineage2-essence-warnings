---
phase: 03-persist-ncia-de-observa-es
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - l2scanner/__main__.py
  - l2scanner/mercado_registro.py
  - .gitignore
  - tests/test_mercado_registro.py
autonomous: true
requirements: [PERS-01, PERS-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`montar_registro_de_mercado` tenta, degrada com log, devolve `None` e deixa o scanner subir — nunca levanta, pelo trilho de `montar_despachante` e `montar_gravador` (PERS-03, D-13)"
    - "O construtor de `RegistroDeObservacoes` continua ingenuo e e a MONTAGEM que o envolve num `try` inteiro, `mkdir` incluido — o `mkdir` do analog levanta `FileExistsError` fora de qualquer rede, e e esse o bug que `montar_gravador` foi escrito para consertar"
    - "Os quatro modos de falha medidos nesta maquina (arquivo somente-leitura, pasta inexistente, nome ocupado por diretorio, `mkdir` sobre nome de arquivo) sao todos subclasse de `OSError`, e um `except` so cobre as quatro"
    - "Cabecalho divergente tambem desliga a feature pela montagem, e nao so por excecao solta: a montagem captura `ContratoDoArquivoQuebrado` ao lado do `OSError`, com a divergencia justificada no fonte (D-12)"
    - "O aviso alto sai em `log.error` com DUAS mensagens, e a segunda diz o que CONTINUA funcionando — a frase e a mesma que `montar_gravador` ja usa, e ela e o PERS-03 escrito uma vez"
    - "O aviso chega ao CONSOLE e ao LOG pelo mesmo caminho que todo o resto do scanner: `configurar_log` instala um `StreamHandler` sobre `sys.stdout` alem do arquivo rotativo (D-13)"
    - "`.mercado/LEIAME.txt` e escrito UMA VEZ, na criacao da pasta, nunca sobrescrito, e carrega o roteiro de importacao no Sheets, a explicacao dos centesimos e a distincao entre residuo vazio e residuo zero (PERS-01)"
    - "O LEIAME nao toca o CSV e nao entra na importacao: a alternativa de por uma linha de instrucao no topo do arquivo foi REJEITADA porque quebraria o cabecalho-contrato e o Sheets a importaria como dado"
    - "O `.gitignore` ja cobre o arquivo novo por `.mercado/`, que e ignore de DIRETORIO: nenhuma entrada nova entra, so o comentario deixa de falar de um arquivo so"
    - "A funcao de montagem nasce SEM CHAMADOR, e isso e desenho: o modo `--mercado` e DETC-02, Fase 4"
  artifacts:
    - path: "l2scanner/__main__.py"
      provides: "`montar_registro_de_mercado`, ao lado de `montar_vigia_do_mercado`, no trilho da casa"
      contains: "montar_registro_de_mercado"
    - path: "l2scanner/mercado_registro.py"
      provides: "`escrever_leiame`, chamado na criacao da pasta e nunca sobrescrevendo o que ja existe"
      contains: "ARQUIVO_DO_LEIAME"
    - path: ".gitignore"
      provides: "o comentario de `.mercado/` citando os DOIS arquivos com donos distintos"
      contains: ".mercado/"
  key_links:
    - from: "l2scanner/__main__.py"
      to: "l2scanner/mercado_registro.py"
      via: "import adiado dentro da funcao de montagem, no precedente de `montar_vigia_do_mercado`"
      pattern: "mercado_registro"
    - from: "l2scanner/mercado_registro.py"
      to: ".mercado/LEIAME.txt"
      via: "escrito na criacao da pasta, ao lado do CSV que ele explica"
      pattern: "LEIAME"
---

<objective>
Dar ao modulo do 03-01 a porta de entrada que a casa exige — uma funcao de montagem que tenta,
degrada com log alto e devolve `None` — e por ao lado do dado o unico documento que o usuario
vai procurar seis meses depois: como importar este arquivo no Google Sheets sem que as colunas
colapsem.

A funcao de montagem existe por um motivo especifico e ja pago em incidente neste repositorio.
`Catalogo.__init__` faz `mkdir(parents=True, exist_ok=True)` FORA de qualquer `try`, e a medicao
desta fase mostrou que esse `mkdir` levanta `FileExistsError` (errno 17, winerror 183) quando um
ARQUIVO ocupa o nome da pasta. `Catalogo` sobrevive a isso hoje so porque nao tem instanciador
de producao. `RegistroDeObservacoes` vai ter — na Fase 4 — e o padrao da casa e claro: o
construtor pode ser ingenuo, o que nao pode e ser chamado nu. `montar_gravador` existe
literalmente porque `Gravador.__init__` fazia `mkdir` e `open` fora de qualquer rede e derrubava
o scanner inteiro por um `recordings/` somente-leitura.

O LEIAME resolve na origem a tensao entre os criterios 1 e 2 da fase. O criterio 1 quer que o
usuario ENTENDA o que le; a decisao travada grava `6200` onde a tela dizia `62,00`. A saida
recomendada pela pesquisa e dupla: o NOME DA COLUNA carrega a unidade (feito no 03-01) e um
arquivo de texto ao lado carrega o roteiro de importacao. As outras duas saidas foram
rejeitadas com motivo: uma linha de instrucao no topo do CSV quebraria o cabecalho-contrato e
seria importada como dado; uma coluna `total_exibido` seria dois campos para o mesmo fato e cai
na mesma objecao que derrubou a coluna do unitario.

Purpose: sem a montagem, a Fase 4 nao tem por onde ligar o registro sem reintroduzir o
traceback cru no arranque. Sem o LEIAME, o portao humano do criterio 2 comeca com o usuario
adivinhando qual opcao do dialogo do Sheets usar — e a opcao que ele procuraria nao existe.

Output: `montar_registro_de_mercado` em `l2scanner/__main__.py`, `escrever_leiame` em
`l2scanner/mercado_registro.py`, o comentario do `.gitignore` atualizado, e os testes dos dois.

**A FUNCAO NASCE SEM CHAMADOR, e isso e desenho.** Nao ligar no laco, nao criar flag, nao tocar
`l2scanner/rastreador.py` nem o gate de brilho da barra propria em `l2scanner/visao.py`. O modo
`--mercado` e DETC-02, Fase 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-CONTEXT.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-RESEARCH.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-PATTERNS.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: montar_registro_de_mercado — tenta, degrada, avisa alto, e deixa o scanner subir</name>
  <precondition>O 03-01 ja entregou `l2scanner/mercado_registro.py` com `RegistroDeObservacoes`, `ContratoDoArquivoQuebrado` e `PASTA_DO_MERCADO`. Confira que `python -m pytest tests/test_mercado_registro.py -q` esta verde antes de comecar — esta task envolve aquele construtor, nao o reescreve.</precondition>
  <files>l2scanner/__main__.py, tests/test_mercado_registro.py</files>
  <read_first>
    - `l2scanner/__main__.py:225-262` — `montar_gravador` inteiro: a docstring que explica por
      que a funcao existe, o `except OSError` unico, as DUAS mensagens de `log.error`, e o
      `return None` que nunca vira `raise`
    - `l2scanner/__main__.py:189-224` — `montar_despachante`, e a frase-irma da segunda mensagem
    - `l2scanner/__main__.py:406-465` — `montar_vigia_do_mercado`: a vizinhanca onde a funcao
      nova vai morar, e o precedente do import ADIADO de modulo de mercado dentro da funcao
    - `l2scanner/__main__.py:155-187` — `configurar_log`: o `RotatingFileHandler` mais o
      `StreamHandler` sobre `sys.stdout`. E isto que faz "no console E no log" ser verdade sem
      codigo novo
    - `tests/test_gravador_honesto.py:478-501` — o teste de montagem inteiro: o import de
      `l2scanner.__main__` DENTRO da funcao, o nome-ocupado-por-arquivo como forma de falha
      deterministica, e os asserts sobre o CONTEUDO da mensagem e nao so sobre o nivel
    - `03-RESEARCH.md`, secao "Lacuna 5" — os quatro modos com `errno`/`winerror`, e as duas
      armadilhas de teste que a medicao revelou
    - `03-PATTERNS.md`, secao 1 — as cinco regras do padrao de montagem
  </read_first>
  <behavior>
    - Com uma pasta normal, a funcao devolve um `RegistroDeObservacoes` e nao emite `ERROR`
    - Com um ARQUIVO ocupando o nome da pasta, devolve `None` e emite `ERROR` — sem traceback
    - Com o CSV marcado somente-leitura e um arranque que precisa escrever o cabecalho, devolve
      `None` e emite `ERROR`
    - Com um arquivo cujo cabecalho diverge do contrato, devolve `None` e emite `ERROR`
    - Em todos os casos de falha, a SEGUNDA mensagem diz que morte, saida e ressurreicao seguem
      sendo detectadas e entregues
    - Chamada sem argumento de pasta, ela resolve a pasta de producao do modulo de registro
    - A funcao nunca levanta, em nenhum dos casos acima
  </behavior>
  <action>
Acrescentar `montar_registro_de_mercado(pasta: Path | None = None) -> RegistroDeObservacoes |
None` a `l2scanner/__main__.py`, imediatamente depois de `montar_vigia_do_mercado`, que e a
vizinhanca tematica certa.

O import do modulo de registro e ADIADO para dentro da funcao, no precedente de
`montar_vigia_do_mercado`, que importa `mercado_visao` la dentro. O parametro `pasta` e opcional
e resolvido em tempo de CHAMADA: quando vem `None`, a funcao usa a constante de producao do
modulo de registro. Resolver na chamada e o que permite ao teste apontar para `tmp_path` — por
parametro, no caso comum, ou por substituicao da constante, no caso em que se quer provar
justamente que o padrao e a pasta de producao.

O corpo e o trilho da casa, e as cinco regras vem do `montar_gravador`:

Primeira, o `try` envolve o construtor INTEIRO. Isso inclui o `mkdir`, que e onde nasce o
`FileExistsError` (errno 17, winerror 183) quando um arquivo ocupa o nome da pasta — medido
nesta maquina. O construtor nao se defende; quem o defende e esta funcao.

Segunda, a captura. `OSError` cobre os quatro modos medidos: `PermissionError` (errno 13) para
arquivo somente-leitura e para nome ocupado por diretorio, `FileNotFoundError` (errno 2) para
pasta inexistente, e o `FileExistsError` acima. Esta funcao captura TAMBEM
`ContratoDoArquivoQuebrado`, e a divergencia com a regra da casa vai escrita na docstring: o
cabecalho divergente nao e falha de sistema de arquivos, e uma recusa deliberada de escrever
desalinhado sobre dado que o usuario acumulou e edita a mao — o desfecho e o mesmo (feature
desligada, scanner de pe), entao o tratamento e o mesmo.

Terceira, `log.error` e nao `log.warning`. O precedente esta escrito na docstring de
`montar_gravador`: `warning` e para linha descartada, `error` e para feature desligada, e quem
esta lendo o console precisa saber que nao vai ter dado no fim da sessao.

Quarta, DUAS mensagens, e a segunda diz o que CONTINUA funcionando. A primeira nomeia o arquivo
e o erro. A segunda e a frase que `montar_gravador` ja usa: todo o resto do scanner continua
igual, morte, saida e ressurreicao seguem sendo detectadas e entregues. Essa frase e o PERS-03
escrito uma vez, e o teste a confere por substring.

Quinta, `return None`, nunca `raise`. A assinatura devolve `None` e o chamador da Fase 4 trata
`None` como feature desligada.

A docstring diz tambem, por escrito, que a funcao NASCE SEM CHAMADOR: `montar_gravador` tem um
portao de curto-circuito na entrada porque existe a flag `--record`; aqui nao ha flag, porque a
flag `--mercado` e DETC-02, Fase 4. Quem ligar isto la vai encontrar a funcao pronta e no
trilho.

Nos testes, uma classe nova em `tests/test_mercado_registro.py` com o comentario-regua da
metade de montagem, e o import de `l2scanner.__main__` dentro de cada funcao de teste, como
`tests/test_gravador_honesto.py:484` faz. A forma de falha preferida e o NOME OCUPADO POR
ARQUIVO, com a justificativa copiada do analog: ela e deterministica em todo sistema
operacional e nao depende de permissao, que varia entre maquinas. Onde permissao for
inevitavel, marcar o ARQUIVO como somente-leitura e restaurar a permissao no fim; nao marcar a
PASTA, porque a medicao desta pesquisa mostrou que o bit e inerte sobre pasta no Windows e um
teste escrito assim passaria por acidente.

Acrescentar um teste que afirma que `configurar_log` instala um manipulador de fluxo sobre
`sys.stdout` alem do arquivo rotativo. Ele nao reimplementa nada: prova que o `log.error` desta
funcao chega ao console E ao log pelo caminho que o scanner inteiro ja usa, que e a metade do
D-13 que nao se ve no `caplog`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_registro.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_registro.py -q` verde
    - Existe teste que cria um ARQUIVO com o nome da pasta, chama a montagem e afirma com
      `assert` que o retorno e `None` e que ha registro de nivel `ERROR`
    - Existe teste que afirma, por substring e com `assert`, que uma das mensagens de `ERROR`
      diz que morte, saida e ressurreicao seguem sendo detectadas e entregues
    - Existe teste que fabrica um CSV com cabecalho divergente e afirma que a montagem devolve
      `None` em vez de deixar `ContratoDoArquivoQuebrado` subir
    - Existe teste que marca o CSV como somente-leitura antes do arranque, afirma `None` e
      `ERROR`, e restaura a permissao no fim
    - Existe teste que chama a montagem SEM argumento de pasta, com a constante de producao
      substituida por `tmp_path`, e afirma que o arquivo criado esta debaixo de `tmp_path`
    - `python -c "import inspect,l2scanner.__main__ as p; assert callable(p.montar_registro_de_mercado); assert inspect.signature(p.montar_registro_de_mercado).parameters['pasta'].default is None; print('montagem ok')"` imprime `montagem ok`
    - `python -c "import ast,inspect,l2scanner.__main__ as p; a=ast.parse(inspect.getsource(p.montar_registro_de_mercado)); assert not [n for n in ast.walk(a) if isinstance(n,ast.Raise)], 'a montagem nao levanta'; print('nunca levanta')"` imprime `nunca levanta`
    - Existe teste que afirma que `configurar_log` instala um manipulador de fluxo sobre
      `sys.stdout`, provando a metade console do aviso alto
  </acceptance_criteria>
  <done>A funcao de montagem existe ao lado de `montar_vigia_do_mercado`, cobre os quatro modos de falha medidos mais o cabecalho-contrato, avisa alto com as duas mensagens e devolve `None` — e o scanner sobe em todos os casos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O LEIAME ao lado do dado, e o comentario do .gitignore que passou a falar de dois arquivos</name>
  <files>l2scanner/mercado_registro.py, .gitignore, tests/test_mercado_registro.py</files>
  <read_first>
    - `l2scanner/mercado_registro.py` inteiro, como o 03-01 o deixou — em especial o construtor,
      que e onde a pasta e criada e onde o LEIAME entra
    - `.gitignore:28-42` — o bloco de saida em tempo de execucao, com a prosa de razao de
      `.loot/`, `.agenda/` e `.mercado/`
    - `03-RESEARCH.md`, secao "Lacuna 1" inteira — as quatro opcoes do dialogo do Sheets, o
      caminho de importacao que abre o dialogo, e a tabela que rejeita as saidas (b) e (d)
    - `03-RESEARCH.md`, secao "Armadilha 1" — o nome que comeca em `+` e por que ele NAO vira
      codigo de saneamento
    - `03-CONTEXT.md`, secao `<specifics>` — o motivo travado do separador
  </read_first>
  <behavior>
    - Construir o registro numa pasta vazia cria o LEIAME ao lado do CSV
    - Construir de novo sobre a mesma pasta NAO reescreve o LEIAME: um arquivo editado a mao
      permanece exatamente como o usuario o deixou
    - O LEIAME e texto puro em UTF-8 e nao e um CSV: ele nao entra na importacao e nao afeta o
      cabecalho-contrato
    - O LEIAME nomeia o caminho de importacao do Sheets e o separador personalizado
    - O LEIAME explica que os precos sao inteiros em centesimos, com o exemplo concreto
    - O LEIAME explica a diferenca entre a coluna de residuo VAZIA e a coluna de residuo em zero
    - O LEIAME avisa que um nome que comeca com sinal de mais pode ser interpretado como formula
      pela planilha, e que isso e uma pergunta em aberto, nao um defeito conhecido do arquivo
    - O `.gitignore` continua com uma unica entrada `.mercado/`, e o comentario passa a citar os
      dois arquivos
  </behavior>
  <action>
Acrescentar a `l2scanner/mercado_registro.py` a constante `ARQUIVO_DO_LEIAME = "LEIAME.txt"` e a
funcao `escrever_leiame(pasta: Path) -> bool`, chamada pelo construtor logo depois do `mkdir`.
Ela devolve `True` quando escreveu e `False` quando o arquivo ja existia.

A regra e ESCREVER SO SE AUSENTE, nunca sobrescrever, e a razao vai no fonte: o usuario pode
anotar coisas ali — e um arquivo para humano, na pasta dele — e um programa que reescrevesse
esse texto a cada arranque apagaria a anotacao calado, que e a mesma familia de erro que o
cabecalho-contrato existe para impedir do outro lado. A escrita usa `encoding="utf-8"` e nao
levanta para fora por conta propria: ela roda dentro do construtor, que a montagem do 03-02
envolve.

O texto e curto, em portugues, e cobre seis pontos, nesta ordem:

Primeiro, o que a pasta e: estado local e duravel do scanner, um arquivo por dono — o catalogo
de nomes e o vocabulario, escrito pela leitura de pagina; as observacoes sao o registro,
escritas por este modulo. Nenhum dos dois e versionado, e a estatistica e da maquina.

Segundo, o roteiro de importacao no Google Sheets, com o caminho exato: menu Arquivo, depois
Importar, depois Enviar o arquivo. No dialogo, o campo de separador oferece deteccao
automatica, tabulacao, virgula e personalizado — e a escolha certa e PERSONALIZADO com o
caractere ponto-e-virgula digitado. Dizer por extenso que NAO existe uma opcao pronta de
ponto-e-virgula na lista, porque quem procurar uma vai perder tempo. Dizer tambem que abrir o
arquivo com duplo clique no Drive nao oferece esse dialogo, entao o caminho pelo menu e o que
funciona.

Terceiro, por que o separador e ponto-e-virgula: a exibicao do jogo usa padrao brasileiro com
virgula decimal, e um CSV separado por virgula colapsaria a planilha inteira numa coluna so.

Quarto, os precos: numeros inteiros em CENTESIMOS. O valor 6200 no arquivo e 62,00 na tela. O
nome da coluna ja carrega a unidade. Nao ha coluna de preco unitario, e a razao e uma frase: o
unitario que o jogo mostra e arredondado a duas casas, e reconstruir o total a partir dele
devolve um numero que nunca existiu.

Quinto, a coluna do residuo do cruzamento: ela e a diferenca, em centesimos, entre o total lido
e o produto do unitario pela quantidade — uma pista sobre a propria leitura, nunca um preco.
Celula VAZIA quer dizer "nao deu para medir, alguma das tres celulas nao leu"; um ZERO quer
dizer "conferi e bateu". Sao fatos diferentes e nao devem ser tratados como o mesmo.

Sexto, uma linha por observacao, com a data da PRIMEIRA vez que ela foi vista; rever a mesma
pagina nao acrescenta linha. E o aviso do sinal de mais: alguns nomes de item comecam com esse
sinal, e planilhas costumam tratar celula que comeca assim como inicio de formula. Se acontecer
na sua importacao, avise — o arquivo esta certo, e a correcao seria na planilha, nunca no dado.

Nao sanear o nome no arquivo, nem aqui nem em lugar nenhum: sujar o dado para agradar um
importador e o oposto do que este arquivo existe para ser.

No `.gitignore`, editar SO o comentario das linhas 34 a 37, que hoje diz que a pasta guarda o
catalogo de nomes, no singular. Ele passa a citar os dois arquivos e os dois donos. A entrada
`.mercado/` NAO muda e nenhuma entrada nova entra: ela e um ignore de DIRETORIO, com barra
final, e cobre recursivamente todo o conteudo — o CSV novo, o LEIAME e qualquer temporario ja
estao dentro dela.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_registro.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_registro.py -q` verde
    - Existe teste que constroi o registro num `tmp_path` vazio e afirma com `assert` que o
      LEIAME existe ao lado do CSV
    - Existe teste que edita o LEIAME, constroi o registro de novo sobre a mesma pasta, e
      afirma que o conteudo editado permaneceu byte a byte
    - Existe teste que le o LEIAME e afirma, com `assert` e por substring, que ele contem o
      caractere separador entre aspas, a palavra que nomeia a opcao personalizada do dialogo, o
      numero 6200 e a forma 62,00
    - Existe teste que afirma que o LEIAME distingue a celula vazia do zero na coluna do residuo,
      citando as duas leituras
    - Existe teste que afirma que o LEIAME menciona o sinal de mais no comeco do nome como
      pergunta em aberto
    - `grep -v '^#' .gitignore | grep -c '^\.mercado/$'` devolve exatamente `1`
    - `sed -n '30,42p' .gitignore | grep -c 'observa'` devolve valor maior que zero — o
      comentario passou a citar o segundo arquivo
    - `python -c "import l2scanner.mercado_registro as m; assert m.ARQUIVO_DO_LEIAME == 'LEIAME.txt'; assert callable(m.escrever_leiame); print('leiame ok')"` imprime `leiame ok`
  </acceptance_criteria>
  <reversibility rating="reversible">O LEIAME e documento, nao dado: reescrever o texto numa fase futura nao quebra o CSV, nao muda o cabecalho-contrato e nao afeta a dedup. O unico cuidado e nunca sobrescrever o arquivo do usuario, que a propria regra de escrita ja garante.</reversibility>
  <done>A pasta `.mercado/` passa a nascer com um LEIAME que responde sozinho como importar o arquivo no Sheets, o que sao os centesimos e o que a coluna vazia do residuo significa — e o comentario do `.gitignore` deixou de falar de um arquivo so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sistema de arquivos -> processo (arranque) | Pasta, permissao e nome ocupado sao estado do mundo, nao do programa: qualquer um dos quatro modos medidos pode estar presente no arranque |
| arquivo -> processo (cabecalho) | O cabecalho do CSV e a unica declaracao de forma que o arquivo carrega, e o usuario pode edita-lo |
| processo -> usuario (LEIAME) | O texto escrito na pasta e a unica documentacao que o usuario vai achar seis meses depois |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Denial of Service | `mkdir` do construtor levantando fora de qualquer rede e derrubando o scanner inteiro | high | mitigate | A montagem envolve o construtor INTEIRO num `try`, `mkdir` incluido, e captura `OSError`, que cobre os quatro modos medidos. E o bug que `montar_gravador` foi escrito para consertar, e ha teste com nome-ocupado-por-arquivo |
| T-03-10 | Tampering | cabecalho editado a mao fazendo o programa escrever desalinhado sobre dado acumulado | high | mitigate | `ContratoDoArquivoQuebrado` capturado pela montagem ao lado do `OSError`: feature desligada, arquivo intocado, aviso alto. Nenhuma migracao automatica |
| T-03-11 | Repudiation | feature desligada em silencio e usuario farmando uma sessao inteira sem dado | high | mitigate | DUAS mensagens em `log.error`, a segunda dizendo o que continua funcionando, entregues ao console e ao arquivo rotativo pelo `configurar_log` que o scanner ja instala. Testes conferem NIVEL e CONTEUDO |
| T-03-12 | Information Disclosure | pasta de estado local versionada por engano | medium | mitigate | `.mercado/` no `.gitignore` como ignore de diretorio, ja existente; o teste afirma que continua havendo exatamente uma entrada, fora de comentario |
| T-03-13 | Tampering | LEIAME sobrescrito apagando anotacao do usuario | low | mitigate | Escrever so se ausente, com a razao no fonte; teste afirma que o conteudo editado sobrevive a um segundo arranque |
| T-03-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install neste plano.** Tudo e stdlib e modulo interno; auditoria de legitimidade de pacote nao-aplicavel por ausencia de pacote |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**`l2scanner/__main__.py` (modificado):**
- `montar_registro_de_mercado(pasta: Path | None = None) -> RegistroDeObservacoes | None`,
  logo depois de `montar_vigia_do_mercado`, com import adiado do modulo de registro

**`l2scanner/mercado_registro.py` (modificado):**
- `ARQUIVO_DO_LEIAME = "LEIAME.txt"` e `escrever_leiame(pasta) -> bool`
- O construtor passa a chamar `escrever_leiame` logo depois do `mkdir`

**`.gitignore` (modificado):** so o comentario do bloco de `.mercado/`; a entrada nao muda e
nenhuma entrada nova entra

**Arquivo de runtime novo:** `.mercado/LEIAME.txt` (coberto pelo mesmo ignore de diretorio)

**Nao produzido aqui, de proposito:** nenhuma flag de linha de comando e nenhum chamador da
montagem. `--mercado` e DETC-02, Fase 4.
</artifacts_this_phase_produces>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

A tabela completa das 16 decisoes esta no `03-01-PLAN.md`, secao `<indice_de_decisoes>`, e os
IDs sao os mesmos. As citadas aqui:

| ID | Decisao (do `03-CONTEXT.md`) |
|---|---|
| D-10 | Mora em `.mercado/`, ao lado do catalogo de nomes da Fase 2. |
| D-11 | O cabecalho e escrito UMA VEZ, na criacao, e CONFERIDO na abertura. |
| D-12 | O cabecalho e CONTRATO: divergiu, a feature DESLIGA ALTO — nada de migracao automatica. |
| D-13 | "Desligar alto" e: aviso no console E no log, o mercado para, e os alertas de party continuam. |

**Discricao declarada pelo usuario e exercida aqui:** o FORMATO do aviso alto — duas mensagens
em `log.error`, a segunda dizendo o que continua funcionando, entregues por um caminho que ja
alcanca console e arquivo.
</indice_de_decisoes>

<suposicoes_da_sonda>
## As bordas da sonda que este plano toca

**PERS-03.** Este plano resolve a metade que da para resolver hoje: a montagem cobre os quatro
modos medidos mais o cabecalho, avisa alto e devolve `None` sem nunca levantar. A SUPOSICAO que
permanece e a mesma declarada no 03-01: como esta fase nao tem chamador no scanner, "nunca
derruba o nucleo de alertas" e verdadeiro por AUSENCIA DE ACOPLAMENTO, e nao por um teste de
ponta a ponta com o mercado ligado ao lado do detector de morte. A conferencia estrutural que
cabe aqui e que nenhum modulo do nucleo importa o registro; a de ponta a ponta e da Fase 4.

**PERS-01.** O LEIAME resolve a metade documental de "importavel sem conversao": o roteiro do
dialogo esta confirmado em fonte oficial. Permanece a suposicao NAO MEDIDA sobre o nome que
comeca com sinal de mais — e ela entra no proprio texto do LEIAME como pergunta em aberto, alem
de virar item do portao humano do 03-03.
</suposicoes_da_sonda>

<verification>
- `python -m pytest tests/test_mercado_registro.py -q` verde
- `python -m pytest tests/test_gravador_honesto.py tests/test_mercado_catalogo.py -q` verde — os
  analogs de montagem e de arquivo continuam intocados
- `python -m pytest tests/test_mercado_27x.py tests/test_firewall_escopo.py -q` verde
- `git diff --name-only` NAO lista `l2scanner/rastreador.py` nem `l2scanner/visao.py`
- `git status --short` NAO lista `calibration.json` — ele nunca e commitado
- Suite completa: `python -m pytest -q --ignore=tests/test_agenda.py` e depois
  `python -m pytest tests/test_agenda.py -q`. FLAKE CONHECIDO em `tests/test_agenda.py:1141`:
  **abortar nao e falhar — rode de novo.**
- **NAO rodar a varredura do censo:** ela passa de 10 minutos e nao e gate de nada aqui
</verification>

<success_criteria>
A montagem existe, cobre os quatro modos de falha medidos mais o cabecalho-contrato, avisa alto
com as duas mensagens e devolve `None` sem levantar; a pasta `.mercado/` nasce com um LEIAME que
ensina a importacao no Sheets e nunca e sobrescrito; e o `.gitignore` continua com uma entrada
so, agora com o comentario dizendo a verdade sobre dois arquivos.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-02-SUMMARY.md` when done
</output>
