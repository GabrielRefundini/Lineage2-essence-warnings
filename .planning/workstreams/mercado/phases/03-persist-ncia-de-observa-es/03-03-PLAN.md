---
phase: 03-persist-ncia-de-observa-es
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tools/gerar_observacoes_do_censo.py
  - tests/test_gerar_observacoes_do_censo.py
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe um caminho REAL para o usuario produzir um `observacoes.csv` com dado de verdade, sem esperar a Fase 4: a ferramenta passa as gravacoes do censo pelo leitor de pagina da Fase 2 e grava o resultado pelo registro da Fase 3 (PERS-01)"
    - "A ferramenta NAO e o modo `--mercado`: ela le gravacoes em disco, nao captura tela, nao roda no laco do scanner, e nao acrescenta flag nenhuma a `l2scanner/__main__.py`. DETC-02 continua sendo Fase 4"
    - "A lista de gravacoes vem IMPORTADA de `medir_oclusao.GRAVACOES_DO_CENSO`, nunca redigitada e nunca um glob sobre `recordings/` — a pasta tem 16 subdiretorios e so 8 sao do censo"
    - "`recordings/` e aberto SOMENTE PARA LEITURA e a ferramenta nunca escreve dentro dele"
    - "A saida e OBRIGATORIA na linha de comando e a ferramenta RECUSA uma saida que aponte para a pasta de producao: linha derivada de replay carrega carimbo de agora sobre preco visto dias atras, e misturar isso com dado ao vivo e a confusao entre bancada e producao que o CONTEXT proibe"
    - "Rodar a ferramenta DUAS VEZES sobre a mesma saida nao aumenta a contagem de linhas — e a prova de campo do PERS-02, contada pelo proprio usuario"
    - "O carimbo vem de um `Relogio` do projeto, nunca de uma chamada solta ao relogio do sistema (D-16)"
    - "Com a saida quebrada de proposito, a ferramenta imprime o aviso alto do 03-02 no console e sai com codigo diferente de zero — e a unica forma de o usuario VER o desligamento antes de a Fase 4 existir (PERS-03)"
    - "O agente NAO roda a varredura do censo: ela passa de 10 minutos. As tasks sao provadas com paginas construidas a mao, e o material real e do portao humano de fim de fase"
  artifacts:
    - path: "tools/gerar_observacoes_do_censo.py"
      provides: "o replay que produz o CSV real para a conferencia humana de fim de fase, com a saida obrigatoria e a pasta de producao recusada"
      contains: "GRAVACOES_DO_CENSO"
    - path: "tests/test_gerar_observacoes_do_censo.py"
      provides: "a prova das costuras da ferramenta sem tocar `recordings/` e sem OCR"
      contains: "tmp_path"
  key_links:
    - from: "tools/gerar_observacoes_do_censo.py"
      to: "l2scanner/mercado_registro.py"
      via: "grava cada `LinhaLida` da pagina aceita pelo registro, com o carimbo por parametro"
      pattern: "registrar"
    - from: "tools/gerar_observacoes_do_censo.py"
      to: "l2scanner/__main__.py"
      via: "monta o registro pela funcao de montagem do 03-02, para o aviso alto ser o MESMO texto que a Fase 4 vai mostrar"
      pattern: "montar_registro_de_mercado"
    - from: "tools/gerar_observacoes_do_censo.py"
      to: "tools/medir_oclusao.py"
      via: "importa a lista NOMEADA das 8 gravacoes do censo em vez de redigita-la"
      pattern: "GRAVACOES_DO_CENSO"
---

<objective>
Fechar o buraco que a fase tem por desenho: **ela nao tem consumidor de producao**, e por isso
os criterios de sucesso 1, 2, 3 e 5 do ROADMAP — que sao todos "o usuario abre, importa, conta e
ve" — nao teriam sobre o que acontecer.

`LeitorDePagina` nao tem instanciador em `l2scanner/` e o modo `--mercado` e DETC-02, Fase 4.
Entao o arquivo que o usuario vai importar no Google Sheets sera produzido por **replay**, e nao
por sessao ao vivo. Isto e uma consequencia declarada do escopo, nao um esquecimento — e este
plano entrega o caminho pelo qual o usuario produz esse arquivo.

A ferramenta e de `tools/`, que e onde este repositorio ja poe o codigo que passa gravacao real
pelo pipeline para PRODUZIR DADO — `medir_oclusao.py`, `medir_agrupamento_de_nome.py`,
`medir_brilho_da_quantidade.py`. Ela nao e o modo `--mercado`: nao captura tela, nao roda no
laco, nao acrescenta flag ao scanner e nao toca `l2scanner/rastreador.py` nem o gate de brilho
da barra propria em `l2scanner/visao.py`.

**E ela e tambem a unica forma de o usuario VER o aviso alto do PERS-03 nesta fase.** Como o
mercado ainda nao tem chamador no scanner, "a feature desligou e os alertas continuam" nao tem
onde acontecer — a ferramenta monta o registro pela MESMA funcao do 03-02, entao o texto que o
usuario ve quebrando a saida de proposito e byte a byte o texto que a Fase 4 vai mostrar.

Purpose: sem este plano, quatro dos cinco criterios da fase ficam sem material, e a decisao
travada do separador `;` — que o REQUIREMENTS.md manda TESTAR com importacao real, "um CSV que
abre com tudo numa coluna so e pior que nenhum" — continuaria presumida.

Output: `tools/gerar_observacoes_do_censo.py`, seus testes, e os dois roteiros de conferencia
humana que o verificador colhe no fim da fase.

**O AGENTE NAO RODA A VARREDURA DO CENSO.** Ela passa de 10 minutos, precisa do motor de OCR e
do `calibration.json` da maquina do usuario, e ja derrubou dois agentes hoje. As duas tasks sao
provadas com paginas construidas a mao, em `tmp_path`, sem OCR e sem `recordings/`. Quem roda o
censo e o usuario, no portao.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-CONTEXT.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-RESEARCH.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-PATTERNS.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-01-SUMMARY.md
@.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A ferramenta de replay que produz o CSV real, e a costura testavel dela</name>
  <precondition>O 03-01 e o 03-02 ja entregaram `RegistroDeObservacoes` e `montar_registro_de_mercado`, e `python -m pytest tests/test_mercado_registro.py -q` esta verde. A ferramenta usa os dois e nao reimplementa nenhum dos dois. `recordings/` existe apenas no checkout PRINCIPAL, e e somente-leitura — se voce estiver num worktree, os testes desta task continuam valendo porque nenhum deles toca aquela pasta.</precondition>
  <files>tools/gerar_observacoes_do_censo.py, tests/test_gerar_observacoes_do_censo.py</files>
  <read_first>
    - `tools/medir_oclusao.py:1-100` — o cabecalho que explica por que o conjunto de medicao e
      FECHADO, a constante `GRAVACOES_DO_CENSO`, e o `RAIZ` derivado do proprio arquivo
    - `tools/medir_oclusao.py:709-760` — a forma do `main(argv)`: `argparse`, os caminhos por
      parametro, e o codigo de saida diferente de zero quando o resultado nao serve
    - `tests/test_mercado_replay.py:196-260` — `_novo_rastreio`, `montar` e
      `replay_de_uma_janela`: a fiacao minima de `LeitorDePagina`, que e de cinco linhas
    - `tests/test_mercado_replay.py:626-700` — o laco do censo: um leitor POR GRAVACAO, o
      `sorted` sobre `frame_*.png`, e por que a memoria de frame nao atravessa gravacoes
    - `l2scanner/mercado_pagina.py:190-230` — a assinatura de `LeitorDePagina.__init__` e o que
      cada um dos cinco parametros e
    - `l2scanner/ocr.py:153-195` — `disponivel()`, `motivo_indisponivel()`, `ler_texto` e
      `ler_texto_ampliado`, que sao o caminho publico do OCR
    - `l2scanner/relogio.py:80-95` e `:146-163` — o construtor sem argumentos e o `agora()`
    - `l2scanner/__main__.py`, a `montar_registro_de_mercado` que o 03-02 escreveu
    - `03-RESEARCH.md`, secao "Armadilha 5" — por que esta fase nao tem consumidor de producao
  </read_first>
  <behavior>
    - A costura de gravacao recebe uma sequencia de paginas aceitas, um registro e um relogio, e
      devolve a contagem de observacoes gravadas, duplicadas e series distintas
    - Duas paginas identicas passadas na mesma chamada produzem uma observacao e uma duplicada
    - Rodar a costura duas vezes sobre o MESMO registro nao aumenta a contagem de linhas do
      arquivo
    - A lista de gravacoes vem da constante importada das ferramentas de medicao, e um teste
      afirma que ela e a MESMA tupla, nao uma copia redigitada
    - Uma pasta de gravacoes ausente termina com codigo diferente de zero e mensagem legivel, e
      nunca com traceback
    - O motor de OCR indisponivel termina com codigo diferente de zero e imprime o motivo que o
      proprio modulo de OCR sabe dar
    - O relatorio final imprime frames lidos, paginas aceitas, observacoes gravadas, duplicadas
      descartadas, series distintas e o caminho do arquivo
  </behavior>
  <action>
Criar `tools/gerar_observacoes_do_censo.py`.

A docstring do modulo abre dizendo o que ele NAO e, porque essa e a confusao previsivel: ele nao
e o modo `--mercado`, que e DETC-02 da Fase 4. Ele le gravacoes de disco, nao captura tela, nao
roda no laco do scanner e nao acrescenta flag nenhuma ao scanner. Ele existe porque a Fase 3
entrega o registro SEM chamador, e mesmo assim os criterios de sucesso da fase pedem que o
usuario abra, importe, conte e veja um arquivo com dado de verdade.

A docstring diz tambem como rodar, no molde de `medir_oclusao.py`: no checkout PRINCIPAL, porque
`recordings/` e gitignored e nao se materializa em worktree, e com o interpretador que tem o
motor de OCR.

A lista de gravacoes vem IMPORTADA: acrescentar a pasta `tools/` ao caminho de import e ler
`GRAVACOES_DO_CENSO` de `medir_oclusao`, exatamente como `tests/test_mercado_replay.py:632-645`
ja faz, com a mesma razao no comentario — redigitar a lista criaria uma segunda verdade sobre o
mesmo conjunto, e um glob sobre `recordings/` arrastaria as 8 pastas que estao FORA do censo,
incluindo uma com 1.502 imagens de outro dia.

Argumentos, com `argparse`: a pasta de gravacoes e o arquivo de calibracao com padrao derivado
da raiz do projeto, no molde de `medir_oclusao.py`; e a SAIDA, que e OBRIGATORIA e nao tem
padrao. Um argumento opcional de subconjunto permite rodar so uma ou duas gravacoes nomeadas,
para o portao humano nao precisar de dez minutos quando duas paginas bastam.

A saida nao ter padrao e deliberado e a razao vai escrita: uma linha derivada de replay carrega
o carimbo de AGORA sobre um preco que foi visto dias atras. Misturar isso com o registro ao vivo
seria exatamente a confusao entre bancada e producao que o CONTEXT proibe quando recusa colunas
de gravacao e de frame. A ferramenta RECUSA uma saida que resolva para a pasta de producao do
registro — comparando caminhos resolvidos, nao textos — e sai com codigo diferente de zero
explicando por que.

O corpo, na ordem: conferir que o motor de OCR responde, usando `disponivel()` e
`motivo_indisponivel()` do modulo de OCR, e sair cedo com o motivo impresso se nao responder —
o precedente esta em `medir_agrupamento_de_nome.py`. Carregar a calibracao. Montar o registro
pela funcao de montagem do 03-02, que e importada de `l2scanner.__main__` — nao construir
`RegistroDeObservacoes` a mao, porque o ponto e que o usuario veja o MESMO aviso alto que a Fase
4 vai mostrar. Se a montagem devolver `None`, sair com codigo diferente de zero. Montar um
`Relogio` do projeto, sem argumentos, e usar `agora()` — nunca uma chamada solta ao relogio do
sistema (D-16); comentar que sem ancora a hora e a do Windows, que e o caminho honesto que o
proprio modulo de relogio documenta.

Para cada gravacao do conjunto, um leitor POR GRAVACAO — nunca um so para todas — com a razao
copiada do analog: duas gravacoes seguidas nao sao ticks vizinhos de uma sessao, e carregar a
memoria de frame de uma para a outra afirmaria continuidade onde ha um corte. As leitoras de
texto sao `ler_texto` e `ler_texto_ampliado` do modulo de OCR, que e o caminho publico. O
catalogo em memoria e um dicionario vazio, como o analog faz.

Iterar os quadros em ordem, chamar o leitor, e para cada pagina aceita chamar a costura de
gravacao.

Isolar essa costura numa funcao propria, porque ela e a unica parte testavel sem OCR e sem
`recordings/`: ela recebe uma sequencia de paginas aceitas, o registro e o relogio, e devolve
uma contagem simples de observacoes gravadas, duplicadas e series distintas. Ela le SO
`pagina.linhas` — os campos `descartadas` e `motivos` da pagina aceita sao proibidos no CSV pela
decisao da Fase 2, e escrever a linha recusada misturaria descarte com dado.

O relatorio final vai para a saida padrao, no estilo dos outros `tools/`: frames lidos, paginas
aceitas, observacoes gravadas, duplicadas descartadas, series distintas, e o caminho do arquivo
produzido. Codigo de saida 0 quando gravou alguma coisa, diferente de zero quando nao gravou —
um arquivo vazio nao serve para o portao e sair com 0 esconderia isso.

Criar `tests/test_gerar_observacoes_do_censo.py`. Ele importa a ferramenta acrescentando
`tools/` ao caminho, no molde do analog, e NAO toca `recordings/`, NAO chama OCR e NAO roda o
censo. O que ele prova: a costura de gravacao sobre paginas construidas a mao em `tmp_path`; a
dedup entre duas execucoes da costura; a identidade da lista de gravacoes com a constante das
ferramentas de medicao; e as saidas de erro legiveis para pasta ausente.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gerar_observacoes_do_censo.py -x -q</automated>
    <human-check>
      **CRITERIOS 1, 2 e 3 DA FASE — a leitura a olho, a importacao real no Sheets e a
      contagem.** Este roteiro precisa de voce porque o Google Sheets nao tem linha de comando e
      porque o criterio 2 do ROADMAP diz, com todas as letras, "testado com importacao real, nao
      presumido".

      **O que ja foi construido:** o registro de observacoes, a montagem que desliga alto, o
      LEIAME em `.mercado/`, e a ferramenta de replay que produz um CSV com dado de verdade.

      **Passo 1 — produzir o arquivo.** No checkout PRINCIPAL (nao num worktree — `recordings/`
      nao se materializa la), rode a ferramenta apontando a saida para uma pasta de rascunho,
      NUNCA para `.mercado/`:
      `python tools/gerar_observacoes_do_censo.py --saida C:/temp/portao-fase3`
      Se dez minutos for demais, use o argumento de subconjunto para rodar so uma gravacao — o
      relatorio no fim diz quantas paginas e quantas observacoes sairam. Anote o numero de
      observacoes gravadas.

      **Passo 2 — abrir no editor (criterio 1).** Abra `observacoes.csv` no Bloco de Notas ou no
      VS Code. Voce deve conseguir dizer, so lendo: o que e cada coluna, qual e o preco total,
      qual e a quantidade, e quando aquilo foi visto. O preco esta em CENTESIMOS — `6200` e
      `62,00`. Confirme que NAO existe coluna de preco unitario. Se alguma coluna te obrigar a
      adivinhar, diga qual.

      **Passo 3 — a importacao real (criterio 2).** No Google Sheets: menu **Arquivo**, depois
      **Importar**, depois **Enviar** e escolha o arquivo. No dialogo, em separador, escolha
      **Personalizado** e digite `;`. Nao existe opcao pronta de ponto-e-virgula na lista — se
      voce procurar por ela, nao vai achar, e e por isso que o roteiro manda usar
      Personalizado. Confira que as colunas cairam certas, uma por coluna, e nao tudo colapsado
      numa so.

      **Passo 4 — a pergunta do sinal de mais (o unico risco ALTO da pesquisa).** Ainda no
      Sheets, procure uma linha cujo nome comece com `+` — o material do censo tem 15 nomes
      assim, como `+6 Agathion Alpha Hunter Sealed`. **A celula mostra o nome, ou mostra um
      erro?** Esta pergunta nao foi medida e a fonte que a levantou nao e autoritativa. Se
      aparecer erro, diga qual — a correcao sera na planilha ou numa coluna extra, NUNCA sujando
      o nome dentro do arquivo.

      **Passo 5 — a coluna do residuo.** Confira que existem celulas VAZIAS e celulas com `0`
      nessa coluna, e que elas nao viraram a mesma coisa. Vazio quer dizer "nao deu para medir";
      zero quer dizer "conferi e bateu".

      **Passo 6 — a contagem (criterio 3).** Rode a MESMA ferramenta uma segunda vez, com a
      MESMA saida e o mesmo subconjunto. O relatorio deve dizer que gravou ZERO observacoes
      novas e descartou todas como duplicadas, e o arquivo deve continuar com o mesmo numero de
      linhas de antes. Conte antes e depois.

      **O que reportar:** se os seis passos deram certo, "aprovado". Se algum falhou, diga qual
      passo, o que voce viu, e — no caso do passo 4 — como a celula do nome com `+` apareceu.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_gerar_observacoes_do_censo.py -q` verde
    - Existe teste que constroi duas paginas aceitas identicas a mao, passa pela costura de
      gravacao com um registro em `tmp_path`, e afirma com `assert` que a contagem devolve uma
      observacao gravada e uma duplicada
    - Existe teste que chama a costura duas vezes sobre o mesmo registro e afirma que o numero
      de linhas do arquivo nao mudou na segunda vez
    - Existe teste que afirma, com `assert`, que a lista de gravacoes da ferramenta e a MESMA
      tupla objeto de `medir_oclusao.GRAVACOES_DO_CENSO`
    - Existe teste que aponta a ferramenta para uma pasta de gravacoes inexistente e afirma que
      o codigo de saida e diferente de zero e que nenhuma excecao subiu
    - `python -c "import sys,pathlib; sys.path.insert(0,'tools'); import gerar_observacoes_do_censo as g; import inspect; p=inspect.signature(g.main).parameters; print('main ok')"` imprime `main ok`
    - A ferramenta nao acrescenta argumento nenhum ao scanner:
      `git diff --name-only` NAO lista `l2scanner/__main__.py` neste plano
    - A conferencia de que a ferramenta nao chama o relogio do sistema direto e por AST sobre as
      chamadas do modulo, para que um comentario nunca a invalide:
      `python -c "import ast,sys,inspect; sys.path.insert(0,'tools'); import gerar_observacoes_do_censo as g; a=ast.parse(inspect.getsource(g)); nomes={n.func.attr for n in ast.walk(a) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute)}; assert 'now' not in nomes, nomes; print('relogio do projeto')"` imprime `relogio do projeto`
  </acceptance_criteria>
  <done>Existe um comando que o usuario roda para transformar as gravacoes do censo num `observacoes.csv` de verdade, com relatorio no fim, e a costura de gravacao esta provada por teste sem tocar `recordings/` e sem OCR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: As guardas da saida e o caminho de falha — o aviso alto que o usuario consegue VER</name>
  <files>tools/gerar_observacoes_do_censo.py, tests/test_gerar_observacoes_do_censo.py</files>
  <read_first>
    - `l2scanner/__main__.py`, `montar_registro_de_mercado` e `configurar_log`, como o 03-02 os
      deixou — a ferramenta precisa das duas para que o aviso apareca no console
    - `l2scanner/mercado_registro.py`, `PASTA_DO_MERCADO` e `ARQUIVO_DE_OBSERVACOES`
    - `03-RESEARCH.md`, secao "Lacuna 5" — os cenarios de falha REPRODUTIVEIS no Windows e os
      que passam por acidente
    - `03-CONTEXT.md`, secao Falha — a definicao literal de "desligar alto"
    - `.planning/workstreams/mercado/ROADMAP.md`, os cinco criterios de sucesso da Fase 3
  </read_first>
  <behavior>
    - Uma saida que resolve para a pasta de producao do registro e RECUSADA antes de qualquer
      escrita, com codigo de saida diferente de zero e a razao impressa
    - A recusa acontece mesmo quando o caminho chega escrito de outro jeito — com barras
      diferentes, com maiusculas diferentes ou com um `..` no meio — porque a comparacao e sobre
      caminhos resolvidos
    - Uma saida ocupada por um ARQUIVO com o nome da pasta faz a montagem devolver `None`, a
      ferramenta imprime o aviso alto e sai com codigo diferente de zero, sem traceback
    - Nesse caso o aviso que aparece no console e o mesmo texto que a montagem emite, incluindo
      a frase que diz o que continua funcionando
    - A ferramenta instala o log do projeto antes de montar o registro, para que o `log.error`
      da montagem chegue ao console e ao arquivo
  </behavior>
  <action>
Fechar as guardas de `tools/gerar_observacoes_do_censo.py`.

A guarda da saida vem primeiro, antes de qualquer criacao de pasta: resolver o caminho pedido e
o caminho de producao do registro e recusar se forem o mesmo, ou se o pedido estiver DENTRO do
de producao. Comparar caminhos RESOLVIDOS e nao texto — no Windows a mesma pasta chega escrita
de tres jeitos, e uma comparacao de string aprovaria dois deles. A mensagem de recusa diz a
razao inteira, nao so "recusado": uma linha derivada de replay carrega o carimbo de agora sobre
um preco visto dias atras, e o registro de producao e dado acumulado e sem desfazer.

Depois, instalar o log do projeto pelo `configurar_log`, antes de montar o registro. Sem isso o
`log.error` da montagem nao teria manipulador e o usuario nao veria nada — e o criterio 5 e
literalmente "o usuario VE o aviso alto". E o que faz a metade console do D-13 valer tambem aqui
e nao so dentro do scanner.

Montar o registro; se vier `None`, a ferramenta ja tem o que precisa: a montagem imprimiu as
duas mensagens no console e no arquivo de log. A ferramenta acrescenta uma linha propria
dizendo que nao vai produzir arquivo nenhum e sai com codigo diferente de zero. Nao repetir o
texto da montagem — duas versoes da mesma frase e como elas divergem.

Nos testes, provar a guarda da saida com os tres jeitos de escrever o mesmo caminho, e provar o
caminho de falha com o NOME OCUPADO POR ARQUIVO, que a pesquisa mediu ser deterministico em todo
sistema operacional. Nao usar permissao sobre PASTA: a medicao mostrou que o bit e inerte sobre
pasta no Windows e o teste passaria por acidente.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gerar_observacoes_do_censo.py -x -q</automated>
    <human-check>
      **CRITERIO 5 DA FASE — o aviso alto de que o mercado desligou, e os alertas de party
      continuando.**

      **Declaracao honesta do que esta fase pode e nao pode provar aqui:** nesta fase o mercado
      NAO TEM CHAMADOR dentro do scanner — o modo `--mercado` e DETC-02, Fase 4. Entao "os
      alertas de party continuam" e verdadeiro por AUSENCIA DE ACOPLAMENTO: o scanner nem sabe
      que o registro existe. O que da para ver hoje e a outra metade, que e a que costuma
      falhar: o aviso alto, com o texto exato que a Fase 4 vai mostrar, saindo no console.

      **Passo 1 — a pasta ocupada.** Crie um ARQUIVO com o nome da pasta de saida (por exemplo,
      um arquivo de texto chamado `portao-quebrado`, sem extensao) e rode:
      `python tools/gerar_observacoes_do_censo.py --saida C:/temp/portao-quebrado`
      Voce deve ver, no console, DUAS linhas de erro: a primeira dizendo que o mercado desligou
      e por que, a segunda dizendo que morte, saida e ressurreicao seguem sendo detectadas e
      entregues. Nao deve aparecer traceback. O comando termina com codigo diferente de zero.

      **Passo 2 — o arquivo somente-leitura.** Numa pasta de saida que ja tem um
      `observacoes.csv` produzido antes, marque o arquivo como somente-leitura pelas
      propriedades do Windows e rode a ferramenta de novo. Mesmo resultado: aviso alto, sem
      traceback. Desmarque a permissao no fim.

      **Passo 3 — o cabecalho quebrado.** Abra um `observacoes.csv` produzido antes, troque o
      nome de uma coluna do cabecalho a mao, salve, e rode a ferramenta de novo. O mercado deve
      desligar alto em vez de escrever desalinhado. Confira depois, no editor, que **o arquivo
      nao foi alterado** — nenhuma linha nova entrou.

      **Passo 4 — o log.** Abra `logs/scanner.log` e confirme que as mesmas duas mensagens estao
      la. O criterio diz console E log, e as duas metades contam.

      **Passo 5 — o scanner de pe.** Rode o scanner normalmente, como voce ja faz, e confirme
      que nada mudou: ele sobe, detecta e entrega igual. Nesta fase isso e esperado por
      construcao, e o passo existe para pegar uma regressao acidental, nao para provar a fiacao
      — a fiacao e da Fase 4.

      **O que reportar:** "aprovado" se os cinco passos deram certo. Se algum falhou, diga qual,
      e cole as linhas que apareceram no console.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_gerar_observacoes_do_censo.py -q` verde
    - Existe teste que passa a pasta de producao do registro como saida e afirma com `assert`
      que o codigo de saida e diferente de zero e que nada foi escrito
    - Esse teste e parametrizado com pelo menos tres grafias do mesmo caminho — separador
      trocado, caixa trocada e um `..` no meio — e todas sao recusadas
    - Existe teste que afirma que uma saida DENTRO da pasta de producao tambem e recusada
    - Existe teste que cria um ARQUIVO com o nome da pasta de saida e afirma que o codigo de
      saida e diferente de zero, que nenhuma excecao subiu, e que ha registro de nivel `ERROR`
    - Esse teste afirma tambem, por substring, que uma das mensagens diz que morte, saida e
      ressurreicao seguem sendo detectadas e entregues — provando que a ferramenta usa a
      montagem do 03-02 e nao um texto proprio
    - Existe teste que afirma que a ferramenta chama a instalacao de log do projeto antes de
      montar o registro
  </acceptance_criteria>
  <reversibility rating="reversible">A ferramenta e de bancada: ela nao entra no caminho do scanner, nao acrescenta flag, e apagar o arquivo desfaz tudo que ela e. A unica escolha com alguma inercia e a recusa da pasta de producao como saida, e ela e uma guarda — afrouxa-la depois e trivial, apertar depois de o usuario ja ter misturado replay com dado ao vivo nao e.</reversibility>
  <done>A ferramenta recusa escrever no registro de producao, e uma saida quebrada de proposito faz o usuario ver, no console e no log, as duas mensagens exatas que a Fase 4 vai mostrar — sem traceback e com codigo de saida diferente de zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linha de comando -> processo (`--saida`) | O caminho de saida vem do usuario, e e o unico caminho desta fase que NAO e derivado da raiz do projeto |
| `recordings/` -> processo | Imagens em disco, entrada nao confiavel; a pasta e material insubstituivel do censo |
| processo -> planilha do usuario | O CSV produzido sera aberto num programa que interpreta o conteudo das celulas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Tampering | replay gravando no registro de PRODUCAO e misturando carimbo de agora com preco de dias atras | high | mitigate | `--saida` obrigatoria, sem padrao, e recusa por comparacao de caminhos RESOLVIDOS quando ela e, ou esta dentro da, pasta de producao. Teste parametrizado com tres grafias do mesmo caminho |
| T-03-15 | Tampering | ferramenta escrevendo dentro de `recordings/` e estragando material insubstituivel | high | mitigate | A pasta de gravacoes so e aberta para LEITURA; nenhuma escrita e feita sob ela, e a saida e um caminho separado e obrigatorio |
| T-03-16 | Spoofing | glob sobre `recordings/` arrastando as 8 pastas fora do censo e produzindo um arquivo que nao se compara com nenhum numero do projeto | medium | mitigate | Lista NOMEADA importada de `medir_oclusao.GRAVACOES_DO_CENSO`; teste afirma a identidade da tupla |
| T-03-17 | Repudiation | ferramenta terminando em silencio e o usuario achando que produziu dado | medium | mitigate | Relatorio obrigatorio na saida padrao e codigo de saida diferente de zero quando nada foi gravado ou quando a montagem falhou |
| T-03-18 | Tampering | injecao de formula em planilha por nome comecando em `=`, `+`, `-` ou `@` | medium | accept | Nao mitigado no arquivo, de proposito — sanear o nome sujaria o dado que o criterio 1 manda ser legivel. Vira PERGUNTA EXPLICITA do roteiro humano (passo 4 da Task 1), e a resposta decide se uma coluna extra volta a mesa COM medicao |
| T-03-19 | Denial of Service | varredura de 10 minutos consumindo o contexto do agente | medium | mitigate | O agente NAO roda o censo: as tasks sao provadas com paginas construidas a mao, sem OCR e sem `recordings/`. O censo e do usuario, no roteiro humano |
| T-03-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install neste plano.** `argparse`, `sys` e `pathlib` sao stdlib; `cv2` e o modulo de OCR ja estao na arvore desde a Fase 1 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**`tools/gerar_observacoes_do_censo.py` (NOVO):**
- `main(argv) -> int` com `argparse`: pasta de gravacoes, arquivo de calibracao, `--saida`
  obrigatoria e subconjunto opcional de gravacoes
- A costura testavel que recebe paginas aceitas, o registro e o relogio, e devolve a contagem
- A lista de gravacoes IMPORTADA de `medir_oclusao.GRAVACOES_DO_CENSO`
- A guarda que recusa a pasta de producao como saida

**`tests/test_gerar_observacoes_do_censo.py` (NOVO):** as costuras da ferramenta sobre
`tmp_path`, sem OCR, sem `recordings/` e sem rodar o censo

**Artefato de bancada produzido pelo USUARIO, nao pelo agente:** um `observacoes.csv` numa pasta
de rascunho, que e o material do portao de importacao no Sheets

**Nao produzido aqui, de proposito:** nenhuma alteracao em `l2scanner/`. Este plano nao toca o
scanner.
</artifacts_this_phase_produces>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

A tabela completa das 16 decisoes esta no `03-01-PLAN.md`, secao `<indice_de_decisoes>`, e os
IDs sao os mesmos. As citadas aqui:

| ID | Decisao (do `03-CONTEXT.md`) |
|---|---|
| D-04 | So o carimbo de tempo como proveniencia — nada de gravacao nem de frame, que sao artefato de bancada. E a razao de a saida do replay nao poder ser a pasta de producao. |
| D-06 | O mesmo anuncio visto em dias diferentes e UMA linha, com a data da PRIMEIRA vez. E o que o passo 6 do roteiro humano conta. |
| D-13 | "Desligar alto" e: aviso no console E no log, o mercado para, e os alertas de party continuam. |
| D-16 | O carimbo usa o RELOGIO ANCORADO do projeto, nunca `datetime.now()` solto. |
</indice_de_decisoes>

<suposicoes_da_sonda>
## As tres bordas da sonda, no que este plano fecha e no que declara

**PERS-01.** Este plano leva a borda ate onde ela pode ir: o arquivo existe, com dado real, e a
importacao e feita de verdade pelo usuario, com o roteiro exato. A SUPOSICAO A3 da pesquisa — a
celula que comeca com sinal de mais virando formula — continua NAO MEDIDA e vira a pergunta 4 do
roteiro. Ela nao virou codigo de saneamento de proposito: sujar o dado para agradar um
importador e o oposto do que o criterio 1 pede.

**PERS-02.** A dedup ja esta provada por teste no 03-01. Aqui ela ganha a prova de CAMPO que o
criterio 3 pede literalmente — "o usuario pode contar antes e depois e ver o mesmo numero" —
rodando a mesma ferramenta duas vezes. Isso tambem exercita a borda que o usuario aceitou em
2026-08-30: numa captura travada UMA pagina e aceita antes de o congelamento disparar, e a dedup
desta fase e o que impede aquilo de virar linha duplicada.

**PERS-03.** DECLARADO, e a declaracao esta dentro do proprio roteiro humano para o usuario ler:
nesta fase o mercado nao tem chamador no scanner, entao "nunca derruba os alertas" e verdadeiro
por ausencia de acoplamento, e nao por um teste de ponta a ponta com o mercado ligado. O que da
para conferir hoje e o aviso alto — e ele e conferido com o TEXTO da montagem, nao com um texto
proprio da ferramenta, justamente para que o que o usuario ve aqui seja o que ele vai ver na
Fase 4. A ponta a ponta e portao da Fase 4, quando DETC-02 ligar o modo.
</suposicoes_da_sonda>

<verification>
- `python -m pytest tests/test_gerar_observacoes_do_censo.py -q` verde
- `python -m pytest tests/test_mercado_registro.py tests/test_mercado_replay.py -q` verde
- `python -m pytest tests/test_mercado_27x.py tests/test_firewall_escopo.py -q` verde
- `git diff --name-only` NAO lista nada dentro de `l2scanner/` neste plano, e NAO lista
  `l2scanner/rastreador.py` nem `l2scanner/visao.py` em fase nenhuma
- `git status --short` NAO lista `calibration.json` nem nada dentro de `recordings/`
- Suite completa: `python -m pytest -q --ignore=tests/test_agenda.py` e depois
  `python -m pytest tests/test_agenda.py -q`. FLAKE CONHECIDO em `tests/test_agenda.py:1141`:
  **abortar nao e falhar — rode de novo.**
- **O AGENTE NAO RODA A VARREDURA DO CENSO.** Ela nao e gate de task nenhuma deste plano. Quem
  a roda e o usuario, no roteiro humano de fim de fase, no checkout principal
</verification>

<success_criteria>
Existe um comando que produz um `observacoes.csv` com dado real a partir das gravacoes do censo,
com relatorio no fim; ele recusa escrever no registro de producao; e com a saida quebrada de
proposito ele mostra no console e no log as duas mensagens exatas do desligamento alto. Os dois
roteiros de conferencia humana estao escritos e sao colhidos no fim da fase.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/03-persist-ncia-de-observa-es/03-03-SUMMARY.md` when done
</output>
