---
phase: 05-a-aba-adena-e-a-taxa-de-cambio
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - l2scanner/mercado_console.py
  - l2scanner/mercado_modo.py
  - tests/test_mercado_console.py
  - tests/test_mercado_registro_adena.py
  - .planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/deferred-items.md
autonomous: true
requirements: [ADEN-03, ADEN-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A série da Adena sai no console como XM por MILHÃO de adena, marcada como derivada, com `n` e recência."
    - "A série de negociação continua saindo por unidade, byte a byte como hoje."
    - "`mercado_analise` não ganhou uma linha: a taxa continua sendo `Fraction(total, quantidade)`, exata e sem saber de aba nenhuma."
    - "Uma `LinhaLida` da Adena atravessa `mercado_registro` até o CSV e volta — cabeçalho e seis colunas inalterados, `VERSAO_DO_ESQUEMA` intacta (ADEN-03 VERIFICADO, não argumentado)."
    - "O defeito `135,00 -> 13588` está registrado com os números, e NÃO foi consertado nesta fase."
  artifacts:
    - l2scanner/mercado_console.py
    - tests/test_mercado_registro_adena.py
    - .planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/deferred-items.md
  key_links:
    - "`chave_da_serie` -> qual formatador. É o único critério, e ele é o mesmo que o 05-01 gravou (D-A)."
---

<objective>
A exibição aprende UMA coisa sobre a aba Adena — que ali a unidade útil é o milhão — e
`mercado_analise` não aprende nada.

Purpose: `round(Fraction(11600, 10_000_000))` vale **zero**. A taxa por unidade não é
pequena: ela é INEXIBÍVEL em centésimos, e chamar o formatador errado imprimiria `0,00`
com toda a confiança do mundo. Por isso a função da Adena é IRMÃ e não um parâmetro com
default. E `mercado_analise` continua puro: um `if` de aba naquele módulo contaminaria
menor pedido, mediana e tendência para sempre.

Output: `UNIDADE_DA_TAXA`, `formatar_taxa_derivada`, a escolha por `chave_da_serie` nos
quatro pontos de chamada, e a dívida do `13588` registrada com os números.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/mercado/REQUIREMENTS.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-CONTEXT.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-RESEARCH.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-01-SUMMARY.md
</context>

<interface_context>
- `mercado_catalogo.CHAVE_DA_SERIE_DA_ADENA` (do 05-01). O console já alcança
  `mercado_catalogo` sem aresta de import nova: `mercado_console` -> `mercado_registro`
  -> `mercado_catalogo` (`mercado_registro.py:70`, import de nível de módulo).
- `mercado_console.formatar_centesimos(centesimos: int) -> str` (`:214`)
- `mercado_console.formatar_unitario_derivado(unitario: Fraction) -> str` (`:226`)
- Os quatro pontos de chamada: `:267` e `:269` (dentro de `destaque_ao_vivo`), `:443`
  (`_linha_do_menor`), `:476` (`_linha_da_mediana`)
- `mercado_console.TravaDoDestaque.anunciar(linha, destaque, agora)` (`:328`) — tem
  `linha.chave_da_serie` na mão
- `mercado_console.secao_do_vale_quanto(modelo, watchlist, agora, *, relogio_confiavel)`
  (`:480`) — o laço tem `serie.chave` na mão
- `mercado_analise.unitario()` -> `Fraction(total_em_centesimos, quantidade)` (`:178-206`)
  — **não se toca**
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XM por milhão, escolhido pela chave da série</name>
  <files>l2scanner/mercado_console.py, l2scanner/mercado_modo.py, tests/test_mercado_console.py</files>
  <read_first>
    `l2scanner/mercado_console.py:214-241` — `formatar_centesimos` e
    `formatar_unitario_derivado`, com a razão da marca de derivado escrita por extenso. A
    irmã nova herda essa razão inteira.

    `l2scanner/mercado_console.py:414-478` — `_linha_do_menor` e `_linha_da_mediana`, com
    a disciplina do `n` e do carimbo. Ela não muda: o requisito ADEN-04 pede `n` e
    recência, e as duas já os carregam.

    `l2scanner/mercado_console.py:480-538` — `secao_do_vale_quanto`, onde `serie.chave`
    está disponível no laço.
  </read_first>
  <behavior>
    - `formatar_taxa_derivada(Fraction(11600, 10_000_000))` devolve exatamente
      `"11,60 XM por milhao de adena (derivado)"`.
      **Confira a conta antes de escrever a docstring:** `Fraction(11600, 10_000_000)` é
      centésimos por adena; vezes `1.000.000` dá `1160` centésimos, e `formatar_centesimos(1160)`
      é `11,60`. O exemplo de código do `05-RESEARCH.md` escreve `116,00` nessa linha e
      **está errado por um fator de dez** — o ROADMAP e o CONTEXT trazem a conta certa
      (`10.000.000 adena por 116,00 XM -> 11,60 XM por milhão`). Não copie o número da
      pesquisa; recalcule.
    - `formatar_unitario_derivado(Fraction(11600, 10_000_000))` devolve
      `"0,00 por unidade (derivado)"`. Este teste existe para tornar EXECUTÁVEL o perigo
      que justifica as duas funções serem irmãs: o formatador errado não erra feio, erra
      zero.
    - `secao_do_vale_quanto` sobre um modelo com DUAS séries — uma de negociação e uma da
      Adena — produz um texto em que a linha do menor pedido da Adena contém
      `"11,60 XM por milhao de adena (derivado)"` e a linha do menor pedido da negociação
      contém `"por unidade (derivado)"`. É o par que discrimina: um teste que exercitasse
      só a Adena passaria com a escolha trocada em qualquer sentido.
    - A linha da Adena descreve a quantidade em adena e não em "unidades".
    - `TravaDoDestaque.anunciar` sobre uma `LinhaLida` da Adena produz um bloco cuja
      moldura contém a taxa em XM por milhão.
    - A mediana da Adena sai na mesma unidade do menor pedido — os dois números da mesma
      série não podem sair em unidades diferentes.
  </behavior>
  <action>
    Em `l2scanner/mercado_console.py`:

    `UNIDADE_DA_TAXA = 1_000_000`, com a razão ao lado: a unidade útil da taxa é o milhão,
    e essa é a ÚNICA coisa que a exibição precisa saber sobre a aba Adena.

    `formatar_taxa_derivada(taxa: Fraction) -> str`, irmã de `formatar_unitario_derivado`.
    A docstring diz por que é irmã e não um parâmetro com default, com a conta do `0,00`
    escrita, e carrega a marca `(derivado)` pela mesma razão da outra: sem ela alguém
    copia a linha para o WhatsApp e o número derivado vira "o que o scanner leu". O
    arredondamento acontece SÓ aqui, sobre a `Fraction` exata — a comparação entre ofertas
    já aconteceu e aconteceu sem perder um bit.

    `formatador_do_unitario(chave_da_serie: str)`, que devolve uma das duas funções. UM
    ponto de decisão, e não quatro `if` espalhados: quatro divergiriam, e um deles
    imprimiria `0,00` um dia. O critério é a `chave_da_serie` comparada a
    `CHAVE_DA_SERIE_DA_ADENA` (D-A).

    `descrever_a_quantidade(chave_da_serie: str, quantidade: int) -> str`, pelo mesmo
    critério: `"6 unidades"` para negociação (mantendo o singular de hoje) e
    `"10.000.000 de adena"` para a Adena. `_linha_do_menor` hoje monta essa frase inline;
    ela sai para a função e passa a ser uma verdade só.

    `_linha_do_menor` e `_linha_da_mediana` recebem a `chave_da_serie` da série; o laço de
    `secao_do_vale_quanto` já a tem em `serie.chave`. `destaque_ao_vivo` recebe a chave
    junto do nome; `TravaDoDestaque.anunciar` já tem `linha.chave_da_serie` na mão.
    Atualize o teste existente que chama `destaque_ao_vivo` direto.

    A frase de "nenhuma observação ainda" hoje manda abrir o World Exchange na aba de
    negociação; ela passa a citar as duas abas que o scanner sabe ler.

    Em `l2scanner/mercado_modo.py`: a recusa de subir por `mercado_grade.layout` continua
    exatamente como está — a negociação segue sendo a grade de topo e a Adena mora
    aninhada. O que muda é só o TEXTO da mensagem, que hoje afirma que o v1 lê somente a
    grade de negociação e passaria a mentir. Diga o que é verdade depois desta fase: a
    grade de TOPO tem de ser a de negociação, e a Adena entra por `mercado_layouts`. Uma
    string; nenhuma lógica.

    `mercado_analise` e `mercado_registro` não são abertos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_console.py -x -q</automated>
  </verify>
  <done>
    `python -m pytest tests/test_mercado_console.py tests/test_mercado_modo.py tests/test_mercado_analise.py -q` passa,
    e a suíte contém o par que rende `"11,60 XM por milhao de adena (derivado)"` para a
    série da Adena e `"por unidade (derivado)"` para a de negociação no MESMO texto.
  </done>
</task>

<task type="auto">
  <name>Task 2: a dívida do 13588, registrada com os números e não consertada aqui</name>
  <files>.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/deferred-items.md</files>
  <read_first>
    `.planning/workstreams/mercado/milestones/v1-mercado-phases/04-modo-mercado-an-lise-e-console/deferred-items.md`
    — o formato da casa: sintoma, causa lida no fonte, por que NÃO foi consertado aqui, e
    o preço exato de consertar depois.
  </read_first>
  <action>
    Crie o `deferred-items.md` da Fase 5 com os itens abaixo. Cada um com os NÚMEROS, não
    com a impressão.

    **(1) `135,00` lido como `13588` — dívida do v1, achada agora, e atinge a negociação
    também.** Em `tests/fixtures/mercado/janela_adena_f014.png`, linha 5, a tela diz
    `135,00` e `ler_celula_de_numero` devolve `13588`. É o par `0`x`8`, a margem mais
    estreita do sistema (0,0370), na linha DESTACADA (Vmax 255 contra 226-230 das linhas
    normais; no piso 180 os traços saem mais grossos e o miolo do `0` estreita). Nenhuma
    peneira de hoje o pega: a gramática aceita `135,88`; `linha_ocluida` é `False` nas dez
    linhas; o acordo entre dois frames CONCORDA no erro, porque a linha continua destacada
    no frame seguinte; e a guarda de cruzamento está desligada
    (`mercado_tolerancia_do_cruzamento: None`, reprovada por medição no 02-02). Escreva
    que a Fase 5 o pega **só na Adena**, pela guarda do 05-01, e que na aba de negociação
    ele **continua ativo**. O que falta medir: se a aba de negociação também destaca linhas
    assim — a fixtura de negociação não tem nenhuma linha destacada (Vmax 215/226/230
    uniformes, conferido). Por que não foi consertado aqui: o escopo desta fase é a Adena,
    e consertar na negociação exigiria remedir a tolerância do cruzamento, que é a medição
    do 02-02 inteira.

    **(2) O caso de arredondamento não tem pixel no repositório (A3).** O par
    `133,33 / 66,66` vem da captura do usuário de 2026-09-01 17h e NÃO está versionado. O
    critério `residuo <= n x 0,5` o aceita por margem ZERO (resíduo 1 contra limite 1,0), e
    todas as 9 linhas boas de `janela_adena_f014.png` dividem exato — nenhuma delas
    exercita esse ramo sobre pixels. Escreva que o ramo é **inferência aritmética
    verificada em teste de unidade, e não medição sobre pixels**, e o que o fecharia: uma
    gravação curta da aba Adena com a coluna `5 mln increment` ordenada, contendo uma linha
    cujo incremento não divide o total exatamente. E a consequência de errar: a suspeita já
    registrada no fonte (`mercado_leitura.py:1042-1049`) é que o cliente TRUNCA em vez de
    arredondar, o que dobraria o limite — e essa mudança tocaria a negociação também.

    **(3) A `Auction List` continua não sendo lida.** O que custaria: um segundo conjunto
    de 12 moldes de dígito cortados daquela célula (10 dígitos + `,` + a palavra `Adena`),
    mais uma chave opcional. Não é ajuste de piso: a varredura 180/200/210/220/230/240/250
    não achou vale entre a população em que o glifo está gordo (5-6 px contra os 4 dos
    moldes) e aquela em que ele já se partiu. Consequência de não ler: ofertas de adena que
    não sejam múltiplo de 5.000.000 são recusadas — falha fechada, perde dado, não inventa
    (A2, sem contraexemplo até hoje).

    **(4) A aba `busca`** (9 linhas) continua fora, e o `--layout busca` do calibrador
    grava geometria que nenhuma leitora consome.

    Nada aqui vira código nesta fase.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; p=pathlib.Path('.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/deferred-items.md'); t=p.read_text(encoding='utf-8'); f=[n for n in ('13588','0,0370','133,33','66,66','janela_adena_f014.png','mercado_tolerancia_do_cruzamento') if n not in t]; print('FALTAM:',f); sys.exit(1 if f else 0)"</automated>
  </verify>
  <done>
    O arquivo existe e cita, por escrito, os seis marcadores que o comando de verificação
    procura — cada um deles um número ou um nome de arquivo MEDIDO, e não uma impressão.
    Os quatro itens estão lá, cada um com sintoma, causa, por que não foi consertado aqui
    e o preço de consertar depois.
  </done>
</task>

<task type="auto">
  <name>Task 3: a taxa chega ao CSV, e isso é MEDIDO e não argumentado</name>
  <files>tests/test_mercado_registro_adena.py</files>
  <read_first>
    `l2scanner/mercado_registro.py:125-215` — `COLUNAS` (as seis), `chave_da_observacao`
    (`(série, total, quantidade)`) e `campos_da_observacao`. É o contrato que o ADEN-03
    promete não mexer.

    `l2scanner/mercado_registro.py:383-512` e `:795-880` — `RegistroDeObservacoes` e a
    leitura de volta, com o cabeçalho-contrato e `ContratoDoArquivoQuebrado`.

    `tests/test_mercado_registro.py` — o idioma da casa para dirigir o registro contra
    `tmp_path`. Este arquivo é irmão dele.
  </read_first>
  <action>
    O ADEN-03 é hoje a única promessa da fase garantida por ARGUMENTO: o 05-02 para na
    `PaginaAceita` e este plano só formata. "O esquema já comporta a taxa" é uma
    afirmação sobre código que ninguém executou com uma linha de Adena dentro. Este
    arquivo novo a transforma em medição, e nenhum módulo de produção é aberto.

    Casos, todos com o registro apontado para dentro de `tmp_path` — **nada é escrito em
    `.mercado/`**:

    1. **Ida e volta.** Registre uma `LinhaLida` com a sentinela, `total_em_centesimos`
       `11600` e `quantidade` `10_000_000`. Leia o arquivo de volta e afirme que a
       observação voltou com os três campos idênticos.
    2. **O cabeçalho-contrato não mudou.** A primeira linha do arquivo é exatamente a que
       `COLUNAS` produz, e são SEIS colunas. Compare com o cabeçalho gerado a partir de
       `COLUNAS`, não com uma string escrita à mão — uma cópia à mão validaria a si mesma.
    3. **O par que discrimina.** Grave, no MESMO arquivo, uma observação de negociação
       (chave de item, quantidade pequena) e uma da Adena. As duas voltam, e o arquivo
       continua com um cabeçalho só. Sem este caso, o caso 2 passaria num arquivo que só
       vê Adena.
    4. **A dedup vale para a taxa.** Duas observações com a MESMA sentinela, o mesmo total
       e a mesma quantidade viram UMA; a mesma sentinela com total diferente vira DUAS. É
       o que faz duas passadas sobre a mesma página não inflarem a mediana da taxa.
    5. **A taxa exata sobrevive ao disco.** Sobre a observação lida de volta,
       `mercado_analise` produz `Fraction(11600, 10_000_000)` — igualdade de `Fraction`, e
       não comparação de `float`. É o que prova que o unitário derivado nasce do que o
       CSV guardou, e não do que a memória lembrava.
    6. **A versão do esquema não subiu.** Afirme o valor de `VERSAO_DO_ESQUEMA` contra o
       que o v1 gravou, para que um bump futuro caia aqui e não no `calibration.json` do
       usuário.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_registro_adena.py tests/test_mercado_registro.py -x -q</automated>
  </verify>
  <done>
    Os seis casos passam. `l2scanner/mercado_registro.py` e `l2scanner/mercado_analise.py`
    saem desta task sem uma linha alterada — o `git status` os mostra limpos —, o que é a
    forma mais forte de afirmar que o esquema já comportava a taxa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Fraction` exata -> texto arredondado | o único ponto em que a precisão é perdida, e ele é de EXIBIÇÃO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | `formatador_do_unitario` escolhendo a função errada | high | mitigate | A taxa por unidade arredonda para `0,00`: o erro não é feio, é zero, e é convincente. Um ponto de decisão só, e o par de testes que rende as DUAS strings no mesmo texto. |
| T-05-08 | Repudiation | número derivado copiado como se fosse lido | medium | mitigate | A marca `(derivado)` colada ao texto, pela mesma regra já escrita em `formatar_unitario_derivado`: o CSV guarda `total` e `quantidade`, a taxa é derivação, e o rótulo tem de dizer isso. |
| T-05-09 | Tampering | `mercado_analise` ganhando um `if` de aba | high | mitigate | O módulo não é aberto neste plano. `tests/test_mercado_analise.py` roda na verificação como guarda. |

**Sem instalação de pacote nesta fase** (FIRE-01).
</threat_model>

<verification>
```
python -m pytest tests/test_mercado_console.py -q
python -m pytest tests/test_mercado_registro_adena.py -q
python -m pytest tests/test_mercado_modo.py tests/test_mercado_analise.py tests/test_mercado_registro.py -q
```
A segunda linha prende as fronteiras que este plano promete não cruzar: a análise
continua pura e o CSV continua com as mesmas seis colunas.
</verification>

<success_criteria>
- `formatar_taxa_derivada(Fraction(11600, 10_000_000))` devolve `"11,60 XM por milhao de adena (derivado)"`, afirmado por teste.
- No MESMO texto de `secao_do_vale_quanto`, a série da Adena sai em XM por milhão e a de negociação sai por unidade.
- `mercado_analise.py` e `mercado_registro.py` não foram modificados.
- Uma `LinhaLida` da Adena faz a ida e volta pelo `mercado_registro` em `tmp_path`, com seis colunas e o cabeçalho-contrato intactos.
- `VERSAO_DO_ESQUEMA` do CSV não mudou; nenhuma coluna nova.
- O `deferred-items.md` da fase existe com os quatro itens e os números.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-03-SUMMARY.md` when done
</output>