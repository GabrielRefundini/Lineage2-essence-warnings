---
phase: 05-a-aba-adena-e-a-taxa-de-cambio
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - l2scanner/calibracao.py
  - l2scanner/mercado_pagina.py
  - tests/fixtures/mercado/calibracao_de_fixture.json
  - tests/test_mercado_adena_pagina.py
autonomous: true
requirements: [ADEN-01, ADEN-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Um `calibration.json` SEM `mercado_layouts` produz leitura de negociação idêntica à de hoje — a chave é opcional e a ausência é o estado normal."
    - "`pecas_de_calibracao_de_mercado_faltando` continua com QUINZE itens: quem nunca calibrou a Adena continua com o `--mercado` subindo."
    - "O portão de layout devolve o NOME do layout vencedor, e empate não é veredito."
    - "`janela_adena_f014.png` lida ponta a ponta produz 9 linhas de taxa e descarta a linha 5 com motivo `cruzamento`."
    - "A coluna `Auction List` não é recortada em lugar nenhum do caminho da Adena."
  artifacts:
    - l2scanner/calibracao.py
    - l2scanner/mercado_pagina.py
    - tests/fixtures/mercado/calibracao_de_fixture.json
    - tests/test_mercado_adena_pagina.py
  key_links:
    - "`_casamento_do_layout` -> `_ler_a_pagina`: o layout vencedor escolhe o modelo de coluna E a leitora de linha. Errar aqui corrompe a série por fator inteiro (ADEN-02)."
    - "`mercado_layouts.adena.grade` herda `dx`/`dy`/`altura_da_linha` de `mercado_grade` (D-D) — uma verdade só sobre a geometria."
---

<objective>
As duas grades convivem no `calibration.json`, e o portão de layout deixa de só RECUSAR e
passa a ESCOLHER.

Purpose: hoje `_casamento_do_layout` devolve `bool` e conhece UM molde só. O vão entre
Adena e negociação já está medido e é de oito décimos (`0,1331` contra `1,0000`), então o
discriminador existe — falta ele dizer QUEM ganhou. E a chave nova tem de entrar opcional:
obrigatória, ela deixaria o scanner morto no próximo arranque de todo usuário que nunca
calibrou a Adena.

Output: `Calibracao.mercado_layouts`, a matriz de casamento MEDIDA nos dois sentidos, o
portão que escolhe, o modelo de coluna próprio da Adena e a fixtura que prova tudo isso
sobre pixels versionados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/REQUIREMENTS.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-CONTEXT.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-RESEARCH.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-01-SUMMARY.md
</context>

<interface_context>
Do 05-01 (já no fonte quando este plano roda):

- `mercado_leitura.ADENA_POR_INCREMENTO`, `quantidade_de_adena(total, incremento) -> tuple[int, int] | None`
- `mercado_leitura.ler_linha_de_adena(indice, bgr_da_linha, recorte_do_total, recorte_do_incremento, *, moldes, piso, margem, valor_minimo_do_numero, folga_de_cola, sonda, limiar_de_dispersao, catalogo) -> LinhaLida | Descarte | None`
- `mercado_catalogo.CHAVE_DA_SERIE_DA_ADENA`, `NOME_EXIBIDO_DA_ADENA`

Do fonte existente:

- `mercado_leitura.casamento_do_cabecalho(banda, molde, corte) -> float` (`:1876`) — a matriz das 4 bandas está na docstring dela
- `mercado_visao.cabecalho_de_calibracao(dict) -> np.ndarray` (`:791`) — levanta `ValueError` em entrada corrompida
- `mercado_pagina.pecas_de_calibracao_de_mercado_faltando(cal) -> list[str]` (`:110`) — QUINZE nomes
- `calibracao._conferir_uma_coluna(dados, chave)` (`:1095`), `_conferir_o_cabecalho_de_coluna(dados)` (`:1145`), `CONSERTO_DO_MERCADO`
- `calibrar_mercado.medir_o_corte_de_brilho_do_cabecalho(banda)`, `sugerir_o_molde_do_cabecalho(banda, layout, dy, corte)` (`:1521`), `CASAMENTO_MINIMO_DA_ANCORA`
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: a chave nova entra ANINHADA e OPCIONAL, e a lista das quinze não cresce</name>
  <files>l2scanner/calibracao.py, tests/test_mercado_adena_pagina.py</files>
  <read_first>
    `l2scanner/calibracao.py:228-265` — os três precedentes de campo opcional
    (`banner_manutencao`, `tiat_chat`/`tiat_alvo`, o bloco de mercado) e a razão de a
    `VERSAO_DO_ESQUEMA` seguir em 2, escrita por extenso.

    `l2scanner/calibracao.py:733-736` — o portão de versão. Ele recusa QUALQUER versão
    diferente da constante, e é por isso que subir para 3 apagaria os 13 moldes de glifo e
    as 3 âncoras que só a mão do usuário produz.

    `l2scanner/calibracao.py:1095-1250` — `_conferir_uma_coluna`,
    `_conferir_o_cabecalho_de_coluna` e `_conferir_a_sonda_do_fundo`: a disciplina de
    validação que o bloco aninhado tem de herdar, com `bool` excluído do `int` e toda
    mensagem terminando em `CONSERTO_DO_MERCADO`.

    `l2scanner/mercado_catalogo.py:145-160` — a razão, escrita ao lado de `PISO_DO_RESTO`,
    de uma chave nova nunca entrar como obrigatória.
  </read_first>
  <action>
    Campo `mercado_layouts: dict | None = None` na `Calibracao`, com o comentário no
    molde dos três precedentes acima: opcional, `VERSAO_DO_ESQUEMA` intacta em 2, ausência
    é o estado NORMAL (e não uma degradação — diferente da `mercado_folga_de_cola_do_glifo`,
    cuja falta custa 6,08% das linhas e por isso avisa alto). Aqui a ausência não avisa
    nada.

    Em `carregar`, `mercado_layouts=dados.get("mercado_layouts")`, ao lado das outras.

    Validação nova, `_conferir_os_layouts_de_mercado(dados)`, chamada de dentro de
    `_conferir_as_chaves_da_leitura_de_pagina`. Ela existe porque o `calibration.json` é
    entrada NÃO CONFIÁVEL e um `bytes` hex corrompido lá dentro faria
    `cabecalho_de_calibracao` levantar dentro do construtor do leitor — que é o caminho
    que vira feature OFF com aviso, e o usuário nunca leria a causa. Confere:

    - `mercado_layouts` ausente ou `None` passa sempre.
    - Presente, precisa ser dict; cada valor precisa ser dict.
    - `negociacao` NÃO pode aparecer como chave aninhada: ela mora no topo, e duas
      verdades sobre a mesma grade divergiriam. Mensagem explícita.
    - Cada bloco precisa de `cabecalho` (validado com a MESMA disciplina de
      `_conferir_o_cabecalho_de_coluna`: tipo, campos, faixa e contagem de bytes contra
      `altura * largura`) e de `limiar_do_cabecalho` em `(0, 1]`.
    - `colunas` precisa ser dict de `{"dx": int, "largura": int}` com `largura > 0`,
      pela disciplina de `_conferir_uma_coluna`. `dx` negativo é LEGÍTIMO — é
      deslocamento a partir da origem do painel, e a coluna do nome de negociação já é
      `-385`.
    - `grade`, quando presente, precisa ser dict; os campos ausentes são herdados de
      `mercado_grade` na leitura (D-D), e isso vai escrito aqui para o dia em que alguém
      procurar por que o bloco pode ser curto.

    Reaproveite os helpers existentes onde a forma for a mesma, extraindo o corpo comum
    em vez de copiar: duas validações parecidas sobre a mesma forma divergem, e é o
    argumento que este arquivo já faz duas vezes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena_pagina.py tests/test_calibracao_mercado.py tests/test_calibracao_generica.py -x -q</automated>
  </verify>
  <done>
    Um teste constrói uma `Calibracao` com TODOS os campos de mercado em `None` e afirma
    que `pecas_de_calibracao_de_mercado_faltando` devolve exatamente 15 nomes — a lista
    não cresceu. Outro teste grava um `mercado_layouts` com `bytes` de tamanho errado num
    JSON em `tmp_path` e afirma que `Calibracao.carregar` levanta `CalibracaoInvalida`
    citando o conserto, em vez de deixar o erro para o meio do tick. Um terceiro afirma
    que `mercado_layouts` ausente carrega sem erro e chega como `None`.
  </done>
</task>

<task type="auto">
  <name>Task 2: a matriz medida nos DOIS sentidos, e o portão que escolhe</name>
  <files>l2scanner/mercado_pagina.py, tests/fixtures/mercado/calibracao_de_fixture.json, tests/test_mercado_adena_pagina.py</files>
  <read_first>
    `l2scanner/mercado_pagina.py:633-706` — `_layout_confere`, `_casamento_do_layout` e
    `_banda_do_cabecalho`, inteiros. O latch de log (`_layout_ja_recusado`) e a razão dele
    — o veredito é ESTADO e não evento — continuam valendo e não se mexem.

    `l2scanner/mercado_leitura.py:1876-1901` — `casamento_do_cabecalho` e a matriz de hoje,
    medida SÓ com o molde de negociação. A metade que falta é o que esta task mede.

    `l2scanner/mercado_pagina.py:378-410` — onde o molde de cabeçalho é decodificado UMA
    VEZ, no arranque. Os moldes novos entram no mesmo lugar e pelo mesmo motivo: ~28 KB
    de hex por tick, 3.600 vezes por hora de farm, seria trabalho puro.
  </read_first>
  <action>
    **Primeiro MEÇA, depois escreva.** Rode um script descartável (em
    `%TEMP%`/scratchpad, não no repositório) que:

    (a) carrega `tests/fixtures/mercado/cabecalho_adena.png`, mede o corte com
    `calibrar_mercado.medir_o_corte_de_brilho_do_cabecalho` e produz o dict com
    `sugerir_o_molde_do_cabecalho(banda, "adena", 224, corte)`;

    (b) calcula `casamento_do_cabecalho` para as OITO combinações — os dois moldes
    (negociação, do `calibration.json` de fixtura; adena, o recém-cortado) contra as
    quatro bandas versionadas (`cabecalho_negociacao_goods.png`,
    `cabecalho_negociacao_unitprice.png`, `cabecalho_adena.png`, `cabecalho_busca.png`).

    Escreva a matriz de 8 números na docstring de `_casamento_do_layout`, no formato da
    que já existe em `casamento_do_cabecalho`.

    **PORTÃO DE PARADA, e ele cobre as OITO combinações — não só as de negociação.**
    Antes de escrever uma linha de código, confronte a matriz medida com o que se espera
    de cada casa. PARE, escreva os oito números no SUMMARY e devolva o achado, sem ajustar
    limiar nenhum, se QUALQUER uma destas acontecer:

    - o molde de adena passa o limiar contra `cabecalho_negociacao_goods.png` ou
      `cabecalho_negociacao_unitprice.png` — o vão é assimétrico (A1) e a premissa da fase
      caiu;
    - o molde de negociação passa o limiar contra `cabecalho_adena.png` — hoje ele dá
      0,1331 contra um limiar de 0,73, e um valor diferente disso significa que a fixtura
      ou a calibração mudaram debaixo da pesquisa;
    - **qualquer um dos dois passa o limiar contra `cabecalho_busca.png`** — a busca não
      tem leitora e não pode ganhar casamento nenhum;
    - o molde de adena NÃO passa o limiar contra `cabecalho_adena.png` — sem isso não há
      portão para a Adena e a fase não tem por onde começar.

    **Nenhuma dessas quatro é premissa desta task: as quatro são MEDIÇÃO, e o plano não
    afirma o resultado de nenhuma antes de olhar.** Escolher o número contra material que
    ainda não se olhou é o defeito que esta fase inteira existe para não repetir.

    Ler a aba errada com confiança é a corrupção por fator inteiro que o ADEN-02 nomeia.

    Grave o bloco `mercado_layouts.adena` em
    `tests/fixtures/mercado/calibracao_de_fixture.json` (arquivo de teste, versionado, do
    repositório — **nada é escrito no `calibration.json` da máquina**), com: `cabecalho`
    (o dict cortado em (a)), `limiar_do_cabecalho` (`CASAMENTO_MINIMO_DA_ANCORA`), e
    `colunas` com `total` (`dx: 62, largura: 209`) e `unitario` (`dx: 271, largura: 174`)
    — os retângulos de negociação, que a pesquisa mediu lendo a Adena exatamente. Sem
    `nome` e sem `quantidade`. O `grade` sai CURTO ou ausente, herdando `dx`, `dy`,
    `largura`, `altura_da_linha` e `linhas_por_pagina` de `mercado_grade` (D-D), com um
    comentário no SUMMARY dizendo que hoje eles são idênticos e que herdar é o que impede
    duas cópias do mesmo número de envelhecerem separadas.

    Em `mercado_pagina.py`:

    Helper puro `modelo_de_layout(cal, nome) -> dict | None`, que monta a geometria de UM
    layout: para `negociacao`, a partir das chaves de topo; para os aninhados, a partir de
    `cal.mercado_layouts[nome]`, com cada campo de `grade` caindo em `mercado_grade`
    quando ausente. Uma função só, porque duas montagens parecidas divergem.

    `_casamento_do_layout` passa a devolver `str | None` — o nome do layout VENCEDOR.
    Vencer é passar o próprio limiar **e** ter o maior score. Empate devolve `None`:
    aceitar "o primeiro que passou" faria o veredito depender da ordem de iteração de um
    dict lido de JSON, que é a ordem em que o usuário calibrou, e isso não é critério de
    nada. Os moldes aninhados são decodificados UMA VEZ, no construtor, ao lado do de
    negociação, e um bloco que `cabecalho_de_calibracao` recusa entra como ausente com
    aviso — nunca derruba o leitor.

    `_layout_confere` guarda o vencedor em `self._layout_atual` e devolve `bool`, mantendo
    o latch. A mensagem de recusa é REESCRITA: ela hoje afirma que o v1 lê somente o
    layout calibrado e explica a normalização por cinco milhões como razão de recusar —
    depois desta fase isso passa a ser meia-verdade. A mensagem nova diz quais layouts
    ESTÃO calibrados nesta instalação e que nenhum deles casou a banda desta tela.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena_pagina.py tests/test_mercado_pagina.py -x -q</automated>
  </verify>
  <done>
    Um teste calcula as OITO combinações e prende o veredito por banda — qual molde vence
    e se algum passa o limiar —, com os oito valores MEDIDOS nesta execução escritos no
    fonte e no SUMMARY. O teste afirma o que a medição mostrou, e o portão de parada acima
    é quem decide se a medição permite seguir; este `<done>` não antecipa nenhuma das oito
    casas. Outro teste registra dois layouts com
    moldes de bytes idênticos e afirma que `_casamento_do_layout` devolve `None` e que
    nenhuma linha é lida — empate não é veredito. `tests/test_mercado_pagina.py` continua
    verde, o que é a prova de que a negociação não mudou.
  </done>
</task>

<task type="auto">
  <name>Task 3: a página da Adena, de pixels a nove linhas de taxa</name>
  <files>l2scanner/mercado_pagina.py, tests/test_mercado_adena_pagina.py</files>
  <read_first>
    `l2scanner/mercado_pagina.py:707-838` — `_ler_a_pagina` e `_recortes_de_coluna`,
    inteiras. O catálogo PROVISÓRIO da página e a razão dele (a sessão de 2026-08-31 que
    gravou a mesma série com dois rótulos) continuam valendo para a Adena, ainda que lá a
    chave seja sempre a mesma.

    `l2scanner/mercado_pagina.py:470-540` — `observar` e a ordem dos portões. A ordem não
    muda; o que muda é o que acontece DEPOIS do portão 4.
  </read_first>
  <action>
    `_recortes_de_coluna` passa a receber o modelo do layout vencedor em vez de ler as
    quatro chaves de topo por `getattr`. Para a Adena são DOIS retângulos —
    `total` e `unitario` — e nenhum outro: a coluna do nome de negociação começa a −1 px
    do primeiro run da `Auction List` (medido: o texto claro começa em x=624, o retângulo
    em x=625), então reaproveitá-la cortaria o `1` de `10,000,000` e produziria
    `0,000,000` — corrupção por fator inteiro, plausível e calada. Esse é mais um motivo
    para o modelo da Adena não ter coluna de nome, e ele vai escrito.

    No laço de linhas, o layout vencedor escolhe a leitora: `negociacao` -> `ler_linha`
    (inalterada, mesmos argumentos); `adena` -> `ler_linha_de_adena` do 05-01. Nada de um
    `if` de aba dentro de `ler_linha`: são dois modelos de coluna, e o que os separa é a
    geometria, não um ramo.

    **O CONJUNTO DE CANDIDATOS DO PORTÃO É FECHADO, e são dois: `negociacao` e `adena`.**
    Um layout sem leitora conhecida — `busca`, que o calibrador já aceita e para o qual
    `LINHAS_ESPERADAS` já tem 9 — **não entra no laço de casamento**, e a razão é a que
    esta fase existe para atender: antes do 05-02 a geometria de busca era inerte, mas com
    o portão escolhendo por maior score ela passaria a poder VENCER, e vencer com uma
    leitora ausente significa recusar a página. O usuário perderia a leitura da negociação
    — conferida em campo, 353 páginas lidas contra 2 perdidas — pelo mecanismo exato que o
    ADEN-01 proíbe.

    Escreva isso ao lado do conjunto, e derive-o das leitoras que existem (não uma segunda
    lista escrita à mão que possa divergir): quem tem leitora entra, quem não tem fica
    fora. Um bloco de layout calibrado e sem leitora produz UM aviso no arranque dizendo
    que ele está gravado e não será usado — não um silêncio, e não uma recusa por tick.

    Nada em `mercado_registro` muda, nada em `mercado_analise` muda. A `LinhaLida` da
    Adena atravessa o acordo entre dois frames, a dedup e o CSV pelo caminho que já
    existe (ADEN-03).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena_pagina.py tests/test_mercado_replay.py -x -q</automated>
  </verify>
  <done>
    Um teste lê `tests/fixtures/mercado/janela_adena_f014.png` DUAS vezes (o acordo de
    dois frames) com a calibração de fixtura e afirma sobre a `PaginaAceita`: 9 linhas,
    o índice 5 em `descartadas` com motivo `cruzamento`, toda `chave_da_serie` igual à
    sentinela, e o conjunto de `quantidade` contido em `{5_000_000, 10_000_000}`.

    Um teste afirma a truth que hoje nenhum critério alcança: os recortes de coluna do
    layout `adena` são EXATAMENTE `{"total", "unitario"}` — conjunto igual, não contido.
    Sem ele, as 9 linhas passariam com um terceiro recorte inútil sobre a `Auction List`,
    e a promessa de que aquela coluna não é tocada seria prosa.

    Um teste PAR afirma a não-regressão de forma discriminante: `janela_negociacao_f005.png`
    lida com a calibração de fixtura SEM a chave `mercado_layouts` (removida em memória) e
    COM ela produz `LeituraDaPagina` iguais campo a campo. Um clone que nunca calibrou a
    Adena lê negociação como hoje.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> geometria de leitura | dict editável à mão vira retângulo de recorte |
| banda de cabeçalho -> escolha de layout | pixels decidem com qual modelo de coluna a página é lida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `mercado_layouts` corrompido no `calibration.json` | high | mitigate | `_conferir_os_layouts_de_mercado` confere tipo, faixa e contagem de bytes no ARRANQUE, onde a mensagem ainda pode dizer "recalibre" com o usuário olhando. Sem ela, um hex torto levanta dentro do construtor do leitor e vira feature OFF sem causa legível. |
| T-05-05 | Spoofing | `_casamento_do_layout` escolhendo o layout errado | critical | mitigate | Vencer exige passar o próprio limiar E ter o maior score; empate devolve `None`. A matriz de 8 números é MEDIDA nos dois sentidos e afirmada por teste (A1). Ler a aba errada com confiança corrompe a série por fator inteiro. |
| T-05-06 | Denial of Service | um bloco de layout corrompido derrubando o leitor | medium | mitigate | Molde que `cabecalho_de_calibracao` recusa entra como ausente com aviso; o leitor segue com os layouts que decodificaram. Feature OFF, nunca `raise` no tick. |

**Sem instalação de pacote nesta fase** (FIRE-01).
</threat_model>

<verification>
```
python -m pytest tests/test_mercado_adena_pagina.py -q
python -m pytest tests/test_mercado_pagina.py tests/test_mercado_replay.py tests/test_calibracao_mercado.py tests/test_mercado_modo.py -q
```
A segunda linha é a superfície de regressão da negociação. `test_mercado_modo.py` entra
porque `pecas_de_calibracao_de_mercado_faltando` vira recusa de subir lá.
</verification>

<success_criteria>
- A matriz de 8 números está escrita no fonte E afirmada por teste, com o vencedor por banda.
- `pecas_de_calibracao_de_mercado_faltando` devolve exatamente 15 nomes numa `Calibracao` vazia de mercado.
- `janela_adena_f014.png` produz 9 linhas de taxa; a linha 5 cai por `cruzamento`.
- `janela_negociacao_f005.png` lê igual com e sem `mercado_layouts`.
- `VERSAO_DO_ESQUEMA` continua 2.
- Nada foi escrito em `.mercado/` nem no `calibration.json` da máquina.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-02-SUMMARY.md` when done
</output>
