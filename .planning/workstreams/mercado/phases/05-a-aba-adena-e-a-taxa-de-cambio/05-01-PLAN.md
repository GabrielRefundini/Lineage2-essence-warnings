---
phase: 05-a-aba-adena-e-a-taxa-de-cambio
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - l2scanner/mercado_catalogo.py
  - l2scanner/mercado_leitura.py
  - tests/test_mercado_adena.py
autonomous: true
requirements: [ADEN-02, ADEN-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`quantidade_de_adena(13333, 6666)` devolve 10.000.000 adena e 2 incrementos (D-B: o caso de arredondamento do usuário)."
    - "`quantidade_de_adena(13588, 6750)` devolve `None` — a leitura errada da linha 5 da fixtura NÃO vira taxa (D-C: guarda)."
    - "Uma linha da Adena vira `LinhaLida` com `chave_da_serie` de sentinela e `nome_exibido` `Adena`, sem OCR nenhum (D-A)."
    - "Uma linha da Adena cujo cruzamento estoura vira `Descarte` com motivo `cruzamento`, nunca `LinhaLida`."
    - "Nenhuma leitura de nome é chamada no caminho da Adena — a coluna `Auction List` não é tocada."
  artifacts:
    - l2scanner/mercado_leitura.py
    - l2scanner/mercado_catalogo.py
    - tests/test_mercado_adena.py
  key_links:
    - "`quantidade_de_adena` chama `limite_derivado_do_cruzamento` — o critério não é escolhido, é reusado (D-B)."
    - "`ler_linha_de_adena` devolve a MESMA `LinhaLida` que `ler_linha` — o CSV da Fase 3 não muda (ADEN-03)."
---

<objective>
As duas peças PURAS da aba Adena: a quantidade que se DERIVA (porque não se lê) e a
sentinela que dá identidade a uma série só.

Purpose: a Adena não tem coluna de quantidade e a `Auction List` **não se lê com os
moldes de dígito deste projeto** — medido em sete pisos de brilho, `ler_celula` devolve
`None` nas dez linhas, e não há vale entre as populações de largura de run. A quantidade
vem das duas colunas de moeda, que leem exatamente. E o critério que aceita a derivação é
o mesmo que **rejeita o `13588`** da fixtura — um defeito ativo, silencioso e plausível.

Output: `ADENA_POR_INCREMENTO`, `quantidade_de_adena`, `CHAVE_DA_SERIE_DA_ADENA`,
`NOME_EXIBIDO_DA_ADENA`, `ler_linha_de_adena` e a suíte que exercita os DOIS sentidos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/REQUIREMENTS.md
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-CONTEXT.md
@.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-RESEARCH.md
</context>

<interface_context>
Assinaturas que ESTE plano consome e não altera (lidas em 2026-09-01):

- `mercado_leitura.LIMITE_DERIVADO_POR_UNIDADE = 0.5` (`:1024`)
- `mercado_leitura.limite_derivado_do_cruzamento(quantidade: int) -> float` (`:1027`)
- `mercado_leitura.residuo_do_cruzamento(total, unitario, quantidade) -> int | None` (`:1054`)
- `mercado_leitura.ler_celula_de_numero(recorte, moldes, piso, margem, *, valor_minimo, folga_de_cola) -> int | None` (`:862`)
- `mercado_leitura.linha_vazia(bgr) -> bool`, `linha_ocluida(cinza, sonda, limiar_de_dispersao) -> bool`
- `mercado_leitura._recusar(indice: int, motivo: str, detalhe: str) -> Descarte` (`:1834`)
- `mercado_leitura.MOTIVO_DA_OCLUSAO/_DA_GRAMATICA/_DO_CRUZAMENTO` (`:1206-1208`)
- `mercado_leitura.LinhaLida(indice, chave_da_serie, nome_exibido, total_em_centesimos, quantidade, serie_nova, residuo_do_cruzamento)` (`:1213`)
- `mercado_catalogo.SUFIXO_DA_GRADE` / `SEPARADOR_DA_ASSINATURA = "#"` / `PISO_DO_RESTO` (`:99-160`) — o precedente literal de constante-no-fonte
- `mercado_catalogo.EntradaDoCatalogo(chave, nome, assinatura)`
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: a quantidade que se deriva, e o critério que já existe</name>
  <files>l2scanner/mercado_leitura.py, tests/test_mercado_adena.py</files>
  <read_first>
    `l2scanner/mercado_leitura.py:1024-1100` — `LIMITE_DERIVADO_POR_UNIDADE`,
    `limite_derivado_do_cruzamento` e `residuo_do_cruzamento`, com a derivação e o T-02-38
    escritos ao lado. É de lá que sai TUDO o que esta task decide; ela não escolhe número.

    `l2scanner/mercado_catalogo.py:99-113` — `SUFIXO_DA_GRADE`, para copiar o argumento
    literal de por que uma constante de "como o jogo escreve a coluna" fica no fonte
    (D-E).
  </read_first>
  <behavior>
    Testes ANTES da implementação, em `tests/test_mercado_adena.py`. Os dois primeiros
    são o par que dá valor um ao outro — eles discriminam em sentidos OPOSTOS:

    - ACEITA `quantidade_de_adena(13333, 6666)` -> `(10_000_000, 2)`.
      É o `133,33` por `66,66` da captura do usuário de 2026-09-01 17h. Resíduo
      `|13333 - 2*6666| = 1`, limite `2 * 0,5 = 1,0`. Passa RASPANDO, e o teste tem de
      dizer isso na docstring: não há folga aqui.
    - REJEITA `quantidade_de_adena(13588, 6750)` -> `None`.
      É a linha 5 de `janela_adena_f014.png`: a tela diz `135,00`, `ler_celula_de_numero`
      devolve `13588` (dois `0` lidos como `8`, o par de margem 0,0370). Resíduo
      `|13588 - 2*6750| = 88` contra limite `1,0`.
    - ACEITA `(11600, 5800)` -> `(10_000_000, 2)` — o `116,00` do CONTEXT.
    - ACEITA `(30000, 10000)` -> `(15_000_000, 3)` — o `15.000.000 por 300,00` do CONTEXT.
    - ACEITA `(6200, 6200)` -> `(5_000_000, 1)` — a linha 0 da fixtura, lida de verdade.
    - ACEITA `(6499, 6499)` -> `(5_000_000, 1)` — a linha 1 da fixtura.
    - `None` em qualquer argumento -> `None`.
    - `incremento` zero ou negativo -> `None`. `total` zero ou negativo -> `None`.
    - `n` arredondando para zero (`(100, 6666)`) -> `None`.
    - Um teste que afirma que a função CHAMA `limite_derivado_do_cruzamento`: com
      `monkeypatch` sobre `mercado_leitura.limite_derivado_do_cruzamento` devolvendo
      `0.0`, o caso `(13333, 6666)` passa a devolver `None`. Um teste que só afirmasse o
      resultado não distinguiria "usa o critério medido" de "usa um número copiado".
  </behavior>
  <action>
    Em `l2scanner/mercado_leitura.py`, ao lado de `limite_derivado_do_cruzamento`:

    Constante `ADENA_POR_INCREMENTO = 5_000_000`, no FONTE e não na calibração (D-E).
    Escreva ao lado a razão literal, no molde de `SUFIXO_DA_GRADE`: não é número medido,
    é como o jogo escreve a coluna (`5 mln increment`), e quem garante que a coluna é
    essa é o molde de cabeçalho, que já mora na calibração — gravá-la também criaria
    duas verdades sobre uma coluna só.

    Função `quantidade_de_adena(total: int | None, incremento: int | None) -> tuple[int, int] | None`,
    devolvendo `(quantidade_em_adena, incrementos)`. Aritmética INTEIRA no julgamento
    (T-02-38): o `round` só escolhe o candidato `n`, quem decide é a multiplicação
    `abs(total - n * incremento)` comparada a `limite_derivado_do_cruzamento(n)`.

    **A COMPARAÇÃO É `residuo <= limite`, E O SINAL É LOAD-BEARING.** Com `<`, o
    caso-bandeira `(13333, 6666)` REPROVA — resíduo 1 contra limite 1,0 —, e a fase
    perderia justamente a linha de arredondamento que o usuário capturou. O `<=` é o
    mesmo sentido que `_observar_o_cruzamento` já usa (`:1700`, `if residuo <= limite...:
    return`), e escrever o outro aqui criaria duas leituras opostas do mesmo limite
    derivado. Deixe isso dito no fonte, ao lado da comparação.

    Falha FECHADA em coluna não lida, incremento não-positivo, total não-positivo,
    `n < 1` e resíduo estourado.

    A docstring carrega, por escrito: (a) que a quantidade NÃO é lida da tela e por quê —
    a varredura de piso 180/200/210/220/230/240/250 sobre a `Auction List` devolveu
    `None` nas dez linhas, o texto de lá sai 5-6 px onde os moldes têm 4, e no piso em
    que a largura fecha o `0` se parte em dois runs; (b) as leituras que sustentam a rota
    derivada (6200/6200, 6499/6499, 6500, 6600, 6700, 6800, 6850, 7000, 7000 sobre
    `janela_adena_f014.png` com os retângulos de negociação); (c) os dois casos difíceis
    com as contas; (d) que o ramo de ACEITAR o arredondamento passa por margem zero e
    **não tem pixel no repositório** — ele é inferência aritmética, e apertar o limite
    exigiria material com o caso dentro (A3).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena.py -x -q</automated>
    <human-check>
      D-F, O MATERIAL DO CASO DIFÍCIL. O ramo que ACEITA o arredondamento
      (`133,33 / 66,66`) está exercitado em teste de UNIDADE com inteiros literais — o
      que é não-vácuo para a função —, mas ele **não tem um pixel no repositório**: as 9
      linhas boas de `janela_adena_f014.png` dividem todas exato. Pergunte ao usuário se
      ele consegue uma gravação curta da aba Adena com a coluna `5 mln increment`
      ordenada, contendo uma linha cujo incremento não divida o total exatamente. Se ele
      não conseguir, o ramo fica declarado como inferência aritmética não medida sobre
      pixels — o que já está escrito no `deferred-items.md` da fase (05-03).
    </human-check>
  </verify>
  <done>
    `python -m pytest tests/test_mercado_adena.py -q` passa, e a suíte contém tanto o caso
    que ACEITA `(13333, 6666)` quanto o que REJEITA `(13588, 6750)`, mais o teste de
    monkeypatch que prova que `limite_derivado_do_cruzamento` foi CHAMADA.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a linha da Adena, sem nome e sem OCR, com identidade de sentinela</name>
  <files>l2scanner/mercado_catalogo.py, l2scanner/mercado_leitura.py, tests/test_mercado_adena.py</files>
  <read_first>
    `l2scanner/mercado_leitura.py:1488-1665` — `ler_linha` inteira. A ordem dos portões
    (vazia -> oclusão -> números -> cruzamento -> nome) é a decisão principal daquela
    função, e a da Adena é a MESMA menos os dois últimos passos. Copie a ordem, não a
    reinvente.

    `l2scanner/mercado_leitura.py:1213-1246` — `LinhaLida`, campo a campo.
    `l2scanner/mercado_pagina.py:896-924` — `_assinatura_da_chave` e `_acrescentar_serie`,
    para conferir que uma chave terminada em `#` produz assinatura vazia sem levantar.
  </read_first>
  <behavior>
    - `ler_linha_de_adena` sobre uma linha vazia devolve `None` (fim da página).
    - Sobre uma linha ocluída devolve `Descarte` com `MOTIVO_DA_OCLUSAO`.
    - Com a coluna de total ilegível devolve `Descarte` com `MOTIVO_DA_GRAMATICA`.
    - Com a coluna de incremento ilegível devolve `Descarte` com `MOTIVO_DA_GRAMATICA` —
      na Adena o incremento NÃO é opinião opcional como o unitário da negociação: sem ele
      não há quantidade, e sem quantidade não há taxa (D-C).
    - Com total e incremento lidos mas o cruzamento estourando, devolve `Descarte` com
      `MOTIVO_DO_CRUZAMENTO` — e o detalhe cita total, incremento, `n` e resíduo.
    - Com a linha boa devolve `LinhaLida` com `chave_da_serie == CHAVE_DA_SERIE_DA_ADENA`,
      `nome_exibido == NOME_EXIBIDO_DA_ADENA`, `quantidade` em adena e
      `total_em_centesimos` em XM.
    - `serie_nova` é `True` quando a sentinela ainda não está no catálogo recebido, e
      `False` quando já está — DUAS linhas seguidas produzem uma série só (D-A). Este é o
      teste que discrimina a sentinela de uma chave derivada do nome: com a chave derivada
      duas quantidades diferentes dariam duas séries.
    - Um teste que afirma que `ler_linha_de_adena` NÃO recebe leitora de texto: inspecione
      `inspect.signature` e afirme que os parâmetros são exatamente os esperados. Afirmar
      que a assinatura não tem por onde receber OCR é mais forte que afirmar que o OCR não
      foi chamado numa execução.
  </behavior>
  <action>
    Em `l2scanner/mercado_catalogo.py`, ao lado de `SUFIXO_DA_GRADE` e
    `SEPARADOR_DA_ASSINATURA` (D-A):

    `CHAVE_DA_SERIE_DA_ADENA = "adena" + SEPARADOR_DA_ASSINATURA` e
    `NOME_EXIBIDO_DA_ADENA = "Adena"`. Montada a partir do separador, e não escrita como
    literal solto, para que a forma da chave continue tendo uma verdade só. A razão da
    sentinela vai escrita ao lado, e é a decisão do usuário: a Adena é UMA série, e
    derivar a chave do nome faria a trava de dígitos (D-03) partir a Adena em uma série
    por quantidade — 5M, 10M e 15M viram três séries e a mediana da taxa nasce partida ao
    meio, que é exatamente o que esta fase existe para não fazer.

    Este módulo é a casa certa porque `mercado_registro` já o importa em nível de módulo
    (`mercado_registro.py:70`), então o console alcança a constante sem nenhuma aresta de
    import nova — conferido: `import l2scanner.mercado_console` já traz
    `l2scanner.mercado_catalogo` para `sys.modules` hoje.

    Em `l2scanner/mercado_leitura.py`, `ler_linha_de_adena(indice, bgr_da_linha,
    recorte_do_total, recorte_do_incremento, *, moldes, piso, margem,
    valor_minimo_do_numero, folga_de_cola, sonda, limiar_de_dispersao, catalogo)`.
    NUNCA levanta — ela roda dentro do tick, no modelo de `ler_linha`, com o mesmo
    `except Exception` no fim.

    A ordem: linha vazia -> oclusão -> `Total Price` -> `5 mln increment` ->
    `quantidade_de_adena` -> `LinhaLida`. `residuo_do_cruzamento(total, incremento, n)`
    entra no campo homônimo da `LinhaLida`: é a mesma grandeza, só que a escala de `n` são
    incrementos e não unidades, e isso vai escrito.

    A docstring diz o que a diferencia de `ler_linha`, e a diferença de STATUS é a parte
    que importa (D-C): na negociação o cruzamento é OBSERVAÇÃO registrada, porque a guarda
    foi reprovada por medição e `mercado_tolerancia_do_cruzamento` está gravada como
    `None`; aqui ele é GUARDA e derruba a linha, porque é a única rede entre uma leitura
    errada e uma taxa plausível no CSV. Cite o número que justifica: sem ela a linha 5 da
    fixtura entraria como taxa `135,88`.

    Nada aqui lê nome, nada aqui recorta a coluna `Auction List`, e nada aqui recebe uma
    função de OCR.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena.py -x -q</automated>
  </verify>
  <done>
    A suíte passa. `ler_linha_de_adena` devolve `LinhaLida` com a sentinela para a linha
    boa e `Descarte` de motivo `cruzamento` para a linha do `13588`, e o teste de duas
    linhas seguidas afirma UMA série só.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixels da tela -> número | recorte de imagem vira inteiro que vai para o CSV do usuário |
| `calibration.json` -> geometria | (não tocado neste plano; ver 05-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `ler_celula_de_numero` na coluna `Total Price` | high | mitigate | Substituição de glifo (`0` lido como `8`, margem 0,0370) produz taxa plausível e errada. `quantidade_de_adena` a derruba pelo resíduo do cruzamento — medido: resíduo 88 contra limite 1,0 na linha 5 da fixtura. |
| T-05-02 | Information Disclosure | nenhuma | low | accept | Fase local, sem rede, sem credencial. O caminho da Adena não toca Chatwoot, `.env` nem `CHATWOOT_API_TOKEN`. |
| T-05-03 | Denial of Service | `ler_linha_de_adena` dentro do tick | medium | mitigate | `except Exception` contendo a linha, no modelo já escrito de `ler_linha`: uma exceção aqui pararia o scanner que existe para avisar que alguém da party morreu. |

**Sem instalação de pacote nesta fase.** FIRE-01: nenhuma linha entra em
`requirements.txt`, e `tests/test_mercado_firewall_de_fase.py::TestNenhumaDependenciaNova`
já prende isso por teste.
</threat_model>

<verification>
```
python -m pytest tests/test_mercado_adena.py -q
python -m pytest tests/test_mercado_leitura.py tests/test_mercado_catalogo.py -q
```
A segunda linha é a guarda de regressão: as duas peças novas moram em módulos que a
negociação usa, e nenhuma delas pode mudar o comportamento de lá.
</verification>

<success_criteria>
- `quantidade_de_adena(13333, 6666)` devolve `(10_000_000, 2)` e `quantidade_de_adena(13588, 6750)` devolve `None`, os dois afirmados por teste.
- Com `limite_derivado_do_cruzamento` monkeypatchada para `0.0`, o caso `(13333, 6666)` passa a devolver `None` — prova de que a função foi CHAMADA.
- `ler_linha_de_adena` produz `LinhaLida` com a sentinela e, em duas linhas seguidas, UMA série.
- `tests/test_mercado_leitura.py` e `tests/test_mercado_catalogo.py` continuam verdes.
- Nada foi escrito em `.mercado/` nem em `calibration.json`.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/05-a-aba-adena-e-a-taxa-de-cambio/05-01-SUMMARY.md` when done
</output>
