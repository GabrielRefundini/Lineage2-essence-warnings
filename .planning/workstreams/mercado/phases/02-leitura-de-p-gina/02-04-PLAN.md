---
phase: 02-leitura-de-p-gina
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - l2scanner/mercado_leitura.py
  - l2scanner/mercado_pagina.py
  - l2scanner/mercado_catalogo.py
  - l2scanner/calibrar_mercado.py
  - tests/test_mercado_leitura.py
  - tests/test_mercado_pagina.py
  - tests/test_mercado_glifos.py
  - tests/fixtures/mercado/calibracao_de_fixture.json
autonomous: true
requirements: [LEIT-02, LEIT-05, LEIT-01]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A convencao de recorte de coluna e meio-aberta `[dx, dx + largura)`, a mesma de `segmentar_glifos`, e colunas vizinhas nunca compartilham um pixel — a convencao e contrato e fica presa por teste (LEIT-05)"
    - "Quando as duas escalas caem na mesma serie, o `nome_exibido` gravado vem sempre da MESMA escala (a 3x), por regra escrita e nao por acaso de ordem: duas execucoes sobre o mesmo frame produzem o mesmo rotulo"
    - "Recorte de coluna com largura zero ou fora do painel e recusado no portao de carga, e a leitura de mercado simplesmente nao acontece — nunca um recorte degenerado lido com confianca (LEIT-05)"
    - "Uma linha da grade de negociacao atravessa o caminho inteiro — janela, painel, portao de layout, fatia da linha, sonda de oclusao, coluna do nome, coluna do numero, catalogo, estabilizador — e sai como uma pagina aceita, contra fixture VERSIONADA, num clone limpo"
    - "As primitivas de segmentacao e classificacao FORAM MOVIDAS de `calibrar_mercado.py` para `mercado_leitura.py`, nao copiadas: a ferramenta passa a importar do modulo puro, e nenhum modulo de producao importa a ferramenta"
    - "A leitura de numero e TUDO OU NADA: um run que reprove no piso E na margem derruba a celula inteira, e a linha e descartada — preco nunca e inventado (LEIT-02)"
    - "A gramatica do numero e trava de validacao: milhar agrupa em blocos de exatamente 3, decimal tem exatamente 2; `5,00,000` e `62,000` violam e a linha cai"
    - "A sonda de oclusao roda ANTES do OCR: uma linha coberta cai sem que se pague 7 ms de OCR por ela, e a recusa NUNCA vem da confianca do casamento — a tooltip e semitransparente e produz glifo plausivel com valor errado (D-14)"
    - "A recusa e por LINHA, nunca por pagina: a tooltip cobriu 8 linhas seguidas num frame medido e as outras 2 seguem sendo lidas (D-15)"
    - "A marcacao de alvo cai pelo MESMO mecanismo, sem caso especial no codigo (D-16)"
    - "Linha vazia e decidida por AUSENCIA DE CONTEUDO dentro do retangulo, nunca por cor de fundo — a listra alternada da grade da dois fundos para linha cheia e dois para linha vazia (D-12)"
    - "Pagina de layout nao calibrado e RECUSADA com aviso alto, nunca lida — e o molde do cabecalho casa a mesma coluna com e sem a seta de ordenacao (D-09, D-11)"
    - "Nenhum limiar desta fase e constante de fonte: piso, margem, dispersao, corte e limiar de cabecalho vem todos do `calibration.json`"
  artifacts:
    - path: "l2scanner/mercado_leitura.py"
      provides: "o transform puro pixels->LinhaLida|Descarte: segmentacao e classificacao promovidas, gramatica do numero, sonda de oclusao, linha vazia e portao de layout"
      contains: "LinhaLida"
    - path: "l2scanner/mercado_pagina.py"
      provides: "a maquina de estado da pagina: PaginaAceita e o laco que junta as partes"
      contains: "PaginaAceita"
    - path: "tests/fixtures/mercado/calibracao_de_fixture.json"
      provides: "instantaneo da calibracao de negociacao CONFIRMADA pelo usuario, versionado para o teste ficar verde num clone limpo"
      min_lines: 10
    - path: "tests/test_mercado_leitura.py"
      provides: "o tracer ponta a ponta mais a oclusao, a linha vazia e o portao de layout, todos sobre fixture versionada"
      contains: "def test_"
  key_links:
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/mercado_leitura.py"
      via: "a ferramenta IMPORTA as primitivas do modulo puro depois da promocao; a seta aponta sempre da ferramenta para o puro, nunca o contrario"
      pattern: "from .mercado_leitura import"
    - from: "l2scanner/mercado_pagina.py"
      to: "l2scanner/mercado_visao.py"
      via: "instancia RastreioDoPainel; nao reimplementa memoria de posicao nem rebusca"
      pattern: "RastreioDoPainel"
    - from: "l2scanner/mercado_leitura.py"
      to: "l2scanner/mercado_geometria.py"
      via: "usa nivel_de_fundo_da_linha para medir; o limiar de decisao mora aqui, vindo do calibration.json"
      pattern: "nivel_de_fundo_da_linha"
---

<objective>
Ligar o caminho inteiro de leitura, uma linha de cada vez, comecando por UMA fatia vertical que
atravessa todas as camadas e prova a arquitetura antes de qualquer expansao.

O trabalho pesado ja esta construido e medido: `segmentar_glifos` (projecao de coluna),
`_alinhar_por_preenchimento` (o alinhamento certo para glifos de larguras diferentes) e o
argmax com margem de `propor_rotulo` vivem hoje dentro de `calibrar_mercado.py` e leram 25 de
25 precos sem erro contra tres frames reais. Esta fase nao inventa o leitor de digitos: ela
PROMOVE codigo de ferramenta a codigo de producao. E producao nao pode importar de um modulo
que abre janela do OpenCV e le teclado.

Purpose: o tracer existe para descobrir um beco arquitetural depois de um commit, e nao depois
de dez camadas ja commitadas. Se a fatia fina nao atravessar, o problema aparece antes de haver
codigo para jogar fora.

Output: `mercado_leitura.py` e `mercado_pagina.py` nascidos, com a linha lida ponta a ponta
contra fixture versionada; a sonda de oclusao no lugar certo do pipeline; e o portao de layout
recusando o que nao esta calibrado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-RESEARCH.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-02-SUMMARY.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — uma linha da grade de negociacao atravessa todas as camadas e sai como pagina aceita</name>
  <precondition>`calibration.json` traz `mercado_grade.layout == "negociacao"`, as quatro colunas, o cabecalho, e os seis numeros medidos pelos planos 02-02 e 02-03. A fonte da assinatura de digitos foi decidida pelo usuario na Task 2 do 02-03 e esta escrita no `02-03-SUMMARY.md`.</precondition>
  <files>l2scanner/mercado_leitura.py, l2scanner/mercado_pagina.py, l2scanner/mercado_catalogo.py, l2scanner/calibrar_mercado.py, tests/test_mercado_leitura.py, tests/test_mercado_glifos.py, tests/fixtures/mercado/calibracao_de_fixture.json</files>
  <read_first>
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md`, secao
      "Shared Patterns -> 1. Mover a primitiva, nao copia-la" — os 3 precedentes e o
      **checklist de 5 passos por primitiva**, que e o roteiro literal desta task
    - `l2scanner/mercado_geometria.py:1-9` — o charter a copiar para o topo de `mercado_leitura.py`
    - `l2scanner/calibrar_mercado.py`: `segmentar_glifos` (:359-455, com a docstring de 45
      linhas), `mascara_do_sufixo` (:456-468), `recortar_sufixo` (:469-500),
      `_alinhar_por_preenchimento` (:501-528), `_par_incalculavel` (:529-568),
      `MARGEM_DO_RETANGULO_DE_PRECO` (:1356), `propor_rotulo` (:1596-1643)
    - `l2scanner/calibrar_mercado.py:63-91` — o bloco de imports da ferramenta sobre os modulos
      puros, onde a linha nova entra
    - `l2scanner/mercado_visao.py:102-136` (`casamento_da_ancora`) e `:447-510`
      (`RastreioDoPainel.observar`, a memoria de posicao com rebusca)
    - `l2scanner/ocr.py:195-217` (`_ler`) — o modelo de "NUNCA levanta" para o leitor de linha
    - `l2scanner/manutencao.py:384-402` — a injecao de leitora no `__init__`, que e como o teste
      exercita o acordo entre escalas sem WinRT
    - `l2scanner/identidade.py:109-113` — argmax + margem, o padrao
    - `tests/test_mercado_glifos.py:41-60` — os imports que apontam para `calibrar_mercado` hoje
      e que precisam trocar de origem; ele e o detector de regressao do movimento
    - `SPIKE-RESPOSTAS.md` secao 2 (em `.planning/workstreams/mercado/phases/01-*/`) — a
      gramatica travada do numero e a desambiguacao da virgula pelo SUFIXO
  </read_first>
  <behavior>
    - Sobre `tests/fixtures/mercado/janela_negociacao_f010.png`, a linha 0 sai como uma
      `LinhaLida` com nome agrupado, total em centesimos e quantidade, e com `serie_nova` verdadeiro
      na primeira vez
    - Dois frames com a MESMA pagina produzem acordo, e o resultado e uma `PaginaAceita`
    - Um unico frame nunca produz `PaginaAceita`: acordo exige dois
    - `100,00` vira `10000` centesimos; `5,000,000` com sufixo `Adena` vira `500000000` centesimos
    - `62,000` e recusado pela gramatica (decimal com 3 casas) e a linha e descartada
    - `5,00,000` e recusado pela gramatica (bloco de milhar com 2 digitos) e a linha e descartada
    - Uma celula em que um run reprova no piso ou na margem devolve `None`, e a linha vira
      `Descarte` com motivo — nunca um numero parcial
    - O unitario exibido NAO e lido nem guardado: `40,00 / 48` aparece na tela como `0,83`, e
      reconstruir o total a partir dele devolve `39,84`, um numero que nunca existiu
  </behavior>
  <action>
Criar `l2scanner/mercado_leitura.py` com o charter de modulo puro copiado da forma de
`mercado_geometria.py:1-6`: nao abre janela, nao le teclado, nao escreve arquivo, nao tem
relogio. O bloco de imports segue a convencao dos modulos puros (`dataclasses`, `numpy`, `cv2` e
irmaos `.puros`); ele NAO pode ter o cabecalho de DPI com `# noqa: E402` que as ferramentas tem.

MOVER — nao copiar — para dentro dele, seguindo o checklist de 5 passos do PATTERNS: cortar a
funcao INTEIRA junto com a docstring (a docstring e onde a medicao que a justifica vive), colar,
acrescentar a linha de import em `calibrar_mercado.py` no bloco que ja importa dos modulos
puros, trocar a origem do import em `tests/test_mercado_glifos.py`, e rodar a suite.
As primitivas: `segmentar_glifos`, `mascara_do_sufixo`, `recortar_sufixo`,
`_alinhar_por_preenchimento`, `_par_incalculavel` e `MARGEM_DO_RETANGULO_DE_PRECO`.
`propor_rotulo` NAO se move: ela e a versao de FERRAMENTA, com os limiares de propor. Nasce ao
lado dela, em `mercado_leitura.py`, a gemea de producao `ler_glifos(mascara, faixa, runs, moldes,
piso, margem) -> str | None`, com `piso` e `margem` como parametros OBRIGATORIOS, sem default. O
tudo-ou-nada e a falha fechada de LEIT-02 e ja esta escrito na versao da ferramenta: basta um
run que nao passe no piso E na margem para a funcao devolver `None`.

Acrescentar `numero_valido(lido: str) -> bool`, a gramatica travada: milhar agrupa em blocos de
exatamente 3 e decimal tem exatamente 2. Ela pega o modo de falha que a classificacao nao pega —
glifo perdido ou glifo a mais — e custa tres linhas. O que ela nao pega e substituicao (`0`
virando `8` mantem a gramatica), e para isso servem a margem e o acordo entre frames; escrever
isso na docstring, para ninguem confundir o alcance dela.

Acrescentar `ler_celula_de_numero(bgr, moldes, piso, margem) -> int | None`, que devolve o valor
em CENTESIMOS como inteiro. Nunca float: a acumulacao de erro de ponto flutuante entraria pela
porta dos fundos exatamente onde o parsing a evitou. A virgula e ambigua e a desambiguacao vem
do SUFIXO, nao do numero — `5,000,000 Adena` e `62,00 XM Coin` aparecem na mesma linha, e
`XM Coin` e `Adena` sao moldes de primeira classe entre os 13 ja cortados. Nenhum PONTO apareceu
como separador em 335 frames.

Acrescentar as dataclasses `LinhaLida` (`indice`, `chave_da_serie`, `nome_exibido`,
`total_em_centesimos`, `quantidade`, `serie_nova`) e `Descarte` (`indice`, `motivo`), e
`ler_linha(...) -> LinhaLida | Descarte`, que nunca levanta, no modelo de `ocr._ler`, porque ela
roda dentro do tick.

O unitario exibido NAO entra em `LinhaLida`. Ele e derivacao arredondada, nao dado: a Fase 3
guarda `Total` e `Quantity`, e reconstruir o total a partir do unitario devolve um numero que
nunca existiu. Escrever isso na docstring da dataclass.

Criar `l2scanner/mercado_pagina.py` com `PaginaAceita` (`linhas`, `descartadas`, `motivos`) e
`LeitorDePagina`, que INSTANCIA `RastreioDoPainel` em vez de reimplementar memoria de posicao —
ele ja existe, foi medido em 6 de 6 frames, e a varredura custa ~39 ms contra confirmacao barata
por ancora a cada tick. As leitoras de OCR entram por INJECAO no `__init__`, como
`VigiaDeManutencao` faz, e nao por monkeypatch de modulo: e o que permite o teste rodar no Python
GLOBAL, que nao tem as bindings WinRT. Nesta task `LeitorDePagina.observar` faz o minimo: le as
linhas do frame e exige que DOIS frames consecutivos concordem na tupla parseada antes de
devolver `PaginaAceita`. O congelamento e o minimo de linhas comparadas chegam no 02-05.

Ligar a assinatura de digitos escolhida no portao do 02-03: `chave_da_serie` passa a receber a
assinatura daquela fonte, e o `agrupar` de `mercado_catalogo.py` entra no caminho com `corte` e
`piso` vindos do `calibration.json`. Nesta task o catalogo ainda vive em memoria; o arquivo e o
02-05.

Cortar e versionar as fixtures que o tracer precisa: `janela_negociacao_f010.png` (a JANELA
inteira de `20260828-060622-mercado-pagina-cheia/frame_000010`) e
`janela_negociacao_f010_repetida.png` (o frame vizinho da mesma pagina parada, para o acordo). E
`tests/fixtures/mercado/calibracao_de_fixture.json`: um INSTANTANEO da calibracao de negociacao
que o usuario confirmou, versionado. Ele nao e uma calibracao diferente da de producao — e uma
copia da mesma, e o cabecalho do teste precisa dizer isso, porque a alternativa (uma calibracao
de fixture com layout proprio) foi explicitamente recusada pelo usuario. Ele existe porque
`recordings/` e gitignored e nao se materializa num worktree nem num clone limpo, e todo criterio
que precise ficar verde num worktree tem de vir de fixture VERSIONADA.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_leitura.py tests/test_mercado_glifos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_leitura.py -q` passa, incluindo o teste ponta a ponta
      que parte da janela versionada e chega a uma `PaginaAceita` com a linha 0 lida
    - `tests/test_mercado_glifos.py` segue verde com os imports apontando para
      `l2scanner.mercado_leitura` — ele e o detector de regressao do movimento, e a convencao de
      recorte nao pode ter mudado
    - A seta de dependencia aponta so num sentido:
      `python -c "import sys, l2scanner.mercado_leitura, l2scanner.mercado_pagina; assert 'l2scanner.calibrar_mercado' not in sys.modules; print('puro ok')"`
      imprime `puro ok`
    - `python -c "import inspect, l2scanner.mercado_leitura as m; s=inspect.getsource(m); assert 'imshow' not in s and 'waitKey' not in s and 'selectROI' not in s; print('charter ok')"` imprime `charter ok`
    - Existem testes que afirmam a gramatica nas duas bordas: `62,000` recusado e `5,00,000`
      recusado, cada um com a linha virando `Descarte`
    - Existe teste que afirma que UM frame sozinho nunca produz `PaginaAceita`
    - `python -c "import l2scanner.mercado_leitura as m, inspect; sig=inspect.signature(m.ler_glifos); assert sig.parameters['piso'].default is inspect.Parameter.empty and sig.parameters['margem'].default is inspect.Parameter.empty; print('sem default ok')"` imprime `sem default ok` — nenhum limiar tem valor de fabrica
  </acceptance_criteria>
  <done>Uma linha da grade de negociacao vai de pixels de janela ate uma pagina aceita, contra fixture versionada, num clone limpo; as primitivas mudaram de arquivo sem duplicar e a ferramenta importa do modulo puro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A sonda de oclusao no lugar certo do pipeline, e a linha vazia por ausencia de conteudo</name>
  <files>l2scanner/mercado_leitura.py, tests/test_mercado_leitura.py, tests/fixtures/mercado/</files>
  <read_first>
    - `l2scanner/mercado_geometria.py` — `nivel_de_fundo_da_linha`, criada no 02-02
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-02-SUMMARY.md` — o limiar
      medido e a folga do caso apertado
    - `l2scanner/manutencao.py:506-527` — o log de desacordo, com os delimitadores `>>><<<` e a
      decisao explicita de nao ter rate-limit; e a forma a copiar para o log de linha recusada
    - `02-CONTEXT.md`, secao "Oclusao e recusa — a licao do incidente 27x" — D-14, D-15, D-16, D-17
    - `02-RESEARCH.md`, Pitfall 4 — a prova de que o recorte de coluna sozinho nao basta, com as
      leituras de tooltip que vazam
    - `02-RESEARCH.md`, pergunta 4 — as duas ressalvas: a moda nao muda sob tooltip em varias
      linhas, e os valores absolutos nao sao universais entre layouts
  </read_first>
  <behavior>
    - Uma linha sob a tooltip vira `Descarte` com motivo de oclusao, e o OCR NAO e chamado para
      ela — o teste conta as chamadas da leitora injetada
    - No frame de tooltip, as linhas cobertas caem e as descobertas seguem sendo lidas: a recusa
      e por linha, nunca por pagina
    - Uma linha sob a marcacao de alvo cai pelo mesmo caminho, sem nenhum ramo de codigo dedicado
    - Uma linha VAZIA nao vira `Descarte` nem `LinhaLida`: ela marca o fim da pagina
    - Uma linha vazia de banda par e uma de banda impar dao o mesmo veredito — a decisao nao olha
      cor de fundo
    - A linha descartada NAO entra na comparacao do estabilizador; ela nao conta como desacordo
  </behavior>
  <action>
Acrescentar a `mercado_leitura.py` `linha_ocluida(cinza_da_linha, sonda, limiar) -> bool`, que
chama `nivel_de_fundo_da_linha` de `mercado_geometria` e compara a DISPERSAO com o limiar vindo
do `calibration.json`. O limiar de decisao mora aqui; a medicao mora la. A docstring precisa
carregar a razao inteira, porque ela e a licao do incidente 27x um nivel acima: a recusa NUNCA
vem da confianca do casamento, porque a tooltip e SEMITRANSPARENTE — um numero coberto ainda
produz glifos plausiveis com boa confianca e valor errado. Confianca alta sobre pixel adulterado
e exatamente o modo de falha que esta fase existe para impedir.

Colocar a chamada ANTES do OCR e antes da leitura de digito no pipeline de `ler_linha`. A ordem
importa por dois motivos e os dois vao no comentario: nao se paga ~7 ms de OCR por uma linha que
vai cair, e o recorte de coluna sozinho nao fecha o buraco — medido, com a tooltip por cima da
propria coluna do nome o OCR devolveu frases inteiras da tooltip como se fossem nome de item.
LEIT-05 reduz a superficie; a sonda e que a fecha.

A marcacao de alvo NAO ganha ramo proprio. Ela e opaca e previsivel (topo do painel) e cobre o
cabecalho e o nome da linha 1; o mesmo detector pega as duas. Um `if` dedicado seria um caminho
a mais para manter e uma promessa a mais para quebrar. Prender isso por teste, com a fixture do
frame de alvo.

Acrescentar `linha_vazia(bgr_da_linha, moldes) -> bool`, decidida por AUSENCIA DE CONTEUDO
dentro do retangulo — sem icone e sem glifo — e nunca por cor de fundo. Medido: a listra
alternada da grade da dois fundos diferentes para linhas cheias e dois para linhas vazias, entao
um teste de cor decidiria pelo motivo errado e acertaria por acidente ate parar de acertar.
Linha vazia marca o FIM DA PAGINA: ela nao e descarte (nao houve falha) nem leitura (nao ha
dado). Distinguir os tres estados no tipo de retorno, para que a contagem "li 7, perdi 3" do
console (Fase 4) nunca conte uma linha vazia como perda.

O motivo do descarte vai para o log no formato de `_registrar_desacordo`, com os delimitadores
`>>><<<` porque espaco em branco importa, e sem rate-limit. E NADA disso vai para o CSV:
escrever a linha recusada misturaria descarte com dado, que e a confusao que a falha fechada
existe para evitar (D-17).

Cortar as fixtures que faltarem: a JANELA inteira de `tooltip/frame_000015` e a de
`alvo-sobreposto/frame_000024`, mais uma janela com linhas vazias no fim da pagina.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_leitura.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Teste sobre a janela de tooltip afirma que as linhas cobertas viram `Descarte` e que as
      descobertas do MESMO frame viram `LinhaLida` — recusa por linha, nao por pagina
    - Teste com leitora de OCR injetada e contadora afirma ZERO chamadas de OCR para as linhas
      recusadas pela sonda: a ordem do pipeline esta presa
    - Teste sobre a janela de alvo afirma o mesmo veredito pelo mesmo caminho; e um teste de
      inspecao de fonte afirma que `ler_linha` nao tem ramo que mencione a marcacao de alvo
    - Teste afirma que uma linha vazia de banda par e uma de banda impar dao o mesmo veredito
    - Teste afirma que linha vazia, `Descarte` e `LinhaLida` sao tres estados distintos no
      retorno, e que a linha vazia nao entra na contagem de perdas
    - `python -m pytest tests/test_mercado_leitura.py -q` verde
  </acceptance_criteria>
  <done>Toda linha coberta cai antes de custar OCR, pelo mesmo mecanismo para tooltip e para marcacao de alvo; a linha vazia e reconhecida por ausencia de conteudo e marca o fim da pagina.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: O portao de layout — pagina que nao e a grade calibrada e recusada alto, nunca lida</name>
  <files>l2scanner/mercado_leitura.py, l2scanner/mercado_pagina.py, tests/test_mercado_leitura.py, tests/fixtures/mercado/</files>
  <read_first>
    - `l2scanner/mercado_visao.py:102-136` (`casamento_da_ancora`) e `:171`
      (`CASAMENTO_MINIMO_DA_ANCORA`) — o casamento numa posicao conhecida, que e o padrao da casa
    - `l2scanner/mercado_visao.py` — `cabecalho_de_calibracao`, criada no 02-01
    - `02-RESEARCH.md`, pergunta 6 inteira — as tres assinaturas de layout medidas no censo dos
      335 frames, onde a seta de ordenacao cai, e o corte de brilho que a remove
    - `02-CONTEXT.md`, o bullet do layout (D-09) e o do molde de cabecalho (D-11)
    - `l2scanner/mercado_visao.py:465-467` — "feature OFF e o unico padrao seguro"
  </read_first>
  <behavior>
    - A banda de cabecalho da grade de negociacao casa o molde e a pagina segue
    - A banda de cabecalho da aba Adena NAO casa, e a pagina e recusada com aviso alto — nenhuma
      linha e lida
    - A banda de cabecalho da tela de busca NAO casa, e a pagina e recusada
    - O MESMO frame de negociacao ordenado por outra coluna continua casando: a seta de ordenacao
      andou de celula e o molde nao depende dela
    - Sem `mercado_cabecalho_de_coluna` no calibration.json, a leitura de mercado simplesmente
      nao acontece, com aviso alto — e nunca um `raise` no arranque
  </behavior>
  <action>
Acrescentar a `mercado_leitura.py` `layout_confere(janela, origem, cabecalho, limiar) -> bool`,
que recorta a banda do cabecalho na posicao conhecida (o `dy`, `altura` e `largura` gravados),
aplica a mascara de brilho com o `corte_de_brilho` gravado, e casa contra o molde com
`casamento_da_ancora` — uma posicao so, que e o padrao da casa e reusa codigo existente.

Chama-la em `mercado_pagina.LeitorDePagina.observar` DEPOIS de achar o painel e ANTES de fatiar
a grade. A razao vai no comentario e ela e medida: em `scroll/frame_000009` a sonda de fundo leu
modas de 47 e 65 com a paridade invertida, porque aquele frame e a TELA DE BUSCA e aplicar a
grade calibrada ali produz lixo. O portao de layout tem de vir antes da sonda de oclusao, senao
a sonda mede sobre uma geometria que nao vale.

A recusa e ALTA e diz o que houve: qual layout esta calibrado, que a pagina na tela nao e ele, e
que o v1 le somente o layout calibrado. Ela nao propoe ler mesmo assim — ler a coluna errada com
confianca corrompe a serie por um fator inteiro, e a aba Adena e o exemplo vivo: ali a coluna e
`5 mln increment`, normalizada por 5 milhoes de adena e NAO por unidade.

Sem `mercado_cabecalho_de_coluna` no arquivo, `layout_confere` devolve falso e a leitura nao
acontece, com aviso alto — nunca um `raise` no arranque. Feature OFF e o unico padrao seguro
para um sinal que a Fase 4 vai usar perto do detector de morte.

Cortar as fixtures das tres bandas de cabecalho, resgatadas dos frames que o censo nomeou:
negociacao ordenada por `Goods` (`063752-mercado-aberto/frame_000000`), a MESMA gravacao
ordenada por `Unit price` (`frame_000020`), Adena (`scroll/frame_000014`) e busca
(`scroll/frame_000048`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_leitura.py tests/test_mercado_pagina.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Teste afirma casamento para as DUAS bandas de negociacao (ordenada por `Goods` e por
      `Unit price`) e recusa para Adena e para busca, sobre as quatro fixtures versionadas
    - Teste afirma que uma pagina recusada por layout produz ZERO linhas lidas e ZERO chamadas de
      OCR, com a leitora injetada contadora
    - Teste afirma que `layout_confere` roda ANTES da sonda de oclusao no caminho de `observar`
    - Teste afirma que sem `mercado_cabecalho_de_coluna` a leitura devolve nada e nao levanta
    - A mensagem de recusa nomeia o layout calibrado e diz que so ele e lido
  </acceptance_criteria>
  <done>O portao de layout roda antes de tudo que depende da geometria da grade, casa a negociacao nas duas ordenacoes e recusa Adena e busca com aviso alto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela -> processo | Pixels que a UI do jogo desenha. Uma tooltip semitransparente e a marcacao de alvo sao conteudo adulterante do ponto de vista do leitor |
| arquivo -> processo (`calibration.json`) | Limiares e moldes que decidem o que vira numero |
| ferramenta -> producao | A direcao do import: se producao importar a ferramenta, um `imshow` entra no tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Spoofing | linha coberta lida com confianca alta | critical | mitigate | Sonda de dispersao de fundo ANTES do OCR e antes da leitura de digito, com limiar calibrado. A recusa nunca vem da confianca do casamento — a tooltip e semitransparente e produz glifo plausivel com valor errado. E a licao do incidente 27x um nivel acima |
| T-02-17 | Tampering | leitura da coluna errada num layout nao calibrado | critical | mitigate | Portao de layout por molde de cabecalho antes de fatiar a grade. A aba Adena normaliza por 5 milhoes de adena e nao por unidade: ler a coluna errada corrompe a serie por um fator inteiro |
| T-02-18 | Tampering | numero parcial virando preco plausivel | high | mitigate | Tudo-ou-nada na celula (um run reprovado derruba a celula inteira) mais a gramatica do numero (milhar em blocos de 3, decimal com 2). Fecha glifo perdido e glifo a mais; a SUBSTITUICAO fica com a margem calibrada e com a guarda de cruzamento, que chega no **02-06** |
| T-02-19 | Elevation of Privilege | `cv2.imshow` entrando no tick por import invertido | high | mitigate | `mercado_leitura.py` com charter de modulo puro, preso por teste de import: nenhum modulo de producao carrega `calibrar_mercado` |
| T-02-20 | Tampering | erro de ponto flutuante acumulando no preco | medium | mitigate | Valores guardados como inteiro em centesimos, nunca float. O unitario exibido nao e guardado como preco, porque e derivacao arredondada: `0,83 x 48 = 39,84` onde a tela diz `40,00`. Ele e LIDO para a guarda de cruzamento do 02-06, e nunca gravado |
| T-02-21 | Denial of Service | limiar ausente no calibration.json derrubando o arranque | medium | mitigate | Ausencia e feature OFF com aviso alto, nunca `raise` — o padrao ja escrito em `mercado_visao.py:465-467` |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install nesta fase.** Todas as bibliotecas usadas (`cv2`, `numpy`, `difflib`) ja estao no `.venv` ou sao stdlib. `tests/test_firewall_escopo.py` segue varrendo o venv instalado |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Modulo novo `l2scanner/mercado_leitura.py`:**
- Promovidas de `calibrar_mercado.py` (MOVIDAS, nao copiadas): `segmentar_glifos`,
  `mascara_do_sufixo`, `recortar_sufixo`, `_alinhar_por_preenchimento`, `_par_incalculavel`,
  `MARGEM_DO_RETANGULO_DE_PRECO`
- Novas: `ler_glifos(mascara, faixa, runs, moldes, piso, margem) -> str | None`,
  `numero_valido(lido) -> bool`, `ler_celula_de_numero(bgr, moldes, piso, margem) -> int | None`,
  `linha_ocluida(cinza_da_linha, sonda, limiar) -> bool`,
  `linha_vazia(bgr_da_linha, moldes) -> bool`,
  `layout_confere(janela, origem, cabecalho, limiar) -> bool`,
  `ler_linha(...) -> LinhaLida | Descarte | None`
- Dataclasses: `LinhaLida(indice, chave_da_serie, nome_exibido, total_em_centesimos, quantidade, serie_nova)` — o campo `residuo_do_cruzamento` e acrescentado pelo 02-06,
  `Descarte(indice, motivo)`

**Modulo novo `l2scanner/mercado_pagina.py`:** `PaginaAceita(linhas, descartadas, motivos)`,
`LeitorDePagina(rastreio, catalogo, ler_texto, ler_texto_conferencia, cal)` com `.observar(janela)`

**Modificado:** `l2scanner/calibrar_mercado.py` passa a importar as primitivas de
`mercado_leitura`; `tests/test_mercado_glifos.py` troca a origem dos imports;
`l2scanner/mercado_catalogo.py` recebe a fiacao da assinatura escolhida no 02-03

**Fixtures versionadas novas:** `janela_negociacao_f010.png`,
`janela_negociacao_f010_repetida.png`, `janela_tooltip_f015.png`, `janela_alvo_f024.png`,
`janela_com_linhas_vazias.png`, `cabecalho_negociacao_goods.png`,
`cabecalho_negociacao_unitprice.png`, `cabecalho_adena.png`, `cabecalho_busca.png`,
`calibracao_de_fixture.json`

**Arquivos de teste novos:** `tests/test_mercado_leitura.py`, `tests/test_mercado_pagina.py`
</artifacts_this_phase_produces>

<bordas_da_sonda>
## As 8 bordas da sonda determinista, e onde cada uma e resolvida

Nao existe SPEC para esta fase, entao a sonda de bordas rodou sobre os requisitos e voltou 8
itens `unresolved`. Nenhum foi descartado: cada um esta resolvido por decisao do usuario ou
declarado como suposicao explicita.

| # | Requisito / categoria | A pergunta | Resolucao | Onde |
|---|---|---|---|---|
| 1 | LEIT-01 adjacency | Duas coisas iguais ou que se tocam fundem, colidem ou separam? | A trava de digitos separa POR CONSTRUCAO quando as assinaturas diferem. Com assinaturas iguais: `>= corte` agrupa, `piso <= s < corte` descarta (faixa cinzenta), `< piso` e serie nova. Empate exatamente no corte agrupa; exatamente no piso descarta | 02-03 `must_haves` e `<behavior>` |
| 2 | LEIT-01 empty | Qual o resultado para entrada vazia, de um elemento, ou nula? | Nome vazio nao produz chave (`None`). Catalogo vazio na primeira execucao e estado legitimo, e a primeira serie nasce dele. Linha sem conteudo no retangulo e LINHA VAZIA: marca fim de pagina, e nao e descarte nem leitura | 02-03 e 02-04 `must_haves` |
| 3 | LEIT-01 ordering | Quando elementos empatam, a ordem de saida e especificada e estavel? | Desempate deterministico no catalogo (maior score, depois ordem lexicografica da chave), nunca ordem de insercao. As linhas da `PaginaAceita` saem na ordem da grade, de cima para baixo | 02-03 e 02-05 `must_haves` |
| 4 | LEIT-02 unclassified | — | Falha FECHADA em tres peneiras, nesta ordem: tudo-ou-nada na celula (um run reprovado no piso E na margem derruba a celula inteira), a gramatica do numero (milhar em blocos de exatamente 3, decimal com exatamente 2), e a guarda de cruzamento contra `Unit price x Quantity`. Valor em centesimos como inteiro, nunca float; o unitario e lido para conferir e nunca guardado como preco | 02-04 `must_haves` Task 1 (as duas primeiras) e 02-06 (a terceira) |
| 5 | LEIT-03 unclassified | — | Pagina aceita so com dois frames consecutivos concordando na TUPLA PARSEADA das posicoes aceitas em AMBOS, com minimo de posicoes comparadas. Tres janelas consecutivas bit-identicas sao captura congelada, e a comparacao e sobre a janela inteira | 02-05 `must_haves` |
| 6 | LEIT-05 adjacency | Duas coisas iguais ou que se tocam fundem, colidem ou separam? | Recorte de coluna meio-aberto `[dx, dx + largura)`, a mesma convencao de `segmentar_glifos`; colunas vizinhas nunca compartilham um pixel. A convencao e contrato e fica presa por teste | 02-04 `must_haves` |
| 7 | LEIT-05 empty | Qual o resultado para entrada vazia, de um elemento, ou nula? | Retangulo de largura zero ou fora do painel e recusado no portao de carga, com o conserto na mensagem; a leitura de mercado simplesmente nao acontece. Recorte valido sobre linha vazia devolve linha vazia, que marca o fim da pagina | 02-01 e 02-04 `must_haves` |
| 8 | LEIT-05 ordering | Quando elementos empatam, a ordem de saida e especificada e estavel? | Quando as duas escalas caem na mesma serie, o `nome_exibido` gravado vem sempre da escala 3x, por regra escrita e nao por acaso de ordem | 02-04 `must_haves` |

**Suposicoes que sobram DECLARADAS, e nao resolvidas** (herdadas do `02-RESEARCH.md`, cada uma
com a task que a fecha):

- **A4 (risco ALTO):** um limiar de dispersao em torno de 0,10 separa "limpa" de "coberta". A
  folga do caso apertado (marcacao de alvo) e de ~4x e foi medida em UM frame. **Fechada pela
  varredura da Task 1 do 02-02**, que recusa propor numero quando as populacoes se sobrepoem.
- **A8 (risco ALTO):** os moldes de digito, cortados da coluna de preco, casam tambem os digitos
  DENTRO do nome. **Fechada pela Task 1 do 02-03** contra o gabarito conhecido, e a decisao que
  ela alimenta esta no portao da Task 2 daquele plano.
- **A2 (risco MEDIO):** o corte de brilho separa o rotulo de cabecalho da seta em todas as peles
  e resolucoes. Medido numa so. **Mitigada, nao fechada:** o corte vai para o `calibration.json`,
  medido no frame do proprio usuario, e nunca para o fonte.
- **A3 (risco MEDIO):** o trecho da sonda de fundo e "sem texto" em toda linha do layout de
  negociacao. **Mitigada:** o trecho e escolhido por varredura e derivado da geometria das
  colunas calibradas, nao digitado.
- **A5 (risco MEDIO):** cortar um molde por paridade de banda de fundo elevaria as medianas de
  `0` e `8`. **NAO endereçada nesta fase** — o conserto de raiz dos moldes fica registrado como
  candidato para depois. Enquanto isso valem a margem calibrada, a gramatica do numero e, desde a
  decisao do usuario de 2026-08-29, a guarda de cruzamento do **02-06**, que e a unica das tres
  que pega a substituicao `0`<->`8`.
- **A7 (risco MEDIO):** a coluna `Quantity` le pelo mesmo pipeline do preco. **Fechada pela
  Task 2 do 02-02**, que a mede contra o gabarito 10/5/48 do spike.
</bordas_da_sonda>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

O `02-CONTEXT.md` registra as decisoes do usuario em prosa, sem numera-las. Os IDs abaixo
sao estaveis e seguem a ORDEM DE LEITURA da secao `<decisions>` daquele arquivo — eles nao
inventam decisao nenhuma, so dao nome ao que ja esta escrito la.

| ID | Decisao (do `02-CONTEXT.md`) |
|---|---|
| D-09 | O v1 le SOMENTE o layout calibrado — e o layout calibrado passa a ser a GRADE DE NEGOCIACAO (REVISADO 2026-08-29 pelo censo dos 335 frames). |
| D-11 | O layout e reconhecido por molde do cabecalho de coluna, cortado SEM a seta de ordenacao. |
| D-12 | Linha vazia e decidida por AUSENCIA DE CONTEUDO dentro do retangulo da linha, nunca por cor de fundo. |
| D-14 | O sinal de oclusao e a FAIXA DE FUNDO ALTERNADA da linha, medida num trecho sem texto. A recusa NUNCA vem da confianca do casamento. |
| D-15 | A recusa e por LINHA, nao por pagina. A linha descartada NAO conta como desacordo no estabilizador. |
| D-16 | A marcacao de alvo usa o MESMO mecanismo, sem caso especial. |
| D-17 | O usuario ve a recusa como CONTAGEM no console e o motivo no log — e NADA no CSV. |
</indice_de_decisoes>

<verification>
- `python -m pytest tests/test_mercado_leitura.py tests/test_mercado_pagina.py tests/test_mercado_glifos.py tests/test_calibrar_mercado.py -q` verde
- `python -m pytest tests/test_mercado_27x.py -q` verde — o detector de morte segue intocado:
  esta fase NAO toca `l2scanner/rastreador.py` nem o gate de brilho da barra propria em
  `l2scanner/visao.py` (`barra_propria_legivel`, `_moldura_da_barra_propria`,
  `_bordas_da_barra_intactas`). O consumidor de oclusao e DETC-02, na Fase 4
- `python -m pytest tests/test_firewall_escopo.py -q` verde
- Suite completa: `python -m pytest -q`. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um
  `KeyboardInterrupt` que aborta a sessao perto de ~88 testes. **Abortar nao e falhar — rode de
  novo.** Baseline verde: **1704 passed, 2 skipped**
</verification>

<success_criteria>
Uma linha da grade de negociacao atravessa todas as camadas ate uma pagina aceita, contra
fixture versionada e num clone limpo; toda linha coberta cai antes de custar OCR; uma pagina de
layout nao calibrado e recusada alta sem que uma linha seja lida. A guarda de cruzamento nao
entra aqui: ela e o 02-06, e depende do veredito que o 02-02 mede.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-04-SUMMARY.md` when done
</output>
