---
phase: 02-leitura-de-p-gina
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - l2scanner/mercado_geometria.py
  - tools/medir_oclusao.py
  - tools/medir_leitura_de_glifo.py
  - tests/test_mercado_geometria.py
  - tests/test_medir_leitura_de_glifo.py
autonomous: true
requirements: [LEIT-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O corte entra no `calibration.json` por VARREDURA, nunca por escolha — e a mesma regra que derrubou o 88 herdado do `WRatio` (D-05)"
    - "O sinal de oclusao medido aqui e a FAIXA DE FUNDO ALTERNADA num trecho sem texto, e nao a confianca do casamento (D-14)"
    - "O limiar de dispersao que separa linha limpa de linha coberta e MEDIDO por varredura sobre as 8 gravacoes, e nao escolhido — a marcacao de alvo e o caso apertado (0,12 contra 0,027 das limpas, folga de ~4x medida em UM frame) e e ela que decide o numero"
    - "O piso e a margem de LEITURA de glifo sao numeros PROPRIOS, medidos sobre glifos reais de tela, gravados em chaves proprias — `mercado_limiar_de_glifo = 0.8555` NAO e reaproveitado, porque ele certifica separabilidade do CONJUNTO de moldes e rejeitaria praticamente todo `8` da tela"
    - "A sonda de fundo mede DISPERSAO (fracao de pixels fora da moda), nunca 'a moda e 48 ou 66' — medido: sob tooltip a moda continua 48 em varias linhas, e so a dispersao separa"
    - "A ferramenta PROPOE os numeros e os grava no calibration.json; nenhum deles entra no fonte"
    - "A leitura da coluna Quantity foi MEDIDA pelo mesmo pipeline do preco antes de qualquer codigo de producao depender disso (A7 da pesquisa era deducao, nao medicao)"
    - "`nivel_de_fundo_da_linha` devolve None quando nao da para medir, e nunca levanta — o limiar de decisao NAO mora em mercado_geometria.py"
  artifacts:
    - path: "l2scanner/mercado_geometria.py"
      provides: "nivel_de_fundo_da_linha — (moda, dispersao) num trecho sem texto, com folga de linhas nas pontas"
      contains: "nivel_de_fundo_da_linha"
    - path: "tools/medir_oclusao.py"
      provides: "varredura da dispersao de fundo sobre as 8 gravacoes; propoe e grava mercado_sonda_do_fundo e mercado_limiar_de_dispersao_do_fundo"
      contains: "mercado_limiar_de_dispersao_do_fundo"
    - path: "tools/medir_leitura_de_glifo.py"
      provides: "distribuicao de score e margem sobre glifos REAIS das colunas calibradas; propoe e grava piso e margem de leitura"
      contains: "mercado_margem_de_leitura_de_glifo"
    - path: "tests/test_mercado_geometria.py"
      provides: "a sonda de fundo afirmada contra fixtures VERSIONADAS de linha limpa, linha sob tooltip e linha sob marcacao de alvo"
      contains: "nivel_de_fundo_da_linha"
  key_links:
    - from: "tools/medir_oclusao.py"
      to: "l2scanner/mercado_geometria.py"
      via: "a ferramenta VARRE usando a mesma primitiva que a producao vai usar — medir de um jeito e decidir com outro seria comparar convencoes"
      pattern: "nivel_de_fundo_da_linha"
    - from: "tools/medir_leitura_de_glifo.py"
      to: "l2scanner/calibrar_mercado.py"
      via: "classifica com segmentar_glifos + o mesmo alinhamento por preenchimento da matriz de confusao"
      pattern: "segmentar_glifos"
---

<objective>
Produzir, por ferramenta que mede, os quatro numeros que esta fase precisa e que ainda nao
existem: o retangulo e o limiar da sonda de oclusao, e o piso e a margem de LEITURA de glifo.

Nenhum deles pode ser herdado. `mercado_limiar_de_glifo = 0.8555` e o limiar de COLISAO,
derivado de `(1.0 + pior_par)/2` sobre molde-contra-molde; medido sobre 2.057 glifos reais de
tela ele rejeitaria 18% deles, e o `8` tem MEDIANA 0,7242 contra o proprio molde — um piso ali
mataria todo preco com `8`, `135,88` inclusive. E `0,12` de margem, herdado de `identidade.py`,
e maior que a margem minima medida do par `0`x`8` (0,0370), que e o par mais estreito do
sistema inteiro.

Purpose: e a disciplina fundadora do projeto. Constante magica no fonte garante reescrita na
primeira "nao funciona no meu PC"; e um numero escolhido em vez de medido e uma promessa que
ninguem conferiu. A onda de leitura (02-04, 02-05) nao pode comecar antes desta.

Output: dois `tools/medir_*.py`, a primitiva `nivel_de_fundo_da_linha` em
`mercado_geometria.py`, e quatro chaves preenchidas no `calibration.json` com o numero medido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-RESEARCH.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A sonda de fundo, e o limiar de dispersao MEDIDO por varredura sobre as 8 gravacoes</name>
  <precondition>`calibration.json` traz `mercado_grade.layout == "negociacao"` e as quatro colunas — o portao humano do 02-01 ja rodou. E `recordings/` existe: num worktree, apenas pelo caminho ABSOLUTO do checkout principal (`C:/Users/refun/Desktop/Lineage2-warnings/recordings`), somente leitura.</precondition>
  <files>l2scanner/mercado_geometria.py, tools/medir_oclusao.py, tests/test_mercado_geometria.py, tests/fixtures/mercado/</files>
  <read_first>
    - `l2scanner/mercado_geometria.py`: o charter (:1-9), `TOLERANCIA_DE_NIVEL` (:105) e o
      comentario de :96-98 que ja registra os fundos 48 e 66; `perfil_por_mediana` (:193),
      `trechos_de_nivel` (:212), `extensao_do_separador` (:313) e `fim_da_alternancia` (:353-398)
      — em especial a folga de 2 linhas nas pontas, que e o cuidado a copiar
    - `02-RESEARCH.md`, pergunta 4 inteira — os valores medidos por condicao, as DUAS ressalvas
      (a moda NAO muda sob tooltip em varias linhas; os valores absolutos nao sao universais
      entre layouts) e o esboco de `dispersao_do_fundo`
    - `02-RESEARCH.md`, Assumptions Log A3 e A4 — A4 e risco ALTO e e exatamente o que esta
      task existe para fechar
    - `tools/conferir_gravacoes_do_spike.py` — o precedente de ferramenta de medicao em lote
      que le `recordings/` e imprime
    - `tests/test_mercado_glifos.py:1-60` — o cabecalho que declara o material e a razao de o
      teste rodar sobre fixture VERSIONADA e nunca sobre `recordings/`
  </read_first>
  <behavior>
    - `nivel_de_fundo_da_linha` sobre a fixture de linha limpa devolve dispersao abaixo de 0,10
    - `nivel_de_fundo_da_linha` sobre a fixture de linha coberta por tooltip devolve dispersao
      acima de 0,40
    - `nivel_de_fundo_da_linha` sobre a fixture de linha sob a marcacao de alvo devolve
      dispersao ESTRITAMENTE maior que a da linha limpa do MESMO frame — a relacao e o que se
      afirma, o valor absoluto e faixa
    - Um retangulo de largura zero ou fora da imagem devolve `None`, sem levantar
    - Uma linha cuja moda de fundo e 48 e outra cuja moda e 66 dao dispersoes comparaveis: o
      resultado NAO depende de conhecer os dois valores
  </behavior>
  <action>
Escrever `nivel_de_fundo_da_linha(cinza, retangulo, folga) -> tuple[int, float] | None` em
`l2scanner/mercado_geometria.py`. Ela recorta o retangulo, descarta `folga` linhas em cima e
embaixo (o mesmo cuidado de `fim_da_alternancia`, :470-472), calcula a MODA dos niveis de cinza
e devolve `(moda, fracao de pixels que se afastam da moda por mais de TOLERANCIA_DE_NIVEL)`.
Devolve `None` quando o recorte fica vazio ou sai da imagem — o arquivo inteiro reporta em vez
de levantar, e o charter dele proibe abrir janela, ler teclado ou escrever arquivo.

O LIMIAR DE DECISAO NAO MORA AQUI. A geometria mede; quem decide e `mercado_leitura.py` (02-04),
com o numero vindo do `calibration.json`. A docstring precisa dizer isso, e dizer POR QUE a
grandeza e DISPERSAO e nao "a moda e 48 ou 66": medido em `tooltip/frame_000015`, nas linhas
2, 4 e 6 a moda continua 48 mesmo cobertas — testar a moda falharia. A dispersao e
auto-referente e nao depende de conhecer os valores do layout.

Escrever `tools/medir_oclusao.py`. Ela recebe `--gravacoes <caminho>` (aceita caminho absoluto,
para funcionar de dentro de um worktree), `--calibracao <caminho>` e `--gravar`. Para cada frame
com painel aberto de cada uma das 8 gravacoes: localiza o painel com `RastreioDoPainel`, fatia a
grade nas `linhas_por_pagina` linhas, e varre a linha em blocos de largura fixa medindo a
dispersao em cada um, para ESCOLHER o trecho "sem texto" em vez de supo-lo. O trecho escolhido e
o que minimiza a dispersao mediana em TODAS as linhas de TODOS os frames limpos, e ele e
derivado dos limites das colunas calibradas — o vao entre o fim da coluna do nome e o comeco da
coluna Quantity —, nunca de um par de numeros digitado. A pesquisa mediu esse vao em
x ∈ [180, 510) relativo a esquerda da grade; se a varredura discordar, vale a varredura, e a
discordancia entra no relatorio.

A ferramenta imprime, e o SUMMARY registra: a distribuicao de dispersao separada por condicao
(limpa; sob tooltip, usando `recordings/20260828-061253-mercado-tooltip`; sob marcacao de alvo,
usando `recordings/20260828-061409-mercado-alvo-sobreposto`), com minimo, p5, mediana, p95 e
maximo de cada. Propoe o limiar como o ponto que separa as duas populacoes com a MAIOR folga
relativa, e diz explicitamente qual e a folga no caso apertado — a marcacao de alvo, que a
pesquisa mediu em UM frame so. Se as populacoes se sobrepuserem em qualquer frame, a ferramenta
NAO propoe numero: ela imprime os frames que se sobrepoem e sai com codigo diferente de zero,
porque um limiar que nao separa e pior que nenhum.

Com `--gravar`, escreve `mercado_sonda_do_fundo` (`dx0`, `dx1`, `folga`) e
`mercado_limiar_de_dispersao_do_fundo` no `calibration.json`, por load-mutate-save, e imprime o
que gravou.

Cortar para `tests/fixtures/mercado/` os recortes de linha que os testes desta primitiva usam,
resgatados dos frames nomeados, com o cabecalho de teste declarando de onde veio cada um:
`linha_limpa_par_f010.png` e `linha_limpa_impar_f010.png` (de `pagina-cheia/frame_000010`),
`linha_sob_tooltip_f015.png` e `linha_limpa_no_frame_do_tooltip_f015.png` (de
`tooltip/frame_000015`, linhas 0 e 9), `linha_sob_alvo_f024.png` e
`linha_limpa_no_frame_do_alvo_f024.png` (de `alvo-sobreposto/frame_000024`, linhas 0 e 5).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_geometria.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_geometria.py -q` passa, com testes que afirmam a
      RELACAO (coberta > limpa no mesmo frame) e a FAIXA de cada condicao, sobre as 6 fixtures
      novas versionadas — e nao um valor exato preso
    - `python -c "import inspect,l2scanner.mercado_geometria as g; s=inspect.getsource(g); import re; assert 'cv2.imshow' not in s and 'input(' not in s; print('charter ok')"` imprime `charter ok`
    - A varredura roda: `.venv/Scripts/python.exe tools/medir_oclusao.py --gravacoes <abs>/recordings`
      imprime a distribuicao por condicao e o limiar proposto, e sai com codigo 0
    - Rodada com `--gravar`, `calibration.json` passa a ter `mercado_sonda_do_fundo` e
      `mercado_limiar_de_dispersao_do_fundo`, e `Calibracao.carregar` os aceita
    - O SUMMARY registra os numeros medidos e nomeia a folga do caso apertado (marcacao de alvo)
  </acceptance_criteria>
  <done>A sonda existe como primitiva pura, e o limiar que a usa foi medido por varredura sobre as 8 gravacoes e gravado no calibration.json — nao escolhido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O piso e a margem de LEITURA de glifo, medidos sobre glifos reais das colunas calibradas</name>
  <precondition>`calibration.json` traz `mercado_coluna_do_total`, `mercado_coluna_da_quantidade` e `mercado_coluna_do_unitario` — o portao humano do 02-01 ja rodou.</precondition>
  <files>tools/medir_leitura_de_glifo.py, tests/test_medir_leitura_de_glifo.py, tests/fixtures/mercado/</files>
  <read_first>
    - `l2scanner/calibrar_mercado.py:359-455` (`segmentar_glifos` e a docstring de 45 linhas que
      carrega a convencao de faixa COMPARTILHADA e a medicao 8/8)
    - `l2scanner/calibrar_mercado.py:501-568` (`_alinhar_por_preenchimento`, `_par_incalculavel`)
      e `:1596-1643` (`propor_rotulo`, o argmax + margem que vira a leitura de producao)
    - `l2scanner/calibrar_mercado.py:1283-1387` — as constantes de ferramenta
      `MINIMO_PARA_PROPOR_ROTULO`/`MARGEM_MINIMA_PARA_PROPOR` e o comentario que registra a
      medicao refutada do piso unico de 0,95
    - `l2scanner/mercado_visao.py:102-136` (`casamento_da_ancora`) e `:667-741`
      (`glifos_de_calibracao`)
    - `02-RESEARCH.md`, Pitfall 1 e Pitfall 2 inteiros — a distribuicao dos 2.057 glifos e a
      margem 0,0370 do par `0`x`8`
    - `02-RESEARCH.md`, Open Questions 5 e 6 — a coluna Quantity (A7, deducao) e o cruzamento
      `Total / Quantity` contra o unitario exibido
    - `SPIKE-RESPOSTAS.md` secoes 2 e 4 (em `.planning/workstreams/mercado/phases/01-*/`) — o
      gabarito de precos e quantidades lidos a olho pelo usuario
  </read_first>
  <behavior>
    - Sobre `tests/fixtures/mercado/glifos_precos_f010.png` a ferramenta le
      `100,00 3,00 18,90 7,50 18,00 2,45`, dígito a dígito, com os 13 moldes do calibration.json
    - Com o piso posto em 0,8555 a mesma leitura PERDE linhas que contem `8` — o teste afirma a
      relacao, para prender por escrito por que o limiar de colisao nao serve de piso de leitura
    - Uma celula em que qualquer run nao passa no piso E na margem devolve `None` inteiro,
      nunca um numero parcial
    - A ferramenta nao propoe numero nenhum quando a populacao de acerto e a de erro se
      sobrepoem; ela imprime os casos e sai com codigo diferente de zero
  </behavior>
  <action>
Escrever `tools/medir_leitura_de_glifo.py`, com `--gravacoes`, `--calibracao` e `--gravar`.
Ela varre os frames com painel aberto das 8 gravacoes, recorta as colunas CALIBRADAS
(`mercado_coluna_do_total`, `mercado_coluna_da_quantidade`, `mercado_coluna_do_unitario`) linha
a linha, segmenta com `segmentar_glifos` e classifica cada run contra os 13 moldes com
`_alinhar_por_preenchimento` + `casamento_da_ancora` — exatamente a mecanica de `propor_rotulo`,
sem reescrever nenhuma, porque reaproveitar a mecanica e o que faz o numero significar a mesma
coisa dos dois lados.

Ela produz TRES relatorios, e os tres entram no SUMMARY:

1. A DISTRIBUICAO. Para o melhor score e para a margem sobre o segundo colocado: minimo, p1,
   p5, mediana, e o pior por rotulo. Propoe o par (piso, margem) como o maior par que ainda
   aceita todos os glifos cuja leitura bate com o gabarito de campo, com folga declarada. O
   relatorio nomeia explicitamente o par `0`x`8` — a pesquisa mediu a menor margem do sistema
   ali (0,0370) e e ele que dimensiona a margem, nao o `0,12` herdado de `identidade.py`.
   NAO usar `mercado_limiar_de_glifo` como ponto de partida: registrar no cabecalho da
   ferramenta que ele e o limiar de COLISAO, `(1.0 + pior_par)/2`, medido molde-contra-molde, e
   que sobre glifo real ele rejeita 18% deles.

2. A COLUNA QUANTITY, que a pesquisa nunca mediu (A7 era deducao). Relatar: quantos runs saem
   por linha, se aparece separador de milhar, e se o carimbo de quantidade desenhado sobre o
   ICONE invade o retangulo calibrado — a pesquisa viu esse carimbo produzir lixo (`436,569,,,96,5,3`)
   quando a coluna era localizada por `numeros_com_sufixo` em vez de retangulo calibrado.
   Conferir contra o gabarito da secao 4 do `SPIKE-RESPOSTAS.md` (quantidades 10, 5 e 48 em
   `scroll-transicao/frame_000012`).

3. O CRUZAMENTO, medicao apenas, sem ligar nada. Para cada linha em que as tres colunas leram:
   a fracao de linhas em que `Total / Quantity` bate com o unitario exibido dentro de uma
   tolerancia de arredondamento a duas casas, e a tolerancia honesta que faria a relacao fechar
   em 100% das linhas medidas. **Esta e a unica saida desta ferramenta que NAO vira comportamento
   nesta fase** — ela existe para dar ao usuario a evidencia de uma decisao que ainda nao foi
   tomada (o cruzamento como guarda independente contra o par `0`x`8`). O relatorio precisa
   dizer isso em voz alta, para ninguem confundir medicao com mecanismo.

Com `--gravar`, escreve `mercado_limiar_de_leitura_de_glifo` e
`mercado_margem_de_leitura_de_glifo` por load-mutate-save. Os dois numeros vao para chaves
PROPRIAS, e o comentario ao lado registra por que nao sao aliases de `mercado_limiar_de_glifo`:
aliasar os dois faria o afrouxamento de um viajar para o outro, que e a razao ja escrita em
`calibrar_mercado.py:344-350`.

Os testes rodam sobre as fixtures VERSIONADAS que ja existem (`glifos_precos_f010.png`,
`glifos_unitario_f010.png`) mais uma nova, `glifos_quantidade_f012.png`, cortada da coluna
Quantity de `scroll-transicao/frame_000012` com o gabarito 10/5/48. Eles nunca leem
`recordings/`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_medir_leitura_de_glifo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_medir_leitura_de_glifo.py -q` passa, com um teste que le os
      seis precos de `glifos_precos_f010.png` digito a digito e um que afirma que um piso em
      0,8555 perde leitura que o piso medido mantem
    - Existe teste que afirma o TUDO OU NADA: uma celula com um run reprovado devolve `None`,
      nunca um numero truncado
    - A varredura roda: `.venv/Scripts/python.exe tools/medir_leitura_de_glifo.py --gravacoes <abs>/recordings`
      imprime os tres relatorios e sai com codigo 0
    - Rodada com `--gravar`, `calibration.json` ganha `mercado_limiar_de_leitura_de_glifo` e
      `mercado_margem_de_leitura_de_glifo`, e `Calibracao.carregar` os aceita
    - O SUMMARY registra: o par (piso, margem) proposto com a folga; o veredito sobre a coluna
      Quantity; e a taxa de fechamento do cruzamento `Total / Quantity` contra o unitario, com a
      nota de que essa terceira medicao NAO virou comportamento nesta fase
  </acceptance_criteria>
  <done>O piso e a margem de leitura sao numeros medidos sobre glifos reais das colunas calibradas, gravados em chaves proprias; a coluna Quantity deixou de ser deducao; e o cruzamento de unitario esta medido e reportado, sem ter sido ligado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco -> ferramenta (`recordings/`) | PNGs de terceiros do ponto de vista do processo; a pasta e somente leitura |
| ferramenta -> arquivo (`calibration.json`) | A ferramenta ESCREVE no arquivo que carrega 13 moldes e 3 ancoras insubstituiveis |
| arquivo -> processo (`calibration.json`) | Entrada nao confiavel, ja coberta pelo portao de carga do 02-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | `--gravar` sobrescrevendo `calibration.json` | high | mitigate | Load-mutate-save: carrega a `Calibracao` inteira, atribui SO as chaves proprias da ferramenta, grava por `os.replace`. Nunca montar uma `Calibracao` do zero — isso apagaria moldes e ancoras |
| T-02-08 | Tampering | escrita em `recordings/` | high | mitigate | As duas ferramentas so ABREM para leitura. Teste afirma por inspecao de fonte que nenhuma delas chama `imwrite`/`open(..., "w")` sobre um caminho sob `recordings` |
| T-02-09 | Spoofing | limiar proposto sobre populacoes sobrepostas | high | mitigate | As duas ferramentas RECUSAM propor numero quando as populacoes se sobrepoem: imprimem os casos e saem com codigo diferente de zero. Um limiar que nao separa e pior que nenhum |
| T-02-10 | Information Disclosure | relatorio de medicao vazando conteudo de tela | low | accept | Os relatorios imprimem numeros e nomes de item do jogo. Nao ha segredo, token nem `.env` no caminho desta fase |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install nesta fase.** As duas ferramentas usam so `cv2`, `numpy` e stdlib, todos ja no `.venv`. `tests/test_firewall_escopo.py` segue varrendo o venv instalado |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Simbolos novos:**
- `l2scanner/mercado_geometria.py`: `nivel_de_fundo_da_linha(cinza, retangulo, folga) -> tuple[int, float] | None`
- `tools/medir_oclusao.py`: `main(argv)`, `varrer(gravacoes, cal)`, `escolher_o_trecho_sem_texto(...)`, `propor_o_limiar(...)`
- `tools/medir_leitura_de_glifo.py`: `main(argv)`, `varrer(gravacoes, cal)`, `classificar_celula(...)`, `propor_piso_e_margem(...)`, `medir_o_cruzamento_do_unitario(...)`

**Flags de CLI novas:** `tools/medir_oclusao.py --gravacoes <caminho> --calibracao <caminho> --gravar`;
`tools/medir_leitura_de_glifo.py --gravacoes <caminho> --calibracao <caminho> --gravar`

**Chaves de `calibration.json` PREENCHIDAS** (declaradas no 02-01): `mercado_sonda_do_fundo`,
`mercado_limiar_de_dispersao_do_fundo`, `mercado_limiar_de_leitura_de_glifo`,
`mercado_margem_de_leitura_de_glifo`

**Fixtures versionadas novas em `tests/fixtures/mercado/`:** `linha_limpa_par_f010.png`,
`linha_limpa_impar_f010.png`, `linha_sob_tooltip_f015.png`,
`linha_limpa_no_frame_do_tooltip_f015.png`, `linha_sob_alvo_f024.png`,
`linha_limpa_no_frame_do_alvo_f024.png`, `glifos_quantidade_f012.png`

**Arquivos de teste novos:** `tests/test_medir_leitura_de_glifo.py`; testes novos em
`tests/test_mercado_geometria.py`
</artifacts_this_phase_produces>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

O `02-CONTEXT.md` registra as decisoes do usuario em prosa, sem numera-las. Os IDs abaixo
sao estaveis e seguem a ORDEM DE LEITURA da secao `<decisions>` daquele arquivo — eles nao
inventam decisao nenhuma, so dao nome ao que ja esta escrito la.

| ID | Decisao (do `02-CONTEXT.md`) |
|---|---|
| D-05 | O CORTE precisa ser REMEDIDO, e com ferramenta. Nenhum corte entra no codigo antes dessa medicao. |
| D-14 | O sinal de oclusao e a FAIXA DE FUNDO ALTERNADA da linha, medida num trecho sem texto. A recusa NUNCA vem da confianca do casamento. |
</indice_de_decisoes>

<verification>
- `python -m pytest tests/test_mercado_geometria.py tests/test_medir_leitura_de_glifo.py tests/test_mercado_glifos.py -q` verde
- `python -m pytest tests/test_firewall_escopo.py -q` verde
- As duas varreduras rodadas no checkout PRINCIPAL (onde `recordings/` existe), com a saida
  registrada no SUMMARY. Elas NAO sao gate de plano: `recordings/` e gitignored e nao se
  materializa num worktree nem num clone limpo, entao nenhum criterio verde depende dela
- Suite completa: `python -m pytest -q`. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um
  `KeyboardInterrupt` que aborta a sessao perto de ~88 testes. **Abortar nao e falhar — rode de
  novo.** Baseline verde: **1704 passed, 2 skipped**
</verification>

<success_criteria>
`calibration.json` carrega quatro numeros novos, cada um produzido por varredura sobre as 8
gravacoes e cada um com a sua folga declarada por escrito; a coluna Quantity deixou de ser
deducao; e nenhum desses numeros aparece como constante no fonte de producao.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-02-SUMMARY.md` when done
</output>
