---
phase: 02-leitura-de-p-gina
plan: 05
type: execute
wave: 6
depends_on: ["02-04", "02-06"]
files_modified:
  - l2scanner/mercado_catalogo.py
  - l2scanner/mercado_pagina.py
  - .gitignore
  - tests/test_mercado_catalogo.py
  - tests/test_mercado_pagina.py
  - tests/test_mercado_replay.py
autonomous: true
requirements: [LEIT-01, LEIT-03]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As linhas da `PaginaAceita` saem na ordem da grade, de cima para baixo, sempre — a ordem e contrato, porque a Fase 3 grava uma observacao por linha e a ordem e o unico vinculo com o que o usuario viu na tela"
    - "O catalogo de nomes vive em ARQUIVO PROPRIO, ao lado do CSV de observacoes e nunca no calibration.json — aquele arquivo e reescrito inteiro pela ferramenta de calibracao, e o catalogo e dado ACUMULADO que sumiria na primeira recalibracao (D-07)"
    - "Nome novo entra DIRETO como serie nova, com contagem de avistamentos e data da primeira vez, sem quarentena — a quarentena esconderia a primeira aparicao, que e justamente o evento que o usuario quer ver (D-08)"
    - "Uma escrita interrompida no meio de uma linha faz a leitura seguinte descartar APENAS a linha truncada, com aviso, e nunca tratar o arquivo inteiro como corrompido"
    - "O catalogo nao tem poda, de proposito: estatistica de item e para sempre"
    - "Uma pagina so e aceita quando dois frames consecutivos concordam nas linhas PARSEADAS, e a linha DESCARTADA fica FORA da comparacao — ela nao conta como desacordo (LEIT-03, D-15, D-18)"
    - "Tres janelas consecutivas bit-identicas sao captura congelada: aviso alto e nenhuma pagina aceita. A comparacao e sobre a JANELA INTEIRA, nunca sobre a grade — medido, o painel e bit-estavel e grade parada e o caso NORMAL (D-19)"
    - "A Fase 2 escreve o catalogo de nomes e NAO escreve observacao nenhuma: o CSV de observacoes e da Fase 3. Dois arquivos, dois donos"
    - "`mercado_minimo_de_linhas_comparadas` e LIDO do calibration.json com o valor que a varredura do 02-02 mediu sobre a INTERSECAO entre frames vizinhos — a mesma grandeza que esta comparacao julga. Ele nao e escolhido por este plano, e chave nula desliga a leitura em vez de valer zero"
    - "O replay atras do portao itera a lista NOMEADA das 8 gravacoes do censo, importada da constante das ferramentas de medicao — nunca um glob sobre `recordings/`"
  artifacts:
    - path: "l2scanner/mercado_catalogo.py"
      provides: "o catalogo duravel em CSV com separador ponto-e-virgula, escrita atomica, leitura defensiva e sem poda"
      contains: "os.replace"
    - path: "l2scanner/mercado_pagina.py"
      provides: "congelamento por janela inteira, acordo entre dois frames sobre a tupla parseada, e os contadores de lidas e perdidas"
      contains: "array_equal"
    - path: ".gitignore"
      provides: "`.mercado/` como estado local e duravel, ao lado de `.loot/` e `.agenda/`"
      contains: ".mercado/"
    - path: "tests/test_mercado_replay.py"
      provides: "o replay sobre as 8 gravacoes atras de pytest.skip, mais a aceitacao da fase sobre fixtures versionadas"
      contains: "pytest.skip"
  key_links:
    - from: "l2scanner/mercado_catalogo.py"
      to: "l2scanner/config.py"
      via: "caminho derivado da RAIZ do projeto, nunca de entrada do usuario"
      pattern: "RAIZ"
    - from: "l2scanner/mercado_pagina.py"
      to: "l2scanner/mercado_catalogo.py"
      via: "a pagina aceita carrega a chave da serie que o catalogo produziu; o catalogo e o unico que escreve nele"
      pattern: "Catalogo"
---

<objective>
Fechar a fase: dar ao catalogo de nomes um arquivo proprio que sobrevive a uma recalibracao, e
ao estabilizador as duas guardas que faltam — o congelamento de captura e o acordo entre frames
com a linha descartada fora da conta.

O catalogo tem a natureza exata do estado durável que este repositorio ja sabe guardar: local,
acumulado, nunca podado. O `.gitignore` ja explica o porque para `.loot/` — versionar
misturaria a estatistica de maquinas diferentes. E o `os.replace` de `loot.py` ja carrega, por
escrito, a razao de a escrita ser atomica: no mesmo volume, ou fica o arquivo antigo inteiro ou
o novo inteiro, nunca meio.

O congelamento e a decisao que contradiz a leitura ingenua de LEIT-03, e ela e medida: a secao 9
do spike provou que o painel e BIT-ESTAVEL — o mundo atras mudou completamente e o retangulo da
ancora nao mudou um bit. Grade identica entre frames e o caso NORMAL de uma pagina parada;
usa-la como sinal de congelamento daria falso alarme o tempo todo. O que se mexe e o mundo atras
do painel, entao a comparacao e sobre a JANELA INTEIRA.

Purpose: sem esta ultima onda a Fase 3 nao tem uma `PaginaAceita` estavel para consumir, e o
catalogo — que e o dono da chave que ela vai referenciar — nao existe em disco.

Output: `.mercado/catalogo-de-nomes.csv`, o estabilizador completo, e o replay que prova a fase
inteira contra o material real, atras do `pytest.skip` que o repositorio ja usa.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-RESEARCH.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-03-SUMMARY.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-04-SUMMARY.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O catalogo de nomes em arquivo proprio — acumulado, atomico e sem poda</name>
  <precondition>O 02-04 e o 02-06 ja alteraram `l2scanner/mercado_leitura.py`: `ler_linha` le TRES colunas de numero e a guarda de cruzamento esta ligada ou desligada conforme o veredito do 02-02. Leia o `02-06-SUMMARY.md` antes de tocar em `mercado_pagina.py` — os motivos de `Descarte` que o estabilizador conta e o replay afirma dependem de qual rota da guarda foi executada.</precondition>
  <files>l2scanner/mercado_catalogo.py, .gitignore, tests/test_mercado_catalogo.py</files>
  <read_first>
    - `l2scanner/loot.py:185-300` — `RegistroDeLoot` inteiro: o construtor que possui a pasta
      (:192-194), o `# NAO tem poda, de proposito` (:189), a escrita atomica (:264-281) e a
      docstring da leitura defensiva (:286-295)
    - `l2scanner/calibracao.py:433-441` — a mesma escrita atomica, com a razao escrita com mais
      palavras: por que o temporario vive AO LADO do destino e nao no `%TEMP%`
    - `l2scanner/config.py:32` — `RAIZ = Path(__file__).resolve().parent.parent`
    - `l2scanner/gravador.py:81-86` — o precedente de justificar o MODO de abertura por escrito
    - `.gitignore`, o bloco "Saida em tempo de execucao" — o texto que explica `.loot/` e
      `.agenda/`, e a excecao `!tests/fixtures/`
    - `02-RESEARCH.md`, pergunta 8 — a forma proposta do CSV, as colunas e a fronteira Fase 2 x
      Fase 3
    - `02-CONTEXT.md`, os bullets do catalogo (D-07, D-08) e a discricao declarada sobre a forma
      exata do arquivo
    - `REQUIREMENTS.md`, secao Persistencia — a razao do separador ponto-e-virgula e as tres
      falhas que o CSV traz junto
  </read_first>
  <behavior>
    - Catalogo inexistente carrega como vazio e nao levanta: a primeira execucao numa maquina
      limpa e um estado legitimo
    - O primeiro nome visto vira serie nova com `avistamentos = 1` e `primeira_vez` carimbada
    - Ver o mesmo nome de novo sobe `avistamentos` e atualiza `ultima_vez`, sem criar serie
    - Um arquivo cuja ULTIMA linha esta truncada carrega todas as anteriores e descarta so ela,
      com aviso — o arquivo inteiro nunca e tratado como corrompido
    - Uma linha do meio do arquivo com numero errado de campos e descartada com aviso, e as
      demais carregam
    - Um `nome_exibido` editado a mao com um ponto-e-virgula dentro nao parte a linha em duas na
      releitura
    - Ida e volta: gravar e recarregar devolve as mesmas series, com as mesmas contagens
    - O arquivo nunca perde series: uma serie que nao apareceu nesta sessao continua la
  </behavior>
  <action>
Completar `l2scanner/mercado_catalogo.py` com a metade de arquivo. O caminho e
`RAIZ / ".mercado" / "catalogo-de-nomes.csv"`, SEMPRE derivado da raiz do projeto e nunca de
entrada do usuario. `.mercado/` e o terceiro diretorio-ponto de estado local durável, ao lado de
`.loot/` e `.agenda/`, e entra no `.gitignore` com a mesma prosa de razao que os outros dois tem:
versionar misturaria a estatistica de maquinas diferentes.

`Catalogo` possui a pasta no construtor (`mkdir(parents=True, exist_ok=True)`, como
`RegistroDeLoot`), carrega no arranque e mantem as chaves em memoria — com um escritor so, o
conjunto cabe. Colunas, separadas por ponto-e-virgula: `chave`, `nome_exibido`, `primeira_vez`,
`ultima_vez`, `avistamentos`. Usar o modulo `csv` da stdlib com `delimiter=";"`, e nao
`split(";")` a mao: o `nome_exibido` e editavel pelo usuario no Sheets e um ponto-e-virgula
digitado ali partiria a linha. O separador e ponto-e-virgula e nao virgula porque a decisao
travada da exibicao usa virgula decimal (`62,00`) e um CSV separado por virgula colapsaria tudo
numa coluna. Esta escolha vale por dois: a Fase 3 herda o dialeto que esta task fixar.

A escrita e atomica, copiada de `loot.py:264-281`: um `.tmp-<pid>` AO LADO do destino, seguido de
`os.replace`. O temporario nao vai para o `%TEMP%` porque `os.replace` entre volumes diferentes
nao e atomico e no Windows nem funciona — a razao ja esta escrita em `calibracao.py:433-441`.
Justificar o modo de abertura por escrito, como `gravador.py:81-86` faz.

A leitura e DEFENSIVA e a regra e literal: descartar a linha malformada com aviso, nunca tratar
o arquivo inteiro como corrompido. Um append interrompido trunca a ultima linha, e essa e a
mesma familia do FUND-01 — o dado parcial nao pode virar dado plausivel. O aviso nomeia o numero
da linha e o que ela tinha.

SEM PODA, de proposito, com o comentario escrito: estatistica de item e para sempre, igual ao
`.loot/`.

Nome novo entra DIRETO como serie nova, com `avistamentos` e `primeira_vez` — sem quarentena. A
quarentena esconderia a primeira aparicao, que e justamente o evento que o usuario quer ver, e a
contagem e o que deixa ele julgar a serie nova no Sheets. Os filtros ja existem e sao outros: o
acordo entre as duas escalas e a faixa cinzenta.

Deixar escrito, na docstring do modulo, a fronteira: **a Fase 2 escreve o catalogo de nomes; a
Fase 3 escreve o CSV de observacoes.** Dois arquivos, dois donos, nenhum compartilhado. A Fase 3
LE a chave que esta fase produziu e nunca escreve aqui; se ela encontrar uma chave que nao esta
no catalogo, isso e erro de programa e nao caso de uso.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_catalogo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_catalogo.py -q` verde, com testes para cada
      comportamento listado, usando `tmp_path` e nunca a `.mercado/` real
    - Existe teste que grava um arquivo com a ultima linha cortada no meio e afirma que as
      anteriores carregam e que so ela cai, com aviso capturado
    - Existe teste que grava um `nome_exibido` contendo ponto-e-virgula e afirma que a releitura
      devolve o nome inteiro
    - `grep -c '^\.mercado/' .gitignore` devolve 1
    - `python -c "import inspect,l2scanner.mercado_catalogo as m; s=inspect.getsource(m); assert 'os.replace' in s; print('atomico ok')"` imprime `atomico ok`
    - Nenhuma funcao deste modulo escreve observacao: teste de inspecao afirma que o modulo nao
      conhece o nome do arquivo de observacoes da Fase 3
  </acceptance_criteria>
  <done>O catalogo vive em `.mercado/catalogo-de-nomes.csv`, acumula sem podar, escreve de forma atomica e le de forma defensiva; a fronteira com a Fase 3 esta escrita no fonte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O estabilizador completo — congelamento pela janela inteira e acordo sobre a tupla parseada</name>
  <precondition>O 02-04 e o 02-06 ja alteraram `l2scanner/mercado_leitura.py`: `ler_linha` le TRES colunas de numero e a guarda de cruzamento esta ligada ou desligada conforme o veredito do 02-02. Leia o `02-06-SUMMARY.md` antes de tocar em `mercado_pagina.py` — os motivos de `Descarte` que o estabilizador conta e o replay afirma dependem de qual rota da guarda foi executada.</precondition>
  <files>l2scanner/mercado_pagina.py, tests/test_mercado_pagina.py</files>
  <read_first>
    - `l2scanner/mercado_visao.py:447-510` (`RastreioDoPainel`) — memoria entre ticks, as duas
      cadencias e o contador publico `self.varreduras`, que e o padrao a copiar para
      `frames_congelados`
    - `l2scanner/captura_janela.py:43` — `SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO = 5.0`, o precedente
      do intervalo de rebusca
    - `l2scanner/manutencao.py:466-527` — o acordo entre duas leituras com o desacordo tratado
      como estado proprio, e o log com `>>><<<`
    - `02-RESEARCH.md`, pergunta 7 — os custos medidos (`np.array_equal` 0,89 ms contra sha256
      3,70 ms e blake2b 6,81 ms) e a nota de desenho sobre listas de tamanhos diferentes
    - `02-CONTEXT.md`, secao "Estabilizador de pagina e cadencia" — D-18, D-19, D-20, D-21
    - `SPIKE-RESPOSTAS.md` secao 9 — a prova de que o painel e bit-estavel
  </read_first>
  <behavior>
    - Duas janelas DIFERENTES com a mesma pagina parada produzem acordo e `PaginaAceita`
    - Tres janelas consecutivas bit-identicas viram captura congelada: aviso alto, nenhuma pagina
      aceita, e o contador `frames_congelados` sobe
    - Duas janelas bit-identicas ainda NAO congelam — a regra e tres
    - Depois de uma janela diferente o contador de congelamento zera
    - Uma linha descartada no frame A e aceita no frame B nao gera desacordo: ela fica FORA da
      comparacao, que so olha as posicoes aceitas em AMBOS
    - Uma pagina em que sobram menos posicoes comparadas que o minimo nao e aceita, mesmo que as
      poucas concordem — e reportada como perdida com o motivo
    - Uma pagina cujas linhas mudaram entre os dois frames (rolagem) nao e aceita, e o frame novo
      vira a nova referencia
    - As linhas da `PaginaAceita` saem na ordem da grade, de cima para baixo, sempre
    - Uma pagina inteiramente vazia nao e aceita e nao conta como perda
    - `paginas_lidas` e `paginas_perdidas` contam as duas metades, e a soma delas mais as
      congeladas cobre todos os ticks com painel aberto
  </behavior>
  <action>
Completar `l2scanner/mercado_pagina.py`.

CONGELAMENTO. Guardar a janela anterior e comparar com `np.array_equal` — 0,89 ms medido, contra
3,70 ms do sha256 e 6,81 ms do blake2b; hash e 4 a 15 vezes mais caro e nao compra nada, porque
a regra exige lembrar de UM frame e um contador, nao de muitos. O custo de memoria e 7,2 MB, o
mesmo que ja se paga para capturar. Tres janelas consecutivas bit-identicas sao captura
congelada: aviso alto e nenhuma pagina aceita ate a janela mudar. A comparacao e sobre a JANELA
INTEIRA e o comentario tem de dizer por que, porque isso contradiz a leitura ingenua de LEIT-03:
medido, o painel e bit-estavel e grade parada e o caso NORMAL; o que se mexe e o mundo atras.
Comparar a grade daria falso alarme o tempo todo.

ACORDO. A chave de comparacao e a lista de tuplas parseadas — chave da serie, total em
centesimos, quantidade — das linhas ACEITAS, na ordem da grade. Nunca pixels: ja travado por
LEIT-03. A linha DESCARTADA fica fora da comparacao e nao conta como desacordo; entao quando a
linha 3 cai no frame A e passa no frame B, as listas tem tamanhos diferentes, e a leitura fiel
da regra e comparar apenas as posicoes aceitas em AMBOS. Isso abre um buraco que precisa de
fechadura propria: se sobrarem poucas posicoes, um acordo trivial aceitaria uma pagina
praticamente nao lida. Exigir um MINIMO de posicoes comparadas para a pagina valer, LENDO
`mercado_minimo_de_linhas_comparadas` do `calibration.json`. Esse numero nao se escolhe aqui: ele
foi medido pela varredura de oclusao do 02-02 sobre a MESMA grandeza que esta comparacao usa: o
tamanho da INTERSECAO entre as posicoes sobreviventes de dois frames vizinhos, e nao a contagem
por frame isolado — essa superestimaria, porque a intersecao e sempre menor ou igual ao minimo dos
dois. O `02-02-SUMMARY.md` traz a folga com que o piso separa par normal de par majoritariamente
coberto, e traz tambem a distribuicao por frame isolado ao lado, para conferencia. Reportar a pagina como perdida com o motivo quando ele nao for
atingido. Se a chave vier nula, a leitura de pagina nao acontece e avisa alto — feature OFF e o
default seguro, e um piso ausente valeria zero, que e o acordo trivial de volta.

CONTADORES publicos, no padrao de `RastreioDoPainel.varreduras`, para o teste afirmar
comportamento sem relogio: `paginas_lidas`, `paginas_perdidas`, `frames_congelados`,
`linhas_descartadas`. Eles sao a superficie que LEIT-04 (Fase 4) vai exibir como "li 7, perdi 3";
esta fase produz o numero e NAO desenha console.

CADENCIA. A busca do painel usa a memoria da ultima posicao com rebusca a cada 5 s, reusando
`RastreioDoPainel` e o precedente `SEGUNDOS_ENTRE_BUSCAS_DO_DIALOGO`; medido, a varredura custa
~45 ms numa janela de 1720x1392 e em 255 frames houve so 34 posicoes distintas — o painel fica
parado a maior parte do tempo. A leitura roda a 1 Hz, igual ao resto do scanner, e o orcamento do
pior tick foi medido em ~110 ms de 1000 ms.

Nada aqui toca `l2scanner/rastreador.py` nem o gate de brilho da barra propria em
`l2scanner/visao.py`. Acoplar o sinal de mercado ao detector de morte e precisamente a manobra
que causou o incidente das 27 mortes falsas; o consumidor de oclusao e DETC-02, na Fase 4.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_pagina.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_pagina.py -q` verde, com um teste por comportamento
      listado
    - Existe teste que afirma a borda do congelamento nos dois lados: duas janelas identicas NAO
      congelam, tres congelam
    - Existe teste que afirma que a linha descartada num dos frames nao conta como desacordo, e
      outro que afirma a recusa por minimo de posicoes comparadas
    - Existe teste que afirma a ordem das linhas da `PaginaAceita` de cima para baixo
    - `python -c "import inspect,l2scanner.mercado_pagina as m; s=inspect.getsource(m); assert 'hashlib' not in s; print('sem hash ok')"` imprime `sem hash ok`
    - `python -m pytest tests/test_mercado_27x.py -q` verde — o detector de morte nao mudou
  </acceptance_criteria>
  <done>O congelamento olha a janela inteira e exige tres iguais; o acordo compara tuplas parseadas nas posicoes aceitas em ambos os frames, com minimo proprio; e os contadores das duas metades existem.</done>
</task>

<task type="auto">
  <name>Task 3: O replay da fase — verde num clone limpo, e completo onde as gravacoes existem</name>
  <precondition>A metade completa do replay so roda onde `recordings/` existe, ou seja no checkout PRINCIPAL: a pasta e gitignored e nao se materializa num worktree nem num clone limpo. A metade que sempre roda vive de fixture versionada em `tests/fixtures/mercado/`. E o `02-06-SUMMARY.md` diz qual rota da guarda foi executada, o que muda quais motivos de descarte o replay pode afirmar.</precondition>
  <files>tests/test_mercado_replay.py, tests/fixtures/mercado/</files>
  <read_first>
    - `tests/test_mercado_27x.py:45-60` e `:266-278` — `RECORDINGS` derivado de `parents[1]` e a
      classe `TestOReplayCOMPLETO` com o `pytest.skip` cuja razao esta escrita
    - `tests/test_mercado_glifos.py:1-40` — o cabecalho que declara o material, de onde cada
      fixture veio, e o que NAO e contrato de proposito
    - `l2scanner/ocr.py:107-130` — `disponivel()`, o portao para pular teste que precise de WinRT
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-04-SUMMARY.md` — as fixtures
      versionadas que ja existem
  </read_first>
  <behavior>
    - Num clone limpo, sem `recordings/` e sem WinRT, a suite desta fase roda verde: tudo que
      afirma comportamento vem de fixture versionada
    - O teste que precisa das 8 gravacoes se pula sozinho com a razao escrita quando a pasta nao
      existe
    - O teste que precisa de OCR de verdade se pula sozinho quando `ocr.disponivel()` e falso
    - Sobre a janela versionada de pagina cheia, o replay produz as 10 linhas com nome agrupado e
      numeros lidos, e a segunda passada nao cria serie nova nenhuma
    - Sobre a janela versionada de tooltip, a contagem de descartadas bate com o numero de linhas
      cobertas, e as descobertas sao lidas
  </behavior>
  <action>
Escrever `tests/test_mercado_replay.py` em DUAS metades, seguindo o desenho que
`test_mercado_27x.py` ja usa.

A metade que sempre roda afirma a fase inteira sobre as fixtures VERSIONADAS: a janela de pagina
cheia atravessando ate `PaginaAceita` com as 10 linhas; a de tooltip com a contagem de
descartadas batendo; a de alvo com a linha 1 caindo; a de layout Adena sendo recusada pelo
portao; e a idempotencia do catalogo — a segunda passada sobre a mesma pagina nao cria serie
nova. Ela usa `tests/fixtures/mercado/calibracao_de_fixture.json`, o `tmp_path` para a `.mercado/`
e leitoras de OCR injetadas alimentadas por `leituras_de_nome.json`, para nao depender de WinRT.

A metade atras do portao afirma o material real. Ela comeca com o mesmo `pytest.skip` de
`test_mercado_27x.py`, com a razao escrita: `recordings/` e gitignored e nao se materializa num
worktree nem num clone limpo, entao um teste que dependesse dela ficaria verde nesta maquina e
amarelo em toda outra. Quando a pasta existe, ela varre **as 8 gravacoes NOMEADAS do conjunto de medicao** — as mesmas
constantes que as ferramentas `tools/medir_*.py` carregam, importadas de la e nunca redigitadas —
e afirma o que so o material real prova: que nenhum frame de layout nao calibrado produz linha lida; que a contagem
de paginas aceitas mais perdidas mais congeladas cobre todos os ticks com painel aberto; e que
nenhuma serie do catalogo nasce de uma linha que a sonda de oclusao recusou.

O cabecalho do arquivo declara o material, arquivo por arquivo, com o frame de origem de cada
fixture — e declara tambem o que NAO e contrato de proposito, no espirito do
`test_mercado_glifos.py`: os valores exatos de score e dispersao mudam com a convencao de
recorte e com a calibracao do usuario; o que se afirma e a RELACAO e a FAIXA.

Registrar no SUMMARY os numeros do replay completo rodado no checkout principal: paginas
aceitas, perdidas e congeladas por gravacao, series criadas, e linhas recusadas por motivo. Eles
sao a evidencia dos tres criterios de sucesso da fase, e a metade que precisa deles nao e gate,
por construcao.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_replay.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mercado_replay.py -q` verde, e o resumo mostra os testes da
      metade atras do portao como `skipped` quando `recordings/` nao existe
    - A metade que sempre roda nao referencia `recordings/` em nenhum caminho: teste de inspecao
      de fonte afirma que so a classe do portao usa a constante da pasta
    - Existe teste de idempotencia: a segunda passada sobre a mesma pagina versionada nao
      aumenta a contagem de series do catalogo
    - No checkout principal, com `recordings/` presente, a suite roda com a metade completa
      ativa, e os numeros por gravacao estao no SUMMARY
    - A metade atras do portao itera a lista NOMEADA das 8 gravacoes, importada da constante das
      ferramentas de medicao — nunca um glob sobre subdiretorios de `recordings/`, que arrastaria
      as 8 pastas fora do censo, incluindo a `pre-voo` com os seus 1502 PNGs
    - `python -m pytest -q` completa. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um
      `KeyboardInterrupt` que aborta a sessao perto de ~88 testes. Abortar nao e falhar — rode de
      novo. Baseline verde: 1704 passed, 2 skipped, mais os testes desta fase
  </acceptance_criteria>
  <done>A fase esta afirmada ponta a ponta por fixture versionada num clone limpo, e o replay sobre as 8 gravacoes roda no checkout principal atras do portao que o repositorio ja usa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo -> processo (`catalogo-de-nomes.csv`) | O usuario e convidado a corrigir `nome_exibido` no Sheets; o arquivo volta como entrada nao confiavel |
| processo -> arquivo (append) | Uma escrita interrompida deixa a ultima linha pela metade |
| tela -> processo (captura) | Uma captura congelada devolve o mesmo frame para sempre, e ele parece uma pagina valida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Tampering | linha truncada do catalogo virando serie plausivel | high | mitigate | Leitura defensiva: descartar SO a linha malformada, com aviso que nomeia a linha, e nunca tratar o arquivo inteiro como corrompido. Mesma familia do FUND-01 — o dado parcial nao pode virar dado plausivel |
| T-02-23 | Tampering | ponto-e-virgula digitado a mao no `nome_exibido` partindo a linha | medium | mitigate | Escrita e leitura pelo modulo `csv` da stdlib com `delimiter=";"`, que cita e escapa; nunca `split(";")` a mao |
| T-02-24 | Tampering | escrita interrompida corrompendo o catalogo inteiro | high | mitigate | `.tmp-<pid>` ao lado do destino seguido de `os.replace`, atomico no mesmo volume — o precedente de `loot.py:264-281`, com a razao ja escrita |
| T-02-25 | Spoofing | captura congelada lida como pagina viva | high | mitigate | Tres janelas consecutivas bit-identicas por `np.array_equal` sobre a JANELA INTEIRA; aviso alto e nenhuma pagina aceita. A grade e bit-estavel e nao serve de sinal |
| T-02-26 | Spoofing | acordo trivial sobre pouquissimas linhas aceitando pagina nao lida | high | mitigate | Minimo de posicoes comparadas em AMBOS os frames, com o numero MEDIDO pela varredura de oclusao do 02-02 (distribuicao de linhas sobreviventes por pagina) e lido do `calibration.json`. Abaixo dele a pagina e perdida com motivo; chave nula desliga a leitura em vez de valer zero |
| T-02-27 | Information Disclosure | catalogo versionado por engano | medium | mitigate | `.mercado/` no `.gitignore`, ao lado de `.loot/` e `.agenda/`, com a mesma prosa de razao. Teste afirma a linha |
| T-02-28 | Denial of Service | catalogo crescendo sem limite | low | accept | Sem poda, de proposito: estatistica de item e para sempre, como `.loot/`. O volume e de milhares de linhas, nao milhoes |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install nesta fase.** `csv`, `os` e `difflib` sao stdlib. `tests/test_firewall_escopo.py` segue varrendo o venv instalado com a banlist de 9 nomes |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**`l2scanner/mercado_catalogo.py` (completado):**
- `PASTA_DO_MERCADO = RAIZ / ".mercado"`, `ARQUIVO_DO_CATALOGO = "catalogo-de-nomes.csv"`,
  `SEPARADOR = ";"`, `COLUNAS = ("chave", "nome_exibido", "primeira_vez", "ultima_vez", "avistamentos")`
- `SerieDeNome` dataclass: `chave`, `nome_exibido`, `primeira_vez`, `ultima_vez`, `avistamentos`
- `Catalogo` classe: `__init__(pasta)`, `carregar()`, `registrar(chave, nome_exibido, agora)`,
  `gravar()`, `series` (mapa chave -> `SerieDeNome`)

**`l2scanner/mercado_pagina.py` (completado):**
- `JANELAS_IGUAIS_PARA_CONGELAR = 3`
- `LeitorDePagina` ganha `frames_congelados`, `paginas_lidas`, `paginas_perdidas`,
  `linhas_descartadas`, e a comparacao por tupla parseada nas posicoes aceitas em ambos os frames
- `tupla_comparavel(linha) -> tuple` e `posicoes_comparaveis(a, b) -> list[int]`

**Chave de `calibration.json` CONSUMIDA** (declarada no 02-01, MEDIDA pela varredura de oclusao
do 02-02): `mercado_minimo_de_linhas_comparadas`. Ela nao nasce neste plano de proposito — um
numero declarado pelo consumidor nasce sem ferramenta que o meca, e este e o numero que impede o
acordo trivial (T-02-26). Quem tem a distribuicao de linhas sobreviventes por pagina e a varredura
que conta linha recusada, e ela vive no 02-02

**Arquivo de runtime novo:** `.mercado/catalogo-de-nomes.csv` (gitignored)

**`.gitignore`:** entrada `.mercado/` com a prosa de razao

**Arquivo de teste novo:** `tests/test_mercado_replay.py`
</artifacts_this_phase_produces>

<indice_de_decisoes>
## Indice das decisoes citadas neste plano

O `02-CONTEXT.md` registra as decisoes do usuario em prosa, sem numera-las. Os IDs abaixo
sao estaveis e seguem a ORDEM DE LEITURA da secao `<decisions>` daquele arquivo — eles nao
inventam decisao nenhuma, so dao nome ao que ja esta escrito la.

| ID | Decisao (do `02-CONTEXT.md`) |
|---|---|
| D-01 | Duas escalas de OCR (2x e 3x) que precisam CONCORDAR; discordancia descarta a linha. |
| D-07 | O catalogo de nomes ja vistos vive em ARQUIVO PROPRIO, ao lado do CSV de observacoes, nunca no calibration.json. |
| D-08 | Nome novo entra DIRETO como serie nova, com contagem de avistamentos e data da primeira vez. Sem quarentena. |
| D-15 | A recusa e por LINHA, nao por pagina. A linha descartada NAO conta como desacordo no estabilizador. |
| D-18 | A comparacao entre dois frames e sobre a TUPLA PARSEADA de cada linha, nunca sobre pixels. |
| D-19 | A deteccao de captura congelada olha a JANELA INTEIRA. 3 janelas consecutivas bit-identicas = captura congelada. |
| D-20 | A busca do painel usa memoria da ultima posicao, com rebusca a cada 5 s e confirmacao barata por ancora a cada tick. |
| D-21 | Cadencia de leitura: 1 Hz, igual ao resto do scanner. |
</indice_de_decisoes>

<verification>
- `python -m pytest tests/test_mercado_catalogo.py tests/test_mercado_pagina.py tests/test_mercado_replay.py tests/test_mercado_leitura.py -q` verde
- `python -m pytest tests/test_mercado_27x.py -q` verde — o detector de morte segue intocado
- `python -m pytest tests/test_firewall_escopo.py -q` verde
- Suite completa: `python -m pytest -q`. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um
  `KeyboardInterrupt` que aborta a sessao perto de ~88 testes (5 abortos em 60 rodadas).
  **Abortar nao e falhar — rode de novo.** Baseline verde: **1704 passed, 2 skipped**
- **A distincao entre gate e nao-gate, que vale para toda esta fase:** as VARREDURAS de medicao
  (02-02, 02-03) sao gate das suas tasks e rodam no checkout principal, porque produzir o numero
  e producao de dado e o material so existe la. O REPLAY completo desta task e outra coisa: ele e
  um TESTE, e teste nao pode depender de material que nao vem de clone limpo. Por isso ele fica
  atras do `pytest.skip` e a metade que sempre roda vive de fixture versionada. As duas regras
  nao se contradizem porque tratam de coisas diferentes — dado que se fabrica versus asserção que
  se afirma
</verification>

<success_criteria>
O catalogo de nomes existe em arquivo proprio, acumula sem podar, sobrevive a uma recalibracao e
a uma escrita interrompida; a pagina so e aceita com dois frames concordando nas linhas
parseadas, com a descartada fora da conta e um minimo de posicoes comparadas; captura congelada
e reconhecida e recusada; e a fase inteira esta afirmada por fixture versionada num clone limpo.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-05-SUMMARY.md` when done
</output>
