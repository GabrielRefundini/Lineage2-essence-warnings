---
phase: 02-leitura-de-p-gina
plan: 07
type: execute
wave: 6
depends_on: ["02-04", "02-06"]
files_modified:
  - tools/medir_brilho_da_quantidade.py
  - l2scanner/mercado_leitura.py
  - l2scanner/mercado_pagina.py
  - l2scanner/calibracao.py
  - tests/test_medir_brilho_da_quantidade.py
  - tests/test_mercado_leitura.py
  - tests/test_mercado_pagina.py
  - tests/test_calibracao_mercado.py
  - tests/fixtures/mercado/calibracao_de_fixture.json
autonomous: true
requirements: [LEIT-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O piso de brilho da coluna Quantity e MEDIDO por varredura sobre as 8 gravacoes NOMEADAS do censo e gravado no calibration.json — nenhum numero novo entra no fonte antes da medicao (D-05). Este plano NAO escolhe o piso; ele constroi a ferramenta que o mede e o caminho por onde ele chega a producao"
    - "O rotulo da medicao e NAO CIRCULAR: a quantidade esperada de cada linha e DERIVADA de `Total` e `Unit price`, duas colunas que a coluna Quantity nao toca. Rotular pelo que a propria coluna le seria medir a leitura contra ela mesma, e foi assim que o 02-02 REFUTOU o rotulo por pasta de origem"
    - "A ferramenta REPROVA quando nao ha piso que separe: baixar o piso demais nao apenas deixa de ler, ele passa a ler ERRADO — medido pelo planejador, com o piso em 150 o `30` da quantidade vira `38` com score 0,724 e margem 0,127, que atravessam o piso 0,4698 e a margem 0,0370 de LEITURA. Um piso que produza UMA leitura divergente do rotulo e pior que o defeito de hoje, porque troca falha FECHADA por numero errado plausivel (LEIT-02)"
    - "O piso e PROPRIO DA COLUNA e nunca global, e a razao esta medida: a coluna Total carrega a palavra de sufixo dentro do proprio recorte, e a palavra vive entre V=120 e V=173 (o `VALOR_MINIMO_DO_SUFIXO = 120` existe por causa disso). Baixar o piso das colunas de moeda arrasta o sufixo para dentro da celula — sondado pelo planejador em 3 frames: `18,90` (score 1,000 no piso 180) vira `18,907` (score 0,293) ja no piso 170. As duas colunas pedem faixas DISJUNTAS: nao existe piso global"
    - "Quem consome o piso recebe o parametro SEM VALOR DE FABRICA, e isso e afirmado por `inspect.signature`, nunca por leitura de codigo — um default aqui e uma constante magica no caminho que decide quantidade, e a quantidade multiplica o preco no CSV da Fase 3"
    - "`segmentar_glifos` NAO muda de assinatura: os ~35 pontos de chamada dela em ferramentas e testes querem o piso compartilhado, e quebra-los todos por causa de uma coluna seria custo sem informacao. O piso proprio entra por uma primitiva IRMA, exatamente como `mascara_do_sufixo` nasceu ao lado de `mascara_de_texto` em vez de substitui-la"
    - "As colunas de MOEDA leem EXATAMENTE o mesmo que hoje, e isso e preso por valor: os totais que o tracer do 02-04 ja le nas fixturas versionadas continuam identicos depois desta onda. Se um deles mudar, o piso vazou para a coluna errada"
    - "A recusa continua sendo por LINHA e com motivo, e o usuario segue vendo contagem no console e nada no CSV (D-17): este plano muda QUANTAS linhas atravessam, nunca o que acontece com a que nao atravessa"
    - "`VERSAO_DO_ESQUEMA` SEGUE EM 2 e a chave nova entra por `.get` opcional: o calibration.json do usuario carrega 13 moldes de glifo e 3 ancoras que so a mao dele produz, e um bump custaria uma tarde dele"
    - "O conjunto de medicao sao as 8 gravacoes NOMEADAS, importadas da constante que ja existe e nunca redeclaradas — `recordings/` tem 16 pastas e a `pre-voo` sozinha tem 1.502 PNGs, que dominariam qualquer distribuicao"
  artifacts:
    - path: "tools/medir_brilho_da_quantidade.py"
      provides: "a varredura que deriva o rotulo de Total/Unit price, propoe o piso proprio da coluna Quantity com folga declarada, mede o custo de um piso GLOBAL na coluna Total, e grava a chave por load-mutate-save"
      contains: "os.replace"
    - path: "l2scanner/mercado_leitura.py"
      provides: "`mascara_de_numero` e `segmentar_glifos_no_brilho` como primitivas irmas, e o `valor_minimo` como parametro obrigatorio de `ler_celula`, `ler_celula_de_numero`, `ler_celula_de_quantidade` e `ler_linha`"
      contains: "valor_minimo"
    - path: "l2scanner/calibracao.py"
      provides: "`mercado_limiar_de_brilho_da_quantidade` como campo opcional lido por `.get`, com faixa conferida no arranque"
      contains: "mercado_limiar_de_brilho_da_quantidade"
    - path: "tests/test_medir_brilho_da_quantidade.py"
      provides: "o rotulo derivado, a recusa quando as populacoes se tocam, a parada quando falta pasta do censo, e a prova de que o censo foi IMPORTADO e nao copiado"
      contains: "GRAVACOES_DO_CENSO"
  key_links:
    - from: "tools/medir_brilho_da_quantidade.py"
      to: "tools/medir_oclusao.py"
      via: "`GRAVACOES_DO_CENSO` e `MOTIVO_PARA_IGNORAR` IMPORTADOS pelo mesmo `importlib` de `medir_leitura_de_glifo._carregar_o_censo`, nunca redeclarados"
      pattern: "GRAVACOES_DO_CENSO"
    - from: "l2scanner/mercado_pagina.py"
      to: "l2scanner/calibracao.py"
      via: "`LeitorDePagina` le `mercado_limiar_de_brilho_da_quantidade` no arranque e a ausencia dela e feature OFF com aviso alto, no mesmo portao por AUSENCIA das outras nove chaves"
      pattern: "_calibrado"
    - from: "tests/fixtures/mercado/calibracao_de_fixture.json"
      to: "calibration.json"
      via: "a chave nova e copiada VERBATIM da calibracao de producao, como as outras `mercado_*` — a fixtura nunca inventa um valor proprio"
      pattern: "mercado_limiar_de_brilho_da_quantidade"
---

<objective>
Fechar a refutacao 4 do tracer: **o digito `1` da coluna Quantity nao se le**, e por isso ZERO
linhas atravessam nas gravacoes de tooltip e de alvo-sobreposto inteiras — porque toda
quantidade ali e `1`, e `1` e o caso comum do mercado.

O 02-04 mediu o defeito pixel a pixel e, corretamente, NAO inventou o conserto: o tronco do `1`
da Quantity e desenhado a V=177, tres niveis abaixo do piso 180 de `identidade.mascara_de_texto`,
enquanto o MESMO `1` da coluna Total tem V=205. A mascara fica so com a serifa e a base, o
casamento devolve 0,2988 (o proprio molde `1` vale −0,1810) e o piso de LEITURA 0,4698 reprova.
A falha e FECHADA — nada errado e gravado — e por isso este plano nao e um conserto de bug: e
uma MEDICAO, do mesmo formato da onda 02-02, que produz um numero e o entrega a producao por um
parametro sem valor de fabrica.

Purpose: sem este numero o 02-05 — o replay que afirma a fase — mede um rendimento ARTEFATO.
Ele contaria como "leitura correta" um conjunto de linhas do qual o caso mais comum do mercado
foi sistematicamente excluido, e o criterio 2 da fase ("os precos e quantidades lidos batem
digito a digito com o que o usuario ve no frame") passaria por ausencia da populacao que o
derruba.

Output: `tools/medir_brilho_da_quantidade.py`, a chave `mercado_limiar_de_brilho_da_quantidade`
MEDIDA no `calibration.json` da maquina do usuario, o piso chegando a `ler_celula_de_quantidade`
por parametro obrigatorio, e a regressao presa por fixtura VERSIONADA.
</objective>

<as_quatro_perguntas_respondidas_por_escrito>

Estas sao as quatro perguntas que o planejamento tinha de responder. Tres delas foram
respondidas por SONDAGEM do planejador sobre 3 frames de campo e 4 fixturas versionadas (40+
celulas); a Task 1 as re-responde no censo inteiro, e onde a medicao em escala contradisser a
sondagem, **a medicao manda** — e a contradicao vira registro, no padrao da casa.

## 1. O piso e por coluna, e nao existe piso global. Com numero.

Sondado: a coluna Total carrega a **palavra de sufixo dentro do proprio recorte** (largura 209
px, e a palavra fica a direita do numero). A palavra vive entre V=120 e V=173 — e por isso
`VALOR_MINIMO_DO_SUFIXO = 120` existe. O piso 180 das colunas de moeda **e o que mantem o
sufixo FORA da celula**. Baixa-lo arrasta a palavra para dentro:

| fixtura / frame | linha | piso 180 | piso 170 | piso 165 |
|---|---|---|---|---|
| `janela_negociacao_f010.png` | L6 Total | `18,90` score 1,000 | `18,907` score 0,293 | `18,4067738` score 0,22 |
| `janela_negociacao_f005.png` | L0 Total | `3,00` score 1,000 | `3,007` score 0,293 | `3,0067738` score 0,22 |
| `janela_tooltip_f012.png` | L0 Total | `80,00` score 1,000 | `80,007` score 0,293 | `80,0067738` score 0,22 |

A coluna Quantity precisa de um piso **abaixo de 177**; a coluna Total quebra **abaixo de 180**.
As duas faixas sao DISJUNTAS: nao ha piso global, e a pergunta esta respondida por medida e nao
por gosto. A Task 1 quantifica isso no censo inteiro (quantas celulas de Total que hoje leem
deixam de ler sob o piso proposto), porque um numero de 3 frames nao e o numero da fase.

## 2. Por que a Quantity e mais escura — e a razao MUDA o desenho.

Sondado: a coluna inteira e desenhada mais apagada. Pico de V por celula, em 3 gravacoes:
**Quantity 206–215**, **Total 220–255**, **Unit price 220–255**. Ou seja, ha uma diferenca
sistematica de brilho entre colunas, e nao um defeito de um glifo.

Mas o pico da Quantity (215) ainda esta ACIMA de 180 — entao a coluna inteira nao some. O que
some e so o `1`, e a razao e geometrica: o traco vertical do `1` tem **1 px de largura**, e o
anti-aliasing de um traco de 1 px nunca atinge o pico da fonte. O tronco fica em 177, no vao
entre "fundo" (66) e "pico" (215).

**Consequencia para o desenho: um piso FIXO por coluna serve.** A diferenca e uma propriedade
do desenho da UI e da geometria da fonte, nao do conteudo do frame — ela nao varia com o item,
com a pagina nem com o mundo atras do painel. Um piso derivado do conteudo a cada frame seria
mecanismo caro para uma constante de tela, e introduziria uma dependencia do conteudo
exatamente na peneira que existe para nao depender dele. **A Task 1 tem de CONFIRMAR isso**
imprimindo a dispersao do pico da coluna Quantity ao longo das 8 gravacoes: se o pico variar
mais que a folga do piso proposto, a resposta muda e o plano tem de ser refeito — e isso fica
escrito, nao suposto.

## 3. Nao e so o `1` que e escuro — mas e so o `1` que MORRE. E ha um teto.

Sondado, com os moldes reais, no piso 180: `2` (0,94), `3` (0,88–0,94), `5` (0,59–0,94), `6`
(0,89), `30`, `51`, `60`, `18`, `32`, `45`, `66`, `10` — todos LEEM. So o `1` sozinho cai
(score 0,299 contra o molde `0`). Entao o piso resolve.

**E o achado que dimensiona o risco:** baixar demais nao volta a falhar fechado, passa a falhar
ABERTO. Sondado em `scroll-transicao/frame_000016`:

| linha | rotulo | piso 180 | piso 170 | piso 160 | piso 150 |
|---|---|---|---|---|---|
| L0 | `30` | `30` (0,882) | `30` (1,000) | `30` (0,756) | **`38` (0,724 / margem 0,127)** |
| L2 | `60` | `60` (0,886) | `60` (0,886) | `60` (0,756) | **`68` (0,724 / margem 0,127)** |
| L8 | `10` | `10` (0,595) | `10` (1,000) | `10` (0,756) | **`18` (0,724 / margem 0,127)** |

O `38` tem score 0,724 (acima do piso de leitura 0,4698) e margem 0,127 (acima da margem
0,0370): ele ATRAVESSA as duas peneiras e vira numero errado no CSV. **O piso tem teto e chao**,
e por isso a Task 1 nao propoe por "o mais baixo que ainda le" — ela propoe pelo vao entre a
pior leitura CORRETA e a melhor leitura ERRADA contra o rotulo derivado, recusando propor quando
o vao nao existe. E a mesma disciplina de "maior folga relativa contra o gabarito" que o 02-02
adotou depois de a alternativa ser refutada.

## 4. Onde esta onda encaixa — e sim, o 02-05 ganha dependencia.

Hoje: **02-04 (w4, feito) -> 02-06 (w5) -> 02-05 (w6)**.

Esta onda e a **wave 6**, com `depends_on: ["02-04", "02-06"]`, e o **02-05 passa para a wave 7**
com `02-07` somado ao `depends_on` dele. Duas razoes independentes, e as duas sao vinculantes:

1. **Conflito de arquivo com o 02-06.** Aquele plano modifica `l2scanner/mercado_leitura.py` e
   `tests/test_mercado_leitura.py`, e este tambem. Planos da mesma wave nao podem ter
   `files_modified` em comum, entao a wave 5 esta fechada para este plano. Ir DEPOIS do 02-06 e
   melhor do que ir antes: quando o piso virar parametro obrigatorio, a coluna do unitario que o
   02-06 acabou de acrescentar ja existe, e os tres pontos de chamada sao atualizados numa
   passagem so — o que torna o criterio de `inspect.signature` verdadeiro para todos eles.
2. **O numero tem de existir ANTES do replay.** O 02-05 e a onda que afirma a fase contra as 8
   gravacoes. Rodar o replay antes desta medicao mediria um rendimento em que o caso mais comum
   do mercado (quantidade `1`) foi sistematicamente excluido — e um rendimento assim nao e um
   resultado, e um artefato do defeito.

Colocar 02-07 e 02-05 juntos na wave 6 seria admissivel pelo criterio de arquivo (eles nao se
cruzam), e ERRADO pelo criterio 2: o replay leria o `calibration.json` no meio da escrita da
onda vizinha. Por isso o 02-05 ganha a dependencia explicita, e nao apenas a ordem.

</as_quatro_perguntas_respondidas_por_escrito>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-02-SUMMARY.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A varredura que MEDE o piso proprio da coluna Quantity, com rotulo derivado e recusa quando o vao nao existe</name>

  <files>tools/medir_brilho_da_quantidade.py, tests/test_medir_brilho_da_quantidade.py</files>

  <read_first>
    - `tools/medir_oclusao.py` linhas 75–115 — `GRAVACOES_DO_CENSO`, `GRAVACOES_COM_OCLUSAO` e `MOTIVO_PARA_IGNORAR`, a tabela das 8 pastas ignoradas com o motivo de cada uma
    - `tools/medir_leitura_de_glifo.py` linhas 60–155 — o cabecalho, o `RAIZ`/`sys.path`, o `_carregar_o_censo` por `importlib`, e quais primitivas sao IMPORTADAS de `l2scanner.mercado_leitura` em vez de reescritas
    - `tools/medir_leitura_de_glifo.py` linhas 215–330 — `limite_derivado_do_cruzamento`, `residuo_do_cruzamento` e `veredito_da_guarda`: a aritmetica do arredondamento do unitario ja esta escrita e MEDIDA aqui, e este plano a reutiliza como ROTULO (nunca como guarda)
    - `tools/medir_leitura_de_glifo.py` linhas 422–510 e 555–640 — `varrer` (o laco frame/linha/coluna com `RastreioDoPainel`), `propor_piso_e_margem` e `gravar` (load-mutate-save por `NamedTemporaryFile` + `os.replace`)
    - `l2scanner/mercado_leitura.py` linhas 84–200 — `segmentar_glifos`, o bloco de comentario de `VALOR_MINIMO_DO_SUFIXO` e `mascara_do_sufixo`: o precedente EXATO de um piso de brilho proprio, com o plato medido e o vale vazio
    - `l2scanner/mercado_leitura.py` linhas 395–475 — `pontuar_glifos` (sem piso, de proposito, para a medicao nao ser circular) e `ler_glifos`
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-02-SUMMARY.md` secao "Task 1 — a sonda de oclusao" — a forma do relatorio: TODOS os candidatos tabelados com o veredito de cada um, e o custo do corte impresso porque descarte nao e de graca
  </read_first>

  <precondition>As 8 pastas de `GRAVACOES_DO_CENSO` existem sob `C:/Users/refun/Desktop/Lineage2-warnings/recordings/` — elas sao gitignored e so existem no checkout PRINCIPAL, entao esta task nao roda em worktree.</precondition>

  <behavior>
    - `quantidade_derivada(total, unitario)` devolve um inteiro quando `unitario > 0` e o residuo
      cabe no limite derivado do arredondamento, e `None` caso contrario. Casos: total 4000 e
      unitario 83 devolvem 48 (residuo 16 centesimos contra limite 24); total 300 e unitario 300
      devolvem 1 com residuo 0; unitario 0 devolve `None`; um par cujo residuo estoure o limite
      devolve `None`. Ela NUNCA recebe pixel da coluna Quantity — e essa a propriedade que a torna
      um rotulo e nao um espelho.
    - `propor_o_piso(amostras)` recebe as leituras da coluna Quantity em cada piso candidato,
      rotuladas por `quantidade_derivada`, e devolve `(piso, folga, veredito)`. Com uma populacao
      em que existe um piso sem NENHUMA leitura divergente do rotulo, ele propoe o meio do vao
      entre o pior piso que ainda le tudo certo e o melhor piso que ja le algo errado. Com uma
      populacao em que toda faixa produz pelo menos uma divergencia, ele devolve veredito
      REPROVADO e piso `None`.
    - `custo_na_coluna_de_moeda(amostras, piso)` conta quantas celulas de Total que LEEM no piso
      compartilhado deixam de ler (ou passam a ler outra coisa) sob o piso candidato. Com o piso
      compartilhado ele devolve zero por construcao; com um piso mais baixo ele devolve um numero
      positivo sobre as fixturas versionadas.
    - `GRAVACOES_DO_CENSO` do modulo e o MESMO OBJETO de `medir_oclusao.GRAVACOES_DO_CENSO` —
      identidade, nao igualdade: uma copia com o mesmo conteudo passaria por igualdade e
      envelheceria separada.
    - `main` devolve 2 quando o diretorio de gravacoes nao existe, 3 quando uma pasta esperada do
      censo falta (nomeando as que faltam), e 0 nos dois vereditos.
  </behavior>

  <action>
Criar `tools/medir_brilho_da_quantidade.py`, no molde estrutural de `tools/medir_leitura_de_glifo.py`: mesmo cabecalho de `RAIZ` + `sys.path`, mesmo `_carregar_o_censo` por `importlib.util` apontando para `tools/medir_oclusao.py`, e as primitivas de segmentacao e pontuacao IMPORTADAS de `l2scanner.mercado_leitura` (`pontuar_glifos`, `numero_valido`, `inteiro_de_quantidade`, `centesimos_de_moeda`) em vez de reescritas — a seta aponta sempre ferramenta para puro, e reescrever faria a ferramenta medir com uma convencao e a producao decidir com outra.

O ROTULO. Escrever `quantidade_derivada(total, unitario)`: quando as colunas `Total` e `Unit price` da linha leem inteiras no piso compartilhado, a quantidade esperada e `round(total / unitario)`, aceita apenas quando o residuo cabe no limite derivado do arredondamento a duas casas — a mesma aritmetica ja escrita em `limite_derivado_do_cruzamento` e `residuo_do_cruzamento` de `tools/medir_leitura_de_glifo.py`, importada e nao reescrita. Ela e o unico rotulo nao circular disponivel, porque nao toca um pixel da coluna Quantity. Deixar claro na docstring que ela e usada aqui como ROTULO DE MEDICAO e nao como guarda de producao: a guarda de cruzamento REPROVOU no 02-02 por tolerancia (1273 centesimos por unidade contra o maximo 1,0) e continua desligada; um rotulo precisa de precisao alta e pode ter recall baixo, e uma guarda precisa das duas coisas — sao criterios diferentes sobre a mesma aritmetica, e confundi-los seria ressuscitar por acidente um mecanismo que o usuario ja viu reprovar.

A VARREDURA. Reproduzir o laco de `medir_leitura_de_glifo.varrer`: para cada uma das 8 gravacoes nomeadas, `RastreioDoPainel` com as ancoras da calibracao, e para cada frame com painel aberto, cada uma das dez linhas da grade, os recortes das colunas `Total`, `Unit price` e `Quantity` pelo `dx`/`largura` da calibracao. Para cada celula de Quantity, segmentar e pontuar em CADA piso candidato de uma faixa declarada por constante do modulo, com passo declarado — a faixa vai do piso compartilhado ate um valor bem abaixo do tronco medido, para que o relatorio mostre os dois lados do vao e nao apenas o lado bom. Guardar por celula: o rotulo derivado (ou `None`), o texto lido em cada piso, o pior score e a pior margem.

O PORTAO DE LAYOUT ESTA LIGADO. Diferentemente do 02-02, a varredura roda `layout_confere` de `l2scanner.mercado_leitura` antes de fatiar a pagina, com o molde de cabecalho e o limiar da calibracao, e conta as paginas recusadas por layout no relatorio. O 02-02 mediu necessariamente sobre todos os layouts porque o portao so nasceu no 02-04; medir a coluna Quantity sobre a aba Adena seria medir uma coluna que nem existe la (`5 mln increment`), e o rotulo derivado nao vale por construcao naquela aba.

A PROPOSTA. `propor_o_piso` classifica, em cada piso candidato, cada celula rotulada em tres baldes: LE CERTO (o texto lido, passado por `numero_valido` e `inteiro_de_quantidade`, e igual ao rotulo), NAO LE (a celula cai pelo piso ou pela margem de leitura, ou pela gramatica — falha fechada, o comportamento de hoje) e LE ERRADO (a celula produz um inteiro DIFERENTE do rotulo). Propor o piso pelo vao entre o pior candidato com ZERO no balde LE ERRADO e o melhor candidato que ja tem alguem nele, tomando o MEIO do vao e imprimindo a folga em niveis de V. Recusar propor quando nenhum candidato tem o balde LE ERRADO vazio, com a mesma disciplina do 02-02: uma razao contra zero nao e medicao, e um piso que nao se provou nao pode ser gravado.

Quando a proposta REPROVA, gravar mesmo assim o piso COMPARTILHADO na chave nova e imprimir o veredito com os numeros ao lado. A razao esta escrita e vale a pena repetir na docstring de `gravar`: o piso compartilhado E o comportamento de hoje, ele falha FECHADA, e grava-lo como o piso da coluna e o que permite a producao nao carregar nenhum ramo de fallback nem nenhum numero de fabrica. O veredito vive no relatorio, no SUMMARY e no `WINDOWS.md`, que e onde `mercado_tolerancia_do_cruzamento = None` ja mora.

OS TRES RELATORIOS, que sao a resposta escrita das perguntas 1, 2 e 3 do plano. RELATORIO 1, o pico por coluna: minimo, p5, mediana, p95 e maximo do pico de V das celulas de `Quantity`, `Total` e `Unit price`, com a dispersao do pico da Quantity ao longo das 8 gravacoes — se ela for maior que a folga do piso proposto, imprimir alto que um piso FIXO nao serve e que a resposta da pergunta 2 mudou. RELATORIO 2, a tabela de TODOS os pisos candidatos com os tres baldes e o veredito de cada um, no molde da tabela dos 16 candidatos de sonda do 02-02, com o escolhido marcado. RELATORIO 3, o custo de um piso GLOBAL: quantas celulas de `Total` e de `Unit price` que leem no piso compartilhado deixam de ler ou passam a ler outro texto sob o piso proposto para a Quantity — e o rendimento por gravacao, antes e depois, para que o ganho seja um numero e nao uma promessa. Imprimir tambem a taxa de leitura por DIGITO do rotulo, antes e depois, para que a pergunta 3 tenha resposta no censo e nao so na sondagem.

A GRAVACAO. `gravar(caminho, piso)` faz load-mutate-save: `json.loads` do arquivo, mutacao da unica chave `mercado_limiar_de_brilho_da_quantidade`, `NamedTemporaryFile` no mesmo diretorio e `os.replace`. Montar uma `Calibracao` do zero apagaria os 13 moldes de glifo e as 3 ancoras — foi o modo de falha REAL da janela quebrada 13, e ele ja custou uma recuperacao a mao. `--gravar` e opcional e o padrao e so relatar.

Os testes de `tests/test_medir_brilho_da_quantidade.py` correm sobre as fixturas VERSIONADAS de `tests/fixtures/mercado/` e a `calibracao_de_fixture.json`, nunca sobre `recordings/`, no padrao de cabecalho de `tests/test_medir_leitura_de_glifo.py` (que declara de onde veio cada fixtura). Afirmar: o rotulo derivado nos quatro casos da `<behavior>`; a proposta sobre uma populacao construida com vao (propoe o meio) e sobre uma sem vao (REPROVA e devolve `None`); a identidade do censo importado; e que `janela_tooltip_f012.png` e uma fixtura em que as dez linhas tem rotulo derivado `1` — que e a razao de ela ser o caso de regressao da Task 2.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -m pytest tests/test_medir_brilho_da_quantidade.py -x -q</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('mbq','tools/medir_brilho_da_quantidade.py'); m=importlib.util.module_from_spec(s); sys.modules['mbq']=m; s.loader.exec_module(m); o=sys.modules['medir_oclusao']; assert m.GRAVACOES_DO_CENSO is o.GRAVACOES_DO_CENSO, 'o censo foi COPIADO, nao importado'; print('censo importado ok', len(m.GRAVACOES_DO_CENSO))"</automated>
  </verify>

  <acceptance_criteria>
    - GATE DE PRODUCAO DE DADO, no checkout PRINCIPAL, com caminhos absolutos:
      `C:/Users/refun/Desktop/Lineage2-warnings/.venv/Scripts/python.exe C:/Users/refun/Desktop/Lineage2-warnings/tools/medir_brilho_da_quantidade.py --gravacoes C:/Users/refun/Desktop/Lineage2-warnings/recordings --calibracao C:/Users/refun/Desktop/Lineage2-warnings/calibration.json`
      sai com codigo 0 e imprime os TRES relatorios, as 8 gravacoes do censo com a contagem de PNGs, e as pastas ignoradas com o motivo de cada uma
    - A mesma ferramenta apontada para um diretorio que contenha apenas UMA das 8 pastas sai com
      codigo 3 e nomeia as 7 que faltam
    - A gravacao roda com `--gravar` e o PORTAO DE DIGEST e conferido: md5 dos 13 moldes de glifo
      e das 3 ancoras de `calibration.json` ANTES e DEPOIS da escrita, identicos nas duas pontas.
      Registrar os dois digests no SUMMARY
    - `git status --short | grep -c calibration.json` devolve 0 — o arquivo e gitignored e NUNCA
      e commitado
    - O veredito (PROPOSTO com a folga, ou REPROVADO com o numero que o derrubou) fica no SUMMARY
      e uma linha nova entra em `.planning/WINDOWS.md`, referenciando a entrada #16 que este plano
      fecha ou reclassifica
    - Se o RELATORIO 1 mostrar que a dispersao do pico da Quantity e maior que a folga do piso
      proposto, a Task 2 NAO comeca: o plano volta a mesa, porque a resposta da pergunta 2 mudou
      e um piso fixo deixou de servir
  </acceptance_criteria>

  <done>
`tools/medir_brilho_da_quantidade.py` existe, importa o censo em vez de copia-lo, varre as 8
gravacoes com o portao de layout ligado, produz os tres relatorios, e gravou
`mercado_limiar_de_brilho_da_quantidade` no `calibration.json` da maquina do usuario com o
veredito por escrito — ou REPROVOU com o numero que o derrubou, tambem por escrito. Os 13 moldes
e as 3 ancoras estao intactos, conferidos por md5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O piso chega a producao por parametro obrigatorio, e a coluna de moeda nao se mexe um digito</name>

  <files>l2scanner/mercado_leitura.py, l2scanner/mercado_pagina.py, l2scanner/calibracao.py, tests/test_mercado_leitura.py, tests/test_mercado_pagina.py, tests/test_calibracao_mercado.py, tests/fixtures/mercado/calibracao_de_fixture.json</files>

  <read_first>
    - `l2scanner/mercado_leitura.py` linhas 1–83 — o charter, e em especial o bloco "TODO LIMIAR CHEGA POR PARAMETRO, SEM VALOR DE FABRICA": esta task acrescenta um limiar a essa lista e a regra ja esta escrita
    - `l2scanner/mercado_leitura.py` linhas 473–585 — `ler_celula`, `ler_celula_de_numero` e `ler_celula_de_quantidade`, incluindo a docstring que ja descreve o defeito e nomeia o conserto
    - `l2scanner/mercado_leitura.py` linhas 723–810 — `ler_linha` e a ordem do pipeline; os pontos de chamada das colunas de numero ficam aqui
    - `l2scanner/mercado_pagina.py` linhas 113–170 e 300–435 — o `__init__` do `LeitorDePagina`, o ponto de chamada de `ler_linha` e o `_calibrado()` que confere as nove chaves por AUSENCIA
    - `l2scanner/calibracao.py` linhas 370–440 — os comentarios das chaves irmas (`mercado_limiar_de_leitura_de_glifo`, `mercado_tolerancia_do_cruzamento`, `mercado_minimo_de_linhas_comparadas`): a forma do comentario que explica a faixa valida e o que um valor errado custa
    - `l2scanner/calibracao.py` linhas 605–760 — `carregar` com os `.get`, e `_conferir_as_chaves_de_mercado`, onde a faixa da chave nova e conferida no arranque
    - `l2scanner/identidade.py` linhas 56 e 134–145 — `VALOR_MINIMO_DO_TEXTO` e `mascara_de_texto`: o piso compartilhado que as colunas de MOEDA continuam usando, e que este plano NAO altera
    - `tests/test_calibracao_mercado.py` linhas 374–400 — a classe que afirma que as chaves novas sao OPCIONAIS e que `VERSAO_DO_ESQUEMA` segue em 2
  </read_first>

  <precondition>`mercado_limiar_de_brilho_da_quantidade` existe em `C:/Users/refun/Desktop/Lineage2-warnings/calibration.json`, gravado pelo gate da Task 1 — esta task copia esse valor VERBATIM para a calibracao de fixtura e nao o escolhe.</precondition>

  <reversibility rating="costly">Baixar o piso de brilho de uma coluna troca uma peneira que falha FECHADA por uma que pode aceitar um digito parecido. Voltar atras e reescrever uma chave, mas as observacoes que a Fase 3 gravar sob um piso errado ficam no CSV com aparencia plausivel. Por isso a Task 1 recusa propor sem vao, e por isso o criterio central desta task e a IGUALDADE das colunas de moeda, e nao apenas o ganho na Quantity.</reversibility>

  <behavior>
    - `mascara_de_numero(bgr, valor_minimo)` devolve a mascara binaria de brilho no piso recebido,
      com a mesma mecanica de `mascara_de_texto` (so V, sem filtro de saturacao). Com o piso
      compartilhado ela e IGUAL a `mascara_de_texto` sobre o mesmo recorte, pixel a pixel — e essa
      igualdade e o que prova que nada mudou para quem nao pediu piso proprio. Recorte vazio
      devolve matriz vazia e nao levanta.
    - `segmentar_glifos_no_brilho(recorte, valor_minimo)` devolve `(faixa, runs)` com a convencao
      de faixa compartilhada inalterada. `segmentar_glifos(recorte)` passa a ser a casca fina que
      a chama com o piso compartilhado, e devolve exatamente o que devolvia antes — afirmado
      sobre `glifos_precos_f010.png` e `glifos_quantidade_f012.png`.
    - `ler_celula`, `ler_celula_de_numero` e `ler_celula_de_quantidade` recebem `valor_minimo` como
      parametro somente-nomeado SEM valor de fabrica: chama-las sem ele levanta `TypeError`, e
      `inspect.signature(...).parameters["valor_minimo"].default` e `inspect.Parameter.empty` nas
      tres. `ler_linha` recebe `valor_minimo_do_numero` e `valor_minimo_da_quantidade` pela mesma
      regra.
    - A mascara e a segmentacao dentro de `ler_celula` usam o MESMO `valor_minimo`: uma celula
      segmentada num piso e pontuada em outro produziria runs que apontam para colunas que a
      mascara nao tem.
    - Sobre `tests/fixtures/mercado/janela_tooltip_f012.png`, com a calibracao de fixtura: hoje
      NENHUMA das dez linhas produz quantidade lida; com o piso medido, as linhas que a sonda de
      oclusao NAO recusa passam a produzir quantidade `1`. A contagem exata depende do numero que
      a Task 1 mediu e do veredito da sonda, entao o teste afirma a DESIGUALDADE (depois > antes)
      e fixa o valor lido (`1`) das linhas que atravessam, nunca um total inventado no plano.
    - Sobre `janela_negociacao_f005.png` e `janela_negociacao_f010.png`, os totais que o tracer do
      02-04 ja lia continuam IDENTICOS: `f005` linha 1 total 3666, linha 3 total 1500, linha 5
      total 1139, linha 6 total 500; `f010` linha 6 total 1890 e linha 8 total 1800. Um total que
      mude significa que o piso da Quantity vazou para a coluna de moeda.
    - `Calibracao.carregar` le `mercado_limiar_de_brilho_da_quantidade` por `.get`; um arquivo sem
      a chave carrega inteiro com o campo em `None`, e `VERSAO_DO_ESQUEMA` segue em 2. Um valor
      que nao seja inteiro, um `bool`, ou um inteiro fora de `[1, 254]` levanta `CalibracaoInvalida`
      no arranque, com a mensagem dizendo qual ferramenta rodar.
    - `LeitorDePagina._calibrado()` passa a conferir a chave nova por AUSENCIA, junto das outras
      nove: sem ela a leitura de mercado NAO acontece, com um unico aviso alto nomeando a chave e
      a ferramenta — nunca `raise` dentro do tick.
  </behavior>

  <action>
Em `l2scanner/mercado_leitura.py`, acrescentar duas primitivas IRMAS e nao substituir nenhuma. `mascara_de_numero(bgr, valor_minimo)` faz o que `mascara_do_sufixo` ja faz — converter para HSV e comparar o canal V com um piso recebido — mas com o piso vindo de fora em vez de constante de modulo. `segmentar_glifos_no_brilho(recorte, valor_minimo)` carrega o corpo atual de `segmentar_glifos`, e `segmentar_glifos(recorte)` vira uma casca fina que a chama nomeando `identidade.VALOR_MINIMO_DO_TEXTO`. Manter a assinatura de `segmentar_glifos` intacta e deliberado e a razao vai na docstring: ela tem cerca de trinta e cinco pontos de chamada em ferramentas e testes, todos querendo o piso compartilhado, e quebra-los todos por causa de uma coluna seria custo sem informacao. A casca e o unico lugar do repositorio que nomeia o piso compartilhado para a leitura de mercado, e nomear uma vez e o contrario de espalhar.

Acrescentar `valor_minimo` como parametro somente-nomeado e SEM valor de fabrica a `ler_celula`, `ler_celula_de_numero` e `ler_celula_de_quantidade`, e passa-lo tanto para `segmentar_glifos_no_brilho` quanto para `mascara_de_numero` dentro de `ler_celula` — as duas chamadas recebem o mesmo valor, porque segmentar num piso e pontuar em outro produziria runs apontando para colunas que a mascara nao tem. Estender a docstring de `ler_celula_de_quantidade`: o paragrafo que hoje descreve o defeito e nomeia o conserto passa a dizer que o conserto CHEGOU, por qual chave, e com que numero e folga — no padrao da casa de deixar o numero que caiu ao lado do que o substituiu.

Em `ler_linha`, acrescentar `valor_minimo_do_numero` e `valor_minimo_da_quantidade` como somente-nomeados sem valor de fabrica, e reparti-los pelos pontos de chamada: as colunas de MOEDA (o `Total`, e o `Unit price` que o 02-06 acrescentou) recebem o primeiro; a coluna Quantity recebe o segundo. Registrar na docstring de `ler_linha` a razao pela qual sao dois parametros e nao um, com o numero da sondagem: as duas colunas pedem faixas disjuntas porque a coluna de moeda carrega a palavra de sufixo dentro do proprio recorte, e um piso unico mais baixo arrasta a palavra para dentro da celula.

Em `l2scanner/calibracao.py`, acrescentar o campo `mercado_limiar_de_brilho_da_quantidade` com o mesmo comentario-de-vizinhanca das chaves irmas: por que ele existe, qual a faixa valida, quem o mede e o que um valor errado custa. Le-lo por `.get` no `carregar` e acrescenta-lo ao `como_dict`. Em `_conferir_as_chaves_de_mercado`, conferir tipo e faixa no arranque: `None` sempre passa (e o estado legitimo de nao calibrado); `bool` e recusado explicitamente porque e subclasse de `int` e viraria piso 1 calado; um valor fora de `[1, 254]` e recusado com a mensagem dizendo o que um piso de 0 faria (mascara cheia, toda celula virando ruido) e o que um piso de 255 faria (mascara vazia, toda celula caindo). `VERSAO_DO_ESQUEMA` NAO sobe.

Em `l2scanner/mercado_pagina.py`, ler a chave nova no `__init__` do `LeitorDePagina`, acrescenta-la a lista do `_calibrado()` como decima entrada conferida por AUSENCIA, e passar os dois pisos no ponto de chamada de `ler_linha`: o compartilhado, nomeado a partir de `identidade`, para as colunas de moeda; o da calibracao para a Quantity. Nao introduzir ramo de fallback: sem a chave a leitura simplesmente nao acontece, que e exatamente o que o portao por ausencia ja faz com as outras nove.

Copiar o valor medido VERBATIM para `tests/fixtures/mercado/calibracao_de_fixture.json`, como as outras chaves `mercado_*` ja sao copiadas — a fixtura nunca inventa um valor proprio, senao teste e producao passam a medir coisas diferentes.

Os testes novos vao em `tests/test_mercado_leitura.py` (as duas primitivas irmas, a igualdade com a mascara compartilhada no piso compartilhado, a ausencia de valor de fabrica por `inspect.signature` nas tres leitoras e em `ler_linha`, e o `TypeError` quando o parametro e omitido), em `tests/test_mercado_pagina.py` (a decima chave como feature OFF, no molde parametrizado que ja existe para as outras nove; e a leitura ponta a ponta sobre `janela_tooltip_f012.png` e `janela_negociacao_f005.png` com as duas leitoras injetadas, afirmando a desigualdade do rendimento e a IGUALDADE dos totais) e em `tests/test_calibracao_mercado.py` (a chave nova entra na mesma parametrizacao de opcionalidade das outras, e a versao do esquema segue em 2). Os testes de rendimento nao fixam um total de linhas escolhido pelo plano: eles comparam a contagem sob o piso compartilhado com a contagem sob o piso medido, no mesmo arquivo de teste, o que torna a afirmacao verdadeira qualquer que tenha sido o numero medido.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -m pytest tests/test_mercado_leitura.py tests/test_mercado_pagina.py -x -q</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -m pytest tests/test_mercado_glifos.py tests/test_calibracao_mercado.py tests/test_calibrar_mercado.py tests/test_medir_leitura_de_glifo.py tests/test_medir_agrupamento_de_nome.py -q</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -c "import inspect; from l2scanner import mercado_leitura as m; [__import__('sys').exit('DEFAULT PROIBIDO em '+f.__name__) for f in (m.ler_celula, m.ler_celula_de_numero, m.ler_celula_de_quantidade) if inspect.signature(f).parameters['valor_minimo'].default is not inspect.Parameter.empty]; p=inspect.signature(m.ler_linha).parameters; assert p['valor_minimo_do_numero'].default is inspect.Parameter.empty and p['valor_minimo_da_quantidade'].default is inspect.Parameter.empty; print('sem valor de fabrica ok')"</automated>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings &amp;&amp; python -c "from l2scanner.calibracao import VERSAO_DO_ESQUEMA; assert VERSAO_DO_ESQUEMA == 2, 'a versao do esquema SUBIU'; print('esquema 2 ok')"</automated>
  </verify>

  <acceptance_criteria>
    - As quatro linhas que o tracer do 02-04 ja lia em `janela_negociacao_f005.png` continuam com
      os MESMOS totais (3666, 1500, 1139, 500) e as MESMAS quantidades (3, 3, 6, 2), e as duas de
      `janela_negociacao_f010.png` continuam com 1890 e 1800 — preso por teste com os valores
      escritos
    - Sobre `janela_tooltip_f012.png`, a contagem de linhas com quantidade lida sob o piso medido
      e ESTRITAMENTE MAIOR que sob o piso compartilhado, e as que atravessam leem `1` — preso por
      teste que compara as duas contagens no mesmo arquivo, sem numero escolhido pelo plano
    - `inspect.signature` confirma ausencia de valor de fabrica em `ler_celula`,
      `ler_celula_de_numero`, `ler_celula_de_quantidade` e nos dois parametros de `ler_linha`
    - Um `calibration.json` SEM a chave nova carrega inteiro, com o campo em `None`, e a leitura de
      mercado fica OFF com um unico aviso alto que nomeia a chave — nunca `raise` no tick
    - `l2scanner/rastreador.py` e `l2scanner/visao.py` NAO aparecem em `git diff --name-only`
    - `calibration.json` NAO aparece em `git log --name-only` de nenhum commit desta onda
    - Suite completa verde: `python -m pytest -q` sem `tests/test_agenda.py` na faixa de
      2023 passed + os testes novos, e `python -m pytest tests/test_agenda.py -q` em 132 passed.
      Um aborto por `KeyboardInterrupt` na linha 1141 de `test_agenda.py` NAO e falha — rodar de novo
  </acceptance_criteria>

  <done>
O piso medido chega a `ler_celula_de_quantidade` por parametro obrigatorio, vindo do
`calibration.json` pelo portao por ausencia do `LeitorDePagina`; as colunas de moeda leem
exatamente os mesmos digitos de antes, preso por valor sobre fixtura versionada; a coluna
Quantity le mais linhas do que lia, preso por comparacao entre as duas contagens; e
`VERSAO_DO_ESQUEMA` segue em 2 com a chave nova entrando por `.get`.
  </done>
</task>

</tasks>

<threat_model>

Nivel: **OWASP ASVS L1**. Bloqueia em `high`. O produto e um leitor passivo de tela que roda na
maquina do usuario; a superficie de ataque real e o `calibration.json` (arquivo que o usuario
edita e que ferramentas escrevem) e os PNGs de `recordings/` (entrada nao confiavel do ponto de
vista do decodificador).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> `Calibracao.carregar` | arquivo em disco, editavel a mao, escrito por tres ferramentas diferentes; atravessa para dentro do laco que decide preco |
| PNG de `recordings/` -> `cv2.imread` na ferramenta | dado nao confiavel do ponto de vista do decodificador de imagem; a ferramenta le 478+ frames |
| ferramenta de medicao -> `calibration.json` | escrita que pode destruir os 13 moldes e as 3 ancoras que so a mao do usuario produz |
| valor lido da tela -> `LinhaLida` -> (Fase 3) CSV | um numero errado plausivel atravessa a fronteira e vira dado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | `mercado_limiar_de_brilho_da_quantidade` no `calibration.json` | high | mitigate | Faixa `[1, 254]` conferida em `_conferir_as_chaves_de_mercado` no ARRANQUE, com `bool` recusado explicitamente (e subclasse de `int` e viraria piso 1 calado). Piso 0 faria toda celula virar ruido; piso 255 faria toda celula cair. `None` passa e significa feature OFF |
| T-02-07-02 | Tampering | `gravar` de `tools/medir_brilho_da_quantidade.py` | high | mitigate | Load-mutate-save por `json.loads` + `NamedTemporaryFile` no mesmo diretorio + `os.replace`, mutando UMA chave. Nunca montar `Calibracao` do zero — foi o modo de falha real da janela quebrada 13. Portao de digest md5 dos 13 moldes e das 3 ancoras antes/depois da escrita, registrado no SUMMARY |
| T-02-07-03 | Spoofing | pixel adulterado da coluna Quantity (tooltip semitransparente sobre o numero) | high | mitigate | Nenhuma peneira e removida por este plano: a sonda de oclusao roda antes, o tudo-ou-nada da celula continua, e a gramatica continua. O piso proposto e obrigado a ter ZERO leituras divergentes do rotulo derivado; um piso que produza UMA divergencia e recusado, e o veredito e gravado |
| T-02-07-04 | Information Disclosure | `tests/fixtures/mercado/calibracao_de_fixture.json` versionada | medium | mitigate | So a chave `mercado_*` nova e copiada; as chaves de party seguem com os valores neutros que o 02-04 gravou. Nenhum nome nem recorte de tela de pessoa real entra no repositorio |
| T-02-07-05 | Denial of Service | `cv2.imread` sobre PNG corrompido de `recordings/` na varredura | low | accept | A ferramenta e offline e roda sob supervisao do desenvolvedor; um frame que devolve `None` e pulado, no mesmo padrao de `medir_leitura_de_glifo.varrer`. O material e produzido pelo proprio gravador do projeto |
| T-02-07-06 | Denial of Service | excecao dentro do tick por causa do parametro novo | high | mitigate | `ler_linha` mantem o `try/except` que contem qualquer excecao e devolve `Descarte`, e a ausencia da chave e tratada por AUSENCIA no `_calibrado()` do `LeitorDePagina`, nunca por `raise` — o tick e o mesmo laco que vigia a party, e uma excecao ali faria a proxima morte real passar despercebida |
| T-02-07-07 | Repudiation | um piso gravado sem que se saiba qual medicao o produziu | medium | mitigate | O veredito (PROPOSTO com folga, ou REPROVADO com o numero) entra no SUMMARY, no relatorio da ferramenta e no `.planning/WINDOWS.md`, e a docstring de `ler_celula_de_quantidade` passa a citar a chave e a folga |
| T-02-07-SC | Tampering | instalacao de pacote npm/pip/cargo | n/a | n/a | Este plano NAO instala pacote nenhum. `rapidfuzz` continua fora e nenhuma dependencia nova entra na arvore — `tests/test_firewall_escopo.py` (FIRE-01) continua sendo o detector |

**A ameaca que dimensiona o plano e a T-02-07-03**, e ela e a razao de o criterio de proposta ser
"zero leituras divergentes" e nao "o mais baixo que ainda le": medido pelo planejador, com o piso
em 150 o `30` vira `38` com score 0,724 e margem 0,127, atravessando as duas peneiras de leitura.
Trocar falha FECHADA por numero errado plausivel e exatamente o modo de falha que LEIT-02 existe
para impedir, e e um nivel acima do incidente 27x.

</threat_model>

<artifacts_this_phase_produces>

Desta onda:

| Artefato | Natureza | Versionado? |
|---|---|---|
| `tools/medir_brilho_da_quantidade.py` | ferramenta de medicao offline, com `--gravar` opcional | sim |
| `tests/test_medir_brilho_da_quantidade.py` | testes da ferramenta sobre fixturas versionadas | sim |
| `mercado_limiar_de_brilho_da_quantidade` | chave MEDIDA no `calibration.json` da maquina do usuario | **NAO — gitignored, e nunca pode ser commitado** |
| `mascara_de_numero`, `segmentar_glifos_no_brilho` | primitivas puras irmas em `l2scanner/mercado_leitura.py` | sim |
| o `valor_minimo` sem valor de fabrica nas tres leitoras e em `ler_linha` | contrato de producao | sim |
| o campo novo em `l2scanner/calibracao.py` com faixa conferida no arranque | contrato de configuracao | sim |
| a decima chave no portao por ausencia de `LeitorDePagina._calibrado()` | feature OFF honesta | sim |
| a chave copiada VERBATIM em `tests/fixtures/mercado/calibracao_de_fixture.json` | calibracao de fixtura | sim |
| os TRES relatorios da ferramenta (pico por coluna, tabela de candidatos, custo do piso global) | evidencia da medicao, transcrita no SUMMARY | no SUMMARY |
| a atualizacao de `.planning/WINDOWS.md` #16 | ledger de janela quebrada | sim |

Da fase inteira, para contexto — esta onda e a setima de sete:

- 02-01: as 14 chaves de mercado, as quatro colunas e o molde de cabecalho; portao humano de recalibracao
- 02-02: a sonda de oclusao, o limiar de dispersao, o piso de linhas comparadas, o piso e a margem de LEITURA, e o veredito REPROVADO da guarda de cruzamento
- 02-03: o corte e o piso de similaridade, a trava de digitos, e a fonte `ocr-estrito` escolhida pelo usuario
- 02-04: o TRACER — `mercado_leitura.py` e `mercado_pagina.py`, nove primitivas promovidas, e quatro refutacoes medidas
- 02-06: a guarda de cruzamento e a terceira coluna de numero
- **02-07 (esta): o piso de brilho proprio da coluna Quantity, MEDIDO**
- 02-05: o catalogo de nomes em arquivo proprio, o congelamento por janela inteira, e o replay que afirma a fase

</artifacts_this_phase_produces>

<verification>

1. **Os testes desta onda, isolados:**
   `python -m pytest tests/test_medir_brilho_da_quantidade.py tests/test_mercado_leitura.py tests/test_mercado_pagina.py -q`

2. **Os detectores de regressao do movimento e da calibracao:**
   `python -m pytest tests/test_mercado_glifos.py tests/test_calibrar_mercado.py tests/test_calibracao_mercado.py tests/test_medir_leitura_de_glifo.py tests/test_medir_agrupamento_de_nome.py -q`
   Estes cinco arquivos sao quem pega um `segmentar_glifos` que mudou de comportamento pelas costas.

3. **O detector de morte segue intocado:**
   `python -m pytest tests/test_mercado_27x.py tests/test_visao.py -q` e
   `git diff --name-only` sem `l2scanner/rastreador.py` e sem `l2scanner/visao.py`.

4. **O firewall de escopo:** `python -m pytest tests/test_firewall_escopo.py -q` — nenhuma
   dependencia nova entrou, e `rapidfuzz` continua fora.

5. **A suite completa**, no Python GLOBAL: `python -m pytest -q` (baseline 2023 passed, 2 skipped
   antes desta onda) e `python -m pytest tests/test_agenda.py -q` (132 passed). Um aborto por
   `KeyboardInterrupt` na linha 1141 de `test_agenda.py` NAO e falha — rodar de novo.

6. **Os gates de producao de dado**, no checkout PRINCIPAL, com caminhos absolutos: a varredura
   sai com codigo 0 e imprime os tres relatorios; apontada para um diretorio com uma pasta so,
   sai com codigo 3 nomeando as sete que faltam; e o portao de digest md5 dos 13 moldes e das 3
   ancoras confere identico antes e depois do `--gravar`.

7. **O arquivo do usuario esta fora do git:** `git status --short | grep -c calibration.json`
   devolve 0, e `git log --name-only` dos commits desta onda nao o menciona.

8. **Portao humano de fim de fase** (`human_verify_mode: end-of-phase`, junto do que o 02-04 ja
   registrou como D8 e D9):
   <human-check>O usuario abre o World Exchange na grade de negociacao, roda a leitura e confere
   na tela que as quantidades exibidas — incluindo as linhas com quantidade `1` — batem com o que
   o console mostra. Este e o unico criterio que fixtura nenhuma alcanca, porque o pytest roda no
   Python global e injeta as duas leitoras de OCR.</human-check>

</verification>

<success_criteria>

- [ ] O piso da coluna Quantity foi MEDIDO por varredura sobre as 8 gravacoes NOMEADAS do censo,
      com o portao de layout ligado, e o veredito (PROPOSTO com folga em niveis de V, ou REPROVADO
      com o numero) esta escrito no SUMMARY e no `.planning/WINDOWS.md`
- [ ] O rotulo da medicao e derivado de `Total` e `Unit price` e nao toca a coluna Quantity — a
      nao-circularidade e afirmada por teste
- [ ] A ferramenta RECUSA propor quando nenhum candidato tem zero leituras divergentes do rotulo,
      e isso e afirmado por teste sobre uma populacao construida sem vao
- [ ] As tres perguntas de desenho (piso por coluna x global, causa da diferenca de brilho, quais
      digitos sao afetados) tem resposta com NUMERO nos tres relatorios da ferramenta, medidos no
      censo e nao apenas na sondagem do planejamento
- [ ] `mercado_limiar_de_brilho_da_quantidade` chega a `ler_celula_de_quantidade` por parametro
      SEM valor de fabrica, afirmado por `inspect.signature`
- [ ] As colunas de MOEDA leem exatamente os mesmos digitos de antes, preso por valor sobre
      fixtura versionada
- [ ] A coluna Quantity le mais linhas do que lia sobre `janela_tooltip_f012.png`, preso por
      comparacao entre as duas contagens no mesmo teste
- [ ] `VERSAO_DO_ESQUEMA` segue em 2, a chave nova entra por `.get`, e `calibration.json` nunca
      foi commitado
- [ ] `l2scanner/rastreador.py`, o gate de brilho de `l2scanner/visao.py`, o
      `fundir_com_a_calibracao_em_disco` e a mira da janela de `l2scanner/calibrar.py` NAO foram
      tocados
- [ ] Nenhuma dependencia nova entrou na arvore; `rapidfuzz` continua fora
- [ ] O bloco `<threat_model>` foi conferido: nenhuma ameaca `high` ficou sem mitigacao
      implementada e verificada

</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-07-SUMMARY.md` when done.

Registrar no SUMMARY, obrigatoriamente:
- o piso proposto, a folga em niveis de V, e as duas populacoes que a definiram
- os TRES relatorios em forma resumida (pico por coluna com a dispersao, a tabela de candidatos
  com o veredito de cada um, o custo de um piso global na coluna de moeda)
- o rendimento por gravacao ANTES e DEPOIS, e a taxa de leitura por digito
- os dois digests md5 (13 moldes, 3 ancoras) antes e depois da escrita
- toda medicao que CONTRADIGA a sondagem de 3 frames registrada em
  `<as_quatro_perguntas_respondidas_por_escrito>` deste plano, com o numero que a derrubou
</output>
