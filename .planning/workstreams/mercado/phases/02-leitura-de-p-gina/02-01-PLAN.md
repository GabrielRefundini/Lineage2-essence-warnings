---
phase: 02-leitura-de-p-gina
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/calibracao.py
  - l2scanner/mercado_visao.py
  - l2scanner/calibrar_mercado.py
  - tests/test_calibracao.py
  - tests/test_calibrar_mercado.py
autonomous: false
requirements: [LEIT-05, LEIT-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A ferramenta de calibracao marca a COLUNA DO NOME como dx+largura relativos a ORIGEM DO PAINEL, no mesmo fluxo propor-e-confirmar (D-13, LEIT-05) — o usuario da ENTER num retangulo ja desenhado, ou arrasta o seu"
    - "A ferramenta marca tambem os retangulos de Quantity, Total e Unit price, pela mesma mecanica — em producao a coluna de numero e retangulo CALIBRADO, nunca localizada por `numeros_com_sufixo`"
    - "O molde do cabecalho de coluna e cortado de uma mascara de brilho cujo corte foi MEDIDO neste frame e gravado no calibration.json — a seta de ordenacao e a borda da banda ficam de fora do molde (D-11)"
    - "Uma calibration.json gravada ANTES desta fase continua carregando sem uma linha de migracao: toda chave nova entra por `.get`, e VERSAO_DO_ESQUEMA segue em 2 — os 13 moldes de glifo e as 3 ancoras do usuario nao somem"
    - "Toda chave nova e ENTRADA NAO CONFIAVEL e e conferida no portao de carga, com mensagem que termina no conserto ('Recalibre...')"
    - "O passo da watchlist (moldes de nome) nao pede mais nada ao usuario nem manda ele escrever watchlist: LEIT-01 passou a ler o nome por OCR em 2026-08-29 e `mercado_templates_de_nome` fica None para sempre"
    - "O usuario roda `calibrar-mercado.bat` sobre um frame da GRADE DE NEGOCIACAO e o calibration.json passa a dizer `mercado_grade.layout == 'negociacao'` (D-09, D-10)"
  artifacts:
    - path: "l2scanner/calibracao.py"
      provides: "os 12 campos novos de mercado com `.get`, serializacao em `como_dict` e conferencia no portao de carga"
      contains: "mercado_coluna_do_nome"
    - path: "l2scanner/mercado_visao.py"
      provides: "cabecalho_de_calibracao — desserializacao NAO CONFIAVEL do molde do cabecalho, no molde de glifos_de_calibracao"
      contains: "cabecalho_de_calibracao"
    - path: "l2scanner/calibrar_mercado.py"
      provides: "marcacao propor-e-confirmar das quatro colunas + corte do molde de cabecalho; passo de watchlist aposentado"
      contains: "mercado_coluna_do_nome"
    - path: "tests/test_calibracao.py"
      provides: "a garantia de que um calibration.json v2 antigo carrega inteiro e que cada chave nova e recusada quando mentida"
      contains: "mercado_coluna_do_nome"
  key_links:
    - from: "l2scanner/calibrar_mercado.py"
      to: "l2scanner/calibracao.py"
      via: "load-mutate-save: carrega a Calibracao inteira, atribui SO as chaves novas, grava — nunca monta uma Calibracao do zero"
      pattern: "cal.mercado_coluna_do_nome"
    - from: "l2scanner/calibracao.py"
      to: "l2scanner/mercado_visao.py"
      via: "o portao de carga chama a conferencia das chaves de mercado antes de qualquer consumidor ver o dado"
      pattern: "_conferir_as_chaves_de_mercado"
---

<objective>
Dar ao `calibration.json` a superficie que a leitura da grade de NEGOCIACAO precisa, e
parar para que o usuario a preencha com a propria mao.

Hoje o arquivo da maquina dele diz `mercado_grade.layout: "adena"`, e a aba Adena nao tem
nome de item — o OCR da primeira coluna devolve literalmente `'Adena'` em toda linha. LEIT-01
e LEIT-05 nao tem objeto ali. O censo das 335 gravacoes confirma o tamanho do problema: ~25
frames de Adena contra ~283 da grade de negociacao. O usuario escolheu recalibrar (D-10), e
essa escolha e um PORTAO: nenhuma linha de codigo desta fase substitui a mao dele.

Purpose: sem as quatro colunas marcadas e o layout gravado como `negociacao`, a onda de
medicao (02-02, 02-03) mediria contra o recorte errado — e a docstring de `segmentar_glifos`
ja avisa que os numeros MUDAM com o recorte. Medir com uma convencao e ler com outra e
comparar convencoes.

Output: 12 chaves novas no `calibration.json` (declaradas, validadas, opcionais), a marcacao
delas em `calibrar_mercado.py`, e um `calibration.json` recalibrado para a grade de
negociacao pela mao do usuario.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-RESEARCH.md
@.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: As 12 chaves novas de mercado no calibration.json, declaradas e conferidas</name>
  <files>l2scanner/calibracao.py, l2scanner/mercado_visao.py, tests/test_calibracao.py</files>
  <read_first>
    - `l2scanner/calibracao.py` inteiro, com atencao em: `VERSAO_DO_ESQUEMA` (:23), os campos
      `mercado_*` (:231-306), `como_dict` (:405-416), `carregar` (:494-510) e o comentario que
      explica o `.get`, o portao `_conferir_as_chaves_de_mercado` (:463 e :555-570)
    - `l2scanner/mercado_visao.py:667-741` (`glifos_de_calibracao`) e `:227-260` (`molde_de_hex`
      e a conferencia de bytes antes do reshape) — a estrutura a copiar inteira
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-PATTERNS.md`, secao
      "`l2scanner/calibracao.py` (MOD)" — os QUATRO lugares que uma chave nova toca
    - `tests/test_calibracao.py` — onde os testes das chaves irmas ja moram
  </read_first>
  <behavior>
    - Um `calibration.json` gravado ANTES desta fase (sem nenhuma chave nova) carrega inteiro,
      com os 13 moldes de glifo e as 3 ancoras preservados, e todos os campos novos em `None`
    - `mercado_coluna_do_nome` com `largura` <= 0 e recusado no portao de carga, com mensagem
      que termina mandando recalibrar
    - `mercado_coluna_do_nome` com `dx` booleano (`True`) e recusado — `bool` e subclasse de
      `int` e passaria por um `isinstance(x, int)` ingenuo
    - `mercado_cabecalho_de_coluna` cujo campo `bytes` nao tem `altura * largura` bytes e
      recusado ANTES do reshape, dizendo quantos bytes faltam
    - `mercado_cabecalho_de_coluna` ausente devolve `None` e NAO levanta: feature OFF e o
      default seguro
    - `mercado_limiar_de_dispersao_do_fundo` fora de `[0.0, 1.0]` e recusado
    - `como_dict()` seguido de `carregar()` devolve os mesmos valores (ida e volta)
  </behavior>
  <action>
Acrescentar a `Calibracao` doze campos opcionais, todos com default `None`, cada um com o
comentario de razao no estilo dos campos irmaos (`calibracao.py:231-306`):
`mercado_coluna_do_nome`, `mercado_coluna_da_quantidade`, `mercado_coluna_do_total`,
`mercado_coluna_do_unitario` (cada um um dict `{"dx": int, "largura": int}`, deslocamento em x
relativo a ORIGEM DO PAINEL e largura em px, pela mesma razao que `mercado_grade` guarda
deslocamento: o painel anda 827x831 px);
`mercado_cabecalho_de_coluna` (dict `{"layout": str, "dy": int, "altura": int, "largura": int,
"bytes": str-hex, "corte_de_brilho": int}`, lista CRUA sem decodificar, pela mesma razao de
`mercado_molde_da_ancora`); `mercado_limiar_do_cabecalho` (float);
`mercado_sonda_do_fundo` (dict `{"dx0": int, "dx1": int, "folga": int}`);
`mercado_limiar_de_dispersao_do_fundo` (float);
`mercado_limiar_de_leitura_de_glifo` (float); `mercado_margem_de_leitura_de_glifo` (float);
`mercado_corte_de_similaridade` (float); `mercado_piso_de_similaridade` (float).

Os quatro limiares de leitura ganham, no comentario do campo, a razao de NAO serem
`mercado_limiar_de_glifo`: aquele e derivado de `(1.0 + pior_par)/2` e certifica que o
CONJUNTO de moldes e separavel — medido molde-contra-molde, nunca contra glifo de tela. A
medicao da pesquisa sobre 2.057 glifos reais mostrou o `8` com mediana 0,7242 contra o proprio
molde; um piso em 0,8555 rejeitaria praticamente todo `8` da tela. Registrar esse numero
refutado ao lado do campo, no padrao de `ocr.py:36-39`.

Ligar os doze em `como_dict` e em `carregar`, este ultimo SEMPRE por `dados.get(...)`,
estendendo o comentario que ja explica o porque (`calibracao.py:494-510`). NAO tocar
`VERSAO_DO_ESQUEMA`: ele segue em 2, e a razao ja esta escrita em `:199` e `:218`. Um bump
faria o portao de versao recusar o arquivo do usuario, que carrega 13 moldes de glifo e 3
ancoras que nao podem sumir.

Estender `_conferir_as_chaves_de_mercado` para cobrir as doze, na disciplina ja escrita ali:
tipo, faixa, `bool` excluido explicitamente do `int`, e toda mensagem terminando no conserto
(`Recalibre o mercado: calibrar-mercado.bat --gravacao recordings\<pasta>`). Ausencia nunca e
erro — e feature OFF.

Em `mercado_visao.py`, criar `cabecalho_de_calibracao(dados: dict | None) -> np.ndarray | None`
copiando a estrutura inteira de `glifos_de_calibracao` (:667-741): `None`/vazio devolve `None`
sem levantar; dimensoes nao-positivas recusam; contagem de bytes conferida contra
`altura * largura` ANTES do `bytes.fromhex`/reshape, dizendo quantos bytes ha e quantos faltam;
delega o reshape a `molde_de_hex` com `forma_esperada`.

Os testes novos vao em `tests/test_calibracao.py`, ao lado dos das chaves irmas.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibracao.py tests/test_mercado_visao.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json,l2scanner.calibracao as c; print(c.VERSAO_DO_ESQUEMA)"` imprime `2`
    - `grep -c 'dados.get("mercado_coluna_do_nome")' l2scanner/calibracao.py` devolve 1 — a chave
      entra por `.get`, nunca por indexacao
    - `python -m pytest tests/test_calibracao.py -q` passa, incluindo um teste que carrega um
      dict SEM nenhuma chave nova e afirma que os 12 campos saem `None` e os moldes de glifo
      seguem la
    - Existe teste que afirma recusa para: largura <= 0, `dx` booleano, bytes em contagem errada,
      limiar de dispersao fora de [0,1] — e cada mensagem de recusa contem a palavra `Recalibre`
    - A calibracao REAL do usuario segue carregando:
      `python -c "from pathlib import Path; from l2scanner.calibracao import Calibracao; c=Calibracao.carregar(Path('calibration.json')); print(len(c.mercado_templates_de_digito), len(c.mercado_ancoras))"`
      imprime `13 3`
  </acceptance_criteria>
  <done>Os 12 campos existem, serializam, carregam por `.get` e sao conferidos no portao; o calibration.json do usuario carrega intacto e `VERSAO_DO_ESQUEMA` segue 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A ferramenta marca as quatro colunas e corta o molde do cabecalho; o passo da watchlist e aposentado</name>
  <files>l2scanner/calibrar_mercado.py, tests/test_calibrar_mercado.py</files>
  <read_first>
    - `l2scanner/calibrar_mercado.py`: o fluxo `calibrar()` (:2091-2371), com atencao em
      `watchlist = ler_watchlist(...)` (:2129), o bloco de moldes de nome (:2247-2274), a
      atribuicao (:2315-2340) e o resumo final (:2363-2365)
    - `l2scanner/calibrar_mercado.py:1646-1699` (`_pedir_rotulo`) — a docstring que define o
      contrato propor-e-confirmar: a ferramenta PROPOE, o ENTER confirma, o que o usuario
      digitar sempre vence
    - `l2scanner/calibrar_mercado.py:1511-1595` (`sugerir_a_coluna_de_preco`) — o proponente
      medido do retangulo de numero
    - `l2scanner/calibrar_mercado.py:63-68` — o import de `_selecionar_regiao` e
      `_gravar_conferencia` vindos de `calibrar.py`
    - `l2scanner/mercado_geometria.py:399-506` (`medir_a_grade`, `localizar_o_titulo`) — de onde
      sai a proposta medida
    - `l2scanner/identidade.py:134` (`mascara_de_texto`) — a mascara de brilho ja compartilhada
    - `02-RESEARCH.md`, pergunta 6 — a banda do cabecalho `[topo_da_grade-32, topo_da_grade-2)`,
      os grupos medidos por layout, e onde a seta de ordenacao cai (3 px, linhas 15..22)
  </read_first>
  <behavior>
    - `sugerir_a_coluna_do_nome` devolve um retangulo que comeca depois do icone e cobre 270 px
      de largura no frame de pagina cheia; devolve `None` quando nao consegue medir, e a janela
      abre vazia para o usuario arrastar
    - O corte de brilho proposto para o cabecalho fica ESTRITAMENTE entre o brilho maximo da
      seta de ordenacao e o brilho maximo dos rotulos de coluna, medidos no proprio frame
    - Com o corte aplicado, o conjunto de grupos de colunas claros da banda do cabecalho NAO
      contem o grupo de 3 px da seta — e e o MESMO conjunto quando o mesmo frame esta ordenado
      por outra coluna
    - Um retangulo de coluna marcado fora dos limites da grade e recusado na hora, com o
      conserto na mensagem
    - Rodar a ferramenta com o `config.toml` sem watchlist nao imprime nenhum pedido de escrever
      watchlist e nao pede recorte de nome nenhum
  </behavior>
  <action>
Acrescentar ao fluxo de `calibrar()`, depois da grade e antes dos glifos, quatro marcacoes
propor-e-confirmar — uma por coluna: NOME (per D-13, LEIT-05), QUANTITY, TOTAL e UNIT PRICE.
Cada uma abre a janela com o retangulo JA DESENHADO em verde, ENTER aceita, qualquer outra
tecla deixa arrastar, ESC cancela sem gravar. Reusar `_selecionar_regiao` de `calibrar.py`;
nao escrever mecanica de selecao nova.

A proposta vem de medicao, nunca de constante: para as tres colunas de numero, reusar
`sugerir_a_coluna_de_preco`; para a coluna do nome, escrever `sugerir_a_coluna_do_nome`, que
mede na primeira linha cheia da grade o fim do icone e o comeco do primeiro grupo de texto
claro, e devolve `(dx, largura)` relativos a origem do painel. As duas devolvem `None` quando
a medicao nao fecha — e `None` abre a janela vazia, sem inventar retangulo. Persistir cada uma
como `{"dx": <x - origem_x>, "largura": <largura>}`, NUNCA em coordenada absoluta, pela razao
ja escrita na docstring de `derivar_grade` (:712-726): o painel anda.

Acrescentar o corte do MOLDE DO CABECALHO. A banda e `[topo_da_grade - 32, topo_da_grade - 2)`
por toda a largura da grade — medida na pesquisa, e ela vai para o `calibration.json` junto do
molde, nunca para o codigo. O corte de brilho e MEDIDO neste frame, nao herdado: varrer a banda
medindo o brilho maximo dos grupos de texto claro e o da seta de ordenacao, e propor o ponto
medio entre os dois; o valor medido na pesquisa (rotulos 229, seta 181, borda 201) e o
resultado esperado, nao a constante. Gravar o corte junto do molde em `corte_de_brilho`, com o
comentario registrando que ele foi medido em UMA resolucao e UMA pele — por isso mora no
arquivo, e nao no fonte. Guardar o molde em hex com `altura`/`largura`, no mesmo formato de
`mercado_molde_da_ancora`. Gravar `mercado_limiar_do_cabecalho` reusando o precedente
`CASAMENTO_MINIMO_DA_ANCORA` como proposta, confirmada pelo casamento medido do proprio molde
contra o proprio frame.

APOSENTAR o passo da watchlist. `mercado_templates_de_nome` e `mercado_limiar_de_template`
morreram com a reescrita de LEIT-01 em 2026-08-29 e devem ficar `None` para sempre. Remover o
bloco que pede recorte por item e o aviso que manda o usuario escrever a lista no
`config.toml`; deixar no lugar uma linha de resumo dizendo que o nome do item passou a ser
lido por OCR e agrupado por similaridade, com ponteiro para REQUIREMENTS.md. Manter
`ler_watchlist` e a matriz de confusao de nomes no arquivo, sem chamador no fluxo principal —
elas continuam sendo o precedente medido citado por outros textos, e apagar a funcao apagaria
a medicao junto. O resumo final da ferramenta passa a listar as quatro colunas e o cabecalho
no lugar da contagem de moldes de nome.

Toda a mutacao segue load-mutate-save sobre a `Calibracao` carregada: atribuir SO as chaves
novas e gravar. Nunca montar uma `Calibracao` do zero — isso apagaria os 13 moldes de glifo.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrar_mercado.py tests/test_mercado_glifos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_calibrar_mercado.py -q` passa com testes novos para:
      `sugerir_a_coluna_do_nome` sobre a fixture de pagina cheia, o corte de brilho proposto
      caindo entre seta e rotulos, e a recusa de retangulo fora da grade
    - Existe teste que roda a banda do cabecalho do mesmo frame em duas ordenacoes diferentes e
      afirma que o conjunto de grupos com o corte aplicado e IGUAL nos dois — a seta some
    - `python -m l2scanner.calibrar_mercado --help` lista `--layout` com `negociacao` como padrao
    - Rodar a ferramenta contra um `config.toml` sem `[mercado] watchlist` nao emite nenhuma
      instrucao de preencher lista de itens: teste afirma isso sobre a saida capturada
    - `tests/test_mercado_glifos.py` segue verde — os 13 moldes e a convencao de recorte nao
      mudaram
  </acceptance_criteria>
  <done>A ferramenta propoe e persiste as quatro colunas e o molde do cabecalho com corte de brilho medido; o passo de moldes de nome nao existe mais no fluxo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: PORTAO HUMANO — o usuario recalibra o mercado para a GRADE DE NEGOCIACAO</name>
  <precondition>As Tasks 1 e 2 estao commitadas e `python -m l2scanner.calibrar_mercado --help` roda no `.venv`. Este passo acontece no checkout PRINCIPAL, onde `recordings/` existe (a pasta e gitignored e nao se materializa num worktree).</precondition>
  <read_first>
    - `calibrar-mercado.bat` — o texto que o usuario le ao abrir
    - `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-CONTEXT.md`, secao
      "Coluna do nome e os tres layouts" — D-09 e D-10, a decisao e a razao dela
  </read_first>
  <action>O usuario roda `calibrar-mercado.bat` sobre um frame da grade de negociacao e confirma os retangulos. Nenhuma linha de codigo substitui esta mao: o `calibration.json` e estado da MAQUINA dele, gitignored, e a alternativa (dar ao teste uma calibracao de fixture propria) foi explicitamente recusada por ele em 2026-08-29 — ela deixaria producao e teste calibrados em layouts diferentes.</action>
  <instructions>
Ja automatizado: as 12 chaves existem, validam e carregam; a ferramenta PROPOE cada retangulo
medido nos pixels e desenha em verde; o passo de moldes de nome saiu do fluxo. O que sobra e a
mao humana sobre a maquina do usuario.

Apresente ao usuario, nesta ordem:

1. POR QUE. O `calibration.json` dele hoje diz `mercado_grade.layout: "adena"`. A aba Adena nao
   tem nome de item — o OCR da primeira coluna devolve literalmente `'Adena'` em toda linha,
   porque a mercadoria ali E adena. LEIT-01 (nome por OCR) e LEIT-05 (coluna do nome) nao tem
   objeto naquele layout. E o censo das 335 gravacoes da o tamanho: ~25 frames de Adena contra
   ~283 da grade de negociacao. Sem recalibrar, o replay recusaria 283 dos ~308 frames com
   painel aberto.

2. O COMANDO, exatamente:
       calibrar-mercado.bat --gravacao recordings60828-063752-mercado-aberto --indice 0
   O `--layout` ja tem `negociacao` como padrao; nao precisa passar. Esse frame foi escolhido por
   medicao: e a grade de negociacao com o cabecalho ordenado por `Goods`, e os quatro rotulos de
   coluna (`Goods`, `Quantity`, `Total`, `Unit price`) aparecem inteiros na banda. Alternativa
   igualmente boa, com a pagina cheia de dez linhas:
       calibrar-mercado.bat --gravacao recordings60828-060622-mercado-pagina-cheia --indice 10

3. O QUE ELE VAI CONFIRMAR, um retangulo de cada vez, com ENTER aceitando o verde ja desenhado:
   as tres ancoras do painel; a area da lista e a primeira linha; a COLUNA DO NOME; as colunas
   Quantity, Total e Unit price; a banda do cabecalho. Ele NAO vai mais ser perguntado sobre
   nomes de item — esse passo saiu do fluxo.

4. O QUE CONFERIR NA IMAGEM. O retangulo da coluna do nome tem de comecar DEPOIS do icone do item
   e terminar depois do fim do nome mais longo da pagina, sem invadir a coluna Quantity. Um nome
   medido num recorte de 143 px saiu truncado (`Common Mafia Leader Lucia`); com 270 px saiu
   inteiro. Se o verde parecer curto, arraste mais largo — cortar do nosso lado e um modo de
   falha conhecido e medido.

5. NAO ROLE A PAGINA nem mude a aba entre os retangulos: eles sao marcados sobre o MESMO frame
   gravado, entao nada na tela ao vivo importa.

Depois que ele confirmar que rodou, conferir por leitura e reportar de volta:
`mercado_grade.layout`, `mercado_grade.linhas_por_pagina`, e a presenca e os valores das quatro
colunas e do cabecalho. Se `linhas_por_pagina` sair diferente de 10, PARE de novo e mostre a ele:
10 e o numero medido em campo para a grade de negociacao, e errar por baixo custa um anuncio
inteiro por pagina, calado, la na frente.
  </instructions>
  <indice_de_decisoes>
## Indice das decisoes citadas neste plano

O `02-CONTEXT.md` registra as decisoes do usuario em prosa, sem numera-las. Os IDs abaixo
sao estaveis e seguem a ORDEM DE LEITURA da secao `<decisions>` daquele arquivo — eles nao
inventam decisao nenhuma, so dao nome ao que ja esta escrito la.

| ID | Decisao (do `02-CONTEXT.md`) |
|---|---|
| D-04 | difflib da stdlib, e NAO rapidfuzz — nenhuma dependencia nova entra na arvore. |
| D-09 | O v1 le SOMENTE o layout calibrado — e o layout calibrado passa a ser a GRADE DE NEGOCIACAO (REVISADO 2026-08-29 pelo censo dos 335 frames). |
| D-10 | PORTAO HUMANO: o usuario recalibra. A alternativa da calibracao de fixture propria foi recusada por ele. |
| D-11 | O layout e reconhecido por molde do cabecalho de coluna, cortado SEM a seta de ordenacao. |
| D-13 | A coluna do nome e calibrada como x + largura relativos a ORIGEM DO PAINEL, persistida em calibration.json. |
</indice_de_decisoes>

<verification>
Rodar, no checkout principal:
`python -c "import json;d=json.load(open('calibration.json',encoding='utf-8'));g=d['mercado_grade'];assert g['layout']=='negociacao',g;assert g['linhas_por_pagina']==10,g;assert d.get('mercado_coluna_do_nome'),'coluna do nome ausente';assert d.get('mercado_coluna_do_total'),'coluna do total ausente';assert d.get('mercado_coluna_da_quantidade'),'coluna da quantidade ausente';assert d.get('mercado_cabecalho_de_coluna'),'cabecalho ausente';assert len(d['mercado_templates_de_digito'])==13,'os 13 moldes de glifo sumiram';assert len(d['mercado_ancoras'])==3,'as 3 ancoras sumiram';print('OK', g)"`
e conferir que `Calibracao.carregar(Path('calibration.json'))` aceita o arquivo sem recusa.
  </verification>
  <resume-signal>Digite "calibrei" depois de rodar e conferir a imagem de conferencia, ou descreva o que saiu diferente do esperado.</resume-signal>
  <verify>
    <human-check>O usuario confirma que rodou `calibrar-mercado.bat` sobre um frame da grade de negociacao e que os retangulos verdes da imagem de conferencia caem onde ele espera — em especial o da coluna do nome, que precisa cobrir o nome mais longo da pagina inteiro.</human-check>
    <automated>python -c "import json;d=json.load(open('calibration.json',encoding='utf-8'));g=d['mercado_grade'];assert g['layout']=='negociacao',g;assert g['linhas_por_pagina']==10,g;assert d.get('mercado_coluna_do_nome');assert d.get('mercado_coluna_do_total');assert d.get('mercado_coluna_da_quantidade');assert d.get('mercado_cabecalho_de_coluna');assert len(d['mercado_templates_de_digito'])==13;assert len(d['mercado_ancoras'])==3;print('OK', g)"</automated>
  </verify>
  <acceptance_criteria>
    - `calibration.json` traz `mercado_grade.layout == "negociacao"` e `linhas_por_pagina == 10`
    - As quatro colunas (`nome`, `quantidade`, `total`, `unitario`) e `mercado_cabecalho_de_coluna`
      existem e passam por `Calibracao.carregar` sem recusa
    - Os 13 moldes de glifo e as 3 ancoras continuam la — a recalibracao nao apagou nada
    - `mercado_templates_de_nome` continua `null`
    - O usuario declarou por escrito que conferiu a imagem de conferencia
  </acceptance_criteria>
  <done>O calibration.json da maquina do usuario descreve a grade de negociacao, com as quatro colunas e o cabecalho marcados pela mao dele, e sem perder nada do que ja havia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo -> processo (`calibration.json`) | O usuario pode editar o arquivo a mao; ele atravessa a fronteira como entrada NAO CONFIAVEL |
| humano -> ferramenta (`selectROI`) | Retangulo arrastado com o mouse; nada garante que ele caia dentro do painel |
| disco -> processo (`config.toml`) | TOML editado a mao pelo usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `mercado_cabecalho_de_coluna` -> `molde_de_hex` | high | mitigate | Conferir `len(bytes) == altura * largura` ANTES do reshape (Task 1); dimensao nao-positiva recusa. Sem isso o reshape devolve um molde silenciosamente errado e o portao de layout recusa TODA pagina sem uma linha de erro |
| T-02-02 | Tampering | `mercado_coluna_do_nome` e irmas | high | mitigate | Faixa conferida no portao de carga (Task 1): largura > 0, e o retangulo dentro dos limites da grade. Um dx mentido faz a leitura recortar OUTRA coluna e produzir numero plausivel e errado por fator inteiro |
| T-02-03 | Tampering | `bool` passando por `isinstance(x, int)` | medium | mitigate | `bool` excluido explicitamente, como `glifos_de_calibracao` ja faz (`mercado_visao.py:667-741`) |
| T-02-04 | Denial of Service | `VERSAO_DO_ESQUEMA` | high | mitigate | NAO bumpar. Um bump faz o portao de versao recusar o arquivo do usuario e apagar 13 moldes de glifo e 3 ancoras que so a mao dele produz |
| T-02-05 | Spoofing | `calibrar()` sobre frame do layout errado | medium | mitigate | `derivar_grade` ja avisa alto quando `linhas_por_pagina` diverge do medido em campo; o portao humano (Task 3) confere o numero por leitura |
| T-02-06 | Repudiation | corte de brilho do cabecalho hardcodado | medium | mitigate | O corte vai para `calibration.json` como `corte_de_brilho`, medido no frame — nunca constante de fonte. Medido em UMA resolucao e UMA pele (A2 da pesquisa) |
| T-02-SC | Tampering | instalacoes npm/pip/cargo | high | accept | **Nenhum install nesta fase.** A unica dependencia candidata (`rapidfuzz`) foi recusada por decisao travada (D-04): o agrupamento usa `difflib` da stdlib. `tests/test_firewall_escopo.py` segue varrendo o venv instalado com a banlist de 9 nomes |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Chaves novas em `calibration.json`** (todas opcionais, todas por `.get`, `VERSAO_DO_ESQUEMA` inalterada):
`mercado_coluna_do_nome`, `mercado_coluna_da_quantidade`, `mercado_coluna_do_total`,
`mercado_coluna_do_unitario` — cada uma `{"dx": int, "largura": int}` relativa a origem do painel;
`mercado_cabecalho_de_coluna` — `{"layout": str, "dy": int, "altura": int, "largura": int, "bytes": str, "corte_de_brilho": int}`;
`mercado_limiar_do_cabecalho` (float);
`mercado_sonda_do_fundo` — `{"dx0": int, "dx1": int, "folga": int}`;
`mercado_limiar_de_dispersao_do_fundo` (float);
`mercado_limiar_de_leitura_de_glifo` (float); `mercado_margem_de_leitura_de_glifo` (float);
`mercado_corte_de_similaridade` (float); `mercado_piso_de_similaridade` (float).

**Simbolos novos:**
- `l2scanner/calibracao.py`: os 12 campos acima na dataclass `Calibracao`; ramos novos em
  `_conferir_as_chaves_de_mercado`
- `l2scanner/mercado_visao.py`: `cabecalho_de_calibracao(dados) -> np.ndarray | None`
- `l2scanner/calibrar_mercado.py`: `sugerir_a_coluna_do_nome(...)`,
  `sugerir_o_molde_do_cabecalho(...)`, `medir_o_corte_de_brilho_do_cabecalho(...)`

**Removido do fluxo (nao do arquivo):** o passo de recorte de moldes de nome da watchlist e o
aviso que manda escrever `[mercado] watchlist`. `ler_watchlist` e `matriz_de_confusao`
permanecem no arquivo como precedente medido.
</artifacts_this_phase_produces>

<verification>
- `python -m pytest tests/test_calibracao.py tests/test_mercado_visao.py tests/test_calibrar_mercado.py tests/test_mercado_glifos.py -q` verde
- `python -m pytest tests/test_firewall_escopo.py -q` verde — nenhuma dependencia nova entrou
- Suite completa: `python -m pytest -q`. Baseline **1704 passed, 2 skipped**, mais os testes
  novos. FLAKE CONHECIDO: `tests/test_agenda.py` vaza um `KeyboardInterrupt` que aborta a
  sessao perto de ~88 testes (5 abortos em 60 rodadas). **Abortar nao e falhar — rode de novo.**
- `calibration.json` recalibrado, conferido pelo comando automatico da Task 3
</verification>

<success_criteria>
As 12 chaves existem, validam e carregam sem migracao; a ferramenta as marca por
propor-e-confirmar; e o `calibration.json` da maquina do usuario descreve a GRADE DE
NEGOCIACAO com as quatro colunas e o cabecalho, sem ter perdido os 13 moldes de glifo nem as
3 ancoras.
</success_criteria>

<output>
Create `.planning/workstreams/mercado/phases/02-leitura-de-p-gina/02-01-SUMMARY.md` when done
</output>

