---
phase: 04-modo-mercado-an-lise-e-console
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - l2scanner/config.py
  - l2scanner/mercado_analise.py
  - l2scanner/mercado_console.py
  - l2scanner/mercado_modo.py
  - tests/test_mercado_receitas.py
autonomous: true
requirements: [ANAL-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Sem `[[receita]]` configurada a margem simplesmente NAO APARECE — nao e erro, nao e aviso"
    - "Um `[[receita]]` mal formado RECUSA O ARRANQUE, com o nome da receita na mensagem"
    - "Cada componente da margem mostra a PROPRIA staleness, e a mais velha sobe para a linha da margem"
    - "Um ingrediente sem observacao nenhuma quebra a margem INTEIRA, com o nome do que falta"
    - "Um nome de receita que casa com duas series quebra a margem, LISTANDO as candidatas"
    - "O cabecalho da secao diz, uma vez, que a oferta pode ja ter sido comprada"
  artifacts:
    - "`ler_receitas` e `ReceitaInvalida` em l2scanner/config.py"
    - "`margem_de_craft` em l2scanner/mercado_analise.py"
    - tests/test_mercado_receitas.py
  key_links:
    - "A resolucao nome digitado -> `chave_da_serie` e casamento EXATO com caixa normalizada; NUNCA `mercado_catalogo.similaridade`"
    - "`isinstance(valor, bool)` ANTES do teste numerico — sem ele `quantidade = true` vira 1"
    - "Os dois lados da conta sao menor pedido visivel, ou seja OFERTAS: a margem e bruta e sobre pedidos, nunca sobre transacoes"
---

<objective>
A margem de craft do ANAL-04, com a staleness de CADA componente visivel — e com as cinco
coisas que ela nao resolve ditas em voz alta.

Purpose: O requisito pede granularidade por componente porque uma margem calculada com um
ingrediente visto hoje e outro visto ha uma semana nao e uma margem. E o problema real
deste requisito nao e a aritmetica: e resolver o nome que o usuario DIGITA para a
`chave_da_serie` que o CSV guarda, num universo em que o `nome_exibido` oscila no OCR e em
que variantes de encanto sao series deliberadamente separadas. Resolver isso "na melhor"
produziria uma margem perfeitamente formatada e completamente falsa.

Output: `ler_receitas` em `config.py`, `margem_de_craft` no modulo puro, e o desenho da
secao no console.

Requisito coberto: **ANAL-04**.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-CONTEXT.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-RESEARCH.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-03-SUMMARY.md
@./.claude/CLAUDE.md
</context>

<restricoes_da_fase>
Valem para TODAS as tasks deste plano.

1. **Nenhuma dependencia nova.** `tomllib`, `fractions` e `datetime` sao stdlib.
2. **NAO tocar `config.toml` neste plano.** O bloco `[[receita]]` comentado e a ULTIMA
   task da fase, no plano 04-05, isolada e sequenciada por ultimo — o arquivo e disputado
   com o agente que trabalha em `tiat` nesta mesma arvore. Este plano escreve o LEITOR; o
   plano 04-05 escreve o exemplo comentado.
3. **NAO tocar** `rastreador.py`, `visao.py`, `respawn.py`, `bosses.py`, `agenda.py`,
   `sessao.py`, `tests/test_bosses.py`, `l2scanner/__main__.py`.
4. **`config.py`**: a funcao nova vai no **FIM** do modulo, sem tocar nada existente.
5. **NUNCA use similaridade fuzzy para casar nome de receita com serie.**
   `mercado_catalogo.similaridade` existe e e calibrada (`mercado_corte_de_similaridade =
   0.8947`), mas foi medida para agrupar duas leituras de OCR **do mesmo pixel** — nao para
   casar texto que o humano digitou. Usa-la aqui casaria um item base com sua variante de
   encanto, que sao series deliberadamente separadas.
6. **Nenhuma comprehension nao-afirmante em bloco de verificacao** — `assert` de verdade.
7. **Sem travessao e sem acento em texto de erro.**
8. Pytest no **Python GLOBAL**. **FLAKE conhecido:** `tests/test_agenda.py:1141` levanta
   `KeyboardInterrupt` de proposito — **abortar nao e falhar**.
</restricoes_da_fase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ler_receitas` — o `[[receita]]` validado no molde de `_boss_de_dict`</name>

  <read_first>
    - `l2scanner/config.py:296-323` (`ler_bosses`) e `:327-408` (`_boss_de_dict`) — o molde
      inteiro: o `onde` que cita o NOME sempre que ele existe, a razao de os campos serem
      OBRIGATORIOS em vez de opcionais com default, e o comentario medido em `:383-390`
      explicando por que `isinstance(valor, bool)` vem ANTES do teste numerico.
    - `l2scanner/config.py` FIM DO MODULO — o formato das mensagens de recusa de tipo.
    - `l2scanner/bosses.py` — a excecao de configuracao e como o `__main__` a trata (recusa
      de arranque com a mensagem e sem traceback). **Leia, nao edite: arquivo proibido.**
    - `config.toml:180-204` — o padrao de secao COMENTADA que o `[[receita]]` vai seguir no
      plano 04-05.
    - `04-RESEARCH.md` secao 4.2 (a forma minima do `[[receita]]`, verificada no `tomllib`)
      e a linha A6 do Assumptions Log.
  </read_first>

  <files>l2scanner/config.py, tests/test_mercado_receitas.py</files>

  <behavior>
    - Teste: arquivo ausente devolve lista vazia, sem levantar.
    - Teste: arquivo presente sem nenhum bloco `[[receita]]` devolve lista vazia, sem
      levantar.
    - Teste: TOML quebrado levanta a excecao de configuracao com o nome do arquivo.
    - Teste: um bloco que nao e tabela levanta, nomeando a posicao do bloco.
    - Teste: `produto` ausente ou vazio levanta, e a mensagem diz o formato certo.
    - Teste: `rende` ausente levanta — ele e OBRIGATORIO, nao opcional com padrao 1.
    - Teste: `rende = true` levanta em vez de virar `1`.
    - Teste: `rende = 0` e `rende = -2` levantam.
    - Teste: `componentes` que nao e lista levanta.
    - Teste: componente sem `item` levanta, e a mensagem NOMEIA a receita.
    - Teste: `quantidade = true` levanta em vez de virar `1`.
    - Teste: uma receita valida com dois componentes em tabela inline parseia e devolve os
      dois com `item` e `quantidade` corretos.
    - Teste: uma receita sem nome utilizavel e citada pela posicao do bloco, e uma com nome
      e citada pelo nome.
  </behavior>

  <action>
Acrescente ao FIM de `l2scanner/config.py` a excecao `ReceitaInvalida`, a dataclass da
receita (com `produto`, `rende` e a tupla de componentes, cada um com `item` e
`quantidade`) e a funcao `ler_receitas(caminho=None)`.

**O molde e `ler_bosses` mais `_boss_de_dict`, literalmente.** Arquivo ausente nao e erro;
secao ausente nao e erro (sem receita a margem simplesmente NAO APARECE — nao e erro e nao
e aviso, e uma secao opcional que o usuario preenche quando quiser). TOML presente e mal
formado E erro de ARRANQUE: uma receita torta tem de parar o programa enquanto o usuario
olha para o console, nunca produzir uma margem plausivel horas depois enquanto ele esta
AFK.

**A forma aceita e a de TABELA INLINE** para os componentes: uma lista de tabelas inline
dentro da chave `componentes`, cada uma com `item` e `quantidade`. As duas formas TOML
possiveis parseiam, mas esta e a que se comenta e descomenta como uma UNIDADE — a forma
alternativa com sub-blocos vira quatro pedacos soltos que o usuario descomenta pela metade
sem perceber.

**Cada recusa NOMEIA a receita** quando ha nome utilizavel, e cita a POSICAO do bloco
quando nao ha. E o mesmo argumento do `_boss_de_dict`: "o segundo bloco esta errado" faz o
usuario contar blocos; "a receita 'X' tem tal campo errado" ele conserta em cinco segundos.

**`isinstance(valor, bool)` ANTES de qualquer teste numerico**, tanto para `rende` quanto
para `quantidade`. Isso nao e zelo: em Python `True` passa por `int` e por `> 0`, e sem
essa guarda um `true` digitado vira `1` em silencio. O comentario medido que justifica isso
ja esta escrito em `config.py`; cite-o.

**`rende` e OBRIGATORIO, nao opcional com padrao 1**, pelo mesmo argumento que tornou os
campos de respawn obrigatorios: um padrao silencioso produz uma margem plausivel e errada
para quem crafta cinco de cada vez. Escreva que isto e ESCOLHA, e que a alternativa
(padrao 1) trocaria verbosidade por risco de numero errado.
  </action>

  <reversibility rating="costly">
    A forma do `[[receita]]` e o contrato que o usuario vai escrever a mao. Mudar o formato
    depois obriga ele a reeditar o `config.toml` — e o arquivo e disputado com outro agente
    nesta arvore. A forma escolhida (tabela inline) esta verificada contra o `tomllib` e e
    a que se comenta como unidade; ela nao deve mudar depois do plano 04-05.
  </reversibility>

  <verify>
    <automated>python -m pytest tests/test_mercado_receitas.py -x -q</automated>
    <automated>python -m pytest tests/test_bosses.py tests/test_mira_da_janela.py tests/test_calibracao_generica.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from l2scanner.config import ler_receitas; from pathlib import Path; assert ler_receitas(Path('nao-existe.toml')) == []"` sai com codigo 0.
    - Um teste escreve em `tmp_path` um TOML com `rende = true` e afirma com
      `pytest.raises(ReceitaInvalida)` — e afirma com `assert` que o valor NAO virou `1`.
    - Um teste com `quantidade = true` num componente afirma com `pytest.raises(ReceitaInvalida)`.
    - Um teste com `rende` ausente afirma com `pytest.raises(ReceitaInvalida)`.
    - Um teste com componente sem `item` afirma com `assert` que o texto da excecao contem o
      valor de `produto` daquela receita.
    - Um teste com bloco sem `produto` afirma com `assert` que o texto da excecao cita a
      posicao do bloco.
    - Um teste com uma receita valida de dois componentes em tabela inline afirma com
      `assert` os quatro valores lidos (dois `item`, duas `quantidade`) e o `rende`.
    - `python -c "import inspect, l2scanner.config as c; s = inspect.getsource(c.ler_receitas); assert 'bool' in inspect.getsource(c)"` sai com codigo 0.
    - Os tres testes que leem o `config.toml` REAL (`tests/test_bosses.py`,
      `tests/test_mira_da_janela.py`, `tests/test_calibracao_generica.py`) seguem verdes —
      este plano nao toca aquele arquivo.
  </acceptance_criteria>

  <done>
    O `[[receita]]` tem leitor, e ele recusa o arranque com o nome da receita em cada um dos
    modos de erro que o usuario consegue escrever a mao.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `margem_de_craft` — a resolucao de nome que quebra em vez de adivinhar, e a staleness por componente</name>

  <read_first>
    - `l2scanner/mercado_catalogo.py:637-660` (`assinatura_da_chave`) — a `chave_da_serie` e
      o nome mais um separador mais a assinatura de digitos, e a assinatura e RECUPERADA da
      chave em vez de recalculada porque o `nome_exibido` e editavel pelo usuario.
    - `l2scanner/mercado_registro.py:113-115` — o comentario que diz por que ha DOIS campos,
      a chave que AGRUPA e o nome que se LE: o OCR faz o rotulo oscilar.
    - `l2scanner/mercado_catalogo.py` — a funcao de similaridade e o corte calibrado.
      **Leia para saber por que NAO usa-la aqui**, e escreva essa razao no fonte.
    - `l2scanner/mercado_analise.py` — `unitario`, `menor_pedido_visivel`,
      `mediana_dos_unitarios`, `ModeloDeMercado`, escritos pelos planos 04-02 e 04-03.
    - `config.toml:186-189` — "misturar `+3` e `+4` na mesma serie nao acrescenta ruido:
      destroi a serie".
    - `04-RESEARCH.md` secoes 4.1, 4.3, 4.4 e **4.5 inteira** (as cinco coisas que esta
      proposta nao resolve), mais a linha A9 do Assumptions Log.
  </read_first>

  <files>l2scanner/mercado_analise.py, tests/test_mercado_receitas.py</files>

  <behavior>
    - Teste: com produto e dois componentes todos resolvidos e com evidencia, a margem sai
      com o valor esperado, calculado sobre unitarios em `Fraction` e nunca em `float`.
    - Teste: um ingrediente sem observacao NENHUMA quebra a margem inteira, e o resultado
      carrega o NOME do que faltou.
    - Teste: um nome que casa com DUAS series quebra a margem, e o resultado LISTA as duas
      candidatas.
    - Teste: o casamento e exato com caixa normalizada e espacos colapsados — um nome com
      caixa diferente e espacos duplicados casa; um nome com prefixo de encanto NAO casa com
      o item base.
    - Teste: cada componente devolve a PROPRIA idade, e a mais velha e promovida ao
      resultado com o nome do componente junto.
    - Teste: o componente mais velho passa a ser marcado quando ultrapassa o limiar de
      staleness, e nao antes.
    - Teste da nomenclatura: nem o resultado nem as docstrings publicas de
      `margem_de_craft` contem qualquer item de `EXPRESSOES_PROIBIDAS_DA_MARGEM`, definida
      no topo de `tests/test_mercado_receitas.py` com exatamente quatro itens:
      `"lucro"`, `"preco de venda"`, `"preço de venda"` e `"lucro bruto"`, comparados em
      caixa baixa. Os dois lados da conta sao pedidos visiveis, e o scanner nao ve transacao.
  </behavior>

  <action>
Acrescente `margem_de_craft(receita, modelo, agora)` a `l2scanner/mercado_analise.py`
(puro: o carimbo entra por parametro).

**(a) A definicao, e ela e a unica defensavel com o que o CSV tem:** o menor pedido visivel
do PRODUTO multiplicado pelo quanto a receita rende, menos a soma, sobre os componentes,
do menor pedido visivel de cada um multiplicado pela quantidade que a receita pede. Tudo
em unitario-`Fraction` antes de multiplicar, e o arredondamento so na formatacao.

**Os dois lados sao menor pedido visivel, ou seja OFERTAS.** O numero resultante e a
diferenca entre o que estao PEDINDO pelo produto e o que estao PEDINDO pelos ingredientes.
Escreva isso na docstring com todas as letras, e escreva tambem que as expressoes que
prometem transacao sao proibidas aqui pela mesma razao que sao proibidas no resto da fase —
o teste da tupla proibida cobre esta funcao tambem.

**Por que "menor" e nao "mediana" como conta principal:** e assim que um humano compraria
os ingredientes e venderia o produto, pelo melhor pedido visivel; e porque a mediana exige
o piso de cinco por componente, o que multiplicaria a chance de a margem inteira cair. A
mediana entra como linha SECUNDARIA, nao como a conta principal.

**(b) A resolucao nome digitado -> `chave_da_serie`, que e o problema real desta task.** O
usuario escreve um nome; o CSV guarda a chave, que e o nome mais a assinatura de digitos.
Tres desfechos, cada um com comportamento NOMEADO:

- **exatamente uma serie** cujo `nome_exibido` casa: usa ela;
- **nenhuma serie** casa: a margem inteira NAO APARECE, com o nome do que faltou — decisao
  travada no `04-CONTEXT.md` ("Ingrediente sem observacao nenhuma quebra a margem
  inteira"). Calcular com um buraco daria numero plausivel e errado;
- **duas ou mais** casam: **quebra tambem, LISTANDO as candidatas.** Escolher a mais
  parecida produziria uma margem plausivel e errada, que e o pecado central deste projeto.

**O casamento e IGUALDADE EXATA sobre `nome_exibido`, com caixa normalizada e espacos
colapsados. Nao use similaridade fuzzy.** Escreva a razao no fonte: o corte calibrado foi
medido para agrupar duas leituras de OCR do MESMO pixel, e usa-lo aqui casaria um item
base com sua variante de encanto — series deliberadamente separadas, porque misturar
encantos destroi a serie. Quando o nome exato nao casa, a mensagem deve LISTAR os nomes
disponiveis mais parecidos para o usuario copiar: ajuda sem decidir por ele.

**(c) A staleness POR COMPONENTE.** Cada componente devolve a propria idade, derivada do
`agora` que entrou por parametro e da recencia do PRECO daquele componente
(`max(primeira_vez)` das ofertas dele), nunca do `ultima_vez` do catalogo. A idade mais
VELHA e promovida ao resultado da margem, com o nome do componente junto: e o numero que
decide se a margem vale alguma coisa, e enterra-lo no meio da lista seria esconde-lo.
Defina `HORAS_PARA_MARCAR_COMPONENTE_VELHO = 24` como constante nomeada, com a razao
escrita e a marca de que e ESCOLHA e nao medicao.

**(d) As cinco limitacoes, escritas na docstring do modulo ou da funcao, em voz alta:**
(1) o programa nao sabe se a oferta ainda existe — o CSV nao registra desaparecimento, a
dedup e por conteudo e sem tempo de saida, entao nenhuma margem deste projeto e acionavel
sem o usuario conferir na tela; (2) ela ignora comissao do World Exchange e chance de
falha do craft, entao e uma diferenca bruta; (3) ela nao lida com quantidade minima — se o
menor pedido de um componente e um lote de 100 e a receita precisa de 20, o usuario compra
100, e usar o unitario e a simplificacao certa mas AINDA E uma simplificacao; (4) ela
herda a fusao de series ainda aberta desde a Fase 2 (duas variantes de grade a 0,9375
podem estar na MESMA serie, e se uma delas for componente a margem sai errada sem o
programa ter como saber); (5) ela nao valida que a receita e real — o programa nao conhece
o crafting do jogo, e uma receita errada produz um numero perfeitamente formatado e
completamente falso. A unica defesa e que o usuario escreveu a receita.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_receitas.py tests/test_mercado_analise.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - Um teste com produto e dois componentes resolvidos afirma com `assert` o valor exato
      da margem, e afirma com `assert` que o tipo intermediario usado e `Fraction`.
    - Um teste com um componente sem nenhuma observacao afirma com `assert` que o resultado
      esta no estado quebrado e que o nome daquele componente aparece no motivo.
    - Um teste com duas series cujo `nome_exibido` normaliza para o mesmo texto afirma com
      `assert` que o resultado esta no estado ambiguo e que AS DUAS chaves candidatas
      aparecem listadas.
    - Um teste afirma com `assert` que um nome com caixa diferente e espacos duplicados
      resolve para a mesma serie, e que um nome com prefixo de encanto NAO resolve para a
      serie do item base.
    - Um teste afirma com `assert` que cada componente do resultado carrega a propria idade
      e que a promovida ao topo e a maior delas, junto do nome do componente.
    - `python -c "import l2scanner.mercado_analise as m; assert m.HORAS_PARA_MARCAR_COMPONENTE_VELHO == 24"` sai com codigo 0.
    - `tests/test_mercado_receitas.py` define no topo a tupla `EXPRESSOES_PROIBIDAS_DA_MARGEM`
      com os quatro itens listados no `<behavior>` desta task, e um teste afirma com `assert`,
      item por item, que nenhum deles aparece no texto do resultado nem na docstring publica
      de `margem_de_craft` (comparacao em caixa baixa dos dois lados), falhando com o nome da
      expressao encontrada.
    - `python -c "import inspect, l2scanner.mercado_analise as m; d = inspect.getsource(m.margem_de_craft); assert 'similaridade' in d"` sai com codigo 0 — a razao de NAO usar fuzzy esta escrita no fonte.
  </acceptance_criteria>

  <done>
    A margem sai quando tudo resolve, quebra com o nome do que falta quando nao resolve, e
    quebra listando as candidatas quando o nome e ambiguo — nunca adivinha.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A margem desenhada — uma linha por componente, a mais velha no topo, e a advertencia no cabecalho</name>

  <read_first>
    - `l2scanner/mercado_console.py` — as funcoes ja escritas pelos planos 04-01 e 04-03, e
      a disciplina de devolver texto em vez de imprimir.
    - `l2scanner/__main__.py:2186-2193` (`desenhar_status` saindo por intervalo e nao por
      tick) — o precedente da cadencia desta secao.
    - `l2scanner/console.py:97-119` (`moldurar`).
    - `04-RESEARCH.md` secao 4.4 (as tres decisoes do layout) e 4.5 (a limitacao numero 1,
      que vira o texto do cabecalho).
  </read_first>

  <files>l2scanner/mercado_console.py, l2scanner/mercado_modo.py, tests/test_mercado_receitas.py</files>

  <behavior>
    - Teste: sem nenhuma receita configurada, o texto da secao e vazio — a secao nao aparece,
      e nada de erro nem de aviso e emitido.
    - Teste: com uma receita completa, o texto traz uma linha para o produto e uma linha
      INDENTADA por componente, cada uma com o menor pedido visivel (total e quantidade), o
      `n` e a idade.
    - Teste: a idade sai em uma palavra por linha, e a mais velha aparece TAMBEM na linha da
      margem, com o nome do componente.
    - Teste: o componente mais velho ganha um marcador quando passa do limiar, e nao ganha
      quando esta abaixo dele.
    - Teste: com um ingrediente sem observacao, a secao mostra o motivo com o nome do que
      falta, e nenhum numero de margem e impresso.
    - Teste: com nome ambiguo, a secao lista as candidatas e nenhum numero de margem e
      impresso.
    - Teste: o cabecalho da secao contem a advertencia de que a oferta pode ja ter sido
      comprada, e ela aparece exatamente uma vez.
    - Teste: a secao nao e emitida no repintar de 1 Hz.
  </behavior>

  <action>
Acrescente a `l2scanner/mercado_console.py` a funcao que desenha a secao da margem,
recebendo as receitas, o modelo e o `agora`, e devolvendo TEXTO.

**(a) Sem receita, texto vazio.** A secao e opcional: sem `[[receita]]` ela simplesmente
nao aparece. Nao e erro e nao e aviso — decisao travada no `04-CONTEXT.md`.

**(b) O layout: uma linha por componente, indentada, com a idade como ULTIMA coluna.** A
idade sai em uma palavra por linha, nao num carimbo completo por componente — e o que
mantem a tabela legivel quando ha cinco ingredientes. A idade mais VELHA sobe para a linha
da margem, com o nome do componente junto. Marque o componente mais velho quando ele passa
do limiar nomeado da Task 2.

**(c) A linha do produto e as dos componentes trazem o menor pedido visivel com total E
quantidade lado a lado**, mais o `n` — a mesma disciplina da secao "vale quanto agora": um
total solto e sem significado, e um numero sem `n` e adivinhacao com cara de numero.

**(d) O cabecalho da secao carrega a advertencia, uma vez:** o programa nao sabe se a
oferta ainda existe. Um menor pedido visivel de vinte minutos atras pode ter sido comprado
ha dezenove — o CSV nao registra desaparecimento porque a dedup e por conteudo, sem tempo
de saida. Nenhuma margem deste projeto e acionavel sem o usuario conferir na tela, e o
console tem de dizer isso em vez de deixar o numero falar sozinho.

**(e) Quando a margem quebra, a secao mostra o MOTIVO em vez do numero:** o nome do
ingrediente que falta, ou a lista de candidatas quando o nome e ambiguo. Nenhum numero de
margem sai nesses dois casos.

**(f) A cadencia.** Esta secao NAO sai no repintar de 1 Hz: ela e cara de ler e nao muda a
cada segundo. Saia no arranque, e depois so quando uma das series envolvidas ganha
observacao nova — ou por intervalo, no precedente de `desenhar_status`. Ligue isso no laco
de `mercado_modo.py` com o menor acrescimo possivel: as receitas sao lidas UMA vez no
arranque, junto da watchlist.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_receitas.py tests/test_mercado_console.py tests/test_mercado_modo.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - Um teste sem receitas afirma com `assert` que o texto devolvido e vazio e que
      `caplog.records` nao ganhou nenhuma entrada de `WARNING` nem de `ERROR`.
    - Um teste com uma receita de dois componentes afirma com `assert` que ha exatamente
      tres linhas de item no texto (o produto e os dois componentes) e que cada uma contem
      um total, uma quantidade e um `n=`.
    - Um teste afirma com `assert` que o nome do componente mais velho aparece na linha da
      margem.
    - Um teste com o componente mais velho abaixo do limiar afirma com `assert` que o
      marcador NAO aparece; outro, acima do limiar, afirma que aparece.
    - Um teste com ingrediente ausente afirma com `assert` que o nome do que falta aparece e
      que nenhum valor de margem foi impresso.
    - Um teste afirma com `assert` que a advertencia sobre a oferta poder ja ter sido
      comprada aparece exatamente uma vez no texto.
    - Um teste roda varios ticks do laco e afirma com `assert` que a funcao da secao da
      margem foi chamada menos vezes que o numero de ticks.
  </acceptance_criteria>

  <done>
    A margem aparece com a staleness de cada componente visivel e a mais velha promovida,
    quebra com o motivo quando nao pode ser calculada, e diz uma vez que nao e acionavel sem
    o usuario conferir na tela.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` `[[receita]]` -> processo | texto que o usuario escreve a mao, sem validador externo |
| nome digitado -> `chave_da_serie` | fronteira de IDENTIDADE: e onde o usuario, sem querer, pode apontar para a serie errada |
| margem calculada -> decisao de compra | o numero sai formatado e parece acionavel, e nao e |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Tampering | `rende = true` ou `quantidade = true` virando `1` em silencio | medium | mitigate | `isinstance(valor, bool)` ANTES do teste numerico, nos dois campos, com teste dedicado para cada (Task 1) |
| T-04-18 | Tampering | nome de item ambiguo resolvido "na melhor" | high | mitigate | Casamento EXATO com caixa normalizada; ambiguidade QUEBRA listando as candidatas; proibido usar similaridade fuzzy, com a razao escrita no fonte e verificada por grep (Task 2b) |
| T-04-19 | Tampering | margem calculada com um ingrediente faltando | high | mitigate | Ingrediente sem observacao quebra a margem INTEIRA, com o nome do que falta; nenhum numero e impresso nesse caso (Tasks 2b, 3e) |
| T-04-20 | Repudiation | margem com um ingrediente de hoje e outro de uma semana atras | medium | mitigate | Staleness POR componente exigida pelo requisito; a mais velha promovida a linha da margem com o nome junto; marcador acima de limiar nomeado (Tasks 2c, 3b) |
| T-04-21 | Repudiation | numero formatado lido como acionavel | medium | mitigate | Advertencia unica no cabecalho de que a oferta pode ja ter sido comprada, mais as cinco limitacoes escritas na docstring (Tasks 2d, 3d) |
| T-04-22 | Tampering | nomenclatura que promete transacao ou ganho | high | mitigate | Tupla `EXPRESSOES_PROIBIDAS_DA_MARGEM` com assercao item por item sobre o texto do resultado e a docstring publica (Task 2, criterio da tupla) |
| T-04-SC | Tampering | instalacao de pacote | low | accept | Esta fase nao instala nada. Ver `04-RESEARCH.md` > Package Legitimacy Audit |
</threat_model>

<verification>
1. `python -m pytest tests/test_mercado_receitas.py tests/test_mercado_console.py tests/test_mercado_analise.py -x -q` verde.
2. `python -m pytest tests/ --ignore=tests/test_agenda.py -q` mantem o verde de referencia
   mais os testes novos. `KeyboardInterrupt` vindo de `tests/test_agenda.py` **nao e falha**.
3. `git diff --stat -- config.toml` VAZIO — o bloco comentado e a ULTIMA task da fase, no
   plano 04-05.
4. `git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py l2scanner/__main__.py`
   VAZIO.
5. `git status --porcelain calibration.json` VAZIO.
6. `git diff -- requirements.txt` VAZIO.

<human-check>
Com pelo menos uma sessao de `--mercado` ja gravada, o usuario descomenta um `[[receita]]`
de um craft que ele conhece de verdade e confere:
  - que a margem aparece, e que os numeros batem com o que ele ve no World Exchange;
  - que a staleness de cada componente e plausivel;
  - que um nome digitado errado quebra a margem com uma mensagem que o ajuda a corrigir, em
    vez de produzir um numero.
Este e o unico jeito de fechar a limitacao numero 5: **o programa nao conhece o crafting
do jogo, e uma receita errada produz um numero perfeitamente formatado e completamente
falso.** A unica defesa e que o usuario escreveu a receita.
</human-check>
</verification>

<success_criteria>
- `[[receita]]` tem leitor que recusa o arranque nomeando a receita em cada modo de erro.
- A margem sai com a staleness de cada componente e a mais velha promovida ao topo.
- Ingrediente ausente e nome ambiguo QUEBRAM a margem com o motivo, e nunca produzem numero.
- As cinco limitacoes e a advertencia de nao-acionabilidade estao escritas onde o usuario le.
</success_criteria>

## Artifacts this phase produces

| Artefato | Novo? | O que e |
|---|---|---|
| `ReceitaInvalida` e `ler_receitas` em `l2scanner/config.py` | novo | O leitor do `[[receita]]`, no molde de `_boss_de_dict`, com `isinstance(bool)` antes do teste numerico |
| `margem_de_craft` e `HORAS_PARA_MARCAR_COMPONENTE_VELHO` em `l2scanner/mercado_analise.py` | novo | A margem sobre pedidos visiveis, com resolucao de nome que quebra em vez de adivinhar e staleness por componente |
| A secao da margem em `l2scanner/mercado_console.py` | novo | O desenho de ANAL-04, com a mais velha promovida e a advertencia no cabecalho |
| `tests/test_mercado_receitas.py` | novo | Prende os treze modos de erro do `[[receita]]`, a resolucao de nome e a nomenclatura |

<output>
Create `.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-04-SUMMARY.md` when done
</output>
