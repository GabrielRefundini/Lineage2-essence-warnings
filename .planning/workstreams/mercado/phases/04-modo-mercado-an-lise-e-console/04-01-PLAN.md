---
phase: 04-modo-mercado-an-lise-e-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/mercado_modo.py
  - l2scanner/mercado_console.py
  - l2scanner/mercado_pagina.py
  - l2scanner/__main__.py
  - l2scanner/captura_janela.py
  - tests/test_mercado_modo.py
  - tests/test_mercado_firewall_de_fase.py
autonomous: true
requirements: [DETC-02, LEIT-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`python -m l2scanner --janela \"...\" --mercado` sobe como TERCEIRA invocacao, sem vigiar party e sem enviar alerta"
    - "Sem OCR ou sem calibracao de mercado o modo NAO SOBE: sai com codigo 2 e diz exatamente qual chave falta e como calibrar"
    - "Uma pagina aceita produz linha no `.mercado/observacoes.csv` E linha no `.mercado/catalogo-de-nomes.csv` na MESMA sessao"
    - "O console mostra ao vivo paginas lidas, paginas perdidas e o ultimo item reconhecido"
    - "O resumo final conta AS DUAS METADES (lidas e perdidas) com os motivos de descarte agregados por sessao"
    - "O resumo final imprime p50/p95/maximo do tempo de tick e quantos ticks estouraram o orcamento"
    - "`l2scanner/rastreador.py` e `l2scanner/visao.py` continuam sem uma linha de diff"
  artifacts:
    - l2scanner/mercado_modo.py
    - l2scanner/mercado_console.py
    - tests/test_mercado_modo.py
    - tests/test_mercado_firewall_de_fase.py
  key_links:
    - "`Catalogo.registrar()` chamado por linha aceita E `Catalogo.gravar()` no `finally` — sem os dois o catalogo de nomes fica vazio para sempre"
    - "`montar_catalogo_de_mercado` no trilho de `montar_registro_de_mercado`: `try` em volta do construtor INTEIRO, `mkdir` incluido"
    - "`pecas_de_calibracao_de_mercado_faltando` e a UNICA verdade sobre 'calibrado para mercado' — `_calibrado` passa a chamar a mesma funcao"
    - "`JanelaSource(minimum_update_interval=None)` por padrao: o caminho da party continua byte-identico"
---

<objective>
Ligar os TRES fios que as Fases 2 e 3 deixaram sem chamador, num modo `--mercado` que
roda de ponta a ponta e conta honestamente as duas metades do que viu.

Purpose: A Fase 2 entregou `LeitorDePagina` e `Catalogo` sem chamador de producao; a
Fase 3 entregou `RegistroDeObservacoes` e `montar_registro_de_mercado` sem chamador, de
proposito. Este plano e o processo que os chama. Sem ele nao ha dado, e sem dado os
planos 02 a 05 nao tem o que analisar.

Output: `l2scanner/mercado_modo.py` (o laco), `l2scanner/mercado_console.py` (o desenho),
uma flag `--mercado` de ~14 linhas no `__main__.py` disputado, e a prova executavel de
que o modo party nao mudou.

Requisitos cobertos: **DETC-02** (modo separado, terceira invocacao) e **LEIT-04**
(console ao vivo com as duas metades e resumo que conta as duas metades).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/STATE.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-CONTEXT.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-RESEARCH.md
@./.claude/CLAUDE.md
</context>

<restricoes_da_fase>
Valem para TODAS as tasks deste plano. Quebrar qualquer uma reprova a task.

1. **Nenhuma dependencia nova.** `requirements.txt` nao muda. `rich` NAO entra — a razao
   e doutrina de zero-install (o `vigiar-party.bat` roda `pip install -r requirements.txt`
   na primeira execucao), e NAO o FIRE-01: a banlist do FIRE-01 e so de sintese de input.
   Se o fonte repetir a justificativa errada do CONTEXT, corrija-a por escrito.
2. **NAO TOCAR** `l2scanner/rastreador.py` nem o gate de brilho da barra propria em
   `l2scanner/visao.py`. O portao da fase e o comando que REPROVA sozinho:
   `test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py)"` — a forma
   crua sai com codigo 0 tendo mudado os dois arquivos inteiros.
3. **Outro agente trabalha nos workstreams `tiat`/`identidade` NESTA MESMA ARVORE.** NAO
   tocar `respawn.py`, `bosses.py`, `agenda.py`, `sessao.py`, `tests/test_bosses.py`. O
   `l2scanner/__main__.py` e DISPUTADO: diff minimo, so o que a Task 1 lista.
4. **`calibration.json` e LIDO, NUNCA escrito** por este modo. Nunca comitar
   `calibration.json`. `recordings/` e somente leitura.
5. **`--mercado` nao pode ganhar um `--saida`.** A guarda `razao_para_recusar_a_saida` da
   ferramenta do censo existe para impedir replay de contaminar `.mercado/` com carimbo
   de agora; uma flag de destino aqui furaria essa protecao pelo outro lado.
6. **Nenhuma comprehension nao-afirmante em bloco de verificacao.** Use `assert` de
   verdade — ja ha seis ocorrencias desse defeito neste projeto.
7. **Sem travessao e sem acento em texto de erro** (doutrina da casa, cp1252).
8. **NAO rodar a varredura do censo nem o replay completo** — passam de 10 minutos e ja
   mataram dois agentes.
9. **Pytest roda no Python GLOBAL** (3.12.10), nao no `.venv`. Verde de referencia: 2819
   passed, 2 skipped sem `tests/test_agenda.py`. **FLAKE conhecido:**
   `tests/test_agenda.py:1141` levanta `KeyboardInterrupt` de proposito — **abortar nao e
   falhar**.
</restricoes_da_fase>

<tasks>

<task type="tracer">
  <name>Task 1: O caminho unico — `--mercado` sobe, le uma pagina, grava nos DOIS arquivos e encerra limpo</name>

  <read_first>
    - `tools/gerar_observacoes_do_censo.py` INTEIRO — e o laco desta fase ja escrito para
      PNG. Reusar: `Contagem` e `absorver` (:175-195), `gravar_as_paginas` (:197-221),
      `_novo_leitor` (:229-242), a sequencia de portoes de arranque (:365-413) e o
      relatorio das duas metades (:434-444).
    - `l2scanner/__main__.py:227-277` (`montar_gravador`) e `:468-544`
      (`montar_registro_de_mercado`) — o trilho literal da montagem que degrada.
    - `l2scanner/__main__.py:~2343` (a `add_argument` de `--so-agenda`) e `:~2421-2428`
      (o `if args.so_agenda:`) — o molde da flag; `:~2394-2400` (`--record-janela`) — o
      molde do `parser.error`; `:~2461-2467` (`--testar-manutencao`) — o ponto de saida
      DEPOIS da calibracao e DEPOIS da resolucao de `--janela AUTO`.
      **Os numeros sao aproximados de proposito: `__main__.py` esta sendo editado por
      outro agente nesta arvore agora.** Confira por `grep -n` do nome da flag, nunca
      pelo numero cru.
    - `l2scanner/__main__.py:2188-2199` (compensacao de deriva) e `:2205-2264` (o `finally`).
    - `l2scanner/__main__.py:1476-1652` (`laco_da_agenda`) — o precedente de laco separado.
    - `l2scanner/__main__.py:1905-1930` — como `mercado_geometria_da_captura` vira a `Regiao`.
    - `l2scanner/mercado_catalogo.py:408-440` (construtor sem `try`), `:542-573`
      (`registrar`), `:575-600` (`gravar` atomico), `:617-648` (`entradas`, que devolve
      SNAPSHOT desconectado).
    - `l2scanner/mercado_pagina.py:189-215` (o construtor do `LeitorDePagina`), `:300-332`
      (os contadores publicos e `ultima_leitura`), `:334-404` (`observar` e a ordem dos
      portoes), `:694-756` (`_calibrado` e as onze chaves).
    - `l2scanner/mercado_registro.py:91-131` (`ARQUIVO_DE_OBSERVACOES`, `COLUNAS`),
      `:648-729` (`registrar` e o que o `True`/`False` significa).
    - `l2scanner/frames.py:108-113` (a porta `FrameSource`) e `:71-106` (`Regiao`).
    - `tests/test_mercado_replay.py:1-110` — as fixturas VERSIONADAS de janela inteira, a
      proveniencia e a leitora de OCR REPRODUZIDO (`leituras_de_nome.json`). E o material
      com que esta task se verifica sem WinRT e sem `recordings/`.
    - `04-RESEARCH.md` secoes 2.1, 2.2, 2.3 e 2.5.
  </read_first>

  <files>l2scanner/mercado_modo.py, l2scanner/mercado_pagina.py, l2scanner/__main__.py, tests/test_mercado_modo.py</files>

  <action>
Uma fatia vertical que atravessa TODAS as camadas do modo, por UM caminho so: flag,
portoes de arranque, fiacao, tick, persistencia nos dois arquivos, desligamento. Nada de
segunda entrada, nada de ramo alternativo. As Tasks 2 e 3 expandem a partir daqui.

**(a) A verdade unica sobre "calibrado para mercado", em `l2scanner/mercado_pagina.py`.**
Extraia a lista de dentro de `_calibrado` para uma funcao de MODULO chamada
`pecas_de_calibracao_de_mercado_faltando(cal)`, que recebe a `Calibracao` e devolve a
lista de nomes de chave ausentes, em ordem. `_calibrado` passa a chamar essa funcao e
mantem intacto o LATCH `_falta_ja_avisada` e o texto do `log.warning`. A funcao carrega,
alem das onze chaves que `_calibrado` ja confere, as TRES que hoje so o arranque do
scanner confere: `mercado_ancoras`, `mercado_limiar_da_ancora` e
`mercado_geometria_da_captura`. Preserve na docstring a razao de cada uma das chaves que
ja tem razao escrita; para as tres novas escreva a razao (`mercado_ancoras` e
`mercado_limiar_da_ancora` porque sem elas nao ha `RastreioDoPainel`;
`mercado_geometria_da_captura` porque sem ela nao ha `Regiao` para pedir a janela
inteira). Este e refactor puro: `tests/test_mercado_pagina.py` e a rede.

**(b) `montar_catalogo_de_mercado(pasta=None)` em `l2scanner/__main__.py`**, colada logo
depois de `montar_registro_de_mercado` e no trilho IDENTICO ao dela: import adiado de
`l2scanner.mercado_catalogo`, `try` em volta do construtor INTEIRO (o `mkdir` do
`Catalogo.__init__` roda fora de qualquer `try` hoje e e o mesmo modo de falha que fez
`montar_registro_de_mercado` existir), `except OSError` estreito e nomeado, `log.error`
com DUAS mensagens sendo a segunda o que continua funcionando, `return None`, nunca
`raise`. Tipo de retorno NAO anotado, pelo mesmo motivo escrito em
`montar_registro_de_mercado`. A pasta padrao resolve para `PASTA_DO_MERCADO` em tempo de
CHAMADA, para o teste apontar `tmp_path` sem tocar a `.mercado/` do usuario.

**(c) `l2scanner/mercado_modo.py` (novo) com `laco_do_mercado`.** Assinatura:
`laco_do_mercado(args, cal, *, fonte=None, ler_texto=None, ler_texto_conferencia=None,
pasta=None, ticks_maximos=None)`. Os parametros nomeados sao injecao de dependencia no
precedente ja escrito de `montar_relogio(args, fonte=None)` e de `Catalogo(pasta)`:
producao nao passa nenhum. `ticks_maximos=None` significa laco infinito.

Ordem obrigatoria dentro da funcao:

1. **Portoes de arranque, que RECUSAM A SUBIR** (nunca "sobem degradado" — subir cego
   faria o usuario achar que esta coletando): `ocr.disponivel()` falso encerra com
   `log.error` citando `ocr.motivo_indisponivel()`; `pecas_de_calibracao_de_mercado_faltando(cal)`
   nao vazia encerra dizendo quais chaves faltam e mandando rodar
   `calibrar-mercado.bat` sobre um frame da grade de negociacao;
   `cal.mercado_grade.get("layout")` diferente de `"negociacao"` encerra dizendo qual
   layout esta gravado. Codigo de saida **2** nos tres — o `__main__` ja usa `2` para
   recusa de configuracao, e a numeracao nomeada da ferramenta do censo (`SAIDA_SEM_OCR`)
   NAO vem junto, para o produto nao ganhar duas convencoes.
2. **A linha de arranque que conta o orcamento**, no precedente literal de
   `montar_vigia_do_mercado`: quem le o log precisa saber o que o recurso custa antes do
   farm comecar. Diga o pior tick medido (~110 ms de 1000), que este e o TERCEIRO
   processo, e que a party nao le este modo e este modo nao le a party.
3. **A fiacao**, nesta ordem: `montar_registro_de_mercado(pasta)` (None encerra com 2 —
   aqui a feature E o produto, ao contrario do scanner de party);
   `montar_catalogo_de_mercado(pasta)` (None encerra com 2); `montar_relogio(args)`;
   `fonte` (quando nao injetada) e uma `JanelaSource` sobre `args.janela` com a `Regiao`
   de origem `0,0` e largura/altura vindas de `cal.mercado_geometria_da_captura`, com
   `relativa=True`; o `LeitorDePagina` recebe `RastreioDoPainel(ancoras_de_calibracao(...),
   float(cal.mercado_limiar_da_ancora))` e **`catalogo.entradas()`**.
4. **O tick**, dentro de `try` / `except KeyboardInterrupt` / `finally`. Cada volta:
   marca `time.monotonic()`, `fonte.capturar()`, `leitor.observar(frame.pixels)`. Quando
   `observar` devolve uma `PaginaAceita`, para CADA linha de `pagina.linhas`, nesta ordem:
   `catalogo.registrar(linha.chave_da_serie, linha.nome_exibido, agora)` e depois
   `registro.registrar(linha, agora)`, contando em uma `Contagem` copiada de
   `gerar_observacoes_do_censo.py` — `observacoes` quando devolve `True`, `duplicadas`
   quando devolve `False` e `registro.ligado`, `perdidas` quando devolve `False` e o
   registro desligou. `agora` vem de `relogio.agora()`, nunca do relogio do sistema.
   O `except Exception` com contador `erros_seguidos` e desistencia em 10 seguidos vem do
   laco principal (`__main__.py:2088-2098`): a captura falha parcialmente e o PNG nunca
   falhava.
5. **A cadencia**: `dormir = args.intervalo - (time.monotonic() - inicio)` e
   `time.sleep(dormir)` so quando positivo. **Nunca `time.sleep(args.intervalo)` puro** —
   com 110 ms de trabalho o tick viraria 1,11 s e o console mentiria sobre a cadencia.
6. **O `finally`**: `fonte.fechar()`, `catalogo.gravar()` (reescrita ATOMICA, e por isso
   so aqui e nunca por tick) e o resumo. Grave o catalogo tambem a cada
   `PAGINAS_ENTRE_GRAVACOES_DO_CATALOGO = 20` paginas aceitas, com a razao escrita na
   constante: um encerramento por `taskkill` nao pode custar as series da sessao inteira.
   Escreva na constante que 20 e ESCOLHA e nao medicao.

**(d) O diff minimo no `l2scanner/__main__.py` (arquivo DISPUTADO — nada alem disto).**
Uma `parser.add_argument("--mercado", action="store_true", ...)` no fim da lista de
flags, no molde do `--so-agenda`, com help dizendo que roda so a leitura do World
Exchange como terceira invocacao e que nao vigia party nem envia alerta. Um
`parser.error` logo apos `parse_args()` recusando `--mercado` sem `--janela`, no molde do
`--record-janela`: o painel e procurado na janela INTEIRA e so o caminho da janela a
expoe. E o `if args.mercado:` com import adiado de `.mercado_modo` e `return
laco_do_mercado(args, cal)`, colocado **depois** da carga de `Calibracao.carregar` e
**depois** da resolucao de `--janela AUTO`, junto do `--testar-manutencao` — ao contrario
do `--so-agenda`, este modo OLHA para a tela, e sair antes duplicaria a verdade sobre
como a janela e escolhida.

**(e) O console minimo desta fatia**: uma linha por pagina aceita via `log.info` com
paginas lidas, paginas perdidas e o `nome_exibido` da ultima linha reconhecida, e um
resumo de tres linhas no `finally`. A Task 3 substitui isto pela superficie completa do
LEIT-04 — o que nasce aqui e o caminho, nao o desenho.
  </action>

  <reversibility rating="costly">
    O modo passa a ESCREVER em `.mercado/`, que e dado acumulado sem poda e sem desfazer
    (`Catalogo`: "estatistica de item e para sempre"). Uma fiacao errada polui o catalogo
    do usuario de forma que so apagar o arquivo desfaz, perdendo historico junto. Por isso
    a verificacao desta task roda inteira sobre `tmp_path` e fixturas versionadas, e a
    primeira execucao real e portao humano de campo.
  </reversibility>

  <verify>
    <automated>python -m pytest tests/test_mercado_modo.py tests/test_mercado_pagina.py -x -q</automated>
    <automated>python -m pytest tests/test_mercado_replay.py tests/test_mercado_27x.py tests/test_mercado_registro.py tests/test_mercado_catalogo.py -x -q</automated>
    <automated>test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py)" || { echo "REPROVADO: rastreador.py ou visao.py foram tocados"; exit 1; }</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_mercado_modo.py` existe e contem um teste PONTA A PONTA que: monta uma
      fonte falsa que implementa a porta `FrameSource` e devolve, em sequencia,
      `janela_negociacao_f005.png` e `janela_negociacao_f005_repetida.png` das fixturas
      versionadas; injeta as leitoras de OCR REPRODUZIDO de `tests/test_mercado_replay.py`
      (`leituras_de_nome.json`); usa `calibracao_de_fixture.json`; passa `pasta=tmp_path` e
      `ticks_maximos` pequeno; chama `laco_do_mercado`; e depois **afirma com `assert`**
      que `tmp_path/observacoes.csv` tem pelo menos uma linha de dado ALEM do cabecalho
      **E** que `tmp_path/catalogo-de-nomes.csv` tem pelo menos uma linha de dado alem do
      cabecalho. As duas afirmacoes na mesma execucao — e o terceiro fio provado.
    - Um teste afirma que `laco_do_mercado` devolve `2` quando
      `pecas_de_calibracao_de_mercado_faltando` devolve lista nao vazia, e que a mensagem
      de erro capturada por `caplog` cita ao menos um nome de chave ausente.
    - Um teste afirma que `pecas_de_calibracao_de_mercado_faltando` devolve os nomes de
      todas as quatorze chaves quando recebe uma calibracao vazia, e lista vazia quando
      recebe `calibracao_de_fixture.json`.
    - Um teste afirma que `montar_catalogo_de_mercado` devolve `None` e NAO levanta quando
      a pasta de destino esta ocupada por um ARQUIVO de mesmo nome, e que `caplog`
      registra `ERROR` com duas mensagens.
    - `test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py)" || { echo "REPROVADO: rastreador.py ou visao.py foram tocados"; exit 1; }` sai com codigo 0. O comando REPROVA sozinho; um `git diff --stat` cru sai com codigo 0 tendo mudado tudo.
    - `grep -n "saida" l2scanner/mercado_modo.py` nao encontra nenhuma `add_argument` nem
      parametro de destino de arquivo vindo da linha de comando.
    - `test "$(git diff --numstat -- l2scanner/__main__.py | cut -f1)" -le 70` sai com
      codigo 0 — TETO TOTAL de linhas acrescentadas no arquivo disputado, contando
      `montar_catalogo_de_mercado` junto da flag, do `parser.error` e do `if` de saida.
      **O teto e total de proposito:** `--numstat` e agregado e nao sabe separar a funcao
      nova das ~14 linhas da flag, entao um criterio que fingisse essa separacao nao seria
      verificavel pelo comando que cita. Se o executor estourar 70, ele para e reporta em
      vez de continuar cortando — o numero existe para pegar um diff que cresceu, nao para
      forcar compressao de docstring.
  </acceptance_criteria>

  <done>
    `--mercado` sobe, recusa a subir sem calibracao dizendo o que falta, le uma pagina das
    fixturas versionadas, grava nos DOIS arquivos de `.mercado/` e encerra gravando o
    catalogo de forma atomica. O caminho unico esta commitado e verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A alavanca de DETC-02 e os tripwires que provam que a party nao mudou</name>

  <read_first>
    - `l2scanner/captura_janela.py:207-290` — `JanelaSource.__init__` e a construcao de
      `WindowsCapture(cursor_capture=False, draw_border=False, monitor_index=None,
      window_name=...)`, alem do `on_frame_arrived` e do comentario de ~38 fps.
    - `l2scanner/mercado_pagina.py:88-93` (`JANELAS_IGUAIS_PARA_CONGELAR = 3`) e
      `:408-455` (o detector de congelamento pela janela INTEIRA, e o memcpy de ~7,2 MB).
    - `tests/test_mercado_27x.py:245-268` — os dois tripwires de arquitetura ja escritos,
      que este trabalho espelha na direcao oposta.
    - `tests/test_firewall_escopo.py:66-78` — a banlist do FIRE-01, para nao repetir a
      justificativa errada do CONTEXT sobre o `rich`.
    - `04-RESEARCH.md` secoes 1.2, 1.3 e 1.4, e a linha A1 do Assumptions Log.
  </read_first>

  <files>l2scanner/captura_janela.py, l2scanner/mercado_modo.py, tests/test_mercado_firewall_de_fase.py</files>

  <behavior>
    - Teste: `JanelaSource` construida sem o parametro novo passa `minimum_update_interval`
      como `None` para `WindowsCapture` — o caminho da party fica byte-identico ao de hoje.
    - Teste: `JanelaSource(..., minimum_update_interval=250)` repassa `250`.
    - Teste: a constante do modulo de mercado e estritamente MENOR que o intervalo padrao
      do laco em milissegundos, com folga de pelo menos 4x — a margem que impede o falso
      congelamento.
    - Teste: `inspect.getsource(l2scanner.mercado_modo)` nao cita `rastreador`, `visao`,
      `sessao` nem `presenca`.
    - Teste: o modulo de mercado, importado, nao traz `l2scanner.rastreador` para
      `sys.modules` quando este ainda nao estava la.
    - Teste: rodar `laco_do_mercado` com `Rastreador` substituido por um objeto que levanta
      ao ser chamado NAO levanta — o modo nunca constroi um rastreador.
  </behavior>

  <action>
**(a) `minimum_update_interval` opcional em `l2scanner/captura_janela.py`.** Acrescente
`minimum_update_interval: int | None = None` ao fim da assinatura de
`JanelaSource.__init__` e repasse-o para o construtor de `WindowsCapture`. O padrao `None`
e o contrato: o caminho da party continua identico ao de hoje, e ha teste prendendo isso.
Escreva na docstring do parametro o que ele compra e o que ele custa: a WGC entrega ~38
fps e cada frame desta janela custa um memcpy de ~7,2 MB, entao uma sessao copia da ordem
de 273 MB/s — numero DERIVADO, nao medido, e escreva que ele e derivado.

**(b) `MS_ENTRE_FRAMES_DO_MERCADO = 250` em `l2scanner/mercado_modo.py`**, usada so na
construcao da `JanelaSource` do modo mercado. A constante carrega, por extenso, as duas
razoes: a ~9,5x menos copia que ela compra, e o motivo do valor NAO ser 1000 — com
atualizacoes a cada ~1000 ms e leitura a cada ~1000 ms a deriva de fase entrega o mesmo
buffer tres vezes seguidas, `JANELAS_IGUAIS_PARA_CONGELAR` dispara e o console anuncia
captura congelada com o jogo vivo. Escreva que 250 e ESCOLHA com margem de 4x, e NAO
medicao — um numero que nao foi medido precisa dizer que nao foi.

**(c) `tests/test_mercado_firewall_de_fase.py` (novo)** — o tripwire simetrico ao que
`tests/test_mercado_27x.py:254-268` ja faz do lado do rastreador. Ele prende o
acoplamento nas DUAS direcoes: o rastreador nao conhece o mercado (ja provado la) e o
mercado nao conhece o rastreador (provado aqui). Inclua os tres testes de importacao e o
teste de que o modo nunca constroi `Rastreador`, e um teste que afirma que
`requirements.txt` nao ganhou linha nova nesta fase. Docstring do modulo explicando por
que ele existe: contencao entre tres processos e propriedade do SISTEMA e nao do
programa, entao "nenhuma competicao de recursos" nao e afirmavel por teste
automatizado — o que E afirmavel e que os dois lados nao se conhecem, e e isso que este
arquivo prende. O resto do criterio 1 fecha em portao humano de campo.
  </action>

  <reversibility rating="reversible">
    Parametro opcional com padrao `None` e uma constante nomeada. Voltar atras e apagar
    duas linhas; nenhum dado do usuario muda de forma.
  </reversibility>

  <verify>
    <automated>python -m pytest tests/test_mercado_firewall_de_fase.py tests/test_mercado_27x.py tests/test_firewall_escopo.py -x -q</automated>
    <automated>python -m pytest tests/test_rastreador.py tests/test_party_estavel.py -x -q</automated>
    <automated>test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import inspect, l2scanner.captura_janela as c; s = inspect.signature(c.JanelaSource.__init__); assert s.parameters['minimum_update_interval'].default is None"` sai com codigo 0.
    - Um teste afirma, sobre o fonte de `l2scanner/captura_janela.py`, que
      `minimum_update_interval` aparece na chamada de `WindowsCapture`.
    - Um teste afirma com `assert` que `MS_ENTRE_FRAMES_DO_MERCADO * 4 <= 1000`.
    - Um teste afirma com `assert` que nenhuma das quatro palavras `rastreador`, `visao`,
      `sessao`, `presenca` aparece no fonte de `l2scanner.mercado_modo` obtido por
      `inspect.getsource`.
    - Um teste substitui `l2scanner.rastreador.Rastreador` por um objeto que levanta
      `AssertionError` ao ser chamado, roda `laco_do_mercado` com fonte falsa e
      `ticks_maximos` pequeno, e afirma que a execucao termina sem levantar.
    - `test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }` sai com codigo 0 — o comando reprova sozinho.
    - `tests/test_rastreador.py` e `tests/test_party_estavel.py` seguem verdes.
  </acceptance_criteria>

  <done>
    O modo mercado tem a unica alavanca real de DETC-02 ligada so nele, o party continua
    byte-identico, e o acoplamento esta preso nas duas direcoes por teste.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LEIT-04 inteiro — o console ao vivo, o resumo das duas metades e o orcamento auto-medido</name>

  <read_first>
    - `l2scanner/mercado_pagina.py:300-332` — os SETE contadores publicos
      (`paginas_lidas`, `paginas_perdidas`, `paginas_vazias`, `paginas_de_outro_layout`,
      `ticks_com_painel_aberto`, `frames_congelados`, `linhas_descartadas`), mais
      `ultimo_motivo_de_perda` e a propriedade `ultima_leitura`, escrita nas palavras do
      autor "exatamente para o console da Fase 4".
    - `l2scanner/mercado_leitura.py:1120-1128` — as cinco constantes de motivo, cujos
      valores REAIS sao `oclusao`, `numero`, `cruzamento`, `faixa-cinzenta` e
      `discordancia-entre-escalas`. O CONTEXT chama uma delas de gramatica; o valor da
      constante e outro, e o console usa a CONSTANTE.
    - `l2scanner/mercado_leitura.py:1165-1180` (`Descarte`) — descarte, leitura e linha
      vazia sao tres estados distintos.
    - `l2scanner/console.py:97-119` (`moldurar`) e `:122+` (`destacar`) — a moldura que ja
      existe e ja e sem-cor fora de tty.
    - `l2scanner/__main__.py:155-186` (`configurar_log` e o `reconfigure(encoding="utf-8")`)
      e `:617-724` (`desenhar_status`, o precedente de bloco de status que sai por
      intervalo e nao por tick).
    - `l2scanner/__main__.py:2229-2246` — o resumo do gravador imprimindo contador E disco
      lado a lado, o precedente de "o resumo carrega o numero que julga a sessao".
    - `04-RESEARCH.md` secoes 1.4(a), 2.4 e os Pitfalls 5, 6 e 8.
  </read_first>

  <files>l2scanner/mercado_console.py, l2scanner/mercado_modo.py, tests/test_mercado_modo.py</files>

  <behavior>
    - Teste: a linha ao vivo, montada a partir de um leitor com contadores conhecidos,
      contem o numero de paginas lidas E o de paginas perdidas E o texto do ultimo item.
    - Teste: com zero paginas lidas e tres perdidas, a linha ainda mostra as duas metades —
      nunca so a metade boa.
    - Teste: o resumo final contem os sete contadores, e os motivos agregados aparecem com
      a contagem de cada um.
    - Teste: os motivos agregados somam por SESSAO, e nao so os da ultima pagina: dois
      ticks com o mesmo motivo produzem contagem 2.
    - Teste: o resumo contem p50, p95, maximo do tempo de tick e a contagem de ticks que
      estouraram o orcamento.
    - Teste: com o painel fechado por N ticks seguidos, o log ganha no maximo uma linha de
      transicao — nao N linhas.
  </behavior>

  <action>
**(a) `l2scanner/mercado_console.py` (novo), texto puro, sem dependencia nova.** Funcoes
que recebem o leitor e a contagem e DEVOLVEM texto — nunca imprimem. Devolver texto e o
que torna o desenho afirmavel por teste sem capturar stdout, e e a disciplina que
`console.moldurar` ja segue. Duas funcoes nesta task: `linha_ao_vivo(leitor, contagem,
ultimo_item)` e `resumo_da_sessao(leitor, contagem, motivos, orcamento)`. Use
`console.moldurar` para o cabecalho em vez de repetir uma regua de `=`.

**(b) A linha ao vivo (LEIT-04, metade um).** Repinta na cadencia de captura, 1 Hz, a
mesma do resto do scanner. Mostra paginas lidas, paginas perdidas e o ultimo item
reconhecido — o `nome_exibido` da ultima linha da ultima pagina aceita. **Nunca so a
metade boa:** o censo mediu 151 lidas contra 189 perdidas, e esconder a segunda faria o
usuario confiar numa cobertura que nao existe. NAO mostre `residuo_do_cruzamento` no
repintar: a guarda de cruzamento esta DESLIGADA por medicao, o residuo e observacao e nao
veredito, e ele ja esta no CSV para o usuario olhar no Sheets.

**(c) O anti-spam.** Painel fechado e o estado NORMAL e majoritario de um farm real; uma
linha por tick seriam 3.600 linhas por hora. Use LATCH nas duas transicoes — painel
abriu, painel fechou — no precedente ja usado tres vezes em `mercado_pagina.py`
(`_layout_ja_recusado`, `_congelamento_ja_avisado`, `_falta_ja_avisada`). O resto e
repintado, nao acumulado.

**(d) O resumo final (LEIT-04, metade dois).** No `finally`, com os SETE contadores do
leitor, mais `observacoes` gravadas, `duplicadas` descartadas e, quando houver,
`perdidas` por registro desligado — os tres da `Contagem` sao fatos DIFERENTES e nao se
somam. Agregue os motivos de descarte por SESSAO num `collections.Counter` proprio,
alimentado a cada tick a partir de `leitor.ultima_leitura.motivos`: o campo `motivos` da
pagina carrega so a ultima leitura, nao um acumulado, e somar o acumulado e trabalho do
laco. Imprima cada motivo com sua contagem, usando as constantes de
`mercado_leitura.py`, nunca a palavra do CONTEXT.

**(e) A auto-medicao do orcamento (a metade de DETC-02 que o programa pode afirmar sobre
si mesmo).** O laco ja calcula `agora - inicio` para compensar a deriva; acumule a lista
desses tempos e no resumo imprima p50, p95, maximo e quantos ticks passaram do
`args.intervalo`. Use `statistics.quantiles` ou indexacao sobre a lista ordenada — sem
dependencia nova. Escreva ao lado, no proprio texto do resumo, que esses numeros dizem o
que ESTE processo custou e que contencao entre tres processos e propriedade do sistema,
que so o portao humano de campo fecha.

**(f) `configurar_log` ANTES de qualquer log ou print** do modo, como
`gerar_observacoes_do_censo.py:400` ja faz: sem isso o console do Windows engasga em
acento (cp1252) e as mensagens de erro da montagem nao teriam manipulador nenhum.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_modo.py -x -q</automated>
    <automated>python -m pytest tests/test_mercado_pagina.py tests/test_mercado_leitura.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import l2scanner.mercado_console as c; t = c.linha_ao_vivo.__doc__; assert t"` sai com codigo 0.
    - Um teste monta um leitor falso com `paginas_lidas=7` e `paginas_perdidas=3`, chama
      `linha_ao_vivo` e afirma com `assert` que os dois numeros aparecem no texto devolvido.
    - Um teste chama `resumo_da_sessao` e afirma com `assert` que cada um dos sete nomes de
      contador do `LeitorDePagina` tem seu valor presente no texto devolvido.
    - Um teste alimenta dois ticks com o mesmo motivo de descarte e afirma com `assert` que
      o texto do resumo traz aquele motivo com contagem `2` — provando que a agregacao e
      por sessao e nao por pagina.
    - Um teste afirma com `assert` que o texto do resumo contem os rotulos de p50, p95,
      maximo e de ticks que estouraram o orcamento.
    - Um teste roda o laco com painel fechado por pelo menos cinco ticks e afirma com
      `assert` que `caplog.records` ganhou no maximo uma entrada de transicao de painel.
    - `grep -n "residuo" l2scanner/mercado_console.py` nao encontra ocorrencia na funcao
      `linha_ao_vivo`.
    - `python -m pytest tests/test_mercado_modo.py -q` termina verde e o teste ponta a
      ponta da Task 1 continua passando.
  </acceptance_criteria>

  <done>
    O console mostra ao vivo as duas metades e o ultimo item; o resumo conta as duas
    metades com os motivos agregados por sessao; e o usuario le, no fim da sessao, o que
    este processo custou de tempo de tick.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> processo | arquivo em disco, escrito por OUTRA ferramenta e editavel a mao; entra como geometria, limiar e molde |
| pixels da tela -> processo | conteudo nao confiavel: o jogo, uma tooltip, um navegador por cima |
| processo -> `.mercado/` | dado ACUMULADO do usuario, sem poda e sem desfazer |
| processo `--mercado` -> processos de party | fronteira de RECURSO (CPU, GPU, sessoes WGC), nao de dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `calibration.json` lido por `laco_do_mercado` | medium | mitigate | `Calibracao.carregar` + `pecas_de_calibracao_de_mercado_faltando` recusam a SUBIR com codigo 2 e nome da chave; nada de valor de fabrica implicito (Task 1a, 1c) |
| T-04-02 | Denial of Service | terceira sessao WGC sobre o mesmo HWND das duas de party | medium | mitigate | `minimum_update_interval=250` so no mercado (~9,5x menos copia), padrao `None` no party, auto-medicao p50/p95/estouros no resumo, e portao humano de campo (Tasks 2 e 3) |
| T-04-03 | Tampering | destino de escrita do registro | medium | mitigate | `--mercado` NAO ganha `--saida`; a pasta padrao resolve para `PASTA_DO_MERCADO` em tempo de chamada e so o teste injeta `tmp_path` — replay nao alcanca `.mercado/` por esta porta (Task 1c, criterio de grep) |
| T-04-04 | Tampering | `Catalogo.gravar()` reescreve o arquivo INTEIRO | medium | mitigate | Reuso do `gravar()` atomico existente (`.tmp-<pid>` + `os.replace`); nunca `open("w")` proprio; gravacao no `finally` e a cada 20 paginas, nunca por tick (Task 1c) |
| T-04-05 | Elevation of Privilege | acoplamento acidental do mercado ao detector de morte | high | mitigate | Tripwires de importacao nas DUAS direcoes + teste de que o modo nunca constroi `Rastreador` + `test -z "$(git diff --stat -- ...)"` sobre `rastreador.py` e `visao.py`, que reprova sozinho (Task 2c) |
| T-04-SC | Tampering | instalacao de pacote | low | accept | Esta fase nao instala nada; `requirements.txt` nao muda e ha teste prendendo isso (Task 2c). Auditoria de legitimidade de pacote nao se aplica — ver `04-RESEARCH.md` > Package Legitimacy Audit |
</threat_model>

<verification>
1. `python -m pytest tests/test_mercado_modo.py tests/test_mercado_firewall_de_fase.py -x -q` verde.
2. `python -m pytest tests/ --ignore=tests/test_agenda.py -q` mantem o verde de referencia
   (2819 passed, 2 skipped) mais os testes novos. Um `KeyboardInterrupt` vindo de
   `tests/test_agenda.py` **nao e falha** — por isso o arquivo esta ignorado nesta linha.
3. `test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py)" || { echo "REPROVADO: rastreador.py ou visao.py foram tocados"; exit 1; }` sai com codigo 0.
4. `test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }` sai com codigo 0.
5. `test -z "$(git status --porcelain calibration.json)" || { echo "REPROVADO: calibration.json foi escrito"; exit 1; }` sai com codigo 0 — este modo le, nunca escreve.

   **Os tres sao comandos que REPROVAM sozinhos.** Um `git diff --stat` ou um `git status`
   cru sai com codigo 0 tendo mudado o arquivo inteiro: a verificacao dependeria de alguem
   ler a saida e se autorreprovar, que nao e verificacao.
6. `test "$(git diff --numstat -- l2scanner/__main__.py | cut -f1)" -le 70` sai com codigo
   0, e a leitura do diff confirma que o que entrou e so isto: a flag, o `parser.error`, o
   `if` de saida e `montar_catalogo_de_mercado`. O teto e mecanico; **o "nada mais" e
   conferencia por leitura**, porque nenhum comando decide isso.

<human-check>
**Portao de campo de DETC-02 — o unico jeito honesto de fechar o criterio 1.** Contencao
entre tres processos e propriedade do SISTEMA, nao do programa: nenhum teste automatizado
a alcanca. O usuario roda as duas instancias de party mais `--mercado` por 10 minutos com
o mercado aberto e confere, com criterio objetivo:
  - no Gerenciador de Tarefas, o total de CPU dos tres `python.exe` fica dentro do que ele
    aceita para farmar (a linha de arranque do modo diz o orcamento esperado: ~110 ms de
    1000 no pior tick);
  - o `scanner.log` das DUAS instancias de party nao ganha nenhuma linha de falha de
    captura nem de imagem congelada que ja nao aparecesse antes;
  - nenhuma borda amarela aparece em volta da janela do jogo (tres sessoes WGC
    simultaneas e extrapolacao de duas — assumido A3 no `04-RESEARCH.md`).

**As duas conferencias humanas abertas da Fase 2 fecham aqui**, e so ao vivo:
  - o OCR real rodando DENTRO do tick (nao no replay);
  - o congelamento provocado — cobrir ou pausar a janela e ver o aviso de captura
    congelada aparecer, e sumir quando a janela volta.
</human-check>
</verification>

<success_criteria>
- `--mercado` roda como terceira invocacao, recusa a subir sem OCR ou sem calibracao de
  mercado dizendo o que falta, e nunca sobe cego.
- Uma sessao produz linha nos DOIS arquivos de `.mercado/` — os tres fios ligados.
- O console mostra ao vivo as duas metades e o ultimo item; o resumo conta as duas metades
  com motivos agregados e com o orcamento de tick auto-medido.
- O modo party continua byte-identico, provado por tripwire nas duas direcoes e por um
  comando que REPROVA sozinho se `rastreador.py` ou `visao.py` tiverem qualquer diff.
</success_criteria>

## Artifacts this phase produces

| Artefato | Novo? | O que e |
|---|---|---|
| `l2scanner/mercado_modo.py` | novo | O laco de producao do modo `--mercado`: portoes de arranque, fiacao dos tres fios, tick com cadencia compensada, desligamento com gravacao atomica |
| `l2scanner/mercado_console.py` | novo | O desenho em texto puro: linha ao vivo e resumo da sessao. Devolve texto, nunca imprime |
| `montar_catalogo_de_mercado` em `l2scanner/__main__.py` | novo | A terceira montagem no trilho da casa — o `Catalogo` deixa de ter `mkdir` fora de `try` |
| `pecas_de_calibracao_de_mercado_faltando` em `l2scanner/mercado_pagina.py` | novo (extracao) | A verdade UNICA sobre "calibrado para mercado": as onze chaves do leitor mais as tres do arranque |
| `minimum_update_interval` em `JanelaSource` | novo (parametro opcional) | A unica alavanca real de DETC-02; padrao `None` mantem o party identico |
| `tests/test_mercado_modo.py` | novo | O ponta a ponta sobre fixturas versionadas, sem WinRT e sem `recordings/` |
| `tests/test_mercado_firewall_de_fase.py` | novo | O tripwire simetrico: o mercado nao conhece o rastreador |

<output>
Create `.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-01-SUMMARY.md` when done
</output>
