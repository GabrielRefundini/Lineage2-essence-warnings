---
phase: 04-modo-mercado-an-lise-e-console
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/mercado_analise.py
  - l2scanner/mercado_registro.py
  - tests/test_mercado_analise.py
autonomous: true
requirements: [ANAL-01, ANAL-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O menor pedido visivel de uma serie e a oferta de menor UNITARIO, e ela carrega o carimbo DELA, nao o da serie"
    - "A mediana e `median_low` sobre unitarios em `Fraction` — devolve sempre um valor que existiu de verdade na tela"
    - "Abaixo do piso de evidencia a analise devolve o que FALTA, e nunca um numero"
    - "A tendencia roda sobre o ORDINAL das ofertas distintas, nunca sobre o carimbo, e carrega o tamanho da janela"
    - "A recencia do PRECO e `max(primeira_vez)` do `observacoes.csv`, e nunca o `ultima_vez` do catalogo"
    - "O modulo de analise e PURO: sem disco, sem relogio, sem `print` — a suite roda no Python global sem WinRT"
  artifacts:
    - l2scanner/mercado_analise.py
    - tests/test_mercado_analise.py
  key_links:
    - "A leitura do CSV reusa `_conferir_o_terminador`, `_conferir_o_cabecalho`, `chave_dos_campos` e `residuo_dos_campos` de `mercado_registro.py` — uma verdade so sobre o que o arquivo e"
    - "`Fraction(total_em_centesimos, quantidade)` e o unico comparavel entre ofertas de quantidades diferentes"
    - "Os pisos de evidencia sao constantes NOMEADAS com a razao escrita e a marca de que sao ESCOLHA, nao medicao"
---

<objective>
A estatistica honesta desta fase, num modulo PURO que a suite inteira roda no Python
global sem WinRT e sem tocar disco.

Purpose: O CSV da Fase 3 nao e serie temporal — uma linha e uma OFERTA distinta com o
carimbo da PRIMEIRA vez que foi vista. Toda a nomenclatura e toda a aritmetica desta fase
tem de respeitar isso, ou o console repete o pecado que a expressao "menor pedido
visivel" foi criada para evitar. Separar a metade pura da metade que desenha e o que
permite prender cada regra por teste, sem montar frame nenhum.

Output: `l2scanner/mercado_analise.py` (novo, puro) e uma funcao de leitura de linhas
parseadas em `l2scanner/mercado_registro.py` que reusa o portao de contrato existente.

Requisitos cobertos: **ANAL-01** (menor pedido visivel e mediana com n e recencia) e
**ANAL-03** (tendencia com o tamanho da janela explicito). O ANAL-02 e o desenho de todos
eles ficam no plano 04-03, que consome este modulo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-CONTEXT.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-RESEARCH.md
@./.claude/CLAUDE.md
</context>

<restricoes_da_fase>
Valem para TODAS as tasks deste plano.

1. **Nenhuma dependencia nova.** `statistics`, `fractions`, `csv`, `datetime` e
   `dataclasses` sao stdlib e cobrem a analise inteira.
2. **`l2scanner/mercado_analise.py` e PURO**: nao importa `os`, nao abre arquivo, nao
   chama relogio do sistema e nao imprime. O carimbo entra por PARAMETRO, na disciplina
   que `mercado_registro.campos_da_observacao` ja segue.
3. **NAO tocar** `rastreador.py`, `visao.py`, `respawn.py`, `bosses.py`, `agenda.py`,
   `sessao.py`, `tests/test_bosses.py`, `l2scanner/__main__.py`, `config.toml`.
4. **A Fase 4 nao recalibra e nao reescreve o CSV.** Leitura e leitura: nenhuma funcao
   deste plano abre arquivo em modo de escrita.
5. **Nao invente numero.** Piso escolhido e DECLARADO como escolhido, no proprio fonte.
6. **Nenhuma comprehension nao-afirmante em bloco de verificacao** — `assert` de verdade.
7. **Sem travessao e sem acento em texto de erro.**
8. Pytest no **Python GLOBAL**. **FLAKE conhecido:** `tests/test_agenda.py:1141` levanta
   `KeyboardInterrupt` de proposito — **abortar nao e falhar**.
</restricoes_da_fase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `observacoes_do_arquivo` — as linhas parseadas, pelo portao de contrato que ja existe</name>

  <read_first>
    - `l2scanner/mercado_registro.py:91-131` — `ARQUIVO_DE_OBSERVACOES` e `COLUNAS`, com
      as tres ausencias explicadas (sem unitario, sem gravacao/frame, sem indice).
    - `l2scanner/mercado_registro.py:193-241` (`chave_dos_campos`, as duas redes por linha
      juntas) e `:242-280` (`residuo_dos_campos`).
    - `l2scanner/mercado_registro.py:413-459` (`carregar`), `:460-526`
      (`_conferir_o_terminador` — o achado central da Fase 3: cinco truncagens byte a byte
      deixam o arquivo sem quebra final e DUAS delas produzem seis campos parseaveis, com
      `80` virando `8`), `:527-563` (`_conferir_o_cabecalho`) e `:564-636`
      (`_montar_o_indice`, que valida tudo e depois DESCARTA os campos).
    - `l2scanner/mercado_registro.py:134-147` (`ContratoDoArquivoQuebrado` e por que ela
      desliga a feature ALTO em vez de escrever desalinhado).
    - `tests/test_mercado_registro.py` — o formato dos testes de contrato, para os novos
      caberem ao lado sem inventar convencao.
    - `04-RESEARCH.md` secoes 3.1, 3.6 e a linha de Don't Hand-Roll sobre nao construir um
      parser CSV novo.
  </read_first>

  <files>l2scanner/mercado_registro.py, tests/test_mercado_analise.py</files>

  <behavior>
    - Teste: um arquivo valido com tres linhas devolve tres registros parseados, com
      `total_em_centesimos` e `quantidade` como `int` e `primeira_vez` como `datetime`.
    - Teste: um arquivo cujo ultimo byte nao e quebra de linha levanta
      `ContratoDoArquivoQuebrado` — o mesmo desfecho de `carregar`, e nao um parser novo
      que aceita a truncagem.
    - Teste: um arquivo com cabecalho divergente levanta `ContratoDoArquivoQuebrado`.
    - Teste: uma linha com contagem de campos errada e DESCARTADA com `warning` que nomeia
      o numero da linha, e as demais linhas continuam sendo devolvidas.
    - Teste: `residuo_do_cruzamento` vazio volta como `None` e `"0"` volta como `0` — os
      dois sao fatos diferentes e nao podem colapsar.
    - Teste: arquivo ausente devolve lista vazia, sem levantar.
  </behavior>

  <action>
Acrescente ao FIM da metade publica de `l2scanner/mercado_registro.py` uma dataclass
`ObservacaoLida` (frozen) com os campos `chave_da_serie`, `nome_exibido`, `primeira_vez`
(`datetime`), `total_em_centesimos` (`int`), `quantidade` (`int`) e
`residuo_do_cruzamento` (`int | None`), e uma funcao `observacoes_do_arquivo(caminho)` que
devolve `list[ObservacaoLida]`.

**NAO construa um parser CSV novo.** Reuse as pecas que a Fase 3 ja mediu e escreveu:
o portao do terminador ANTES da contagem de campos, o portao do cabecalho, e as duas
redes por linha de `chave_dos_campos` e `residuo_dos_campos`. Se a forma atual dessas
pecas for de metodo de instancia acoplado ao `RegistroDeObservacoes`, extraia o miolo
para funcoes de modulo e faca os metodos existentes chamarem as mesmas funcoes — refactor
puro, com `tests/test_mercado_registro.py` como rede. O criterio e "uma verdade so sobre
o que o arquivo e": duas implementacoes divergentes de leitura do mesmo arquivo
reintroduziriam exatamente a truncagem parseavel que a Fase 3 gastou um plano inteiro
para pegar.

O parse do carimbo usa `datetime.fromisoformat` sobre o campo `primeira_vez`, coerente
com o `agora.isoformat()` que `campos_da_observacao` escreve — `datetime` ingenuo em hora
local, sem fuso, como o resto do projeto.

`observacoes_do_arquivo` NUNCA abre o arquivo para escrita e nunca cria a pasta. Arquivo
ausente devolve lista vazia sem levantar: a `.mercado/` nasce vazia e a analise tem de
dizer "sem evidencia", nao explodir.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_analise.py tests/test_mercado_registro.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from l2scanner.mercado_registro import observacoes_do_arquivo, ObservacaoLida; import pathlib; assert observacoes_do_arquivo(pathlib.Path('nao-existe.csv')) == []"` sai com codigo 0.
    - Um teste escreve em `tmp_path` um CSV valido de tres linhas e afirma com `assert` que
      `observacoes_do_arquivo` devolve tres itens, e que `type(itens[0].total_em_centesimos)
      is int` e `type(itens[0].primeira_vez) is datetime`.
    - Um teste trunca o ultimo byte de quebra de linha de um CSV valido e afirma com
      `pytest.raises(ContratoDoArquivoQuebrado)`.
    - Um teste troca uma coluna do cabecalho e afirma com `pytest.raises(ContratoDoArquivoQuebrado)`.
    - Um teste com residuo vazio numa linha e `"0"` noutra afirma com `assert` que a
      primeira devolve `None` e a segunda devolve `0`.
    - `tests/test_mercado_registro.py` segue verde sem edicao de expectativa — a extracao e
      refactor puro.
  </acceptance_criteria>

  <done>
    O `observacoes.csv` pode ser lido em registros tipados, pelo mesmo portao de contrato
    que a Fase 3 mediu, sem uma segunda implementacao de leitura no projeto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O unitario exato, o menor pedido visivel, a mediana e as duas recencias</name>

  <read_first>
    - `l2scanner/mercado_registro.py:100-131` — as tres ausencias do CSV, em especial o
      motivo de NAO haver coluna de unitario: o que o jogo exibe e derivacao ARREDONDADA
      (`40,00` por 48 unidades vira `0,83`, e `0,83 x 48 = 39,84`, um numero que nunca
      existiu).
    - `l2scanner/mercado_registro.py:149-166` — a chave de dedup e
      `(chave_da_serie, total_em_centesimos, quantidade)`, SEM tempo, de proposito.
    - `l2scanner/calibrar_mercado.py:58, 1691, 1709` — o precedente da casa para
      `median_low`.
    - `l2scanner/mercado_catalogo.py:383-405` — o `ultima_vez` do CATALOGO, que e a outra
      recencia e nao pode ser confundida com esta.
    - `l2scanner/relogio.py:146-163` — `datetime` ingenuo em hora local, e a degradacao
      para o relogio do Windows com WARNING quando nao ha ancora.
    - `l2scanner/mercado_pagina.py:88-93` — `JANELAS_IGUAIS_PARA_CONGELAR = 3`, o modelo de
      como este projeto escreve uma constante que e escolha.
    - `04-RESEARCH.md` secoes 3.1, 3.2, 3.3 e 3.5, mais o Pitfall 4 e o Pitfall 7.
  </read_first>

  <files>l2scanner/mercado_analise.py, tests/test_mercado_analise.py</files>

  <behavior>
    - Teste: `unitario(4000, 48)` devolve uma `Fraction`, e nao um `float`.
    - Teste: duas ofertas do mesmo item com totais 6200/48 e 4500/100 ordenam pelo
      UNITARIO, e o menor e o de menor unitario — nao o de menor total.
    - Teste: o resultado do menor pedido visivel carrega `total_em_centesimos`,
      `quantidade` e o `primeira_vez` DAQUELA oferta — nao o `max(primeira_vez)` da serie.
    - Teste: com quatro unitarios distintos, a mediana devolve um valor que esta na lista
      de entrada (`median_low`), e nao a media dos dois do meio.
    - Teste: com `n` abaixo do piso da mediana, o resultado diz que falta evidencia e
      informa quantas observacoes seriam necessarias — e nao devolve numero.
    - Teste: com `n = 1`, o menor pedido visivel EXISTE e vem rotulado com `n=1`.
    - Teste: a recencia do preco de uma serie e o `max(primeira_vez)` das linhas dela.
  </behavior>

  <action>
Crie `l2scanner/mercado_analise.py`, PURO. Docstring de modulo abrindo com o fato que mais
importa desta fase, por extenso: **uma linha do CSV nao e "o preco as 14:32"; e "esta
oferta especifica — este item, este total, esta quantidade — foi vista pela PRIMEIRA vez
as 14:32"**. Um anuncio que fica no quadro por uma semana produz UMA linha; duas ofertas
identicas de vendedores diferentes produzem UMA linha; uma oferta que some e volta igual
produz UMA linha. Portanto NAO existe serie temporal de preco — existe uma sequencia de
ofertas distintas ordenada pela primeira vez que cada uma apareceu. Esta fase NARRA essa
limitacao; ela nao a conserta (a serie temporal por dia esta registrada como ideia adiada
no `04-CONTEXT.md`).

**(a) `unitario(total_em_centesimos, quantidade) -> Fraction`.** Comparar totais entre
ofertas de quantidades diferentes e sem sentido: um lote de 100 custa mais que um de 1. O
unico comparavel e o unitario, e ele e `Fraction(total, quantidade)` — exato, ordenavel,
entra em `median_low` sem perder um bit. **Nunca `float`**: divisao em ponto flutuante
reintroduz erro exatamente onde o parsing por molde de digito o evitou. O arredondamento
acontece SO na formatacao, e o numero exibido carrega a marca de derivado. Guardar o
unitario continua proibido; deriva-lo na hora de exibir e outra coisa.

**(b) `menor_pedido_visivel(observacoes)`** devolve a oferta de MENOR unitario, como um
resultado que carrega junto o `total_em_centesimos`, a `quantidade`, o `primeira_vez`
DAQUELA oferta e o `n` da serie. Um total solto e sem significado: quem exibe precisa dos
dois numeros lado a lado. **O carimbo e o da oferta, nunca o da serie** — um minimo de
terca-feira ao lado da recencia de hoje e a mentira plausivel que este projeto inteiro
combate.

**(c) `mediana_dos_unitarios(observacoes)`** usa `statistics.median_low`, que ja e
precedente da casa. `statistics.median` de `n` par inventa meio centavo que nunca existiu
na tela — o mesmo pecado do unitario arredondado que a Fase 3 recusou. `median_low`
devolve sempre um valor OBSERVADO.

**(d) Os pisos de evidencia, como constantes NOMEADAS no modulo, com a razao por extenso
e a marca de que sao ESCOLHA e nao medicao.** Escreva na docstring de cada uma que
nenhum piso desse tipo foi medido neste projeto e que o `observacoes.csv` real ainda nao
existe nesta arvore, e que os numeros medidos que existem (151 paginas lidas, 189
perdidas, 39 series, piso de 7 posicoes comparadas) sao sobre a LEITURA e nao servem de
substituto. Os valores: `N_MINIMO_PARA_MENOR = 1` (um minimo com `n=1` e um fato
observado, nao uma estimativa — a honestidade esta no rotulo, e o rotulo `n=1` e
obrigatorio); `N_MINIMO_PARA_MEDIANA = 5` (com 5 pontos o ponto de ruptura e 2: duas
leituras aberrantes nao movem a mediana para fora do miolo; com `n=2` a mediana e a media
dos dois, literalmente o numero que engana). Escreva tambem, no fonte, por que esses
pisos NAO moram no `calibration.json` apesar da doutrina de "nada de constante magica":
aquele arquivo e escrito pela ferramenta de calibracao e lido, nunca escrito, por este
modo, e estes numeros nao sao calibracao de pixel, sao julgamento de produto. Esta tensao
com a doutrina fica registrada de proposito.

**(e) O resultado abaixo do piso diz o que FALTA, nunca um numero.** Modele isso como
estado explicito no dataclass devolvido — um campo que diz quantas observacoes existem e
quantas faltariam — e nao como `None` mudo, para quem desenha nao ter de adivinhar.

**(f) `recencia_do_preco(observacoes)` = `max(primeira_vez)`.** Escreva na docstring, ao
lado, a OUTRA recencia com que ela nao pode ser confundida: o `ultima_vez` do
`catalogo-de-nomes.csv` diz quando o ITEM foi visto pela ultima vez em qualquer preco, e
pode ser de agora mesmo sobre um preco de tres dias atras. Sao dois fatos diferentes com
nomes parecidos, e trocar um pelo outro e mentir com cara de numero.
  </action>

  <reversibility rating="reversible">
    Pisos e formulas em constantes nomeadas num modulo puro. Mudar `N_MINIMO_PARA_MEDIANA`
    e uma linha, e nenhum dado gravado do usuario muda de forma.
  </reversibility>

  <verify>
    <automated>python -m pytest tests/test_mercado_analise.py -x -q</automated>
    <automated>python -c "import l2scanner.mercado_analise as m, inspect; f = inspect.getsource(m); assert 'import os' not in f and 'open(' not in f and 'print(' not in f, 'o modulo de analise precisa ser PURO'"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from fractions import Fraction; from l2scanner.mercado_analise import unitario; assert isinstance(unitario(4000, 48), Fraction)"` sai com codigo 0.
    - Um teste com ofertas de `(6200, 48)` e `(4500, 100)` afirma com `assert` que a
      escolhida e a de menor unitario, e que o resultado carrega `quantidade` junto do
      total.
    - Um teste afirma com `assert` que o `primeira_vez` devolvido pelo menor pedido
      visivel e o carimbo daquela oferta, e que ele DIFERE do `max(primeira_vez)` da serie
      quando a oferta mais barata nao e a mais nova.
    - Um teste com quatro unitarios afirma com `assert` que a mediana devolvida esta contida
      na lista de unitarios de entrada.
    - Um teste com `n=4` afirma com `assert` que o resultado da mediana esta no estado de
      evidencia insuficiente e informa `5` como o piso.
    - `python -c "import l2scanner.mercado_analise as m; assert m.N_MINIMO_PARA_MEDIANA == 5 and m.N_MINIMO_PARA_MENOR == 1"` sai com codigo 0.
    - `python -c "import l2scanner.mercado_analise as m; assert 'escolha' in m.__doc__.lower() or 'escolhid' in m.__doc__.lower()"` sai com codigo 0 — o modulo declara por escrito que os pisos sao escolha.
    - O modulo nao importa `os`, nao chama `open(` e nao chama `print(`.
  </acceptance_criteria>

  <done>
    O comparavel entre ofertas e exato, o menor pedido visivel carrega o carimbo dele, a
    mediana devolve valor observado, e abaixo do piso a analise diz o que falta em vez de
    inventar numero.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A tendencia sobre o ORDINAL, com o tamanho da janela sempre junto (ANAL-03)</name>

  <read_first>
    - `tools/gerar_observacoes_do_censo.py:197-221` — `gravar_as_paginas` chama
      `relogio.agora()` POR LINHA. E por isso que as dez linhas de uma pagina distam
      microssegundos e que o carimbo nao serve de eixo `x`.
    - `l2scanner/mercado_analise.py` — o que a Task 2 acabou de escrever.
    - `04-RESEARCH.md` secao 3.4 (as tres `StatisticsError` exercitadas e a razao do
      ordinal) e o Pitfall 3.
    - `.planning/workstreams/mercado/REQUIREMENTS.md:133` — ANAL-03 pede regressao stdlib,
      explicita sobre o tamanho da janela de dados que a sustenta.
  </read_first>

  <files>l2scanner/mercado_analise.py, tests/test_mercado_analise.py</files>

  <behavior>
    - Teste: dez ofertas cujos carimbos distam microssegundos, mas cujos unitarios caem de
      forma monotona, produzem uma variacao NEGATIVA e de magnitude plausivel — abaixo de
      100 por cento em valor absoluto.
    - Teste: o mesmo conjunto, se o eixo `x` fosse o carimbo, produziria magnitude absurda;
      o teste prende o ordinal comparando o resultado com o intervalo aceitavel, nao com um
      numero escolhido a mao.
    - Teste: com `n` abaixo do piso da tendencia, o resultado diz que falta evidencia e
      informa o piso — nao devolve inclinacao.
    - Teste: uma serie de unitarios todos iguais devolve variacao zero, e nao levanta.
    - Teste: o resultado carrega o `n` da janela, e o texto que o descreve usa a palavra
      que designa ofertas distintas — nunca a palavra que designa observacoes ao longo do
      tempo.
  </behavior>

  <action>
Acrescente `tendencia(observacoes)` a `l2scanner/mercado_analise.py`, usando
`statistics.linear_regression` — ANAL-03 pede regressao stdlib literalmente, e minimos
quadrados a mao seria reconstruir o que ja existe.

**O eixo `x` e o ORDINAL, e isso e o coracao desta task.** `x` e `1..n` sobre as ofertas
distintas da serie ordenadas por `primeira_vez`; `y` e o unitario convertido para `float`
so no momento da chamada. O carimbo NAO pode ser o `x`: as dez linhas de uma mesma pagina
tem carimbos separados por microssegundos, e uma regressao sobre `x` quase-constante
devolve inclinacao absurda **sem levantar `StatisticsError`**, porque tecnicamente `x`
varia. Numero plausivel e errado e o modo de falha que este projeto inteiro combate.
Ordinais sao distintos por construcao, entao o modo de falha "x is constant" fica
impossivel. Escreva essa razao inteira na docstring da funcao.

**Reporte a variacao percentual sobre a janela INTEIRA**, e nao a inclinacao crua:
`slope` multiplicado por `(n - 1)` e dividido pelo `intercept`. Uma inclinacao de tantos
centesimos por oferta nao significa nada para quem le. Trate `intercept` igual a zero como
caso sem tendencia reportavel, em vez de deixar uma divisao por zero escapar.

**`N_MINIMO_PARA_TENDENCIA = 8`**, constante nomeada com a razao escrita e a marca de que
e ESCOLHA e nao medicao: uma reta sobre 3 pontos tem a mesma cara de uma sobre 300, que e
exatamente o que o ANAL-03 existe para impedir; 8 e o menor numero em que a frase que sai
nao parece piada. Abaixo do piso, o resultado diz o que falta e informa o piso — mesmo
padrao de estado explicito da Task 2.

**O tamanho da janela viaja JUNTO do resultado, sempre.** O dataclass devolvido carrega o
`n`, e a funcao que o descreve em texto (que o plano 04-03 vai chamar) recebe o `n` de
volta obrigatoriamente. A palavra que qualifica esse `n` designa **ofertas distintas**, e
essa escolha e obrigatoria: ela e o que impede o usuario de ler a reta como uma variacao
ao longo de horas. Escreva a razao na docstring.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_analise.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import l2scanner.mercado_analise as m; assert m.N_MINIMO_PARA_TENDENCIA == 8"` sai com codigo 0.
    - Um teste monta dez ofertas com carimbos distantes microssegundos e unitarios em queda
      monotona, e afirma com `assert` que a variacao devolvida e negativa e que seu valor
      absoluto e menor que 100.
    - Um teste com sete ofertas afirma com `assert` que o resultado esta no estado de
      evidencia insuficiente e informa `8` como piso.
    - Um teste com unitarios todos iguais afirma com `assert` que a variacao e `0` e que
      nada foi levantado.
    - Um teste afirma com `assert` que o `n` da janela viaja no resultado e e igual ao
      numero de ofertas passadas.
    - `python -c "import inspect, l2scanner.mercado_analise as m; assert 'ordinal' in inspect.getsource(m.tendencia).lower()"` sai com codigo 0.
  </acceptance_criteria>

  <done>
    A tendencia roda sobre o ordinal das ofertas distintas, reporta variacao percentual
    sobre a janela inteira, carrega o `n` junto e se recusa a falar abaixo de oito ofertas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mercado/observacoes.csv` -> processo | arquivo que o usuario EDITA A MAO no Sheets e reimporta; entrada nao confiavel por definicao |
| numeros do CSV -> texto exibido | fronteira de INTERPRETACAO: e onde um numero plausivel e errado nasce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `observacoes_do_arquivo` sobre CSV truncado a mao | high | mitigate | Reuso obrigatorio do portao do terminador ANTES da contagem de campos, mais o portao do cabecalho e as duas redes por linha; proibido escrever parser novo. Duas truncagens medidas produzem seis campos parseaveis com `80` virando `8` (Task 1) |
| T-04-07 | Tampering | unitario derivado em ponto flutuante | medium | mitigate | `Fraction(total, quantidade)` como unico comparavel; arredondamento so na formatacao; teste afirma o tipo devolvido (Task 2a) |
| T-04-08 | Tampering | mediana de `n` par inventando meio centavo | medium | mitigate | `statistics.median_low`, com teste afirmando que o valor devolvido esta na lista de entrada (Task 2c) |
| T-04-09 | Tampering | regressao sobre carimbos quase-identicos devolvendo inclinacao absurda sem levantar | high | mitigate | Eixo `x` ordinal por construcao, com teste que prende a magnitude abaixo de 100 por cento (Task 3) |
| T-04-10 | Repudiation | estatistica sem `n` e sem recencia | medium | mitigate | `n` e carimbo viajam DENTRO do dataclass devolvido; abaixo do piso o resultado diz o que falta em vez de devolver numero (Tasks 2d, 2e, 3) |
| T-04-SC | Tampering | instalacao de pacote | low | accept | Esta fase nao instala nada; toda a analise e stdlib. Ver `04-RESEARCH.md` > Package Legitimacy Audit |
</threat_model>

<verification>
1. `python -m pytest tests/test_mercado_analise.py tests/test_mercado_registro.py -x -q` verde.
2. `python -m pytest tests/ --ignore=tests/test_agenda.py -q` mantem o verde de referencia
   mais os testes novos. `KeyboardInterrupt` vindo de `tests/test_agenda.py` **nao e falha**.
3. `l2scanner/mercado_analise.py` nao importa `os`, nao chama `open(` nem `print(` —
   comando de verificacao na Task 2.
4. `git status --porcelain calibration.json config.toml` VAZIO — este plano nao toca
   nenhum dos dois.
5. `git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py l2scanner/__main__.py`
   VAZIO — este plano nao toca o disputado nem os proibidos.

<human-check>
Os pisos `1`, `5` e `8` sao ESCOLHAS declaradas, nao medicoes: nenhum piso de evidencia
foi medido neste projeto e o `.mercado/observacoes.csv` real ainda nao existe nesta
arvore. Quando o usuario rodar `--mercado` por uma sessao de verdade, ele confere se a
mediana aparece para os itens que ele quer ver ou se o console cala demais, e ajusta —
cada piso e uma linha. A medicao que resolveria isso e "quantas observacoes DISTINTAS por
serie o material real produz", e ela nao foi feita nesta sessao de proposito (a varredura
do censo passa de 10 minutos).
</human-check>
</verification>

<success_criteria>
- O `observacoes.csv` e lido em registros tipados pelo portao de contrato existente, sem
  um segundo parser no projeto.
- O comparavel entre ofertas e o unitario exato em `Fraction`, e a mediana devolve sempre
  um valor observado.
- Abaixo do piso a analise devolve o que falta, com o piso nomeado, e nunca um numero.
- A tendencia roda sobre o ordinal e carrega o tamanho da janela junto do resultado.
- O modulo e puro e a suite roda no Python global sem WinRT.
</success_criteria>

## Artifacts this phase produces

| Artefato | Novo? | O que e |
|---|---|---|
| `l2scanner/mercado_analise.py` | novo | A estatistica honesta, PURA: unitario em `Fraction`, menor pedido visivel com carimbo proprio, mediana `median_low`, tendencia sobre ordinal, pisos de evidencia declarados como escolha |
| `ObservacaoLida` e `observacoes_do_arquivo` em `l2scanner/mercado_registro.py` | novo | A leitura tipada do CSV, pelo mesmo portao de contrato que a Fase 3 mediu |
| `tests/test_mercado_analise.py` | novo | Prende cada regra da analise sem montar frame nenhum |

<output>
Create `.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-02-SUMMARY.md` when done
</output>
