---
phase: 04-modo-mercado-an-lise-e-console
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - l2scanner/config.py
  - l2scanner/mercado_analise.py
  - l2scanner/mercado_modo.py
  - l2scanner/mercado_console.py
  - tests/test_mercado_console.py
  - tests/test_mercado_modo.py
autonomous: true
requirements: [ANAL-01, ANAL-02, ANAL-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O console responde \"vale quanto agora?\" com menor pedido visivel e mediana, cada um com n e recencia"
    - "Sem watchlist configurada o console responde para as series com MAIS EVIDENCIA — o usuario nao precisa configurar nada para ver valor"
    - "Com watchlist, os itens dela vem PRIMEIRO e marcados, e o resto continua visivel abaixo"
    - "Uma linha lida agora, abaixo da mediana historica, aparece destacada NA HORA"
    - "A mediana que destaca e a de ANTES deste tick — o item nunca se compara consigo mesmo"
    - "A tendencia sai com o tamanho da janela junto, em ofertas distintas"
    - "O texto do console nunca usa a expressao de transacao proibida — ha teste prendendo isso"
  artifacts:
    - tests/test_mercado_console.py
    - "`ModeloDeMercado` em l2scanner/mercado_analise.py"
    - "`ler_watchlist_do_mercado` em l2scanner/config.py"
  key_links:
    - "A ordem do tick: comparar contra o modelo COMO ELE ESTA, depois registrar, depois acrescentar ao modelo"
    - "O CSV e lido UMA vez no arranque e mantido em memoria; reler a 1 Hz abriria corrida com o usuario editando no Sheets"
    - "`ler_watchlist_do_mercado` e funcao NOVA em `config.py` — importar `calibrar_mercado.ler_watchlist` arrastaria `tornar_consciente_de_dpi()` e o `cv2` de GUI no import"
---

<objective>
Ligar a analise do plano 04-02 ao laco do plano 04-01 e desenhar a resposta que o usuario
abre o programa para ver: "vale quanto agora?".

Purpose: A estatistica sem desenho nao responde a ninguem, e o desenho sem a ordem certa
do tick mente. Este plano faz as tres coisas que so existem na juncao: carregar o modelo
uma vez, comparar cada linha nova contra o modelo COMO ELE ESTA antes de grava-la
(ANAL-02), e escrever o resultado com o `n`, a recencia e o tamanho da janela sempre
colados no numero.

Output: `ModeloDeMercado` no modulo puro, `ler_watchlist_do_mercado` em `config.py`, a
secao "vale quanto agora" em `mercado_console.py`, e o teste que prende a expressao
proibida.

Requisitos cobertos: **ANAL-01**, **ANAL-02**, **ANAL-03**.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/mercado/ROADMAP.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-CONTEXT.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-RESEARCH.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-01-SUMMARY.md
@.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-02-SUMMARY.md
@./.claude/CLAUDE.md
</context>

<restricoes_da_fase>
Valem para TODAS as tasks deste plano.

1. **Nenhuma dependencia nova.** `rich` NAO entra — doutrina de zero-install.
2. **NAO tocar** `rastreador.py`, `visao.py`, `respawn.py`, `bosses.py`, `agenda.py`,
   `sessao.py`, `tests/test_bosses.py`, `l2scanner/__main__.py`, `config.toml`.
3. **`config.py` e vizinho de arquivos disputados.** A funcao nova vai no **FIM** do
   modulo, sem tocar nada existente.
4. **A Fase 4 nao recalibra e nao reescreve o CSV.** Nenhuma funcao deste plano abre
   arquivo em modo de escrita.
5. **NAO mostrar `residuo_do_cruzamento` no repintar de 1 Hz.** A guarda de cruzamento
   esta DESLIGADA por medicao: o residuo e observacao, nao veredito, e ele ja esta no CSV
   para o usuario olhar no Sheets. Mostrar um numero que o proprio projeto declarou
   nao-decidivel e convidar a interpretacao errada.
6. **Nenhuma comprehension nao-afirmante em bloco de verificacao** — `assert` de verdade.
7. **Sem travessao e sem acento em texto de erro.**
8. Pytest no **Python GLOBAL**. **FLAKE conhecido:** `tests/test_agenda.py:1141` levanta
   `KeyboardInterrupt` de proposito — **abortar nao e falhar**.
</restricoes_da_fase>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A watchlist como FILTRO DE DESTAQUE, e o topo por evidencia quando ela nao existe</name>

  <read_first>
    - `l2scanner/config.py:296-323` (`ler_bosses`) — arquivo ausente nao e erro, secao
      ausente tambem nao, TOML quebrado E erro de arranque. E o molde desta funcao.
    - `l2scanner/config.py` FIM DO MODULO (ultimas ~30 linhas) — a recusa de tipo quando
      uma chave de texto vem como lista, e o formato da mensagem que cita o exemplo certo.
    - `l2scanner/calibrar_mercado.py:49-51` — o `tornar_consciente_de_dpi()` NO IMPORT. E a
      razao de `calibrar_mercado.ler_watchlist` NAO servir aqui: importa-la arrastaria DPI
      e `cv2` de GUI para dentro do laco de producao.
    - `config.toml:180-204` — a secao `[mercado] watchlist` COMENTADA, e a razao escrita de
      cada variante de encanto ser uma serie independente ("misturar `+3` e `+4` na mesma
      serie nao acrescenta ruido: destroi a serie").
    - `04-CONTEXT.md` > "A watchlist no criterio 3 — a contradicao herdada".
    - `04-RESEARCH.md` secao "Don't Hand-Roll", linha da watchlist.
  </read_first>

  <files>l2scanner/config.py, l2scanner/mercado_analise.py, tests/test_mercado_console.py</files>

  <behavior>
    - Teste: `config.toml` ausente devolve lista vazia, sem levantar.
    - Teste: secao `[mercado]` presente sem a chave devolve lista vazia, sem levantar.
    - Teste: a chave como TEXTO em vez de lista levanta erro de arranque nomeando o arquivo
      e mostrando o formato certo.
    - Teste: um item que nao e texto dentro da lista levanta erro de arranque nomeando a
      posicao.
    - Teste: com watchlist vazia, a ordenacao devolve as series com MAIOR contagem de
      observacoes primeiro.
    - Teste: com watchlist preenchida, as series dela vem primeiro e vem MARCADAS, e as
      demais continuam presentes na lista devolvida abaixo delas.
    - Teste: uma serie da watchlist que ainda nao tem observacao nenhuma nao derruba a
      ordenacao.
  </behavior>

  <action>
**(a) `ler_watchlist_do_mercado(caminho=None)` no FIM de `l2scanner/config.py`**, no molde
literal de `ler_bosses`: arquivo ausente devolve lista vazia e nao e erro; secao ou chave
ausente devolve lista vazia e nao e erro; TOML quebrado E erro de arranque, com o nome do
arquivo na mensagem. Recuse com mensagem nomeada os dois tipos errados que o usuario
consegue escrever: a chave como texto solto em vez de lista, e um item nao-texto dentro da
lista. As mensagens mostram o formato certo, no padrao ja usado no fim do modulo.

**NAO importe `calibrar_mercado.ler_watchlist`.** Aquele modulo chama
`tornar_consciente_de_dpi()` no proprio import e arrasta o `cv2` de GUI: trazer isso para
dentro do laco de producao seria pagar DPI e janela por uma leitura de TOML.

**(b) `ordenar_para_o_console(modelo, watchlist)` em `l2scanner/mercado_analise.py`** (puro,
sem disco). Ele implementa a decisao travada do `04-CONTEXT.md`, e essa decisao contraria
a LETRA do criterio 3 do ROADMAP de proposito — escreva isso na docstring, porque a
divergencia precisa ser visivel para quem verificar a fase:

- **A watchlist volta como FILTRO DE DESTAQUE, opcional, nunca como porta de entrada.** A
  Fase 2 tirou a watchlist da entrada em 2026-08-29: o nome vem por OCR e TUDO que aparece
  e registrado. O `[mercado] watchlist` do `config.toml` continua comentado e o usuario
  nunca o preencheu.
- **Sem watchlist, devolve as series com MAIS EVIDENCIA primeiro** — maior contagem de
  observacoes distintas. O usuario nao precisa configurar nada para ver valor, que e a
  premissa que fez a Fase 2 trocar a watchlist por OCR.
- **Com watchlist, as series dela vem PRIMEIRO e MARCADAS**, e o resto continua na lista
  abaixo. Filtrar de vez esconderia o item novo que a Fase 2 existe para descobrir.
- O casamento entre um nome da watchlist e uma serie usa o mesmo criterio EXATO de caixa
  normalizada e espacos colapsados que o plano 04-04 vai formalizar; nao use similaridade
  fuzzy, que casaria variantes de encanto que sao series deliberadamente separadas.

O `N` de quantas series aparecem no topo e discricao desta task: escolha um numero,
transforme-o em constante nomeada e escreva no fonte que ele e escolha.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_console.py -x -q</automated>
    <automated>python -m pytest tests/test_mira_da_janela.py tests/test_relogio_no_laco.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from l2scanner.config import ler_watchlist_do_mercado; from pathlib import Path; assert ler_watchlist_do_mercado(Path('nao-existe.toml')) == []"` sai com codigo 0.
    - Um teste escreve em `tmp_path` um TOML com a chave como texto solto e afirma com
      `pytest.raises` que o arranque e recusado com o nome do arquivo na mensagem.
    - Um teste com watchlist vazia e tres series de contagens `12`, `3` e `7` afirma com
      `assert` que a ordem devolvida e `12`, `7`, `3`.
    - Um teste com watchlist contendo a serie de contagem `3` afirma com `assert` que ela e
      a primeira da lista devolvida, que ela vem marcada, e que as de contagem `12` e `7`
      continuam presentes depois dela.
    - `python -c "import inspect, l2scanner.mercado_analise as m; d = inspect.getsource(m.ordenar_para_o_console); assert 'criterio 3' in d or 'ROADMAP' in d"` sai com codigo 0 — a divergencia com a letra do criterio esta escrita no fonte.
    - `python -c "import inspect, l2scanner.config as c; assert 'calibrar_mercado' not in inspect.getsource(c)"` sai com codigo 0.
      **`tests/test_bosses.py` NAO entra nesta linha de proposito:** ele e do agente que
      trabalha em `tiat` NESTA arvore agora, e um vermelho dele reprovaria uma task desta
      fase por causa alheia. Ele so aparece no plano 04-05 Task 2, a unica que edita o
      `config.toml`.
  </acceptance_criteria>

  <done>
    O console tem o que mostrar sem o usuario configurar nada, e a watchlist, quando existe,
    promove sem esconder.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ModeloDeMercado` no laco — carga unica, acrescimo por observacao, e o destaque ANTES de gravar (ANAL-02)</name>

  <read_first>
    - `l2scanner/mercado_modo.py` — o laco que o plano 04-01 escreveu, em especial a ordem
      de `catalogo.registrar` e `registro.registrar` dentro do tick.
    - `l2scanner/mercado_analise.py` — `observacoes_do_arquivo`, `menor_pedido_visivel`,
      `mediana_dos_unitarios`, `recencia_do_preco` e os pisos, escritos pelo plano 04-02.
    - `l2scanner/mercado_registro.py:648-729` (`registrar`) — o `True` significa gravou, o
      `False` significa duplicada OU registro desligado, e os dois nao se somam.
    - `l2scanner/mercado_registro.py:149-166` — a chave de dedup e por CONTEUDO, sem tempo.
    - `l2scanner/mercado_catalogo.py:418-419` — o volume esperado, milhares de linhas e nao
      milhoes.
    - `04-RESEARCH.md` secoes 3.6 e 3.7, mais o Pitfall 9.
  </read_first>

  <files>l2scanner/mercado_analise.py, l2scanner/mercado_modo.py, tests/test_mercado_modo.py</files>

  <behavior>
    - Teste: o modelo carrega de um CSV existente no arranque e passa a conhecer as series
      dele sem nenhuma releitura posterior.
    - Teste: uma observacao nova (a que `registrar` devolveu `True`) entra no modelo; uma
      duplicada (`False` com registro ligado) NAO entra duas vezes.
    - Teste: uma linha cujo unitario esta abaixo da mediana historica e marcada como
      destaque; uma acima nao e.
    - Teste central da ordem: uma serie com exatamente o piso de observacoes recebe uma
      linha nova muito barata; o veredito de destaque e calculado contra a mediana de ANTES
      e nao contra a mediana ja contendo a linha nova. O teste prende isso comparando com o
      valor que a mediana anterior tinha.
    - Teste: uma serie abaixo do piso da mediana nao produz destaque nenhum, nem positivo
      nem negativo.
    - Teste: o arquivo de observacoes e aberto uma unica vez durante toda a execucao do
      laco.
  </behavior>

  <action>
**(a) `ModeloDeMercado` em `l2scanner/mercado_analise.py`** (puro: sem disco, sem relogio).
Ele guarda as observacoes agrupadas por `chave_da_serie` e expoe `acrescentar(observacao)`,
alem de responder as perguntas que o plano 04-02 ja sabe calcular. Construa-o a partir de
uma LISTA de `ObservacaoLida` — quem le o arquivo e o laco, nao o modelo, e e isso que
mantem o modulo puro.

**(b) A carga e UNICA, no arranque.** `laco_do_mercado` chama `observacoes_do_arquivo`
uma vez, monta o modelo e mantem em memoria; cada observacao que `registro.registrar`
aceitou entra no modelo por `acrescentar`. **Nao releia o CSV a 1 Hz**: seria desperdicio
sobre milhares de linhas e, pior, abriria corrida com o usuario editando o arquivo no
Sheets no meio da sessao. O contrato do arquivo ja e "lido no arranque"; a analise segue o
mesmo contrato.

**(c) A ORDEM DO TICK, que e o coracao do ANAL-02.** Para cada linha de uma pagina aceita,
nesta sequencia exata:

1. comparar a linha contra o modelo **COMO ELE ESTA** e decidir o destaque;
2. `catalogo.registrar(...)`;
3. `registro.registrar(...)`;
4. so entao `modelo.acrescentar(...)`, e apenas quando o passo 3 devolveu `True`.

Se as linhas do tick ja tiverem entrado no modelo antes do passo 1, o item se compara
consigo mesmo e o destaque vira ruido. Escreva essa razao em comentario no laco, junto do
passo 1 — e o tipo de ordem que um refactor futuro desfaz sem perceber.

**(d) O destaque so existe acima do piso da mediana.** Destacar contra uma mediana que
nao vale seria pintar de vermelho um numero inventado. Abaixo do piso o veredito e
"sem destaque", e nao "acima" nem "abaixo".
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_modo.py tests/test_mercado_analise.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - Um teste roda o laco com `pasta=tmp_path` contendo um `observacoes.csv` pre-existente
      e afirma com `assert` que o modelo conhece as series desse arquivo antes do primeiro
      tick.
    - Um teste registra a mesma linha duas vezes e afirma com `assert` que a contagem de
      observacoes daquela serie no modelo subiu exatamente `1`.
    - Um teste monta uma serie com o piso exato de observacoes e unitarios conhecidos,
      injeta uma linha nova abaixo de todas elas, e afirma com `assert` que o veredito de
      destaque foi calculado contra a mediana ANTERIOR — comparando o valor de referencia
      usado com a mediana de antes, e nao com a de depois.
    - Um teste com serie abaixo do piso afirma com `assert` que o veredito e o estado sem
      destaque.
    - Um teste envolve `l2scanner.mercado_registro.observacoes_do_arquivo` num contador e
      afirma com `assert` que ele foi chamado exatamente `1` vez numa execucao de varios
      ticks.
    - `python -c "import inspect, l2scanner.mercado_modo as m; f = inspect.getsource(m); assert 'ANTES' in f"` sai com codigo 0 — a razao da ordem esta escrita no laco.
  </acceptance_criteria>

  <done>
    O modelo carrega uma vez, cresce por observacao aceita, e a linha lida agora e julgada
    contra a historia que existia antes dela.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "Vale quanto agora?" desenhado — com n, recencia e janela colados no numero</name>

  <read_first>
    - `l2scanner/mercado_console.py` — `linha_ao_vivo` e `resumo_da_sessao`, escritos pelo
      plano 04-01; as funcoes devolvem texto e nunca imprimem.
    - `l2scanner/console.py:97-119` (`moldurar`) — a moldura sem cor fora de tty.
    - `l2scanner/relogio.py:146-163` — `datetime` ingenuo em hora local, e a degradacao
      para o relogio do Windows com WARNING quando nao ha ancora (`relogio.confiavel`).
    - `l2scanner/__main__.py:617-724` (`desenhar_status`) — o precedente de bloco que sai
      por intervalo e nao por tick.
    - `.planning/workstreams/mercado/REQUIREMENTS.md:131-133` — a letra de ANAL-01 e
      ANAL-03.
    - `04-RESEARCH.md` secoes 3.2, 3.4, 3.5 e o Pitfall 4 (nunca imprimir total sem
      quantidade ao lado).
  </read_first>

  <files>l2scanner/mercado_console.py, l2scanner/mercado_modo.py, tests/test_mercado_console.py</files>

  <behavior>
    - Teste: a secao traz, para cada serie, o rotulo `menor pedido visivel` com o TOTAL e a
      QUANTIDADE lado a lado, e o unitario derivado marcado como derivado.
    - Teste: cada numero vem acompanhado de `n=` com a contagem, e de um carimbo de
      recencia.
    - Teste: o carimbo aparece em duas formas juntas, relativa e absoluta.
    - Teste: uma serie abaixo do piso da mediana mostra o que FALTA (a contagem atual e a
      necessaria), e nao um numero de mediana.
    - Teste: a linha de tendencia contem o `n` e a expressao que designa ofertas distintas.
    - Teste da nomenclatura: o texto devolvido pela secao NAO contem nenhuma das
      `EXPRESSOES_PROIBIDAS` definidas no topo de `tests/test_mercado_console.py`. Essa
      tupla contem exatamente tres itens: `"preco de venda"`, `"preço de venda"` e
      `"preço-de-venda"`, todos comparados em caixa baixa contra o texto em caixa baixa.
      O scanner ve ofertas, nao transacoes, e a palavra do requisito e `menor pedido
      visivel`.
    - Teste do FONTE, e nao so do retorno: nenhuma das `EXPRESSOES_PROIBIDAS` aparece em
      `inspect.getsource(l2scanner.mercado_console)` nem em
      `inspect.getsource(l2scanner.mercado_modo)`. Os testes que varrem so o TEXTO
      DEVOLVIDO por uma funcao deixam passar a expressao num rotulo de outra funcao, num
      comentario ou numa docstring — e `linha_ao_vivo` e `resumo_da_sessao`, escritas no
      plano 04-01, nao sao varridas por nenhum outro teste desta fase.
    - Teste: quando `relogio.confiavel` e falso, o cabecalho da secao diz isso UMA vez.
  </behavior>

  <action>
Acrescente a `l2scanner/mercado_console.py` a funcao que desenha a secao "vale quanto
agora", recebendo o modelo, a watchlist ja resolvida e o `agora` por parametro, e
devolvendo TEXTO — nunca imprimindo. Texto simples, sem dependencia nova.

**(a) Cada numero viaja com a evidencia colada.** Estatistica sem `n` e sem data e
adivinhacao com cara de numero. Toda linha traz a contagem de observacoes distintas e o
carimbo. **Nunca imprima um total sem a quantidade ao lado**: um total solto e sem
significado porque um lote de 100 custa mais que um de 1, e foi assim que a leitura de
`4,50` para um item de `1.480,00` virou um pitfall nomeado na pesquisa.

**(b) O rotulo e `menor pedido visivel`, e essa e a palavra do requisito.** A razao e
honestidade: o scanner ve OFERTAS no quadro, nao transacoes concluidas. Ha um teste
prendendo as expressoes proibidas, listadas no bloco `<behavior>` desta task — o teste e
a rede, mas quem escreve o texto precisa saber por que a rede existe.

**(c) O unitario aparece DERIVADO e marcado como tal.** Ele nao esta no CSV de proposito
(o que o jogo exibe e derivacao arredondada a duas casas), e a marca no texto e o que
impede alguem de o tratar como dado gravado. Derive-o na formatacao a partir da
`Fraction`, arredondando so ali.

**(d) A recencia sai em DUAS formas juntas, relativa e absoluta** — o relativo e o que o
olho le, o absoluto e o que sobrevive a copiar a linha para o WhatsApp. Use a recencia do
PRECO (`max(primeira_vez)` do `observacoes.csv`), e o menor pedido visivel carrega o
carimbo DELE. **Nunca use o `ultima_vez` do catalogo aqui**: ele diz quando o ITEM foi
visto, em qualquer preco, e pode ser de agora mesmo sobre um preco de tres dias atras. E
se `relogio.confiavel` for falso, o cabecalho da secao diz isso uma vez — sem esse aviso,
"ha 12 min" pode estar tres horas errado num dual boot, que e o defeito que o `Relogio`
existe para corrigir.

**(e) Abaixo do piso, escreva o que FALTA.** A linha mostra a contagem atual e quantas
observacoes seriam necessarias, em vez de um numero. Uma mediana de duas observacoes e um
numero que engana.

**(f) A tendencia sai com o tamanho da janela SEMPRE junto**, e a unidade e ofertas
distintas — nunca uma variacao percentual solta. Sem o tamanho, a reta de 3 pontos parece
a de 300; e sem a palavra que designa ofertas distintas, o usuario le a reta como uma
variacao ao longo de horas, o que o CSV nao afirma.

**(g) O destaque de ANAL-02 aparece na hora**, na linha da leitura corrente, usando
`console.moldurar` em vez de uma regua de caracteres montada a mao. E ele so aparece
acima do piso da mediana.

**(h) A cadencia da secao.** Ela nao precisa repintar a cada segundo: ela muda quando uma
serie ganha observacao nova. Saia no arranque e depois por evento, ou por intervalo no
precedente de `desenhar_status`, e nunca por tick.
  </action>

  <verify>
    <automated>python -m pytest tests/test_mercado_console.py tests/test_mercado_modo.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_mercado_console.py` define no topo do modulo a tupla `EXPRESSOES_PROIBIDAS`
      com os tres itens listados no `<behavior>` desta task, e um teste afirma com `assert`,
      item por item, que nenhum deles aparece no texto devolvido pela secao (comparacao em
      caixa baixa dos dois lados). O teste falha citando qual expressao apareceu.
    - Um teste afirma com `assert` que o texto contem `menor pedido visivel`.
    - Um teste afirma com `assert`, item por item da tupla `EXPRESSOES_PROIBIDAS`, que
      nenhum deles aparece em `inspect.getsource(l2scanner.mercado_console)` nem em
      `inspect.getsource(l2scanner.mercado_modo)` (comparacao em caixa baixa dos dois
      lados), falhando com o nome da expressao e o nome do modulo em que ela apareceu.
      Isto cobre `linha_ao_vivo` e `resumo_da_sessao`, que nenhum outro teste da fase varre.
    - Um teste com uma serie de `n=12` afirma com `assert` que `n=12` aparece no texto e que
      o total e a quantidade daquela oferta aparecem os dois.
    - Um teste afirma com `assert` que a linha do menor pedido visivel carrega o carimbo
      DAQUELA oferta e nao o `max(primeira_vez)` da serie, num cenario montado para os dois
      diferirem.
    - Um teste com serie de `n=3` afirma com `assert` que o texto informa a contagem `3` e o
      piso `5`, e que nenhum valor de mediana foi impresso para aquela serie.
    - Um teste afirma com `assert` que a linha de tendencia contem o `n` da janela.
    - Um teste com `relogio.confiavel` falso afirma com `assert` que o aviso aparece exatamente
      uma vez no texto.
    - `grep -n "ultima_vez" l2scanner/mercado_console.py` nao encontra ocorrencia.
  </acceptance_criteria>

  <done>
    O console responde "vale quanto agora?" com menor pedido visivel e mediana, cada um com
    `n` e recencia; a linha barata de agora aparece destacada; a tendencia sai com a janela
    junto; e a nomenclatura esta presa por teste.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` -> processo | arquivo que o usuario edita a mao; a watchlist entra por ele |
| modelo em memoria -> texto exibido | fronteira de INTERPRETACAO: onde um numero plausivel e errado nasce |
| relogio -> carimbo exibido | num dual boot a hora crua do Windows esta errada; o `Relogio` pode estar sem ancora |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | `[mercado] watchlist` com tipo errado | medium | mitigate | Recusa de arranque nomeando arquivo e formato certo, no molde de `ler_bosses`; item nao-texto dentro da lista tambem recusado (Task 1a) |
| T-04-12 | Tampering | mediana calculada ja incluindo a linha deste tick | high | mitigate | Ordem do tick fixada e comentada no laco, com teste que compara o valor de referencia usado contra a mediana ANTERIOR (Task 2c) |
| T-04-13 | Repudiation | numero exibido sem `n`, sem carimbo ou sem quantidade | medium | mitigate | `n` e carimbo em toda linha; total nunca sem quantidade ao lado; abaixo do piso o texto diz o que falta (Task 3a, 3e) |
| T-04-14 | Repudiation | recencia do ITEM exibida como recencia do PRECO | medium | mitigate | Uso obrigatorio de `max(primeira_vez)` do `observacoes.csv`; grep de verificacao prova que `ultima_vez` nao aparece no modulo de console (Task 3d) |
| T-04-15 | Repudiation | carimbo errado por relogio sem ancora | low | mitigate | Aviso unico no cabecalho quando `relogio.confiavel` e falso, com teste (Task 3d) |
| T-04-16 | Tampering | nomenclatura que promete transacao onde ha oferta | high | mitigate | Tupla `EXPRESSOES_PROIBIDAS` com assercao item por item sobre o texto devolvido **E sobre o FONTE** de `mercado_console` e `mercado_modo` via `inspect.getsource` — o segundo alcanca `linha_ao_vivo` e `resumo_da_sessao`, que nenhum outro teste varre (Task 3) |
| T-04-SC | Tampering | instalacao de pacote | low | accept | Esta fase nao instala nada. Ver `04-RESEARCH.md` > Package Legitimacy Audit |
</threat_model>

<verification>
1. `python -m pytest tests/test_mercado_console.py tests/test_mercado_modo.py tests/test_mercado_analise.py -x -q` verde.
2. `python -m pytest tests/ --ignore=tests/test_agenda.py -q` mantem o verde de referencia
   mais os testes novos. `KeyboardInterrupt` vindo de `tests/test_agenda.py` **nao e falha**.
3. `test -z "$(git diff --stat -- l2scanner/rastreador.py l2scanner/visao.py l2scanner/__main__.py config.toml)" || { echo "REPROVADO: um arquivo proibido ou disputado foi tocado"; exit 1; }` sai com codigo 0.
4. `python -m pytest tests/test_mercado_firewall_de_fase.py::TestOMercadoNaoEscreveNoCalibrationJson -q` verde.

   **CORRIGIDO — o criterio anterior era VACUO**, pela mesma razao escrita por extenso
   no criterio 5 do `04-01-PLAN.md`: `test -z "$(git status --porcelain
   calibration.json)"` roda sobre um arquivo **gitignored** (`.gitignore:51`) e sai vazio
   SEMPRE, escrito ou nao. A guarda que entra no lugar tira impressao digital
   `(tamanho, mtime_ns, sha256)` antes e depois de uma sessao inteira do laco, varre o
   AST atras do escritor, e **traz o proprio controle negativo** provando que ela reprova
   quando a escrita de fato acontece.
5. `test -z "$(git diff -- requirements.txt)" || { echo "REPROVADO: requirements.txt mudou"; exit 1; }` sai com codigo 0.

   **Os dois com `||` REPROVAM sozinhos**, com codigo diferente de zero e mensagem. A
   forma crua (`git diff --stat ...`) sai com codigo 0 tendo mudado o arquivo inteiro.

<human-check>
Com o mercado aberto e o modo rodando, o usuario confere no console:
  - que a secao responde "vale quanto agora?" para os itens que ele reconhece, e que cada
    numero vem com `n` e com data;
  - que uma linha claramente barata aparece destacada NA HORA em que ele a ve na tela;
  - que a divergencia com a letra do criterio 3 do ROADMAP e aceitavel para ele: **o
    criterio diz "para cada item da watchlist", e esta fase responde para os itens com
    mais evidencia quando nao ha watchlist, e promove os da watchlist quando ha.** A
    decisao esta travada no `04-CONTEXT.md` e foi tomada por Claude, nao pelo usuario — ela
    esta aqui em voz alta exatamente para ele poder discordar ao acordar.
</human-check>
</verification>

<success_criteria>
- O console responde "vale quanto agora?" sem o usuario configurar nada, e a watchlist
  promove sem esconder.
- Menor pedido visivel e mediana saem com `n` e recencia; abaixo do piso sai o que falta.
- O destaque de ANAL-02 e calculado contra a historia de ANTES do tick, provado por teste.
- A tendencia sai com o tamanho da janela junto, em ofertas distintas.
- A expressao de transacao proibida esta presa por teste sobre o texto devolvido.
</success_criteria>

## Artifacts this phase produces

| Artefato | Novo? | O que e |
|---|---|---|
| `ler_watchlist_do_mercado` em `l2scanner/config.py` | novo | A watchlist lida sem arrastar DPI nem `cv2` de GUI, no molde de `ler_bosses` |
| `ordenar_para_o_console` em `l2scanner/mercado_analise.py` | novo | A watchlist como filtro de DESTAQUE, e o topo por evidencia quando ela nao existe |
| `ModeloDeMercado` em `l2scanner/mercado_analise.py` | novo | A historia em memoria, carregada uma vez e crescida por observacao aceita |
| A secao "vale quanto agora" em `l2scanner/mercado_console.py` | novo | O desenho de ANAL-01, ANAL-02 e ANAL-03, em texto puro |
| `tests/test_mercado_console.py` | novo | Prende a nomenclatura, a evidencia colada ao numero e a ordem do destaque |

<output>
Create `.planning/workstreams/mercado/phases/04-modo-mercado-an-lise-e-console/04-03-SUMMARY.md` when done
</output>
