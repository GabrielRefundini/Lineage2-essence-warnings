---
phase: 01-o-acervo-e-o-silencio-dele
workstream: identidade
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - l2scanner/acervo.py
  - tests/test_acervo.py
  - tests/test_calibrar_nao_apaga_identidades.py
autonomous: true
requirements: [DURA-01, OPER-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Com entradas ja no acervo, uma rodada COMPLETA de `calibrar.bat --nomes \"A,B,C,D\"` termina com codigo 0 e TODAS elas continuam no disco, byte a byte intactas. (criterio 1, DURA-01, D-04)"
    - "Na MESMA rodada, `cal.assinaturas` e `cal.nomes` do `calibration.json` sao reescritos exatamente como sao hoje — 4 nomes e 4 assinaturas novas. Sem esta metade, a sobrevivencia acima poderia ser so uma rodada que nao fez nada. (criterio 1, OPER-02)"
    - "Nenhum modulo do laco do scanner regrava o `calibration.json`: a fusao do acervo vive em memoria e nao pode vazar para o arquivo que o `calibrar.bat` reescreve. (D-04, T-01-03)"
    - "Com o acervo VAZIO, o reconhecimento sobre o frame da fixture entrega exatamente o mesmo resultado de hoje: os mesmos nomes nas mesmas linhas. (criterio 2, OPER-02)"
    - "A mesma pessoa presente nos DOIS lugares — calibrada e no acervo — produz UM nome numa linha, e nao uma linha muda: a entrada do acervo e descartada quando a chave de conteudo ou o nome ja vem da calibracao. (criterio 2, OPER-02)"
    - "Quando as duas copias diferem o bastante para terem chaves distintas e a entrada do acervo e anonima, a linha degrada para SILENCIO — nunca para um nome errado — e isso esta escrito como resposta deliberada. (criterio 2)"
  artifacts:
    - "tests/test_calibrar_nao_apaga_identidades.py"
    - "acervo.carregar_identidades com as regras de deduplicacao"
  key_links:
    - "`calibrar.main()` -> `fundir_com_a_calibracao_em_disco` -> `cal.salvar` -> `calibration.json` — caminho que NAO alcanca `.identidades/`"
    - "`carregar_identidades` -> ordem da lista -> desempate `(-i, -j)` de `identificar_linhas`"
    - "`cal.assinaturas` fundido -> `cal.nomes_com_assinatura` -> `Rastreador.nomes_reservados` -> `calibracao.nome_da_linha`"
---

<objective>
Fechar as duas verdades que faltam da Fase 1: **o acervo sobrevive ao
`calibrar.bat`** (criterio 1, DURA-01 — o requisito que mata a feature se
falhar) e **`--nomes` continua entregando o mesmo de hoje, com as assinaturas
calibradas e as do acervo convivendo sem uma sombrear a outra** (criterio 2,
OPER-02).

**O perigo que da nome ao DURA-01, e ele esta confirmado em campo.**
`calibrar.py` monta uma `Calibracao` do zero e grava por cima do arquivo
inteiro. WINDOWS #13, 2026-08-30: o usuario rodou `calibrar.bat` e perdeu 13
moldes de glifo e 3 ancoras do mercado. O conserto ja escrito
(`fundir_com_a_calibracao_em_disco`) preserva por SUBTRACAO tudo o que a party
nao possui — e `CAMPOS_DA_PARTY` **inclui `assinaturas`**. Ou seja: ele NAO
protege assinaturas, e corretamente, porque a party as possui. Uma assinatura
aprendida dentro do `calibration.json` nao morreria por bug; morreria por
desenho. E por isso D-04 poe o acervo em pasta propria.

**E por isso um teste que so afirme "a pasta e outra" nao prova nada.** O que
precisa ser provado e que o CAMINHO DE ESCRITA nao alcanca a pasta — rodando o
caminho de verdade, com entradas de verdade na pasta, e olhando o disco depois.

**A pergunta que o `<phase_specific_direction>` mandou decidir e escrever:** o
que acontece quando a calibracao e o acervo carregam a MESMA pessoa. A resposta
esta na Tarefa 2, e ela nao e retorica — sem regra de deduplicacao o desfecho
seria o PIOR possivel: duas assinaturas quase identicas pontuam ~1.000 e ~0.98
na mesma linha, a diferenca fica abaixo de `MARGEM_MINIMA_SOBRE_O_SEGUNDO`
(0.12), e `identificar_linhas` devolve `Casamento(None, ...)`. Acrescentar a
pessoa ao acervo faria ela PARAR de ser reconhecida. E exatamente "uma
sombreando a outra" que o criterio 2 proibe.

**Decisoes travadas que este plano implementa:** D-01 (a chave e o hash do
conteudo — e o que permite deduplicar sem comparar pixels), D-04 (a pasta e
`.identidades/`, e a garantia e estrutural), D-05 (nada e podado).

Purpose: sem o criterio 1 a feature inteira morre na primeira recalibracao de
party — numa feature cuja promessa e "voce para de precisar rodar
`calibrar.bat`". Sem o criterio 2, quem prefere digitar perde reconhecimento ao
ganhar um acervo.

Output: `tests/test_calibrar_nao_apaga_identidades.py`, as regras de
deduplicacao em `carregar_identidades`, e o portao que impede o laco do scanner
de regravar o `calibration.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/identidade/ROADMAP.md
@.planning/workstreams/identidade/phases/01-o-acervo-e-o-silencio-dele/01-CONTEXT.md
@.planning/workstreams/identidade/phases/01-o-acervo-e-o-silencio-dele/01-01-SUMMARY.md
@.claude/CLAUDE.md

@tests/test_calibrar_nao_apaga_mercado.py
</context>

<interface_context>
O que o plano 01-01 entregou e este plano estende:

```python
# l2scanner/acervo.py
PREFIXO_ASSINATURA = "assinatura_"
PREFIXO_NOME = "nome_"
def chave_da_assinatura(assinatura: Assinatura) -> str        # sha256 hex, 64 digitos
@dataclass(frozen=True)
class Identidades:
    assinaturas: list[Assinatura]
    configuradas: bool
    conhecidas: int
    sem_nome: int
    resumo: str
class AcervoDeIdentidades:
    def __init__(self, pasta: Path) -> None
    def chaves(self) -> list[str]
    def assinaturas(self) -> list[Assinatura]
    def gravar(self, assinatura: Assinatura) -> str            # criado|ja_existia|falhou
def carregar_identidades(calibradas: list[Assinatura],
                         acervo: AcervoDeIdentidades) -> Identidades

# l2scanner/identidade.py
Assinatura.anonima -> bool          # not self.nome
```

O caminho de escrita que a Tarefa 1 precisa dirigir de verdade
(`l2scanner/calibrar.py`, fim de `main()`):

```python
cal = fundir_com_a_calibracao_em_disco(cal, ARQUIVO_CALIBRACAO)
cal.salvar(ARQUIVO_CALIBRACAO)
```

A fixture que torna a rodada REAL possivel, medida na leitura deste plano:
`tests/fixtures/identidade/party_ordem_original.png` tem shape `(522, 174, 3)` e
`tests/fixtures/identidade/calibracao.json` tem
`party_window = {esquerda: 1738, topo: 325, largura: 174, altura: 522}` e
`nomes = ["Korzis", "J4guar", "Kaus", "TioMad"]` com 4 assinaturas gravadas. O
PNG E a party window inteira, entao `capturar_tela` devolvendo
`(pixels, 1738, 325)` faz `janela_px` cair exatamente sobre a imagem e a rodada
produz assinaturas REAIS para os 4 nomes.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: uma rodada de verdade do calibrar.bat, com o acervo cheio, e nada se perde</name>
  <files>tests/test_calibrar_nao_apaga_identidades.py</files>
  <read_first>
    - `tests/test_calibrar_nao_apaga_mercado.py` INTEIRO — a docstring do incidente, o `_semear`, o `_dirigir` (com as duas linhas que prendem o `calibration.json` da maquina fora do alcance da suite) e os seis casos. E o molde a copiar.
    - `l2scanner/calibrar.py` linhas 1265-1345 (`CAMPOS_DA_PARTY` e `fundir_com_a_calibracao_em_disco`) e 1560-1600 (o bloco que grava `cal.assinaturas` a partir de `--nomes`, e o `cal.salvar` final).
    - `l2scanner/calibracao.py` linhas 575-680 (`salvar`, a escrita atomica, e o que entra no dict).
    - `l2scanner/acervo.py` — como o plano 01-01 o deixou.
  </read_first>
  <behavior>
    Em `tests/test_calibrar_nao_apaga_identidades.py`, todos com o
    `calibration.json` e o `.identidades/` dentro de `tmp_path`:

    - **A rodada completa nao encosta no acervo.** Com 4 entradas semeadas em
      `.identidades/` (duas com irmao `nome_<hash>`, duas anonimas), uma rodada
      real de `main()` com `--auto --nomes "Korzis,J4guar,Kaus,TioMad"` devolve
      0 e o retrato da pasta — nomes de arquivo E bytes de cada um — e IDENTICO
      antes e depois.
    - **GUARDA CONTRA PROVA VAZIA: a rodada realmente aconteceu.** No mesmo
      caso, o `calibration.json` depois tem `nomes` com os quatro nomes passados
      e `assinaturas` com quatro entradas novas, cada uma com `altura`,
      `largura` e `bits` — ou seja, os dois campos que a party POSSUI foram
      reescritos, exatamente como sao hoje. Sem esta afirmacao, uma rodada que
      abortasse cedo passaria como prova de sobrevivencia.
    - **O caminho `--selecionar` passa pelo mesmo portao.** Mesmo desfecho.
    - **O acervo inexistente nao quebra a rodada.** Sem a pasta `.identidades/`,
      a rodada termina 0 e nada e criado por ela — a pasta e do scanner, nao do
      calibrador.
    - **A garantia e estrutural, e isto e afirmado em forma POSITIVA:** o
      conjunto dos campos de `Calibracao` (via `dataclasses.fields`) nao contem
      nenhum nome relacionado ao acervo. O acervo nao e um campo, entao nao ha
      caminho por onde `cal.salvar` o alcance.
    - **O laco do scanner nao regrava o `calibration.json`.** Varre
      `l2scanner/*.py`, remove linhas de comentario, e monta o conjunto dos
      modulos cujo texto restante chama `salvar` sobre uma `Calibracao`. Afirma
      igualdade com o conjunto esperado — os modulos de CALIBRACAO, e nunca
      `__main__.py`, `sessao.py` ou `visao.py`. Isto prende a fusao em memoria
      do plano 01-01: se ela vazasse para o arquivo, D-04 seria desfeito pelo
      lado de dentro, e as entradas do acervo (inclusive as anonimas, com
      `nome: ""`) passariam a morrer na proxima `calibrar.bat` junto com
      `cal.assinaturas`.
  </behavior>
  <action>
Criar `tests/test_calibrar_nao_apaga_identidades.py`, irmao declarado de
`tests/test_calibrar_nao_apaga_mercado.py`. Docstring de modulo com o incidente
WINDOWS #13 (2026-08-30, 13 moldes de glifo e 3 ancoras perdidos) e com a
diferenca que separa este arquivo do irmao, dita em voz alta:

> O irmao afirma que a party PRESERVA o que nao e dela dentro do MESMO arquivo.
> Este afirma outra coisa, e mais forte: que existe um lugar que o caminho de
> escrita NAO ALCANCA. `CAMPOS_DA_PARTY` inclui `assinaturas`, entao o conserto
> de subtracao do irmao nao protege assinatura nenhuma — e corretamente, porque
> a party as possui. Uma assinatura aprendida dentro do `calibration.json` nao
> morreria por bug; morreria por desenho. A separacao e a unica defesa, e um
> teste que so afirmasse "a pasta e outra" seria uma repeticao da decisao, e nao
> uma prova dela. Por isso este arquivo RODA a rodada.

**DISCIPLINA INEGOCIAVEL, copiada do irmao e repetida na docstring:** todo caso
faz `monkeypatch.setattr(l2scanner.calibrar, "ARQUIVO_CALIBRACAO", alvo)` com
`alvo` dentro de `tmp_path`, mais os `monkeypatch` de `ler_personagem_do_jogo`,
`listar_janelas_do_jogo`, `janela_que_contem`, `achar_barra_do_proprio` e
`conferir_visualmente`. Sem eles a suite le a configuracao da maquina de quem a
roda e, pior, pode reproduzir o incidente dentro do CI — desta vez sem resgate.

**A rodada precisa ser REAL o bastante para reescrever `cal.assinaturas`.** Nao
usar pixels zerados: com um recorte preto, `criar_assinatura` cai abaixo de
`PIXELS_MINIMOS_DE_TEXTO` e o bloco de `--nomes` pula todos, deixando
`cal.assinaturas` vazio — e a guarda contra prova vazia nao teria o que afirmar.
Usar a fixture medida: `capturar_tela` devolve
`(cv2.imread(party_ordem_original.png), 1738, 325)` e `calibrar_automatico`
devolve a `Calibracao` carregada de `tests/fixtures/identidade/calibracao.json`
com `assinaturas` e `nomes` zerados (a rodada os regrava; e disso que a guarda
trata). Comentar no codigo POR QUE os offsets sao 1738 e 325: o PNG e a party
window inteira, entao `pw.topo - oy` e `pw.esquerda - ox` precisam dar zero.

**O retrato da pasta** e uma funcao auxiliar `_retrato(pasta) -> dict[str, bytes]`
com `{arquivo.name: arquivo.read_bytes()}`, e a afirmacao e `antes == depois`.
Comparar bytes, e nao contagem: uma rodada que reescrevesse uma entrada com o
mesmo nome e conteudo diferente passaria numa comparacao de contagem, e e
exatamente o desfecho que corrompe uma identidade em silencio.

**As entradas semeadas sao escritas A MAO** (`json.dumps` direto), nunca por
`AcervoDeIdentidades.gravar`: esta fase prova que uma entrada posta a mao se
comporta certo, e o semeador nao pode depender do codigo que ele existe para
vigiar.

**O portao de `salvar`** e afirmado por igualdade de conjuntos, nunca por
`grep` negativo, para nao depender de um literal que o proprio texto do plano
poderia invalidar. Remova as linhas de comentario de cada fonte antes de olhar.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibrar_nao_apaga_identidades.py -q</automated>
    <automated>python -m pytest tests/test_calibrar_nao_apaga_mercado.py tests/test_calibracao_generica.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Depois de uma rodada real de `calibrar.main()` com `--auto --nomes "Korzis,J4guar,Kaus,TioMad"` sobre um `.identidades/` com 4 entradas semeadas, o retrato da pasta (nomes de arquivo e bytes) e identico ao de antes.
    - Na MESMA rodada, o `calibration.json` gravado tem `nomes` com os 4 nomes e `assinaturas` com 4 entradas contendo `altura`, `largura` e `bits`.
    - O caminho `--selecionar` produz o mesmo desfecho nas duas afirmacoes acima.
    - Uma rodada com a pasta `.identidades/` ausente termina com codigo 0 e nao cria a pasta.
    - `dataclasses.fields(Calibracao)` nao tem campo relacionado ao acervo, afirmado por igualdade de conjuntos.
    - O conjunto de modulos de `l2scanner/` que chamam `salvar` sobre uma `Calibracao`, com comentarios removidos, e exatamente o conjunto de modulos de calibracao; `__main__.py`, `sessao.py` e `visao.py` estao fora dele.
    - Todo caso escreve dentro de `tmp_path` e nenhum toca o `calibration.json` da maquina.
  </acceptance_criteria>
  <done>Rodar `calibrar.bat --nomes "A,B,C,D"` com o acervo cheio termina com o acervo byte a byte igual e com `cal.assinaturas` e `cal.nomes` reescritos como sempre foram. O requisito que mataria a feature esta preso por um teste que roda o caminho de verdade.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: a calibracao e o acervo carregando a mesma pessoa, sem uma sombrear a outra</name>
  <files>l2scanner/acervo.py, tests/test_acervo.py</files>
  <read_first>
    - `l2scanner/identidade.py` — `identificar_linhas` inteiro, com atencao ao guloso: o `max((pontos[i][j], -i, -j))`, o corte por `LIMIAR_DE_CASAMENTO = 0.75`, e sobretudo `MARGEM_MINIMA_SOBRE_O_SEGUNDO = 0.12` com o comentario "dois candidatos empatados significam que a assinatura nao discrimina".
    - `l2scanner/acervo.py` — `carregar_identidades` como o plano 01-01 a deixou (concatenacao, calibradas primeiro).
    - `l2scanner/calibracao.py` linhas 515-545 — `nomes_com_assinatura` (ja corrigida no plano 01-01 para ignorar nome vazio) e `nome_da_linha`.
    - `tests/test_identidade.py` linhas 1-45 — as fixtures e o idioma de reconhecer o frame real.
  </read_first>
  <behavior>
    - **O de hoje continua igual.** Com o acervo VAZIO, `carregar_identidades`
      devolve exatamente a lista calibrada, na mesma ordem, e o reconhecimento
      sobre `party_ordem_original.png` entrega os mesmos 4 nomes nas mesmas 4
      linhas que a suite ja afirma hoje.
    - **Mesma pessoa, conteudo identico.** Uma entrada do acervo cuja chave e
      igual a de uma calibrada e DESCARTADA; a lista fundida tem uma entrada so
      para aquela pessoa, e a linha recebe o nome CALIBRADO.
    - **Mesma pessoa, mesmo nome, conteudo levemente diferente.** Uma entrada do
      acervo com nome nao vazio que ja aparece entre os nomes calibrados e
      DESCARTADA. Sem esta regra a linha ficaria muda pela margem, que e o pior
      desfecho possivel do criterio 2.
    - **Pessoa diferente entra normalmente.** Uma entrada do acervo com chave e
      nome novos aparece na lista fundida, depois das calibradas.
    - **A entrada anonima so deduplica por chave.** Duas entradas anonimas com
      conteudos diferentes convivem; uma anonima cuja chave e de uma calibrada e
      descartada.
    - **O caso que sobra, e ele degrada com honestidade.** Uma entrada ANONIMA
      posta a mao com conteudo PARECIDO mas nao identico ao de uma calibrada:
      as duas passam do limiar, a margem fica abaixo de 0.12 e a linha vira
      `Casamento(None, ...)` — SILENCIO. O teste afirma o silencio e afirma
      explicitamente que o nome errado NAO sai. Este e o desfecho aceito e
      escrito, nao um defeito a esconder.
    - **`nomes_reservados` vem da lista fundida e nao carrega vazio.** Com uma
      entrada anonima no acervo, `cal.nomes_com_assinatura` sobre a lista fundida
      nao contem a string vazia, e `nome_da_linha` continua devolvendo
      `Membro N` para uma linha nao reconhecida.
  </behavior>
  <action>
Acrescentar as regras de deduplicacao a `carregar_identidades`, com a docstring
que responde por escrito a pergunta que o `<phase_specific_direction>` mandou
fechar.

Regra, nesta ordem: as CALIBRADAS entram primeiro e inteiras; uma entrada do
acervo e descartada quando **(a)** a chave de conteudo dela ja aparece entre as
calibradas, ou **(b)** o nome dela nao e vazio e ja aparece entre os nomes
calibrados. Uma entrada anonima so passa por (a). Nenhuma entrada calibrada e
descartada por nada.

A docstring precisa dizer as tres coisas abaixo, porque nenhuma delas e obvia
lendo o codigo:

1. **POR QUE A CALIBRADA VENCE.** `--nomes` e o usuario digitando o nome de
   proposito; o acervo (na Fase 2) grava sozinho. Entre uma afirmacao deliberada
   e uma inferida sobre a mesma pessoa, vence a deliberada. E o criterio 2 pede
   que `--nomes` continue entregando o mesmo de hoje.
2. **POR QUE A ORDEM E ESTRUTURAL E NAO UMA CONDICAO.** `identificar_linhas`
   desempata com `max(..., -i, -j)`, entao no empate exato vence o indice menor.
   Pondo as calibradas primeiro, a precedencia sai do algoritmo que ja existe.
   Uma condicao "se for calibrada, prefira" seria uma segunda regra de
   precedencia, e as duas divergiriam na primeira vez que alguem mexesse numa.
3. **POR QUE A REGRA DO NOME EXISTE ALEM DA REGRA DA CHAVE, com o numero.** Duas
   copias da mesma pessoa capturadas de frames diferentes tem chaves diferentes
   (um pixel basta) e pontuam perto de 1.000 as duas. A diferenca cai abaixo de
   `MARGEM_MINIMA_SOBRE_O_SEGUNDO = 0.12` e `identificar_linhas` devolve
   `Casamento(None, ...)`: acrescentar a pessoa ao acervo a faria PARAR de ser
   reconhecida. A regra do nome fecha essa porta para todo mundo que ja tem nome.
   **O que fica de fora, dito em voz alta:** uma entrada ANONIMA parecida com uma
   calibrada nao tem nome para comparar e cai no silencio pela margem. Aceito de
   proposito, por dois motivos. E o unico desfecho SEGURO da familia — a linha
   cala em vez de mentir, que e a garantia que o projeto mais preza. E ele nao e
   alcancavel pelo caminho normal: esta fase nunca escreve, e a Fase 2 so grava
   depois de nao casar com nada ja gravado (APRE-04), entao um quase-duplicado
   so nasce de uma entrada posta a mao.

Nao acrescentar nenhuma comparacao de pixels, nenhum limiar novo e nenhum
"parecido o suficiente": a chave de conteudo E a comparacao (D-01), e inventar
um segundo criterio de igualdade aqui seria criar uma segunda definicao de
"mesma pessoa" dentro do mesmo modulo.

Nos testes, a copia levemente diferente e obtida virando um punhado de bits da
mascara — o suficiente para mudar a chave e nao o suficiente para derrubar a
correlacao abaixo do limiar. O caso precisa AFIRMAR essas duas condicoes
(chaves diferentes; as duas pontuacoes acima de `LIMIAR_DE_CASAMENTO`) antes de
afirmar o silencio, senao ele estaria provando outra coisa.
  </action>
  <verify>
    <automated>python -m pytest tests/test_acervo.py -q</automated>
    <automated>python -m pytest tests/test_identidade.py tests/test_rastreador.py tests/test_sessao.py tests/test_presenca.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Com o acervo vazio, o reconhecimento sobre `party_ordem_original.png` entrega os mesmos nomes nas mesmas linhas que a suite ja afirma hoje, e `carregar_identidades` devolve a lista calibrada intacta e na mesma ordem.
    - Uma entrada do acervo com a MESMA chave de uma calibrada nao aparece na lista fundida, e a linha correspondente recebe o nome calibrado.
    - Uma entrada do acervo com nome nao vazio ja presente entre os nomes calibrados nao aparece na lista fundida, mesmo com chave diferente.
    - Uma entrada do acervo com chave e nome novos aparece na lista fundida, depois de todas as calibradas.
    - Uma entrada ANONIMA parecida com uma calibrada produz, comprovadamente, chaves diferentes e duas pontuacoes acima de `LIMIAR_DE_CASAMENTO`; a linha resultante nao tem nome e nenhum evento sai — e o teste afirma que o nome da calibrada NAO foi atribuido.
    - `cal.nomes_com_assinatura` sobre a lista fundida com uma entrada anonima nao contem a string vazia.
    - A docstring de `carregar_identidades` responde as tres perguntas: por que a calibrada vence, por que a ordem e estrutural, e o que fica de fora e por que isso e aceitavel.
    - `python -m pytest tests/ -q` verde, com contagem igual ou maior que a linha de base; nenhum teste existente editado ou afrouxado.
  </acceptance_criteria>
  <done>Quem prefere digitar `--nomes` continua com o reconhecimento de hoje, sem perda; e acrescentar ao acervo alguem que ja estava calibrado nao apaga o reconhecimento dessa pessoa nem inventa um nome para ela.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibrar.py` -> `calibration.json` | Monta uma `Calibracao` do zero e grava o arquivo inteiro. `CAMPOS_DA_PARTY` inclui `assinaturas`, entao o conserto de subtracao existente nao protege assinatura nenhuma. |
| laco do scanner -> `calibration.json` | O plano 01-01 funde o acervo em `cal.assinaturas` EM MEMORIA. Se algum dia algo neste caminho gravar a `Calibracao`, o acervo entra no arquivo que o `calibrar.bat` reescreve — e D-04 e desfeito por dentro. |
| lista fundida -> `identificar_linhas` -> nome no alerta | Duas assinaturas da mesma pessoa na mesma lista sao a maneira mais barata de fazer uma linha calar ou, pior, de mover um nome para a linha errada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | `calibrar.main()` -> `cal.salvar` alcancando o acervo | high | mitigate | Rodada REAL de `main()` com o acervo semeado e comparacao byte a byte da pasta antes e depois; mais a afirmacao positiva de que nenhum campo de `Calibracao` e do acervo. (Tarefa 1) |
| T-01-08 | Tampering | fusao em memoria vazando para o `calibration.json` | high | mitigate | Portao por igualdade de conjuntos: os modulos que chamam `salvar` sobre uma `Calibracao` sao exatamente os de calibracao; `__main__.py`, `sessao.py` e `visao.py` ficam fora. Uma gravacao futura no laco cairia aqui antes de chegar em campo. (Tarefa 1) |
| T-01-09 | Spoofing | duas copias da mesma pessoa na lista fundida | high | mitigate | Deduplicacao por chave de conteudo (D-01) e por nome. O residuo — anonima parecida com calibrada — degrada para SILENCIO pela margem de 0.12 de `identificar_linhas`, e o teste afirma que o nome errado nao sai. Silencio e o desfecho seguro da familia; nome errado nao e alcancavel. (Tarefa 2) |
| T-01-10 | Tampering | entrada do acervo desalojando uma calibrada | medium | mitigate | Nenhuma calibrada e descartada por nenhuma regra; a ordem (calibradas primeiro) faz a precedencia sair do desempate que `identificar_linhas` ja tem, em vez de uma segunda regra que possa divergir. (Tarefa 2) |
| T-01-11 | Denial of Service | acervo grande degradando o reconhecimento por frame | low | accept | O guloso e O(linhas x assinaturas) por frame, com 4 a 8 linhas e uma captura a ~1 Hz. D-05 mede o crescimento em 562 bytes por assinatura e o usuario aceitou; mil entradas dariam ~550 KB e uma varredura de pasta por arranque, nao por frame — a lista e lida UMA vez, no arranque. |
| T-01-SC | Tampering | instalacao de pacotes | high | accept | Nao ha tarefa de instalacao nesta fase; nenhuma dependencia nova. Sem pacote a auditar, nao ha `[ASSUMED]`/`[SUS]` e nao ha checkpoint de legitimidade. Se alguma tarefa passar a instalar qualquer coisa, esta linha deixa de valer. |
</threat_model>

## Artifacts this phase produces

O inventario COMPLETO da fase esta no plano 01-01, secao
"Artifacts this phase produces". O que ESTE plano acrescenta a ele:

### Arquivos novos

- `tests/test_calibrar_nao_apaga_identidades.py` — irmao declarado de `tests/test_calibrar_nao_apaga_mercado.py`.

### Simbolos com comportamento estendido

| Simbolo | O que muda | Tarefa |
|---------|-----------|--------|
| `acervo.carregar_identidades` | ganha as duas regras de deduplicacao (por chave de conteudo e por nome); a assinatura publica `(list[Assinatura], AcervoDeIdentidades) -> Identidades` NAO muda | T2 |

### Nenhum caminho, pasta ou prefixo novo

Este plano nao cria pasta nem prefixo de arquivo. `.identidades/`,
`assinatura_<hash>.json` e `nome_<hash>` sao todos do plano 01-01.

<verification>
- `python -m pytest tests/ -q` verde, contagem igual ou maior que a linha de base do plano 01-01.
- `python -m pytest tests/test_calibrar_nao_apaga_identidades.py -q` cobre a rodada real nos dois caminhos, a guarda contra prova vazia, a pasta ausente, e os dois portoes estruturais.
- `python -m pytest tests/test_acervo.py -q` cobre as quatro regras de convivencia e o residuo silencioso.
- Nenhum teste existente foi editado para passar.
</verification>

<success_criteria>
- Criterio 1 (DURA-01) e criterio 2 (OPER-02) do ROADMAP sao demonstraveis por comando, com o jogo fechado e sem rede.
- A resposta para "o que acontece quando a calibracao e o acervo carregam a mesma pessoa" esta ESCRITA na docstring de `carregar_identidades`, com o numero (`MARGEM_MINIMA_SOBRE_O_SEGUNDO = 0.12`) que a torna necessaria.
- Somados aos criterios 3 a 7 do plano 01-01, os sete criterios da Fase 1 estao cobertos.
</success_criteria>

<output>
Criar `.planning/workstreams/identidade/phases/01-o-acervo-e-o-silencio-dele/01-02-SUMMARY.md` ao terminar.

Registrar explicitamente no SUMMARY:
1. As pontuacoes medidas no caso do quase-duplicado anonimo (as duas correlacoes e a margem), para que a Fase 2 tenha o numero real e nao a previsao deste plano.
2. A contagem de testes no fim.
</output>
