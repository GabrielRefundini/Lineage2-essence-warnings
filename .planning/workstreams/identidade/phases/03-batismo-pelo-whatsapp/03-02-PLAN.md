---
phase: 03-batismo-pelo-whatsapp
workstream: identidade
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - l2scanner/batismo.py
  - l2scanner/__main__.py
  - tests/test_batismo.py
  - tests/test_comandos.py
autonomous: true
requirements: [BATI-04, BATI-05, OPER-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

decision_ids:
  - id: D-06
    titulo: "Batizar e criar/trocar o irmao `nome_<chave>`; corrigir e a MESMA operacao, nunca um caminho segundo"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-07
    titulo: "A recusa de nome duplicado compara contra os `nome_<chave>` gravados, e a excecao e o PROPRIO alvo"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-08
    titulo: "O NOME PASSA A VALER NO MESMO TICK, sem reiniciar"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31

must_haves:
  truths:
    - "Batizar uma SEGUNDA assinatura com um nome que ja pertence a outra e RECUSADO, a resposta diz QUAL entrada ja tem aquele nome (com o apelido curto), e o acervo fica inalterado: nem a nova nem a antiga mudam. (criterio 4, BATI-04, D-07)"
    - "A excecao da recusa e o PROPRIO alvo: rebatizar a entrada X de `kaus` para `Kaus` e ACEITO, senao a correcao de caixa fica impossivel. (D-07)"
    - "Um batismo errado e corrigido por comando, sem rodar `calibrar.bat`, pela MESMA operacao do batismo — e o acervo termina com uma entrada por pessoa, a corrigida com o nome certo e o nome antigo LIBERADO de volta. (criterio 5, BATI-05, D-06)"
    - "O nome liberado e imediatamente utilizavel: corrigir X de `Mostarda` para `Titander` e em seguida batizar Y como `Mostarda` funciona, e as duas ficam com o nome certo. (criterio 5, BATI-05)"
    - "O batismo e a correcao sao RECUSADOS para um telefone `[[membro]]` e ACEITOS para um telefone de dono, pelo caminho real de leitura e com as cinco travas ligadas. (criterio 6, BATI-04, BATI-05)"
    - "A sintaxe anunciada no `_AJUDA` volta como `Comando.BATIZAR` pelo caminho real de leitura: a ajuda nao tem como ensinar sintaxe que nao funciona. (criterio 2)"
    - "A divida T-02-07 fica com SAIDA: um nome dado a uma entrada de lixo QUEIMA o nome, e a recusa de BATI-04 diz exatamente como liberar de volta, usando o unico comando que existe. Nao ha comando de esquecer, e a resposta nao finge que ha. (T-03-11)"
    - "A divida T-02-18 fica LEGIVEL: as duas entradas da mesma pessoa aparecem como duas linhas da MESMA pergunta, e a recusa de nome duplicado diz em voz alta que elas podem ser a mesma pessoa e que a segunda continuar sem nome nao faz mal. (T-03-12)"
    - "A instancia que NAO obedeceu o comando fica com a entrada ANONIMA ate reiniciar, e anonima CALA: ela nunca vira sujeito de alerta. Afirmado por caso, e nao escondido. (T-03-07)"
    - "As ligacoes de producao que esta fase acrescenta MORREM sob mutacao: arrancar qualquer uma delas do `__main__.py` deixa a suite VERMELHA. A Fase 1 e a Fase 2 pagaram cada uma por uma garantia provada na suite e desligada em campo; a Fase 3 nao paga a terceira."
  artifacts:
    - "tests/test_batismo.py com as familias BATI-04, BATI-05, a fronteira de autorizacao e as guardas das ligacoes"
    - "a recusa de nome duplicado redigida, com o apelido da entrada dona e a saida para liberar"
  key_links:
    - "`acervo.nomeados()` -> a comparacao de D-07 (excluindo o proprio alvo) -> recusa OU `acervo.nomear`"
    - "`acervo.nomear` na correcao -> o nome antigo some do `nomeados()` -> ele volta a ser batizavel"
    - "`autorizado_para(BATIZAR, ...)` -> `BATIZAR not in COMANDOS_DE_MEMBRO` -> False para `[[membro]]`, True para dono"
    - "`atender_comandos(acervo=..., assinaturas_vivas=...)` nos DOIS lacos -> portao AST no molde de `TestNenhumaLigacaoDoAprendizSomeEmSilencio`"
    - "`AcervoDeIdentidades(..., simulando=args.dry_run)` -> portao AST no molde de `TestNenhumRastreadorNasceMudo`"
---

<objective>
O plano 03-01 entregou a espinha: a pergunta sai, o comando batiza, o nome vale.
Este plano fecha as duas metades que sobram — **o nome que ja e de outra pessoa**
(BATI-04) e **o batismo errado** (BATI-05) — e paga as duas dividas que a Fase 2
deixou escritas.

**As tres decisoes travadas que este plano implementa.**

| ID | Decisao travada |
|----|-----------------|
| D-06 | Batizar e criar/trocar o irmao `nome_<chave>`, e a assinatura NAO e tocada. Por isso a correcao do BATI-05 e a MESMA operacao do batismo, e nao um caminho segundo. |
| D-07 | A recusa de nome duplicado compara contra os `nome_<chave>` ja gravados, e a excecao e o PROPRIO alvo. A resposta diz QUAL entrada ja tem o nome, com o apelido curto. |
| D-08 | O nome passa a valer no mesmo tick. Vale igual para a correcao: o nome NOVO entra e o ANTIGO tem de sair da lista viva. |

---

## POR QUE UM COMANDO SO, E POR QUE ISSO NAO E ECONOMIA DE SINTAXE

D-06 diz que a operacao e a mesma: escrever o irmao `nome_<chave>`. Um segundo
comando `/corrigir-<apelido> <nick>` teria a MESMA implementacao com um nome
diferente, e os dois divergiriam na primeira vez que alguem mexesse num deles sem
lembrar do outro — a forma de defeito que este projeto ja nomeou em
`autorizado_para`, em `telefone_equivalente` e em `membro_do_remetente`.

O que muda entre batizar e corrigir nao e a operacao, e a RESPOSTA: quando ja
havia um nome, a resposta diz qual era e diz que ele ficou livre. Isso e uma
redacao derivada do estado, e nao um caminho segundo.

---

## A DIVIDA T-02-07, E POR QUE A SAIDA DELA MORA NA RECUSA DE BATI-04

A Fase 2 aceitou, com as tres consequencias escritas, que um recorte CONTAMINADO
(algo claro por cima do nome) pontua 0.0 contra tudo, passa no veto de D-02 e
pode ser aprendido. A terceira consequencia era exatamente esta fase: *"na Fase 3
essa entrada gera uma pergunta no WhatsApp pedindo ao usuario que batize uma
janela de navegador"*.

O plano 03-01 fechou metade: a pergunta DIZ que, se aquilo nao for gente, e so
nao responder, e D-04 garante que ela nao volta. **Ignorar e o descarte, e ele
so e seguro porque a pergunta e de uma vez so.**

Falta a outra metade, e ela e a mais perigosa: **e se o usuario responder?** Uma
entrada de lixo com o nome `Mostarda` colado nela QUEIMA o nome. Quando a
Mostarda de verdade for aprendida e perguntada, o batismo dela sera RECUSADO por
BATI-04 — corretamente, e com uma mensagem que, mal escrita, seria um beco sem
saida: nao ha comando de esquecer no v1 (adiado na Fase 1, e listado em Deferred
Ideas do CONTEXT).

**A saida existe e usa o unico comando que ha:** batizar a entrada de lixo com
qualquer outro nome libera `Mostarda` de volta. Por isso a recusa de D-07 nao
pode ser so "esse nome ja e de outra": ela tem de dizer **qual** entrada tem o
nome (com o apelido, que e a unica coisa acionavel) e **como** liberar. A
mensagem de recusa e a documentacao desta saida, e nao ha outro lugar onde o
usuario iria procura-la.

---

## A DIVIDA T-02-18, E POR QUE ELA VIRA LEGIVEL EM VEZ DE RESOLVIDA

Medido na Fase 2, mascara de 2000 celulas com 60 pixels de texto, nick `TioMad`:

| celulas de drift | correlacao | desfecho |
|---|---|---|
| 42 | 0.7531 | acima de 0.75, nada nasce |
| 43 | 0.7492 | abaixo, uma SEGUNDA entrada da mesma pessoa nasce |

Uma unica celula separa as duas metades, e **43 celulas sao 71,7% do SINAL DE
TEXTO** daquele nick — num nick curto a fronteira chega muito antes. O modelo de
drift usado ACENDE celulas, entao a tabela e um limite OTIMISTA.

Nao ha conserto honesto sem medida de campo do drift entre sessoes, e ela nao
existe: as duas capturas de party window versionadas sao BYTE A BYTE identicas,
entao compara-las mede uma imagem consigo mesma. **Esta fase nao fecha essa porta,
e nao vai fingir que fecha.**

O que ela faz e trocar o silencio por legibilidade, em dois pontos:

1. **A pergunta.** As duas entradas da mesma pessoa aparecem como duas linhas da
   MESMA mensagem (03-01), uma embaixo da outra, em vez de duas perguntas soltas
   que o usuario le como dois desconhecidos diferentes.
2. **A recusa.** Quando o usuario tenta dar o mesmo nome as duas, a recusa diz em
   voz alta o que provavelmente aconteceu: *eu aprendi o rosto dessa pessoa duas
   vezes*. E diz que a segunda continuar sem nome **nao faz mal** — uma assinatura
   anonima e reconhecida e nunca vira sujeito de alerta (APRE-03, Fase 1). Sem
   essa frase o usuario conclui que o scanner esta quebrado e fica tentando.

---

## O QUE ESTE PLANO NAO REFAZ

`W-01` e `W-02` do `02-VERIFICATION.md` **ja foram fechados** antes desta fase
comecar, e a verificacao esta desatualizada em relacao ao codigo:

- `tests/test_aprendiz.py::TestNenhumaLigacaoDoAprendizSomeEmSilencio` guarda os
  tres elos (`ELO_SESSAO`, `ELO_LISTA_VIVA`, `ELO_AJUSTES`) com portao de AST
  sobre o fonte de producao, mais os dois casos de guarda contra vacuidade e as
  tres mutacoes plantadas;
- `LEITURAS_PARA_APRENDER` esta preso junto com a derivacao dele.

**Nao reimplementar.** O que esta fase deve, e a Tarefa 3 paga, e o mesmo portao
aplicado as ligacoes NOVAS que o plano 03-01 acrescentou — porque a Fase 1 e a
Fase 2 pagaram cada uma por uma garantia provada na suite e desligada em campo, e
a Fase 3 acabou de criar cinco lugares onde isso pode acontecer de novo.

Purpose: sem BATI-04 o acervo aceita dois nomes iguais e os alertas ficam
ambiguos; sem BATI-05 um erro de digitacao vira corrupcao permanente num acervo
que nao tem comando de esquecer.

Output: a recusa de nome duplicado com a saida escrita, a correcao pela mesma
operacao, a fronteira de autorizacao provada nos dois telefones, as duas dividas
herdadas legiveis, e as cinco ligacoes novas nao-mesclaveis por esquecimento.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/identidade/ROADMAP.md
@.planning/workstreams/identidade/phases/03-batismo-pelo-whatsapp/03-CONTEXT.md
@.planning/workstreams/identidade/phases/03-batismo-pelo-whatsapp/03-01-PLAN.md
@.planning/workstreams/identidade/phases/03-batismo-pelo-whatsapp/03-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<interface_context>
O que o plano 03-01 entregou e este plano CONSOME. Se alguma forma abaixo
divergir do que esta em disco, **o disco vence** — leia o `03-01-SUMMARY.md`
antes de comecar.

```python
# l2scanner/acervo.py  (03-01)
PREFIXO_PERGUNTA = "perguntado_"
class AcervoDeIdentidades:
    def __init__(self, pasta: Path, simulando: bool = False) -> None
    def marcar_pergunta(self, chave: str) -> bool     # O_EXCL; OSError -> False (D-05)
    def nomear(self, chave: str, nome: str) -> str    # "nomeado"|"invalido"|"falhou"
    def nomeados(self) -> dict[str, str]              # {chave: nome}, derivado
    def anonimas(self) -> list[str]

# l2scanner/batismo.py  (03-01)
DIGITOS_DO_APELIDO = 6
APELIDO_VALIDO = re.compile(r"[0-9a-f]{1,64}")
def apelido_da_chave(chave: str) -> str
@dataclass(frozen=True) class Resolucao:
    chave: str | None; motivo: str; candidatos: tuple[str, ...]
def resolver(prefixo: str, chaves: Sequence[str]) -> Resolucao
@dataclass(frozen=True) class Pendente: chave: str; indice: int | None
def pendentes_do_acervo(acervo) -> list[Pendente]
def montar_pergunta(acervo, pendentes) -> str | None
@dataclass(frozen=True) class RespostaDoBatismo: privado: str; grupo: str | None = None
def interpretar_batismo(argumento) -> tuple[str, str] | None
def responder_batismo(acervo, argumento, assinaturas_vivas=None,
                      nomes_reservados=None) -> RespostaDoBatismo

# l2scanner/comandos.py  (03-01)
Comando.BATIZAR                       # FORA de COMANDOS_DE_MEMBRO
_AJUDA[Comando.BATIZAR]               # familia "Identidade", sintaxe /batizar <apelido> <nick>
```

**Os dois portoes que esta fase copia, e eles ja existem prontos:**

- `tests/test_acervo.py::TestNenhumRastreadorNasceMudo` — `_chamadas_em_producao`
  varre `l2scanner/*.py` pela arvore sintatica e exige que TODA chamada
  `Rastreador(...)` DECIDA explicitamente sobre um argumento cujo default e
  silencioso. Tem `test_o_detector_acusa_um_caso_plantado`,
  `test_o_detector_reconhece_uma_chamada_que_decide` e
  `test_o_portao_nao_passa_por_vacuidade`. **Este e o molde para o `simulando`.**
- `tests/test_aprendiz.py::TestNenhumaLigacaoDoAprendizSomeEmSilencio` —
  `_elos_do_arranque(fonte, arquivo) -> (achados_por_elo, queixas)` e
  `_elos_em_producao()`, com `ELO_SESSAO`, `ELO_LISTA_VIVA`, `ELO_AJUSTES`, o
  `FONTE_QUE_CUMPRE` fabricado e uma mutacao plantada por elo. **Este e o molde
  para as quatro ligacoes novas.**
- `tests/test_janela_sob_demanda.py::TestNaoEcoaNoGrupo::test_a_flag_e_SETADA_no_ramo_e_nao_herdada`
  — le `inspect.getsource(atender_comandos)`, acha o `ast.If` cujo teste cita o
  membro do enum, e exige `avisar_o_grupo` entre as atribuicoes do corpo.
  **Este e o molde para o ramo do `BATIZAR`.**
- `tests/test_janela_sob_demanda.py::TestFuncionaNosDoisLacos` — o molde para
  exigir que os DOIS lacos passem o mesmo argumento.

**A ARMADILHA DO `_AJUDA`, e ela vai pegar quem nao ler isto.**
`tests/test_comandos.py::test_toda_sintaxe_anunciada_volta_como_o_comando_certo`
roda a tabela `_AJUDA` INTEIRA pelo caminho real de leitura, substituindo
**apenas** `<nick>` e `<hora>`:

```python
texto = linha.sintaxe.replace("<nick>", "J4guar").replace("<hora>", "18:00")
```

A sintaxe anunciada do `BATIZAR` tem um terceiro marcador, `<apelido>`, e ele NAO
e substituido — o teste passaria `/batizar <apelido> J4guar` pelo parser, a
gramatica recusaria, e o caso quebraria. **A correcao e acrescentar a
substituicao**, exatamente como `<hora>` foi acrescentado quando o `.pegou`
nasceu. Isso NAO afrouxa o teste: ele continua rodando o caminho real com as
cinco travas ligadas, e continua provando que a ajuda nao ensina sintaxe que nao
funciona.

E o motivo de a substituicao bastar esta na separacao de responsabilidades que o
`.pegou` ja estabeleceu: `interpretar_dinamico` valida a GRAMATICA
(`interpretar_batismo`), e nunca a existencia da chave. Um apelido que nao casa
nenhuma entrada continua sendo um `Comando.BATIZAR` bem formado; quem descobre
que ele nao existe e `responder_batismo`, e a resposta e uma recusa redigida.

**A regra de precedencia do acervo, que este plano NAO pode contradizer**
(`acervo.carregar_identidades`):

> NAO ACRESCENTAR AQUI nenhuma comparacao de pixels, nenhum limiar novo e nenhum
> "parecido o suficiente". A chave de conteudo E a comparacao (D-01); um segundo
> criterio de igualdade criaria uma segunda definicao de "mesma pessoa" dentro do
> mesmo modulo, e as duas discordariam exatamente nos casos em que a resposta
> importa.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: o nome que ja e de outra pessoa, e o batismo errado que se conserta pela mesma porta</name>
  <precondition>O plano 03-01 esta entregue e a suite esta verde: `python -m pytest tests/ -q` termina sem falhas, com a contagem registrada no `03-01-SUMMARY.md`. Se ja houver vermelho, PARE e reporte.</precondition>
  <files>l2scanner/batismo.py, tests/test_batismo.py</files>
  <read_first>
    - `.planning/workstreams/identidade/phases/03-batismo-pelo-whatsapp/03-01-SUMMARY.md` INTEIRO — a forma real do que foi entregue vence este plano onde divergirem, e o texto exato da pergunta esta registrado la.
    - `l2scanner/batismo.py` — o modulo como o 03-01 o deixou, com atencao ao ponto de extensao que a Tarefa 1 daquele plano deixou marcado em `responder_batismo` (a chamada de `acervo.nomeados()` com o comentario dizendo que a comparacao chega aqui).
    - `l2scanner/acervo.py` — `nomear`, `nomeados` e `_nome_de`, e a docstring de `carregar_identidades` linhas 360-430 INTEIRA: a regra de precedencia, por que a regra do NOME existe alem da regra da chave, e a proibicao de criar um segundo criterio de igualdade.
    - `l2scanner/loot.py` linhas 81-95 — `apelido` e `exibir`. **Nao importar**, e sim ler: `batismo` nao pode importar `loot` (que importa `agenda`), e a razao esta escrita no topo de `acervo.py`. O que interessa aqui e o IDIOMA de comparar nick sem depender da caixa.
    - `tests/test_acervo.py` linhas 360-430 e os casos de `carregar_identidades` — para nao escrever um teste que contradiga a precedencia ja provada.
  </read_first>
  <behavior>
    - **BATI-04, A RECUSA.** O acervo tem a entrada A ja batizada `Mostarda` e a
      entrada B anonima. `/batizar <apelido de B> Mostarda` e RECUSADO. O teste
      afirma as tres coisas: `nome_<chave de B>` NAO existe; `nome_<chave de A>`
      continua com `Mostarda` byte a byte; e a resposta contem o **apelido de A**.
      Um teste que so afirmasse a recusa deixaria passar uma implementacao que
      recusa e mesmo assim escreve.
    - **E A RESPOSTA E ACIONAVEL.** A resposta diz qual entrada tem o nome (o
      apelido) e diz como liberar. Um caso afirma que o texto contem o apelido de
      A e a forma do comando — porque sem a saida escrita a recusa e um beco sem
      saida num acervo que nao tem comando de esquecer.
    - **A EXCECAO E O PROPRIO ALVO (D-07).** A entrada A esta batizada `kaus`.
      `/batizar <apelido de A> Kaus` e ACEITO, e `nome_<chave de A>` passa a ter
      `Kaus`. Sem esta excecao a correcao de caixa seria impossivel, e a entrada
      seria recusada como duplicata dela mesma.
    - **A COMPARACAO IGNORA A CAIXA, E O TESTE DIZ POR QUE.** Com A batizada
      `Mostarda`, `/batizar <apelido de B> mostarda` tambem e recusado. A
      mensagem de falha do teste carrega o argumento: duas entradas chamadas
      `Mostarda` e `mostarda` sao a mesma pessoa para qualquer humano lendo um
      alerta, e deixar as duas passarem produziria dois alertas com nomes que so
      diferem na caixa.
    - **BATI-05, A CORRECAO, E ELA E A MESMA OPERACAO.** A entrada A esta batizada
      `Mostarda` por engano. `/batizar <apelido de A> Titander` e ACEITO,
      `nome_<chave de A>` passa a ter `Titander`, o acervo continua com o mesmo
      NUMERO de entradas, e `assinatura_<chave de A>.json` esta **byte a byte
      identico** — a assinatura nao foi tocada, entao a chave nao mudou (D-06).
    - **E A RESPOSTA DIZ O QUE ERA.** A resposta da correcao cita o nome ANTIGO e
      o NOVO. Sem o antigo, quem digitou nao tem como conferir que corrigiu a
      entrada que queria.
    - **O NOME ANTIGO E LIBERADO DE VOLTA, E ISSO E O CRITERIO 5 INTEIRO.**
      Sequencia num caso so: A e `Mostarda`; corrige A para `Titander`; batiza B
      como `Mostarda`. As duas ultimas chamadas sao ACEITAS, e no fim o acervo tem
      DUAS entradas, uma `Titander` e uma `Mostarda` — uma entrada por pessoa.
      Se o nome nao fosse liberado, a terceira chamada seria recusada.
    - **A CORRECAO VALE NO MESMO TICK, NAS DUAS DIRECOES (D-08).** Depois de
      corrigir A de `Mostarda` para `Titander`: a `Assinatura` da lista viva com
      aquela chave tem `Titander`; `nomes_reservados` contem `Titander`; e o nome
      ANTIGO **saiu** da lista viva. O ultimo e o que quase ninguem escreve, e
      sem ele um `extrair` seguinte ainda casaria a linha como `Mostarda`.
    - **E O `nomes_reservados` NAO E LIMPO DO NOME ANTIGO, E ISSO E DELIBERADO.**
      Um caso afirma que `Mostarda` CONTINUA em `nomes_reservados` depois da
      correcao, com a mensagem explicando: tirar de la faria aquele nome voltar a
      ser emprestado por POSICAO para uma linha nao reconhecida, e o conjunto e
      um conservador — ele so diz "este nome nao serve de rotulo por posicao",
      nunca "esta pessoa esta aqui". Errar para o lado do silencio e a regra do
      projeto.
    - **NADA MUDA QUANDO A RECUSA ACONTECE.** Retrato da pasta byte a byte antes e
      depois de cada recusa desta tarefa, e ele e identico.
    - **A GRAVACAO QUE FALHA NAO MENTE.** Com `acervo.nomear` devolvendo
      `"falhou"`, a resposta diz que nada mudou, e a lista viva e o
      `nomes_reservados` continuam intactos. Um `"falhou"` que atualizasse a lista
      viva faria a tela mostrar um nome que o disco nao tem, e ele sumiria no
      proximo arranque sem explicacao.
  </behavior>
  <action>
Completar `responder_batismo` no ponto de extensao que o 03-01 deixou marcado.
Nao ha modulo novo, nao ha comando novo e nao ha simbolo novo: a operacao ja
existe, e o que falta e a comparacao e as duas redacoes.

**1. A comparacao de D-07, no lugar exato.** Depois de `resolver` ter devolvido
`"ok"` e ANTES de `acervo.nomear`:

- ler `acervo.nomeados()`;
- procurar uma chave DIFERENTE do alvo cujo nome case o nick pedido **por
  `casefold()`**;
- achou: recusar, sem escrever nada.

**A docstring precisa justificar o `casefold()` contra a proibicao que o
`carregar_identidades` escreveu**, porque a primeira leitura e que este e o
"segundo criterio de igualdade" que aquela docstring proibe. Nao e, e a razao e a
DIRECAO do erro:

> `carregar_identidades` compara nomes por igualdade EXATA para decidir se uma
> entrada do acervo duplica uma CALIBRADA. La, um erro para o lado frouxo custa
> silencio (a entrada entra e as duas se sombreiam pela margem), e a regra e
> conservadora de proposito.
>
> Aqui a pergunta e outra: "este nome ja esta ocupado?". Um erro para o lado
> frouxo custa DUAS entradas com nomes que so diferem na caixa, e dois alertas
> que um humano le como a mesma pessoa. Ser MAIS ESTRITO aqui nunca contradiz a
> regra de la — ele so RECUSA mais, e recusar mais nao pode produzir um nome
> errado. As duas regras nao discordam em nenhum caso em que a resposta importa,
> porque a estrita e um subconjunto da frouxa.
>
> E isto NAO e uma comparacao de imagem, nem um limiar, nem um "parecido o
> suficiente". A chave de conteudo continua sendo a unica definicao de "mesma
> pessoa" (D-01); isto e uma regra sobre o NOME, no mesmo registro da regra do
> nome que `carregar_identidades` ja tem.

**2. A recusa, redigida, e ela e a documentacao de duas dividas.** Portugues sem
acento e SEM TRAVESSAO. Ela precisa conter, nesta ordem:

- que o batismo NAO aconteceu e que nada mudou, nem numa entrada nem na outra;
- **qual** entrada ja tem o nome, pelo apelido de `DIGITOS_DO_APELIDO` digitos —
  sem isso o usuario nao tem como agir (D-07);
- **a leitura T-02-18**: que, se as duas forem a mesma pessoa, o scanner aprendeu
  o rosto dela duas vezes, e que a segunda continuar sem nome nao faz mal —
  assinatura sem nome e reconhecida e nunca vira sujeito de alerta;
- **a saida T-02-07**: que, para o nome ficar livre AQUI, e so batizar a outra
  entrada com outro nome. E a unica saida que existe, porque nao ha comando de
  esquecer no v1, e a mensagem de recusa e o unico lugar onde o usuario vai
  procurar por ela.

Um comentario ao lado do texto tem de dizer que essas duas frases NAO sao enfeite:
elas sao a mitigacao escrita de T-03-11 e T-03-12 no `<threat_model>` do 03-01, e
apaga-las reabre as duas dividas.

**3. As duas redacoes de sucesso, derivadas do estado e nao de um segundo
caminho** (D-06):

- **nome novo**: apelido, nick, e que os alertas daquela pessoa passam a sair com
  esse nome.
- **nome trocado**: apelido, nick ANTIGO, nick NOVO, e que o antigo ficou livre.
  A mencao ao antigo e o que permite conferir na hora que a entrada certa foi
  corrigida — o mesmo raciocinio ja escrito no `.pegou`, cuja resposta "sempre diz
  o DIA de volta, para quem digitou conferir na hora que acertou o boss".

O `grupo` das duas e uma linha curta; o `grupo` de toda recusa e `None`.

**4. Os dois elos vivos, na CORRECAO.** O 03-01 escreveu a substituicao na lista
viva e o `add` no `nomes_reservados`. A correcao precisa que a substituicao troque
o nome ANTIGO pelo NOVO na mesma posicao da lista — e ela ja faz isso por
construcao, porque substitui a `Assinatura` inteira pela chave. **Escrever um caso
que prove**, porque uma implementacao que fizesse `append` em vez de substituir
deixaria as duas convivendo, e as duas se sombreariam pela margem: a pessoa
pararia de ser reconhecida, em silencio, que e a pior familia de defeito deste
workstream.

E `nomes_reservados` **nao perde** o nome antigo. Comentario obrigatorio com a
razao: o conjunto e um conservador que so diz "este nome nao serve de rotulo por
POSICAO". Tirar o antigo de la faria ele voltar a ser emprestado para qualquer
linha nao reconhecida, e um nome que ja pertenceu a uma assinatura nunca deveria
voltar a ser um palpite posicional.

E mutar esse conjunto em tempo de execucao e SEGURO, conferido antes de planejar:
`calibracao.py:519` e uma `@property` que constroi um `set` NOVO a cada leitura,
entao o snapshot do rastreador nao e aliasado a nada dentro da `Calibracao`; e nao
ha thread nenhuma em `__main__.py` (o unico `threading.Thread` do projeto vive no
`Despachante` e so consome a fila de envio).

**5. Nos testes, o retrato da pasta e a prova de que a recusa nao escreve.** Reusar
`_retrato_da_pasta` de `tests/test_acervo.py` (dicionario de nome de arquivo para
bytes) e afirmar igualdade antes e depois de cada recusa. Afirmar so a resposta
deixaria passar uma implementacao que recusa por fora e escreve por dentro.

**6. Um caso injeta `"falhou"`** monkeypatchando `acervo.nomear`, e afirma que a
resposta nao promete nada e que os dois containers vivos ficaram intactos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_batismo.py -q</automated>
    <automated>python -m pytest tests/test_acervo.py tests/test_aprendiz.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Batizar B com o nome de A e recusado, `nome_<chave de B>` nao existe, `nome_<chave de A>` continua identico, e a resposta contem o apelido de A.
    - A resposta da recusa contem a forma do comando que libera o nome, e as duas frases das dividas herdadas (a mesma pessoa aprendida duas vezes, e como liberar o nome).
    - Rebatizar a propria entrada com o mesmo nome em outra caixa e ACEITO e grava.
    - `mostarda` contra `Mostarda` e recusado, com a mensagem de falha explicando por que a comparacao ignora a caixa.
    - Corrigir A de `Mostarda` para `Titander` grava, mantem o numero de entradas, e deixa `assinatura_<chave de A>.json` byte a byte identico.
    - A resposta da correcao cita o nome antigo e o novo.
    - A sequencia corrigir-A-e-batizar-B-com-o-nome-antigo termina com duas entradas, uma `Titander` e uma `Mostarda`.
    - Depois da correcao: a lista viva tem UMA assinatura com aquela chave e o nome novo, e nenhuma com o nome antigo; `nomes_reservados` contem o novo E continua contendo o antigo, com a razao na mensagem de falha.
    - O retrato byte a byte da pasta e identico antes e depois de cada recusa desta tarefa.
    - Com `nomear` devolvendo `"falhou"`, a resposta nao promete nada e a lista viva e o `nomes_reservados` ficam intactos.
  </acceptance_criteria>
  <done>Dar duas vezes o mesmo nome deixou de ser possivel, e errar o nome deixou de ser permanente: o mesmo comando que batiza tambem conserta, e a recusa diz, com o apelido na mao, qual entrada ja tem aquele nome e como liberar.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: quem pode batizar, a ajuda que nao ensina sintaxe quebrada, e as duas dividas herdadas afirmadas em vez de escondidas</name>
  <files>tests/test_batismo.py, tests/test_comandos.py</files>
  <read_first>
    - `l2scanner/comandos.py` linhas 250-280 (`COMANDOS_DE_MEMBRO`, a LISTA DE INCLUSAO e a razao de o teste da fronteira ser DERIVADO) e 715-761 (`autorizado_para`, a ordem das duas perguntas e o sentinela `_NAO_RESOLVIDO`).
    - `tests/test_comandos.py` linhas 285-362 — o tripwire do `_AJUDA` e `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` INTEIRO, com a linha de substituicao que esta tarefa precisa estender.
    - `tests/test_comandos.py` linhas 1765-1800 — o teste da fronteira derivado de `set(Comando) - COMANDOS_DE_MEMBRO`.
    - `tests/test_janela_sob_demanda.py` linhas 44-155 — `DONO`, `MEMBRO`, `MEMBROS`, `LeitorDeUmaMensagem` e `perguntar`. Os dois telefones ja foram escolhidos para nao colidirem nos 8 digitos finais, e essa escolha nao pode ser refeita no olho.
    - `l2scanner/acervo.py` linhas 127-154 (`Identidades.configuradas`, `.sem_nome`, `.resumo`) — para o caso do OPER-01 que conta a entrada de lixo.
    - `.planning/workstreams/identidade/phases/02-aprender-sozinho/02-VERIFICATION.md`, secao 4 — os numeros medidos de T-02-18 e T-02-07, que os casos desta tarefa citam.
  </read_first>
  <behavior>
    - **CRITERIO 6, PELO CAMINHO REAL, NOS DOIS TELEFONES.** Com o telefone de
      DONO, `/batizar <apelido> Mostarda` e obedecido e o nome e gravado. Com o
      telefone de um `[[membro]]` declarado, o MESMO texto NAO produz comando
      nenhum, nada e gravado e nenhum despacho sai. O caso roda por
      `comandos_novos` com as cinco travas ligadas, e nao por `autorizado_para`
      isolado — a trava de autorizacao mora na costura.
    - **E A RECUSA DO MEMBRO E SILENCIOSA, E O TESTE DIZ ISSO EM VOZ ALTA.** Zero
      despachos, e a mensagem de falha explica que comando nao autorizado morre no
      `continue` do laco, sem resposta de recusa — e que e exatamente por isso que
      o texto da pergunta (03-01) precisa dizer quem responde.
    - **A FRONTEIRA DERIVADA CONTINUA VALENDO.** `Comando.BATIZAR` aparece em
      `set(Comando) - COMANDOS_DE_MEMBRO`, sem uma linha nova no teste: a prova
      cresceu sozinha, e o caso apenas afirma isso explicitamente para que a
      leitura do arquivo mostre a decisao.
    - **O DONO ALCANCA, MESMO SEM ESTAR EM `[[membro]]`.** Um caso afirma que o
      nivel de dono e ADITIVO, no mesmo registro do que `autorizado_para` ja
      documenta: o dono do scanner nao pode deixar de alcancar um comando so
      porque aquele comando ganhou um segundo publico.
    - **A AJUDA NAO ENSINA SINTAXE QUEBRADA.** A sintaxe anunciada do `BATIZAR`,
      com os marcadores substituidos, volta como `Comando.BATIZAR` pelo caminho
      real; e o apelido anunciado tambem. O tripwire `set(_AJUDA) == set(Comando)`
      segue verde.
    - **T-02-07 AFIRMADO, E NAO ESCONDIDO.** Um caso monta a cadeia inteira: uma
      entrada de lixo e batizada `Mostarda`; a pessoa de verdade e aprendida
      depois e o batismo dela e RECUSADO; a recusa cita o apelido da entrada de
      lixo; batizar a entrada de lixo com outro nome LIBERA `Mostarda`; e o
      batismo da pessoa de verdade passa. A docstring do caso diz que este e o
      preco aceito de T-02-07 e que nao ha comando de esquecer no v1 — **e que
      NAO se conserta este caso acrescentando um**, porque isso foi adiado na
      Fase 1 e esta em Deferred Ideas do CONTEXT.
    - **E O DANO RESIDUAL DE T-02-07 TAMBEM E AFIRMADO.** A entrada de lixo
      continua contando em `Identidades.conhecidas` para sempre. O caso afirma o
      numero e diz na mensagem que isso e conhecido e aceito, e nao um defeito a
      consertar aqui.
    - **T-02-18 AFIRMADO, NA FORMA EM QUE ESTA FASE O TORNA LEGIVEL.** Duas
      entradas anonimas da MESMA pessoa (montadas com a perturbacao medida da
      Fase 2, acima do ponto em que a correlacao cai abaixo de 0.75) produzem UMA
      pergunta com DUAS linhas; o usuario batiza a primeira; a segunda e recusada;
      e a recusa contem a frase que diz que a segunda continuar sem nome nao faz
      mal. A docstring do caso carrega os dois numeros medidos (42 -> 0.7531,
      43 -> 0.7492) e o veto: **NAO "conserte" este caso** — o comportamento e
      conhecido, e se ele comecou a falhar alguem MUDOU o codigo.
    - **T-03-07 AFIRMADO.** Duas listas vivas separadas sobre a MESMA pasta (as
      duas instancias do usuario): o comando e obedecido com uma delas, e a outra
      continua com a assinatura ANONIMA. O caso afirma que a linha correspondente
      na segunda continua produzindo `nome == ""` e rotulo `Membro N`, e ZERO
      eventos — ela CALA, nunca mente. A mensagem diz que este e o preco aceito do
      marcador `comando_<id>` compartilhado, e que o nome chega no proximo
      arranque.
  </behavior>
  <action>
Nao ha codigo de producao nesta tarefa, e isso e deliberado: tudo que ela afirma
ja existe depois da Tarefa 1. O que falta e a PROVA, e duas das provas sao a
unica forma pela qual as dividas herdadas deixam de ser prosa num relatorio.

**1. `tests/test_comandos.py` — a substituicao de `<apelido>`.** Em
`test_toda_sintaxe_anunciada_volta_como_o_comando_certo`, acrescentar
`.replace("<apelido>", "<um apelido hex de exemplo>")` ao lado das duas que ja
existem, e estender a docstring dizendo que o marcador novo nasceu com o batismo,
pela mesma razao pela qual `<hora>` nasceu com o `.pegou`.

**ISTO NAO E AFROUXAR O TESTE, e a docstring precisa dizer por que**, porque a
proxima pessoa a ler o diff vai desconfiar: o caso continua passando pelo caminho
real com as cinco travas ligadas, e continua afirmando que a sintaxe anunciada
volta como o comando certo. O que muda e que a tabela passou a ter um terceiro
marcador. A alternativa — anunciar uma sintaxe sem marcador — ensinaria uma forma
que nao existe, que e exatamente o que este tripwire impede.

**Escolher o apelido de exemplo com cuidado e escrever a escolha:** ele precisa
casar `APELIDO_VALIDO` e NAO pode casar nada mais no parser. Um apelido que
tambem seja um nick plausivel nao e problema aqui (a posicao decide), mas o
comentario deve registrar a auditoria, como o `_VOCABULARIO` manda.

**2. `tests/test_batismo.py` — a classe da fronteira de autorizacao.** Reusar
`DONO`, `MEMBRO` e `MEMBROS` no molde de `tests/test_janela_sob_demanda.py`,
copiados e nao importados (a razao ja esta escrita naquele arquivo). Os casos vao
pelo helper que o 03-01 criou, passando `telefone=`.

**A afirmacao do lado do MEMBRO tem de ser TRIPLA**: nenhum comando obedecido,
nenhum arquivo escrito (retrato da pasta byte a byte) e nenhum despacho. Afirmar
so o primeiro deixaria passar uma implementacao que recusa no lugar errado e
escreve antes.

**3. A classe de T-02-07, e ela e uma HISTORIA e nao um caso solto.** Cinco passos
num caso so, com afirmacao entre cada dois — o molde e
`TestAFronteiraDaDedupe` de `tests/test_aprendiz.py`, que afirma um desfecho
conhecido e o declara aceito na propria mensagem de falha. A docstring precisa:

- citar T-02-07 e a linha do `02-01-PLAN.md` onde ele foi aceito com as tres
  consequencias escritas;
- dizer que a terceira consequencia era esta fase, e que ela foi paga em dois
  lugares: a frase "ignore" no texto da pergunta (03-01) e a saida na recusa
  (Tarefa 1);
- **vetar o conserto errado**: nao acrescentar comando de esquecer, porque isso
  foi adiado na Fase 1 e esta em Deferred Ideas do CONTEXT desta fase.

**4. A classe de T-02-18.** Montar as duas entradas da mesma pessoa com a
perturbacao MEDIDA — vire um numero conhecido de celulas e AFIRME a correlacao
antes de afirmar o desfecho, no idioma do bloco `BITS_VIRADOS` de
`tests/test_acervo.py` e do `quase_igual` que ele usa. Um caso que perturbe "um
pouco" provaria outra coisa.

A docstring carrega os dois numeros da Fase 2 e a leitura que generaliza: 43
celulas sao 2,15% da mascara mas **71,7% do sinal de texto**, e num nick curto a
fronteira chega muito antes; o modelo de drift usado ACENDE celulas, entao a
tabela e um limite OTIMISTA.

**5. A classe de T-03-07.** Duas listas vivas independentes sobre a mesma pasta,
e a prova de que a segunda CALA. Reusar `mortes_apos_zerar` de
`tests/test_acervo.py` para a metade comportamental: zero eventos e rotulo
`Membro N`. Sem essa metade, "continua anonima" seria uma afirmacao sobre um
campo, e nao sobre o que o usuario ve.

**6. Nenhuma destas classes pode ficar num arquivo diferente do resto.** Elas vao
para `tests/test_batismo.py`, junto dos casos que provam o caminho feliz, pela
razao que a Fase 2 ja escreveu: o caso que documenta a fronteira mora ao lado do
caso que documenta o sucesso "para ninguem ler o de cima sozinho e concluir que a
regra vale sempre".
  </action>
  <verify>
    <automated>python -m pytest tests/test_batismo.py -q</automated>
    <automated>python -m pytest tests/test_comandos.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Com o telefone de DONO o batismo e obedecido e grava; com o telefone de um `[[membro]]` nada e obedecido, nada e escrito (retrato byte a byte) e nenhum despacho sai.
    - A mensagem de falha do caso do membro explica que a recusa e SILENCIOSA e por que isso obriga a pergunta a dizer quem responde.
    - Um caso afirma explicitamente que `Comando.BATIZAR` esta em `set(Comando) - COMANDOS_DE_MEMBRO`.
    - Um caso afirma que o dono alcanca o comando sem estar declarado em `[[membro]]`.
    - `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` passa com o marcador `<apelido>` substituido, e a docstring explica que a substituicao nao afrouxa o caso.
    - O tripwire `set(_AJUDA) == set(Comando)` segue verde.
    - A historia de T-02-07 roda os cinco passos e afirma cada um, e a docstring veta o conserto por comando de esquecer citando a Fase 1 e o Deferred Ideas.
    - Um caso afirma que a entrada de lixo continua contando em `Identidades.conhecidas`, declarando isso conhecido e aceito.
    - O caso de T-02-18 afirma a correlacao MEDIDA antes do desfecho, produz UMA pergunta com duas linhas, e afirma que a recusa contem a frase de que a segunda ficar sem nome nao faz mal.
    - O caso de T-03-07 afirma, na segunda lista viva, `nome == ""`, rotulo `Membro N` e ZERO eventos.
    - Nenhum teste existente foi afrouxado. A unica edicao em `tests/test_comandos.py` e a substituicao de marcador, e ela vem com a docstring que explica a diferenca entre estender e afrouxar.
  </acceptance_criteria>
  <done>Um party-mate nao consegue batizar ninguem, a ajuda ensina uma sintaxe que funciona de verdade, e as duas dividas que a Fase 2 deixou por escrito agora tem um caso cada uma que as afirma, as mede e as declara aceitas.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: as cinco ligacoes novas que poderiam sumir em silencio</name>
  <files>tests/test_batismo.py</files>
  <read_first>
    - `tests/test_aprendiz.py` linhas 2795-3090 — `ELO_SESSAO`/`ELO_LISTA_VIVA`/`ELO_AJUSTES`, `_alvo_da_chamada`, `_nomes_ligados_a`, `_elos_do_arranque`, `_elos_em_producao`, `FONTE_QUE_CUMPRE` e a classe `TestNenhumaLigacaoDoAprendizSomeEmSilencio` INTEIRA, com os quatro casos. **E o molde, e ele ja esta pronto: copie a forma, nao reinvente o detector.**
    - `tests/test_acervo.py` linhas 560-645 — `TestNenhumRastreadorNasceMudo` e as duas guardas contra prova vazia. **E o molde para um argumento com DEFAULT SILENCIOSO**, que e o caso do `simulando`.
    - `tests/test_janela_sob_demanda.py` linhas 297-345 (`test_a_flag_e_SETADA_no_ramo_e_nao_herdada`) e 345-400 (`TestFuncionaNosDoisLacos`).
    - `l2scanner/__main__.py` — as regioes que o plano 03-01 alterou: a construcao do acervo nos DOIS lacos, a varredura de arranque, as DUAS chamadas de `atender_comandos` e a construcao da `Sessao`.
  </read_first>
  <behavior>
    - **OS CINCO ELOS ESTAO NO FONTE DE PRODUCAO**, lidos da arvore sintatica de
      `l2scanner/*.py`:
      1. **`Sessao(acervo=...)`** — a `Sessao` de producao recebe o acervo, e nao
         `None`. Sem ele o aprendizado nunca pergunta, e a feature nasce muda em
         campo com a suite verde.
      2. **`atender_comandos(acervo=...)` nos DOIS lacos** — sem ele o batismo
         responde "nao consigo mexer nas identidades agora" e o marcador
         `comando_<id>` ja foi consumido: o comando do usuario e PERDIDO.
      3. **`atender_comandos(assinaturas_vivas=...)` no laco principal** — sem ele
         o nome so vale depois de reiniciar, e D-08 morre em silencio.
      4. **a varredura de arranque** — `montar_pergunta(...)` e chamada em
         `laco_principal`. Sem ela as entradas que ja estao no disco nunca sao
         perguntadas, e a fase inteira nao funciona no unico acervo real que
         existe.
      5. **`AcervoDeIdentidades(..., simulando=...)`** — toda construcao em
         producao DECIDE sobre o argumento. O default e `False`, que e o valor
         silenciosamente perigoso: um `--dry-run` que esqueca de passar queima o
         marcador da instancia real.
    - **O DETECTOR ACUSA CADA MUTACAO PLANTADA.** Um `FONTE_QUE_CUMPRE` fabricado,
      no formato do `laco_principal` real, mais uma mutacao por elo — cinco casos,
      cada um afirmando que a mutacao PEGOU (o fonte mudou) e que o detector
      RECLAMOU.
    - **O DETECTOR NAO ACUSA QUEM CUMPRIU.** O `FONTE_QUE_CUMPRE` intacto passa
      sem queixa, e todos os elos aparecem como achados.
    - **O PORTAO NAO PASSA POR VACUIDADE.** Se nenhum elo for encontrado em
      `l2scanner/`, o teste FALHA dizendo que o detector esta olhando para o lugar
      errado. E a mesma assercao de tres linhas que as duas guardas anteriores
      tem, e a razao e o comando de verificacao que "passava sem rodar nada" do
      plano 10-02.
    - **O RAMO DO `BATIZAR` ATRIBUI `avisar_o_grupo`.** Portao de estrutura sobre
      `inspect.getsource(atender_comandos)`: o `ast.If` cujo teste cita `BATIZAR`
      tem `avisar_o_grupo` entre as atribuicoes do proprio corpo. A mensagem de
      falha diz que sem a atribuicao o ramo herdaria em silencio o valor do
      comando ANTERIOR da mesma volta do laco, e o efeito e resposta privada
      vazando para o grupo.
    - **E ELE E VERDE HOJE, E ISSO E AFIRMADO.** Um caso irmao prova que o
      detector do `avisar_o_grupo` acusaria um ramo fabricado SEM a atribuicao —
      senao o portao passaria por acidente se o `ast.If` nunca fosse encontrado.
  </behavior>
  <action>
Nenhum codigo de producao. Esta tarefa e inteira sobre tornar o ESQUECIMENTO
nao-mesclavel, e ela existe porque o projeto ja pagou o mesmo preco duas vezes:

- **Fase 1**: `Rastreador.assinaturas_configuradas` tinha default `False` e era
  atribuido em OITO lugares, todos em teste. O unico construtor de producao nao
  passava. O silencio do `#linhaN` estava provado na suite e DESLIGADO no jogo,
  desde que a identidade por imagem foi escrita.
- **Fase 2**: tres linhas de `__main__.py` sem guarda nenhuma. A verificacao
  arrancou `aprendiz=aprendiz` e os 3514 testes ficaram VERDES — a feature
  inteira desligada em campo, e nada acusando.

**A Fase 3 acabou de criar cinco lugares novos com exatamente essa forma.**

**1. Copiar o molde, e nao reinventa-lo.** `_elos_do_arranque` e
`_elos_em_producao` de `tests/test_aprendiz.py` ja fazem o trabalho pesado: leem a
arvore sintatica de cada `l2scanner/*.py`, acumulam por elo e devolvem
`(achados, queixas)`. **Escreva o irmao em `tests/test_batismo.py`**, com os cinco
elos desta fase, e nao mexa no de la — os dois portoes guardam fases diferentes, e
fundi-los faria uma queixa de uma aparecer no relatorio da outra.

**AST e nunca `grep`, e a razao ja esta escrita naquele arquivo:** as docstrings
deste projeto citam `aprendiz=` e `acervo=` em prosa ao explicar por que os elos
existem, e uma busca textual acusaria justamente a explicacao.

**2. `FONTE_QUE_CUMPRE`, no formato do `laco_principal` REAL.** Ele precisa conter
as cinco linhas certas, incluindo as duas chamadas de `atender_comandos` (a do
laco principal e a do laco da agenda), para que a mutacao "so um dos lacos" seja
plantavel. **O caso `TestFuncionaNosDoisLacos` de
`tests/test_janela_sob_demanda.py` e o precedente**: logica certa ligada num
caminho so e a familia de defeito que este projeto pagou duas vezes, e o usuario
roda os dois lacos.

**3. As cinco mutacoes plantadas, uma por caso**, cada uma com a mesma forma dos
tres casos da Fase 2: `envenenado = FONTE_QUE_CUMPRE.replace(...)`, seguido de
`assert envenenado != FONTE_QUE_CUMPRE, "a mutacao plantada nao pegou"` — sem essa
linha, um `replace` que nao casa deixaria o caso verde provando nada.

**4. O `simulando` segue o molde do `Rastreador`, e nao o dos elos**, e a docstring
precisa dizer por que a forma e outra: aqui a pergunta nao e "esta ligacao existe",
e sim "toda chamada DECIDE sobre um argumento cujo default e silencioso". A
resposta certa pode ser `False` — o laco da agenda pode legitimamente construir
sem simulacao —, entao o portao exige a DECISAO e nunca um valor.

**Nao tornar o parametro obrigatorio.** Isso quebraria os casos de
`tests/test_acervo.py` e de `tests/test_aprendiz.py` que constroem
`AcervoDeIdentidades(tmp_path)`, e esta fase nao reescreve teste que passa. Quem
quiser o default continua podendo — mas em PRODUCAO tem de escrever a escolha, e
ai ela aparece no diff.

**5. O portao do `avisar_o_grupo`** e uma copia direta de
`test_a_flag_e_SETADA_no_ramo_e_nao_herdada`, com `BATIZAR` no lugar de `JANELA`,
mais o caso irmao que prova que o detector acusa um ramo fabricado sem a
atribuicao. O `raise AssertionError("nao ha ramo para Comando.BATIZAR em
atender_comandos")` no fim do laco e obrigatorio, pela mesma razao: um portao que
nao acha o ramo passa sem provar nada.

**6. A docstring da classe e onde a licao fica escrita**, e ela e o produto mais
duravel desta tarefa. Ela precisa contar as duas falhas anteriores com os numeros
(oito atribuicoes em teste e zero em producao; 3514 testes verdes com a feature
arrancada), dizer que esta e a terceira aplicacao do mesmo molde, e dizer o que o
portao NAO conserta: ele nao muda default nenhum, e nao impede que um teste
construa sem os argumentos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_batismo.py -q</automated>
    <automated>python -m pytest tests/test_aprendiz.py tests/test_acervo.py tests/test_janela_sob_demanda.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Os cinco elos sao encontrados no fonte de producao de `l2scanner/`, e o portao nao produz queixa.
    - Existe um caso por elo com a mutacao plantada, e cada um afirma que a mutacao PEGOU antes de afirmar que o detector reclamou.
    - O `FONTE_QUE_CUMPRE` intacto passa sem queixa e com todos os elos achados.
    - Um caso falha explicitamente quando nenhum elo e encontrado em `l2scanner/`, dizendo que o detector esta olhando para o lugar errado.
    - `AcervoDeIdentidades(...)` em producao decide sobre `simulando`, no molde de `TestNenhumRastreadorNasceMudo`, com os dois casos de guarda contra prova vazia e sem tornar o parametro obrigatorio.
    - O ramo de `Comando.BATIZAR` em `atender_comandos` atribui `avisar_o_grupo`, e existe o caso irmao que prova que o detector acusaria um ramo fabricado sem a atribuicao.
    - A mutacao "so um dos lacos passa o acervo" e plantavel e acusada.
    - `python -m pytest tests/ -q` termina verde, com contagem igual ou maior que a linha de base registrada na `<precondition>` da Tarefa 1.
  </acceptance_criteria>
  <done>Arrancar qualquer uma das cinco ligacoes que esta fase acrescentou ao `__main__.py` deixa a suite VERMELHA. A Fase 1 descobriu em campo que uma garantia provada estava desligada; a Fase 2 descobriu a mesma forma por mutacao; a Fase 3 nao vai descobrir uma terceira vez.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nick vindo do WhatsApp -> espaco de nomes do acervo | Um nome e um recurso EXCLUSIVO a partir desta fase: dar um nome a uma entrada TIRA esse nome de todas as outras, para sempre, num acervo sem comando de esquecer. |
| correcao -> lista viva e snapshot | A correcao troca uma identidade viva em tempo de execucao. Uma troca pela metade deixa duas assinaturas quase iguais na lista, e duas quase iguais se sombreiam pela margem. |
| `[[membro]]` -> comando de escrita duravel | O segundo nivel de autorizacao existe justamente para nao alcancar comandos que reescrevem estado nunca podado. |
| esquecimento humano -> `__main__.py` | Cinco ligacoes novas cujo default e silencioso. A ameaca nao e um comportamento errado; e uma linha que some num refactor sem nada acusar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Spoofing | duas entradas com o mesmo nome | critical | mitigate | BATI-04/D-07: recusa contra `acervo.nomeados()`, com o alvo como excecao. Sem ela, dois alertas com o mesmo nome vindos de pessoas diferentes, e nenhum jeito de saber qual e qual. Provado com retrato byte a byte da pasta antes e depois. (T1) |
| T-03-14 | Spoofing | nomes que so diferem na CAIXA passando pela recusa | high | mitigate | Comparacao por `casefold()`, com o argumento escrito de por que ser mais estrito aqui nao contradiz a igualdade exata de `carregar_identidades`: a regra estrita e um subconjunto da frouxa e so RECUSA mais. (T1) |
| T-03-15 | Tampering | correcao deixando a assinatura antiga na lista viva | high | mitigate | A substituicao e NO LUGAR, pela chave, e ha caso proprio: um `append` deixaria duas quase iguais convivendo, elas se sombreariam pela margem e a pessoa pararia de ser reconhecida em silencio. (T1) |
| T-03-16 | Repudiation | correcao que grava na tela e nao no disco | medium | mitigate | `"falhou"` nao atualiza a lista viva nem o `nomes_reservados`, e a resposta diz que nada mudou. Um nome so na tela sumiria no proximo arranque sem explicacao. (T1) |
| T-03-17 | Elevation of Privilege | nome antigo saindo de `nomes_reservados` e voltando a ser palpite posicional | medium | mitigate | O conjunto NAO perde o nome antigo, de proposito, e ha caso que afirma isso com a razao na mensagem: ele e um conservador que so diz "este nome nao serve de rotulo por POSICAO". (T1) |
| T-03-03 | Elevation of Privilege | party-mate batizando ou corrigindo | high | mitigate | `BATIZAR` fora de `COMANDOS_DE_MEMBRO`, provado pelo caminho real nos DOIS telefones, com afirmacao TRIPLA do lado do membro: nada obedecido, nada escrito, nada despachado. (T2) |
| T-03-11 | Spoofing | nome QUEIMADO por uma resposta a pergunta absurda (T-02-07) | medium | accept | Nao ha comando de esquecer no v1 (adiado na Fase 1, Deferred Ideas do CONTEXT). A saida e a propria recusa de BATI-04, que diz qual entrada tem o nome e como liberar. Historia de cinco passos afirmada em caso, com veto escrito contra "consertar" acrescentando o comando adiado. Dano residual afirmado: a entrada de lixo conta em `Identidades.conhecidas` para sempre. (T2) |
| T-03-12 | Repudiation | a mesma pessoa perguntada duas vezes (T-02-18) | medium | accept | Medido: 42 celulas -> 0.7531 (nada nasce), 43 -> 0.7492 (segunda entrada nasce), e 43 celulas sao 71,7% do sinal de texto. Sem medida de campo do drift entre sessoes nao ha conserto honesto. Mitigado em LEGIBILIDADE: duas linhas na mesma pergunta, e a recusa que diz que a segunda sem nome nao faz mal. Caso com a correlacao MEDIDA antes do desfecho e veto contra "consertar". (T2) |
| T-03-07 | Repudiation | a instancia que nao obedeceu fica com o nome velho | medium | accept | Consequencia do marcador `comando_<id>` compartilhado. A degradacao e segura: a entrada continua ANONIMA, e anonima CALA (APRE-03). Afirmado por caso comportamental — zero eventos e rotulo `Membro N` — e nao por leitura de campo. (T2) |
| T-03-18 | Denial of Service | uma das cinco ligacoes novas sumindo em silencio | high | mitigate | Portao de AST sobre o fonte de producao, no molde ja escrito duas vezes neste projeto, com guarda contra vacuidade e uma mutacao plantada por elo. Mais o portao do `avisar_o_grupo` no ramo novo. (T3) |
| T-03-SC | Tampering | instalacao de pacotes | high | accept | **Nao ha tarefa de instalacao nesta fase.** Nenhuma dependencia nova; tudo e stdlib ou ja esta no projeto. `pyautogui`/`pydirectinput` continuam PROIBIDOS por teste. Se alguma tarefa passar a instalar qualquer coisa, esta linha deixa de valer. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` verde, com contagem igual ou maior que a linha de base do `03-01-SUMMARY.md`.
- `python -m pytest tests/test_batismo.py -q` cobre: a recusa de nome duplicado com o retrato da pasta, a excecao do proprio alvo, a comparacao por caixa, a correcao pela mesma operacao, o nome liberado de volta, os dois elos vivos na correcao, a fronteira de autorizacao nos dois telefones, as tres dividas afirmadas (T-02-07, T-02-18, T-03-07) e os cinco elos de producao com mutacao plantada.
- `python -m pytest tests/test_comandos.py -q` verde, com o tripwire do `_AJUDA` e a sintaxe anunciada do batismo voltando pelo caminho real.
- Nenhum teste existente foi afrouxado. A unica edicao fora de `tests/test_batismo.py` e a substituicao do marcador `<apelido>` em `tests/test_comandos.py`, e ela vem com a docstring que distingue estender de afrouxar.
- `W-01` e `W-02` do `02-VERIFICATION.md` NAO foram reimplementados: eles ja estao fechados em `tests/test_aprendiz.py`, e a verificacao e que esta desatualizada.
</verification>

<success_criteria>
- Criterios 4, 5, 6 e 7 do ROADMAP sao demonstraveis por comando, com o jogo fechado e sem rede.
- O acervo termina com uma entrada por pessoa e um nome por entrada, e nenhum caminho produz dois nomes iguais.
- Um batismo errado e reversivel pelo mesmo comando que o causou, sem `calibrar.bat`.
- As duas dividas que a Fase 2 deixou por escrito tem, cada uma, um caso que as afirma com os numeros medidos e as declara aceitas — nenhuma foi disfarcada de resolvida.
- Arrancar qualquer uma das cinco ligacoes novas do `__main__.py` deixa a suite vermelha.
</success_criteria>

<output>
Criar `.planning/workstreams/identidade/phases/03-batismo-pelo-whatsapp/03-02-SUMMARY.md` ao terminar.

Registrar explicitamente no SUMMARY:
1. A contagem de testes na linha de base e no fim.
2. O TEXTO EXATO da recusa de nome duplicado, copiado do teste — ela e a
   documentacao da unica saida que T-02-07 tem, e o usuario precisa da redacao de
   verdade.
3. Quantas celulas foram viradas para montar o par de T-02-18, e a correlacao
   medida — o numero medido nesta maquina, e nao o da Fase 2 repetido.
4. Quais das cinco mutacoes plantadas exigiram ajuste no detector, e por que.
5. O estado de `W-01`, `W-02` e `W-03` do `02-VERIFICATION.md` no fim da fase.
</output>
