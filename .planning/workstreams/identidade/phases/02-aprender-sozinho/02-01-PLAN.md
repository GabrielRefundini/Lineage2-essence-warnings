---
phase: 02-aprender-sozinho
workstream: identidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/aprendiz.py
  - l2scanner/visao.py
  - l2scanner/sessao.py
  - l2scanner/config.py
  - l2scanner/__main__.py
  - config.toml
  - tests/test_aprendiz.py
  - tests/test_acervo.py
  - tests/test_presenca.py
autonomous: true
requirements: [APRE-01, APRE-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

decision_ids:
  - id: D-01
    titulo: "So e candidata a linha COM_MEMBRO, com ui_visivel, que produziu recorte"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-02
    titulo: "So aprende quando o melhor casamento ficou ABAIXO de LIMIAR_DE_CASAMENTO; falha por MARGEM cala"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-03
    titulo: "A recem-gravada entra no reconhecimento VIVO no mesmo tick"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-04
    titulo: "A estabilidade e julgada sobre a MASCARA DE TEXTO, nunca sobre pixels crus"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-05
    titulo: "Duas leituras sao a mesma quando a distancia de Hamming <= celulas_toleradas, DEFAULT 0"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-06
    titulo: "Teto duro derivado da medida da Fase 1; valor acima e recusado no arranque"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-07
    titulo: "A recusa por instabilidade registra a DISTANCIA MEDIDA"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-08
    titulo: "O contador de estabilidade e POR CONTEUDO, nunca por indice de linha"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31
  - id: D-09
    titulo: "Modulo novo l2scanner/aprendiz.py, SEM RELOGIO, na tupla MODULOS do portao AST"
    estado: travada
    decidida_por: usuario
    data: 2026-08-31

must_haves:
  truths:
    - "Uma linha OCUPADA e NAO RECONHECIDA por N leituras seguidas com o recorte de nome ESTAVEL faz o acervo ganhar EXATAMENTE uma entrada, SEM nome — e aquela linha continua sem anunciar nada: zero eventos e rotulo `Membro N`. (criterio 1, APRE-01, D-01/D-03/D-04)"
    - "A MESMA sequencia com o recorte MUDANDO entre as leituras nao grava NADA: instabilidade e recusada, e nao mediada nem tirada por mediana. (criterio 2, APRE-02, D-08)"
    - "A estabilidade e julgada pelo CONTEUDO da mascara e nunca pelo indice da linha: duas pessoas diferentes passando pela MESMA posicao nunca somam leituras entre si, e a mesma pessoa que troca de posicao continua somando. (criterio 2, APRE-02, D-08)"
    - "Toda recusa por instabilidade registra a DISTANCIA MEDIDA em celulas, junto da tolerancia vigente — o modo de falha de um default errado e AUTO-DIAGNOSTICO, e nao silencio. (D-07)"
    - "Cegueira nao ensina: com `ui_visivel` falso nenhum candidato existe, e a contagem CONGELA em vez de zerar, pelo mesmo motivo que congela em `_contar_linhas_sem_nome`. (D-01)"
    - "Uma `celulas_toleradas` acima do TETO derivado da medida da Fase 1 e RECUSADA no arranque, com mensagem e sem traceback, no precedente de `BossInvalido`. (D-06)"
    - "`l2scanner/aprendiz.py` nao tem relogio proprio, esta na tupla `MODULOS` do portao AST de `tests/test_presenca.py`, e nao importa `visao`, `sessao`, `rastreador` nem `calibracao`. (D-09)"
    - "A mascara que o aprendiz consome viaja na `Observacao` e o `rastreador.py` NAO a le — mesma protecao estrutural de `hp_proprio_aparente` e `mercado_aberto_aparente`. (mitigacao T-02-08)"
    - "Nenhum alerta novo sai desta fase: nada e despachado, nada e anunciado no console. O aprendizado aparece no `ResultadoDoTick` e no `scanner.log`, e em nenhum outro lugar."
    - "A virada de `assinaturas_configuradas` no meio da sessao muda o regime da party INTEIRA e NAO e muda: um `log.warning` sai UMA vez dizendo que linhas nao reconhecidas passam a aparecer como `Membro N`, e um caso atravessando `Sessao` afirma a transicao nas duas metades."
    - "Todo `Aprendizado` carrega a CONFIANCA com que foi decidido, e ela sai no `log.info`: e o unico rastro que torna diagnosticavel uma segunda entrada da mesma pessoa nascida ABAIXO do limiar, que D-02 nao guarda (T-02-18, plano 02-02)."
  artifacts:
    - "l2scanner/aprendiz.py"
    - "tests/test_aprendiz.py"
    - "a secao `[identidade]` do `config.toml`, comentada e com os dois numeros"
  key_links:
    - "`visao.extrair` -> `Observacao.mascaras_de_nome` -> `sessao._candidatas_para_aprender` -> `Aprendiz.observar` -> `AcervoDeIdentidades.gravar`"
    - "`Aprendizado.assinatura` -> `cal.assinaturas` (lista VIVA) -> proximo `extrair` -> `identificar_linhas` (D-03)"
    - "primeira assinatura aprendida -> `rastreador.assinaturas_configuradas = True` -> `_e_so_uma_posicao` / `_rotular` — SEM este elo a recem-aprendida ganha nome por POSICAO"
    - "`config.toml [identidade]` -> `config.ler_ajustes_do_aprendiz` -> `AjustesDoAprendiz.__post_init__` -> teto de D-06"
    - "`aprendiz.py` -> tupla `MODULOS` do portao AST de `tests/test_presenca.py`"
---

<objective>
A linha que ninguem reconhece deixa de ser misterio permanente. Nasce
`l2scanner/aprendiz.py`: quando uma linha OCUPADA fica N leituras seguidas sem
casar com ninguem e com o recorte do nome ESTAVEL, a assinatura dela e gravada
no acervo da Fase 1, SEM nome — e a linha continua exatamente tao calada quanto
era antes.

Esta fase NAO PERGUNTA e NAO BATIZA. Nenhum alerta novo sai dela, nem no
console, nem no WhatsApp. Perguntar e a Fase 3 inteira (BATI-01).

**As nove decisoes travadas no CONTEXT, com os identificadores que este plano
cita.** Nenhuma delas e reaberta aqui; a porta ja foi atravessada.

| ID | Decisao travada |
|----|-----------------|
| D-01 | So e candidata a linha `COM_MEMBRO`, num frame com `ui_visivel` verdadeiro, e que produziu recorte de nome. Cegueira nao ensina. |
| D-02 | So aprende quando o melhor casamento ficou ABAIXO de `LIMIAR_DE_CASAMENTO` (0.75). Falha por MARGEM nao aprende, cala. |
| D-03 | A pessoa recem-gravada entra no reconhecimento VIVO no mesmo tick, acrescentada a lista que `identificar_linhas` recebe. |
| D-04 | A estabilidade e julgada sobre a MASCARA DE TEXTO, nunca sobre os pixels crus. O painel e semitransparente e o cenario anda por tras do texto. |
| D-05 | Duas leituras sao "a mesma" quando a distancia de Hamming entre as mascaras e no maximo `celulas_toleradas`, com DEFAULT 0. |
| D-06 | Ha um TETO DURO derivado da medida da Fase 1, e um valor acima dele e recusado no arranque, com mensagem. |
| D-07 | A recusa por instabilidade e registrada COM A DISTANCIA MEDIDA. E a peca que substitui uma ferramenta de spike. |
| D-08 | O contador de estabilidade e POR CONTEUDO, nunca por indice de linha. Instabilidade REINICIA a sequencia. |
| D-09 | Modulo novo `l2scanner/aprendiz.py`, SEM RELOGIO, na tupla `MODULOS` do portao AST de `tests/test_presenca.py`. |

**A DESCOBERTA QUE DECIDE O TAMANHO DO TRACER, e ela e o coracao do criterio 1.**

Gravar a assinatura NAO BASTA para a linha continuar calada. `Rastreador` decide
entre calar e pegar emprestado o nome da lista por POSICAO olhando um unico
booleano, `assinaturas_configuradas`, e ele e calculado UMA VEZ no arranque
(`__main__.py:2026`, `identidades.configuradas`, entregue pela Fase 1).

Numa instalacao que ainda nao tem assinatura nenhuma — que e exatamente a
instalacao onde esta fase mais importa — esse booleano nasce `False`. Nesse
estado:

- `_rotular` (rastreador.py:405) cai em `self.nome_de(linha.indice)`, ou seja o
  nome da lista por POSICAO;
- `_e_so_uma_posicao` (rastreador.py:403) devolve `False`, e o portao de MORREU
  e RESSUSCITOU deixa passar uma identidade `#linhaN`.

Entao, sem um elo novo, a primeira pessoa aprendida por esta fase seria gravada
como ANONIMA no disco e ANUNCIADA COM O NOME DE OUTRA na tela. O criterio 1
exige as duas metades — "o acervo ganha uma entrada" E "a linha continua sem
anunciar nada" — e a segunda so e verdadeira se, no instante em que a primeira
assinatura passa a existir, `rastreador.assinaturas_configuradas` passar a ser
verdadeiro tambem. E por isso que o tracer atravessa mascara, aprendiz, acervo,
lista viva, rastreador e console: sem o elo do meio, a fase entrega o defeito
que a Fase 1 acabou de consertar, por outro caminho.

**E A GARANTIA NAO E TOTAL, E ISSO PRECISA ESTAR ESCRITO.** O aprendizado roda
DEPOIS do `rastreador.observar` do mesmo tick — posicao deliberada, pela mesma
razao que o bloco do mercado ja tem — entao nas N leituras ate a primeira
assinatura existir a flag ainda e `False`, e uma instalacao sem assinatura
nenhuma continua, nessa janela, com o comportamento de hoje. Esta fase ENCURTA
para N leituras uma janela que hoje dura a sessao inteira; ela nao a fecha. Quem
ler "a linha continua sem anunciar nada" como valendo desde o primeiro frame vai
escrever um teste que nao passa, e concluir que o codigo esta errado quando e a
leitura que esta.

**O DETALHE DE UMA LINHA QUE, ERRADO, DESFAZ O APRE-04 INTEIRO.**

A condicao de candidatura e `linha.nome is None`, e NUNCA `not linha.nome`.

`Casamento.nome` de uma entrada anonima do acervo e a string VAZIA, e nao
`None`. `""` significa "reconheci esta pessoa e ninguem a batizou"; `None`
significa "nao sei quem e". Escrever `not linha.nome` colapsaria os dois: toda
pessoa ja aprendida voltaria a ser candidata em todo tick, o recorte dela mudaria
por uma celula aqui e ali, e o acervo ganharia uma entrada nova a cada N ticks —
para a MESMA pessoa. E o inchaco que o APRE-04 existe para proibir, escrito num
operador.

Purpose: hoje, trocar a composicao da party exige uma rodada de `calibrar.bat`.
Depois desta fase o scanner grava sozinho quem ele nao conhece, e a Fase 3 so
precisa acrescentar o NOME.

Output: `l2scanner/aprendiz.py`, `tests/test_aprendiz.py`, o campo de mascaras na
`Observacao`, o elo do `assinaturas_configuradas` vivo, a secao `[identidade]` do
`config.toml` e o teto de D-06 recusado no arranque.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/identidade/ROADMAP.md
@.planning/workstreams/identidade/REQUIREMENTS.md
@.planning/workstreams/identidade/phases/02-aprender-sozinho/02-CONTEXT.md
@.planning/workstreams/identidade/phases/01-o-acervo-e-o-silencio-dele/01-01-SUMMARY.md
@.planning/workstreams/identidade/phases/01-o-acervo-e-o-silencio-dele/01-02-SUMMARY.md
@.claude/CLAUDE.md

@l2scanner/acervo.py
</context>

<interface_context>
O que ja existe e este plano CONSOME, com a forma exata. Nada aqui muda de
assinatura publica.

```python
# l2scanner/identidade.py
LIMIAR_DE_CASAMENTO = 0.75            # abaixo disto, "nao conheco ninguem parecido"
MARGEM_MINIMA_SOBRE_O_SEGUNDO = 0.12  # acima do limiar mas sem margem: "conheco DOIS parecidos"
PIXELS_MINIMOS_DE_TEXTO = 12          # abaixo disto o recorte nao e um nome, e linha vazia

@dataclass(frozen=True)
class Assinatura:
    nome: str
    mascara: np.ndarray               # 0/1, do recorte do nome
    @property
    def pixels_de_texto(self) -> int  # int(self.mascara.sum())
    @property
    def anonima(self) -> bool         # not self.nome

def mascara_de_texto(bgr: np.ndarray) -> np.ndarray          # hsv[:,:,2] > 180, uint8 0/1
def criar_assinatura(nome: str, recorte: np.ndarray) -> Assinatura
def identificar_linhas(recortes: dict[int, np.ndarray],
                       assinaturas: list[Assinatura]) -> dict[int, Casamento]

# l2scanner/acervo.py  (Fase 1)
def chave_da_assinatura(assinatura: Assinatura) -> str        # sha256 hex, 64 digitos
class AcervoDeIdentidades:
    def __init__(self, pasta: Path) -> None
    def chaves(self) -> list[str]
    def assinaturas(self) -> list[Assinatura]
    def gravar(self, assinatura: Assinatura) -> str            # "criado"|"ja_existia"|"falhou"
def carregar_identidades(calibradas: list[Assinatura],
                         acervo: AcervoDeIdentidades) -> Identidades

# l2scanner/visao.py
@dataclass(frozen=True)
class LeituraDeLinha:
    indice: int
    estado: EstadoDaLinha             # COM_MEMBRO | VAZIA
    hp: float | None
    mp: float | None
    nome: str | None = None           # None = nao sei quem e; "" = reconheci e ninguem batizou
    confianca_do_nome: float = 0.0    # a MELHOR pontuacao, mesmo quando nao ha nome

@dataclass(frozen=True)
class Observacao:                     # frozen, e NENHUM teste a compara com `==`
    indice_do_frame: int
    ui_visivel: bool
    linhas: tuple[LeituraDeLinha, ...]
    hp_proprio: float | None = None
    hp_proprio_aparente: float | None = None      # SO PARA MOSTRAR, com tripwire
    estado_do_cliente: EstadoDoCliente | None = None
    mercado_aberto_aparente: bool | None = None   # SO PARA MOSTRAR, com tripwire

def extrair(frame: Frame, cal: Calibracao) -> Observacao       # funcao PURA
def _recorte_do_nome(pixels, cal, indice) -> np.ndarray | None

# l2scanner/rastreador.py
@dataclass
class Rastreador:
    assinaturas_configuradas: bool = False   # decide entre `Membro N` e o nome por POSICAO
    def observar(self, obs: Observacao, agora: float) -> list[Evento]

# l2scanner/sessao.py
log = logging.getLogger("l2scanner")
class Sessao:
    def tick(self, frame: Frame, momento: float | None = None) -> ResultadoDoTick
    def _contar_linhas_sem_nome(self, observacao) -> None      # so com ui_visivel; cegueira CONGELA
```

**As duas medidas da Fase 1 que este plano usa como numero, e nao como
impressao.** Estao em `tests/test_acervo.py`, no bloco de `BITS_VIRADOS`, sobre
a mascara `20x100` (2000 celulas, 48 pixels de texto) da linha 1 da fixture:

```
bits virados   calibrada   copia    margem    desfecho de identificar_linhas
     1           1.0000    0.9895   0.0105    SILENCIO
     3           1.0000    0.9694   0.0306    SILENCIO
     8           1.0000    0.9212   0.0788    SILENCIO
    12           1.0000    0.8879   0.1121    SILENCIO   (ainda abaixo de 0.12)
    20           1.0000    0.8306   0.1694    o nome SAI
    40           1.0000    0.7237   0.2763    o nome SAI
```

`12` e o MAIOR numero medido de celulas em que o proprio reconhecedor ainda se
RECUSA a distinguir as duas mascaras. Em `20` ele ja distingue. E dessa
transicao que o teto de D-06 e derivado na Tarefa 3 — derivado, e nao escolhido.

O caminho onde o candidato nasce hoje (`l2scanner/visao.py`, dentro de
`extrair`):

```python
recorte = _recorte_do_nome(pixels, cal, i)
if recorte is not None:
    recortes_de_nome[i] = recorte
...
casamentos = identificar_linhas(recortes_de_nome, cal.assinaturas)
```

E os dois portoes de FRONTEIRA DE FASE que a Fase 1 instalou e que ESTA FASE
altera de proposito, com a instrucao escrita na docstring deles
(`tests/test_acervo.py::TestEstaFaseLeENaoAprende`):

- `test_so_dois_modulos_conhecem_o_acervo` — hoje afirma
  `{"acervo.py", "__main__.py"}`. **AJUSTAR.**
- `test_o_laco_real_nao_encosta_no_acervo` — hoje afirma que uma `Sessao` real
  deixa a pasta byte a byte igual. **APAGAR e SUBSTITUIR pelo oposto.**
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: a fatia inteira — a linha desconhecida vira UMA entrada sem nome, e continua calada</name>
  <precondition>A suite esta verde na linha de base antes de comecar: `python -m pytest tests/ -q` termina sem falhas (~3130 passando, 23 skipped ao fim da Fase 1). Se ja houver vermelho, PARE e reporte — este plano nao pode distinguir uma regressao propria de uma herdada.</precondition>
  <files>l2scanner/aprendiz.py, l2scanner/visao.py, l2scanner/sessao.py, l2scanner/__main__.py, tests/test_aprendiz.py, tests/test_acervo.py, tests/test_presenca.py</files>
  <read_first>
    - `l2scanner/acervo.py` INTEIRO — ja esta em `<context>`. Atencao em `gravar` (o tri-estado e a razao de `falhou` nao poder colapsar) e em `carregar_identidades` (as duas regras de deduplicacao, que a Fase 2 nao altera).
    - `l2scanner/identidade.py` linhas 95-135 (as quatro constantes com as medicoes ao lado), 146-200 (`Assinatura`, `anonima`, `como_dict`), 232-256 (`Casamento.identificado`) e 340-460 (`_pontuar_mascara` e `identificar_linhas` inteiro, com o guloso, o corte por limiar e o ramo da margem).
    - `l2scanner/visao.py` linhas 46-155 (`LeituraDeLinha`, `Observacao` e os dois campos "SO PARA MOSTRAR" com a razao inteira do tripwire) e 568-760 (`extrair` de ponta a ponta, com atencao ao `break` do primeiro vao, a coleta de `recortes_de_nome`, a chamada de `identificar_linhas` e o `_truncar_no_primeiro_vao` que roda DEPOIS).
    - `l2scanner/sessao.py` linhas 61-145 (`ResultadoDoTick` e a razao de cada campo ser estruturado), 240-260 (o fim do `__init__` e `ticks_sem_reconhecer`), 280-365 (`tick` inteiro, com a ordem deliberada e o bloco do mercado DEPOIS do rastreador) e 536-553 (`_contar_linhas_sem_nome`, e o congelamento sob cegueira).
    - `l2scanner/rastreador.py` linhas 198-206 (`_chave_da_linha`), 228-240 (`assinaturas_configuradas` e `nomes_reservados`) e 388-425 (`_e_so_uma_posicao` e `_rotular`).
    - `l2scanner/__main__.py` linhas 2010-2045 — a carga do acervo, a atribuicao em memoria e a construcao do `Rastreador`.
    - `tests/test_acervo.py` linhas 60-140 (as fixtures `calibracao`/`pixels`, `assinatura_da_linha`, `semear`, `observar_frame`, `mortes_apos_zerar`) e 925-1035 (os dois portoes de fronteira de fase e a docstring que manda altera-los).
    - `tests/test_mercado_27x.py` linhas 240-265 — o idioma exato do tripwire de arquitetura sobre o fonte do rastreador.
  </read_first>
  <behavior>
    A fatia atravessa mascara -> aprendiz -> acervo -> lista viva -> rastreador ->
    console. Os testes que a definem, em `tests/test_aprendiz.py`:

    - **GUARDA CONTRA PROVA VAZIA, PRIMEIRO.** Com o acervo VAZIO e a calibracao
      trazendo as assinaturas de tres das quatro linhas da fixture, a linha
      restante e a UNICA nao reconhecida: o teste afirma isso ANTES de qualquer
      outra coisa (`nome is None` naquela linha, `nome` nao nulo nas outras
      tres). Sem esta afirmacao, "gravou exatamente uma entrada" passaria
      igualzinho num cenario em que nenhuma linha era candidata e a entrada veio
      de outro lugar.
    - **N-1 leituras nao gravam nada.** Depois de `N-1` ticks estaveis a pasta do
      acervo continua vazia. Esta afirmacao vem ANTES da proxima e existe para
      que "gravou" nao possa ser confundido com "gravou no primeiro frame" — que
      e literalmente o que o APRE-02 proibe.
    - **A enesima leitura grava EXATAMENTE UMA entrada, sem nome.** A pasta tem
      uma chave; o irmao `nome_<chave>` NAO existe; e o conteudo gravado bate
      com a mascara daquela linha.
    - **Continuar rodando nao grava mais nada.** Mais 10 ticks identicos e a
      contagem continua UM. Esta e a metade de D-03 que o criterio 1 cobra.
    - **A linha continua CALADA, e a prova e comportamental.** Rodando o
      rastreador de verdade sobre uma sequencia que confirmaria morte naquela
      linha, saem ZERO eventos, a identidade e `#linhaN` e o rotulo exibido e
      `Membro N` — mesmo com `cal.nomes` cheio de nomes de gente de verdade.
    - **O elo do `assinaturas_configuradas`.** Partindo de uma sessao onde ele
      nasceu `False` (nenhuma assinatura em lugar nenhum), ele e `True` depois do
      tick que aprendeu. E o unico jeito de a afirmacao acima ser verdadeira.
    - **E A TRANSICAO E DELIBERADA E AVISADA.** Um caso atravessando uma `Sessao`
      real, com `cal.nomes` preenchido e NENHUMA assinatura no comeco, afirma as
      duas metades: ANTES do aprendizado uma linha nao reconhecida com HP zerado
      produz uma morte com o nome da lista por posicao (o comportamento de hoje,
      que a suite ja documenta como intencional); DEPOIS do aprendizado a mesma
      linha nao produz evento nenhum e o rotulo e "Membro N". E um `log.warning`
      sai UMA vez no tick da virada, e nao se repete nos ticks seguintes. Sem
      este caso a mudanca de regime da party inteira entraria em campo sem
      ninguem ter escrito que ela e deliberada.
    - **Cegueira nao ensina.** Uma sequencia de N ticks com `ui_visivel` falso
      nao grava nada, e a contagem de estabilidade CONGELA: N-1 ticks visiveis,
      depois 3 cegos, depois 1 visivel identico ao anterior, e a entrada e
      gravada nesse ultimo — a cegueira nao zerou a conta, e tambem nao contou
      como leitura.
    - **Uma linha VAZIA nunca e candidata**, mesmo com `confianca_do_nome` valendo
      0.0 — que e o valor que `identificar_linhas` devolve para ela.
    - **Um recorte quase sem texto nunca e candidato.** Uma mascara com menos de
      `PIXELS_MINIMOS_DE_TEXTO` celulas acesas nao vira entrada nem depois de 100
      leituras perfeitamente estaveis. Este e o caso que a lista de assinaturas
      VAZIA destapa: com `assinaturas=[]`, `identificar_linhas` retorna cedo e
      NENHUM portao de pixel chega a rodar, entao o do aprendiz e o unico que
      existe.
    - **Nada e despachado e nada e anunciado.** O `ResultadoDoTick` do tick que
      aprendeu tem `despachos == []` e `eventos == []`.
    - **O rastreador nao le a mascara.** Tripwire de arquitetura sobre o fonte de
      `l2scanner/rastreador.py`, no idioma de `tests/test_mercado_27x.py`.
    - **O aprendiz nao conhece o mundo.** Portao AST por igualdade de conjuntos:
      os modulos do pacote que `aprendiz.py` importa nao incluem `visao`,
      `sessao`, `rastreador` nem `calibracao` (D-09).
  </behavior>
  <action>
**1. `l2scanner/aprendiz.py` — o modulo novo.** Docstring de modulo no padrao do
projeto: POR QUE, com medida de campo. Ela deve dizer, em blocos com titulo:

- **Por que este modulo conta LEITURAS e nao SEGUNDOS** (D-09): o tick nao e
  garantido — captura ~1 Hz, mas um frame doente, uma cegueira ou uma varredura
  de mercado mudam o intervalo real — e "N segundos" viraria uma dependencia de
  relogio dentro da unica logica nova da fase. O modulo nao tem relogio nenhum, e
  a proibicao esta presa pelo portao AST de `tests/test_presenca.py`.
- **Por que a estabilidade e julgada sobre a MASCARA e nunca sobre os pixels
  crus** (D-04): o texto do nome e opaco e o painel da party window e
  semitransparente. O cenario que anda por tras muda os pixels crus a cada frame
  e nao muda a mascara (`mascara_de_texto` e so `V > 180`). Julgar por pixel cru
  seria julgar o cenario, e num campo aberto de dia o candidato nunca seria
  estavel — a feature simplesmente nao aconteceria, sem erro em lugar nenhum.
- **Por que a gravacao prefere NAO acontecer quando ha duvida**: o acervo e
  IRREVERSIVEL no v1, nao ha comando de esquecer (decisao da Fase 1, e o usuario
  avaliou os 562 bytes por assinatura e dispensou a limpeza). Uma pessoa nao
  aprendida custa um "Membro N" no console; uma entrada de lixo gravada fica para
  sempre e ainda pode sombrear gente de verdade pela margem.
- **O que este modulo NAO conhece**: nao importa `visao`, `sessao`, `rastreador`
  nem `calibracao`. Fala `Assinatura` e `AcervoDeIdentidades`, e so — o mesmo
  isolamento que o `acervo` conquistou. Quem sabe o que e uma linha da party
  window e o `sessao`, e e la que a candidatura por `COM_MEMBRO` e por
  `ui_visivel` mora.

Conteudo, todo em portugues sem acento e sem travessao em texto que o usuario le:

- `LEITURAS_PARA_APRENDER = 5` — o default de N. **A docstring precisa justificar
  o numero contra o que o projeto ja mediu, e nao no olho.** `Ajustes` tem duas
  familias: as baratas de reverter custam 3 leituras
  (`confirmacoes_para_morte`, `confirmacoes_para_entrada`) e as caras custam 5
  (`confirmacoes_para_ressurreicao`, `confirmacoes_para_saida`), e a docstring de
  `Ajustes` chama isso de histerese assimetrica. Gravar num acervo irreversivel
  e a direcao CARA por definicao — nao existe desfazer — entao ela paga o preco
  da familia cara. A ~1 Hz sao ~5 segundos: quem vai ficar horas na party espera
  cinco segundos, e uma linha que pisca por quatro leituras nunca chega a ser
  gravada.
- `@dataclass(frozen=True) class AjustesDoAprendiz`: `leituras_para_aprender: int
  = LEITURAS_PARA_APRENDER`, `celulas_toleradas: int = 0`. O default 0 e D-05, e
  a docstring precisa dizer POR QUE ele e zero: a fase nao tem medicao de campo
  do ruido entre frames consecutivos — nao existe gravacao multi-frame da party
  no repositorio, so imagens soltas — e adotar uma tolerancia inventada seria
  adotar exatamente o tipo de constante que este projeto proibe. O `__post_init__`
  e o teto de D-06 chegam na Tarefa 3; **nesta tarefa a classe ja nasce com a
  forma final** para que a Tarefa 3 acrescente a validacao e nada mais.
- `@dataclass(frozen=True) class Candidata`: `indice: int`, `mascara: np.ndarray`,
  `confianca: float`. O `indice` viaja SO PARA O DIAGNOSTICO — e o que faz a
  linha de log de D-07 poder dizer QUAL linha — e a docstring precisa dizer que
  ele nunca entra em decisao nenhuma, porque a party window compacta quando
  alguem sai e a linha 2 de agora pode ser outra pessoa daqui a um tick (D-08).
- `@dataclass(frozen=True) class Aprendizado`: `chave: str`, `indice: int`,
  `desfecho: str`, `assinatura: Assinatura`, `confianca: float`. Estruturado, e
  nao texto, pelo mesmo motivo que `ResultadoDoTick.despachos` e estruturado: o
  teste afirma estrutura, e a redacao muda toda vez que alguem a melhora.

  **O campo `confianca` nao e enfeite, e a docstring precisa dizer por que.** Ele
  e a melhor pontuacao que aquela linha teve contra tudo que o scanner ja
  conhecia no instante em que decidiu aprender — o dado ja viaja na `Candidata` e
  copia-lo custa uma linha. Ele existe porque D-02 so guarda UMA das duas
  fronteiras: veta acima do limiar, e nao tem nada a dizer sobre uma pessoa que
  volta correlacionando 0.70 contra a propria entrada gravada. Nesse caso ela E
  candidata, e uma SEGUNDA entrada da mesma pessoa nasce — e as duas depois se
  sombreiam pela margem. Nao ha medida de campo do drift entre sessoes para
  fechar essa porta agora (o CONTEXT admite isso ao fixar a tolerancia em zero),
  entao o que esta fase pode fazer e o mesmo que D-07 faz pela recusa: gravar o
  NUMERO junto do fato. Um aprendizado sem confianca registrada, num acervo
  irreversivel, tem como unico modo de falha o silencio. Ver T-02-18 no plano
  02-02.
- `@dataclass(frozen=True) class RecusaPorInstabilidade`: `indice: int`,
  `distancia: int | None`, `tolerado: int`. `distancia` e `None` quando as duas
  mascaras tem FORMA diferente e a distancia de Hamming nao existe. Esta classe e
  a peca de D-07; ela e preenchida na Tarefa 2, e **nesta tarefa ela ja nasce
  declarada e ja e devolvida vazia** — dividir a estrutura entre duas tarefas
  faria a segunda mexer na assinatura publica da primeira.
- `distancia_de_hamming(a: np.ndarray, b: np.ndarray) -> int | None`: `None`
  quando `a.shape != b.shape`; senao `int(np.count_nonzero(a != b))`. Docstring:
  a unidade e CELULA porque e a unidade em que a unica medida que temos foi
  feita (8 e 12 celulas numa mascara de 2000), e comparar contra ela e comparar
  contra o perigo real. Forma diferente nao e "muito diferente", e uma pergunta
  sem sentido: as duas mascaras nao descrevem o mesmo retangulo de tela.
- `class Aprendiz`: `__init__(self, acervo: AcervoDeIdentidades, ajustes:
  AjustesDoAprendiz | None = None)`. Estado interno: uma lista de "vigias", cada
  um com a mascara ANCORA e a contagem de leituras.
  - `observar(self, candidatas: Sequence[Candidata]) -> ResultadoDoAprendiz` —
    ou uma tupla `(list[Aprendizado], list[RecusaPorInstabilidade])`; escolha uma
    forma e mantenha. O corpo, em ordem:
    1. Descartar a candidata cuja mascara tem menos de `PIXELS_MINIMOS_DE_TEXTO`
       celulas acesas. **Docstring obrigatoria neste ponto**, porque o motivo nao
       e obvio: `identificar_linhas` devolve `Casamento(None, 0.0)` para uma
       linha sem texto suficiente, e `0.0` passa folgado na condicao de D-02.
       Pior, com a lista de assinaturas VAZIA — a instalacao nova, que e onde
       esta fase mais importa — `identificar_linhas` retorna cedo e o portao de
       pixel de `_pontuar_mascara` nem chega a rodar. O portao do aprendiz e o
       UNICO que existe nesse caminho, e sem ele a primeira coisa que o scanner
       aprenderia numa instalacao nova seria uma linha vazia.
    2. Descartar a candidata com `confianca >= LIMIAR_DE_CASAMENTO` (D-02). A
       docstring da a tabela de dois motivos do CONTEXT: abaixo do limiar
       significa "nao conheco ninguem parecido com isto" e aprende; no limiar ou
       acima, sem margem, significa "conheco DOIS parecidos demais" e CALA,
       porque aprender ai acrescenta um quase-duplicado e faz a pessoa PARAR de
       ser reconhecida — medido na Fase 1: virando 8 celulas, a original casa
       1.000 e a copia 0.921, diferenca 0.079, abaixo dos 0.12 de
       `MARGEM_MINIMA_SOBRE_O_SEGUNDO`. **A prova exaustiva deste ramo e o plano
       02-02**; aqui basta a regra implementada e um caso unitario, porque
       implementa-la pela metade deixaria este plano com uma condicao que se sabe
       errada.
    3. Casar cada candidata sobrevivente com o vigia MAIS PROXIMO de forma igual.
       Distancia `<= celulas_toleradas` conta como "a mesma leitura" e INCREMENTA
       aquele vigia. Distancia maior recusa, e a recusa e materia da Tarefa 2.
    4. Vigias que nenhuma candidata casou nesta leitura sao DESCARTADOS. Isso
       mantem a estrutura limitada pelo numero de linhas da party window e faz a
       instabilidade REINICIAR em vez de acumular (D-08).
    5. Vigia que chegou a `leituras_para_aprender` grava:
       `acervo.gravar(Assinatura(nome="", mascara=<a mascara ANCORA>))`. Em
       `"criado"` e em `"ja_existia"` o vigia sai da mesa e um `Aprendizado` e
       devolvido; em `"falhou"` o vigia **tambem** sai da mesa e a proxima
       sequencia comeca do zero — e a docstring precisa dizer por que
       `ja_existia` conta como sucesso: as duas instancias do usuario (Yazalaque
       e Faerlina) aprendem sobre a mesma pasta, e tratar `ja_existia` como
       motivo para tentar de novo faria a instancia perdedora da corrida ficar
       tentando para sempre. `falhou` NAO conta como aprendido, exatamente como a
       docstring de `acervo.gravar` antecipou.
  - **A mascara gravada e a ANCORA da sequencia, e nao a ultima leitura.** A
    docstring precisa justificar: com `celulas_toleradas` maior que zero,
    comparar cada leitura com a ANTERIOR deixa uma deriva de uma celula por
    leitura somar `N` celulas ao longo da sequencia, e a sequencia seria chamada
    de estavel com a ultima leitura longe da primeira. Comparando com a ANCORA, a
    deriva total fica limitada pela propria tolerancia — que e o unico numero
    desta fase que tem um teto medido.
  - **Nada de media e nada de mediana.** D-08 e explicito: instabilidade e
    recusada, e nao mediada. Uma assinatura media de duas leituras diferentes e
    uma assinatura de ninguem.

**2. `l2scanner/visao.py` — o campo que carrega o candidato.**

Acrescentar a `Observacao` o campo
`mascaras_de_nome: dict[int, np.ndarray] = field(default_factory=dict)`, com o
bloco de comentario no MESMO registro dos dois vizinhos "SO PARA MOSTRAR", porque
a disciplina e a mesma e a razao e mais forte:

> SO PARA APRENDER. Nenhuma DECISAO do rastreador pode sair deste campo, nunca.
> Ele carrega a mascara de texto do recorte do nome de cada linha, que e o
> material do qual uma assinatura nova e feita (D-04: a estabilidade e julgada
> sobre a mascara, nunca sobre os pixels crus — o painel e semitransparente e o
> cenario anda por tras do texto). A mascara, e nao o recorte cru, tambem por
> desenho: guardar o recorte aqui daria a um consumidor futuro a chance de julgar
> estabilidade por pixel, que e exatamente a leitura que D-04 proibe.
> A seguranca aqui NAO vem de guardas no rastreador — vem de esta leitura NAO
> EXISTIR para ele, a mesma protecao que `hp_proprio_aparente` e
> `mercado_aberto_aparente` ja tem, com o tripwire de arquitetura em
> `tests/test_aprendiz.py`.
>
> ESTE DICIONARIO PODE TER MAIS CHAVES DO QUE `linhas` TEM LINHAS OCUPADAS. Os
> recortes sao coletados enquanto o laco varre as posicoes, e
> `_truncar_no_primeiro_vao` so roda DEPOIS de `identificar_linhas`: uma posicao
> alem do primeiro vao pode ter deixado uma mascara aqui e ter virado `VAZIA`
> ali. A LEITURA CORRETA E SEMPRE `mascaras_de_nome[linha.indice]`, partindo de
> uma linha de `Observacao.linhas` — nunca iterar este dicionario e tratar cada
> chave como se fosse uma pessoa. Hoje isso e inerte porque quem consome parte
> das linhas; escrito aqui para que a Fase 3, que herda o campo, nao descubra
> sozinha.

Dentro de `extrair`, preencher o dicionario ao lado da coleta que ja existe:
para cada `recorte` coletado em `recortes_de_nome`, guardar
`mascara_de_texto(recorte)`. Importar `mascara_de_texto` de `.identidade`, ao
lado do `identificar_linhas` que ja e importado. Recalcular a mascara aqui e
deliberado e barato: `identificar_linhas` ja calcula a dela por dentro, mas
devolver a de la exigiria mudar a assinatura publica de uma funcao que quatro
arquivos de teste exercitam, e um `cvtColor` mais um limiar sobre um recorte de
~20x100 a 1 Hz nao aparece em perfil nenhum.

`extrair` continua sendo uma FUNCAO PURA — mesmo frame, mesma saida — e isso nao
pode mudar. Nada de estado, nada de escrita, nada de acervo dentro de `visao.py`.

**3. `l2scanner/sessao.py` — a candidatura e o laco.**

- `ResultadoDoTick` ganha dois campos, estruturados pelo mesmo motivo dos
  vizinhos: `aprendizados: list = field(default_factory=list)` e
  `recusas_de_aprendizado: list = field(default_factory=list)`. O comentario deve
  dizer que **lista vazia e o estado NORMAL**, e nao um erro: na esmagadora
  maioria dos ticks a party inteira ja e conhecida.
- `Sessao.__init__` ganha `aprendiz=None`, com o comentario no mesmo molde do
  `loot`, do `manutencao`, do `mercado` e do `bosses`: default `None` mantem
  TODA construcao de `Sessao` que ja existe valida sem edicao, e sem ele o tick
  simplesmente nao aprende nada e nada mais muda.
- `Sessao._candidatas_para_aprender(observacao) -> tuple[Candidata, ...]`,
  metodo privado e puro. Devolve vazio quando `not observacao.ui_visivel`
  (D-01). Para cada linha: `estado is EstadoDaLinha.COM_MEMBRO`, `linha.nome is
  None`, e existe mascara para `linha.indice` em `observacao.mascaras_de_nome`.
  **A docstring precisa conter, com todas as letras, a razao de a condicao ser
  `linha.nome is None` e nunca `not linha.nome`** — e o paragrafo esta escrito no
  `<objective>` deste plano; copie o argumento, nao o resuma. E precisa dizer por
  que `ui_visivel` e o portao certo: ele ja cai quando a moldura da barra some
  (outra janela por cima) e quando a party window aparece sem nenhum icone, que
  sao os dois casos em que os pixels da linha nao sao a pessoa. Aprender sob
  cegueira gravaria a janela do navegador como se fosse gente, e a gravacao e
  irreversivel.
- Em `tick`, DEPOIS do bloco do mercado e ANTES do laco que despacha os eventos,
  chamar o aprendizado. **A posicao e a mesma decisao que o comentario do mercado
  ja explica, e pela mesma razao: nesta posicao e ESTRUTURALMENTE IMPOSSIVEL o
  aprendizado influenciar a lista de eventos deste tick, porque ela ja existe.**
  O bloco deve:
  1. Sair cedo se `self.aprendiz is None` ou se `not observacao.ui_visivel` — o
     segundo portao repetindo, de proposito, o mesmo congelamento que
     `_contar_linhas_sem_nome` ja faz: nao dava para ver, entao nao da para
     afirmar nada, nem que reconheceu, nem que deixou de reconhecer. Nao chamar o
     aprendiz e o que CONGELA a contagem em vez de zera-la, e o congelamento e
     seguro justamente porque a contagem e por CONTEUDO: se a pessoa mudou
     durante a cegueira, a mascara muda e o vigia cai na primeira leitura
     visivel.
  2. Chamar `self.aprendiz.observar(...)` dentro de um `try/except Exception`
     que NUNCA levanta, no precedente ja escrito para o mercado
     (`_ja_avisou_da_falha_do_mercado`): uma falha aqui nao pode derrubar o farm,
     e tambem nao pode ficar muda para sempre. Registrar o aviso UMA vez por
     sessao, com um sinalizador `_ja_avisou_da_falha_do_aprendiz`.
  3. Para cada `Aprendizado` com desfecho `"criado"` ou `"ja_existia"`:
     acrescentar `aprendizado.assinatura` a `self.cal.assinaturas` **no lugar**
     (D-03: e a lista VIVA que o proximo `extrair` entrega a
     `identificar_linhas`), e ligar `self.rastreador.assinaturas_configuradas =
     True`.
     **O comentario deste par de linhas e obrigatorio e precisa dizer as duas
     coisas.** A primeira: sem a insercao na lista viva, a mesma pessoa voltaria
     a ser candidata no tick seguinte, o recorte mudaria por uma celula aqui e
     ali, e o acervo ganharia uma entrada nova a cada N ticks para a MESMA pessoa
     — o inchaco que o APRE-04 proibe. A segunda: sem ligar
     `assinaturas_configuradas`, uma instalacao que comecou sem assinatura
     nenhuma continuaria com `_rotular` caindo em `nome_de(indice)`, e a pessoa
     que o scanner acabou de aprender como ANONIMA seria anunciada com o nome de
     OUTRA — o defeito que a Fase 1 consertou, chegando de novo por outra porta.

     **A VIRADA NAO PODE SER MUDA, e este e um requisito e nao um enfeite.** Ela
     muda o comportamento da party INTEIRA, e nao so o da linha aprendida: a
     partir dali `_rotular` devolve "Membro N" para toda linha nao reconhecida,
     `_e_so_uma_posicao` VETA MORREU e RESSUSCITOU para toda identidade
     `#linhaN`, e a purga da chave posicional na lacuna passa a valer. Para
     quem nunca calibrou assinatura mas preencheu `cal.nomes`, o efeito visivel
     e o scanner PARAR de anunciar mortes por nome no meio do farm.

     A virada continua sendo a decisao certa — silencio vence mentira plausivel,
     e e exatamente a mesma regra que ja vale hoje para qualquer instalacao que
     tenha uma assinatura — mas o usuario nao pode descobri-la pela ausencia de
     alertas. Emitir `log.warning` UMA vez, no instante da virada, com um
     sinalizador proprio, dizendo em portugues sem acento e sem travessao que a
     partir de agora ha identidade visual em jogo e que linhas nao reconhecidas
     passam a aparecer como "Membro N" em vez de pegar o nome da lista por
     posicao. `tests/test_identidade.py` documenta o comportamento de destino
     como intencional; o que falta e o aviso da TRANSICAO.
  4. Registrar no `scanner.log` o que aconteceu: para um `Aprendizado`, uma linha
     `log.info` dizendo a chave, a linha em que foi visto, quantos pixels de
     texto a mascara tem e a CONFIANCA com que ele decidiu aprender. O pixel e o
     que torna diagnosticavel, depois, um aprendizado de recorte contaminado
     (T-02-07); a confianca e o que torna diagnosticavel uma segunda entrada da
     MESMA pessoa nascida abaixo do limiar (T-02-18, plano 02-02) — uma sequencia
     de aprendizados com confianca em torno de 0.70 e a assinatura desse caso, e
     sem o numero no log ele e invisivel. O texto do log e portugues sem acento e
     SEM TRAVESSAO.
- **NADA e despachado.** Nao chamar `_despachar`, nao acrescentar a
  `resultado.eventos`, nao mexer no console. Esta fase e CALADA por decisao de
  escopo: perguntar e a Fase 3 (BATI-01).

**4. `l2scanner/__main__.py` — a construcao.** Ao lado da carga do acervo que ja
existe (a mesma instancia de `AcervoDeIdentidades` — construir uma segunda seria
duas verdades sobre a mesma pasta), montar o `Aprendiz` e passa-lo a `Sessao`.

**Os ajustes CHEGAM DE FORA, e nunca sao lidos aqui dentro.** `laco_principal`
ganha um parametro `ajustes_do_aprendiz: AjustesDoAprendiz` e usa o que recebeu.
Nesta tarefa `main()` passa `AjustesDoAprendiz()`, os defaults, para o tracer
fechar sem depender do leitor de configuracao; a Tarefa 3 troca essa unica linha
pela leitura do `config.toml`. A razao de o parametro nascer ja na Tarefa 1 esta
na Tarefa 3: `laco_principal` constroi a fonte de captura logo no comeco, entao
qualquer leitura de configuracao feita LA DENTRO so pode ser exercitada com tela
viva — e o teto de D-06 tem de ser demonstravel com o jogo fechado.

**5. `tests/test_presenca.py`.** Acrescentar `"aprendiz.py"` a tupla `MODULOS` e
estender a docstring da classe com o paragrafo do modulo novo, no mesmo registro
dos que ja estao la: `aprendiz.py` entra ANTES de ter uma linha de logica de
tempo, e a antecipacao E a decisao. Contar LEITURAS e nao SEGUNDOS e o coracao de
D-09; um `time.time()` que aparecesse aqui para "so aprender depois de 5
segundos" transformaria a unica logica nova da fase numa dependencia de relogio,
e o replay de uma gravacao deixaria de produzir o mesmo acervo.

**6. `tests/test_acervo.py` — os dois portoes de fronteira de fase, alterados de
PROPOSITO.** A docstring de `TestEstaFaseLeENaoAprende` manda fazer exatamente
isto; ler antes de mexer.

- `test_so_dois_modulos_conhecem_o_acervo`: o conjunto esperado passa a ser
  `{"acervo.py", "aprendiz.py", "__main__.py"}`. Atualizar o nome da classe e a
  docstring para dizer que a Fase 2 chegou e que `sessao.py` e `visao.py`
  continuam presos do lado de FORA — que era o ponto do portao desde sempre. O
  `sessao.py` fala com o `aprendiz`, e nao com o acervo; o `visao.py` nao fala com
  nenhum dos dois.
- `test_o_laco_real_nao_encosta_no_acervo`: **APAGAR**, e escrever no lugar o
  caso OPOSTO, que e a mesma prova comportamental virada — uma `Sessao` real,
  sobre o mesmo frame da fixture, ESCREVE no acervo, e escreve exatamente uma
  vez. Mantida a mesma guarda contra vacuidade que a Fase 1 acrescentou (o
  ultimo tick produziu observacao). Substituir, e nao so remover: o par de casos
  e o que mantem a afirmacao "o laco toca o acervo do jeito certo" ancorada em
  comportamento, e nao em texto.

**7. `tests/test_aprendiz.py` — o arquivo novo.** Docstring de modulo dizendo o
que esta fase entrega e o que ela deliberadamente NAO entrega (nao pergunta, nao
batiza, nao anuncia), e que o silencio da recem-aprendida e HERDADO da string
vazia da Fase 1 mais o elo do `assinaturas_configuradas`, e nao remendado por
cima. Reusar o idioma de `tests/test_acervo.py`: fixtures `calibracao` e
`pixels`, `_recorte_do_nome`, `criar_assinatura`, e o helper de morte
`mortes_apos_zerar`. Todo caso trabalha dentro de `tmp_path`.

Para montar o cenario "tres conhecidas, uma desconhecida", carregue a
`Calibracao` da fixture com as quatro assinaturas dela e REMOVA a da linha alvo,
afirmando em seguida que a linha alvo e a unica com `nome is None`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_aprendiz.py -q</automated>
    <automated>python -m pytest tests/test_acervo.py tests/test_presenca.py tests/test_sessao.py tests/test_identidade.py tests/test_rastreador.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Com tres das quatro linhas da fixture ja conhecidas, o teste afirma ANTES de tudo que a quarta e a UNICA com `nome is None`; sem essa afirmacao inicial o caso nao prova nada.
    - Depois de `leituras_para_aprender - 1` ticks estaveis a pasta do acervo continua VAZIA.
    - No enesimo tick a pasta ganha EXATAMENTE uma chave, e o irmao `nome_<chave>` NAO existe.
    - Mais 10 ticks identicos deixam a contagem em UM.
    - Rodando o rastreador sobre uma sequencia que confirmaria morte naquela linha saem ZERO eventos, a identidade e `#linhaN` e o rotulo exibido e `Membro N`, com `cal.nomes` cheio de nomes reais.
    - Numa sessao que comecou sem assinatura nenhuma, `rastreador.assinaturas_configuradas` e falso antes e verdadeiro depois do tick que aprendeu.
    - Um caso atravessando `Sessao` afirma a TRANSICAO: antes do aprendizado uma linha nao reconhecida com HP zerado morre com o nome da lista por posicao; depois, a mesma linha nao produz evento e o rotulo e `Membro N`.
    - Um `log.warning` sai UMA vez no tick da virada, dizendo que linhas nao reconhecidas passam a aparecer como `Membro N`, e nao se repete nos ticks seguintes.
    - Uma sequencia de ticks com `ui_visivel` falso nao grava nada; e a sequencia `N-1 visiveis, 3 cegos, 1 visivel identico` grava no ultimo — a cegueira nem contou nem zerou.
    - Uma linha `VAZIA` nunca vira entrada, mesmo com `confianca_do_nome` valendo 0.0.
    - Uma mascara com menos de `PIXELS_MINIMOS_DE_TEXTO` celulas acesas nao vira entrada nem depois de 100 leituras estaveis, inclusive com a lista de assinaturas vazia.
    - O `ResultadoDoTick` do tick que aprendeu tem `despachos` e `eventos` vazios.
    - O fonte de `l2scanner/rastreador.py` nao cita o campo de mascaras da `Observacao`, afirmado por tripwire com mensagem que explica por que apagar o teste nao e a correcao.
    - O conjunto de modulos do pacote importados por `aprendiz.py`, lido da arvore sintatica, nao contem `visao`, `sessao`, `rastreador` nem `calibracao`.
    - `"aprendiz.py"` esta na tupla `MODULOS` de `tests/test_presenca.py` e os dois casos parametrizados passam para ele, inclusive o que injeta um relogio no fonte e exige que o portao acuse.
    - `test_so_dois_modulos_conhecem_o_acervo` afirma `{"acervo.py", "aprendiz.py", "__main__.py"}` e a mensagem de falha explica que `sessao.py` e `visao.py` continuam fora de proposito.
    - `test_o_laco_real_nao_encosta_no_acervo` nao existe mais, e no lugar ha o caso oposto: uma `Sessao` real escreve no acervo exatamente uma vez, com a mesma guarda contra vacuidade.
    - A suite inteira segue verde: nenhum teste existente foi afrouxado, e os dois portoes de fronteira foram alterados pelo motivo que a docstring deles ja mandava.
  </acceptance_criteria>
  <done>Uma pessoa que o scanner nunca viu, sentada numa linha da party window por cinco leituras seguidas, aparece sozinha no acervo como uma entrada sem nome — e a linha dela continua tao muda quanto antes, com "Membro N" no console e nenhum evento em nome de ninguem.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: instabilidade e RECUSADA e nao mediada, e a recusa diz a distancia que mediu</name>
  <files>l2scanner/aprendiz.py, l2scanner/sessao.py, tests/test_aprendiz.py</files>
  <read_first>
    - `l2scanner/aprendiz.py` — o modulo como a Tarefa 1 o deixou, com atencao ao casamento por vigia e ao descarte dos nao casados.
    - `l2scanner/sessao.py` linhas 236-258 — os dois sinalizadores `_ja_avisou_*` do mercado e a razao escrita de eles existirem: um recurso ligado que nunca produz nada e degradacao SILENCIOSA, e repetir o aviso a cada tick e a outra forma de nao ser lido.
    - `l2scanner/identidade.py` linhas 134-145 (`mascara_de_texto`) — para montar, no teste, uma perturbacao que muda a mascara em um numero CONHECIDO de celulas.
    - `tests/test_acervo.py` linhas 1035-1080 — o bloco de `BITS_VIRADOS`, com a tabela medida e a funcao `quase_igual` que vira bits deterministicamente. E o molde a reusar.
  </read_first>
  <behavior>
    - **O recorte que muda entre leituras nao grava NADA.** `N * 3` leituras, cada
      uma com a mascara diferente da anterior, e a pasta continua vazia.
    - **A instabilidade REINICIA, e nao acumula.** Alternar leitura estavel e
      leitura mudada, por muitos ticks, nunca grava: a sequencia nunca chega a N.
    - **E ela nao e mediada.** Depois de uma sequencia instavel seguida de N
      leituras estaveis, a entrada gravada bate BIT A BIT com a mascara dessas N
      leituras — e nao com nenhuma media, mediana ou mistura das anteriores.
    - **D-07, e este e o caso que substitui uma ferramenta de spike.** Cada recusa
      aparece em `ResultadoDoTick.recusas_de_aprendizado` com a DISTANCIA em
      celulas e a tolerancia vigente; e a distancia afirmada e o numero EXATO de
      celulas que o teste virou. Um caso separado, com `caplog`, afirma que esse
      mesmo numero sai no `scanner.log` — porque um campo estruturado que so o
      teste ve nao ajuda o usuario a escolher a tolerancia dele.
    - **Forma diferente nao vira distancia inventada.** Duas mascaras de `shape`
      diferente produzem `distancia is None`, e a recusa continua sendo uma
      recusa.
    - **O CONTEUDO manda, e nao o indice — as duas metades.**
      (a) A mesma pessoa que TROCA de linha entre as leituras continua somando a
      mesma sequencia, e a entrada e gravada na enesima leitura mesmo tendo
      passado por tres posicoes diferentes.
      (b) Duas pessoas DIFERENTES passando pela MESMA posicao nunca somam
      leituras entre si: `N` leituras alternando duas mascaras na linha 2 nao
      gravam nada. Um contador por indice gravaria — e gravaria uma assinatura de
      ninguem.
    - **A tolerancia funciona quando ela e maior que zero.** Com
      `celulas_toleradas` igual a 3 e leituras que diferem em 2 celulas, a
      sequencia SOMA e a entrada e gravada; com leituras que diferem em 4, nao.
      A tolerancia e um limite, e nao uma sugestao.
    - **A ancora e a referencia, e a deriva nao passa por baixo dela.** Com
      `celulas_toleradas` igual a 3 e uma deriva de 2 celulas POR LEITURA — cada
      leitura a 2 celulas da anterior, mas a quinta a 8 celulas da primeira — a
      sequencia e RECUSADA. Comparar com a leitura anterior aceitaria; comparar
      com a ancora recusa, e o teste afirma qual das duas o codigo faz.
    - **A degradacao do log nao vira enxurrada.** Uma sessao com 300 recusas
      seguidas produz um numero de linhas de `scanner.log` que nao cresce com o
      numero de recusas depois que o retrato para de mudar.
  </behavior>
  <action>
Completar a recusa por instabilidade no `Aprendiz`, com o diagnostico que D-07
exige.

**1. A recusa, medida.** Quando uma candidata nao casa com nenhum vigia de forma
igual dentro da tolerancia, produzir uma `RecusaPorInstabilidade(indice,
distancia, tolerado)` onde `distancia` e a MENOR distancia de Hamming entre a
candidata e os vigias de mesma forma que existiam nesta leitura, ou `None` quando
nao havia nenhum vigia de forma igual. A candidata recusada vira um vigia NOVO
com contagem 1 — e essa e a forma exata de "reinicia a sequencia" de D-08.

A docstring da recusa precisa dizer, com todas as letras, por que ela existe:

> Sem este numero, o desfecho de um `celulas_toleradas` errado e a feature
> simplesmente NAO ACONTECER, em silencio, sem nada no log dizendo por que. O
> default e zero porque esta fase nao tem medicao de campo do ruido entre frames
> consecutivos — nao existe gravacao multi-frame da party no repositorio, so
> imagens soltas — e inventar uma tolerancia seria adotar exatamente o tipo de
> constante que este projeto proibe. Registrar a distancia MEDIDA troca esse
> silencio por auto-diagnostico: o `scanner.log` da primeira sessao real diz, com
> numero, o quanto as leituras diferem entre si, e o usuario sobe a tolerancia
> com um numero medido em vez de tentar valores. E por isso que isto e requisito
> de plano, e nao um "nice to have".

**2. O retrato, e a cadencia que nao foi inventada.** O `Aprendiz` acumula, por
sessao, quantas recusas houve e a MENOR, a MAIOR e a MEDIANA das distancias
medidas (ignorando as `None`). O `sessao` emite a linha de resumo no
`scanner.log` **somente quando o retrato MUDA** — a primeira recusa, e depois so
quando aparece um minimo ou um maximo novo.

Escolher a emissao por MUDANCA, e nao a cada K recusas, e deliberado e a
docstring precisa dizer por que: qualquer K seria um numero inventado, e este
plano nao pode acrescentar um. A emissao por mudanca e auto-limitada — as
primeiras leituras registram, e assim que as distancias convergem ela cala
sozinha — e ainda entrega exatamente o que o usuario precisa: a faixa real das
distancias, uma vez, no comeco do log.

O texto do resumo e portugues sem acento e SEM TRAVESSAO, e precisa dizer o que
fazer com o numero: quantas recusas, a faixa em celulas, a tolerancia vigente, e
que subir `[identidade] celulas_toleradas` no `config.toml` para um valor dentro
dessa faixa e a acao. Cada recusa individual vai para `log.debug`, para quem
estiver depurando ter a serie inteira.

**3. `sessao.py`.** Preencher `resultado.recusas_de_aprendizado` e emitir o
resumo, com um sinalizador proprio no mesmo molde de
`_ja_avisou_da_falha_do_mercado`. O aprendizado bem-sucedido continua em
`log.info`, como a Tarefa 1 deixou.

**4. Nos testes, a perturbacao e MEDIDA e nao suposta.** Vire um numero conhecido
de celulas da mascara e AFIRME `distancia_de_hamming` antes de afirmar o
desfecho, no mesmo idioma do `quase_igual` de `tests/test_acervo.py`. Um caso que
perturbe "um pouco" e depois afirme o desfecho estaria provando outra coisa.

Alimentar o `Aprendiz` direto com `Candidata` nos casos unitarios — sem `Sessao`,
sem frame, sem disco alem do `tmp_path` do acervo — e usar a `Sessao` real so nos
dois casos de ponta a ponta. E o mesmo isolamento que D-09 comprou: o aprendiz
nao conhece linha nem frame, entao testa-lo nao deveria exigir nenhum dos dois.
  </action>
  <verify>
    <automated>python -m pytest tests/test_aprendiz.py -q</automated>
    <automated>python -m pytest tests/test_acervo.py tests/test_sessao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `N * 3` leituras com a mascara mudando a cada uma deixam a pasta do acervo VAZIA.
    - Alternar leitura estavel e leitura mudada por muitos ticks nunca grava nada.
    - Depois de uma sequencia instavel seguida de N leituras estaveis, a entrada gravada bate BIT A BIT com a mascara dessas N leituras.
    - Cada recusa aparece em `ResultadoDoTick.recusas_de_aprendizado` com a distancia em celulas igual ao numero EXATO de celulas viradas pelo teste, e com a tolerancia vigente.
    - Um caso com `caplog` afirma que esse mesmo numero aparece no log, e a mensagem diz qual chave do `config.toml` mexer.
    - Duas mascaras de `shape` diferente produzem `distancia is None` e a recusa continua sendo uma recusa.
    - A mesma pessoa passando por tres linhas diferentes ao longo da sequencia GRAVA na enesima leitura.
    - Duas pessoas alternando na MESMA linha nao gravam nada em N leituras; um contador por indice gravaria, e o teste diz isso na mensagem de falha.
    - Com `celulas_toleradas = 3`: leituras a 2 celulas de distancia somam e gravam; a 4 celulas, nao.
    - Com `celulas_toleradas = 3` e deriva de 2 celulas POR LEITURA, a sequencia e RECUSADA — a referencia e a ancora, e nao a leitura anterior.
    - Uma sessao com 300 recusas seguidas nao produz 300 linhas de resumo no log.
  </acceptance_criteria>
  <done>O cenario andando por tras do texto transparente nao vira assinatura: o scanner recusa e diz, com numero, o quanto as leituras diferiram. Um default errado deixa de ser silencio e vira uma linha do log que o usuario consegue agir em cima.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: o teto que vem da medida, e a tolerancia que o usuario consegue mexer</name>
  <files>l2scanner/aprendiz.py, l2scanner/config.py, l2scanner/__main__.py, config.toml, tests/test_aprendiz.py</files>
  <read_first>
    - `l2scanner/config.py` linhas 820-905 — `ler_watchlist_do_mercado` inteiro: o molde de leitor de secao do `config.toml` (arquivo ausente nao e erro, secao ausente nao e erro, TOML quebrado E erro, a mensagem MOSTRA o formato certo) e o `_EXEMPLO_*` escrito uma vez.
    - `l2scanner/config.py` linhas 15-40 — a nota sobre a direcao das importacoes que trouxe `Boss` e `BossInvalido` para `bosses.py`. E o precedente exato que este plano segue.
    - `l2scanner/__main__.py` linhas 2552-2580 — os `except BossInvalido` e `except ConfiguracaoPerigosa`, com o comentario que diz que recusar a subir com mensagem e sem traceback e deliberadamente MELHOR do que o que `AgendaInvalida` faz.
    - `config.toml` linhas 1-30 — o tom dos comentarios: eles ensinam o que o numero faz e o que muda ao mexer nele.
    - `tests/test_acervo.py` linhas 1035-1060 — a tabela medida de `BITS_VIRADOS`, que e de onde o teto e derivado.
  </read_first>
  <behavior>
    - **O teto e derivado, e o codigo diz de onde.** Uma constante do
      `aprendiz.py` com a tabela medida na docstring, e um caso que afirma que a
      docstring cita os TRES numeros que a sustentam: o ponto que ainda e seguro
      (12), o ponto em que o reconhecedor ja distingue (20) e os 48 PIXELS DE
      TEXTO da mascara em que a medida foi feita. O terceiro e a condicao de
      validade: sem ele o teto parece uma propriedade do reconhecedor, quando e
      um limite aferido num nome de tamanho medio.
    - **Acima do teto e recusa, e a recusa e no ARRANQUE.** Construir
      `AjustesDoAprendiz(celulas_toleradas=<teto + 1>)` levanta; e `main()`,
      CHAMADA DE VERDADE com esse valor num `config.toml` de `tmp_path`, devolve
      2, com a mensagem no log e sem excecao escapando — e sem tocar em fonte de
      captura nenhuma, porque a leitura acontece antes de qualquer uma existir.
    - **No teto exato e aceito.** A recusa e sobre passar do teto, e nao sobre
      chegar nele.
    - **Valor negativo e recusado**, e a mensagem diz que a unidade e celula.
    - **`leituras_para_aprender` menor que 1 e recusado**: N igual a zero
      gravaria no primeiro frame, que e literalmente o que o APRE-02 proibe.
    - **O leitor de configuracao herda a disciplina da secao vizinha.** Arquivo
      ausente devolve os defaults; secao ausente devolve os defaults; chave
      ausente devolve o default daquela chave; TOML quebrado levanta; valor de
      tipo errado levanta com a mensagem mostrando a secao pronta para copiar.
      Um booleano NAO passa por inteiro — `True` valendo 1 e a armadilha classica
      do Python, e uma tolerancia `true` significaria uma celula.
    - **O caminho inteiro funciona.** Um `config.toml` de teste com
      `celulas_toleradas` maior que zero produz um `Aprendiz` que de fato tolera
      aquele numero de celulas, provado por comportamento e nao por leitura do
      campo.
  </behavior>
  <action>
**1. O TETO, em `l2scanner/aprendiz.py`.** `TETO_DE_CELULAS_TOLERADAS = 12`, com
a docstring que faz dele uma derivacao e nao uma escolha. Ela precisa carregar a
tabela medida da Fase 1 (`tests/test_acervo.py`, bloco de `BITS_VIRADOS`, mascara
`20x100`, 2000 celulas, **48 pixels de texto**) e o raciocinio:

> A tolerancia diz "estas duas leituras sao a MESMA pessoa". O reconhecedor
> tambem responde essa pergunta, e as duas respostas nao podem se contradizer:
> uma tolerancia maior do que o ponto em que o RECONHECEDOR passa a distinguir
> duas mascaras chamaria de estaveis duas leituras que ele considera pessoas
> diferentes, e a assinatura gravada seria uma media de duas pessoas.
>
> Medido na Fase 1, virando bits de uma mascara de 2000 celulas com 48 PIXELS DE
> TEXTO: em 12 celulas o reconhecedor ainda se RECUSA a distinguir as duas
> (margem 0.1121, abaixo dos 0.12 de `MARGEM_MINIMA_SOBRE_O_SEGUNDO`); em 20 ele
> ja distingue (margem 0.1694). A transicao esta entre 12 e 20, e 12 e o maior
> ponto MEDIDO que ainda cai do lado seguro. O teto e esse numero, e nao um
> arredondamento dele: se um dia a medicao for refeita com mais pontos, e a
> medicao que muda o teto.
>
> **A CONDICAO DE VALIDADE, e ela nao pode ficar de fora.** Os 48 pixels de texto
> sao a BASE da medida, e nao um detalhe da fixture. Doze celulas sao 25% do
> sinal DAQUELA mascara; num nick curto, com 20 pixels de texto, as mesmas 12
> celulas sao 60% do sinal e destroem a assinatura muito antes de o reconhecedor
> chegar perto da faixa medida. Ou seja: este teto e um limite superior aferido
> num nome de tamanho MEDIO, e nao uma propriedade universal do reconhecedor.
> Ele protege contra o erro grosseiro — uma tolerancia de 30, 50 celulas — e nao
> promete seguranca para todo nick em toda tolerancia abaixo dele. Quem subir a
> tolerancia perto do teto com uma party de nicks curtos esta fora da faixa em
> que a medida foi feita. Refazer a medida por faixa de pixels de texto e o
> caminho honesto quando houver gravacao multi-frame de campo; ate la, o default
> continua sendo zero.

**2. `AjustesDoAprendiz.__post_init__`.** Recusa `celulas_toleradas` negativa,
recusa acima de `TETO_DE_CELULAS_TOLERADAS`, e recusa `leituras_para_aprender`
menor que 1. Levanta `ToleranciaAlemDoTeto`, uma excecao NOVA declarada no
proprio `aprendiz.py`.

A validacao mora no `__post_init__`, e nao no leitor do `config.toml`, e a
docstring precisa dizer por que: o teto nao e uma pergunta de sintaxe de arquivo,
e uma propriedade MEDIDA do reconhecedor, e ela tem de valer para TODO caminho de
construcao — inclusive um teste, um script ou um chamador futuro que nunca
encoste no `config.toml`. Validar so na leitura deixaria a porta aberta para
todos os outros.

A mensagem e para o usuario: portugues sem acento, sem travessao, dizendo o valor
recebido, o teto, a unidade (celulas da mascara do nome) e o que fazer.

**3. `l2scanner/config.py` — `ler_ajustes_do_aprendiz(caminho=None) ->
AjustesDoAprendiz`.** Secao `[identidade]`, chaves `leituras_para_aprender` e
`celulas_toleradas`. Copiar a disciplina de `ler_watchlist_do_mercado` linha por
linha: arquivo ausente devolve default, secao ausente devolve default, chave
ausente devolve o default daquela chave, `TOMLDecodeError` vira `AgendaInvalida`,
tipo errado vira `AgendaInvalida` com um `_EXEMPLO_DA_IDENTIDADE` escrito UMA vez
e mostrado em toda recusa. Rejeitar `bool` explicitamente antes de aceitar `int`.

`config.py` importa de `aprendiz.py` e nao o contrario, pelo mesmo argumento de
direcao de importacao ja escrito no topo de `config.py` para `Boss` e
`BossInvalido`: `aprendiz` fala `Assinatura`, `AcervoDeIdentidades` e stdlib, e
nunca `config` — declarar `AjustesDoAprendiz` dentro de `config.py` obrigaria
`aprendiz` a importar `config`, e o ciclo fecharia no primeiro uso. Registrar
essa razao num comentario ao lado do import, no mesmo tom do vizinho.

**4. `l2scanner/__main__.py` — e A POSICAO DA CHAMADA E METADE DO CONSERTO.**

`ler_ajustes_do_aprendiz()` e chamada dentro de **`main()`**, na regiao das outras
leituras de configuracao (por volta de `__main__.py:2500-2533`, junto do
`--so-agenda` e da carga da `Calibracao`), **ANTES de qualquer fonte de captura**,
e o resultado ja validado desce para `laco_principal` pelo parametro que a
Tarefa 1 criou.

**Nao chamar dentro de `laco_principal`.** La a leitura ficaria depois de
`MssSource(cal.party_window, ...)` / `JanelaSource(...)`, e um teste que dirigisse
o caminho para provar "codigo 2 e sem traceback" precisaria de uma fonte de
captura viva. Isso quebraria "demonstravel sem jogo aberto e sem rede"
justamente na unica tarefa desta fase onde essa linha CUSTA alguma coisa. A
recusa por configuracao tem de acontecer antes de o programa olhar para a tela,
porque e antes de olhar para a tela que o usuario ainda esta no console.

Acrescentar `except ToleranciaAlemDoTeto as erro: log.error("%s", erro);
return 2` ao bloco de `main()`, imediatamente ao lado de `BossInvalido` e
`ConfiguracaoPerigosa`, com um comentario curto dizendo que segue esses dois de
proposito: uma tolerancia alem do teto e uma configuracao que produziria
assinaturas de duas pessoas misturadas num acervo irreversivel, e subir com ela
seria pior do que nao subir.

**5. `config.toml` — a secao `[identidade]`, COMENTADA.** No tom dos vizinhos:
explicar o que cada numero faz, o que muda ao mexer, e — para
`celulas_toleradas` — dizer que o valor certo esta no `scanner.log` da primeira
sessao, na linha que o scanner escreve quando recusa aprender por instabilidade.
E a frase que fecha o circuito de D-07: o log produz o numero, e o comentario diz
onde colocar. Mencionar o teto e a razao dele em uma linha.

Os dois valores entram COMENTADOS com os defaults, no molde do
`[mercado] watchlist`: uma secao que o usuario nunca preencheu tem de continuar
funcionando, e um numero escrito no arquivo e um numero que alguem vai achar que
precisa ajustar.

**6. Os testes** vao para `tests/test_aprendiz.py`, com um `config.toml` de
mentira dentro de `tmp_path` (nunca o do repositorio).

**O caso do arranque CHAMA `main()` de verdade** — no molde de `_dirigir` de
`tests/test_calibrar_nao_apaga_identidades.py`, com o `monkeypatch` dos caminhos
de configuracao apontando para `tmp_path` — e afirma que ele devolve `2` e que
nenhuma excecao escapou. Como a leitura agora acontece antes da fonte de captura,
isso roda com o jogo fechado e sem rede.

**PROVAR O `except` POR INSPECAO DA ARVORE ESTA PROIBIDO NESTA TAREFA.** Um teste
que afirme apenas "existe um `except ToleranciaAlemDoTeto` em `main()`" prova que
o bloco EXISTE, nunca que a excecao CHEGA nele: ele continuaria verde no dia em
que alguem envolvesse a leitura de configuracao num `try/except Exception` e a
recusa parasse de subir. D-06 diz que o valor acima do teto e recusado NO
ARRANQUE, e arranque e comportamento, nao topologia de codigo.
  </action>
  <verify>
    <automated>python -m pytest tests/test_aprendiz.py -q</automated>
    <automated>python -m pytest tests/test_config.py tests/test_presenca.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `TETO_DE_CELULAS_TOLERADAS` existe com a tabela medida na docstring, e um caso afirma que a docstring cita os TRES numeros: o ponto que ainda e seguro, o ponto em que o reconhecedor ja distingue, e os 48 pixels de texto da mascara em que a medida foi feita.
    - A docstring do teto diz, em voz alta, que os 48 pixels sao a CONDICAO DE VALIDADE da medida: num nick curto as mesmas celulas sao uma fracao maior do sinal, e o teto protege contra o erro grosseiro sem prometer seguranca para todo nick.
    - `AjustesDoAprendiz(celulas_toleradas=TETO_DE_CELULAS_TOLERADAS)` e ACEITO; `TETO_DE_CELULAS_TOLERADAS + 1` levanta `ToleranciaAlemDoTeto`.
    - `celulas_toleradas` negativa e `leituras_para_aprender` menor que 1 sao recusados, cada um com a propria mensagem.
    - Um `config.toml` ausente, uma secao `[identidade]` ausente e uma chave ausente devolvem os defaults sem levantar.
    - Um TOML quebrado e um valor de tipo errado levantam `AgendaInvalida`, e a mensagem mostra a secao pronta para copiar.
    - `celulas_toleradas = true` e recusado, e nao lido como 1.
    - Um `config.toml` com `celulas_toleradas` maior que zero produz um `Aprendiz` que TOLERA aquele numero de celulas, provado por comportamento.
    - `main()` CHAMADA DE VERDADE, com um valor acima do teto num `config.toml` de `tmp_path`, devolve 2, com a mensagem no log e sem excecao escapando — e o caso roda com o jogo fechado e sem rede porque a leitura acontece antes de qualquer fonte de captura ser construida.
    - Nao existe no arquivo nenhum caso que prove a recusa por INSPECAO da arvore sintatica: um `except` que existe nao e um `except` que recebe.
    - A secao `[identidade]` existe no `config.toml`, comentada, e o comentario diz onde no `scanner.log` achar o numero para `celulas_toleradas`.
    - `python -m pytest tests/ -q` termina verde, com a contagem IGUAL ou MAIOR que a linha de base registrada na `<precondition>` da Tarefa 1.
  </acceptance_criteria>
  <done>A tolerancia deixou de ser um numero enterrado no codigo: o usuario le no log o quanto as leituras dele diferem, escreve o numero no `config.toml`, e o scanner recusa qualquer valor que ja seria grande o bastante para misturar duas pessoas na mesma assinatura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixels da tela -> entrada PERMANENTE no acervo | Esta e a fronteira NOVA da fase, e a mais cara do workstream. Ate ontem o acervo so recebia o que um humano digitou em `calibrar.bat --nomes`; a partir daqui ele recebe o que a tela mostrou. E a gravacao e IRREVERSIVEL: nao ha comando de esquecer no v1. |
| `config.toml [identidade]` -> regra de estabilidade | Um numero que o usuario escreve passa a decidir quando duas imagens sao "a mesma pessoa". Acima do teto medido, ele produz assinaturas que sao a media de duas pessoas. |
| `Observacao.mascaras_de_nome` -> rastreador | Um campo novo de pixels dentro do objeto que alimenta a maquina de estado de morte. A historia do projeto (incidente 27x, `hp_proprio_aparente`) diz exatamente o que acontece quando um sinal "so para mostrar" vira consumidor de decisao. |
| duas instancias -> a mesma pasta | Yazalaque e Faerlina agora ESCREVEM, e nao so leem. O `O_EXCL` da Fase 1 ja cobre a corrida; o que muda e que ela passa a acontecer de verdade. |

**A ameaca honesta desta fase e UMA ENTRADA DE LIXO GRAVADA PARA SEMPRE.** Nao ha
como apaga-la no v1, ela conta na linha de arranque do OPER-01, e — no pior caso
— ela sombra uma pessoa de verdade pela margem e faz essa pessoa parar de ser
reconhecida, em silencio.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | candidatura sob cegueira | high | mitigate | D-01: `ui_visivel` verdadeiro, `COM_MEMBRO` e recorte presente. `ui_visivel` ja cai quando a moldura da barra some (outra janela por cima) e quando a party window aparece sem icone nenhum — os dois casos em que os pixels da linha nao sao a pessoa. Sem isso, a janela do navegador vira uma entrada permanente. (T1) |
| T-02-02 | Spoofing | aprender um quase-duplicado de quem ja e conhecido | high | mitigate | D-02: so aprende com `confianca < LIMIAR_DE_CASAMENTO`; falha por MARGEM cala. Medido na Fase 1: 8 celulas viradas dao 1.000 contra 0.921, margem 0.079, abaixo dos 0.12 — a pessoa PARA de ser reconhecida. Regra implementada na T1; prova exaustiva no plano 02-02. |
| T-02-03 | Spoofing | aprender uma linha vazia ou quase sem texto | high | mitigate | Portao de `PIXELS_MINIMOS_DE_TEXTO` dentro do aprendiz. Com a lista de assinaturas VAZIA, `identificar_linhas` retorna cedo e o portao de pixel de `_pontuar_mascara` nao roda: o do aprendiz e o unico que existe no caminho da instalacao nova. (T1) |
| T-02-04 | Spoofing | contador por INDICE atravessando pessoas diferentes | high | mitigate | D-08: a sequencia e chaveada pelo CONTEUDO da mascara. A party window compacta quando alguem sai, e um contador por indice somaria leituras de pessoas diferentes ate N e gravaria uma assinatura de ninguem. Provado nas duas direcoes na T2. |
| T-02-05 | Elevation of Privilege | uma linha recem-aprendida virando SUJEITO de alerta | critical | mitigate | O elo `assinaturas_configuradas = True` no instante do primeiro aprendizado. Sem ele, `_rotular` cai em `nome_de(indice)` e a anonima recem-gravada e anunciada com o nome de OUTRA pessoa — a mentira plausivel que o `identidade.py` existe para impedir, chegando pela porta nova. Provado por comportamento na T1. |
| T-02-06 | Tampering | tolerancia grande demais misturando duas pessoas numa assinatura | high | mitigate | D-06: teto DERIVADO da medida da Fase 1 (12 celulas seguro, 20 ja distinguivel), validado no `__post_init__` para valer em TODO caminho de construcao, e recusado no arranque com mensagem e sem traceback. (T3) |
| T-02-07 | Spoofing | aprender um recorte CONTAMINADO (algo claro por cima do nome) | medium | accept | O recorte contaminado pontua 0.0 contra tudo (`FATOR_MAXIMO_DE_CONTAMINACAO`), entao passa em D-02 e pode ser aprendido se ficar N leituras parado. **Aceito e REGISTRADO, e nao mitigado por limiar:** nao existe medida de campo para um teto superior de pixels sem uma assinatura de referencia, e inventar uma constante e o que este projeto proibe. O dano NAO e zero, e as tres consequencias precisam estar escritas: (a) uma mascara enorme correlaciona mal com todo mundo, entao ela nao rouba a linha de ninguem — este e o limite do dano; (b) a entrada de lixo CONTA na linha de arranque do OPER-01, e o usuario passa a ler todo dia que o scanner conhece uma pessoa a mais do que conhece, num acervo que nao tem comando de esquecer; (c) na Fase 3 essa entrada gera uma pergunta no WhatsApp pedindo ao usuario que batize uma janela de navegador — e uma pergunta absurda no grupo custa a confianca no recurso inteiro. O `log.info` do aprendizado registra a contagem de pixels, que e o que torna o caso diagnosticavel depois. Reavaliar quando houver gravacao multi-frame de campo; a Fase 3 deve tratar uma entrada nunca respondida como sinal. |
| T-02-08 | Elevation of Privilege | `mascaras_de_nome` virando consumidor de DECISAO no rastreador | high | mitigate | Tripwire de arquitetura sobre o fonte de `rastreador.py`, no molde de `hp_proprio_aparente` e do incidente 27x. A seguranca vem de a leitura NAO EXISTIR para ele. (T1) |
| T-02-09 | Denial of Service | crescimento sem limite da estrutura de vigias | medium | mitigate | Vigias nao casados na leitura sao DESCARTADOS, entao a estrutura fica limitada pelo numero de linhas da party window. Sem isso, um cenario que muda a cada frame criaria um vigia por leitura pela sessao inteira. (T1) |
| T-02-10 | Denial of Service | enxurrada de log de recusa a 1 Hz | low | mitigate | O resumo sai so quando o retrato MUDA; cada recusa individual fica em `log.debug`. Uma linha por segundo no `scanner.log` e a outra forma de nao ser lido, e a razao ja esta escrita nos sinalizadores `_ja_avisou_*` do mercado. (T2) |
| T-02-11 | Repudiation | uma entrada aprendida sem rastro de por que | medium | mitigate | `log.info` no aprendizado com chave, linha e pixels de texto; `ResultadoDoTick.aprendizados` estruturado. Um acervo que nunca e podado precisa que cada entrada tenha uma linha de log explicando de onde veio. (T1/T2) |
| T-02-12 | Denial of Service | falha do aprendiz derrubando o farm | medium | mitigate | O bloco do aprendizado no `tick` nunca levanta, e avisa UMA vez por sessao — no precedente ja escrito de `_ja_avisou_da_falha_do_mercado`. Nem morrer calado nem morrer junto. (T1) |
| T-02-SC | Tampering | instalacao de pacotes | high | accept | **Nao ha tarefa de instalacao nesta fase.** `numpy` ja esta no projeto; `hashlib`, `os`, `re`, `json`, `tomllib`, `logging` e `pathlib` sao stdlib. Nenhuma dependencia nova, e `pyautogui`/`pydirectinput` continuam PROIBIDOS por teste. Sem pacote a auditar, nao ha `[ASSUMED]`/`[SUS]` e nao ha checkpoint de legitimidade. Se alguma tarefa passar a instalar qualquer coisa, esta linha deixa de valer. |
</threat_model>

## Artifacts this phase produces

Inventario da FASE inteira (planos 02-01 e 02-02). Nada fora desta lista e criado
por esta fase.

### Modulos novos

- `l2scanner/aprendiz.py`

### Simbolos novos, com o modulo de origem

| Simbolo | Modulo | Forma |
|---------|--------|-------|
| `LEITURAS_PARA_APRENDER` | `aprendiz` | `int` = 5 (a familia CARA da histerese assimetrica de `Ajustes`) |
| `TETO_DE_CELULAS_TOLERADAS` | `aprendiz` | `int` = 12, DERIVADO da tabela medida da Fase 1 |
| `ToleranciaAlemDoTeto` | `aprendiz` | `Exception`, recusada no arranque com `return 2` |
| `AjustesDoAprendiz` | `aprendiz` | `@dataclass(frozen=True)`: `leituras_para_aprender`, `celulas_toleradas`; valida no `__post_init__` |
| `Candidata` | `aprendiz` | `@dataclass(frozen=True)`: `indice`, `mascara`, `confianca` |
| `Aprendizado` | `aprendiz` | `@dataclass(frozen=True)`: `chave`, `indice`, `desfecho`, `assinatura`, `confianca` |
| `RecusaPorInstabilidade` | `aprendiz` | `@dataclass(frozen=True)`: `indice`, `distancia`, `tolerado` |
| `distancia_de_hamming` | `aprendiz` | `(np.ndarray, np.ndarray) -> int \| None` |
| `Aprendiz` | `aprendiz` | classe: `__init__(acervo, ajustes)`, `observar(candidatas)` |
| `ler_ajustes_do_aprendiz` | `config` | `(Path \| None) -> AjustesDoAprendiz` |
| `SECAO_DA_IDENTIDADE` | `config` | `str` = `"identidade"` |
| `Observacao.mascaras_de_nome` | `visao` | `dict[int, np.ndarray]`, SO PARA APRENDER |
| `ResultadoDoTick.aprendizados` | `sessao` | `list[Aprendizado]` |
| `ResultadoDoTick.recusas_de_aprendizado` | `sessao` | `list[RecusaPorInstabilidade]` |
| `Sessao.aprendiz` | `sessao` | kwarg com default `None` |
| `Sessao._candidatas_para_aprender` | `sessao` | `(Observacao) -> tuple[Candidata, ...]` |

### Arquivos com secao nova

- `config.toml` — `[identidade]`, comentada, com `leituras_para_aprender` e `celulas_toleradas`.

### Arquivos de teste novos

- `tests/test_aprendiz.py` (planos 02-01 e 02-02)

### Portoes estruturais que esta fase instala

| Portao | Arquivo | O que prende |
|--------|---------|--------------|
| Sem relogio proprio | `tests/test_presenca.py` (tupla `MODULOS`) | `aprendiz.py` conta LEITURAS e nunca segundos. Permanente. |
| O aprendiz nao conhece o mundo | `tests/test_aprendiz.py` | `aprendiz.py` nao importa `visao`, `sessao`, `rastreador` nem `calibracao`. Permanente. |
| O rastreador nao le a mascara | `tests/test_aprendiz.py` | `mascaras_de_nome` nunca vira consumidor de decisao. Permanente. |

### Portoes da Fase 1 alterados de PROPOSITO

| Portao | O que muda | Por que |
|--------|-----------|---------|
| `test_so_dois_modulos_conhecem_o_acervo` | `{acervo.py, __main__.py}` -> `{acervo.py, aprendiz.py, __main__.py}` | O aprendiz e o consumidor novo. `sessao.py` e `visao.py` continuam FORA, que sempre foi o ponto. |
| `test_o_laco_real_nao_encosta_no_acervo` | APAGADO e substituido pelo oposto | Aprender no laco e o objetivo desta fase. A docstring da Fase 1 mandava fazer exatamente isto. |

### Deliberadamente NAO produzido nesta fase

- Qualquer pergunta no WhatsApp ao aprender — e a Fase 3 inteira (BATI-01).
- `AcervoDeIdentidades.nomear` e qualquer escrita de `nome_<hash>` — Fase 3.
- Comando para esquecer uma entrada — adiado desde a Fase 1.
- Ferramenta autonoma de medicao do ruido entre frames — adiada no CONTEXT: D-07 entrega o mesmo numero pelo log da primeira sessao real, sem codigo novo.

<verification>
- `python -m pytest tests/ -q` verde, com contagem igual ou maior que a linha de base da `<precondition>` da Tarefa 1.
- `python -m pytest tests/test_aprendiz.py -q` cobre: a fatia de ponta a ponta, o elo do `assinaturas_configuradas`, a cegueira que congela, a linha vazia, o recorte quase sem texto, a instabilidade recusada nas duas direcoes de D-08, a distancia medida no resultado e no log, a ancora contra a deriva, o teto derivado e as recusas de configuracao.
- `python -m pytest tests/test_presenca.py -q` verde com `aprendiz.py` na tupla.
- `python -m pytest tests/test_acervo.py -q` verde com os dois portoes de fronteira alterados de proposito.
- Nenhum teste existente foi editado para passar. Os dois portoes alterados nao sao excecao a essa regra: eles vem com instrucao escrita na propria docstring, redigida pela Fase 1 para este momento.
</verification>

<success_criteria>
- Criterio 1 (APRE-01) e criterio 2 (APRE-02) do ROADMAP sao demonstraveis por comando, com o jogo fechado e sem rede.
- A recusa por instabilidade registra a distancia MEDIDA, e o `config.toml` diz onde achar esse numero: o modo de falha de um default errado e auto-diagnostico, e nao silencio (D-07).
- Nenhum alerta novo saiu desta fase. O scanner aprendeu e continuou calado.
</success_criteria>

<output>
Criar `.planning/workstreams/identidade/phases/02-aprender-sozinho/02-01-SUMMARY.md` ao terminar.

Registrar explicitamente no SUMMARY:
1. A contagem de testes na linha de base e no fim.
2. A distancia de Hamming REAL observada entre duas leituras do mesmo recorte quando ha uma, e o motivo de nao haver, quando nao houver — a Fase 3 e o usuario precisam do numero de campo, e nao da previsao deste plano.
3. Quantas linhas de resumo de recusa o caso de 300 recusas produziu.
4. O conjunto observado pelo portao "quem conhece o acervo" depois do ajuste.
</output>
