---
phase: 01-ponte-viva-conexao-provada-e-texto-real
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - l2scanner/ponte_config.py
  - l2scanner/ponte_nucleo.py
  - l2scanner/ponte_discord.py
  - config.toml
  - ENV-EXEMPLO.txt
  - ponte-discord.bat
  - PORTAO-DISCORD.txt
  - tests/test_ponte_config.py
  - tests/test_ponte_nucleo.py
  - tests/test_ponte_bat.py
autonomous: false
requirements: [PONTE-01, PONTE-02, PONTE-03, PONTE-04, PONTE-05, CONF-01, CONF-02, OPER-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O usuario da dois cliques em ponte-discord.bat e o console diz em qual servidor conectou e quais dois canais esta ouvindo"
    - "Uma mensagem postada num dos dois canais alvo aparece no console com o texto de verdade, em segundos"
    - "Uma mensagem postada em qualquer outro canal do servidor nao aparece"
    - "Uma mensagem de bot ou de webhook nos canais alvo APARECE; so a mensagem que a propria ponte enviou e descartada"
    - "O vigiar-party.bat continua subindo e rodando exatamente como antes"
    - "tests/test_firewall_escopo.py continua verde com a dependencia nova do Discord instalada no .venv"
  artifacts:
    - requirements.txt
    - l2scanner/ponte_config.py
    - l2scanner/ponte_nucleo.py
    - l2scanner/ponte_discord.py
    - config.toml
    - ENV-EXEMPLO.txt
    - ponte-discord.bat
    - PORTAO-DISCORD.txt
    - tests/test_ponte_config.py
    - tests/test_ponte_nucleo.py
    - tests/test_ponte_bat.py
  key_links:
    - "ponte-discord.bat -> .venv/Scripts/python.exe -m l2scanner.ponte_discord"
    - "ponte_discord.Ponte.on_message -> MensagemRecebida -> NucleoDaPonte.receber -> log.info"
    - "ponte_config.ler_token_do_discord -> l2scanner.config.ler_env -> .env DISCORD_TOKEN"
    - "ponte_config.ler_config_da_ponte -> tomllib -> config.toml secao [discord]"
    - "ponte_nucleo.ClienteDiscordEmMemoria -> NucleoDaPonte (o caminho de teste, sem rede e sem discord)"
---

<objective>
A fatia mais fina que atravessa TODAS as camadas da ponte: segredo -> config ->
cliente async do Discord -> nucleo sincrono -> console. Um caminho so, ponta a
ponta, provado antes de qualquer expansao.

Purpose: o modo de falha mais caro deste milestone e a ponte parecer saudavel
replicando branco. Antes de investir em formato (Fase 2) ou entrega (Fase 3),
o usuario precisa VER o texto de verdade aparecendo no console. Esta fatia
existe para chegar nesse momento com o menor codigo possivel.

Output: tres modulos novos, a secao `[discord]` no `config.toml`, a chave
`DISCORD_TOKEN` no `.env`, o `.bat` proprio, o roteiro do portao humano, e a
primeira dependencia de rede do projeto declarada e auditada.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/discord/ROADMAP.md
@.planning/workstreams/discord/REQUIREMENTS.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-RESEARCH.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-PATTERNS.md

@l2scanner/config.py
@l2scanner/console.py
@l2scanner/calibrar.py
@avisos-tvt.bat
@vigiar-party.bat
@requirements.txt
@tests/test_firewall_escopo.py
@tests/test_calibrar_mercado_bat.py
</context>

<decisoes_travadas>
Estas decisoes vieram do orquestrador, da pesquisa e do mapa de padroes. NAO as
re-derive; construa em cima delas.

1. **O `.claude/CLAUDE.md` descreve uma stack que este repositorio NAO usa.**
   Nao existe `requests`, `rich`, `pydantic`, `python-dotenv`, `uv`,
   `pyproject.toml` nem `rapidfuzz`. Tudo que e HTTP, config, segredo e console
   e stdlib pura. Planeje e escreva contra o CODIGO.

2. **O `.venv` e Python 3.12.10** (medido). Em 3.12 o `discord.py` NAO puxa
   `audioop-lts`; em 3.13 puxaria. Nao suba a versao do Python nesta fase.

3. **A chave do `.env` e `DISCORD_TOKEN`**, lida pelo `ler_env` de
   `l2scanner/config.py:40` — o parser de 13 linhas escrito a mao. As chaves
   reais do repo sao `CHATWOOT_URL`, `CHATWOOT_ACCOUNT`, `CHATWOOT_TOKEN`; a
   nova segue esse mesmo formato.

4. **`l2scanner/notificador.py` e `l2scanner/__main__.py` NAO SAO TOCADOS.**
   Nenhuma tarefa desta fase os abre para escrita. O `__main__.py` esta sendo
   editado em paralelo pelo workstream `mercado` na mesma branch.

5. **`discord.py` e declarado DIRETO no `requirements.txt`.** Um segundo
   arquivo trazido por `-r` faz
   `tests/test_firewall_escopo.py::test_as_linhas_que_trazem_pacote_por_fora_sao_RECUSADAS`
   falhar por construcao.

6. **Nao ligue a intent SERVER MEMBERS.** O fallback do `clean_content` via
   `Message.mentions` ja entrega um `Member` com apelido de servidor. Uma
   segunda intent privilegiada seria mais um clique no portao humano por nada.

7. **A DIVISAO EM TRES MODULOS E OBRIGATORIA, E O MOTIVO E MEDIDO.**
   O `pytest` deste repositorio roda no Python **GLOBAL** (3.12.10, com pytest
   9.1.1 e cv2), nao no `.venv` de producao (3.12.10, SEM pytest) — o proprio
   `tests/test_firewall_escopo.py` documenta e depende disso. Um
   `pip install -r requirements.txt` instala o `discord` no `.venv` e **nao** no
   ambiente que roda a suite. Logo, qualquer arquivo de teste que importe
   `discord`, direta ou transitivamente, quebra na COLETA e leva a suite inteira
   junto. Entao:
   - `l2scanner/ponte_config.py` — zero import de terceiro.
   - `l2scanner/ponte_nucleo.py` — zero import de terceiro.
   - `l2scanner/ponte_discord.py` — o UNICO que escreve `import discord`.
   Isto tambem e a decisao "async so na beirada" (precedente `ocr.py:259`)
   virando estrutura em vez de intencao.

8. **Async so na beirada, miolo sincrono.** `on_message` normaliza e chama um
   metodo SINCRONO, no espirito de `Sessao.tick(frame, momento)`
   (`sessao.py:187`). Nao ha `pytest-asyncio` instalado e ele NAO entra.

9. **Relogio e fontes entram por parametro, nunca por monkeypatch** — a lei
   escrita em `relogio.py:25-28` e `relogio.py:83`.

10. **Portugues do Brasil SEM ACENTO** em identificadores, comentarios,
    docstrings e tambem no `PORTAO-DISCORD.txt`. A voz da casa justifica com
    evidencia medida e cita o codigo da exigencia (`PONTE-02`, `CONF-01`), como
    `notificador.py:256-261` e `agenda.py:385-393` fazem.
</decisoes_travadas>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Portao de legitimidade do pacote — confirmar que `discord.py` e o pacote certo</name>
  <what-built>
Nada ainda — este portao vem ANTES da instalacao, de proposito.

`discord.py` sera a PRIMEIRA dependencia de rede da historia deste projeto: ate
hoje ate o cliente HTTP e `urllib` da stdlib. Ela trara 11 distribuicoes para o
`.venv` que roda na mesma maquina do jogo, ao lado do scanner.

A auditoria ja feita (`01-RESEARCH.md`, secao "Package Legitimacy Audit"):
o wheel `discord_py-2.7.1-py3-none-any.whl` foi baixado do PyPI e teve o fonte
lido arquivo a arquivo na pesquisa desta fase; o repositorio
`github.com/Rapptz/discord.py` esta ativo (ultimo push em 2026-07-27, 16158
estrelas, nao arquivado); nenhuma das 11 distribuicoes esta na banlist do
`tests/test_firewall_escopo.py` nem declara uma banida. Os avisos `SUS` do seam
foram `unknown-downloads` e `no-repository`, os dois limitacoes da API do PyPI.

Mesmo assim o portao existe, porque um controle de cadeia de suprimentos que se
auto-aprova nao e controle.
  </what-built>
  <how-to-verify>
Sao dois cliques e uma leitura:

  1. Abra `https://pypi.org/project/discord.py/` e confira que a pagina mostra a
     versao 2.7.1 e aponta para `github.com/Rapptz/discord.py`.
  2. Confirme que o nome a declarar e `discord.py` — com o ponto e o `py`. O
     PyPI tem os vizinhos `discord` (um pacote-casca) e `discordpy`, e nenhum
     dos dois e a biblioteca que a pesquisa leu. Instalar o nome errado poe
     codigo de terceiro desconhecido dentro do `.venv`.
  </how-to-verify>
  <resume-signal>Responda "aprovado" para liberar a instalacao, ou diga o que encontrou de diferente. Sem essa resposta a Task 2 nao comeca — este portao NAO pode ser aprovado automaticamente.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Declarar e instalar a dependencia, e VER o firewall de escopo passar</name>
  <files>requirements.txt</files>
  <precondition>Acesso de rede ao PyPI a partir desta maquina (o `pip install` baixa 11 distribuicoes).</precondition>
  <read_first>requirements.txt, tests/test_firewall_escopo.py</read_first>
  <action>
Acrescente ao `requirements.txt`, ACIMA do bloco final que comeca com
"NAO ADICIONE AQUI, POR DECISAO DE PROJETO", uma entrada nova no idioma do
proprio arquivo: um comentario em portugues sem acento explicando por que a
biblioteca entra, seguido da linha `discord.py>=2.7.1,<3`.

O comentario deve dizer, na voz da casa (por que, com o fato medido): que ela e
o CONTRATO DO PROTOCOLO do gateway do Discord, nao "mais uma dependencia" — ela
carrega handshake, IDENTIFY, RESUME, numero de sequencia, heartbeat, compressao
zlib, backoff exponencial de reconexao e a excecao de intent privilegiada, tudo
codigo que este projeto nao vai escrever nem manter; que o piso `2.7.1` e a
versao cujo fonte foi lido na pesquisa desta fase; e que o teto `<3` segue o
idioma que `opencv-python>=4.10,<5` ja usa no mesmo arquivo. Diga tambem que o
`vigiar-party.bat` continua sem sondar `discord` porque o scanner nunca a
importa — os dois processos so dividem o `.venv`.

A linha vai DIRETA no `requirements.txt`. Nao crie um segundo arquivo de
requisitos e nao use `-r`: a varredura declarada do firewall recusa `-r`,
`--requirement`, `-e` e `--editable` de proposito e levantaria `AssertionError`.

Depois instale no `.venv` de producao e rode o firewall. As quatro varreduras
(requirements declarado, ambiente da suite, `.venv` de producao e `Requires-Dist`)
precisam estar verdes DEPOIS da instalacao, nao antes — o roadmap manda VER
passar, nao supor.

Confira tambem, com um comando, que o `.venv` importa `discord` e reporta 2.7.1,
e que o Python global (que roda o pytest) continua SEM `discord` — essa
assimetria e o motivo da divisao em tres modulos, e ela precisa ser um fato
observado, nao uma lembranca.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pip install -r requirements.txt && .venv/Scripts/python.exe -c "import discord; print('venv discord', discord.__version__)" && python -m pytest tests/test_firewall_escopo.py -q</automated>
  </verify>
  <done>`requirements.txt` declara `discord.py>=2.7.1,<3` direto; o `.venv` importa `discord` 2.7.1; `tests/test_firewall_escopo.py` passa com 100% dos testes verdes DEPOIS da instalacao.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: A fatia ponta a ponta — segredo, config, cliente, nucleo, console</name>
  <files>l2scanner/ponte_config.py, l2scanner/ponte_nucleo.py, l2scanner/ponte_discord.py, config.toml, ENV-EXEMPLO.txt, tests/test_ponte_config.py, tests/test_ponte_nucleo.py</files>
  <read_first>l2scanner/config.py, l2scanner/sessao.py, l2scanner/notificador.py, l2scanner/calibrar.py, tests/test_notificador.py, tests/test_agenda.py</read_first>
  <behavior>
Comportamento do nucleo, escrito ANTES da implementacao. Cada item vira um teste
em `tests/test_ponte_nucleo.py`, com nome-frase em portugues sem acento e
docstring dizendo o custo humano de a asercao quebrar.

  - PONTE-02: mensagem num canal que nao esta na lista e IGNORADA, e a decisao
    devolvida diz que o motivo foi o canal.
  - PONTE-02: mensagem em CADA um dos dois canais configurados e ACEITA.
  - PONTE-04: mensagem cujo autor e o proprio bot e IGNORADA.
  - PONTE-04: mensagem de OUTRO bot num canal alvo e ACEITA — este e o caso
    principal, anuncio de guild costuma vir de bot.
  - PONTE-04: mensagem de webhook num canal alvo e ACEITA (o autor de um webhook
    e o id do webhook, nunca o id da ponte).
  - PONTE-03: mensagem de alguem sem cargo nenhum, mensagem que menciona
    `@everyone` e mensagem que nao menciona ninguem sao todas ACEITAS —
    nao existe filtro de autor, de cargo ou de mencao alem do da PONTE-04.
  - PONTE-02: um id de thread (um id qualquer que nao esta na lista) nao passa,
    sem precisar de uma linha a mais no codigo.
  - A decisao devolvida por `receber` carrega o texto que vai para o console,
    para que o chamador async nao precise remontar nada.
  - `ClienteDiscordEmMemoria` alimentado com uma lista de mensagens fabricadas
    produz exatamente as decisoes acima, sem rede, sem `asyncio` e sem importar
    `discord`.

Comportamento da config, em `tests/test_ponte_config.py`:

  - `[discord]` presente e completo devolve guild, os dois canais e a conversa
    de destino, com os tipos certos.
  - A leitura aceita um `caminho` explicito (para `tmp_path`) e cai no arquivo
    padrao quando recebe `None`.
  - O token sai do `.env` pela chave `DISCORD_TOKEN`.
  - Nenhuma mensagem de erro contem o VALOR do token.
  </behavior>
  <action>
Escreva os tres modulos, na forma minima que faz a fatia inteira funcionar. Nada
de formato (Fase 2), nada de entrega (Fase 3), nada de livro de ja-vistos (isso
e o `01-03-PLAN.md`). Cada modulo abre com `from __future__ import annotations`
e uma docstring de modulo dizendo por que ele existe, na voz da casa.

**`l2scanner/ponte_config.py` — zero import de terceiro.**
Constantes de caminho ancoradas em `RAIZ = Path(__file__).resolve().parent.parent`,
como `__main__.py:107-114` e `config.py:32` fazem. Uma excecao de dominio
`PonteInvalida`, irma de `AgendaInvalida`, e uma dataclass congelada
`ConfigDaPonte` com `guild: int`, `canais: frozenset[int]`,
`conversa_de_destino: str` e `simulacao: bool`.
`ler_config_da_ponte(caminho: Path | None = None) -> ConfigDaPonte` le o
`config.toml` com `tomllib` (stdlib), no molde de `config.py:152-164`.
`ler_token_do_discord(caminho: Path | None = None) -> str` reusa `ler_env` de
`l2scanner.config` — nao escreva um segundo parser de `.env`. Nesta tarefa a
validacao pode ser o minimo que faz a fatia rodar (chave ausente levanta
`PonteInvalida` nomeando a chave); o rigor completo, a armadilha do `bool`, o
modo simulacao e o guarda de higiene sao o `01-02-PLAN.md`.

AO CONTRARIO de `ler_agenda`, aqui **arquivo ou secao AUSENTE E ERRO**. A agenda
e o mercado escolheram "ausente nao e erro" porque sao acessorios de um scanner
que roda sem eles; a ponte e um processo DEDICADO que nao existe sem a secao
`[discord]`. Escreva esse contraste na docstring, citando `CONF-03`.

**`l2scanner/ponte_nucleo.py` — zero import de terceiro. E o miolo testavel.**
Uma dataclass congelada `MensagemRecebida` com a forma NORMALIZADA de uma
mensagem: `id: int`, `canal: int`, `autor: int`, `autor_nome: str`, `texto: str`,
e os campos que a Fase 2 e o aviso de intent vao precisar
(`tem_anexo`, `tem_embed`, `tem_figurinha`, `tem_componente`, `tem_enquete`,
`e_encaminhamento`, `e_mensagem_de_sistema`), todos booleanos. Um `Enum`
`Decisao` com `ACEITA`, `IGNORADA_CANAL` e `IGNORADA_PROPRIA` — deixe espaco
para os membros que o `01-03-PLAN.md` acrescenta, mas nao os invente agora.
Uma dataclass `Resultado` com `decisao` e `linha` (o texto pronto para o
console).

`NucleoDaPonte` recebe no construtor `guild: int`, `canais: frozenset[int]` e
`id_da_ponte: int | None`, e expoe
`receber(mensagem: MensagemRecebida) -> Resultado` — SINCRONO, sem `await`, no
espirito de `Sessao.tick(frame, momento)`. A ordem das travas e: canal fora da
lista sai primeiro (PONTE-02), mensagem da propria ponte sai depois
(PONTE-04), e mais nada filtra (PONTE-03). Escreva na docstring, em maiusculas
como a casa faz, que O FILTRO E SO O DO PROPRIO ID: descartar `author.bot`
e o recorte mais copiado de tutorial de bot do Discord e descartaria justamente
o caso principal desta ponte.

`ClienteDiscordEmMemoria` mora AQUI, no codigo de producao, ao lado do nucleo —
como `NotificadorEmMemoria` mora em `notificador.py:209`. Ele guarda uma lista
de `MensagemRecebida`, bombeia cada uma pelo nucleo e acumula os `Resultado` em
`self.resultados` e as linhas aceitas em `self.linhas`. E o duble que substitui
o cliente do Discord inteiro nos testes: sem rede, sem `asyncio`, sem `discord`.
Acrescente tambem uma fabrica `mensagem_de_teste(**campos) -> MensagemRecebida`
com padroes sensatos, no molde do helper `evento(**kwargs)` de
`tests/test_agenda.py:32-40`, mas em producao para que os dois lados usem a
mesma.

**`l2scanner/ponte_discord.py` — o UNICO com `import discord`. Fino de proposito.**
`INTENCOES` construida a partir de `discord.Intents.none()` com exatamente tres
bits ligados, cada um com um comentario dizendo o que compra:
`guilds` (sem ela cargo e canal viram "deleted" na Fase 2), `guild_messages`
(e o que dispara o evento de mensagem) e `message_content` (PRIVILEGIADA, o
texto em si). Nao ligue nenhuma outra.

`Ponte(discord.Client)` recebe a `ConfigDaPonte` e um `NucleoDaPonte`. Ganchos
de ciclo de vida minimos nesta fatia: ao ficar pronta, uma linha de log dizendo
com que identidade conectou e quais canais esta ouvindo; ao retomar sessao, uma
linha dizendo que nada se perdeu; ao cair, uma linha de debug lembrando que a
biblioteca retoma sozinha. O `on_message` faz UMA coisa: converte o
`discord.Message` num `MensagemRecebida` e entrega ao nucleo, imprimindo a linha
quando a decisao for aceita. Toda a decisao mora no nucleo; a beirada async nao
decide nada.

`main() -> int` no molde de `calibrar.py:943`: monta o log basico, le config e
token, constroi a ponte e chama `run` passando `log_handler=None` — o parametro
tem padrao `MISSING`, que NAO e `None`, entao sem isso a propria biblioteca
instala um handler de console que o `01-03-PLAN.md` (OPER-02) precisa nao ter.
Erros de config viram `log.error("%s", erro)` e `return 2`, nunca traceback cru
— e o idioma de `__main__.py:1994-2002`. Feche o arquivo com a guarda
`if __name__ == "__main__": sys.exit(main())`.

NAO troque a politica de event loop do asyncio. O `ProactorEventLoop` e o padrao
certo no Windows; a receita de trocar para o seletor vem de problemas com
`aiodns`, que este projeto nao usa, e custa um teto de 512 sockets.
NAO chame o metodo de historico de canal em lugar nenhum: o gateway nao faz
backfill, historico retroativo esta Out of Scope, e uma varredura de historico
transformaria o primeiro restart num flood.

**`config.toml`** ganha uma secao `[discord]` no fim, com o cabecalho comentado
no estilo do resto do arquivo (que ensina o leigo, campo a campo). Valores:
`guild = 936957935572103169`, `canais = [1536506379752185953, 1538202612762022020]`,
`conversa_de_destino = ""` com o comentario mandando rodar
`.venv\Scripts\python.exe tools\check_whatsapp.py conversas` para descobrir o id,
e `simulacao = true` com o comentario dizendo que na Fase 1 a ponte SO simula.
O token NAO entra aqui — CONF-01.

**`ENV-EXEMPLO.txt`** ganha um bloco comentado novo, no mesmo tom didatico dos
existentes, ensinando onde achar o token
(`discord.com/developers/applications`, a aplicacao, a pagina Bot, Reset Token)
e terminando na linha `DISCORD_TOKEN=`. Diga no comentario que este token da
acesso ao bot inteiro e por isso mora no `.env`, que ja esta no `.gitignore`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_nucleo.py tests/test_ponte_config.py -q && python -c "import ast,sys; [ast.parse(open(f,encoding='utf-8').read()) for f in ('l2scanner/ponte_config.py','l2scanner/ponte_nucleo.py','l2scanner/ponte_discord.py')]; print('sintaxe ok')" && .venv/Scripts/python.exe -c "import l2scanner.ponte_discord as p; print('modulo carrega no venv', p.INTENCOES.value)"</automated>
    <human-check>PORTAO HUMANO — NAO CONFERIVEL ANTES DOS 4 PASSOS. Com o token no `.env` e o bot convidado, o usuario roda o modulo e ve no console o texto REAL de uma mensagem que acabou de postar num dos dois canais. Ate la esta linha fica PENDENTE e nao pode ser marcada verde.</human-check>
  </verify>
  <done>Os tres modulos existem e importam; `tests/test_ponte_nucleo.py` e `tests/test_ponte_config.py` passam no Python global SEM `discord` instalado nele; o `.venv` carrega `l2scanner.ponte_discord` e o valor de `INTENCOES` e 33281 (guilds + guild_messages + message_content, e nada alem).</done>
</task>

<task type="auto">
  <name>Task 4: O `.bat` proprio e o roteiro do portao humano</name>
  <files>ponte-discord.bat, PORTAO-DISCORD.txt, tests/test_ponte_bat.py</files>
  <read_first>avisos-tvt.bat, vigiar-party.bat, tests/test_calibrar_mercado_bat.py</read_first>
  <action>
**`ponte-discord.bat`** e o `avisos-tvt.bat` com o titulo trocado e mais uma
guarda. Na ordem, e sem inventar nada fora do molde: `@echo off`, `setlocal`,
cabecalho `REM` em portugues sem acento explicando para leigo o que a janela e
(uma ponte que le dois canais do Discord e, por enquanto, so mostra no console),
`cd /d "%~dp0"`, a pre-condicao de o `.venv` existir mandando rodar o
`vigiar-party.bat` primeiro (com `pause` e `exit /b 1`), a pre-condicao do
`config.toml`, e a chamada final
`".venv\Scripts\python.exe" -m l2scanner.ponte_discord %*` seguida de `pause`.
Caminho literal entre aspas, sem `activate` e sem `uv` — OPER-01.

Acrescente, ANTES da chamada, a sonda de dependencia que o `vigiar-party.bat` ja
usa na sua propria forma: rodar o Python do `.venv` importando `discord`,
descartando saida, e se der `errorlevel` instalar `-r requirements.txt` em modo
silencioso, com um caminho de erro claro se a instalacao falhar. So o
`vigiar-party.bat` monta o ambiente; este aqui exige que ele ja tenha rodado.

Acrescente tambem uma linha de despedida (`echo` dizendo que a ponte foi
encerrada) DEPOIS da chamada do Python e antes do `pause`. Ha relato recorrente
de um `RuntimeError` cosmetico do transporte do Proactor aparecer no Ctrl+C no
Windows depois de o processo ja ter saido limpo; a linha de despedida e o que
diz ao usuario nao-desenvolvedor que a saida foi normal. Nao tente suprimir a
excecao — ela e do interpretador.

**`PORTAO-DISCORD.txt`**, na raiz, em portugues SEM ACENTO (assim ele abre igual
em qualquer editor e em qualquer console), e o roteiro dos 4 passos que so o
usuario pode fazer:

  1. Criar a aplicacao em `https://discord.com/developers/applications`,
     adicionar um Bot e copiar o token para a linha `DISCORD_TOKEN=` do `.env`.
  2. Na pagina Bot, em "Privileged Gateway Intents", LIGAR
     `MESSAGE CONTENT INTENT` e clicar em "Save Changes".
  3. Convidar o bot para o servidor XM Games com `View Channel` e
     `Read Message History` nos dois canais.
  4. Descobrir o `conversation_id` do Chatwoot que vai receber os anuncios
     (rodando `tools\check_whatsapp.py conversas`) e escrever em
     `conversa_de_destino` no `config.toml`.

E entao, com destaque proprio, a secao mais importante do arquivo — o AVISO DA
ARMADILHA DE TESTE. Escreva com estas palavras:

  - A mensagem de teste NAO pode mencionar o bot e NAO pode ser postada pelo
    proprio bot.
  - Poste exatamente `teste da ponte 1`, sem arroba nenhuma, com a sua conta de
    pessoa.
  - O motivo, dito por inteiro: uma mensagem que MENCIONA o bot chega com o
    texto preenchido MESMO COM A INTENT DESLIGADA. Testar com uma arroba faz o
    console mostrar verde enquanto o modo de falha mais caro do projeto continua
    escondido — e ele so apareceria no primeiro anuncio de guild de verdade.

Diga tambem, em uma linha, que a conversa do Chatwoot escolhida no passo 4 nao
pode ser nenhuma das que estao em `CHATWOOT_CONVERSAS_COMANDO` no `.env`.

**`tests/test_ponte_bat.py`** no molde de `tests/test_calibrar_mercado_bat.py`:
le o `.bat` (o arquivo esta em cp1252, como os outros da raiz) e o
`PORTAO-DISCORD.txt`, e afirma, com nomes-frase e docstrings que dizem o custo:
que os dois arquivos existem; que o `.bat` faz `cd /d "%~dp0"`, chama o Python
do `.venv` por caminho literal, repassa `%*` e termina em `pause`; que a guarda
do `.venv` vem ANTES da chamada do Python e sai com `exit /b 1`; e que o roteiro
contem os quatro passos, a frase `MESSAGE CONTENT`, o texto de teste literal
`teste da ponte 1` e a instrucao de nao mencionar o bot. O ultimo grupo e o que
impede a armadilha de sumir numa reescrita: ela e a diferenca entre um portao
humano provado e um portao humano encenado.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_bat.py -q</automated>
  </verify>
  <done>`ponte-discord.bat` existe, segue a forma do `avisos-tvt.bat` e sonda `discord` antes de subir; `PORTAO-DISCORD.txt` existe com os 4 passos e o aviso da armadilha de mencao; `tests/test_ponte_bat.py` passa e falharia se o aviso fosse removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> `.venv` de producao | Codigo de terceiro entra na maquina do usuario pela primeira vez neste projeto |
| Discord (qualquer pessoa do servidor XM Games) -> processo da ponte | Texto arbitrario de terceiro nao confiavel cruza aqui |
| `.env` -> processo | Segredo de longa duracao com permissao total sobre o bot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `pip install -r requirements.txt` (pypi) | high | mitigate | Task 1 e um `checkpoint:human-verify` bloqueante confirmando `discord.py` de Rapptz contra os vizinhos `discord`/`discordpy`; `tests/test_firewall_escopo.py` verde DEPOIS da instalacao, nas quatro varreduras |
| T-01-01 | Information Disclosure | `DISCORD_TOKEN` no `.env` | high | mitigate | Token so via `ler_env`; nunca interpolado em log, console ou mensagem de excecao; `.env` ja coberto pelo `.gitignore`; o `config.toml` (versionado) nao recebe o token — CONF-01 |
| T-01-02 | Elevation of Privilege | `on_message` recebe tudo que o bot enxerga no servidor | medium | mitigate | `frozenset` fechado de dois ids de canal, avaliado como PRIMEIRA trava do nucleo, antes de qualquer efeito colateral — PONTE-02 |
| T-01-03 | Tampering | texto de terceiro impresso no console | low | accept | O texto so vai para `logging` e para o console; nao e avaliado, nao vira comando de shell e nao e interpolado em consulta nenhuma. Na Fase 3 ele vira `content` de um JSON montado por `json.dumps` no `notificador.py:280-282`, que fecha injecao de JSON |
| T-01-04 | Spoofing | intent privilegiada a mais (`members`, `presences`) ampliaria superficie e o portao humano | low | mitigate | `Intents.none()` como base e exatamente tres bits ligados, cada um com o motivo escrito ao lado |
</threat_model>

<verification>
1. `python -m pytest tests/test_firewall_escopo.py tests/test_ponte_nucleo.py tests/test_ponte_config.py tests/test_ponte_bat.py -q` passa inteiro no Python GLOBAL.
2. `python -m pytest -q` (a suite inteira) nao regride: nenhum teste novo quebra os existentes, e nenhum arquivo de teste importa `discord`.
3. `.venv/Scripts/python.exe -c "import l2scanner.ponte_discord"` carrega sem erro.
4. `l2scanner/notificador.py` e `l2scanner/__main__.py` continuam byte-identicos: `git diff --stat l2scanner/notificador.py l2scanner/__main__.py` sai vazio.
5. PENDENTE ATE O PORTAO HUMANO: dois cliques no `ponte-discord.bat` mostram a conexao e o texto real de uma mensagem postada sem arroba num dos dois canais.
</verification>

<success_criteria>
- `discord.py>=2.7.1,<3` declarado direto no `requirements.txt`, instalado no `.venv`, com o firewall de escopo verde depois da instalacao.
- Tres modulos novos, e apenas `ponte_discord.py` importa `discord`.
- O nucleo aceita bot e webhook e recusa so o proprio id e os canais de fora, provado por teste sem rede.
- `ponte-discord.bat` sobe o modulo sem tocar em `__main__.py`.
- `PORTAO-DISCORD.txt` carrega o aviso da armadilha de mencao, com o texto de teste literal.
</success_criteria>

<output>
Create `.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-01-SUMMARY.md` when done
</output>
