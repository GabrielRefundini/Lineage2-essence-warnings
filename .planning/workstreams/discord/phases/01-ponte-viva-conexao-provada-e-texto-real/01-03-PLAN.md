---
phase: 01-ponte-viva-conexao-provada-e-texto-real
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - l2scanner/ponte_nucleo.py
  - l2scanner/ponte_discord.py
  - .gitignore
  - tests/test_ponte_nucleo.py
  - tests/test_ponte_vistos.py
  - tests/test_ponte_guardas.py
autonomous: true
requirements: [ENTR-04, OPER-02, OPER-03, PONTE-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O usuario mata a ponte e sobe de novo: as mensagens que ela ja tinha visto antes do restart nao reaparecem"
    - "Duas copias da ponte rodando ao mesmo tempo nao processam a mesma mensagem duas vezes"
    - "A pasta de ja-vistos nao cresce sem teto: marcadores velhos sao podados no arranque"
    - "A ponte escreve logs/ponte-discord.log, rotativo, separado de logs/scanner.log, e a biblioteca do Discord nao instala um segundo handler de console"
    - "Uma mensagem com emoji imprime no console do Windows sem derrubar o handler"
    - "O console mostra o estado da conexao, ha quanto tempo ela dura, ha quanto tempo o ultimo evento do gateway chegou, e qual foi a ultima mensagem vista"
    - "Nenhum modulo da ponte chama o metodo de fechamento deliberado do cliente como sinal de saude, e nenhum busca historico de canal"
    - "O pre-voo da intent escrito no 01-02 continua sendo chamado no setup_hook depois de a tarefa de fundo do painel entrar nele"
  artifacts:
    - l2scanner/ponte_nucleo.py
    - l2scanner/ponte_discord.py
    - .gitignore
    - tests/test_ponte_vistos.py
    - tests/test_ponte_guardas.py
  key_links:
    - "NucleoDaPonte.receber -> LivroDeVistos.marcar (O_CREAT|O_EXCL) -> .ponte/vistos/"
    - "Ponte.on_connect / on_ready / on_resumed / on_disconnect -> SinalDeVida -> linhas_de_status"
    - "Client.run(log_handler=None) -> configurar_log -> logs/ponte-discord.log + logger `discord`"
    - "tests/test_ponte_guardas.py -> ast.parse(l2scanner/ponte_*.py) -> nenhuma chamada proibida"
    - "tests/test_ponte_guardas.py -> ast.parse(l2scanner/ponte_discord.py) -> a chamada do pre-voo do 01-02 continua la"
---

<objective>
As tres coisas que separam uma ponte que parece funcionar de uma ponte em que se
pode confiar: nao repetir o que ja viu, ter o proprio diario, e dizer a verdade
sobre estar viva.

Purpose: ENTR-04 mora na Fase 1 e nao na Fase 3 de proposito — o livro de
ja-vistos decide como cada mensagem e identificada logo na ENTRADA, e enfiar
isso depois custaria reescrever o caminho de recebimento inteiro. OPER-02 e
OPER-03 fecham o criterio 4 da fase: o usuario reinicia, nada reaparece, e o log
mostra a mesma sessao do console.

Output: `LivroDeVistos` durável e atomico, log rotativo proprio, painel de status
honesto, e dois guardas de AST que travam as duas decisoes mais faceis de
desfazer sem querer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/discord/ROADMAP.md
@.planning/workstreams/discord/REQUIREMENTS.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-RESEARCH.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-PATTERNS.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-01-SUMMARY.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-02-SUMMARY.md

@l2scanner/agenda.py
@l2scanner/loot.py
@l2scanner/console.py
@l2scanner/relogio.py
@l2scanner/ponte_nucleo.py
@l2scanner/ponte_discord.py
@.gitignore
</context>

<decisoes_travadas>
1. **O LIVRO DE JA-VISTOS USA O IDIOMA A DO REPOSITORIO — UM ARQUIVO VAZIO POR
   MENSAGEM, CRIADO COM `O_CREAT | O_EXCL` — E NAO O JSONL LIMITADO QUE A
   PESQUISA SUGERIU.** A pesquisa (`01-RESEARCH.md` §4) recomendou um conjunto
   limitado de ids em JSONL append-only, e o `01-PATTERNS.md` §6 apontou o idioma
   de escrita atomica de `loot.py:260-305`. Os dois foram reconciliados assim, e
   a razao precisa ir para a docstring:

   - **A semantica e "esta mensagem especifica ja foi processada?"** — um fato
     discreto que se acumula, nao um valor corrente que muda. O idioma B
     (`tmp` + `os.replace`) existe para valor corrente; o idioma A existe para
     fato discreto. `01-PATTERNS.md` §6 diz exatamente isso.
   - **O repositorio JA resolve este problema exato, com este idioma.** O scanner
     desduplica mensagens de ENTRADA do Chatwoot por id, com marcador vazio no
     `.agenda/` (`__main__.py:801` chamando `RegistroEmDisco.marcar` com a chave
     da mensagem). ENTR-04 e a mesma exigencia que AGEN-06 mais AGEN-07, e a
     docstring de `agenda.py:312-343` ja escreveu a justificativa inteira.
   - **`O_CREAT | O_EXCL` fecha a corrida que o conjunto em memoria abre.** A
     mesma docstring diz, com todas as letras, que nao ha janela de corrida entre
     ler e escrever, "que e justamente o furo de um 'le o JSON, checa, escreve o
     JSON'". A pesquisa aponta duas copias da ponte (dois cliques duplos no
     `.bat`) como a fonte de duplicata MAIS PROVAVEL na pratica; o JSONL
     precisaria de um arquivo de PID por fora para cobrir o que o `O_EXCL` cobre
     de graca.
   - **A unica objecao que o repositorio registra contra este idioma NAO se
     aplica aqui.** `agenda.py:303-308` anota como defeito conhecido que os
     marcadores `comando_<id>` nunca sao podados, "por outro motivo, o de nao
     existir data nenhuma no nome deles. Resolve-los pede um segundo criterio de
     idade (mtime, ou id monotonico), que e outro desenho e outra fase." Um
     snowflake do Discord **carrega o proprio milissegundo de criacao nos 42 bits
     de cima**. O criterio de idade que falta la existe aqui, de graca, como
     funcao pura do nome do arquivo.

2. **NAO REUSE `RegistroEmDisco`.** Ele poda por `_PREFIXOS_CONHECIDOS`, e um
   prefixo novo teria de ser acrescentado em `agenda.py` — um arquivo fora do
   raio desta fase. Escreva um `LivroDeVistos` proprio, pequeno, que copia a
   primitiva com uma docstring citando `agenda.py:374` e dizendo por que nao
   reusou.

3. **Em erro de disco, PREFIRA O DUPLICADO AO PERDIDO.** `agenda.py:365-367` ja
   tomou essa decisao com a razao escrita ("a party consegue ignorar uma
   repeticao, mas nao consegue adivinhar um TvT que ninguem anunciou"), e o
   Core Value deste milestone e o mesmo: um anuncio de guild perdido e a falha
   que o milestone inteiro existe para evitar.

4. **O metodo de fechamento deliberado do `Client` NAO e sinal de saude.** O
   fonte lido na pesquisa (`client.py:942-944`) mostra que ele devolve apenas se
   `close()` foi chamado de proposito — um cabo arrancado nao muda o valor. E
   exatamente o sinal que continua verde depois que o socket morreu. Use maquina
   de estado propria + `latency` + tempo desde o ultimo frame do gateway.

5. **Restart NAO faz backfill.** Apenas o RESUME repete eventos; um IDENTIFY novo
   nao. Entao o livro de ja-vistos e guarda de duplicata, NUNCA gatilho de
   inundacao — desde que nada busque historico de canal. Historico retroativo
   esta Out of Scope no `REQUIREMENTS.md`, e um guarda executavel trava isso.

6. **`log_handler=None` nao e detalhe, e o OPER-02.** O padrao do parametro e um
   sentinela que NAO e `None`, entao por omissao a propria biblioteca instala um
   handler de console e o log da ponte aparece duplicado e parcialmente fora do
   arquivo rotativo.

7. **Relogio por injecao, nunca por monkeypatch** (`relogio.py:25-28`). O
   `SinalDeVida` recebe `monotonico` e `parede` por parametro, com os padroes da
   `time`.

8. **`l2scanner/notificador.py` e `l2scanner/__main__.py` NAO SAO TOCADOS.**
   O `configurar_log` do `__main__.py:145-176` e COPIADO para a ponte, nao
   importado nem movido — a ponte e um processo separado e importar o
   `__main__.py` de 2047 linhas so para pegar uma funcao arrastaria o scanner
   inteiro para dentro dela.
</decisoes_travadas>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `LivroDeVistos` — nunca processar duas vezes, nem depois do restart, nem com duas copias</name>
  <files>l2scanner/ponte_nucleo.py, l2scanner/ponte_discord.py, .gitignore, tests/test_ponte_vistos.py, tests/test_ponte_nucleo.py</files>
  <read_first>l2scanner/agenda.py, l2scanner/loot.py, l2scanner/ponte_nucleo.py, tests/test_loot.py</read_first>
  <behavior>
Testes em `tests/test_ponte_vistos.py`, todos com `tmp_path`, nomes-frase em
portugues sem acento e docstring dizendo o custo humano.

  - `marcar` de um id inedito devolve verdadeiro e cria o arquivo.
  - `marcar` do MESMO id de novo devolve falso — este e o ENTR-04 em uma linha.
  - Um `LivroDeVistos` NOVO apontando para a mesma pasta continua devolvendo
    falso para aquele id: e a prova de restart, e ela e o criterio 4 da fase.
  - DOIS livros vivos ao mesmo tempo sobre a mesma pasta: o primeiro a marcar um
    id ganha, o segundo perde. E a prova das duas copias, e ela e a razao de o
    idioma ser `O_CREAT | O_EXCL` e nao "le o conjunto, checa, escreve".
  - Pasta inexistente no arranque e criada, e nao e erro.
  - Um arquivo com nome lixo dentro da pasta (nao numerico) nao derruba nem o
    arranque nem a poda — a pasta e duravel e qualquer coisa que caia nela tem
    de ser pulada em silencio, como `loot.py:227-250` faz.
  - `momento_do_snowflake` devolve o instante de criacao a partir do id, e um
    teste ancora isso num id conhecido dos canais alvo, com o valor esperado
    escrito no proprio teste (constante fixa, como `test_agenda.py:29-30` faz
    com a segunda-feira).
  - A poda apaga marcador mais velho que o teto de dias e PRESERVA o mais novo.
  - A poda nao apaga nome que nao parece snowflake.
  - Um livro em modo simulacao de disco indisponivel (pasta apontando para um
    caminho impossivel de escrever) devolve VERDADEIRO em `marcar` — prefere o
    duplicado ao perdido — e nao levanta.
  - Integracao com o nucleo: a mesma `MensagemRecebida` entregue duas vezes ao
    `NucleoDaPonte` produz ACEITA na primeira e a decisao de ja-vista na segunda,
    e a linha de console so sai uma vez.
  </behavior>
  <action>
Escreva `LivroDeVistos` no `l2scanner/ponte_nucleo.py` (que continua sem import
de terceiro). Ele recebe a pasta no construtor, cria a pasta com
`mkdir(parents=True, exist_ok=True)` e roda a poda ali mesmo, como
`RegistroEmDisco.__init__` faz.

`marcar(id_da_mensagem: int) -> bool` devolve verdadeiro quando ESTE processo
registrou agora. A implementacao e `os.open` com
`os.O_CREAT | os.O_EXCL | os.O_WRONLY` sobre um arquivo de nome igual ao id em
decimal, `FileExistsError` devolvendo falso e `OSError` devolvendo VERDADEIRO —
disco cheio, permissao ou pasta sumida preferem o duplicado ao perdido. A
docstring precisa dizer que AQUI MORA A GARANTIA e que a decisao de processar
tem de ser esta chamada, nunca uma checagem anterior, exatamente como
`agenda.py:374-403` escreve.

`momento_do_snowflake(id_da_mensagem: int) -> float` desloca 22 bits a direita e
soma a epoca do Discord (1420070400000 ms), devolvendo segundos. A docstring
explica que o layout do snowflake e documentado pelo Discord — 42 bits de
timestamp no topo — e que e ele que da a esta pasta o criterio de idade que
`agenda.py:303-308` registra como faltante para os marcadores de comando.

`podar(agora: float | None = None, dias: int = 30) -> int` apaga marcadores cujo
snowflake e mais velho que o teto e devolve quantos apagou. Nome que nao converte
para inteiro e pulado sem levantar. `OSError` na pasta inteira devolve zero. O
teto de 30 dias precisa da sua justificativa escrita: o gateway nao faz backfill
e nada nesta ponte busca historico, entao o Discord nunca vai reentregar um
evento de um mes atras; o que a poda protege e a pasta, que a um punhado de
anuncios por dia cresceria para sempre.

`ja_vistos()` devolve o conjunto de ids lidos da pasta, tolerante a `OSError`,
para o caminho comum e para o painel de status — mas a docstring precisa dizer,
como `agenda.py:355-361` diz, que ESTE metodo nao e a garantia; a garantia e o
`O_EXCL` do `marcar`.

Ligue ao nucleo: `NucleoDaPonte` passa a receber um `LivroDeVistos` no construtor
e chama `marcar` DEPOIS das travas de canal e de autor proprio, e ANTES de
qualquer efeito (nesta fase, antes de montar a linha do console). Acrescente o
membro `IGNORADA_JA_VISTA` ao `Decisao`.

O parametro novo e OBRIGATORIO, sem padrao `None`, e isso muda a assinatura de
3 para 4 argumentos: ATUALIZE, na mesma tarefa, todas as construcoes de
`NucleoDaPonte` que o `01-01` ja escreveu em `tests/test_ponte_nucleo.py`,
passando um `LivroDeVistos` apontado para o `tmp_path` de cada teste. Voce vai
encontrar essa mudanca como instrucao aqui, e nao como uma suite vermelha depois
de salvar. Um padrao `None` significando "sem livro" seria mais comodo hoje e
caro depois: a ENTR-04 sumiria calada no dia em que alguem construisse o nucleo
sem passar o livro, e nao existe teste capaz de pegar uma garantia que foi
desligada por omissao. Escreva na docstring que a ordem "gravar
antes de entregar" e uma escolha deliberada de *at-most-once*: ENTR-04 diz
"nunca entregue duas vezes", e essa e a troca que a exigencia pede — e e tambem o
que faz um Ctrl+C no meio do caminho nao perder nada, porque nao existe fila para
drenar.

Instancie o livro em `ponte_discord.main`, apontando para `RAIZ / ".ponte" / "vistos"`,
e acrescente `.ponte/` ao `.gitignore` na secao "Saida em tempo de execucao",
junto de `.agenda/` e `.loot/`, com um comentario no mesmo tom dos vizinhos
explicando que e estado local desta maquina e que versiona-lo faria um clone novo
achar que ja replicou anuncios que nunca viu.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_vistos.py tests/test_ponte_nucleo.py -q && git check-ignore -q .ponte && echo ".ponte ignorado ok"</automated>
  </verify>
  <done>Marcar duas vezes o mesmo id devolve verdadeiro e depois falso, inclusive com o livro recriado e com dois livros vivos; a poda apaga so o que e velho e so o que parece snowflake; falha de disco prefere o duplicado; `.ponte/` esta no `.gitignore`.</done>
</task>

<task type="auto">
  <name>Task 2: Log rotativo proprio, e o console que aguenta o que o Discord manda</name>
  <files>l2scanner/ponte_discord.py, tests/test_ponte_guardas.py</files>
  <read_first>l2scanner/__main__.py, l2scanner/ponte_discord.py</read_first>
  <action>
Leia `configurar_log` em `l2scanner/__main__.py:145-176` e COPIE a funcao para o
`ponte_discord.py`, trocando tres coisas e apenas tres: o nome do arquivo passa a
ser `ponte-discord.log`, o logger raiz passa a ser
`logging.getLogger("l2scanner.ponte_discord")`, e o handler tambem e anexado ao
logger `discord`. Mantenha `maxBytes=2_000_000`, `backupCount=3`,
`encoding="utf-8"`, o mesmo formatter com `datefmt="%H:%M:%S"` e a mesma pasta
`logs/`.

COPIE TAMBEM O QUE A FUNCAO USA E NAO ESTA DENTRO DELA, senao o arquivo importa
e a primeira chamada morre com `NameError`. O corpo fecha sobre tres nomes de
modulo do `__main__.py`: `PASTA_LOGS` (que e `RAIZ / "logs"`), o `RAIZ` do qual
ela deriva (`Path(__file__).resolve().parent.parent`), e o `log` do modulo. Os
dois primeiros o `ponte_discord.py` pode reusar do `ponte_config.py`, que ja
ancora o `RAIZ` da mesma forma e e primeiro-parte — nunca do `__main__.py`; ou
pode definir os seus proprios, que e igualmente aceitavel. O terceiro ja esta
resolvido pela troca do logger acima. Confira, depois de escrever, que nenhum
nome do corpo copiado ficou sem dono neste arquivo. Copiar em vez de importar e deliberado: importar do `__main__.py` de
2047 linhas arrastaria o scanner inteiro para dentro da ponte e amarraria os dois
processos que PONTE-05 manda separar — escreva isso no comentario.

Anexar ao logger `discord` e o que faz os avisos de gateway da propria biblioteca
("Can't keep up, shard ID ... websocket is ...s behind", sessao invalidada)
cairem no arquivo rotativo da ponte. Sem isso, o unico diagnostico util de uma
conexao doente fica fora do diario.

Copie tambem o `reconfigure(encoding="utf-8", errors="replace")` sobre `stdout` e
`stderr`, dentro do mesmo `try/except (AttributeError, ValueError)`. O comentario
precisa dizer por que ele importa MAIS aqui do que no scanner: o scanner imprime
alertas que ele mesmo escreveu; a ponte imprime texto ARBITRARIO de usuario do
Discord — emoji, cirilico, o que a pessoa digitar. O console do Windows abre em
cp1252, e sem isso a primeira mensagem com emoji levanta `UnicodeEncodeError` e
derruba o handler no meio de um anuncio.

Confirme que `run` continua sendo chamado com `log_handler=None` e escreva o
comentario que fixa a razao: o padrao do parametro e um sentinela que NAO e
`None`, entao por omissao a biblioteca instala um handler de console proprio e o
log aparece duplicado e parcialmente fora do arquivo — OPER-02 quebrado sem
nenhuma mensagem de erro.

Escreva `tests/test_ponte_guardas.py`, que le o codigo-fonte dos modulos da ponte
e afirma, sem importar `discord`:

  - `logs/ponte-discord.log` e o nome usado, e `scanner.log` nao aparece — os dois
    diarios sao separados (OPER-02).
  - `log_handler` aparece com valor `None` na chamada de `run`.
  - `reconfigure` esta presente com `errors="replace"`.
  - `l2scanner/ponte_discord.py` AINDA CONTEM uma chamada a
    `veredito_do_pre_voo` — a porta do pre-voo da intent que o `01-02` escreveu
    no `setup_hook`, e que por sua vez chama `intent_ligada_no_painel`. Mesma
    caminhada de `ast.parse`, procurando um no de chamada cujo alvo tenha esse
    nome (como `Name` ou como `Attribute`). Este guarda existe porque a Task 3
    volta ao MESMO `setup_hook` para pendurar a tarefa de fundo do painel: sem
    ele, uma reescrita distraida daquele metodo apaga o unico mecanismo que faz
    o criterio 2 da fase ser deterministico, e nenhum outro teste deste arquivo
    perceberia — os outros olham `log_handler`, `reconfigure`, `is_closed` e
    `history`, nao o pre-voo.
  - Nenhum modulo da ponte contem uma CHAMADA ao metodo de fechamento deliberado
    do cliente (`is_closed`) nem ao metodo de historico de canal (`history`).
    Faca essa checagem com `ast.parse` e uma caminhada procurando nos de chamada
    cujo alvo e um atributo com esses nomes — NAO com busca de texto. A diferenca
    importa: um teste de texto ficaria vermelho por causa da propria docstring que
    explica por que a chamada e proibida, e um teste que falha por motivo errado
    convida a ser desligado. E o mesmo cuidado que
    `_nomes_declarados_no_requirements` toma ao cortar comentarios antes de
    varrer, e que `test_calibrar_mercado_bat.py` toma ao exigir que a frase venha
    de um `echo` e nao de um `REM`.

A docstring do modulo de guardas precisa dizer o custo de cada um: uma busca de
historico transforma o primeiro restart num flood no grupo do WhatsApp, e o sinal
de fechamento deliberado transforma o painel num semaforo que fica verde depois
que a rede caiu.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_guardas.py -q && .venv/Scripts/python.exe -c "import l2scanner.ponte_discord as p; p.configurar_log(False); import logging,pathlib; log=logging.getLogger('l2scanner.ponte_discord'); log.info('linha de prova com emoji \U0001F389'); assert pathlib.Path('logs/ponte-discord.log').exists(); print('log proprio ok')"</automated>
  </verify>
  <done>`logs/ponte-discord.log` e criado e recebe linha com emoji sem excecao (nenhum nome do corpo copiado ficou sem dono); `logs/scanner.log` nao e tocado pela ponte; os guardas de AST passam e falhariam se alguem acrescentasse uma chamada de historico, o sinal de fechamento deliberado, ou se o pre-voo da intent sumisse do `setup_hook`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sinal de vida honesto — o painel que nao fica verde depois do socket morrer</name>
  <files>l2scanner/ponte_nucleo.py, l2scanner/ponte_discord.py, tests/test_ponte_nucleo.py</files>
  <read_first>l2scanner/__main__.py, l2scanner/console.py, l2scanner/relogio.py, l2scanner/ponte_nucleo.py</read_first>
  <behavior>
Testes em `tests/test_ponte_nucleo.py`, todos sobre `SinalDeVida` e
`linhas_de_status`, com relogio INJETADO — nenhum `time.sleep`, nenhum
monkeypatch, nenhum `asyncio`.

  - Recem-construido, o estado e "conectando" e o painel diz isso.
  - Depois de `pronta`, o estado e "conectada" e o painel mostra ha quanto tempo.
  - Depois de `caiu`, o estado e "caiu" — e o painel NAO diz conectada. Este e o
    teste que carrega a exigencia inteira: um painel que continua verde depois
    da queda e pior do que nenhum painel, porque o usuario para de olhar.
  - Depois de `caiu` seguido de `retomou`, o estado e "retomada".
  - O painel mostra ha quanto tempo chegou o ultimo evento do gateway, e esse
    numero cresce quando o relogio injetado avanca sem eventos novos.
  - O painel mostra a latencia quando ela e um numero, e mostra um traco quando
    ela nao e (a biblioteca devolve um nao-numero quando nao ha socket e
    infinito quando nao ha keep-alive; imprimir "inf" segundos no console e pior
    do que nao imprimir).
  - O painel nomeia os dois canais vigiados e o contador de mensagens vistas.
  - Sem nenhuma mensagem ainda, o painel diz isso com todas as letras em vez de
    ficar em branco — um silencio precisa PARECER proposital, que e a regra de
    produto escrita em `__main__.py:445-448`.
  - `linhas_de_status` e uma funcao pura que devolve `str`: chamada duas vezes
    com o mesmo estado devolve o mesmo texto.
  </behavior>
  <action>
No `ponte_nucleo.py`, escreva `EstadoDaConexao` (um `Enum` com conectando,
conectada, caiu, retomada) e `SinalDeVida`, que recebe no construtor
`monotonico=time.monotonic` e `parede=time.time` — por parametro, nunca por
monkeypatch, que e a lei escrita em `relogio.py:25-28` e `relogio.py:83`. Ele
expoe `conectou`, `pronta`, `retomou`, `caiu` e `viu_evento`, guarda o instante
de cada transicao, o instante do ultimo frame do gateway, a contagem de mensagens
aceitas e a ultima linha vista.

Escreva `linhas_de_status(sinal, canais, latencia) -> str`: funcao PURA que
devolve uma string de varias linhas, no molde de `desenhar_status`
(`__main__.py:409-475`), com o estado entre colchetes no topo, ha quanto tempo o
estado dura, ha quanto tempo chegou o ultimo evento do gateway, a latencia
formatada em milissegundos (ou um traco quando ela nao e um numero finito), os
dois canais vigiados, o total de mensagens vistas e a ultima linha replicada.
Nada de `rich` — ele nao esta no `requirements.txt` e nao e importado em lugar
nenhum deste repositorio.

A docstring precisa registrar o que este painel substitui e por que: o metodo de
fechamento deliberado do `Client` (`client.py:942-944`) so vira verdadeiro quando
`close()` foi chamado de proposito, entao um cabo arrancado nao muda o valor —
ele e literalmente o sinal que continua verde depois que o socket morreu. E
precisa registrar o que fecha o buraco do outro lado: o cao-de-guarda de
heartbeat da propria biblioteca fecha o socket quando nenhum frame chega dentro
do tempo limite (padrao de 60 segundos), entao um socket zumbi vira uma queda em
no maximo cerca de um minuto — o que torna a maquina de estado honesta, desde
que o painel mostre NUMEROS e nao uma bolinha.

No `ponte_discord.py`, ligue os quatro ganchos de ciclo de vida ao `SinalDeVida`
e acrescente o gancho de tipo de evento de socket alimentando `viu_evento`.
Passe `client.latency` para o painel na hora de imprimir. Use
`console.destacar` para conectou, caiu e retomou — sao os poucos eventos que
precisam saltar aos olhos no meio das linhas de log, que e o proposito declarado
de `console.py:1-9`. Uma mensagem aceita imprime a sua linha e atualiza o painel.

Como um canal de anuncio parado pode passar horas sem evento, ACRESCENTE ao
`setup_hook` QUE O `01-02` JA ESCREVEU — DEPOIS do pre-voo da intent, sem
reescrever nem reordenar o que ja esta la — uma tarefa de fundo com `asyncio` da
stdlib que repinta o painel a cada 60 segundos, e cancele-a de forma limpa no
fechamento. Nao crie um `setup_hook` novo: aquele metodo e onde mora o pre-voo
deterministico que sustenta o criterio 2 da fase, e um `setup_hook` fresco
apagaria essa protecao sem uma linha de erro. O guarda de AST da Task 2 falha se
isso acontecer — leia-o antes de editar o metodo. Nao troque a politica
de event loop: o Proactor e o padrao certo no Windows, e a receita de trocar para
o seletor vem de um problema com resolucao de DNS assincrona que este projeto nao
tem, custando um teto de 512 sockets.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_nucleo.py -q && python -m pytest -q</automated>
  </verify>
  <done>`SinalDeVida` e `linhas_de_status` sao testados sem relogio real e sem `asyncio`; o painel mostra estado, duracao, tempo desde o ultimo evento, latencia formatada, canais, contador e ultima mensagem; a suite inteira do repositorio continua verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disco local (`.ponte/vistos/`) -> processo | Estado duravel compartilhado entre execucoes e entre duas copias do processo |
| Gateway do Discord -> processo | Eventos podem ser repetidos por RESUME; nada garante entrega exatamente-uma-vez |
| Processo -> `logs/ponte-discord.log` | Texto de terceiro nao confiavel e persistido em disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | duas copias da ponte processando o mesmo evento | medium | mitigate | Marcador por snowflake criado com `O_CREAT \| O_EXCL`: entre duas copias competindo exatamente uma cria o arquivo e a outra recebe erro de arquivo existente; sem lock e sem janela entre ler e escrever |
| T-01-11 | Repudiation | painel de status mostrando conexao viva depois da queda | medium | mitigate | Maquina de estado propria por gancho + latencia + tempo desde o ultimo frame; guarda de AST proibindo a chamada do sinal mentiroso |
| T-01-12 | Denial of Service | busca de historico de canal transformando um restart em flood | high | mitigate | Guarda de AST proibindo a chamada de historico nos modulos da ponte; historico retroativo ja e Out of Scope no `REQUIREMENTS.md` |
| T-01-13 | Denial of Service | `.ponte/vistos/` crescendo sem teto | low | mitigate | Poda no arranque por idade derivada do proprio snowflake, com teto de 30 dias |
| T-01-14 | Information Disclosure | texto de anuncio de terceiro persistido em `logs/ponte-discord.log` | low | accept | Log e local e `logs/` mais `*.log` ja estao no `.gitignore`; o conteudo e o mesmo que qualquer pessoa do servidor XM Games ja pode ler no Discord |
| T-01-15 | Tampering | arquivo lixo caindo na pasta de ja-vistos | low | mitigate | Leitura e poda tolerantes: nome que nao converte e pulado em silencio, `OSError` na pasta nao derruba o arranque |
</threat_model>

<verification>
1. `python -m pytest -q` — a suite inteira verde, incluindo os arquivos novos da ponte.
2. `git check-ignore .ponte` confirma que a pasta de estado nao entra no git.
3. `.venv/Scripts/python.exe -m l2scanner.ponte_discord --conferir` continua saindo com 0 numa config valida (nada nesta wave quebrou o arranque).
4. `git diff --stat l2scanner/notificador.py l2scanner/__main__.py` sai vazio.
5. PENDENTE ATE O PORTAO HUMANO: o usuario posta duas mensagens, mata a ponte, sobe de novo e confere que nenhuma das duas reaparece no console, e que `logs/ponte-discord.log` mostra a mesma sessao.
</verification>

<success_criteria>
- ENTR-04 provado sem rede: mesmo id marcado duas vezes, com o livro recriado e com duas instancias vivas.
- OPER-02 provado: `logs/ponte-discord.log` existe, rotaciona, recebe tambem os avisos do logger `discord`, e nao ha um segundo handler de console instalado pela biblioteca.
- OPER-03 provado sem rede: painel puro, testado com relogio injetado, mostrando numeros em vez de semaforo.
- Os dois guardas de AST existem e falhariam se a decisao fosse desfeita.
</success_criteria>

<output>
Create `.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-03-SUMMARY.md` when done
</output>
