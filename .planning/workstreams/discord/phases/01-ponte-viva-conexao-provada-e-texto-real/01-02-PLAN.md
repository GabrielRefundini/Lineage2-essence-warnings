---
phase: 01-ponte-viva-conexao-provada-e-texto-real
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - l2scanner/ponte_config.py
  - l2scanner/ponte_nucleo.py
  - l2scanner/ponte_discord.py
  - config.toml
  - ENV-EXEMPLO.txt
  - PORTAO-DISCORD.txt
  - tests/test_ponte_config.py
  - tests/test_ponte_nucleo.py
  - tests/test_ponte_arranque.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Apagar DISCORD_TOKEN do .env faz a ponte morrer no arranque dizendo qual chave falta e onde pegar o valor"
    - "Apagar guild, canais ou conversa_de_destino do config.toml faz a ponte morrer no arranque nomeando a chave"
    - "Um valor com o tipo errado (inclusive true no lugar de um numero) e recusado no arranque, nao aceito calado"
    - "Com a intent MESSAGE CONTENT desligada no painel, a ponte NAO sobe: ela morre com a nossa mensagem em portugues listando os cliques do conserto, antes de abrir o WebSocket"
    - "Se a intent for desligada com a ponte ja rodando, a primeira mensagem vazia produz um erro alto nomeando a intent como causa provavel — e a mensagem NAO e descartada"
    - "Uma conversa de destino que tambem esta em CHATWOOT_CONVERSAS_COMANDO e recusada no arranque"
    - "Rodar a ponte com --conferir valida tudo e sai sem tocar na rede"
    - "Nenhuma mensagem de erro contem o valor do token"
  artifacts:
    - l2scanner/ponte_config.py
    - l2scanner/ponte_nucleo.py
    - l2scanner/ponte_discord.py
    - tests/test_ponte_config.py
    - tests/test_ponte_nucleo.py
    - tests/test_ponte_arranque.py
  key_links:
    - "ponte_discord.Ponte.setup_hook -> Client.application_flags -> gateway_message_content / gateway_message_content_limited"
    - "ponte_config.conferir_a_configuracao -> PonteInvalida -> main() -> log.error + return 2"
    - "ponte_config -> l2scanner.config.ler_env -> CHATWOOT_CONVERSAS_COMANDO (guarda de higiene)"
    - "ponte_nucleo.parece_intent_desligada -> MensagemRecebida (puro, sem discord)"
---

<objective>
Fazer a ponte MORRER ALTO em vez de subir muda. Duas familias do mesmo erro: a
configuracao errada (chave faltando, tipo errado, conversa perigosa) e o botao
do painel do Discord desligado.

Purpose: o criterio 5 da fase pede que apagar uma chave derrube o arranque
nomeando a chave e o conserto. O criterio 2 pede que uma ponte com a intent
desligada nunca finja estar saudavel replicando branco. As duas coisas sao a
mesma disciplina — a regra escrita em `calibracao.py:429-432` ("Falhar alto aqui
e muito melhor do que medir a regiao errada calado") aplicada a config e ao
painel.

Output: validacao completa de arranque, o pre-voo deterministico da intent, a
heuristica de runtime como segunda linha, e um modo `--conferir` que prova tudo
isso sem tocar na rede e sem depender do portao humano.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/discord/ROADMAP.md
@.planning/workstreams/discord/REQUIREMENTS.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-RESEARCH.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-PATTERNS.md
@.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-01-SUMMARY.md

@l2scanner/config.py
@l2scanner/calibracao.py
@l2scanner/ponte_config.py
@l2scanner/ponte_nucleo.py
@l2scanner/ponte_discord.py
@tests/test_firewall_escopo.py
</context>

<decisoes_travadas>
1. **O pre-voo da intent e DETERMINISTICO, nao heuristica.** `Client.login()`
   chama `GET /applications/@me` e preenche `self.application_flags` ANTES de
   rodar `setup_hook()` e ANTES do IDENTIFY (fonte lido: `client.py:679-693`).
   As flags `gateway_message_content` (1<<18, app verificado) e
   `gateway_message_content_limited` (1<<19, app com menos de 100 servidores)
   dizem direto da API se o botao do painel esta ligado. A ponte deste usuario
   roda em UM servidor, entao o bit que vai ligar e o `_limited` — teste os dois
   com `or`.

2. **A excecao da biblioteca continua valendo como rede.** Se a API de flags
   mentir, o gateway ainda fecha com 4014 e a biblioteca levanta
   `PrivilegedIntentsRequired` — e ela aborta o laco MESMO com reconexao ligada
   (`client.py:773-779`). Capture as duas, mais `LoginFailure`.

3. **A heuristica de runtime AVISA, NUNCA DESCARTA.** Ela existe para o caso de
   o usuario desligar o botao com a ponte rodando. Ela e probabilistica; o
   pre-voo e deterministico. Uma mensagem que dispare a heuristica ainda e
   processada e impressa — com o aviso ao lado.

4. **O guarda da conversa de destino tem a razao HONESTA, e ela e menor do que a
   pesquisa sugere.** A pesquisa afirma que um anuncio do Discord contendo
   `.status` viraria comando no scanner. **Isso e falso e nao deve ser escrito
   no codigo:** `comandos.py:887` descarta tudo cujo `message_type` nao seja
   `incoming`, e essa e uma regra deliberada e documentada em `comandos.py:20`
   ("So `incoming`. O scanner nunca pode obedecer as proprias mensagens"). A
   ponte posta `outgoing` (`notificador.py:281`), entao esse caminho ja esta
   fechado. A razao REAL, que e a que deve ir para a docstring: **quem RESPONDE
   numa conversa de anuncio compartilhada manda `incoming`** — e uma resposta
   assim, numa conversa que tambem esta em `CHATWOOT_CONVERSAS_COMANDO`, seria
   lida como comando do scanner. Higiene de configuracao com o motivo certo,
   nao seguranca inflada.

5. **CONF-04 nesta fase e um interruptor honesto.** `simulacao` mora em
   `[discord]` e o padrao e `true`. Como a Fase 1 nao tem caminho de entrega
   nenhum, `simulacao = false` e RECUSADO no arranque, dizendo que a entrega no
   WhatsApp so existe a partir da Fase 3. Isso e melhor do que aceitar a chave e
   nao fazer nada com ela: uma chave que mente e pior do que uma chave ausente.

6. **`l2scanner/notificador.py` e `l2scanner/__main__.py` NAO SAO TOCADOS.**
   O `CHATWOOT_CONVERSAS_COMANDO` e LIDO via `ler_env` de `l2scanner/config.py`;
   nada e escrito la.

7. **Nenhum arquivo de teste importa `discord`.** O pytest roda no Python global,
   onde `discord` nao existe. O que precisa do `.venv` roda por `subprocess`,
   com `pytest.skip` quando o `.venv` nao existe — o mesmo idioma de
   `tests/test_firewall_escopo.py::test_o_venv_de_producao_nao_tem_biblioteca_de_input`.
</decisoes_travadas>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validacao de arranque completa — a chave, o tipo, o modo e a conversa perigosa</name>
  <files>l2scanner/ponte_config.py, config.toml, ENV-EXEMPLO.txt, tests/test_ponte_config.py</files>
  <read_first>l2scanner/config.py, l2scanner/calibracao.py, l2scanner/ponte_config.py, tests/test_agenda.py</read_first>
  <behavior>
Cada item vira um teste em `tests/test_ponte_config.py`, com nome-frase em
portugues sem acento e docstring dizendo o custo humano da asercao quebrar.
Todos usam `tmp_path` e caminhos explicitos; nenhum le o `.env` da maquina.

  - `config.toml` sem a secao `[discord]` levanta `PonteInvalida` dizendo que a
    secao falta e mostrando o bloco de exemplo.
  - `config.toml` que nao e TOML valido levanta `PonteInvalida` citando o NOME
    do arquivo (com dois arquivos em jogo, "o TOML esta quebrado" sem dizer qual
    e um convite a editar o errado).
  - Falta `guild`, falta `canais`, falta `conversa_de_destino`: tres testes, cada
    um afirmando que a mensagem NOMEIA a chave que falta.
  - `guild = "936957935572103169"` (texto no lugar de numero) e recusado dizendo
    o que veio e o que era esperado.
  - `guild = true` e recusado. Esta e a armadilha do `bool`: `isinstance(True, int)`
    e verdadeiro em Python, entao sem uma checagem explicita de `bool` o valor
    passaria como o servidor de id 1 — exatamente o buraco que
    `calibracao.py:466-480` fecha e que `config.py:229-244` documenta.
  - `canais = []` e recusado; `canais = [123]` (um so) e ACEITO, porque a lista e
    do usuario e nada exige exatamente dois.
  - `canais = [123, 123]` reduz a um canal so, sem erro (e um `frozenset`).
  - `canais = [123, "456"]` e recusado nomeando o elemento errado.
  - `simulacao = false` e recusado com uma mensagem que cita a Fase 3.
  - `simulacao` ausente vale `true`.
  - `.env` inexistente levanta `PonteInvalida` mandando copiar o `ENV-EXEMPLO.txt`.
  - `.env` presente sem `DISCORD_TOKEN` levanta `PonteInvalida` nomeando a chave.
  - `DISCORD_TOKEN` com o valor de exemplo literal e recusado (mesmo tratamento
    que `config.py:74-75` da ao `CHATWOOT_TOKEN`).
  - NENHUMA mensagem de erro produzida por este modulo contem o VALOR do token:
    um teste que cria um token reconhecivel, provoca todos os erros que
    conseguir, e afirma que o texto do token nao aparece em nenhuma delas.
  - `conversa_de_destino` que tambem aparece em `CHATWOOT_CONVERSAS_COMANDO` e
    RECUSADA, com a mensagem explicando que quem responde nessa conversa manda
    `incoming` e seria lido como comando do scanner.
  - `conversa_de_destino` que NAO aparece nessa lista passa, inclusive quando a
    lista esta vazia (que e o padrao).
  - A comparacao das duas listas ignora espaco em volta, porque
    `CHATWOOT_CONVERSAS_COMANDO` viaja como CSV e e partida com `split(",")` mais
    `strip()` em `config.py:97-101`.
  </behavior>
  <action>
Complete o `l2scanner/ponte_config.py` que a fatia deixou minimo. Zero import de
terceiro continua valendo — este modulo tem de ser importavel no Python global
que roda o pytest.

Escreva `_conferir_as_chaves_do_discord(dados: dict) -> ConfigDaPonte` no molde
exato de `calibracao.py:442` (`_conferir_as_chaves_de_mercado`): uma funcao a
mao, campo a campo, que levanta `PonteInvalida` com uma mensagem no formato
"nome da chave: o que veio. O conserto." Nada de biblioteca de validacao — nao
ha `pydantic` neste repositorio e ele nao entra aqui.

Cada checagem de numero precisa recusar `bool` explicitamente antes de checar
`int`, e o comentario ao lado precisa dizer por que: `bool` e subclasse de `int`
em Python, entao sem a linha o valor passa calado e a ponte vai ouvir o servidor
errado sem uma unica mensagem no console. Copie o raciocinio de
`config.py:229-244`, que ja documenta esse buraco e explica por que o campo
irmao NAO foi consertado junto.

`ler_token_do_discord` ganha a cascata de tres checagens do
`config_do_chatwoot` (`config.py:56-86`): `.env` inexistente, chave ausente,
valor de exemplo — cada uma com o comando ou o passo exato que conserta. Nenhuma
delas pode interpolar o valor do token na mensagem; escreva isso na docstring
citando a regra ja fixada no cabecalho de `config.py:6`.

Escreva `conferir_a_configuracao(caminho_config=None, caminho_env=None)`
devolvendo uma tupla `(ConfigDaPonte, token)`, que e a porta unica do arranque:
le o TOML, valida, le o `.env`, valida, e por fim aplica o guarda de higiene
comparando `conversa_de_destino` com as conversas de comando lidas do `.env`. A
docstring desse guarda precisa carregar a razao HONESTA e apenas ela: quem
RESPONDE numa conversa de anuncio compartilhada manda uma mensagem de entrada, e
uma conversa que tambem e canal de comando transformaria essa resposta em
comando do scanner. Registre explicitamente que o caminho contrario — um anuncio
postado pela ponte virar comando — JA esta fechado, porque a ponte posta
`outgoing` e `comandos.py:887` so aceita `incoming`; escrever que ele e o motivo
seria justificar um guarda com um risco que nao existe, e uma justificativa falsa
e o comeco de um guarda removido.

Acrescente `resumo_da_configuracao(cfg) -> str`, uma funcao PURA que devolve o
bloco que o `--conferir` imprime: o servidor, os canais, a conversa de destino,
o modo simulacao e a origem de cada valor. Ela nunca imprime o token — no maximo
diz que ele foi encontrado e quantos caracteres tem.

Atualize os comentarios da secao `[discord]` do `config.toml` e o bloco do
`ENV-EXEMPLO.txt` para descreverem exatamente estas regras (incluindo que
`simulacao = false` so vale a partir da Fase 3, e que a conversa escolhida nao
pode estar em `CHATWOOT_CONVERSAS_COMANDO`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_config.py -q</automated>
  </verify>
  <done>Toda chave ausente, todo tipo errado e a conversa perigosa levantam `PonteInvalida` com mensagem que nomeia a chave e diz o conserto; `simulacao = false` e recusado citando a Fase 3; nenhum texto de erro contem o valor do token; os testes rodam sem `.env` e sem `config.toml` de verdade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O pre-voo da intent que mata o arranque, e a heuristica que grita em regime</name>
  <files>l2scanner/ponte_nucleo.py, l2scanner/ponte_discord.py, tests/test_ponte_nucleo.py</files>
  <read_first>l2scanner/ponte_nucleo.py, l2scanner/ponte_discord.py, l2scanner/console.py</read_first>
  <behavior>
Testes em `tests/test_ponte_nucleo.py`, todos sobre funcoes PURAS — nenhum
importa `discord`.

  - `intent_ligada_no_painel(False, False)` e falso; `(True, False)`,
    `(False, True)` e `(True, True)` sao verdadeiros. O caso `(False, True)` e o
    real desta ponte (um servidor so, aplicacao nao verificada) e merece o proprio
    teste com docstring dizendo isso.
  - O texto de conserto da intent contem a URL do painel, a frase
    `MESSAGE CONTENT`, o passo de salvar e a instrucao de subir a ponte de novo.
    Um teste afirma cada um. Sem esse teste a mensagem vira prosa generica na
    primeira reescrita, e ela e a unica coisa que o usuario vai ler quando o
    milestone inteiro estiver travado.
  - `parece_intent_desligada` e VERDADEIRO para uma mensagem comum com texto,
    anexo, embed, figurinha, componente, enquete e encaminhamento todos vazios.
  - E FALSO quando ha texto.
  - E FALSO para cada um dos casos de "vazio mas legitimo", um teste por campo:
    so anexo, so embed, so figurinha, so componente, so enquete, so encaminhamento.
  - E FALSO para mensagem de SISTEMA (entrou no servidor, fixou mensagem, criou
    thread, boost): elas sao vazias por direito. Esta e a linha que faz a
    heuristica funcionar em vez de gritar todo dia.
  - Uma mensagem que dispara a heuristica ainda produz decisao ACEITA — a
    heuristica avisa, nunca descarta. Este teste e o que impede a regressao mais
    cara possivel: transformar o detector do problema no proprio problema.
  </behavior>
  <action>
**No `ponte_nucleo.py` (puro, sem `discord`):**

Escreva `intent_ligada_no_painel(verificada: bool, limitada: bool) -> bool` — um
`or` das duas flags, com a docstring explicando que o bit "limitada" e o que vale
para uma aplicacao com menos de 100 servidores e que testar os dois cobre o dia
em que o bot crescer.

Escreva a constante `CONSERTO_DA_INTENT` com o texto que o usuario le quando o
arranque morre: em portugues sem acento, dizendo primeiro a CONSEQUENCIA (sem a
intent o bot conecta, parece saudavel e recebe TODA mensagem com o texto vazio —
por isso a ponte prefere nao subir) e depois os passos numerados do conserto:
abrir `https://discord.com/developers/applications`, escolher a aplicacao,
clicar em Bot, ligar `MESSAGE CONTENT INTENT` em "Privileged Gateway Intents",
clicar em "Save Changes", subir a ponte de novo. Uma excecao de dominio
`IntentDeConteudoDesligada` acompanha.

Escreva `parece_intent_desligada(mensagem: MensagemRecebida) -> bool`. Mensagem
de sistema sai antes de tudo, devolvendo falso. Depois, verdadeiro apenas quando
texto, anexo, embed, figurinha, componente, enquete e encaminhamento estao TODOS
vazios. A docstring precisa dizer o fato que sustenta a heuristica — o Discord
nao deixa postar uma mensagem sem nada dentro, entao uma mensagem comum
inteiramente vazia e o retrato da intent desligada — e precisa listar os falsos
positivos conhecidos e o que os corta. Registre que `enquete` e `encaminhamento`
estao na lista sem confirmacao de que a intent os censura (Assumptions A1 e A2 da
pesquisa): mante-los custa, no pior caso, um aviso a menos; tira-los custaria, no
pior caso, um anuncio perdido, e essa assimetria decide.

Ligue a heuristica ao `receber` do nucleo de forma que ela apenas ANOTE: o
`Resultado` ganha um campo indicando suspeita de intent desligada, e a decisao
continua `ACEITA`. Escreva na docstring, em maiusculas como a casa faz, que ela
AVISA E NAO DESCARTA.

**No `ponte_discord.py` (a beirada):**

Implemente `setup_hook` na `Ponte`. Ele roda depois do login REST e antes do
IDENTIFY, entao `self.application_flags` ja esta preenchido e o WebSocket ainda
nao abriu. Leia as duas flags de conteudo de mensagem do gateway, passe para
`intent_ligada_no_painel`, e levante `IntentDeConteudoDesligada` com o texto de
conserto quando o painel estiver desligado. Confira tambem o caso espelhado —
ligada no painel mas nao declarada em `INTENCOES` — com a sua propria frase. A
docstring precisa dizer que isto NAO substitui a excecao de intent privilegiada
da biblioteca; existe para que a mensagem que o usuario le seja a nossa, em
portugues, com os cliques.

Em `main`, envolva a chamada de `run` com tres capturas, cada uma virando
`log.error` e `return 2`: a nossa `IntentDeConteudoDesligada`; a
`PrivilegedIntentsRequired` da biblioteca (rede de seguranca — se a API de flags
mentir, o gateway ainda fecha 4014, e a mensagem dela em ingles vai acompanhada
do nosso texto de conserto); e `LoginFailure`, dizendo que o `DISCORD_TOKEN`
esta invalido ou revogado e onde gerar outro. Nenhuma delas pode imprimir o
token.

No `on_message`, quando o resultado indicar suspeita de intent desligada, emita
um `log.error` nomeando o autor, o canal e a intent como causa provavel, seguido
do texto de conserto — e siga processando a mensagem normalmente. Use
`console.destacar` para esse aviso: e exatamente o tipo de evento que
`console.py:1-9` diz que nao pode se perder no meio das linhas de log.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_nucleo.py -q && .venv/Scripts/python.exe -c "import l2scanner.ponte_discord as p; assert p.INTENCOES.message_content and p.INTENCOES.guilds and p.INTENCOES.guild_messages; assert not p.INTENCOES.members and not p.INTENCOES.presences; print('assinatura minima ok')"</automated>
  </verify>
  <done>O pre-voo levanta `IntentDeConteudoDesligada` quando as duas flags estao desligadas; `main` traduz as tres excecoes em codigo de saida 2 com mensagem em portugues; a heuristica classifica os sete casos de vazio corretamente e NUNCA descarta a mensagem; a assinatura de intents tem exatamente tres bits.</done>
</task>

<task type="auto">
  <name>Task 3: `--conferir` — provar o arranque inteiro sem tocar na rede</name>
  <files>l2scanner/ponte_discord.py, PORTAO-DISCORD.txt, tests/test_ponte_arranque.py</files>
  <read_first>l2scanner/calibrar.py, l2scanner/ponte_config.py, tests/test_firewall_escopo.py</read_first>
  <action>
Sem isto, tudo que o criterio 5 promete so seria conferivel depois do portao
humano — e um criterio que so pode ser testado depois do bloqueio externo nao
protege ninguem durante o desenvolvimento.

Acrescente a `main` do `ponte_discord.py` um analisador de argumentos minimo
(`argparse` da stdlib) com tres opcoes: `--conferir`, `--config CAMINHO` e
`--env CAMINHO`. As duas ultimas apenas repassam caminhos para
`conferir_a_configuracao`, seguindo a regra do repositorio de que toda funcao que
le arquivo aceita um caminho e cai no padrao (`config.py:40`, `config.py:141`) —
e uma opcao explicita de caminho e o que torna o proprio arranque testavel com
`tmp_path`.

Com `--conferir`, `main` roda `conferir_a_configuracao`, imprime
`resumo_da_configuracao` e devolve 0 — sem construir cliente, sem abrir socket,
sem chamar `run`. Config invalida devolve 2 com a mesma mensagem de sempre.
Escreva na docstring que `--conferir` NAO prova que a intent esta ligada nem que
o token e valido: as duas coisas exigem falar com o Discord, e prometer mais do
que se confere e exatamente o defeito que este modo existe para nao ter.

Escreva `tests/test_ponte_arranque.py`. Ele nao importa `discord`: ele roda o
Python do `.venv` por `subprocess`, e faz `pytest.skip` com mensagem explicativa
quando `.venv/Scripts/python.exe` nao existe — o mesmo idioma e a mesma razao do
`test_o_venv_de_producao_nao_tem_biblioteca_de_input`. Os casos:

  - Com um `config.toml` e um `.env` completos escritos num `tmp_path`,
    `-m l2scanner.ponte_discord --conferir --config ... --env ...` sai com codigo
    0 e imprime o servidor e os dois canais.
  - Com o `.env` sem a linha do token, sai com codigo 2 e a saida NOMEIA
    `DISCORD_TOKEN`.
  - Com o `config.toml` sem `conversa_de_destino`, sai com codigo 2 e a saida
    NOMEIA a chave.
  - Com um token reconhecivel valido, a saida de sucesso NAO contem o valor do
    token.
  - O processo termina em poucos segundos (use `timeout` no `subprocess.run`):
    se algum dia alguem fizer o `--conferir` abrir o gateway, este teste trava e
    denuncia.

Acrescente ao `PORTAO-DISCORD.txt`, no fim, uma linha ensinando o usuario a
rodar a conferencia antes de tentar conectar, e dizendo em uma frase o que ela
prova e o que ela NAO prova.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ponte_arranque.py -q</automated>
  </verify>
  <done>`--conferir` valida config e segredo e sai com 0 ou 2 sem abrir socket; os cinco casos de `tests/test_ponte_arranque.py` passam (ou pulam com mensagem clara se o `.venv` nao existir); o `PORTAO-DISCORD.txt` ensina o comando e e honesto sobre o que ele nao cobre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` (versionado, editado a mao) -> processo | Entrada de configuracao que decide o que a ponte escuta e para onde ela vai falar |
| `.env` -> processo | Segredo com permissao total sobre o bot |
| Painel do Discord -> processo | O estado de um botao que nenhum codigo controla decide se o produto funciona |
| Conversa do Chatwoot -> scanner (indireto) | Uma conversa que e canal de anuncio E canal de comando mistura dois fluxos de confianca |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | `Ponte.setup_hook` / intent MESSAGE CONTENT | high | mitigate | Pre-voo deterministico lendo `application_flags` antes do IDENTIFY, matando o arranque com a nossa mensagem; `PrivilegedIntentsRequired` como rede; heuristica de runtime como terceira linha, avisando e nunca descartando |
| T-01-06 | Elevation of Privilege | `conversa_de_destino` vs `CHATWOOT_CONVERSAS_COMANDO` | medium | mitigate | Recusa no arranque comparando as duas listas (Task 1). A razao registrada e a real: uma RESPOSTA de terceiro numa conversa de anuncio compartilhada chega como `incoming` e seria lida como comando |
| T-01-07 | Information Disclosure | mensagens de erro de config e de login | high | mitigate | Nenhuma mensagem interpola o valor do token; teste dedicado provoca todos os erros e afirma a ausencia do valor; `LoginFailure` e reescrita pela nossa mensagem |
| T-01-08 | Tampering | `guild` / `canais` com tipo errado passando calado | medium | mitigate | Recusa explicita de `bool` antes de checar `int`, com teste; recusa de elemento nao inteiro na lista de canais |
| T-01-09 | Denial of Service | `--conferir` abrindo conexao por engano numa futura mudanca | low | mitigate | Teste com `timeout` no `subprocess`: um `--conferir` que fale com a rede trava e denuncia |
</threat_model>

<verification>
1. `python -m pytest tests/test_ponte_config.py tests/test_ponte_nucleo.py tests/test_ponte_arranque.py -q` passa inteiro.
2. `python -m pytest -q` (a suite inteira) continua verde.
3. `.venv/Scripts/python.exe -m l2scanner.ponte_discord --conferir` com o `.env` real ausente ou incompleto sai com 2 e nomeia a chave.
4. `git diff --stat l2scanner/notificador.py l2scanner/__main__.py` sai vazio.
5. PENDENTE ATE O PORTAO HUMANO: com a intent DESLIGADA no painel, a ponte morre no arranque com a mensagem em portugues; com ela LIGADA, sobe.
</verification>

<success_criteria>
- Toda chave faltando ou com tipo errado derruba o arranque nomeando a chave e o conserto (CONF-03).
- O token so sai do `.env` e nunca aparece em nenhuma saida (CONF-01).
- `simulacao` existe, vale `true` por padrao e recusa `false` citando a Fase 3 (CONF-04).
- A intent desligada no painel impede o arranque com a nossa mensagem, nao com um traceback de biblioteca.
- Tudo acima e conferivel sem o portao humano.
</success_criteria>

<output>
Create `.planning/workstreams/discord/phases/01-ponte-viva-conexao-provada-e-texto-real/01-02-SUMMARY.md` when done
</output>
