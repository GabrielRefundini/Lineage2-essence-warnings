---
phase: 01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - l2scanner/renda_leitura.py
  - l2scanner/calibrar_renda_moldes.py
  - calibrar-renda-moldes.bat
  - tests/test_renda_glifos.py
  - tests/test_calibrar_renda_moldes.py
  - tests/fixtures/renda/moldes_da_barra.json
autonomous: false
requirements: [LEIT-09]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O glifo da fonte da barra e 5 de largura por 10 de altura, e a virgula tem 2 de largura — medido nas DUAS instancias sobre um recorte de L-Coin SEM icone (M-K). A altura 17 do M-I era o icone de moeda dentro do recorte esticando a faixa unica que segmentar_glifos devolve para o retangulo inteiro."
    - "A guarda de altura do cortador compara contra 10, e nunca contra 17: medido, uma guarda escrita contra 17 recusaria TODO molde legitimo desta barra, e o modo de falha seria um cortador que nunca corta nada (M-K, corrigindo M-I)."
    - "A altura de faixa e medida DEPOIS do descarte posicional dos icones e nunca antes — porque foi exatamente o icone dentro do recorte que produziu o 17 do M-I, e porque `1560:1690` ainda deixa entrar um run de largura 9 que estica a faixa de 10 para 17 (M-K, segundo ponto)."
    - "A conclusao do M-I sobrevive inteira: os 13 moldes de mercado_templates_de_digito continuam sem transferir. A diferenca e 4x9 contra 5x10 — um pixel em cada eixo — e nao 9 contra 17, e a refutacao vai escrita onde o numero errado estava, em vez de o numero ser trocado em silencio."
    - "O cortador colhe de QUALQUER campo da barra — adena, L-Coin, bonus, EXP — e nao apenas da regiao da adena. E uma fonte so na barra inteira: largura 5 confirmada por segmentacao no bonus e no EXP das duas instancias (M-L)."
    - "O conjunto 0-9 FECHA com as duas fixturas que ja existem, e o bloqueio 'esperar o farm produzir um 5 e um 7' CAIU: medido, o 5 esta no bonus da Faerlina (592%) e o 7 no EXP da Yazalaque (76.6646%) e na L-Coin da Yazalaque (9.790) — os tres na tela agora (M-L, com a L-Coin vinda do proprio M-K)."
    - "A ressalva do M-L viaja junto e nao e apagada: a regiao do EXP da Yazalaque, em piso alto e recorte largo, cola tudo num run unico de 141 px porque a BARRA VERDE de progresso entra na mascara. Colher dali exige recorte vertical apertado (1366:1386 medido funcionando) ou piso mais alto — e o bonus nao tem esse problema e sozinho ja entrega o 5."
    - "Um run unico anormalmente largo — os 141 px medidos — e RECUSADO com o frame e a largura nomeados, e nunca fatiado: fatiar 141 px por particionar_run fabricaria vinte e oito digitos que nunca estiveram na tela."
    - "A PENEIRA DE FORMA e UMA so nesta fase e ela mora no modulo PURO: `_glifos_do_numero` e escrita por ESTE plano em `l2scanner/renda_leitura.py`, e o `01-04` a CONSOME sem reescrever. Se o corte dos moldes e a leitura peneirassem corridas por regras diferentes, os moldes seriam cortados de um conjunto de corridas e lidos de outro."
    - "A seta continua ferramenta -> puro e nunca o contrario: `renda_leitura` nao importa nenhum modulo `calibrar_*`, e o cortador IMPORTA a peneira em vez de copia-la."
    - "A maquina propoe e o humano confirma, TUDO OU NADA: uma proposta parcial convida ao ENTER distraido exatamente sobre a parte que a maquina nao sabia, e a propria docstring de propor_rotulo escreve isso."
    - "A ferramenta NUNCA inventa molde, NUNCA copia molde de mercado_templates_de_digito, e NUNCA grava rotulo sem confirmacao humana — as tres proibicoes existem contra o MESMO modo de falha medido: meio conjunto falha ABERTO, um 8 sem molde de 8 casa com 0 a 0,7826 contra piso 0,4698, e a folga de 0,1628 nao pega."
    - "Um conjunto incompleto e gravado normalmente e ANUNCIADO por nome, com a consequencia escrita — a adena vai recusar ate os faltantes entrarem —, porque travar a gravacao jogaria fora moldes ja conferidos por um humano."
    - "A RODADA que produz tests/fixtures/renda/moldes_da_barra.json e um checkpoint:human-action com gate blocking-human, e nao uma tarefa auto. A verificacao da Tarefa 2 e inteiramente SINTETICA e fica verde sem a fixtura existir, e o 01-04 inteiro pende dela: um portao que passa sem tocar o artefato que ele existe para produzir e o modo de falha em que a adena sai recusada com a suite toda verde. Nenhum modo automatico aprova este checkpoint — nao ha CLI que olhe um recorte de 5x10 e diga se e um 6 ou um 8."
    - "A fixtura de moldes NAO existe ao fim da Tarefa 2, e isso e criterio: se ela aparecer la, so pode ter vindo de codigo, e molde gerado por codigo e exatamente a falha aberta do LEIT-09."
    - "Uma rodada que grava renda_moldes_da_barra deixa renda_por_personagem, mercado_templates_de_digito e toda outra chave de topo identicas, e existe CONTROLE POSITIVO provando que a rodada escreveu algo."
  artifacts:
    - l2scanner/renda_leitura.py
    - l2scanner/calibrar_renda_moldes.py
    - calibrar-renda-moldes.bat
    - tests/test_renda_glifos.py
    - tests/test_calibrar_renda_moldes.py
    - tests/fixtures/renda/moldes_da_barra.json
  key_links:
    - "renda_leitura._glifos_do_numero -> calibrar_renda_moldes: o cortador IMPORTA a peneira do modulo puro. Este plano e o DONO da funcao; o `01-04` e consumidor. Uma segunda peneira faria os moldes serem cortados de um conjunto de corridas e lidos de outro, e o desalinhamento apareceria como pontuacao baixa que alguem consertaria baixando o piso."
    - "_glifos_do_numero -> mercado_leitura.limite_de_glifo_unico: o limite do run largo e DERIVADO dos moldes e nunca gravado — a docstring daquela funcao ja escreve que gravar a copia criaria duas verdades sobre uma so geometria."
    - "_glifos_do_numero -> a altura de faixa: a faixa e recomputada sobre as colunas SOBREVIVENTES ao descarte. E a leitura direta do M-K: o icone dentro do recorte foi o que produziu a altura 17, e medir antes do descarte e reproduzir o defeito por dentro."
    - "calibrar_renda_moldes -> calibrar_mercado.propor_rotulo: a maquina propoe e o humano confirma, TUDO OU NADA. Reusar a funcao e nao reescreve-la e o que faz a proposta significar a mesma coisa dos dois lados."
    - "calibrar_renda_moldes -> mercado_visao.glifos_para_calibracao: o empacotamento e o MESMO do mercado, em chave nova. Formato conhecido significa que glifos_de_calibracao ja trata a lista como entrada nao confiavel de graca — altura dominante, rotulo repetido e dimensao declarada."
    - "calibrar_renda_moldes -> calibrar_mercado.cobertura_dos_glifos: os faltantes saem NOMEADOS e nao contados — 'faltam 2' nao diz onde procurar, 'faltam 5 e 7' diz."
    - "calibrar_renda_moldes -> Calibracao.renda_moldes_da_barra: chave de TOPO propria, escrita por load-mutate-save. Ela NAO se funde com mercado_templates_de_digito, e a proibicao ja esta escrita no repositorio (calibrar_mercado.py:3125-3131, commit e09a860) — a NEGOCIACAO depende daquela chave."
    - "calibrar_renda_moldes -> os QUATRO campos da barra: adena, L-Coin, bonus e EXP sao superficie de colheita, e nao so a adena. E o M-L virado codigo: uma fonte so na barra inteira, e o conjunto fecha com as fixturas que existem."
---

<objective>
Cortar os moldes de digito da fonte da **barra** — trabalho novo, obrigado pela medicao que tirou
a adena do OCR (M-G, M-H) e provou que os moldes do mercado nao transferem (M-I) — e escrever, no
modulo puro, a **peneira de forma** que o cortador e o leitor do `01-04` usam em comum.

**Este plano nasceu de um corte declarado.** Ele era a Tarefa 4 do `01-03`. Aquele plano tinha
quatro tarefas e 148k de estimativa, violando a regra de 2-3 tarefas por plano, e ele mesmo
escreveu onde o corte deveria cair: *"o corte natural, se o orcamento apertar, e a Tarefa 4 inteira
virar um plano proprio (`01-05`) — ela ja mora em modulo e `.bat` proprios justamente para que
esse corte custe uma renumeracao e nao uma reescrita, e ela nao divide arquivo com nenhuma das
outras tres, entao continuaria na onda 2, sem mexer na onda de ninguem."* O corte aconteceu
exatamente assim: **mesma onda, mesma dependencia (`01-01`), zero arquivo em comum com o `01-03`.**

Purpose: sem este plano a adena nao tem leitor. O `01-04` compoe o caminho de glifo, mas ele
consome um conjunto de moldes que ninguem cortou — e os treze do mercado nao servem. Este e o
plano que transforma pixel de uma fonte especifica em conjunto de moldes confirmado por olho
humano, e e a unica ferramenta desta casa que o usuario roda **mais de uma vez**.

Output: `l2scanner/renda_leitura.py` ganha `_glifos_do_numero`; `l2scanner/calibrar_renda_moldes.py`
e `calibrar-renda-moldes.bat`; dois arquivos de teste e a fixtura de moldes que o `01-04` consome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-CONTEXT.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-RESEARCH.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-01-SUMMARY.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-MEDICOES-DE-CAMPO.md
</context>

## O que este plano herda e nao reabre

As decisoes **CTX-1 a CTX-9** estao enunciadas em `01-01-PLAN.md`. Tres mandam aqui:

- **CTX-6 / CTX-6-linha** — prefixo `renda_`, e **nenhum numero do spike no fonte** fora de teste.
  O portao de arvore de sintaxe que prova isso e da Tarefa 3 do `01-03`; este plano nao o escreve,
  mas nasce **debaixo** dele e a verificacao final o roda.
- **CTX-7** — carregar o arquivo inteiro, mudar so o que e seu, reemitir tudo. Este plano escreve
  **uma** chave de topo, `renda_moldes_da_barra`, e mais nada.
- **A seta aponta ferramenta -> puro, e nunca o contrario.** `renda_leitura` e modulo puro: sem
  `argparse`, sem janela do OpenCV, sem relogio, e sem importar nenhum `calibrar_*`. O cortador
  importa a peneira; a peneira nunca importa o cortador.

A chave `renda_moldes_da_barra` e **criada** pelo `01-01` — dataclass, `salvar`, `carregar`,
conferencia de forma no arranque. **Este plano nao toca em `l2scanner/calibracao.py`.** Ele e o
**primeiro e unico escritor** daquela chave em toda a fase.

## A autoridade deste plano, e o que ela corrigiu duas vezes

O **"Adendo"** de `01-MEDICOES-DE-CAMPO.md` (M-G a M-J) tirou a adena do OCR e obrigou este
trabalho a existir. A **"Correcao ao adendo"** (M-K e M-L), escrita depois contra as mesmas duas
fixturas, corrigiu o adendo em dois pontos — e os dois batem em cheio neste plano. Ficam **citados
e respondidos**, e nao editados em silencio.

### M-K — o glifo da barra e 5x10, e nao 17 de altura

O M-I mediu `faixa=(9, 25)`, **altura 17**, sobre um recorte da adena que **incluia os icones de
moeda das duas pontas**. `segmentar_glifos` devolve **UMA** faixa para o retangulo inteiro — e
decisao de projeto dela, e esta documentada —, entao o icone, mais alto que o digito, empurrou o
topo e a base.

Medido de novo sobre a **L-Coin sem icone nenhum** (`1440,1355 90x40` na Faerlina), em `vmin` 180 e
190:

    faixa = (17, 26)   ALTURA = 10   larguras = [5, 5, 2, 5, 5, 5]   ->  1 3 , 0 9 1
    Yazalaque                        larguras = [5, 2, 5, 5, 5]      ->      9 , 7 9 0

Contagem exata nas duas. **O digito da barra e 5 de largura por 10 de altura; a virgula tem 2.**
Os moldes do mercado sao `altura: 9, largura: 4`.

**O que isso muda aqui, e por que nao e detalhe editorial:**

1. **A conclusao do M-I sobrevive; o numero dela cai.** Os moldes do mercado continuam sem
   transferir — 4x9 contra 5x10 —, mas a distancia e de **um pixel em cada eixo**, e nao de 9
   contra 17. A guarda de altura deste cortador compara contra **10**. **Uma guarda escrita contra
   17 recusaria todo molde legitimo desta barra**, e o modo de falha seria um cortador que nunca
   corta nada — uma ferramenta que roda, sai com codigo 0 e grava zero moldes, a pior forma de
   defeito porque ela **parece** funcionar.
2. **A faixa e medida DEPOIS do descarte posicional, e nunca antes.** Foi o icone **dentro** do
   recorte que produziu o 17. Medir a altura sobre o retangulo inteiro e reproduzir o defeito do
   M-I por dentro da ferramenta que existe para nao reproduzi-lo. O M-K carrega o aviso com
   numero: em `1560:1690` ainda entra um run de **largura 9** no fim, e e ele que estica a faixa de
   10 para 17. Isso **nao** invalida o descarte por largura — um run de 9 e descartavel —, mas
   mostra que **a faixa compartilhada e sensivel ao icone mesmo quando o digito nao e**.

### M-L — os digitos `5` e `7` nao estao faltando, e o bloqueio nao existe

A revisao anterior deste plano registrou, como bloqueio sem conserto no horizonte, que as duas
fixturas dao `0 1 2 3 4 6 8 9` e que o `5` e o `7` teriam de **esperar o farm**. Ela escreveu isso
como um estado a suportar, e o `01-04` chegou a carregar dois desfechos por causa dele.

**Medido: eles estao na tela agora, em outros campos da MESMA barra.**

| campo | personagem | valor | o que ele entrega |
|---|---|---|---|
| bonus | Faerlina | `592%` | **5**, 9, 2 |
| EXP | Yazalaque | `76.6646%` | **7**, 6, 4 |
| L-Coin | Yazalaque | `9.790` | **7**, 9, 0 *(do proprio M-K)* |
| bonus | Yazalaque | `612%` | 6, 1, 2 |
| L-Coin | Faerlina | `13.091` | 1, 3, 0, 9 |
| adena | Faerlina | `13.160.684` | 1, 3, 6, 0, 8, 4 |

Largura 5 confirmada por segmentacao em **todos** eles — bonus da Faerlina em `vmin=180`:
`[2, 11, 2, 5, 5]`; bonus da Yazalaque: `[2, 11, 2, 5, 5, 5]`; EXP da Yazalaque: `[5, 5, 2, 5, ...]`.
**E uma fonte so na barra inteira.**

**Consequencia, e ela e a que muda o desenho desta ferramenta:** o conjunto 0-9 **fecha com as duas
fixturas que ja existem**, desde que o cortador possa colher de **qualquer campo da barra** e nao
so da regiao da adena. O bloqueio era artefato de restringir a colheita a uma regiao. **Ele cai**,
e com ele cai a busca por gravacoes antigas com fundo escuro que a revisao anterior planejou como
plano B — a fusao de iluminacoes diferentes deixou de ter motivo, o que e a melhor forma de fechar
aquele risco.

**A ressalva do M-L viaja junto, e ela e real e medida.** A regiao do **EXP da Yazalaque**, em
`vmin` alto e recorte largo, **cola tudo num run unico de 141 px**, porque a **barra verde de
progresso** atras do texto entra na mascara. Colher dali exige **recorte vertical apertado**
(`1366:1386` medido funcionando) ou piso mais alto. O **bonus nao tem esse problema** e sozinho ja
entrega o `5`; e o `7`, alem do EXP, aparece na **L-Coin da Yazalaque** (`9.790`), que o proprio
M-K cortou **sem icone e sem barra verde** — o caminho mais barato dos dois. O executor mede os
dois e escreve no SUMMARY qual fechou o `7`.

## O que este plano NAO fecha

Ele nao le a adena. Ele produz o **conjunto de moldes** e a **peneira de forma**; quem compoe
`mascara -> segmentar -> peneirar -> ler_glifos -> numero_valido` e imprime a adena na grafia do
jogo e o `01-04`. E ele nao escolhe retangulo nem piso — isso e do `01-03`, que corre na **mesma
onda** e sem dividir um unico arquivo com este.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: A peneira de forma no modulo PURO — e a faixa medida depois do descarte, porque foi o icone que produziu o 17</name>
  <files>l2scanner/renda_leitura.py, tests/test_renda_glifos.py</files>
  <read_first>
    - `01-MEDICOES-DE-CAMPO.md`, secao **"Correcao ao adendo"**, achado **M-K** INTEIRO — a autoridade desta tarefa. O glifo e **5x10**; a altura 17 do M-I era o icone dentro do recorte esticando a faixa unica; e em `1560:1690` um run de **largura 9** ainda entra e estica a faixa de 10 para 17. **Este achado manda sobre qualquer numero que apareca escrito como 17 em qualquer plano desta fase.**
    - `01-MEDICOES-DE-CAMPO.md`, achado **M-J** — as larguras `[15, 5, 2, 5, 5, 5, 2, 5, 5, 5, 16]` e `[15, 5, 5, 2, 5, 5, 5, 2, 5, 5, 7, 16]`, batendo com a verdade de campo nas duas instancias, estaveis de 180 a 190. Icone, digitos de 5, virgulas de 2, icone.
    - `l2scanner/mercado_leitura.py:362-400` — `segmentar_glifos_no_brilho`, e em particular **a decisao documentada de devolver UMA faixa para o retangulo inteiro**. E ela que explica o M-I, e e por causa dela que esta funcao recomputa a faixa.
    - `l2scanner/mercado_leitura.py:720-790` — `limite_de_glifo_unico` e `larguras_com_folga`. A docstring de `limite_de_glifo_unico` tem a regra que esta tarefa obedece: o limite e **derivado dos moldes** e nunca gravado, porque *"gravar uma copia criaria DUAS VERDADES sobre uma so geometria, e na recalibracao seguinte a copia envelheceria contra os moldes que ela descreve."*
    - `l2scanner/mercado_leitura.py` — `particionar_run` e a `folga_de_cola`. **Ler com esta pergunta:** por que esta peneira RECUSA um run largo em vez de fatia-lo. Um run de 141 px (o do EXP da Yazalaque, M-L) fatiado por folga inteira viraria vinte e oito digitos que nunca estiveram na tela.
    - `l2scanner/renda_leitura.py` inteiro, como o `01-01` o deixou — o charter de modulo puro, e o ponto onde esta funcao entra.
  </read_first>
  <behavior>
    - Dado o vetor de corridas `[15, 5, 2, 5, 5, 5, 2, 5, 5, 5, 16]` medido no M-J, a peneira descarta as duas pontas e devolve as nove do meio.
    - Um run largo no **MEIO** nao e descartado: ele **derruba o recorte inteiro** com motivo nomeado. Descartar ali apagaria um digito e devolveria um numero mais curto e plausivel.
    - **Zero** ou **dois** runs largos numa ponta e recusa de forma: significa que o recorte deixou de conter exatamente um numero.
    - Um run **anormalmente largo** — a ordem dos 141 px medidos no EXP da Yazalaque — e recusado com a largura e o motivo nomeados, e **nunca** fatiado.
    - A **faixa de linhas** devolvida e recomputada sobre as colunas **sobreviventes** ao descarte. Sobre o recorte do M-K (L-Coin, sem icone) ela devolve altura **10**; sobre um recorte com icone de ponta, ela devolve **10** e nao 17 — porque o icone saiu antes de ela ser medida.
    - O limite de run largo vem de `limite_de_glifo_unico(moldes)` por parametro, **sem default**: nenhuma geometria entra como constante nesta funcao.
    - A funcao e **pura**: sem disco, sem OCR, sem relogio, sem janela.
  </behavior>
  <action>
Escrever `_glifos_do_numero(mascara, faixa, runs, *, limite)` em **`l2scanner/renda_leitura.py`** —
o modulo puro do `01-01` — e **nao** dentro do cortador da Tarefa 2. **Este plano e o DONO desta
funcao.** A razao e de ordem e de seta: o cortador precisa dela nesta mesma onda e o leitor do
`01-04` precisa dela na onda 3, e a seta desta casa aponta **ferramenta -> puro** e nunca o
contrario. Se ela morasse na ferramenta, `renda_leitura` teria de importar um calibrador para ler
um numero.

Escrever junto, na docstring, o motivo pelo qual as duas pontas usam **uma so**: se o corte dos
moldes e a leitura peneirassem corridas por regras diferentes, os moldes seriam cortados de um
conjunto de corridas e lidos de outro, e o desalinhamento apareceria como **pontuacao baixa** que
alguem "consertaria" baixando o piso de leitura — trocando um defeito visivel por um invisivel.

**As tres regras de forma, nesta ordem, com o numero medido ao lado de cada uma:**

1. **Um run largo em cada PONTA e icone, e sai.** Medido: `[15, ..., 16]` contra digitos de 5 e
   virgulas de 2 (M-J). O limite vem de `limite_de_glifo_unico(moldes)`, **derivado e nunca
   gravado**.
2. **Um run largo no MEIO nao e descartado: ele derruba o recorte**, com motivo nomeado. Descartar
   ali apagaria um digito e devolveria um numero mais curto e plausivel — e numero plausivel e
   errado e exatamente o que esta fase existe para nao produzir.
3. **Exatamente um run largo em cada ponta, ou recusa de forma.** Zero ou dois significam que o
   recorte deixou de conter exatamente um numero.

**E a quarta regra, que nasce do M-K e e a razao de esta funcao devolver a faixa e nao so os
runs.** `segmentar_glifos_no_brilho` devolve **UMA** faixa de linhas para o retangulo inteiro — e
decisao de projeto dela e esta documentada. Foi essa decisao, com um icone dentro do recorte, que
produziu a **altura 17** do M-I sobre uma fonte de **altura 10**. Entao: **recomputar a faixa sobre
as colunas sobreviventes ao descarte**, e devolver essa. Escrever a medicao ao lado, porque quem
ler vai achar que e zelo: sobre a L-Coin sem icone a faixa e `(17, 26)`, altura **10**; sobre o
mesmo campo com o icone dentro, a faixa unica da 17; e em `1560:1690` basta um run de **largura 9**
no fim para esticar de 10 para 17 (M-K). Medir antes do descarte e reproduzir por dentro o defeito
que esta ferramenta existe para nao repetir.

**A guarda do run anormalmente largo, e ela e nova e medida.** Um run muito acima do limite de
glifo unico — a ordem dos **141 px** que o M-L mediu no EXP da Yazalaque, onde a barra verde de
progresso entra na mascara e cola o campo inteiro — e **recusado com a largura nomeada**, e
**nunca** fatiado. Escrever a aritmetica no comentario porque ela e o argumento inteiro: 141 px
fatiados em glifos de 5 dariam **vinte e oito** digitos que nunca estiveram na tela. `folga_de_cola`
nasce `null` no `01-01` justamente para que `particionar_run` nao seja chamado aqui; esta guarda e
o cinto sobre o suspensorio, e ela existe porque o caso foi **medido** e nao imaginado.

Escrever `tests/test_renda_glifos.py` com todos os casos do bloco `behavior`, sobre **mascaras
sinteticas montadas a mao** com as larguras do M-J — sem OCR, sem disco, sem `recordings/`. Os
casos que mais importam sao quatro:

- **O descarte posicional**: `[15, 5, 2, 5, 5, 5, 2, 5, 5, 5, 16]` devolve as nove do meio.
- **O run largo no MEIO recusando**, e nao sendo descartado.
- **A FAIXA MEDIDA DEPOIS DO DESCARTE**, que e o M-K virado teste: um recorte cuja faixa bruta tem
  altura 17 por causa de um icone de ponta devolve, depois da peneira, altura **10**. Sem este
  caso, a correcao do M-K e uma frase no plano em vez de um comportamento no codigo.
- **O run de 141 px recusado e nao fatiado**, com a largura na mensagem.
  </action>
  <verify>
    <automated>PYTHONPATH=. python -m pytest tests/test_renda_glifos.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=. python -m pytest tests/test_renda_glifos.py -q -rs` termina em 0 **sem skip** — o arquivo nao depende de OCR, de disco nem de `recordings/`.
    - `grep -c "def _glifos_do_numero" l2scanner/renda_leitura.py` retorna **1**. Esta e a unica peneira desta fase.
    - A seta continua ferramenta -> puro: `grep -E "^(from|import) +l2scanner\.calibrar" l2scanner/renda_leitura.py` nao acha nada.
    - Existe o caso da FAIXA RECOMPUTADA: uma faixa bruta de altura 17 com icone de ponta devolve altura **10** depois do descarte, e a mensagem do teste cita o M-K. **Este e o caso que prova que a correcao entrou no codigo e nao so no texto.**
    - Existe o caso do run largo no MEIO recusando, e o de zero/dois runs largos numa ponta recusando por forma.
    - Existe o caso do run de 141 px recusado com a largura nomeada, e uma asserção de que a peneira **nao** devolveu glifo nenhum a partir dele.
    - `limite` e somente-nomeado e sem default: existe o caso que afirma `TypeError` na omissao.
    - `PYTHONPATH=. python -m pytest tests/ -q` termina em 0.
  </acceptance_criteria>
  <done>Existe UMA peneira de forma, no modulo puro, com as tres regras posicionais e a faixa recomputada depois do descarte; a altura que ela devolve para a fonte da barra e **10**, e o caso que prova isso cita a medicao que derrubou o 17.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: O cortador dos moldes 5x10 — colhe de QUALQUER campo da barra, a maquina propoe, o humano confirma</name>
  <files>l2scanner/calibrar_renda_moldes.py, calibrar-renda-moldes.bat, tests/test_calibrar_renda_moldes.py</files>
  <read_first>
    - `01-MEDICOES-DE-CAMPO.md`, secao **"Correcao ao adendo"**, achados **M-K e M-L** INTEIROS. **Eles mandam sobre o "Adendo" onde os dois se cruzam.** M-K: o glifo e **5x10** e a altura 17 do M-I era o icone. M-L: o `5` e o `7` **ja estao na tela**, no bonus da Faerlina e no EXP da Yazalaque, e o conjunto fecha com as fixturas que existem **se a colheita for de qualquer campo da barra**. Depois o "Adendo": M-I (os moldes do mercado nao transferem) e M-J (a segmentacao e perfeita em 180-190).
    - `l2scanner/calibrar_mercado.py:2206-2265` — `cortar_glifos` INTEIRO: o laco de um numero por vez, o ENTER que aceita, o fatiamento automatico do numero confirmado em glifos, e a medicao na docstring (10 de 10 linhas em `frame_000012`, 6 de 6 em `frame_000010`). **E o molde desta tarefa passo a passo, e nao de memoria.**
    - `l2scanner/calibrar_mercado.py:2102-2149` — `propor_rotulo` inteira, com as tres frases que mandam aqui: reusar a mecanica da matriz e o que faz o numero significar a mesma coisa dos dois lados; **TUDO OU NADA de proposito**, porque uma proposta parcial (`6?,00`) convida ao ENTER distraido justamente sobre a parte que a ferramenta nao sabia; e *"isto e uma SUGESTAO, nunca uma gravacao"*.
    - `l2scanner/calibrar_mercado.py:2455-2490` — `_conferir_os_glifos` e a matriz de confusao que **recusa gravar** quando os glifos nao sao separaveis. E `:2469-2473`, o argumento de nunca misturar geometrias diferentes na mesma matriz — o mesmo argumento do M-I, uma medicao depois.
    - `l2scanner/calibrar_mercado.py:1113-1126` — `cobertura_dos_glifos`, e a frase que decide o formato da saida desta tarefa: *"faltam 3" nao diz onde procurar; "faltam 6, 8, Adena" diz*.
    - `l2scanner/calibrar_mercado.py:3123-3133` — a proibicao escrita de fundir moldes cortados com iluminacao diferente, e a razao: `mercado_templates_de_digito` e chave de TOPO e a NEGOCIACAO depende dela. **Esta tarefa nao toca naquela chave em nenhuma circunstancia.**
    - `l2scanner/mercado_visao.py:207,630,667` — `molde_para_hex`, `glifos_para_calibracao` e `glifos_de_calibracao`, o desempacotador que trata a lista como **entrada nao confiavel** e confere altura dominante, rotulo repetido e dimensao declarada. Reusar o formato e o que faz essa conferencia vir de graca.
    - `l2scanner/mercado_leitura.py:312-331` — `conjunto_descreve_numeros` e a medicao do conjunto incompleto. **Ler com esta pergunta:** por que esta tarefa NAO completa o conjunto, e por que `False` com oito rotulos e o comportamento certo e nao um defeito a contornar.
    - `l2scanner/calibrar.py:1188` e `1234-1236` — o `carregar` na primeira linha util e a mutacao que toca exatamente o que e dela.
    - `tools/medir_largura_de_run.py:1-60` — a disciplina de uma ferramenta que varre `recordings/`, e a razao pela qual nenhum teste pode depender daquela pasta (`tests/test_mercado_glifos.py:4-8`).
  </read_first>
  <behavior>
    - A ferramenta colhe de **qualquer campo da barra** que o usuario apontar — adena, L-Coin, bonus, EXP —, e nao apenas da regiao da adena.
    - Sobre um recorte, ela propoe **o numero inteiro** ou **nada**: um rotulo por corrida, e um so que nao passe no piso E na margem derruba a proposta toda. Nao existe proposta parcial.
    - Um recorte sem molde nenhum gravado ainda nao produz proposta: a primeira rodada e digitada pelo humano por inteiro, e a ferramenta **diz isso** em vez de propor vazio.
    - A peneira de forma e `renda_leitura._glifos_do_numero`, **importada e nunca copiada**; o recorte que ela recusa e pulado com o frame nomeado.
    - A altura de faixa comparada com a dominante dos moldes ja gravados e a que a peneira devolve — **depois** do descarte dos icones. Alturas diferentes: o recorte e **pulado**, com o frame e as duas alturas nomeadas.
    - Um recorte cujo run unico e da ordem dos 141 px medidos no EXP da Yazalaque e **pulado com a largura nomeada e a causa escrita** — a barra verde de progresso entrou na mascara —, junto da sugestao medida: recorte vertical apertado (`1366:1386`) ou piso mais alto.
    - A cobertura sai **por nome** a cada rodada: quais rotulos ja existem, quais faltam. Nunca so a contagem.
    - Um conjunto **incompleto** e gravado normalmente. `conjunto_descreve_numeros` sobre ele devolve `False`, e a ferramenta **diz isso em voz alta** com a consequencia escrita — a adena vai recusar ate os faltantes entrarem — em vez de tratar como erro.
    - A ferramenta **nunca** inventa molde, nunca copia molde de `mercado_templates_de_digito`, e nunca grava rotulo sem confirmacao humana.
    - Se a matriz de confusao mostrar que dois rotulos nao sao separaveis, a rodada **nao grava** e nomeia o par.
    - `--propor` roda sobre uma gravacao, imprime o que proporia frame a frame, e **nao escreve no disco**.
    - Uma rodada que grava `renda_moldes_da_barra` deixa `renda_por_personagem`, `mercado_templates_de_digito` e toda outra chave de topo identicas.
  </behavior>
  <action>
Escrever `l2scanner/calibrar_renda_moldes.py`: o cortador de moldes da fonte da **barra**, seguindo
`calibrar_mercado.cortar_glifos` passo a passo. A ferramenta mora em modulo proprio, e nao dentro
do `calibrar_renda.py` do `01-03`, por duas razoes que vao escritas: os dois assuntos sao
diferentes — um escolhe retangulos e pisos, o outro corta tipografia — e o cortador e a unica
parte desta fase que o usuario vai rodar **mais de uma vez**.

**Por que esta tarefa existe, com o numero na primeira linha da docstring do modulo, e com a
refutacao ao lado.** A fonte da barra tem glifo de **5 de largura por 10 de altura** e os treze
moldes de `mercado_templates_de_digito` sao **4x9**. **Eles nao transferem** — esta e a resposta
medida a primeira pergunta em aberto que a pesquisa deixou, e a resposta e "nao" (M-I). **E a
docstring escreve, ao lado, que o numero 17 que circulou antes esta refutado**: o M-I mediu a
altura sobre um recorte que continha os icones de moeda, e `segmentar_glifos` devolve UMA faixa
para o retangulo inteiro, entao o icone empurrou o topo e a base; a medicao limpa, sobre a L-Coin
sem icone, deu **10** nas duas instancias (M-K). Escrever a consequencia junto, porque ela e o
motivo de a refutacao estar no fonte e nao so no plano: **uma guarda de altura escrita contra 17
recusaria todo molde legitimo desta barra, e o modo de falha seria um cortador que nunca corta
nada.** Escrever tambem o argumento que ja existe no repositorio, com endereco:
`_conferir_os_glifos:2469-2473` proibe misturar geometrias diferentes na mesma matriz porque
`_alinhar_por_preenchimento` poe o glifo pequeno numa caixa grande e o casamento passa a ser
dominado por area vazia. O M-I e aquele argumento com um numero — e um pixel em cada eixo basta.

**E por que o caminho existe:** `segmentar_glifos_no_brilho` segmenta a barra **perfeitamente** em
`vmin` 180-190, justo onde o OCR devolve vazio, com as larguras do M-J batendo com a verdade de
campo nas duas instancias. A materia-prima esta la; o que falta e o conjunto de moldes.

**A COLHEITA E DA BARRA INTEIRA, e nao da regiao da adena — e este e o ponto em que este plano
mudou.** `--campo` aceita `adena`, `lcoin`, `bonus` e `exp`, e a ferramenta corta de qualquer um
deles. Escrever no fonte a medicao que autoriza isso: largura 5 confirmada por segmentacao no
bonus da Faerlina (`[2, 11, 2, 5, 5]`), no bonus da Yazalaque (`[2, 11, 2, 5, 5, 5]`) e no EXP da
Yazalaque (`[5, 5, 2, 5, ...]`) — **e uma fonte so na barra inteira** (M-L).

**E escrever, na mesma docstring, o bloqueio que caiu e por que ele existia.** A revisao anterior
desta ferramenta registrou que faltavam o `5` e o `7` e que eles so apareceriam "com o tempo de
farm" — e desenhou um plano B de varrer gravacoes antigas de fundo escuro para tentar fecha-los.
**Medido, os dois estao na tela agora**: o `5` no bonus da Faerlina (`592%`) e o `7` no EXP da
Yazalaque (`76.6646%`) e na L-Coin da Yazalaque (`9.790`, cortada pelo proprio M-K sem icone e sem
barra verde). O bloqueio era **artefato de restringir a colheita a uma regiao**, e cai com o
`--campo`. Com ele cai tambem o plano B: **a ferramenta nao precisa mais varrer gravacao antiga
para fechar o conjunto**, e a tentacao de fundir iluminacoes diferentes perde o motivo — que e a
melhor forma de fechar aquele risco, melhor do que qualquer guarda.

**A ressalva do M-L entra como comportamento, e nao como nota.** A regiao do EXP da Yazalaque, em
piso alto e recorte largo, **cola tudo num run unico de 141 px** porque a barra verde de progresso
entra na mascara. A ferramenta **pula** esse recorte, nomeia a largura, escreve a causa e imprime a
sugestao medida: recorte vertical apertado — `1366:1386` funcionou — ou piso mais alto. Ela **nao**
fatia: 141 px em glifos de 5 dariam vinte e oito digitos que nunca estiveram na tela. E ela diz ao
usuario a saida barata: **o bonus nao tem esse problema e sozinho entrega o `5`; a L-Coin da
Yazalaque entrega o `7` sem icone e sem barra verde.**

`propor_e_confirmar(...)`: o laco central, e ele e o `cortar_glifos` com um campo em vez de uma
tabela. Para cada recorte: mascara e segmenta no piso de glifo daquele personagem, peneira por
`renda_leitura._glifos_do_numero` (**importada**, pulando o recorte com o frame nomeado quando ela
recusa), propoe por `calibrar_mercado.propor_rotulo` contra os moldes ja gravados, e mostra ao
humano. **ENTER aceita, digitar corrige, ESC pula.** A proposta e TUDO OU NADA pela razao que a
propria `propor_rotulo` escreve: uma proposta parcial convida ao ENTER distraido exatamente sobre a
parte que a maquina nao sabia. Na primeira rodada nao ha moldes e nao ha proposta — a ferramenta
diz isso e o humano digita o numero inteiro, que e o unico jeito de o primeiro molde nascer certo.

**A guarda de altura, e ela e a que impede a ferramenta de reproduzir por dentro o defeito que o
M-I mediu.** Antes de fatiar, comparar a altura da faixa daquele recorte — **a que a peneira
devolveu, ja depois do descarte dos icones** — com a **altura dominante dos moldes ja gravados**.
Diferentes: pular o recorte e nomear o frame e as duas alturas. Nao gravar. A altura **nao vira
chave**: ela sai dos proprios moldes, pela regra que a docstring de `limite_de_glifo_unico` ja
escreve — gravar uma copia de algo derivado dos moldes cria duas verdades sobre uma so geometria, e
a copia envelhece na recalibracao seguinte. **E a ordem importa e vai escrita:** medir a altura
antes do descarte e reproduzir o M-I, porque foi o icone dentro do recorte que deu 17 sobre uma
fonte de 10.

**A cobertura sai por NOME, e essa e a interface inteira desta ferramenta com o usuario.** Ao fim
de cada rodada, `calibrar_mercado.cobertura_dos_glifos` sobre o conjunto acumulado: o que ja
existe, e **o que falta, nomeado**. A frase que justifica esta escrita naquela funcao — *"faltam
3" nao diz onde procurar; "faltam 6, 8, Adena" diz*. **E quando faltar algo, a ferramenta diz
tambem ONDE PROCURAR na tela**, porque agora ela sabe: os campos da barra e os digitos que o M-L
mediu em cada um.

**A ferramenta trabalha com o conjunto INCOMPLETO e nao trava**, e isso continua verdade mesmo com
o bloqueio derrubado. Travar a gravacao ate os onze estarem la jogaria fora moldes ja conferidos
por um humano por causa de uma rodada interrompida ou de um campo ainda nao varrido. Entao: **grava
incompleto, e anuncia**. A ferramenta roda `conjunto_descreve_numeros` sobre o que gravou e, quando
ela devolve `False`, imprime em voz alta, com a consequencia e o conserto: *o conjunto esta
incompleto, faltam X e Y, e enquanto faltarem a adena vai RECUSAR em vez de ser lida — rode de
novo apontando `--campo bonus` ou `--campo lcoin`.* Isso nao e um erro; e o LEIT-09 funcionando.
**O que mudou e o conserto**: ele deixou de ser "farmar ate o digito aparecer" e passou a ser
"varrer outro campo da barra".

**E o que ela NUNCA faz, com a medicao ao lado de cada proibicao:** nao inventa molde vazio para
completar o conjunto; nao copia molde de `mercado_templates_de_digito` (geometria errada — 4x9
contra 5x10, M-I com o numero do M-K — e proibido por escrito,
`calibrar_mercado.py:3125-3131`); nao grava rotulo sem confirmacao humana. As tres proibicoes
existem contra o **mesmo** modo de falha, e ele esta medido na docstring de
`conjunto_descreve_numeros`: um conjunto pela metade **falha aberto** — um `8` sem molde de `8`
casa com `0` a 0,7826 contra piso 0,4698, com folga de 0,1628 sobre o segundo colocado, o que
significa que nem a margem pega. Um conjunto completado com lixo nao produz meia leitura: produz a
leitura errada com a confianca da certa.

**A matriz de confusao antes de gravar**, no molde de `_conferir_os_glifos`: se dois rotulos nao
forem separaveis com os limiares de leitura, **nao gravar** e nomear o par. Uma copia da mensagem
daquela funcao — *"nada foi gravado: os glifos precisam ser separaveis primeiro"* — e o precedente
certo, porque ela diz ao usuario que o estado anterior esta intacto.

**De onde vem o pixel.** `--gravacoes` aponta uma pasta e a ferramenta varre os frames compativeis,
no idioma de `tools/medir_largura_de_run.py`. A fonte padrao e
`recordings/20260902-004500-renda-duas-instancias`, e **as duas fixturas de campo bastam** — com
`--campo` varrendo adena, L-Coin, bonus e EXP, elas entregam os dez digitos e a virgula (M-L). Nao
ha ramo de gravacao antiga neste plano, e a razao vai escrita: ele existia para caçar o `5` e o
`7`, e o M-L mostrou que os dois ja estao nestas duas fixturas.

`--propor` roda a varredura, imprime o que proporia frame a frame, e **nao escreve no disco** — e o
que da a esta tarefa uma linha de comando verificavel sem o jogo aberto e sem tocar a calibracao.

O `salvar` no molde do `calibrar_tiat`: `carregar` na primeira linha util, mutar **somente**
`renda_moldes_da_barra`, reemitir tudo. E o mesmo `try/except OSError` com a mensagem que afirma
que a calibracao nao foi salva.

Escrever `calibrar-renda-moldes.bat` no dialeto B, irmao do `calibrar-renda.bat` do `01-03`, com o
cabecalho `REM` dizendo **quando rodar** — e aqui o texto e diferente de todos os outros `.bat`
desta casa: *rode de novo, apontando outro `--campo`, quando a saida disser que faltam digitos*.

Escrever `tests/test_calibrar_renda_moldes.py` com todos os casos do bloco `behavior`, com dados
montados a mao — mascaras sinteticas com as larguras do M-J —, **sem OCR, sem disco e sem
`recordings/`**. Os casos que mais importam sao quatro:

- **A guarda de altura contra 10**: um recorte cuja faixa peneirada tem altura 9 contra moldes de
  altura **10** e pulado, com as duas alturas na mensagem. E o M-I virado teste, com o numero do
  M-K. **Ao lado dele, o controle que impede a regressao inversa:** um recorte legitimo de altura
  **10** contra moldes de altura **10** e ACEITO. Sem este segundo caso, um cortador que recusa
  tudo passaria na suite — que e exatamente o defeito que a guarda contra 17 teria produzido.
- **O run de 141 px pulado**, com a largura e a causa nomeadas, e nada gravado a partir dele.
- **O conjunto incompleto**: gravar oito rotulos e afirmar que a gravacao **aconteceu**, que
  `conjunto_descreve_numeros` sobre o resultado devolve `False`, e que a saida lista os faltantes
  **por nome**. Ao lado, o controle negativo: nenhum molde alem dos confirmados foi gravado.
- **A peneira e importada e nao copiada**: o cortador chama `renda_leitura._glifos_do_numero` e nao
  define uma segunda.

**Esta tarefa NAO produz `tests/fixtures/renda/moldes_da_barra.json`, e essa e uma correcao desta
revisao.** A fixtura so pode nascer de uma rodada em que um humano confirmou cada rotulo olhando
para o recorte ampliado — e isso e a **Tarefa 3**, um checkpoint. Escrever aqui, num comentario no
topo do modulo, para quem for executar: o que esta tarefa entrega e a **ferramenta** e a suite
sintetica que a prende; o que fecha o LEIT-09 e a **rodada**. Uma fixtura de moldes gerada por
codigo de teste seria a falha aberta do LEIT-09 entrando pela suite — meio conjunto le errado com a
confianca do certo —, e por isso ela nao pode sair daqui nem por conveniencia.
  </action>
  <verify>
    <automated>PYTHONPATH=. python -m pytest tests/test_calibrar_renda_moldes.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=. python -m pytest tests/test_calibrar_renda_moldes.py -q -rs` termina em 0 **sem skip** — o arquivo nao depende de OCR, de disco nem de `recordings/`.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m l2scanner.calibrar_renda_moldes --gravacoes recordings/20260902-004500-renda-duas-instancias --personagem Faerlina --campo adena --propor` sai com codigo 0, imprime uma linha por frame com a proposta ou a razao de nao propor, e o conteudo parseado do `calibration.json` alvo continua identico — `--propor` nao escreve.
    - `--campo` aceita os quatro campos da barra e existe o caso que prova: `--campo bonus` e `--campo lcoin` propoem normalmente. A colheita **nao** e restrita a adena, e a docstring cita o M-L.
    - Existe a GUARDA DE ALTURA nos DOIS sentidos: altura 9 contra moldes de **10** e pulada com as duas alturas na mensagem; e altura **10** contra moldes de **10** e ACEITA. O segundo caso e obrigatorio — sem ele, um cortador que nunca corta passaria verde.
    - O numero **17 nao aparece** como altura de glifo da barra em `l2scanner/calibrar_renda_moldes.py` fora de uma frase de refutacao: `grep -n "17" l2scanner/calibrar_renda_moldes.py` so acha linha que tambem cita `M-K` ou `refutad`.
    - Existe o caso do run de **141 px** pulado, com a largura e a causa (a barra verde) nomeadas, e a asserção de que nenhum molde saiu dele.
    - A peneira e IMPORTADA e nao copiada: `grep -c "def _glifos_do_numero" l2scanner/calibrar_renda_moldes.py` retorna **0**, e `grep -v '^\s*#' l2scanner/calibrar_renda_moldes.py | grep -c "_glifos_do_numero"` retorna ao menos 1.
    - Existe o CONTROLE DO CONJUNTO INCOMPLETO com os dois lados: oito rotulos gravados com sucesso E `conjunto_descreve_numeros` devolvendo `False` E a saida listando os faltantes **por nome** E a mensagem dizendo o conserto novo (varrer outro campo) e nao o antigo (esperar o farm); mais a asserção de que nenhum molde alem dos confirmados foi gravado.
    - Nenhum caminho toca a chave do mercado: `grep -c "mercado_templates_de_digito" l2scanner/calibrar_renda_moldes.py` retorna 0, e existe um caso que roda a ferramenta sobre um `calibration.json` semeado e afirma que aquela chave voltou identica, com os 13 moldes contados antes da comparacao.
    - Uma rodada que grava `renda_moldes_da_barra` deixa `renda_por_personagem` e toda chave de topo restante identicas — o caso monkeypatcha `ARQUIVO_CALIBRACAO` para `tmp_path`.
    - **`tests/fixtures/renda/moldes_da_barra.json` NAO existe ao fim desta tarefa, e isso e criterio e nao pendencia:** `ls tests/fixtures/renda/moldes_da_barra.json` falha aqui, e a fixtura nasce na Tarefa 3, que e a rodada humana. Uma fixtura que aparecesse nesta tarefa so poderia ter vindo de codigo, e molde gerado por codigo e a falha aberta do LEIT-09.
    - `PYTHONPATH=. python -m pytest tests/ -q` termina em 0.
  </acceptance_criteria>
  <done>Existe um cortador de moldes para a fonte da barra, com a altura de **10 px** presa por guarda nos dois sentidos e a refutacao do 17 escrita no fonte; ele colhe de qualquer campo da barra, propoe para o humano confirmar; e ele grava um conjunto incompleto anunciando por nome o que falta e onde procura-lo, sem nunca inventar, copiar ou completar um molde. A ferramenta esta pronta; o conjunto de moldes e da Tarefa 3.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Tarefa 3: A rodada humana do cortador — o olho no recorte, e a fixtura de moldes que o `01-04` inteiro consome</name>
  <files>tests/fixtures/renda/moldes_da_barra.json</files>
  <action>Rodar o cortador de moldes com o olho no recorte, confirmando glifo a glifo, ate os onze rotulos da fonte da barra fecharem — e produzir `tests/fixtures/renda/moldes_da_barra.json`.</action>
  <instructions>
**Por que isto e um checkpoint e nao mais uma tarefa `auto`.** Ate esta revisao esta rodada morava
dentro da Tarefa 2, com verificacao automatizada inteiramente **sintetica** — mascaras montadas a
mao. Aquela suite fica verde **sem que `tests/fixtures/renda/moldes_da_barra.json` exista**, e o
`01-04` inteiro pende dessa fixtura: a Tarefa 1 dele a funde na calibracao de fixtura, e o criterio
1 da fase so fecha com a adena saindo como numero. Uma tarefa `auto` cuja unica prova nao toca o
artefato que ela existe para produzir e um portao que passa por engano — e o desfecho seria a
adena recusada com todos os testes verdes.

E ela nao pode ser automatizada por mim: **a maquina propoe, o humano confirma**. Nao ha CLI nem
API que olhe um recorte ampliado de 5x10 px e diga se aquilo e um `6` ou um `8`. Um rotulo
confirmado errado nao produz meia leitura — produz a leitura errada com a confianca da certa, e
esta medido na docstring de `conjunto_descreve_numeros`: um `8` sem molde de `8` casa com `0` a
0,7826 contra piso 0,4698, e a folga de 0,1628 sobre o segundo colocado significa que **nem a
margem pega**.

**O que eu ja fiz** (Tarefas 1 e 2): a peneira de forma no modulo puro, o cortador
`l2scanner/calibrar_renda_moldes.py`, o `calibrar-renda-moldes.bat`, a guarda de altura contra
**10** nos dois sentidos, o descarte dos icones por largura, a recusa do run de 141 px, e a suite
sintetica que prende tudo isso. A ferramenta esta pronta e `--propor` ja roda sem tocar o disco.

**O que so voce pode fazer — e a ordem importa, porque o `--campo` e o que o M-L abriu:**

1. `calibrar-renda-moldes.bat --gravacoes recordings/20260902-004500-renda-duas-instancias --personagem Faerlina --campo adena`
   Confirme, **glifo a glifo**, os rotulos propostos, comparando cada recorte ampliado com o numero
   que a barra mostra naquele frame. ENTER aceita, digitar corrige, ESC pula. Na primeira rodada
   nao ha moldes e nao ha proposta: voce digita o numero inteiro, e e assim que o primeiro molde
   nasce certo.
2. `--campo bonus` na **Faerlina** — e de onde sai o `5` (`592%`).
3. `--campo lcoin` na **Yazalaque** — e de onde sai o `7` (`9.790`), sem icone e sem barra verde.
4. Se voce for atras do `7` por `--campo exp` na Yazalaque, **espere o aviso do run de 141 px**: a
   barra verde de progresso entra na mascara e cola o campo inteiro. Reduza o recorte vertical para
   `1366:1386` ou suba o piso, como o M-L mediu. A ferramenta **nao** fatia aquele run — 141 px em
   glifos de 5 dariam vinte e oito digitos que nunca estiveram na tela.

**O que NAO fazer, e o motivo tem numero:** nao complete o conjunto a mao, nao copie molde de
`mercado_templates_de_digito` (4x9 contra 5x10 — M-I no veredicto, M-K no numero) e nao aceite um
rotulo que voce nao conseguiu ler no recorte. Um conjunto incompleto e um desfecho **legitimo** —
ele e gravado, anunciado por nome, e a adena recusa ate ele fechar. Um conjunto **completado** e a
falha aberta do LEIT-09.

**Se os onze nao fecharem:** pare e diga quais rotulos faltaram e em que campo voce procurou. O
esperado, depois do M-L, e que fechem com as duas fixturas que ja existem — nao fechar e
**informacao nova** e vai escrita, nao contornada.
  </instructions>
  <verification>
`tests/fixtures/renda/moldes_da_barra.json` existe e passa por `mercado_visao.glifos_de_calibracao`
sem levantar; `mercado_leitura.conjunto_descreve_numeros` sobre ela devolve `True` — os **onze**
rotulos, os dez digitos e a virgula; e `calibrar_mercado.cobertura_dos_glifos` lista os rotulos
**por nome**. A altura dominante dos moldes e **10**, e nao 17. Eu confiro os quatro depois que
voce disser que terminou, e registro no SUMMARY **quais campos foram varridos, quais rotulos cada
um entregou, e a altura de faixa medida em cada um** — uma altura que nao seja 10 e informacao nova
e vai escrita em vez de ajustada.
  </verification>
  <resume-signal>Diga "pronto" quando tiver confirmado os rotulos e a ferramenta tiver gravado; ou diga quais rotulos faltaram e em que campo voce procurou.</resume-signal>
  <done>`tests/fixtures/renda/moldes_da_barra.json` existe, com os onze rotulos confirmados por olho humano, altura dominante **10**, e passa por `glifos_de_calibracao` e por `conjunto_descreve_numeros`. O `01-04` tem o que consumir sem o jogo aberto.</done>
</task>

</tasks>

## Artifacts que este plano produz

- `l2scanner/renda_leitura.py` — ganha `_glifos_do_numero`, a peneira de forma UNICA da fase: tres
  regras posicionais com as larguras do M-J, a faixa recomputada **depois** do descarte (M-K), e a
  guarda do run anormalmente largo (M-L)
- `l2scanner/calibrar_renda_moldes.py` — o cortador dos moldes **5x10** da fonte da barra: colhe de
  qualquer campo, propoe por `propor_rotulo`, o humano confirma, a guarda de altura recusa
  geometria errada nos dois sentidos, a cobertura sai por nome com o campo onde procurar, e o
  conjunto incompleto e gravado com o aviso em voz alta
- `calibrar-renda-moldes.bat` — dialeto B, e o unico `.bat` desta casa cujo cabecalho diz para
  rodar **de novo, apontando outro campo**, quando a saida disser que faltam digitos
- `tests/test_renda_glifos.py` — a peneira pura, com o caso da faixa recomputada que e o M-K virado
  teste
- `tests/test_calibrar_renda_moldes.py` — a guarda de altura nos dois sentidos, o run de 141 px, o
  conjunto incompleto e a peneira importada, sem OCR, sem disco e sem `recordings/`
- `tests/fixtures/renda/moldes_da_barra.json` — o conjunto que a rodada humana da **Tarefa 3**
  produzir, com a cobertura REAL e nunca completado a mao. **Este plano para aqui.** Fundir esse
  conjunto na chave `renda_moldes_da_barra` de `tests/fixtures/renda/calibracao_de_fixture.json` e
  da **Tarefa 1 do `01-04`**, e a razao e de onda: o `01-03` corre nesta mesma onda 2 e um dos
  criterios dele afirma que aquele arquivo continua **identico** depois de uma rodada de
  `--so-medir`. Dois planos da mesma onda escrevendo e lendo a mesma fixtura e uma corrida, e a
  vitima seria o `01-03`, com falha intermitente e sem causa visivel. O `01-04` e a onda 3.

Nada de novo em `requirements.txt`. `l2scanner/calibracao.py` **nao** e tocado — a chave
`renda_moldes_da_barra` e do `01-01`; este plano e o primeiro escritor dela.
`tests/fixtures/renda/calibracao_de_fixture.json` **nao** e tocada aqui, pela razao acima.
`l2scanner/mercado_leitura.py`, `l2scanner/mercado_visao.py` e `l2scanner/calibrar_mercado.py` sao
**lidos e importados, nunca editados** — e `mercado_templates_de_digito` nao e tocada em
circunstancia nenhuma. `l2scanner/calibrar_renda.py` e do `01-03` e nao e tocado aqui: **os dois
planos correm na onda 2 sem dividir um unico arquivo.** Nenhum arquivo do workstream `dashboard`,
do `mercado` ou do caminho da party e editado.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixel -> molde de digito | Um rotulo errado gravado uma vez envenena toda leitura futura daquele digito, e nao ha teste que perceba — por isso o rotulo passa por olho humano e a matriz de confusao recusa gravar o que nao for separavel |
| icone -> faixa de linhas | `segmentar_glifos` devolve UMA faixa para o retangulo inteiro; um icone dentro do recorte esticou a faixa de 10 para 17 e produziu um numero errado que circulou por uma revisao inteira (M-K) |
| barra verde de progresso -> mascara | No EXP da Yazalaque a barra de progresso entra na mascara e cola o campo num run de 141 px; fatia-lo fabricaria vinte e oito digitos (M-L) |
| cortador -> `calibration.json` | O cortador entra num arquivo que party, mercado, tiat e o calibrador do `01-03` dividem |
| moldes do mercado -> moldes da barra | Duas geometrias de um pixel de diferenca em cada eixo — perto o bastante para alguem achar que servem, longe o bastante para o casamento passar a ser dominado por area vazia |
| conjunto incompleto -> leitura | Meio conjunto falha ABERTO: ele nao produz meia leitura, produz a leitura errada com a confianca da certa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-51 | Tampering | moldes do mercado (4x9) reusados para a fonte da barra (5x10) | critical | mitigate | Guarda de altura no cortador contra a dominante dos moldes ja gravados; `grep` afirma que `mercado_templates_de_digito` nao aparece no modulo novo; e o caso que confere aquela chave identica depois de uma rodada |
| T-01-52 | Tampering | conjunto "completado" com molde inventado ou copiado para fechar os onze rotulos | critical | mitigate | Nenhum rotulo grava sem confirmacao humana; o controle afirma que nenhum molde alem dos confirmados foi gravado; a fixtura carrega a cobertura REAL; e a medicao esta na docstring de `conjunto_descreve_numeros` — meio conjunto falha aberto, e nem a margem pega |
| T-01-53 | Repudiation | conjunto incompleto gravado em silencio, e o usuario descobrindo no meio do farm que a adena nao le | high | mitigate | `conjunto_descreve_numeros` roda depois de gravar, os faltantes saem **por nome** via `cobertura_dos_glifos`, e a mensagem diz a consequencia (a adena vai recusar) e o conserto NOVO: varrer outro campo da barra (M-L), e nao esperar o farm |
| T-01-54 | Tampering | fusao forcada de moldes de gravacao antiga (fundo escuro) para fechar o `5` e o `7` | high | mitigate | **O motivo desta ameaca foi removido, e nao apenas contido:** o M-L mediu o `5` no bonus da Faerlina e o `7` no EXP e na L-Coin da Yazalaque, entao o conjunto fecha com as DUAS fixturas de campo e nao ha ramo de gravacao antiga neste plano. A proibicao de fundir iluminacoes diferentes continua escrita (`calibrar_mercado.py:3125-3131`) e nao se reabre |
| T-01-55 | Tampering | a altura DOMINANTE do conjunto sendo fixada em 17 pela primeira rodada, por um recorte contaminado por icone, travando todo recorte legitimo dali em diante | critical | mitigate | A altura comparada e a que a peneira devolve **depois** do descarte posicional, e nao a faixa bruta (M-K); o teste da faixa recomputada (Tarefa 1) prende isso; e a guarda tem controle nos DOIS sentidos, entao um cortador que recusa tudo falha a suite |
| T-01-56 | Tampering | o run unico de 141 px do EXP da Yazalaque fatiado em vinte e oito digitos por `particionar_run` | critical | mitigate | `folga_de_cola` nasce `null` no `01-01`, e a peneira recusa o run anormalmente largo com a largura nomeada em vez de fatiar; o caso e medido (M-L) e vira teste com a sugestao do recorte `1366:1386` na mensagem |
| T-01-57 | Repudiation | o numero 17 sobrevivendo no fonte como altura da fonte da barra, depois de refutado | medium | mitigate | Criterio de aceitacao por `grep`: `17` so aparece no modulo do cortador em linha que tambem cita `M-K` ou `refutad`; e a docstring do modulo carrega a refutacao com a consequencia escrita (uma guarda contra 17 nunca corta nada) |
| T-01-58 | Elevation of Privilege | biblioteca de sintese de input entrar na arvore por causa da confirmacao interativa | high | mitigate | A confirmacao e teclado por `cv2.waitKey` no molde de `cortar_glifos`; nenhuma dependencia nova; `tests/test_firewall_escopo.py` continua varrendo |
| T-01-59 | Information Disclosure | os recortes ampliados mostrados na confirmacao carregam pixels da tela do usuario | low | accept | Sao numeros do proprio jogo dele, nada e gravado alem dos moldes, e sem a ampliacao a confirmacao humana vira ENTER as cegas |
| T-01-60 | Tampering | a suite escrevendo no `calibration.json` da maquina do usuario | high | mitigate | Monkeypatch de `ARQUIVO_CALIBRACAO` para `tmp_path` em todo caso, disciplina copiada verbatim dos arquivos precedentes |
</threat_model>

<verification>
- `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_glifos.py tests/test_calibrar_renda_moldes.py -q -rs` termina em 0 com **0 skipped**.
- `grep -c "def _glifos_do_numero" l2scanner/renda_leitura.py` retorna **1**, e o mesmo grep sobre `l2scanner/calibrar_renda_moldes.py` retorna **0**. Existe UMA peneira nesta fase, e ela mora no modulo puro.
- **O checkpoint da Tarefa 3 aconteceu de verdade e produziu o artefato.** `tests/fixtures/renda/moldes_da_barra.json` existe, passa por `mercado_visao.glifos_de_calibracao` sem levantar, `conjunto_descreve_numeros` sobre ela devolve `True` (os **onze** rotulos), e a altura dominante dos moldes e **10**. Este e o unico criterio deste plano que a suite **nao** consegue provar sozinha — ela e sintetica de proposito —, e e por isso que ele e um checkpoint `blocking-human` e nao uma tarefa `auto`.
- O SUMMARY registra **quantos e quais** rotulos o conjunto tem, **quais campos da barra foram varridos**, e a **altura de faixa medida em cada um**. Um SUMMARY que diga so "os moldes foram cortados" nao fecha este plano.
- **Se os onze nao fecharem**, o plano nao fecha: o SUMMARY diz quais rotulos faltaram e em que campo foram procurados, e o `01-04` fica com o desfecho (b) — a adena recusada por conjunto incompleto —, que e comportamento certo do produto e **nao** fecha o criterio 1 da fase.
- `git diff --stat l2scanner/mercado_leitura.py l2scanner/mercado_visao.py l2scanner/calibrar_mercado.py` sai vazio: as tres sao importadas, e nunca editadas.
- `git diff --stat l2scanner/calibracao.py l2scanner/calibrar_renda.py` sai vazio: a chave e do `01-01` e o calibrador de regioes e do `01-03`.
- `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_nenhum_numero_do_spike_no_fonte.py -q` continua em 0 depois desta onda — o portao de arvore de sintaxe do `01-03` nao foi quebrado por esta ferramenta.
- `python -m pytest tests/ -q` termina em 0 (os testes de OCR podem pular no Python global; nenhum outro pode).
- `git diff --stat requirements.txt` sai vazio.
- O `calibration.json` da maquina continua com as chaves que tinha antes da rodada. **Nao conferir por `git status`**: o arquivo e gitignored. Conferir por leitura do conteudo parseado, que e o que os testes fazem.
</verification>

<success_criteria>
- Existe **uma** peneira de forma nesta fase, `_glifos_do_numero`, no modulo **puro**, escrita por
  este plano e consumida pelo `01-04` sem ser reescrita. A seta continua ferramenta -> puro.
- A faixa de linhas e medida **depois** do descarte posicional, e o teste que prova isso cita o
  M-K — porque foi o icone dentro do recorte que produziu a altura 17 sobre uma fonte de 10.
- A guarda de altura compara contra **10** e tem controle nos **dois** sentidos: recusa a geometria
  errada **e** aceita a certa. Um cortador que nunca corta nada nao passa nesta suite, e esse era o
  desfecho que uma guarda contra 17 teria produzido.
- O numero **17 nao sobrevive** no fonte como altura da fonte da barra: onde ele aparece, aparece
  como refutacao com o M-K citado e a consequencia escrita.
- O cortador colhe de **qualquer campo da barra** — e o conjunto 0-9 fecha com as **duas fixturas
  que ja existem**, sem esperar farm. O bloqueio anterior esta citado e derrubado, e nao apagado.
- O run de **141 px** do EXP da Yazalaque e recusado com a causa nomeada e a sugestao medida, e
  nunca fatiado.
- A maquina propoe e o humano confirma, TUDO OU NADA; a cobertura sai **por nome** e diz **em que
  campo procurar** o que falta.
- **A rodada humana e um `checkpoint:human-action` com `gate="blocking-human"`, e nao uma tarefa
  `auto`.** A suite da Tarefa 2 e sintetica e fica verde sem a fixtura existir; o `01-04` inteiro
  pende dela. Nenhum modo automatico aprova este checkpoint, porque nao ha CLI que olhe um recorte
  de 5x10 px e diga se aquilo e um `6` ou um `8` — e um rotulo confirmado errado nao produz meia
  leitura, produz a leitura errada com a confianca da certa.
- O artefato do plano e `tests/fixtures/renda/moldes_da_barra.json` com os **onze** rotulos, e ele
  nasce **so** no checkpoint. Se ele aparecer ao fim da Tarefa 2, veio de codigo — e molde gerado
  por codigo e a falha aberta do LEIT-09 entrando pela suite.
- Um conjunto incompleto e gravado e **anunciado** — nunca inventado, nunca copiado do mercado,
  nunca completado para satisfazer `conjunto_descreve_numeros`.
- Uma rodada grava **uma** chave de topo e deixa todas as outras identicas, com controle positivo
  provando que a rodada escreveu algo.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-05-SUMMARY.md` when done
</output>
