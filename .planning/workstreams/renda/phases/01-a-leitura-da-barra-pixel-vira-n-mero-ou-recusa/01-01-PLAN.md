---
phase: 01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/calibracao.py
  - l2scanner/renda_leitura.py
  - l2scanner/renda_modo.py
  - tests/test_renda_tracer.py
  - tests/test_calibracao_renda.py
  - tests/fixtures/renda/
autonomous: true
requirements: [LEIT-02, LEIT-04, LEIT-05]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Um comando de leitura unica apontado para uma fixtura RESGATADA de recordings/ imprime o EXP com as QUATRO casas decimais, e o numero bate com o que a barra daquele frame mostra."
    - "O EXP interno e INTEIRO em decimos de milesimo de ponto percentual, nunca float: 57,9749% viaja como 579749 (CTX-5)."
    - "Um texto de EXP com tres casas, com cinco casas, ou sem o sinal de porcentagem produz RECUSA NOMEADA, e nunca um numero arredondado nem completado com zero (CTX-4, LEIT-02)."
    - "As duas escalas de OCR sao 2x e 3x, e um desacordo entre elas vira recusa com as duas leituras cruas no detalhe, entre >>><<< (CTX-3)."
    - "Um retangulo de calibracao que nao cabe dentro do frame produz RECUSA NOMEADA, e nunca um recorte silenciosamente encurtado — medido: numpy devolveu 24 linhas para um pedido de 26 sem uma linha de aviso."
    - "As seis chaves renda_* sobrevivem a ida e volta pelo calibration.json, e um campo do dataclass que alguem esquecer no salvar derruba um teste ESTRUTURAL em vez de sumir calado (CTX-7)."
    - "Um calibration.json sem nenhuma chave renda_* continua carregando inteiro, com os seis campos em None e VERSAO_DO_ESQUEMA intacta em 2 — ninguem e obrigado a recalibrar."
    - "renda_leitura.py nao importa argparse, nao chama funcao de janela do OpenCV e nao le relogio: um teste de fonte varre o arquivo e prende isso, inclusive em comentario."
  artifacts:
    - l2scanner/renda_leitura.py
    - l2scanner/renda_modo.py
    - tests/test_renda_tracer.py
    - tests/test_calibracao_renda.py
    - tests/fixtures/renda/calibracao_de_fixture.json
  key_links:
    - "renda_modo -> renda_leitura: a seta aponta ferramenta -> puro e NUNCA o contrario; renda_modo abre janela e le argumento, renda_leitura nao."
    - "renda_leitura -> ocr.ler_texto (2x) e ocr.ler_texto_ampliado (3x): o par de producao, e nao a 1x que o CONTEXT.md e o spike rotularam errado."
    - "renda_leitura -> mercado_leitura.mascara_de_numero: a mascara de brilho com o piso por parametro, sem default, vinda do calibration.json."
    - "Calibracao.salvar <-> dataclasses.fields(Calibracao): a amarra estrutural que nao existia no repositorio e que protege toda feature futura, nao so a renda."
---

<objective>
Provar a arquitetura inteira da fase numa fatia fina e de producao: do byte de um PNG
resgatado de `recordings/` ate o EXP com as quatro casas impresso no terminal, passando por
todas as camadas que a fase vai usar — `calibration.json` com chaves novas, carregamento
validado, recorte com guarda, mascara de brilho com piso calibrado, as duas escalas de OCR,
gramatica com trava, inteiro escalado, e a recusa nomeada no lugar de todo numero duvidoso.

E, junto, fechar em UMA onda o risco estrutural que a pesquisa achou e que nenhum documento
anterior mencionava: `Calibracao.salvar` e uma enumeracao literal de 44 chaves e nao preserva
chave desconhecida.

Purpose: se o desenho estiver errado, ele erra aqui, num commit, e nao depois de tres modulos
escritos. O EXP e a fatia certa porque e a especificacao mais apertada da milestone inteira —
seis digitos significativos num texto de 24 px. Se a arquitetura aguenta o EXP, a adena e o
nivel sao expansao.

A fatia NAO e prototipo: ela sai daqui com teste de ponta a ponta preso e e o esqueleto do
sistema final.

Output: `l2scanner/renda_leitura.py`, `l2scanner/renda_modo.py`, seis campos novos em
`l2scanner/calibracao.py`, as fixturas resgatadas em `tests/fixtures/renda/`, e os dois
arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/STATE.md
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-CONTEXT.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-RESEARCH.md
@.planning/spikes/renda-barra-inferior.md
</context>

## Decisoes que estes quatro planos herdam e nao reabrem

O `01-CONTEXT.md` desta fase **nao numera as decisoes com IDs `D-NN`** — a numeracao `D-NN`
neste repositorio pertence ao `PROJECT.md` (`D-02`, "um numero exibido tem de ter existido") e
colidir com ela seria criar duas leituras para o mesmo simbolo. As decisoes travadas da fase
sao citadas nos quatro planos como **CTX-1 a CTX-9**, no precedente que o workstream
`dashboard` ja abriu:

- **CTX-1** — **Leitor por campo.** O `CONTEXT.md` travou "nivel por moldes de glifo"; a
  pesquisa (c3) e a medicao deste planejamento **derrubaram** os moldes para os tres campos.
  Vale **OCR sobre mascara de brilho calibrada**, nos tres. A refutacao vai escrita no fonte,
  com as duas medicoes ao lado (secao "Numeros medidos" abaixo).
- **CTX-2** — **`numero_valido` e recusa nomeada valem para os tres campos.** Nenhum caminho
  devolve numero sem passar por validacao. Inegociavel, e a unica clausula do `CONTEXT.md` que
  nenhuma medicao pode reabrir.
- **CTX-3** — **Cruzamento de duas escalas para o EXP.** O par do `CONTEXT.md` era 1x x 2x;
  **nao existe entrada publica em 1x** (`ocr.py:80-81`) e a 1x abstem por medicao documentada.
  O par de producao e **2x (`ler_texto`) x 3x (`ler_texto_ampliado`)`**.
- **CTX-4** — **Quatro casas ou recusa.** `\d{1,3}[.,]\d{4}%`. Tres casas, cinco casas ou
  ausencia do `%` sao recusa, nunca arredondamento e nunca completar com zero.
- **CTX-5** — **O EXP interno e inteiro**, em decimos de milesimo de ponto percentual, no
  precedente de `total_em_centesimos`. Nunca `float`.
- **CTX-6** — **Chaves achatadas com prefixo `renda_`** no `calibration.json` compartilhado, e
  **nenhum numero do spike no fonte** fora de teste.
- **CTX-7** — **O calibrador carrega o arquivo inteiro, muda so `renda_*` e reemite tudo — e
  PROVA por teste.** O defeito citado pelo `CONTEXT.md` (`calibrar_mercado.py:2721`) **ja foi
  consertado** (`f8dbfe2`); o risco real e `Calibracao.salvar` ser enumeracao literal.
- **CTX-8** — **`--janela` obrigatorio**, titulo resolvido no molde do `--mercado`, e a leitura
  carrega o nome do personagem mesmo sem existir arquivo ainda.
- **CTX-9** — **A Fase 1 e sem estado**: `frame -> LeituraDaRenda | RecusaDaRenda`. A
  monotonicidade entra como funcao **pura sobre um par passado por parametro**, nunca como
  memoria viva.

## Numeros medidos no planejamento (2026-09-02, nesta arvore)

Medidos agora, com `.venv/Scripts/python.exe`, contra frames de `recordings/`. Eles **mudam o
desenho** em relacao ao que o `ROADMAP.md` e o `CONTEXT.md` supunham, e por isso estao aqui em
cima e nao numa nota de rodape.

| # | medicao | resultado |
|---|---|---|
| M1 | `recordings/` guarda a **janela inteira** | `(1392, 1720, 3)` em tres gravacoes conferidas. A barra inferior e o retrato **estao dentro**. As fixturas desta fase saem daqui, sem o jogo aberto. |
| M2 | EXP no recorte **cru**, 2x, `20260901-164159-aba-para-calibrar/frame_000000` | `EXP- 57.9749% 615% j 118` — **quatro casas, legiveis** |
| M3 | EXP no cru, 2x, `20260901-204804-ciano-completo/frame_000000` | `EXP 62.5562%` |
| M4 | EXP no cru, 2x, `20260901-172911-adena-diagnostico/frame_000005` | `845%' 126` — **o EXP simplesmente some** |
| M5 | EXP com mascara `vmin=180`, mesmo frame do M4 | `EXP 58.8189% t 845%` — **a mascara recupera o campo que o cru perdeu** |
| M6 | EXP cru **vs** mascara `vmin=180`, `20260828-063240-mercado-farm-com-party/frame_000000` | cru `57.6499%`, mascara `57.8499%` — **um digito diverge**, na segunda casa. E exatamente o modo de falha que LEIT-02 existe para impedir, visto no pixel. |
| M7 | Adena no cru, 2x, `aba-para-calibrar/frame_000000`, faixa direita | `Special 8,786` — **a adena nao aparece** |
| M8 | Adena com mascara `vmin=150`, mesma faixa | `Special 133,52 8,786 2,207,577 B` — **a adena aparece**, e vem com um `B` de lixo colado no fim |
| M9 | Adena com mascara `vmin=180`, mesma faixa | `Special 133,52` — **some de novo**. A banda util de `vmin` da barra e **estreita**, e nao e a mesma do nivel. |
| M10 | `segmentar_glifos_no_brilho` sobre a barra esquerda, `vmin` 150 e 180 | faixa `(0, 22)`, **UM run de 519 px**. A segmentacao de glifo **nao separa nada** nesta regiao. |
| M11 | Nivel em `246,736 30x20`, cru e com `vmin` 150/170/180/190/200/210/230 | `''` em todos, em todas as gravacoes conferidas — a janela de status do personagem esta **fechada** nestas gravacoes. |
| M12 | `frame[1368 : 1368+26]` numa janela de altura 1392 | numpy devolve **24 linhas, em silencio**. `1368+26 = 1394 > 1392`. |

**O que estes numeros decidem, e o plano executa:**

- **M10 mata os moldes de glifo para a barra**, do mesmo jeito que a pesquisa (c3) ja os tinha
  matado para o nivel. Um run de 519 px nao e "caro de calibrar": e a ferramenta dizendo que
  nao ha o que segmentar. CTX-1 fica com OCR mascarado nos tres campos, e a refutacao vai
  escrita no fonte com M10 ao lado.
- **M7/M8/M9 refutam o `ROADMAP.md`**, que diz *"a leitura da adena ja quase funciona"* sobre o
  recorte cru. Nao funciona: no cru a adena **nao esta la**. Ela aparece so sob mascara, e some
  de novo 30 pontos de `vmin` acima. Consequencia: existe **um `vmin` por regiao**, e a
  calibracao ganha `renda_vmin_da_barra` **alem** de `renda_vmin_do_nivel`.
- **M6 e o argumento medido do CTX-3.** Dois caminhos de leitura discordaram num digito da
  segunda casa decimal, num frame real desta arvore. Sem cruzamento, um dos dois teria virado
  numero gravado.
- **M12 e a armadilha mais barata de evitar e a mais cara de descobrir depois.** Um retangulo
  de calibracao que ultrapassa a borda da janela nao levanta: ele **encurta**. Um EXP lido de
  24 linhas em vez de 26 e uma leitura plausivel e errada. O recorte desta fase **confere e
  recusa**, nao fatia.
- **M11 e o unico buraco honesto da fase**: o nivel nao esta em nenhuma gravacao existente.
  Ele e tratado no plano `01-04`, com o que da para provar sem o jogo dito em voz alta.

Os retangulos de partida, em coordenadas **da janela** (a pesquisa provou que o frame de
`JanelaSource` e janela-relativo, `captura_janela.py:396-402`):

| regiao | esquerda | topo | largura | altura |
|---|---|---|---|---|
| barra esquerda | 0 | 1368 | 520 | 24 |
| barra direita | 1200 | 1368 | 520 | 24 |
| nivel | 246 | 736 | 30 | 20 |

A altura 24 e **corrigida** em relacao ao spike pelo M12: 26 nao cabe. A largura da direita vai
ate a borda da janela, e nao ate `1700`, porque o spike encostou na borda do proprio `grab` e
nunca viu os ultimos 20 px (pesquisa, Armadilha 7). **Estes numeros sao ponto de partida do
calibrador e nao entram no fonte** (CTX-6) — eles vivem aqui, na fixtura de calibracao dos
testes, e no `calibration.json` do usuario.

<tasks>

<task type="tracer">
  <name>Tarefa 1: A fatia — de um PNG resgatado ao EXP com quatro casas, atravessando todas as camadas</name>
  <files>l2scanner/calibracao.py, l2scanner/renda_leitura.py, l2scanner/renda_modo.py, tests/fixtures/renda/, tests/test_renda_tracer.py</files>
  <read_first>
    - `l2scanner/calibracao.py:234-266` — o bloco de comentario que justifica campo opcional com `VERSAO_DO_ESQUEMA` intacta em 2, e `:249-255` (`banner_manutencao`, `tiat_chat`, `tiat_alvo`), o molde exato de tres campos opcionais de feature.
    - `l2scanner/calibracao.py:668-768` — o corpo inteiro de `salvar`: a lista literal de chaves, o idioma condicional `X.como_dict() if X else None`, e a escrita atomica por `.tmp` + `os.replace` em `:761-768`.
    - `l2scanner/calibracao.py:771-900` — `carregar`: portao de versao, `_conferir_as_chaves_de_mercado`, e o `.get` para todo campo opcional com a razao escrita em `:851-856`.
    - `l2scanner/calibracao.py:1505-1548` — `_conferir_o_piso_de_brilho_da_quantidade`, o molde exato do validador de `vmin`: inteiro em faixa fechada, `None` passa, `bool` recusado EXPLICITAMENTE por ser subclasse de `int`, mensagem carregando o conserto.
    - `l2scanner/ocr.py:26-70` — a tabela de custo e o argumento contra a 1x; `:80-81` as duas constantes de escala; `:172-196` as duas entradas publicas; `:197-212` a promessa de nunca levantar e a guarda de recorte vazio.
    - `l2scanner/mercado_leitura.py:1-44` — o charter do modulo puro e a regra "nenhum limiar tem default"; `:155` `mascara_de_numero`; `:559-586` `centesimos_de_moeda`, cuja FORMA se copia e cuja funcao nao se reusa; `:1514-1524` as constantes `MOTIVO_*`; `:1562-1577` `Descarte` (dois campos, nao tres); `:2379-2390` `_recusar` e os delimitadores `>>><<<`.
    - `l2scanner/mercado_modo.py:1-40` e `:400-470` — o molde de um modulo de modo: ele abre janela e le argumento, o modulo puro nao.
    - `l2scanner/frames.py:71-105` — `Regiao`, `de_dict`, `como_dict`.
    - `tests/test_ocr.py:25-56` — o idioma de skip por ausencia de OCR: `TEM_OCR` calculado no nivel do modulo, `_resetar_cache`, e a razao do skip carregando o COMANDO de conserto.
    - `tests/test_mercado_glifos.py:1-40` — por que as fixturas sao RESGATADAS de `recordings/` e nunca lidas de la.
    - A secao "Numeros medidos no planejamento" acima — os retangulos de partida e as gravacoes de origem.
  </read_first>
  <behavior>
    - Um `calibration.json` de fixtura com as seis chaves da renda carrega, e `cal.renda_barra_esquerda` volta como `Regiao`.
    - Um `calibration.json` SEM nenhuma chave da renda carrega igual, com os seis campos em `None`, e a versao continua em 2.
    - O recorte de uma regiao que cabe no frame devolve exatamente a altura e a largura pedidas.
    - O recorte de uma regiao que ultrapassa a borda do frame devolve RECUSA nomeada — nunca um array menor.
    - O EXP da fixtura `aba_para_calibrar` sai como `579749`.
    - O comando de leitura unica, apontado para a montagem da janela e para a calibracao de fixtura, imprime o EXP com quatro casas e sai com codigo 0.
  </behavior>
  <action>
Resgatar as fixturas primeiro, porque sem elas nada aqui e verificavel. Escrever um trecho de
resgate (pode viver como funcao de modulo no proprio arquivo de teste, ou como script de uso
unico que nao fica versionado) que abre as gravacoes citadas na secao "Numeros medidos" acima,
recorta as tres regioes com os retangulos daquela mesma secao, e grava em
`tests/fixtures/renda/` no idioma de nome do repositorio — a gravacao de origem, o numero do
frame, e a regiao, separados como `tests/fixtures/mercado/` ja separa. Resgatar, no minimo: a
barra esquerda dos tres frames citados nos itens M2, M4 e M6, e a barra direita do frame do
item M7. Nenhum teste desta fase le `recordings/` diretamente: a pasta e gitignored e um teste
apoiado nela fica verde nesta maquina e amarelo em qualquer clone.

Gravar tambem `tests/fixtures/renda/montagem_da_janela.png`: uma tela preta com a altura e a
largura da janela carimbada, com os recortes reais colados nas posicoes de calibracao. Ela nao
e uma captura: e uma montagem, e a docstring do teste diz isso com todas as letras, junto do
que ela prova (geometria, posicao, caminho de ponta a ponta) e do que ela nao prova (a regiao
do nivel fica preta de proposito, porque nenhuma gravacao existente a contem — e essa regiao
preta vira o caso de campo vazio do plano `01-04`). Gravar ao lado
`tests/fixtures/renda/calibracao_de_fixture.json`, no molde de
`tests/fixtures/mercado/calibracao_de_fixture.json`, com as seis chaves da renda preenchidas.

Acrescentar a `Calibracao` **seis** campos opcionais, todos com default `None`, num bloco com
comentario proprio que diga tres coisas: que sao opcionais de proposito e por isso a versao do
esquema **nao sobe** (com a razao ja escrita para o `banner_manutencao` citada por referencia);
que o referencial e o canto da **janela** e nunca o desktop, como `hp_proprio` e `tiat_*`; e que
existem **dois** pisos de brilho e nao um, com o item M9 da secao de medicoes ao lado como razao
— a banda util da barra e estreita e nao coincide com a do nivel. Os campos: as tres regioes, os
dois pisos de brilho, e o carimbo de geometria da janela no molde de
`mercado_geometria_da_captura`.

Fazer as tres edicoes que fecham o ciclo: serializar os seis no `salvar` com o mesmo idioma
condicional das irmas, ler os seis no `carregar` com `.get`, e escrever
`_conferir_as_chaves_da_renda(dados)` no molde de `_conferir_o_piso_de_brilho_da_quantidade`,
chamada de dentro do `carregar` ao lado da conferencia do mercado. Para cada piso de brilho: 
inteiro, faixa fechada dos dois lados, `None` passa porque a feature fica desligada, `bool`
recusado explicitamente, e a mensagem dizendo o conserto. Escrever a razao de **cada extremo**
da faixa: no piso de baixo a mascara fica cheia e todo pixel vira tinta, entao o texto se dissolve
no fundo e o OCR le ruido com confianca; no piso de cima a mascara fica vazia e o campo some sem
uma linha de log. Para as tres regioes: dimensao positiva, no precedente de `_conferir_uma_coluna`.

Escrever `l2scanner/renda_leitura.py` com o charter copiado do de `mercado_leitura.py`: modulo
**puro**, sem `argparse`, sem chamada de janela do OpenCV, sem relogio. Escrever a razao
operacional e nao estetica — um modulo de producao que importasse a ferramenta pagaria
`tornar_consciente_de_dpi()` e as chamadas de janela do OpenCV so por existir, e uma janela de
conferencia acabaria abrindo dentro do tick de captura.

Nele, nesta tarefa, o minimo que atravessa todas as camadas:

`RecusaDaRenda` como dataclass congelado de tres campos — o campo, o motivo e o detalhe. Ele
carrega o `detalhe` que o `Descarte` do mercado joga fora, e a razao vai escrita: o `Descarte`
tem `indice` de linha de grade, que nao significa nada aqui, e manda o detalhe so para o log;
para um leitor de tres campos que a Fase 2 vai consumir, o motivo precisa viajar **no dado**.
As constantes de motivo no padrao `MOTIVO_*` do mercado, como strings simples.

`_recusar(campo, motivo, detalhe)` no molde de `mercado_leitura._recusar`: escreve
`log.warning` e devolve a recusa. Os delimitadores em torno do texto lido sao obrigatorios,
porque espaco em branco e o que distingue duas leituras parecidas, e nao ha limite de taxa,
porque o log rotativo e a unica forense pos-farm deste projeto.

`recortar(frame, regiao)`: confere que o retangulo cabe INTEIRO dentro do frame antes de fatiar,
e devolve recusa nomeada quando nao cabe. Escrever o item M12 da secao de medicoes ao lado,
com o numero: numpy devolveu duas linhas a menos do que o pedido, em silencio, e um EXP lido de
uma faixa encurtada e uma leitura plausivel e errada.

`decimos_de_milesimo(texto)`: a gramatica nova, irma e nao parametrizacao. A docstring diz por
que nao reusa `centesimos_de_moeda` — ela trava em duas casas decimais e exige virgula, e
devolve nada para as duas grafias do EXP, medido por execucao na pesquisa — e diz por que o
separador entra como classe de caractere e nao como caractere: o jogo escreve decimal com ponto
num campo e milhar com ponto no outro, e o OCR devolveu virgula para os dois. **Quem decide e a
CONTAGEM de digitos depois do separador, nunca o caractere.** E diz, com todas as letras, que
tres casas, cinco casas ou ausencia do sinal de porcentagem sao recusa e nunca arredondamento
(CTX-4): um EXP de duas casas tratado como se tivesse quatro e uma mentira que ninguem consegue
ver depois de gravada.

`exp_da_barra(recorte, *, piso_de_brilho)`: aplica `mercado_leitura.mascara_de_numero` com o
piso vindo da calibracao, le na escala barata, e so paga a cara se a barata passar no teste da
gramatica — a ordem que `manutencao.py` estabeleceu, e que aqui tambem da um motivo de recusa
mais preciso. Se as duas leem e discordam, recusa por discordancia entre escalas, com as duas
leituras cruas e os dois inteiros no detalhe. O piso e parametro **somente-nomeado e sem
default**, pela regra escrita em `mercado_leitura.py:34-44`: um default e um numero magico que
entra por omissao.

Escrever `l2scanner/renda_modo.py`: o comando de leitura unica. Ele le argumento, resolve a
fonte de pixel e imprime. Duas fontes: um caminho de imagem, que e o que torna esta fatia
verificavel sem o jogo aberto, e a janela do jogo pelo titulo, no molde de `calibrar_tiat` —
`JanelaSource` com regiao unitaria e `relativa` ligado, uma pausa curta para a captura entregar
um frame, `capturar_completo`, e `fechar` no `finally`. O alvo cai para `cal.janela` quando o
titulo nao vem. Aceitar tambem um caminho de calibracao alternativo, que e o que mantem o
`calibration.json` do usuario fora do alcance da suite. Nesta tarefa ele imprime **so o EXP**;
a adena e o nivel entram no plano `01-04`.

Escrever `tests/test_renda_tracer.py` no idioma da casa: docstring de modulo longa nomeando o
que o arquivo existe para impedir, classes por invariante, nomes de teste em portugues com o
fato em CAIXA ALTA, e o idioma de skip por ausencia de OCR copiado de `tests/test_ocr.py`
— com a razao do skip carregando o comando que faz o teste rodar. As classes: a ida e volta da
calibracao com e sem as chaves da renda, a guarda de recorte, a gramatica com os casos do bloco
`behavior`, e o caso de ponta a ponta que roda o comando de leitura unica contra a montagem e
confere o numero impresso.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py -q -rs` termina em 0 e relata **0 skipped**.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m l2scanner.renda_modo --imagem tests/fixtures/renda/montagem_da_janela.png --calibracao tests/fixtures/renda/calibracao_de_fixture.json` sai com codigo 0 e imprime uma linha de EXP com quatro digitos depois do separador.
    - Existe um teste que afirma que a recusa de um retangulo maior que o frame e uma `RecusaDaRenda`, e um CONTROLE NEGATIVO no mesmo arquivo que passa um retangulo que cabe e afirma que a forma do recorte bate exatamente com a altura e a largura pedidas. Sem o controle, uma funcao que recusasse tudo passaria.
    - Existe um teste que afirma que um `calibration.json` sem nenhuma chave da renda carrega com os seis campos em `None` e com a versao do esquema inalterada.
    - `python -c "import ast,sys; src=open('l2scanner/renda_leitura.py',encoding='utf-8').read(); ast.parse(src)"` roda, e `grep -v '^\s*#' l2scanner/renda_leitura.py | grep -c -E "^\s*import argparse|cv2\.imshow|cv2\.selectROI|cv2\.createTrackbar|datetime\.now"` retorna 0.
    - `ls tests/fixtures/renda/*.png | wc -l` retorna ao menos 5, e `ls recordings/ >/dev/null` nao e chamado por nenhum teste: `grep -rc "recordings/" tests/test_renda_tracer.py` retorna 0 fora de linha de comentario.
  </acceptance_criteria>
  <done>Um PNG resgatado entra, o EXP com quatro casas sai impresso, e cada camada do caminho — calibracao validada, recorte com guarda, mascara com piso calibrado, duas escalas, gramatica com trava, inteiro escalado, recusa nomeada — esta exercitada por um teste que roda sem o jogo aberto.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A amarra estrutural que faltava ao repositorio inteiro — todo campo do dataclass sobrevive ao salvar</name>
  <files>l2scanner/calibracao.py, tests/test_calibracao_renda.py</files>
  <read_first>
    - `l2scanner/calibracao.py:668-758` — a lista literal de chaves de `salvar`, lida ate o fim. E ela que este teste amarra.
    - `l2scanner/calibrar.py:1270-1345` — `CAMPOS_DA_PARTY` e `fundir_com_a_calibracao_em_disco`, com o comentario que estabelece a regra que a renda herda: com uma lista de DONOS o campo novo nasce PRESERVADO por omissao, e o default de um campo desconhecido tem de ser SOBREVIVER.
    - `l2scanner/calibrar.py:1300-1345` — o relato do incidente de 2026-08-30, em que uma rodada montou uma `Calibracao` do zero e o `salvar` gravou o objeto inteiro por cima de treze moldes de glifo, tres ancoras e a grade. O resgate manual ainda esta na raiz do repositorio.
    - `tests/test_calibracao_mercado.py:454` e `:479` — `test_sem_a_chave_o_campo_sai_None` e `test_ida_e_volta_devolve_os_mesmos_valores`, os dois testes que EXISTEM e que, por serem parametrizados por lista escrita a mao, deixariam passar exatamente o esquecimento que este teste pega.
    - `01-RESEARCH.md` secao 5c — o enunciado do risco, e a razao de ele ser o oposto do que o `CONTEXT.md` descreve.
  </read_first>
  <behavior>
    - Uma `Calibracao` com TODO campo preenchido, salva e relida, devolve os mesmos valores em todos os campos.
    - Todo nome em `dataclasses.fields(Calibracao)` aparece como chave no JSON emitido por `salvar`.
    - Toda chave emitida por `salvar` corresponde a um campo do dataclass — nenhuma chave orfa.
    - Remover mentalmente uma linha do `salvar` derruba o teste: o CONTROLE POSITIVO prova isso serializando uma copia do dict sem uma das chaves e afirmando que a conferencia acusa.
    - Um piso de brilho fora da faixa fechada faz `carregar` recusar com mensagem que diz o conserto; um piso booleano tambem recusa, com mensagem propria.
  </behavior>
  <action>
Escrever `tests/test_calibracao_renda.py` com uma docstring de modulo que nomeia o dano que ele
existe para impedir, no tempo presente porque o dano ainda e possivel hoje: `Calibracao.salvar`
monta o dicionario a partir de uma lista literal, entao um campo declarado no dataclass e
esquecido na lista **e gravado como se nada fosse e some na proxima leitura**, sem uma linha de
erro. A docstring diz tambem por que os dois testes que ja existem em
`tests/test_calibracao_mercado.py` nao pegam isso: eles sao parametrizados por uma lista de
campos escrita a mao, e a mao que esquece o campo no `salvar` e a mesma que esquece na lista do
teste. Este teste tira a lista da mao e a deriva de `dataclasses.fields`.

Escrever o teste estrutural: construir uma `Calibracao` com **todo** campo preenchido, derivando
os campos de `dataclasses.fields(Calibracao)` e escolhendo o valor por tipo — nunca enumerando
nomes a mao, porque enumerar a mao e o defeito. Salvar num diretorio temporario, reler o JSON, e
afirmar que a diferenca entre os nomes do dataclass e as chaves do arquivo e vazia **nos dois
sentidos**: campo sem chave e o esquecimento no `salvar`; chave sem campo e uma chave orfa que
`carregar` nunca vai ler. A mensagem de falha lista os nomes que faltam, e nao so o tamanho da
diferenca, porque quem le a falha as 2 da manha precisa do nome.

Escrever o CONTROLE POSITIVO ao lado, no mesmo arquivo: sem ele o teste acima e vacuo, porque um
`salvar` que emitisse chave nenhuma e um `fields` que devolvesse nada passariam de maos dadas.
O controle monta o dicionario emitido, remove UMA chave conhecida, e afirma que a mesma
conferencia acusa aquela chave pelo nome.

Escrever a ida e volta completa: preencher todo campo, salvar, `carregar`, e comparar campo a
campo por `dataclasses.fields` — de novo sem lista a mao. Onde o tipo do campo nao suportar
comparacao direta, comparar a forma serializada.

Escrever os testes dos validadores novos: piso de brilho abaixo do minimo, acima do maximo,
booleano, e do tipo errado — cada um afirmando que `carregar` recusa com o tipo de excecao que o
modulo ja usa e que a mensagem contem a palavra que diz ao usuario o que fazer. E o caso que
importa tanto quanto: piso ausente **passa**, porque feature nao calibrada e feature desligada e
nao arquivo invalido.

Ajustar `l2scanner/calibracao.py` no que estes testes pegarem. Se o teste estrutural acusar
campos de OUTRAS features ja faltando no `salvar`, corrigir tambem — e registrar o achado no
SUMMARY, porque um campo de outra feature sumindo em silencio e um defeito ativo e nao um
detalhe desta fase.
  </action>
  <verify>
    <automated>python -m pytest tests/test_calibracao_renda.py tests/test_calibracao_mercado.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_calibracao_renda.py -q` termina em 0 com pelo menos 8 testes coletados.
    - O teste estrutural deriva a lista de campos de `dataclasses.fields(Calibracao)` e nao de um literal: `grep -c "fields(Calibracao)" tests/test_calibracao_renda.py` retorna ao menos 2.
    - Existe um CONTROLE POSITIVO que remove uma chave do dicionario emitido e afirma que a conferencia acusa aquela chave PELO NOME.
    - A conferencia roda nos dois sentidos: existe uma asserção para campo-sem-chave e outra para chave-sem-campo.
    - `python -m pytest tests/test_calibracao_mercado.py tests/test_calibracao_generica.py -q` continua em 0 — a extensao do dataclass nao quebrou nenhuma feature vizinha.
    - Existe um teste que afirma que um piso de brilho booleano e recusado, e a mensagem de falha do teste explica que `bool` e subclasse de `int` e por isso precisa de recusa propria.
  </acceptance_criteria>
  <done>O repositorio ganha a amarra que nunca teve: um campo declarado e esquecido no `salvar` derruba um teste em vez de sumir calado, e a prova tem controle positivo. Isso protege toda feature futura, e nao so a renda.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: A gramatica do EXP endurecida — e o cruzamento que M6 mediu ser necessario</name>
  <files>l2scanner/renda_leitura.py, tests/test_renda_tracer.py</files>
  <read_first>
    - `l2scanner/mercado_leitura.py:559-586` — o corpo de `centesimos_de_moeda` e as duas linhas que a travam: a exigencia de virgula e o comprimento fixo do decimal. E a FORMA que se copia.
    - `l2scanner/mercado_leitura.py:569-573` — a docstring que declara a regra geral: a gramatica e trava de validacao e nao so parsing, e um digito perdido tem de derrubar a linha em vez de virar um numero plausivel.
    - `l2scanner/manutencao.py:800-822` — o cruzamento de escalas e a docstring que corrige a razao antiga em voz alta: a ordem preserva DIVERSIDADE DE METODO, e nao orcamento.
    - `l2scanner/mercado_leitura.py:2295-2362` — `_ler_o_nome` e o texto de log da discordancia, com os delimitadores em torno de cada leitura crua.
    - A secao "Numeros medidos no planejamento" acima, itens M2, M4, M5 e M6 — as quatro leituras reais que esta tarefa transforma em teste, incluindo a que diverge num digito.
    - `01-RESEARCH.md` "Perguntas em aberto" item 4 — a evidencia de que o campo de bonus da mesma barra sai com sinal de porcentagem tambem, e por isso a ancora nao pode ser "o primeiro numero com porcentagem".
  </read_first>
  <behavior>
    - Um texto com quatro digitos depois do separador e o sinal de porcentagem devolve o inteiro escalado.
    - O separador pode ser ponto ou virgula, e o resultado e o mesmo inteiro — a decisao vem da contagem de digitos, nunca do caractere.
    - Tres digitos depois do separador devolvem recusa. Cinco digitos devolvem recusa. Sem o sinal de porcentagem, recusa.
    - Um texto que contem o campo de bonus da mesma barra ANTES do EXP nao faz a gramatica casar no bonus: o resultado e o EXP, e nao o bonus.
    - Um texto vazio e um texto nulo devolvem recusa de campo vazio, distinguivel da recusa de gramatica.
    - Duas escalas que leem o mesmo inteiro devolvem o inteiro. Duas escalas que leem inteiros diferentes devolvem recusa por discordancia, com as duas leituras cruas no detalhe.
    - Uma escala que le e outra que abstem devolve recusa, e nao o numero da que leu.
    - As quatro leituras reais medidas no planejamento (M2, M4, M5, M6) estao no arquivo de teste como casos nomeados, com a gravacao de origem citada.
  </behavior>
  <action>
Endurecer `decimos_de_milesimo` para os casos do bloco `behavior`. A ancora da expressao e
**quatro digitos imediatamente antes do sinal de porcentagem**, e a razao vai escrita ao lado:
a mesma barra carrega um segundo campo com sinal de porcentagem, e uma expressao que pegasse "o
primeiro numero com porcentagem" leria o bonus como se fosse o EXP. A pesquisa mediu que o OCR
inclusive **erra** aquele segundo campo; para nos isso e irrelevante desde que a ancora seja a
contagem de casas, e vira desastre se nao for.

Separar a recusa de **campo vazio** da recusa de **gramatica**, com constantes de motivo
distintas. Elas parecem a mesma coisa e nao sao: campo vazio quer dizer que o OCR abstem, e
aponta para calibracao ou para o jogo estar em outra tela; gramatica quer dizer que o OCR leu
alguma coisa e a leitura nao respeita a forma, e aponta para piso de brilho errado. Sao dois
consertos diferentes, e o criterio 3 do roadmap pede que sejam distinguiveis.

Fechar `exp_da_barra` com o cruzamento das duas escalas, na ordem barata-primeiro, e escrever a
medicao que o justifica **no fonte**, com os numeros: em um frame real desta arvore, dois
caminhos de leitura do mesmo pixel discordaram num digito da segunda casa decimal; sem
cruzamento, um dos dois teria virado numero gravado, e depois de gravado um numero errado e
indistinguivel de um certo. Escrever tambem, ao lado, a refutacao do par que o `CONTEXT.md`
travou: a escala mais baixa nao tem entrada publica neste projeto e abstem por medicao
documentada no proprio modulo de OCR, entao o par de producao e o de deteccao com o de
conferencia (CTX-3).

Escrever no arquivo de teste uma classe por invariante e, dentro da classe das leituras reais,
um caso por medicao do planejamento — cada um com a gravacao e o frame de origem no nome ou na
docstring, para que a proveniencia sobreviva ao proximo mantenedor. O caso da divergencia (M6) e
o mais importante do arquivo: ele afirma que os dois caminhos de leitura daquele frame produzem
inteiros diferentes, e portanto que o cruzamento **tem** trabalho a fazer nesta arvore. Se um dia
ele ficar verde por os dois passarem a concordar, o teste tem de falhar por motivo escrito — deixe
a mensagem de falha dizer "as duas leituras convergiram; reconfira se o caminho de leitura mudou"
em vez de simplesmente passar.

Incluir o CONTROLE de que a gramatica nao completa com zero: um texto com duas casas decimais e
sinal de porcentagem tem de recusar, e o teste afirma explicitamente que o retorno **nao** e o
valor que sairia se as casas faltantes fossem preenchidas com zero. Sem esse controle, uma
implementacao que arredondasse passaria em todos os outros casos.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py -q -rs -k "gramatica or cruzamento or exp or escala"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py -q -rs` termina em 0 e relata 0 skipped.
    - Existe um teste por caso do bloco `behavior`, e os quatro casos de leitura real citam a gravacao de origem na docstring ou no nome.
    - Existe o CONTROLE que afirma que duas casas decimais recusam E que o retorno nao e o valor completado com zero.
    - Existe um teste que passa um texto contendo o campo de bonus da mesma barra e afirma que o resultado e o EXP.
    - Existe um teste que afirma que campo vazio e gramatica invalida produzem motivos DIFERENTES, comparando as duas constantes.
    - `grep -v '^\s*#' l2scanner/renda_leitura.py | grep -c "centesimos_de_moeda"` retorna 0 — a funcao do mercado nao e chamada, so a forma dela foi copiada.
  </acceptance_criteria>
  <done>A especificacao mais apertada da milestone esta presa por teste: quatro casas ou recusa, ancora na contagem e nao no caractere, campo vazio distinguivel de gramatica invalida, e o cruzamento de escalas com a divergencia real desta arvore como caso de teste nomeado.</done>
</task>

</tasks>

## Artifacts que este plano produz

- `l2scanner/renda_leitura.py` — modulo puro com `RecusaDaRenda`, as constantes de motivo,
  `_recusar`, `recortar`, `decimos_de_milesimo` e `exp_da_barra`
- `l2scanner/renda_modo.py` — o comando de leitura unica, por imagem ou por janela
- `l2scanner/calibracao.py` — seis campos novos, seis linhas no `salvar`, seis no `carregar`,
  um validador novo
- `tests/fixtures/renda/` — as fixturas resgatadas, a montagem da janela e a calibracao de
  fixtura
- `tests/test_renda_tracer.py`, `tests/test_calibracao_renda.py`

Nada de novo em `requirements.txt`. Nenhum arquivo do workstream `dashboard`, do `mercado` ou
do caminho da party e tocado.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> processo | Arquivo compartilhado por quatro features, editavel a mao, e entrada NAO confiavel para o carregamento |
| pixel da tela -> numero interno | Um numero errado aqui vira taxa errada em toda a milestone, e depois de gravado nao ha como distingui-lo de um certo |
| disco -> `calibration.json` | A gravacao do calibrador pode ser interrompida; um JSON truncado derruba o scanner de party inteiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `calibration.json` com piso de brilho fora de faixa | medium | mitigate | `_conferir_as_chaves_da_renda` no arranque, faixa fechada dos dois lados, `bool` recusado explicitamente, mensagem carregando o conserto |
| T-01-02 | Tampering | numero lido que nunca esteve na tela | high | mitigate | Gramatica como trava (quatro casas ou recusa), cruzamento de duas escalas, e o caso de teste da divergencia real medida no planejamento |
| T-01-03 | Tampering | recorte silenciosamente encurtado por retangulo maior que o frame | high | mitigate | `recortar` confere antes de fatiar e recusa com motivo; teste com controle negativo |
| T-01-04 | Denial of Service | chave `renda_*` apagada por outro calibrador | high | mitigate | Os seis campos declarados no dataclass E no `salvar`, presos pelo teste estrutural derivado de `fields(Calibracao)` |
| T-01-05 | Repudiation | recusa em silencio, sem forense pos-farm | medium | mitigate | `_recusar` escreve `log.warning` com o texto lido entre delimitadores e sem limite de taxa |
| T-01-06 | Elevation of Privilege | biblioteca de sintese de input entrar na arvore | high | mitigate | Nenhuma dependencia nova; `tests/test_firewall_escopo.py` continua varrendo |
| T-01-07 | Information Disclosure | reflexo do que se le da tela indo para o log | low | accept | Os campos sao numeros do proprio jogo do usuario, o log e local e rotativo, e sem ele nao ha como responder "por que nao gravou" |
</threat_model>

<verification>
- `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py tests/test_calibracao_renda.py -q -rs` termina em 0 com 0 skipped.
- `python -m pytest tests/ -q` termina em 0 (os testes de OCR podem pular no Python global; nenhum outro pode).
- `git diff --stat requirements.txt` sai vazio.
- `git status --porcelain calibration.json` sai vazio e o arquivo continua com as chaves que
  tinha antes da rodada — a suite nunca toca o arquivo real.
</verification>

<success_criteria>
- Um PNG resgatado entra e o EXP com quatro casas sai impresso, sem o jogo aberto.
- Quatro casas ou recusa, com o caso de duas casas provando que nao ha completar com zero.
- Campo vazio, gramatica invalida e discordancia entre escalas sao tres motivos distinguiveis.
- Um retangulo que nao cabe recusa em vez de encurtar.
- Todo campo do dataclass sobrevive ao `salvar`, provado por teste estrutural com controle positivo.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-01-SUMMARY.md` when done
</output>
