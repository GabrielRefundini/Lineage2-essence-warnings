---
phase: 01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - l2scanner/renda_leitura.py
  - l2scanner/renda_modo.py
  - tests/test_renda_completa.py
  - tests/test_renda_par.py
  - tests/fixtures/renda/
autonomous: true
requirements: [LEIT-01, LEIT-03, LEIT-04, LEIT-07, LEIT-08]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O comando de leitura unica imprime os TRES campos na mesma rodada, para UM personagem nomeado: o nivel como inteiro, o EXP com as QUATRO casas e o sinal de porcentagem, e a adena na grafia de milhar do jogo — o usuario compara o terminal com o monitor e fecha o criterio 1 num olhar."
    - "A adena e lida sobre MASCARA, nunca sobre o recorte cru. Medido em campo com fundo de grama clara: o cru devolve '' nas duas escalas. A premissa do LEIT-03 — 'o OCR ja devolve a adena do recorte cru' — veio do spike com fundo escuro e e falsa no caso geral (M-E, LEIT-08)."
    - "A adena sai do ULTIMO grupo que respeita a gramatica de milhar, e nunca do primeiro numero da faixa: o XM e o L-Coin estao no mesmo recorte, e o L-Coin passa na gramatica inteira (M18). A escolha do ultimo e carga estrutural, e nao arrumacao."
    - "A adena reusa numero_valido e inteiro_de_quantidade como estao, com a normalizacao de ponto para virgula; nenhuma terceira gramatica de milhar nasce nesta arvore."
    - "O nivel e lido por OCR sobre mascara de brilho calibrada, e a refutacao dos moldes de glifo vai escrita no fonte com a medicao ao lado: conjunto_descreve_numeros exige o conjunto de digitos inteiro e a regiao do nivel so mostra dois caracteres."
    - "Os tres campos cruzam as duas escalas de producao, e nao so o EXP — mas pela regra da ABSTENCAO e nao pela da igualdade (CTX-3-linha, M-D): as duas rodam sempre, uma valida com a outra abstendo ACEITA, e so duas validas e diferentes recusam. A regra antiga recusaria o nivel e a adena da Faerlina para sempre."
    - "Cada campo lido carrega POR QUANTAS escalas foi sustentado, e o comando marca na tela o campo sustentado por uma so — porque naquele campo o cruzamento nao esta pegando substituicao, e quem le tem direito de saber."
    - "A leitura e de UM personagem, com os retangulos e os pisos DELE (LEIT-07): sem nome nao ha leitura, personagem sem calibracao e recusa nomeada, e nunca ha queda para a entrada do vizinho — o retangulo do vizinho devolve 349 onde deveria estar o nivel."
    - "Cada regiao usa o SEU piso de brilho, tres por personagem (LEIT-08): passar o piso do nivel na adena, ou o contrario, faz o campo sumir em silencio, e as bandas medidas em campo nao tem intersecao."
    - "Um EXP que anda para tras com o nivel parado, um nivel que desce, e uma adena que salta ordem de grandeza sao tres recusas NOMEADAS e DISTINGUIVEIS entre si e das do 01-01 — provado por um teste que deriva os motivos do modulo, e nao de uma lista escrita a mao."
    - "As tres regras de par tem controles negativos REAIS e nao inventados: o par de campo Faerlina 2026-09-01 23h50 (nivel 66, EXP 68,5632%, adena 10.673.628) -> 2026-09-02 00h45 (nivel 67, EXP 8,0012%, adena 13.160.684) e um level up verdadeiro, com as duas pontas gravadas em disco, e as tres regras tem de se abster nele."
    - "As tres regras de par sao funcoes PURAS sobre um par passado por parametro. A Fase 1 continua sem estado: nenhum modulo desta fase guarda a leitura anterior, e nenhum caminho de producao desta fase as chama, porque nao existe segunda leitura numa fase sem memoria (CTX-9)."
    - "O fator de salto da adena entra somente-nomeado e SEM default; o portao de arvore de sintaxe do 01-03 continua verde depois desta onda."
    - "Rodar o comando contra a montagem com a regiao do nivel preta imprime o nivel como RECUSA de campo vazio e sai com codigo proprio — nem numero, nem traceback, nem codigo 0."
  artifacts:
    - l2scanner/renda_leitura.py
    - l2scanner/renda_modo.py
    - tests/test_renda_completa.py
    - tests/test_renda_par.py
    - tests/fixtures/renda/montagem_completa.png
  key_links:
    - "renda_leitura -> mercado_leitura.numero_valido + inteiro_de_quantidade: a adena reusa o par como esta. O par e complementar e nenhuma das duas basta sozinha — medido nesta arvore: o XM passa na primeira e cai na segunda (M22), e uma virgula de milhar perdida faz o caminho contrario."
    - "renda_leitura -> mercado_leitura.mascara_de_numero em TODOS os tres campos, cada um com o piso da sua regiao: a mascara deixou de ser opcional para a adena (M-E), e a razao de existirem tres pisos e que as bandas medidas nao tem intersecao."
    - "renda_leitura -> _cruzar_as_escalas (do 01-01) nos TRES campos: a mesma decisao por abstencao, e nao tres comparacoes escritas a mao."
    - "renda_leitura -> Calibracao.renda_por_personagem via o acessor por nome: a leitura escolhe retangulo e piso pelo personagem, e o acessor devolve a entrada dele ou nada."
    - "renda_modo -> renda_leitura.ler_os_tres_campos: a seta continua ferramenta -> puro. O inteiro vive no puro; a grafia de milhar que o olho compara com o monitor vive no modo."
    - "conferir_o_par -> Fase 2: a Fase 1 entrega as tres regras como funcoes puras e NAO as chama em producao. Quem tem duas leituras e a Fase 2, e e la que o fator de salto vem de cima. O par de campo com o level up verdadeiro vai junto, e a Fase 2 o herda para o REND-03."
---

<objective>
Fechar o criterio 1 do roadmap e as duas metades do LEIT-04 que o `01-01` nao tinha como fechar:
por o **nivel** e a **adena** no comando de leitura unica, ao lado do EXP que ja sai, e escrever
as recusas que so existem quando ha **dois** campos e **duas** leituras — a adena saltando ordem
de grandeza e o EXP andando para tras sem mudanca de nivel.

Purpose: a fase tem cinco criterios de sucesso e quatro deles ja tem plano. O primeiro nao: ele
pede um comando que imprima **os tres** campos batendo com o monitor, e o `01-01` imprime **so o
EXP**, por decisao propria e escrita. A medicao de campo de 2026-09-02 ja decidiu qual leitor le
cada um dos outros dois — e obrigou a mascara na adena; o `01-03` poe os retangulos e os tres
pisos, por personagem, no `calibration.json`. **Este plano e o que transforma essas
tres coisas em uma linha que o usuario le na tela.** Sem ele a fase entrega tres quartos de um
leitor e nenhum criterio 1.

E a segunda metade nao e enfeite. As duas recusas que faltam sao as unicas que enxergam o modo de
falha mais caro deste projeto: um numero **valido, plausivel e errado**. Medido nesta sessao: no
recorte cru em que a adena nao aparece, a extracao devolve `8786` com a gramatica inteira
satisfeita (M19). Nenhuma trava de forma pega isso. So a comparacao com a leitura anterior pega.

Output: `renda_leitura` com os leitores do nivel e da adena, a composicao dos tres campos e as
tres regras de par; `renda_modo` imprimindo os tres campos com codigo de saida proprio para cada
desfecho; e dois arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-CONTEXT.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-MEDICOES-DE-CAMPO.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-RESEARCH.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-01-SUMMARY.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-02-SUMMARY.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-03-SUMMARY.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-MEDICOES.md
</context>

> **Ordem de autoridade quando duas fontes discordarem:** `01-MEDICOES-DE-CAMPO.md` primeiro (a
> tela do usuario com as duas instancias vivas), depois `01-MEDICOES.md` (a bancada do `01-02`),
> depois os numeros de planejamento deste arquivo, depois `01-RESEARCH.md`, depois `01-CONTEXT.md`
> e o `ROADMAP.md`. Uma fonte derrubada nao e apagada: e citada com o numero que a derrubou.

## O que este plano herda e nao reabre

As decisoes **CTX-1 a CTX-9** estao enunciadas em `01-01-PLAN.md` e valem aqui inteiras. Tres
delas mandam neste plano em particular:

- **CTX-1** — **OCR sobre mascara de brilho calibrada nos tres campos.** Os moldes de glifo estao
  derrubados para a barra pelo M10 e para o nivel pela pesquisa (c3). Este plano e o que escreve a
  refutacao do nivel no fonte, porque e ele que escreve o leitor do nivel.
- **CTX-2** — **`numero_valido` e recusa nomeada valem para os tres campos.** E a unica clausula
  do `CONTEXT.md` que nenhuma medicao pode reabrir, e e literalmente o que este plano executa nos
  dois campos que faltavam.
- **CTX-9** — **A Fase 1 e sem estado.** As tres regras de par sao funcoes puras sobre um par
  passado por parametro. Elas nascem aqui, sao testadas aqui, e **nenhum caminho de producao desta
  fase as chama** — porque uma fase sem memoria nao tem a leitura anterior. Quem as chama e a
  Fase 2. No dia em que alguem as ligar num laco dentro desta fase, a fase deixou de ser o que o
  `<domain>` do `CONTEXT.md` fechou.

**A fatia ja foi provada.** O tracador desta fase e a Tarefa 1 do `01-01`: um PNG resgatado entra
e o EXP sai impresso, atravessando todas as camadas. Este plano nao abre uma segunda fatia — ele e
**expansao** sobre um esqueleto que ja esta de pe e ja tem teste de ponta a ponta.

**Os leitores nao sao escolhidos aqui.** A medicao decide, e ela ja aconteceu:
`01-MEDICOES-DE-CAMPO.md` (2026-09-02, as duas instancias vivas) e a autoridade, e o
`01-MEDICOES.md` do `01-02` acrescenta a taxa de discordancia com denominador. Este plano **le**
os dois e implementa o que eles disserem. Se um veredicto contrariar o que esta escrito abaixo,
vale o veredicto, e a divergencia vai no SUMMARY com a secao citada — nao se reabre a medicao com
uma segunda opiniao.

## O que a medicao de campo mudou neste plano, em quatro linhas

O documento e `01-MEDICOES-DE-CAMPO.md` e ele derrubou quatro coisas que este plano dava como
certas. Ficam **citadas e respondidas**, e nao apagadas.

**1. A precondicao deste plano esta SATISFEITA.** A Tarefa 1 exigia uma rodada ao vivo do `01-02`
para produzir um recorte do nivel, porque nenhuma gravacao continha o campo (M11). A gravacao
`recordings/20260902-004500-renda-duas-instancias/` contem, nos **dois** personagens, com verdade
de campo lida a olho (nivel 67 e 69). O `01-01` resgata esses recortes como fixtura. **Este plano
nao precisa mais do jogo aberto para provar o leitor do nivel** — so para o `human-check` do
criterio 1, que sempre foi de olho humano e continua sendo.

**2. A adena EXIGE mascara** (achado M-E, e virou o LEIT-08). A premissa do LEIT-03 e do
`ROADMAP.md` — *"o OCR ja devolve a adena do recorte cru, sem pre-processamento"* — veio do spike,
onde o fundo atras da barra semitransparente estava escuro. Com o fundo em grama clara, o recorte
cru devolve `''` nas **duas** escalas. A mascara nao e uma melhora de qualidade; e o que faz o
campo existir. O leitor da adena nasce mascarado, e a refutacao vai escrita no fonte com as duas
condicoes de fundo ao lado.

**3. O cruzamento e por ABSTENCAO** (achado M-D, CTX-3-linha). Este plano dizia "os tres campos
cruzam as duas escalas" com a regra "iguais ou recusa". Medido, as escalas **se revezam**: o nivel
da Faerlina so sai na 3x, a adena da Faerlina so sai na 2x. Com a regra antiga, os dois campos
seriam recusados **para sempre** — e este plano e justamente o dono desses dois campos. A regra
que vale e a do `01-01`: as duas rodam sempre, abstencao nao e discordancia, uma valida aceita, e
o valor carrega quantas escalas o sustentaram.

**4. A leitura e POR PERSONAGEM** (achado M-F, e virou o LEIT-07). O retangulo do nivel difere em
14 px entre as duas instancias, e o vizinho do nivel e um numero plausivel (`349` / `112`) que
passa em `numero_valido`. Este plano le **de um personagem nomeado**, com os retangulos e os tres
pisos dele; personagem sem calibracao e recusa nomeada; nao existe queda para o vizinho.

## Numeros medidos no planejamento do 01-04 (2026-09-02, nesta arvore)

Medidos agora, por execucao de `l2scanner.mercado_leitura` contra as strings reais que o spike e a
pesquisa registraram. Continuam a numeracao da fase (M1-M12 no `01-01`, M13-M17 no `01-03`) e
**mudam o desenho** dos dois leitores deste plano.

| # | medicao | resultado |
|---|---|---|
| M18 | `numero_valido("13,091")` e `inteiro_de_quantidade("13,091")` — o **L-Coin**, que divide o recorte com a adena | `True` e `13091`. **O L-Coin passa na gramatica inteira.** A forma nao distingue L-Coin de adena; so a POSICAO distingue. A ancora "o ultimo grupo valido" e carga estrutural, e nao arrumacao. |
| M19 | A extracao do ultimo grupo valido sobre `Special 8,786` — a leitura real do M7, o recorte cru em que a adena **nao aparece** | devolve **8786**, com as duas funcoes do mercado felizes. Um numero **valido, plausivel e errado por tres ordens de grandeza**. Nenhuma trava de forma pega isso. |
| M20 | `numero_valido("6b")` — o piso `190` da curva que o proprio roadmap cita para o nivel | `False`. **A leitura errada do nivel que o roadmap usa como exemplo ja e recusada pela validacao que o CTX-2 exige.** A defesa mais barata do nivel ja existe e sai de graca. |
| M21 | `numero_valido("10.673.628")` — a adena com a grafia de PONTO que o jogo escreve na tela | `False`. A normalizacao de ponto para virgula e **obrigatoria**, e nao cosmetica: as duas funcoes do mercado falam virgula. |
| M22 | `numero_valido("58,40")` e `inteiro_de_quantidade("58,40")` — o **XM**, o outro vizinho de recorte | `True` e `None`. O XM passa na primeira e cai na segunda. **Nenhuma das duas funcoes basta sozinha**, e a evidencia disso agora tem nome de campo e nao so de caso sintetico. |
| M23 | A extracao do ultimo grupo valido sobre as leituras completas `Special 58,40 13,091 10,679,769` e `Special 133,52 8,786 2,207,577 B` | devolve `10679769` e `2207577` — a adena nas duas, com o lixo do fim do M8 descartado sem uma linha de tratamento especial. A ancora funciona nas duas gravacoes. |

**O que estes numeros decidem, e o plano executa:**

- **M18 + M23 fixam a ancora da adena.** Nao e "o maior numero", nao e "o com mais digitos", nao e
  "o primeiro". E **o ultimo grupo da faixa que respeita a gramatica de milhar**, porque a barra
  escreve XM, L-Coin e adena nessa ordem e a adena e a ultima. Uma ancora por tamanho quebraria no
  dia em que o usuario tivesse mais L-Coin que adena, que e um dia normal e nao uma borda.
- **M19 e o argumento inteiro da Tarefa 2, e ele e desconfortavel.** A ancora certa, aplicada a um
  recorte errado, produz um numero que passa em tudo. `8786` no lugar de `10673628` nao e um erro
  visivel: e uma taxa de renda errada por tres ordens de grandeza que continua parecendo uma taxa.
  **A unica coisa nesta fase que enxerga isso e a regra de salto sobre um par** — e e por isso que
  ela nao pode ser adiada para a Fase 2, mesmo que so a Fase 2 va chama-la.
- **M20 e a boa noticia do nivel.** O exemplo de leitura errada que o roadmap cita ja e recusado
  pela validacao obrigatoria. O que **nao** e recusado por ela e a substituicao — `66` virando
  `56`, `86` ou `68` passa em tudo. Contra substituicao existem duas defesas nesta arvore: o
  cruzamento de escalas e a regra de par. Este plano usa as duas.
- **M21 e M22 fecham a forma do leitor da adena**: normaliza o separador, valida a forma, converte
  — nessa ordem, e as tres etapas sao obrigatorias porque cada uma pega um modo diferente.

**Nenhum destes numeros entra no fonte.** Eles vivem aqui, na docstring dos testes que os
transformam em caso, e no `01-MEDICOES.md`. O portao de arvore de sintaxe do `01-03`
(`tests/test_nenhum_numero_do_spike_no_fonte.py`) continua valendo depois desta onda, e a
verificacao final o roda.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O nivel e a adena no modulo puro — e os tres campos numa estrutura so</name>
  <files>l2scanner/renda_leitura.py, tests/test_renda_completa.py, tests/fixtures/renda/</files>
  <precondition>Existem em `tests/fixtures/renda/` os recortes RESGATADOS da regiao do nivel dos DOIS personagens, com o numero visivel, vindos de `recordings/20260902-004500-renda-duas-instancias/` e gravados pelo `01-01`. A rodada ao vivo que este plano exigia antes NAO e mais necessaria — a gravacao de campo tem o nivel (M11 fechado). Se as fixturas nao estiverem la, o executor para e pede o resgate do `01-01`, em vez de fabricar um recorte sintetico: um leitor de nivel provado contra pixel inventado nao prova nada.</precondition>
  <read_first>
    - `l2scanner/renda_leitura.py` inteiro, como o `01-01` o deixou — o charter, `RecusaDaRenda`, as constantes `MOTIVO_*`, `_recusar`, `recortar`, `decimos_de_milesimo` e `exp_da_barra`. **A forma de `exp_da_barra` e o molde dos dois leitores desta tarefa**, e copia-la e o ponto: tres leitores com o mesmo desenho sao uma superficie de manutencao; tres desenhos diferentes sao tres.
    - `l2scanner/mercado_leitura.py:588-605` — `inteiro_de_quantidade` inteiro, com a regra de que o primeiro grupo tem no maximo tres digitos e os demais exatamente tres; `:606-650` — `numero_valido`, e sobretudo o bloco da docstring que declara **o que ela nao pega**: substituicao. E a frase que justifica o cruzamento de escalas nos dois campos novos.
    - `l2scanner/mercado_leitura.py:155-200` — `mascara_de_numero(bgr, valor_minimo)`, a mascara do spike com o piso ja por parametro e sem default.
    - `l2scanner/mercado_leitura.py:312-340` — `conjunto_descreve_numeros` e a exigencia do conjunto de digitos **inteiro**. E a medicao que esta tarefa escreve no fonte como refutacao dos moldes para o nivel.
    - `l2scanner/manutencao.py:119-121` — `_HORAS`, `_MINUTOS`, `_SEGUNDOS`: expressoes sobre texto de OCR com **classe de caractere para confusao de glifo**. E o idioma desta casa para "o OCR troca estes dois", e a faixa da adena precisa do mesmo cuidado com o separador.
    - `l2scanner/manutencao.py:800-822` — o cruzamento de escalas e a docstring que corrige a razao antiga: a ordem preserva **diversidade de metodo**, e nao orcamento.
    - `l2scanner/ocr.py:80-81` e `:172-212` — as duas escalas reais e a promessa de nunca levantar; `:26-70` — a tabela de custo, que e o que autoriza pagar a segunda escala em recortes tao pequenos.
    - `l2scanner/cliente.py:111` — `nome_do_personagem`, o campo que a leitura carrega mesmo sem existir arquivo ainda (CTX-8).
    - `01-MEDICOES-DE-CAMPO.md` INTEIRO — a autoridade desta rodada. Achado M-E (o cru NAO le a adena com fundo claro; as bandas de piso por regiao), achado M-D (o cruzamento e por abstencao), achado M-F (a calibracao e por personagem) e a tabela de verdade de campo (nivel 67/69, adena 13.160.684/1.696.020) que os testes desta tarefa afirmam. **Estes veredictos mandam sobre o que estiver escrito nesta tarefa.**
    - `01-MEDICOES.md` (do `01-02`) quando existir — a taxa de discordancia entre leituras validas, com denominador. Ela nao muda o desenho desta tarefa; ela diz se a recusa por discordancia tem trabalho a fazer nesta arvore.
    - A secao "Numeros medidos no planejamento do 01-04" acima — os itens M18 a M23 sao os casos de teste desta tarefa.
  </read_first>
  <behavior>
    - `adena_da_barra` aplica MASCARA antes de qualquer OCR. Sobre o recorte cru do frame de campo da Faerlina — fundo de grama clara — as duas escalas devolvem `''`; sobre o mesmo recorte mascarado com o piso da barra direita, sai `13.160.684`. O teste tem os dois lados, e o lado cru e o CONTROLE que prova que a mascara nao e enfeite.
    - `adena_da_barra` sobre a leitura completa da direita devolve a adena, e nao o XM nem o L-Coin.
    - `adena_da_barra` sobre um texto que contem SO o L-Coin devolve o L-Coin — e o teste afirma isso como comportamento CONHECIDO e perigoso, com o M19 citado, porque e a prova de que a forma sozinha nao basta.
    - `adena_da_barra` sobre um texto sem nenhum grupo valido devolve recusa de gramatica; sobre texto vazio, recusa de campo vazio.
    - A adena escrita com ponto e a adena escrita com virgula produzem o MESMO inteiro.
    - `nivel_da_regiao` sobre o recorte resgatado devolve o inteiro do nivel.
    - `nivel_da_regiao` sobre um recorte todo preto devolve recusa de campo vazio, distinguivel da recusa de gramatica.
    - Um texto de nivel com um caractere que nao e digito devolve recusa de gramatica, e nao um numero truncado.
    - Nos tres campos, duas escalas que leem inteiros diferentes devolvem recusa por discordancia, com as duas leituras cruas no detalhe.
    - Nos tres campos, uma escala valida com a outra ABSTENDO devolve o inteiro, com `escalas = 1`. Os dois casos de campo entram nomeados: o nivel da Faerlina sai `67` so na 3x, e a adena da Faerlina sai `13.160.684` so na 2x — sob a regra antiga os dois seriam recusados para sempre.
    - `ler_os_tres_campos` recebe o NOME do personagem, usa os retangulos e os tres pisos dele, e devolve os tres resultados de uma vez, cada um inteiro OU recusa, sem que um campo que recusou impeca os outros dois de serem lidos.
    - Ler o frame da Faerlina com o nome `Yazalaque` NAO devolve o nivel da Faerlina: os retangulos sao os da Yazalaque, e o resultado e recusa ou um numero diferente — o teste afirma que **nao** e 67, com os dois retangulos nomeados na mensagem.
    - Um personagem sem entrada na calibracao devolve recusa nomeada, distinta da de campo vazio e da de gramatica.
    - `ler_a_renda` devolve `LeituraDaRenda` quando os tres saem inteiros, e a recusa do primeiro campo em falta na ordem declarada quando nao saem.
  </behavior>
  <action>
Escrever, em `l2scanner/renda_leitura.py`, os dois leitores que faltam — **copiando a forma de
`exp_da_barra`**, que o `01-01` ja fixou: mascara com o piso vindo da calibracao, escala barata
primeiro, escala cara so quando a barata passa no teste de forma, recusa nomeada em cada desvio.

`_ultimo_grupo_valido(texto)`: a ancora da adena, e **ela nao existe no repositorio hoje** — o
exemplo da pesquisa a invoca sem defini-la, e essa lacuna e desta tarefa. Varre os grupos de
digitos e separadores da faixa, normaliza ponto para virgula em cada um, e devolve **o ultimo que
passa em `numero_valido` E converte por `inteiro_de_quantidade`**. Escrever ao lado, com o numero,
por que a ancora e a posicao e nao o tamanho: o L-Coin da mesma faixa passa na gramatica inteira
(M18), entao a forma nao distingue os dois campos; e uma ancora por "o maior" quebraria no dia em
que o usuario tivesse mais L-Coin que adena. Escrever tambem por que as duas funcoes do mercado
sao chamadas **as duas**: o XM passa na primeira e cai na segunda (M22), e uma virgula de milhar
perdida faz o caminho contrario. Nenhuma basta sozinha, e a ordem importa.

`adena_da_barra(recorte, *, piso_de_brilho)`: **mascara primeiro, sempre**, depois as duas escalas
com `_ultimo_grupo_valido` em cada uma, e a decisao por `_cruzar_as_escalas`. O piso e o da **barra
direita**, e nao o do nivel nem o da barra esquerda — sao tres bandas medidas e elas nao coincidem
(M-E), e passar o piso errado aqui produz exatamente o desaparecimento silencioso do campo que o
`01-03` calibrou para evitar.

**A obrigatoriedade da mascara e uma refutacao medida, e ela vai escrita no fonte com as duas
condicoes de fundo ao lado.** O `ROADMAP.md` e o LEIT-03 dizem que *"o OCR ja devolve a adena do
recorte cru, sem pre-processamento"*. Isso foi verdade no spike, onde o fundo atras da barra
semitransparente estava escuro. Com o fundo em grama clara, medido em campo, o recorte cru devolve
`''` nas **duas** escalas — o campo simplesmente nao esta la. A barra e semitransparente e o
cenario atras dela muda, entao o contraste do texto branco e uma funcao de onde o usuario esta
farmando. Escrever isso, e escrever a consequencia que o `01-03` ja carrega: a banda util da adena
muda com o cenario, e por isso a largura da banda e gravada e uma banda estreita e um aviso.

`nivel_da_regiao(recorte, *, piso_de_brilho)`: a mesma forma, com o piso do **nivel**, e a
validacao pelas duas funcoes do mercado (medido: a leitura errada da curva do roadmap ja cai na
primeira — M20). Escrever no fonte, junto do leitor, a **refutacao dos moldes de glifo** com a
medicao ao lado, como o `ROADMAP.md` exige de todo numero que caiu: `conjunto_descreve_numeros`
exige o conjunto de digitos inteiro, a regiao do nivel exibe dois caracteres que mudam uma vez por
sessao, e portanto nunca havera de onde cortar os dez moldes. Nao e caro: e impraticavel, que e
outra coisa. Citar o precedente da propria arvore — os moldes de nome de item foram abandonados em
favor de OCR em 2026-08-29.

**Os tres campos cruzam as duas escalas, e nao so o EXP — mas pela regra da ABSTENCAO.** A razao
de cruzar vai escrita e ela e a docstring de `numero_valido`: a gramatica pega glifo perdido e
glifo a mais, e **nao pega substituicao**. Um nivel de dois digitos com um deles trocado passa em
tudo; uma adena com um digito trocado passa em tudo. Contra substituicao existem duas defesas
nesta arvore, e o cruzamento e a que cabe dentro de uma leitura unica. O custo e um segundo OCR de
um recorte pequeno, contra a tabela de custo do proprio modulo de OCR — nao e restricao.

**E a regra do cruzamento nao e "as duas concordam".** Os dois campos desta tarefa sao exatamente
os que a medicao de campo pegou se revezando: o nivel da Faerlina so sai na 3x (a 2x devolve vazio
em toda a banda util) e a adena da Faerlina so sai na 2x (a 3x devolve vazio ou lixo). Com a regra
antiga, **este plano entregaria dois campos que recusam para sempre** — o que e o mesmo que nao
entregar. Usar `_cruzar_as_escalas` do `01-01`, que ja implementa os quatro desfechos, e **nao**
escrever tres comparacoes a mao aqui: uma segunda implementacao da regra e uma segunda chance de
implementar a regra errada.

E como a aceitacao por uma escala so e o caso comum nesses dois campos, o numero de escalas viaja
com o valor ate o `renda_modo`, que o marca na tela. Escrever a razao: naquele campo o cruzamento
nao esta pegando substituicao, e o usuario que compara o terminal com o monitor e, ali, a unica
guarda que resta.

`CamposDaRenda` como dataclass congelado de tres campos, cada um inteiro **com o numero de escalas
que o sustentaram** ou recusa, e `ler_os_tres_campos(...)` que a devolve. A razao de existir uma
estrutura intermediaria em vez de so `ler_a_renda`: o criterio 1 pede que o usuario veja **os
tres** campos na tela, inclusive quando um recusou, e um leitor que aborta no primeiro problema
esconde os outros dois.

`ler_os_tres_campos` recebe o **nome do personagem** e a calibracao, e resolve os tres retangulos e
os tres pisos pelo acessor que o `01-01` escreveu — o que devolve a entrada daquele personagem ou
nada. Personagem sem entrada e recusa nomeada nos tres campos, com o motivo proprio, **antes** de
qualquer OCR: nao se gasta o motor para descobrir que nao se sabia de quem era a tela. E nao
existe caminho neste modulo que leia sem nome, nem que caia na entrada do vizinho — a proibicao
mora no acessor, num lugar so, e o teste que a prende tambem.

`LeituraDaRenda` como dataclass congelado: o personagem, o nivel, o EXP em decimos de milesimo, a
adena e o carimbo — **tudo inteiro**, no precedente de `total_em_centesimos`. O carimbo entra por
parametro e o modulo continua sem ler o relogio do sistema, pela regra que o `01-01` prendeu com
um teste de fonte. `ler_a_renda(...)` compoe: os tres inteiros viram `LeituraDaRenda`; qualquer
recusa devolve a do primeiro campo em falta na ordem declarada, e a ordem vai escrita para que
duas leituras com o mesmo problema produzam sempre a mesma recusa.

As fixturas do nivel **ja existem**: o `01-01` as resgata da gravacao de campo, uma por
personagem, com verdade de campo escrita (67 e 69). Esta tarefa nao as produz, ela as usa — e a
precondicao acima existe para o executor parar caso elas nao estejam la.

Gravar `tests/fixtures/renda/montagem_completa.png`: a mesma montagem do `01-01`, agora com o
recorte do nivel colado na posicao de calibracao em vez da regiao preta, e com o personagem no
nome do arquivo, porque a posicao do nivel e do personagem. A montagem antiga **continua no
repositorio e continua sendo caso de teste** — ela e o caso de campo vazio, e o `01-01` a gravou
com essa promessa escrita na docstring.

E preferir, onde der, o **frame de campo inteiro** a montagem: a montagem prova geometria e
caminho, e o frame de campo prova a leitura contra a tela real do usuario com verdade escrita ao
lado. Os dois existem por razoes diferentes, e o teste diz qual esta usando e por que.

Escrever `tests/test_renda_completa.py` no idioma da casa: docstring de modulo nomeando o que o
arquivo existe para impedir, classes por invariante, nomes em portugues com o fato em CAIXA ALTA,
e o idioma de skip por ausencia de OCR copiado de `tests/test_ocr.py`. Uma classe por campo novo,
uma para a composicao dos tres, e — a mais importante — uma classe para os **vizinhos de
recorte**, com um caso que passa a leitura completa da direita e afirma que o resultado nao e o XM
nem o L-Coin, citando os dois valores pelo nome na mensagem de falha. Sem esse caso, um leitor que
devolvesse o primeiro numero da faixa passaria em todos os outros.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_completa.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_completa.py -q -rs` termina em 0 e relata **0 skipped**.
    - Existe um teste que passa a leitura completa da barra direita e afirma que o retorno **nao** e o XM e **nao** e o L-Coin, com os dois valores nomeados na mensagem de falha.
    - Existe um teste que afirma que a adena com separador de ponto e a adena com separador de virgula produzem o mesmo inteiro.
    - Existe um teste que le o nivel do recorte RESGATADO de cada um dos DOIS personagens e afirma `67` e `69` — os inteiros da verdade de campo —, e outro que le a regiao preta da montagem do `01-01` e afirma recusa de campo vazio, com os dois motivos comparados e afirmados DIFERENTES.
    - Existe o CONTROLE DA MASCARA: o mesmo recorte de adena do frame de campo devolve `''` nas duas escalas SEM mascara e o inteiro `13160684` COM mascara. Sem esse par, a obrigatoriedade da mascara e uma afirmacao de prosa; com ele, e um teste.
    - Existe um teste que le o frame da Faerlina passando o nome `Yazalaque` e afirma que o resultado **nao** e `67`, com os dois retangulos de nivel nomeados na mensagem de falha — a prova de que a leitura e por personagem e nao por retangulo unico.
    - Existe um teste que pede a leitura de um personagem sem entrada na calibracao e afirma recusa nomeada, distinta da de campo vazio e da de gramatica.
    - Os tres leitores cruzam as duas escalas: `grep -c "ler_texto_ampliado" l2scanner/renda_leitura.py` retorna ao menos 3, e a decisao e a de producao: `grep -c "_cruzar_as_escalas" l2scanner/renda_leitura.py` cresce em pelo menos 2 em relacao ao que o `01-01` deixou — os tres campos usam a mesma funcao, e nao tres comparacoes escritas a mao.
    - Existe um teste por campo afirmando que uma escala valida com a outra abstendo devolve o INTEIRO com `escalas = 1`; os dois casos de campo (nivel so na 3x, adena so na 2x) aparecem nomeados, com a mensagem de falha dizendo que a regra antiga recusaria os dois para sempre.
    - A refutacao dos moldes esta no fonte com a funcao que a sustenta citada pelo nome: `grep -c "conjunto_descreve_numeros" l2scanner/renda_leitura.py` retorna ao menos 1.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py tests/test_calibracao_renda.py -q` continua em 0 — a expansao nao quebrou o tracador.
    - `ls tests/fixtures/renda/montagem_da_janela.png` continua existindo: a montagem com a regiao preta e caso de teste, e nao rascunho.
  </acceptance_criteria>
  <done>Os tres campos saem do pixel como inteiro ou como recusa nomeada, com o mesmo desenho de leitor nos tres, para um personagem nomeado e com os retangulos e pisos dele; a adena so e lida sob mascara e o controle sem mascara prova que isso e necessario; e o vizinho de recorte que passaria por adena esta preso por um teste que o nomeia.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: As tres recusas que so existem quando ha um PAR — e a prova de que sao distinguiveis</name>
  <files>l2scanner/renda_leitura.py, tests/test_renda_par.py</files>
  <read_first>
    - `l2scanner/renda_leitura.py` como a Tarefa 1 o deixou — as constantes `MOTIVO_*` existentes e `_recusar`. As tres constantes novas entram no mesmo padrao, e a distincao entre elas e o produto desta tarefa.
    - `l2scanner/mercado_leitura.py:606-650` — a docstring de `numero_valido`, especificamente o bloco **O QUE ELA NAO PEGA: SUBSTITUICAO**, e a frase que diz que para esse modo servem a margem calibrada, o acordo entre dois frames e a guarda de cruzamento. Esta tarefa e o "acordo entre dois frames" da renda.
    - `l2scanner/mercado_leitura.py:559-586` — `centesimos_de_moeda`, pela disciplina de aritmetica inteira: nada nesta tarefa usa `float`, nem para razao.
    - `.planning/workstreams/renda/REQUIREMENTS.md`, LEIT-04 inteiro — as tres frases que este arquivo executa, e o paragrafo que explica por que o modo de falha e o pior possivel para um medidor.
    - `.planning/workstreams/renda/ROADMAP.md`, criterio de sucesso 3 da Fase 1 — a exigencia de que cada modo vire recusa **nomeada e distinguivel**, e a frase que a justifica.
    - `.planning/workstreams/renda/ROADMAP.md`, Fase 2 — a linha sobre subir de nivel nao virar prejuizo e gastar adena nao virar renda negativa. **E a fronteira desta tarefa**: aqui se decide o que e leitura duvidosa; la se decide o que e conta.
    - `01-01-PLAN.md` secao CTX-9 — a fase e sem estado, e a razao operacional disso.
    - A secao "Numeros medidos no planejamento do 01-04" acima, item M19 — o caso que da nome a esta tarefa.
  </read_first>
  <behavior>
    - **O par de campo nao recusa em nenhuma das tres regras.** Faerlina, 2026-09-01 ~23h50 (nivel 66, EXP 68,5632%, adena 10.673.628) -> 2026-09-02 ~00h45 (nivel 67, EXP 8,0012%, adena 13.160.684). E um level up verdadeiro com as duas pontas gravadas em disco, e `conferir_o_par` devolve tupla vazia.
    - Um par com o mesmo nivel e o EXP menor no segundo devolve recusa de EXP para tras.
    - Um par com o nivel MAIOR no segundo e o EXP menor NAO recusa: subir de nivel zera o EXP, e isso e o comportamento normal do jogo.
    - Um par com o nivel MENOR no segundo devolve recusa de nivel para tras, e a regra do EXP se abstem nesse par — cada recusa responde a UMA pergunta, e o mesmo fato nao produz duas.
    - Um par cuja adena cresce de um valor para outro acima do fator devolve recusa de salto; um par cuja adena CAI abaixo do fator tambem — o salto e sobre a razao, e nao sobre a direcao.
    - Um par cuja adena cai por gasto normal, dentro do fator, NAO recusa.
    - Um par com adena zero de um dos lados nao recusa por salto, e a docstring diz por que.
    - Chamar a regra da adena sem o fator levanta `TypeError`: o limiar nao tem valor de fabrica.
    - `conferir_o_par` devolve uma tupla vazia para um par coerente, e uma tupla com todas as recusas encontradas para um par incoerente.
    - Todos os motivos do modulo sao distintos entre si, dois a dois, e o teste que afirma isso os deriva do modulo.
  </behavior>
  <action>
Escrever, em `l2scanner/renda_leitura.py`, as tres regras que o LEIT-04 pede e que o `01-01` nao
tinha como escrever, porque elas precisam de **dois** campos e de **duas** leituras. As tres sao
funcoes **puras sobre um par passado por parametro**, cada uma devolvendo uma recusa nomeada ou
nada. Nenhuma delas guarda leitura, e nenhum caminho de producao desta fase as chama (CTX-9) —
escrever isso na docstring de cada uma, junto de quem as vai chamar, porque uma funcao sem chamador
e um convite a alguem "consertar" ligando um cache dentro do leitor.

`o_exp_andou_para_tras(anterior, atual)`: recusa quando o nivel e o mesmo nos dois e o EXP do
segundo e menor. **Abstem quando o nivel mudou**, para cima ou para baixo, e a razao vai escrita:
subir de nivel zera o EXP, entao com nivel diferente os dois numeros nao sao comparaveis e uma
recusa aqui seria ruido em cima do evento mais normal do jogo. Escrever tambem o custo assumido: se
o jogo tirar EXP na morte dentro do mesmo nivel, esta regra recusa uma amostra legitima. **A troca
esta feita de proposito** — uma recusa custa uma amostra, e um digito trocado gravado custa a taxa
inteira dali para a frente. O motivo e nomeado justamente para que a Fase 2 possa decidir
diferente com a evidencia na mao, em vez de descobrir que nao da para distinguir.

`o_nivel_andou_para_tras(anterior, atual)`: recusa quando o nivel do segundo e menor. Escrever a
razao com a mesma honestidade: o caminho comum para um nivel que desce nao e o jogo, e uma
substituicao de digito — e substituicao e exatamente o que `numero_valido` documenta nao pegar. Se
o jogo permitir perder nivel, a recusa custa uma amostra e o usuario ve o motivo escrito na tela.

`a_adena_saltou_ordem_de_grandeza(anterior, atual, *, fator_de_salto)`: recusa quando um dos dois
valores excede o outro pelo fator, **nas duas direcoes**. A direcao nao e criterio: gastar adena e
normal e a Fase 2 ja trata renda negativa; o que nao e normal e a razao entre as duas leituras
saltar uma ordem de grandeza, porque isso e um digito ganho ou perdido e nao uma compra.
Aritmetica **inteira**, por multiplicacao e nunca por divisao, na mesma disciplina do CTX-5.
Quando um dos lados e zero, a regra se abstem, e a docstring diz por que: nao existe ordem de
grandeza em relacao a zero, e o caso do zero pertence a recusa de campo vazio e a de gramatica,
que ja existem. O `fator_de_salto` e **somente-nomeado e sem default**, pela regra escrita em
`mercado_leitura.py:34-44` — um default aqui seria um limiar entrando por omissao, e limiar por
omissao e a definicao de constante magica.

`conferir_o_par(anterior, atual, *, fator_de_salto)`: aplica as tres e devolve a **tupla** de
todas as recusas encontradas, vazia quando o par e coerente. Devolver todas, e nao a primeira, e o
que permite ao chamador dizer "dois campos discordam do par anterior" em vez de esconder o
segundo problema atras do primeiro.

Escrever `tests/test_renda_par.py`. Uma classe por regra, e **cada classe tem o caso que recusa e
o CONTROLE NEGATIVO que nao recusa** — sem o controle, uma regra que recusasse tudo passaria em
todos os casos positivos.

**O controle negativo das tres regras e o mesmo, e ele e REAL.** A medicao de campo capturou um
level up de verdade da Faerlina, com as duas pontas gravadas: em 2026-09-01 ~23h50 o spike leu
`nivel 66, EXP 68,5632%, adena 10.673.628`, e 55 minutos depois a medicao leu `nivel 67, EXP
8,0012%, adena 13.160.684`. Esse par exercita as tres regras de uma vez e **nenhuma delas pode
recusar**: o EXP caiu (mas o nivel mudou, entao a regra do EXP se abstem), o nivel subiu (a regra
do nivel so olha para baixo), e a adena cresceu 23% (muito abaixo de qualquer fator de salto
razoavel). Um controle negativo inventado prova que a regra nao dispara com numeros que o autor
escolheu; este prova que ela nao dispara com o que aconteceu na tela do usuario.

Escrever na docstring da classe: a procedencia das duas pontas (o spike e
`01-MEDICOES-DE-CAMPO.md`, achado M-B), os horarios, e o fato de o par ser real. E escrever o que
ele **nao** e desta fase: a conta `(100 - 68,5632) + 8,0012 = 39,438` pontos percentuais em 55
minutos e o REND-03, e ela pertence a Fase 2. A Fase 1 so precisa que as tres regras se calem
neste par; a Fase 2 herda o par para fazer a conta, e a nota fica escrita aqui para que ela nao
seja reinventada com numeros sinteticos la.

Os outros controles negativos continuam sendo os eventos normais do jogo, montados a mao: gastar
adena dentro do fator, e ganhar adena de loot.

O caso de honra do arquivo e o do item M19, e ele vai com a procedencia na docstring: a leitura
anterior traz a adena real, a leitura seguinte traz o numero que a extracao produz sobre o recorte
cru em que a adena nao aparece — um numero que passou em `numero_valido`, passou em
`inteiro_de_quantidade`, e esta errado por tres ordens de grandeza. O teste afirma que
`conferir_o_par` o recusa. **Este e o unico teste desta fase que enxerga esse modo de falha**, e a
docstring diz isso com todas as letras para que ninguem o remova por parecer redundante.

Escrever o portao da distincao, que e o criterio 3 do roadmap virado teste: derivar do modulo
todos os nomes que comecam com o prefixo de motivo, afirmar que os **valores** sao distintos dois
a dois, e afirmar que a contagem nao caiu abaixo do numero declarado no proprio teste — para que
fundir dois motivos "porque parecem a mesma coisa" derrube alguma coisa. Ao lado, o **controle
positivo**: montar a mesma conferencia sobre um dicionario com dois valores repetidos e afirmar
que ela acusa, nomeando o valor duplicado. Sem o controle, o portao passa de maos dadas com uma
implementacao que nao confere nada.

Escrever, por fim, o portao de ausencia de memoria: uma conferencia sobre a arvore de sintaxe de
`renda_leitura.py` afirmando que o modulo nao declara escopo global em funcao nenhuma e que
nenhuma atribuicao de nivel de modulo tem valor de tipo mutavel. E a forma executavel de "a Fase 1
e sem estado", no precedente de portao por arvore de sintaxe que o `01-03` estabeleceu — e ela
tambem ganha controle positivo, sobre um fonte montado a mao que declara um acumulador de modulo.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_par.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_renda_par.py -q` termina em 0 no Python global, **sem skip** — este arquivo nao depende de OCR nem de pixel; ele e aritmetica sobre pares.
    - Cada uma das tres regras tem o caso que recusa E o controle negativo que nao recusa, e os controles sao eventos normais do jogo: subir de nivel, gastar adena, ganhar adena.
    - Existe o caso do PAR DE CAMPO — Faerlina 66/68,5632%/10.673.628 -> 67/8,0012%/13.160.684 — afirmando que `conferir_o_par` devolve tupla VAZIA, com a procedencia das duas pontas e os horarios na docstring, e com a nota de que a conta do REND-03 sobre este mesmo par pertence a Fase 2.
    - Existe o caso do item M19, com a gravacao de origem e o numero na docstring, afirmando que o par recusa um numero que passou nas duas funcoes de gramatica.
    - Existe o portao que afirma que todos os motivos do modulo sao distintos dois a dois, derivando-os do modulo, com CONTROLE POSITIVO que acusa um valor duplicado pelo nome.
    - Existe um teste que afirma que chamar a regra da adena sem o fator levanta `TypeError`.
    - Existe o portao de arvore de sintaxe que afirma a ausencia de memoria em `renda_leitura.py`, com controle positivo sobre um fonte montado a mao.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_nenhum_numero_do_spike_no_fonte.py -q` continua em 0 — nenhum limiar novo entrou como literal no fonte.
  </acceptance_criteria>
  <done>Os tres modos de leitura duvidosa que faltavam sao recusas nomeadas, cada uma com controle negativo, e a distincao entre todas as recusas da fase esta presa por um portao com controle positivo — que e o criterio mais importante da fase, virado teste.</done>
</task>

<task type="auto">
  <name>Tarefa 3: O comando imprime os tres campos — o criterio 1 num olhar</name>
  <files>l2scanner/renda_modo.py, tests/test_renda_completa.py</files>
  <read_first>
    - `l2scanner/renda_modo.py` inteiro, como o `01-01` o deixou — o `argparse`, as duas fontes de pixel (`--imagem` e `--janela`), a queda de alvo para `cal.janela`, o `--calibracao` alternativo, e a impressao do EXP. Esta tarefa **estende** esse arquivo; nao o reescreve.
    - `l2scanner/mercado_modo.py:400-470` — o molde de um modulo de modo: ele abre janela e le argumento, o modulo puro nao.
    - `l2scanner/console.py:18-26` e `:97-140` — `LARGURA`, os codigos de cor com `_habilitar_cor`, `moldurar` e `destacar`. **E o idioma de saida desta casa, e `rich` nao esta na arvore** — a pesquisa mediu isso (c5) e este plano nao o introduz.
    - `l2scanner/cliente.py:111` — `nome_do_personagem`, que vai no cabecalho da saida para o usuario saber de qual instancia e a leitura (CTX-8).
    - `l2scanner/calibrar.py:1188` — o `carregar` dentro de `try/except` que imprime sem traceback e devolve codigo diferente de 0. E o molde do desfecho operacional desta tarefa.
    - `.planning/workstreams/renda/ROADMAP.md`, criterio de sucesso 1 da Fase 1 — a frase "conferivel olhando para o monitor e para o terminal ao mesmo tempo". **E ela que define o formato da saida**, e nao o gosto de quem escreve.
    - `01-01-PLAN.md` Tarefa 1, o paragrafo da montagem — a promessa escrita de que a regiao preta do nivel vira o caso de campo vazio deste plano.
  </read_first>
  <action>
Estender `l2scanner/renda_modo.py` para imprimir **os tres campos** em vez de um. A leitura vem de
`ler_os_tres_campos`, e a impressao percorre os tres na ordem em que estao na tela do jogo, uma
linha cada, com o rotulo alinhado.

**A grafia e o produto desta tarefa, e ela mora aqui e nao no modulo puro.** O puro devolve
inteiro — e o inteiro e o que a Fase 2 vai gravar e o que o `dashboard` vai consumir. Quem escreve
o numero de volta na grafia que o jogo usa na tela e a ferramenta, porque o criterio 1 e uma
comparacao entre o terminal e o monitor e uma comparacao so funciona se as duas grafias forem a
mesma: o EXP com as quatro casas e o sinal de porcentagem, a adena com o separador de milhar, o
nivel como inteiro nu. Um usuario nao deveria ter de traduzir mentalmente um inteiro sem
separadores para conferir uma leitura.

Onde um campo recusou, a linha dele imprime a **recusa**, e nao um espaco vazio: o campo, o motivo
pelo nome, e o texto lido entre os delimitadores. E o criterio 3 aparecendo na tela e nao so no
log — o usuario precisa distinguir "o OCR nao leu nada" de "o OCR leu e a forma nao bate", porque
os dois consertos sao diferentes: o primeiro aponta para o retangulo ou o piso de brilho, o
segundo aponta so para o piso. E "este personagem nao esta calibrado" e um terceiro, com conserto
proprio: rodar o `calibrar-renda.bat` para ele.

**Todo campo sustentado por UMA escala so sai marcado**, com uma marca curta na propria linha e
uma legenda de uma linha no rodape dizendo o que ela quer dizer. E a mesma disciplina de `n` e
recencia que o `--mercado` cola em todo numero que vai a tela, e ela existe aqui pela razao
medida: nos campos em que uma escala abstem sempre — o nivel e a adena de uma das instancias —, o
cruzamento nao esta pegando substituicao de digito, e o unico verificador que resta e o olho de
quem compara o terminal com o monitor. Marcar e barato; nao marcar e afirmar uma guarda que nao
esta la.

Definir os desfechos por **codigo de saida**, e escrever a tabela num comentario no topo da
funcao, porque um codigo de saida sem tabela e um numero que so o autor entende:

| desfecho | codigo | quando |
|---|---|---|
| os tres campos lidos | 0 | a leitura fechou; e o unico caso em que existe uma `LeituraDaRenda` |
| ao menos uma recusa nomeada | proprio, diferente de 0 e de 1 | a ferramenta funcionou e o pixel nao deu o campo; a saida diz qual e por que |
| falha operacional | 1 | sem frame, calibracao invalida, regiao ausente ou OCR indisponivel — a ferramenta nao chegou a ler |

A separacao dos dois ultimos e o que impede o desastre de suporte mais comum desta familia de
ferramenta: "recusou" e "quebrou" precisam ser distinguiveis de fora, porque quem vai olhar o
codigo de saida e a Fase 3 e nao um humano. Um campo recusado **nao** e um erro do programa.

Nenhum numero entra neste arquivo: as tres regioes e os tres pisos vem da entrada **daquele
personagem** no `calibration.json` que o `01-03` escreve, e o comando falha alto quando ela esta
ausente, em vez de assumir um valor ou de usar a de outro personagem. Uma feature nao calibrada e
uma feature desligada, e dizer isso e melhor que adivinhar — e usar a calibracao do vizinho e pior
que adivinhar, porque produz um numero.

O nome do personagem sai do titulo da janela no caminho `--janela` e de `--personagem` no caminho
`--imagem`, obrigatorio nos dois, como o `01-01` fixou. Ele vai no cabecalho da saida junto da
fonte de pixel: com duas instancias abertas, uma leitura anonima e uma leitura que nao descreve
ninguem.

Acrescentar a `tests/test_renda_completa.py` uma classe para o comando, com os dois casos de
montagem: contra a montagem do `01-01`, cuja regiao do nivel e preta, a saida traz o EXP e a
adena como numero e o nivel como recusa de campo vazio, e o codigo e o de recusa; contra a
montagem completa desta rodada, os tres saem como numero e o codigo e 0. Os dois casos rodam **sem
o jogo aberto**, que e o que torna o criterio 1 verificavel em qualquer clone com OCR.
  </action>
  <verify>
    <automated>PYTHONPATH=. .venv/Scripts/python.exe -m l2scanner.renda_modo --imagem tests/fixtures/renda/montagem_completa.png --personagem Faerlina --calibracao tests/fixtures/renda/calibracao_de_fixture.json</automated>
    <human-check>Com o jogo aberto e a janela de status do personagem visivel, rodar o comando com `--janela` **nas duas instancias** e conferir, olhando para o monitor e para o terminal ao mesmo tempo, que os tres numeros impressos sao os tres numeros da tela de cada uma. As duas, e nao uma: o achado M-F diz que a calibracao de uma nao descreve a outra, e conferir so uma deixaria passar exatamente o defeito que este plano existe para impedir. Metade deste criterio ja esta fechada offline contra o frame de campo; o que o olho acrescenta e que a calibracao de HOJE ainda bate com a tela de hoje.</human-check>
  </verify>
  <acceptance_criteria>
    - O comando acima sai com codigo 0 e imprime **tres** linhas de campo: o nivel como inteiro, o EXP com quatro digitos depois do separador e o sinal de porcentagem, e a adena com separador de milhar.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m l2scanner.renda_modo --imagem tests/fixtures/renda/montagem_da_janela.png --personagem Faerlina --calibracao tests/fixtures/renda/calibracao_de_fixture.json` sai com o codigo de recusa — diferente de 0 e diferente de 1 — e imprime o nivel como recusa de campo vazio, com o EXP e a adena ainda impressos como numero.
    - Rodar contra o frame de campo da Faerlina imprime os TRES numeros da verdade de campo: nivel `67`, EXP `8,0012%` e adena `13.160.684`. E o criterio 1 verificavel sem o jogo aberto, contra a tela real do usuario, com o valor esperado escrito num documento e nao na cabeca de quem executa.
    - Um campo sustentado por uma escala so sai MARCADO, e existe uma legenda de uma linha explicando a marca; o teste afirma a marca no campo que a medicao de campo diz ser assim.
    - Um personagem sem entrada na calibracao sai com o codigo de recusa e uma mensagem que nomeia o conserto (rodar o calibrador para aquele personagem) — e nao com codigo 0, nem com a leitura do vizinho.
    - Um caminho de calibracao inexistente sai com codigo 1 e uma mensagem sem traceback: quebrou e recusou sao desfechos diferentes.
    - A saida traz o nome do personagem E a fonte de pixel no cabecalho, para que a leitura nao seja anonima quando o usuario roda duas instancias.
    - `grep -v '^\s*#' l2scanner/renda_modo.py | grep -c "10_000\|685632\|1368\|1230"` retorna 0 — a grafia e formatacao, e nao uma segunda escala escrita a mao.
    - `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_completa.py -q -rs` termina em 0 com 0 skipped, incluindo os dois casos de montagem do comando.
  </acceptance_criteria>
  <done>Uma rodada do comando imprime os tres campos na grafia do jogo, com cada recusa nomeada na propria linha, e o codigo de saida distingue "leu", "recusou" e "quebrou" — o criterio 1 fica conferivel num olhar, e verificavel sem o jogo aberto.</done>
</task>

</tasks>

## Artifacts que este plano produz

- `l2scanner/renda_leitura.py` — `_ultimo_grupo_valido`, `adena_da_barra`, `nivel_da_regiao`,
  `CamposDaRenda`, `LeituraDaRenda`, `ler_os_tres_campos`, `ler_a_renda`, as tres regras de par e
  os tres motivos novos
- `l2scanner/renda_modo.py` — a impressao dos tres campos na grafia do jogo, com a tabela de
  codigos de saida
- `tests/test_renda_completa.py` — os dois leitores novos, os vizinhos de recorte, a composicao
  dos tres campos e os dois casos de montagem do comando
- `tests/test_renda_par.py` — as tres regras de par com controle negativo cada, o caso do M19, o
  portao da distincao com controle positivo e o portao de ausencia de memoria
- `tests/fixtures/renda/` — `montagem_completa.png` (a montagem do `01-01` com o recorte do nivel
  no lugar da regiao preta). Os recortes do nivel dos dois personagens sao resgatados pelo `01-01`
  a partir da gravacao de campo; este plano os consome e nao os produz

Nada de novo em `requirements.txt`. `l2scanner/calibracao.py` **nao** e tocado — o campo
`renda_por_personagem` e do `01-01` e este plano so o le. `l2scanner/calibrar_renda.py` **nao** e tocado — ele e do
`01-03`. Nenhum arquivo do workstream `dashboard`, do `mercado` ou do caminho da party e editado.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixel da barra direita -> adena | Tres numeros dividem o mesmo recorte, e **dois deles passam na gramatica de milhar**; so a posicao os separa |
| pixel do nivel -> inteiro | Dois caracteres numa regiao pequena, onde substituicao de digito produz um numero valido e errado |
| par de leituras -> recusa | As tres regras de par sao a ULTIMA defesa da fase contra um numero valido, plausivel e errado; se elas nao existirem, nada mais na Fase 1 o pega |
| leitura -> codigo de saida | Quem vai ler o codigo de saida e a Fase 3, e nao um humano: confundir "recusou" com "quebrou" produz reacao errada automatizada |

## STRIDE Threat Register

Os identificadores continuam a numeracao da fase a partir do `T-01-24`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-24 | Tampering | o L-Coin ou o XM devolvidos como adena | high | mitigate | Ancora no ULTIMO grupo que passa nas duas funcoes de gramatica, com o teste dos vizinhos de recorte que nomeia os dois valores na mensagem de falha (M18, M22, M23) |
| T-01-25 | Tampering | adena valida, plausivel e errada por ordem de grandeza vinda de um recorte que nao contem o campo | critical | mitigate | A regra de salto sobre o par, com o caso do M19 como teste nomeado — e a docstring dizendo que e o unico teste da fase que enxerga esse modo |
| T-01-26 | Tampering | substituicao de digito no nivel passando por `numero_valido` | high | mitigate | Cruzamento das duas escalas tambem no nivel, mais a regra de nivel para tras sobre o par; a limitacao esta documentada na propria docstring de `numero_valido` |
| T-01-27 | Tampering | piso de brilho de uma regiao usado em outra | high | mitigate | Os TRES pisos sao parametros somente-nomeados e sem default, vindos de sub-chaves distintas da entrada do personagem; o M9 supunha e o M-E mediu que as bandas nao tem intersecao — usar o piso errado faz o campo sumir em silencio |
| T-01-40 | Tampering | a adena lida do recorte CRU voltando por "otimizacao" e devolvendo vazio com fundo claro | high | mitigate | A mascara e obrigatoria no leitor da adena, com a refutacao do LEIT-03 escrita no fonte e as duas condicoes de fundo ao lado; e o CONTROLE DA MASCARA no teste, que afirma `''` sem mascara e o inteiro com |
| T-01-41 | Spoofing | a leitura de um personagem usando os retangulos do outro | critical | mitigate | Nome obrigatorio nos dois caminhos, acessor unico que devolve a entrada dele ou nada, recusa nomeada antes de qualquer OCR, e o teste que le o frame da Faerlina com o nome da Yazalaque e afirma que o resultado NAO e 67 |
| T-01-42 | Denial of Service | o cruzamento por igualdade recusando o nivel e a adena para sempre | critical | mitigate | `_cruzar_as_escalas` do `01-01`, por abstencao, usado nos tres campos e nao reimplementado; teste por campo com o caso de uma escala valida e uma abstendo (M-D) |
| T-01-43 | Repudiation | um campo sustentado por uma escala so exibido como se tivesse as duas guardas | medium | mitigate | O numero de escalas viaja com o valor e o comando marca a linha, com legenda; a regra de par e a defesa que sobra e ela esta nomeada no fonte |
| T-01-28 | Spoofing | a Fase 1 ganhando memoria viva por alguem "ligar" as regras de par no leitor | medium | mitigate | Portao de arvore de sintaxe afirmando ausencia de estado de modulo em `renda_leitura.py`, com controle positivo, e a docstring de cada regra nomeando quem a chama |
| T-01-29 | Repudiation | campo recusado em silencio, com o comando saindo 0 | high | mitigate | Tabela de codigos de saida com tres desfechos, a recusa impressa na propria linha do campo com motivo e texto lido, e o caso de montagem que afirma o codigo de recusa |
| T-01-30 | Tampering | limiar de salto entrando como literal no fonte por conveniencia | medium | mitigate | `fator_de_salto` somente-nomeado e sem default, teste que afirma `TypeError` na omissao, e o portao de arvore de sintaxe do `01-03` rodado na verificacao final |
| T-01-31 | Elevation of Privilege | biblioteca de sintese de input entrar na arvore | high | mitigate | Nenhuma dependencia nova; a captura continua sendo a `JanelaSource` somente-leitura do `01-01`; `tests/test_firewall_escopo.py` continua varrendo |
| T-01-32 | Information Disclosure | o nome do personagem e os numeros da tela indo para o terminal e para o log | low | accept | Sao dados do proprio jogo do usuario, o log e local e rotativo, e sem o nome no cabecalho a leitura fica anonima com duas instancias abertas |
</threat_model>

<verification>
- `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_completa.py tests/test_renda_par.py -q -rs` termina em 0 com 0 skipped.
- `PYTHONPATH=. .venv/Scripts/python.exe -m pytest tests/test_renda_tracer.py tests/test_calibracao_renda.py tests/test_calibrar_renda.py tests/test_calibrar_renda_nao_apaga_nada.py tests/test_nenhum_numero_do_spike_no_fonte.py -q` continua em 0 — as tres ondas anteriores seguem de pe.
- `python -m pytest tests/ -q` termina em 0 (os testes de OCR podem pular no Python global; nenhum outro pode).
- O comando contra `montagem_completa.png` sai 0 com os tres campos; contra `montagem_da_janela.png` sai com o codigo de recusa e o nivel nomeado como campo vazio; contra o frame de campo da Faerlina sai 0 com `67`, `8,0012%` e `13.160.684` — os tres numeros da verdade de campo.
- O mesmo frame com `--personagem Yazalaque` NAO devolve 67: a leitura e por personagem.
- `git diff --stat requirements.txt l2scanner/calibracao.py l2scanner/calibrar_renda.py` sai vazio nesta rodada.
- O criterio 1 conferido pelo olho, com o jogo aberto e a janela de status visivel, **nas duas instancias**, e o resultado registrado no SUMMARY com os tres numeros que estavam em cada tela.
</verification>

<success_criteria>
- Um comando imprime os tres campos na grafia do jogo, para um personagem nomeado, e o usuario
  fecha o criterio 1 comparando o terminal com o monitor — e ja fecha metade dele **sem o jogo
  aberto**, contra o frame de campo cuja verdade esta escrita (67, 8,0012%, 13.160.684).
- A adena so e lida sob mascara, com a premissa do LEIT-03 citada e refutada no fonte, e o
  controle sem mascara no teste provando que a refutacao e um fato e nao uma opiniao.
- A leitura e por personagem, com os retangulos e os tres pisos dele, e nunca cai no vizinho.
- Os tres campos cruzam as duas escalas pela regra da abstencao, e um campo sustentado por uma
  escala so sai marcado na tela.
- A adena sai do ultimo grupo valido da faixa, e o vizinho de recorte que passaria por ela esta
  preso por um teste que o nomeia.
- O nivel sai por OCR mascarado, com a refutacao dos moldes escrita no fonte com a medicao ao lado.
- Os tres campos cruzam as duas escalas, porque substituicao e o que a gramatica nao pega.
- EXP para tras com nivel parado, nivel descendo e adena saltando ordem de grandeza sao tres
  recusas nomeadas, cada uma com controle negativo, e todas distinguiveis entre si e das do
  `01-01` — provado por um portao com controle positivo.
- As tres regras sao puras sobre um par por parametro, e a Fase 1 continua sem memoria.
- "Leu", "recusou" e "quebrou" sao tres codigos de saida diferentes.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-04-SUMMARY.md` when done
</output>
