---
phase: 02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/calibracao.py
  - l2scanner/renda_ponte.py
  - tools/semear_a_ponte_de_xp.py
  - tests/test_calibracao_renda.py
  - tests/test_renda_ponte.py
autonomous: true
requirements: [REND-08, REND-09]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`renda_ponte_de_xp` existe no `calibration.json` como terceira chave da renda, por personagem E POR NIVEL, e sobrevive a ida e volta pelo `salvar` e pelo `carregar` com a `VERSAO_DO_ESQUEMA` intacta em 2 (REND-08, CTX-8)."
    - "A constante carrega a PROCEDENCIA e nao so o valor: quantas linhas de chat, quantos abates, que janela, e quando foi medida. Uma constante medida em seis minutos e uma medida em tres horas nao valem o mesmo, e quem le tem de conseguir saber qual e qual (CTX-8)."
    - "Sem constante para o NIVEL ATUAL o XP absoluto e DECLARADO INDISPONIVEL, com motivo nomeado — nunca convertido com a constante do nivel anterior, que e a queda silenciosa que produziria um numero plausivel e errado (CTX-8)."
    - "Os DOIS caminhos sao exercitados contra dado real: com a constante semeada (Faerlina, nivel 67) e sem ela (qualquer outro nivel, qualquer outro personagem). Sem a semeadura, o caminho do XP disponivel terminaria a fase sem nenhum dado real para exercita-lo (C-8)."
    - "A constante semeada e `388.700 XP por ponto percentual`, medida em 2026-09-02 a 55 Hz com CENSO COMPLETO: a soma dos 188 degraus e 1903 unidades e a barra andou exatamente 1903, com ZERO leituras negativas em 8.555 amostras; 114 eventos de abate com media de 9,956 unidades; 387 XP por abate sobre 240 linhas de chat."
    - "A semeadura NUNCA escreve o `calibration.json` a mao: ela carrega, muda so a chave da renda e reemite pelo `salvar` que ja existe, com `.tmp` ao lado e troca atomica — escrever direto deixaria JSON truncado e mataria o scanner de party inteiro."
    - "O bonus do chat JA ESTA no total: `363 XP (bonus: 299)` e 363 e nunca 662, e a prova e o multiplicador — dez das onze linhas medidas caem entre 562% e 567%, e a propria barra exibe 562% ao lado do EXP (REND-09)."
    - "O portao estrutural do `calibration.json` continua pegando o esquecimento NAS DUAS DIRECOES, e a entrada em `VALORES_ESPECIAIS` existe — sem ela, o valor generico do despachante por tipo nao passaria no validador de forma e a ida e volta quebraria."
    - "Nenhuma chave alheia e apagada: a preservacao continua sendo por SUBTRACAO de `fields(Calibracao)` e nao por lista escrita a mao, e os testes dos outros tres calibradores continuam verdes."
    - "Esta fase CONSOME a ponte e nao a mede: medir exige ler o chat em cadencia alta, e isso e ferramenta de calibracao — Fase 3 ou tarefa propria."
  artifacts:
    - l2scanner/calibracao.py
    - l2scanner/renda_ponte.py
    - tools/semear_a_ponte_de_xp.py
    - tests/test_renda_ponte.py
  key_links:
    - "Calibracao.renda_ponte_de_xp -> Calibracao.salvar: a chave e emitida como dict CRU, sem reconstrucao, no molde exato de `renda_por_personagem` (`calibracao.py:913`). O portao estrutural derivado de `fields(Calibracao)` e o que impede o campo de sumir do `salvar` calado."
    - "Calibracao.ponte_do_nivel -> renda_ponte: o acessor devolve a entrada DAQUELE nivel DAQUELE personagem ou NADA, no molde de `renda_do_personagem` (`:1082-1104`), onde a proibicao de queda mora em um lugar so."
    - "renda_ponte -> renda_leitura.DECIMOS_DE_MILESIMO_POR_PONTO: a conversao e de decimos de milesimo para XP, e a unidade sai da constante da Fase 1 e nunca de um literal."
    - "tools/semear_a_ponte_de_xp.py -> Calibracao.carregar + Calibracao.salvar: a semeadura passa pelo caminho que ja existe, com `.tmp` e troca atomica. Escrever o arquivo a mao mataria o scanner de party se fosse interrompida."
    - "renda_ponte NAO -> renda_conta: a ponte e conversao de unidade e a conta e taxa. Manter as duas separadas e o que deixa `renda_conta` sem saber que `calibration.json` existe, e o que permite ao `02-02` rodar em paralelo com este plano."
---

<objective>
Fechar REND-08 e REND-09 inteiros, e fecha-los com **dado real dos dois lados**.

A terceira chave da renda entra no `calibration.json` nos cinco lugares que a pesquisa contou —
e o quinto e o `VALORES_ESPECIAIS` do teste estrutural, que e o que vira quarenta minutos de
depuracao se ficar de fora. O modulo de consumo devolve XP absoluto quando ha constante para o
**nivel atual** e **motivo nomeado** quando nao ha, nunca caindo para a constante do nivel
anterior. E a constante medida e **semeada** com a procedencia inteira, porque sem ela a fase
terminaria com a chave vazia e o caminho "XP absoluto disponivel" nunca teria rodado contra
dado de verdade (C-8).

Purpose: este e o requisito que o usuario pediu com as proprias palavras — *"XP e isso que
aparece no chat e sobe a porcentagem, mas preciso do numero do chat"* — e ele tem duas metades
que falham por motivos opostos. Sem a chave, nao ha o que converter. Sem a recusa nomeada, a
conversao acontece com a constante errada e produz um numero plausivel e errado, que e o unico
defeito que este workstream trata como inaceitavel.

Output: `renda_ponte_de_xp` em `l2scanner/calibracao.py`, `l2scanner/renda_ponte.py`,
`tools/semear_a_ponte_de_xp.py`, e os testes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/STATE.md
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-CONTEXT.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-RESEARCH.md
</context>

## As decisoes que este plano nao reabre

As dez decisoes travadas (**CTX-1** a **CTX-10**) e as oito contradicoes (**C-1** a **C-8**) estao
tabeladas no `02-01-PLAN.md`. Este plano e o dono de **CTX-8** e da contradicao **C-8**.

**Este plano nao depende de nenhum outro e roda na onda 1**, em paralelo com o `02-01`: ele nao
toca `renda_conta.py` nem `renda_registro.py`, e nenhum arquivo dele e tocado por outro plano
desta fase.

## Os cinco lugares da terceira chave, e o quinto e o que some do radar

A pesquisa contou os cinco, e a Fase 1 ja fez este mesmo trabalho **duas vezes** — para
`renda_por_personagem` e para `renda_moldes_da_barra`. Nao e surpresa; e uma lista que precisa
estar escrita, senao vira depuracao.

| # | Arquivo | Onde | O que fazer |
|---|---|---|---|
| 1 | `l2scanner/calibracao.py` | ao lado de `:749` | `renda_ponte_de_xp: dict \| None = None`, com o bloco de comentario no molde das irmas |
| 2 | `l2scanner/calibracao.py` | `salvar`, junto de `:913-914` | a chave emitida como **dict CRU**, sem reconstrucao |
| 3 | `l2scanner/calibracao.py` | `carregar`, junto de `:1077-1078` | leitura opcional, campo em `None` quando ausente |
| 4 | `l2scanner/calibracao.py` | `_conferir_as_chaves_da_renda`, junto de `:1627` | o validador de forma, no molde de `_conferir_a_renda_por_personagem` (`:1382-1440`) |
| **5** | **`tests/test_calibracao_renda.py`** | **`VALORES_ESPECIAIS`, `:134-151`** | **uma entrada com uma ponte de exemplo** |

**Por que o quinto e obrigatorio, e a mecanica exata:** `_valor_por_tipo` despacha **por tipo**
(`tests/test_calibracao_renda.py:154-181`), e `dict | None` cai no ramo generico que devolve um
dicionario de uma chave qualquer. Como este plano escreve um validador **de forma** — e a CTX-8
**exige** que a constante carregue procedencia, o que e forma —, o valor generico **nao passa**, e
`test_TODO_CAMPO_SOBREVIVE_AO_SALVAR_MAIS_CARREGAR` (`:284-288`) quebra. A correcao e uma entrada
em `VALORES_ESPECIAIS`, exatamente como as duas irmas ja tem (`:137-138`).

**`VERSAO_DO_ESQUEMA` fica em 2** (`calibracao.py:23`): as chaves da renda sao opcionais e a Fase 1
ja provou o caminho duas vezes. Ninguem e obrigado a recalibrar.

**E a preservacao contra os outros calibradores e de graca, e ja esta provada:**
`fundir_com_a_calibracao_em_disco` preserva por **subtracao** de `fields(Calibracao)` e nao por
lista escrita a mao — *"ela sobrevive por SUBTRACAO de `fields(Calibracao)`, e nao por alguem ter
lembrado de inscreve-la numa lista"* (`tests/test_calibrar_renda_nao_apaga_nada.py:456-458`).

## A forma da entrada, e por que ela e por nivel dentro de personagem

    renda_ponte_de_xp = {
      "<Personagem>": {
        "<nivel>": {
          "xp_por_ponto": <int>,
          "medido_em": "<AAAA-MM-DD>",
          "n_abates": <int>,
          "n_linhas_de_chat": <int>,
          "janela_em_segundos": <int>,
          "observacao": "<texto livre com a procedencia>"
        }
      }
    }

**Por nivel** porque o custo do nivel muda, e a constante do nivel 66 aplicada ao 67 produz um
numero com a mesma cara e errado. **Por personagem** porque o multiplicador de XP e do personagem
— a barra da Faerlina exibe **562%**.

**A procedencia viaja junto porque ela e o que distingue duas constantes que parecem iguais.** Uma
medida em seis minutos e uma medida em tres horas nao valem o mesmo, e quem le tem de conseguir
saber qual e qual. Isso e a mesma disciplina de `n` e recencia que o `--mercado` aplica a todo
numero que vai a tela, aplicada a um numero que vai ao `calibration.json`.

**A chave do nivel e TEXTO no JSON**, porque JSON nao tem chave inteira. O acessor converte, e ha
teste afirmando que a entrada gravada com o inteiro `67` e encontrada de volta — este e o modo de
falha classico desta forma e ele nao levanta: ele so devolve nada, e o painel diz "indisponivel"
para sempre sem ninguem entender por que.

## A constante semeada, com a procedencia inteira (C-8)

Ela **nao e um numero solto**: e uma medicao com censo completo, e o censo e o que a torna
confiavel. Tudo abaixo esta versionado no `REQUIREMENTS.md`, REND-08.

| Grandeza | Valor | Como foi obtida |
|---|---|---|
| Personagem | **Faerlina** | a instancia medida |
| Nivel | **67** | o nivel na hora da medicao |
| **XP por ponto percentual** | **388.700** | `387 / 0,0009956` |
| Medido em | **2026-09-02** | — |
| Cadencia | **55 Hz** (0,018 s) | 8.555 leituras de EXP em 2,5 min |
| **Censo COMPLETO** | soma dos **188 degraus** = **1903 unidades** = o avanco da propria barra | nenhum evento escapou: isto e censo, nao amostra |
| Leituras negativas | **ZERO em 8.555** | o leitor de EXP da Fase 1 nao oscila — e isto valida a Fase 1 de quebra |
| Degraus de abate | **114 eventos**, media **9,956 unidades** | o aglomerado do abate isolado pelo censo |
| XP por abate | **387** | media de **240 linhas** de chat |
| XP total do nivel | **38,87 milhoes** | a constante vezes cem |

**A primeira conta se sustenta:** a medicao anterior, a 0,8 s de amostragem, dava **383.124**, e o
refinamento a 55 Hz deu **388.700** — **1,5% acima**. Duas medicoes independentes com 1,5% de
diferenca e o que da direito de chamar isto de constante.

**O aglomerado que ninguem explica vai registrado, e nao escondido:** 50 eventos de ~3 unidades, a
20 por minuto, somando 7,9% do XP da janela. Nao sao erro de leitura (nao ha degrau negativo
nenhum) e nao sao abate (o chat mostra XP por abate entre 363 e 439, um intervalo de 1,2x e nao de
3x). **Isso nao afeta a constante** — ela converte pontos percentuais em XP do nivel, seja qual
for a origem do ganho —, e o texto vai na `observacao` da entrada semeada.

## REND-09: o bonus ja esta no total, e a prova e o multiplicador

O usuario perguntou se `363 XP (bonus: 299)` e 363 ou 662, e disse nao saber. **E 363.** O valor
entre parenteses e a PARTE do total que veio de bonus, e nao um extra a somar.

A prova, e ela e por medicao e nao por leitura de documentacao: para dez das onze linhas
distintas capturadas, `total / (total - bonus)` cai entre **562% e 567%** — `363/64 = 5,67`,
`388/69 = 5,62`, `405/72 = 5,62`, `439/78 = 5,63` — e a **propria barra exibe 562%** ao lado do
EXP, que e o multiplicador de XP do personagem. Se o total fosse `363+299`, o multiplicador seria
10,3x e nada na tela corresponderia a ele.

**Nada nesta fase parseia o chat**, e a ferramenta que mede a ponte esta deferida. Mas a convencao
e o que a ferramenta futura vai consumir, e ela fecha aqui como **funcao pura testada**: uma
funcao que recebe total e bonus e devolve o total, com o par medido preso por teste e com a banda
do multiplicador afirmada sobre as quatro linhas versionadas. Sem isso, REND-09 fecharia como
prosa, e a proxima pessoa a escrever o parser somaria o parenteses.

## Numeros medidos que este plano usa como fixtura

| Numero | Valor | Onde esta versionado |
|---|---|---|
| Constante da ponte, 55 Hz | **388.700** XP por ponto percentual | `REQUIREMENTS.md`, REND-08 |
| Constante da ponte, 0,8 s | **383.124** | `REQUIREMENTS.md`, REND-08 |
| Censo completo | 188 degraus somando 1903 = o avanco da barra; zero negativas em 8.555 | `REQUIREMENTS.md`, REND-08 |
| Degrau de abate | 114 eventos, media 9,956 | `REQUIREMENTS.md`, REND-08 |
| XP por abate | 387, sobre 240 linhas | `REQUIREMENTS.md`, REND-08 |
| Linhas do bonus | `363/64`, `388/69`, `405/72`, `439/78` -> 562-567% | `REQUIREMENTS.md`, REND-09 |
| Multiplicador exibido pela barra | 562% | `REQUIREMENTS.md`, REND-08 e REND-09 |
| EXP de campo da Faerlina | `80_012` decimos (= 8,0012%) | `tests/test_renda_par.py:100` |
| Linha de base da suite | **5355 passed**, com `tests/test_agenda.py` desselecionado | medido no planejamento do `02-01` |

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: A terceira chave da renda nos cinco lugares — e o quinto e o que some do radar</name>
  <files>l2scanner/calibracao.py, tests/test_calibracao_renda.py</files>
  <read_first>
    - `l2scanner/calibracao.py:660-760` — os campos `renda_por_personagem` e `renda_moldes_da_barra` com os blocos de comentario inteiros: o molde de campo opcional com a `VERSAO_DO_ESQUEMA` intacta em 2, e a razao de a chave dos moldes nao morar dentro da do personagem.
    - `l2scanner/calibracao.py:900-945` — `salvar`: a lista de chaves, o idioma do dict CRU sem reconstrucao para as chaves da renda, e a escrita atomica por `.tmp` e troca.
    - `l2scanner/calibracao.py:945-1080` — `carregar`: o portao de versao, a chamada de `_conferir_as_chaves_da_renda` antes de montar, e o `.get` para todo campo opcional com a razao escrita.
    - `l2scanner/calibracao.py:1082-1104` — `renda_do_personagem`: o acessor que devolve a entrada daquele personagem ou NADA, com a proibicao de queda escrita e a medicao que a sustenta (M-F). E o molde exato do acessor deste plano.
    - `l2scanner/calibracao.py:1382-1440` — `_conferir_a_renda_por_personagem`, o molde do validador que percorre e confere forma; e `:1580-1612`, o validador de numero em faixa com a mensagem carregando o conserto.
    - `l2scanner/calibracao.py:1615-1630` — `_conferir_as_chaves_da_renda`, onde a chamada nova entra.
    - `tests/test_calibracao_renda.py:100-200` — `VALORES_ESPECIAIS`, o despachante `_valor_por_tipo` e o `raise` no fim que derruba o teste com o nome do campo desconhecido.
    - `tests/test_calibracao_renda.py:200-300` — `diferenca_entre_campos_e_chaves`, o portao das duas direcoes, os dois controles positivos (campo apagado do `salvar`, chave orfa injetada) e o terceiro guarda que impede o teste de ficar vacuo.
    - `tests/test_calibrar_renda_nao_apaga_nada.py:440-540` — a preservacao por subtracao de `fields(Calibracao)` e `TestOControleDoCampoNovo`, que sintetiza uma dataclass com um campo que ainda nao existe.
  </read_first>
  <behavior>
    - Um `calibration.json` **sem** a chave carrega inteiro, com o campo em `None` e a versao intacta em 2.
    - Um `calibration.json` **com** a chave carrega, e o dict volta CRU — sem reconstrucao e sem normalizacao.
    - Um campo do dataclass esquecido no `salvar` derruba o portao estrutural pelo nome, nas duas direcoes.
    - A ida e volta preserva a chave com dois personagens dentro e com dois niveis dentro de um deles.
    - Uma entrada com uma sub-chave que o codigo nao conhece **sobrevive** a ida e volta — o dict e cru.
    - `xp_por_ponto` ausente, zero, negativo, booleano ou texto e recusado no arranque, com o nome do personagem e do nivel na mensagem.
    - `n_abates`, `n_linhas_de_chat` e `janela_em_segundos` ausentes sao recusados — a procedencia e obrigatoria, e uma constante sem ela nao e uma constante util.
    - Uma chave de nivel que nao e numero e recusada, com a mensagem dizendo o que se esperava.
    - O acessor devolve a entrada daquele personagem **e daquele nivel**, ou nada — nunca a de outro nivel, nunca a de outro personagem.
    - O acessor encontra a entrada quando o nivel e pedido como inteiro e a chave no JSON e texto.
    - Os outros tres calibradores continuam sem apagar a chave nova.
  </behavior>
  <action>
Acrescentar `renda_ponte_de_xp` ao dataclass, ao `salvar`, ao `carregar` e ao validador, nos
quatro lugares tabelados acima, no molde literal das duas irmas — inclusive o bloco de comentario
que diz **por que a `VERSAO_DO_ESQUEMA` fica em 2**: a chave e opcional e ninguem e obrigado a
recalibrar, e a Fase 1 ja provou este caminho duas vezes.

O bloco de comentario do campo carrega tres coisas que a proxima pessoa vai precisar: a **forma**
da entrada (personagem -> nivel -> valor mais procedencia); **por que e por nivel dentro de
personagem** (o custo do nivel muda, e o multiplicador e do personagem — a barra da Faerlina exibe
562%); e a armadilha da **chave de nivel como texto**, que e o modo de falha desta forma e que
**nao levanta** — ele so devolve nada, e o painel diz "indisponivel" para sempre sem ninguem
entender por que.

O validador confere **forma e nunca conteudo plausivel**: `xp_por_ponto` inteiro positivo com
`bool` recusado explicitamente antes do teste de inteiro — `True` e um `int` de valor 1 e uma
constante de 1 XP por ponto e um numero absurdo que passaria calado —, e os tres campos de
procedencia obrigatorios. **A procedencia e obrigatoria e nao opcional**, e a razao vai escrita:
a CTX-8 exige que a constante carregue de onde veio, e uma constante sem procedencia e um numero
que ninguem consegue auditar depois. A mensagem de erro carrega o nome do personagem, o nivel e o
conserto, no molde das mensagens de validacao que ja existem.

O acessor novo segue o molde de `renda_do_personagem`, e a proibicao de queda mora **num lugar
so**: ele devolve a entrada daquele nivel daquele personagem, ou nada. **Nunca a do nivel
anterior**, que e o que a CTX-8 proibe explicitamente e o que produziria um numero plausivel e
errado. E ele normaliza a chave do nivel para texto na busca, com o teste que prende isso.

**E o quinto lugar, que e o que some do radar:** a entrada em `VALORES_ESPECIAIS` do
`tests/test_calibracao_renda.py`, com uma funcao de exemplo no molde de
`renda_por_personagem_de_exemplo` e `moldes_da_barra_de_exemplo`. Sem ela, o despachante por tipo
entrega o dicionario generico, o validador de forma o recusa, e o teste de ida e volta quebra com
uma mensagem que nao aponta para o motivo. Este passo esta escrito aqui porque **ja custou
quarenta minutos duas vezes**.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_calibracao_renda.py tests/test_calibrar_renda_nao_apaga_nada.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_calibracao_renda.py tests/test_calibrar_renda_nao_apaga_nada.py -q` termina em 0 com 0 skipped.
    - `python -c "import sys;sys.path.insert(0,'.');import dataclasses;from l2scanner.calibracao import Calibracao,VERSAO_DO_ESQUEMA;print('renda_ponte_de_xp' in {f.name for f in dataclasses.fields(Calibracao)}, VERSAO_DO_ESQUEMA)"` imprime `True 2`.
    - Existe o par ausente/presente: um arquivo sem a chave carrega com o campo em `None` e a versao intacta; um arquivo com a chave carrega e o dict volta cru.
    - Existe um teste com **dois personagens** e **dois niveis** dentro de um deles, afirmando que a ida e volta preserva os quatro valores; e um teste com uma sub-chave desconhecida afirmando que ela **sobreviveu**.
    - Existe um teste por caso invalido — `xp_por_ponto` ausente, zero, negativo, booleano e texto — e cada mensagem de falha nomeia o personagem e o nivel.
    - Existe um teste afirmando que **cada um** dos tres campos de procedencia ausente e recusado, com o nome do campo na mensagem; e o CONTROLE ao lado com a entrada completa afirmando que ela passa. Sem o controle, um validador que recusasse tudo passaria.
    - Existe um teste que grava a entrada com a chave de nivel como **inteiro** e a pede de volta como inteiro, afirmando que ela e encontrada — o modo de falha que nao levanta.
    - Existe um teste afirmando que o acessor **nao** devolve a entrada do nivel anterior quando o nivel atual nao tem entrada, comparando com a entrada que existe no arquivo; e o mesmo para outro personagem.
    - `VALORES_ESPECIAIS` tem a entrada nova: `grep -c "renda_ponte_de_xp" tests/test_calibracao_renda.py` retorna ao menos 2 (a entrada e a funcao de exemplo).
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo de 5355.
    - `git status --porcelain calibration.json` sai vazio — a suite nunca toca o arquivo real do usuario.
  </acceptance_criteria>
  <done>A terceira chave da renda existe, valida forma e procedencia no arranque, sobrevive aos outros tres calibradores, e o acessor nunca cai para o nivel anterior.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: O consumo da ponte — XP absoluto quando ha constante, motivo nomeado quando nao ha</name>
  <files>l2scanner/renda_ponte.py, tests/test_renda_ponte.py</files>
  <read_first>
    - `l2scanner/renda_leitura.py:326` — `DECIMOS_DE_MILESIMO_POR_PONTO`, a unidade do EXP; e `:1300-1319`, a regra de que nenhum ponto flutuante atravessa a fronteira.
    - `l2scanner/mercado_analise.py:414-425` — `Tendencia`: o valor em `None` sempre que nao ha numero a dizer, e `motivo_da_ausencia` dizendo POR QUE. A forma que o resultado da ponte copia.
    - `l2scanner/mercado_analise.py:226-249` — `Evidencia`, e a frase que rege esta tarefa: abaixo do piso a resposta e o que falta, nunca um numero.
    - `l2scanner/calibracao.py:1082-1104` — o acessor irmao, com a proibicao de queda escrita e a razao dela.
    - `02-CONTEXT.md:72-78` — a decisao inteira: sem constante para o nivel atual, o XP absoluto e DECLARADO INDISPONIVEL, e a comparacao com o cambio do `dashboard` (sem taxa informada, mostra XM e diz que o R$ esta indisponivel).
    - `.planning/workstreams/renda/REQUIREMENTS.md`, REND-08 e REND-09 inteiros — a medicao a 55 Hz, o censo completo, o aglomerado nao identificado, e a prova do multiplicador.
    - A secao "A constante semeada" e a secao "REND-09" acima.
  </read_first>
  <behavior>
    - Com a constante do nivel 67 da Faerlina e um ganho de `394_380` decimos, o XP absoluto sai e bate com a conta feita a mao no teste.
    - Com a constante e o EXP de campo `80_012` decimos, o XP acumulado no nivel sai e bate com a conta a mao.
    - **Sem** constante para o nivel atual: o valor e `None` e o motivo e nomeado, mesmo que exista constante para o nivel anterior no mesmo arquivo. E o teste afirma que a constante do nivel anterior **existia** — senao ele passaria por vacuidade.
    - **Sem** constante para o personagem: o valor e `None` e o motivo e nomeado, mesmo que exista constante para outro personagem no mesmo arquivo.
    - Com a chave inteira ausente (`None`): o valor e `None` e o motivo e nomeado, e os tres motivos sao textos distintos.
    - O resultado carrega a **procedencia** da constante que usou, para que quem exibe possa dizer de onde o numero veio.
    - Uma taxa de EXP em pontos percentuais por hora vira uma taxa de XP por hora pela mesma constante, e ela herda o motivo de ausencia quando nao ha ponte.
    - `total_do_xp_da_linha(total=363, bonus=299)` devolve `363`.
    - As quatro linhas medidas produzem multiplicador entre 5,62 e 5,67, e o teste afirma a banda.
    - Nenhum ponto flutuante participa da conversao: o resultado e inteiro ou `Fraction`.
  </behavior>
  <action>
Escrever `l2scanner/renda_ponte.py` — modulo puro, sem disco, sem OpenCV, sem laco, e que nunca
pergunta a hora a maquina. Ele recebe a entrada da ponte por parametro; **nao** importa
`calibracao` e **nao** importa `renda_conta`. A ponte e conversao de unidade e a conta e taxa; se
as duas se misturassem, `renda_conta` passaria a saber que `calibration.json` existe, e os dois
planos deixariam de poder rodar em paralelo.

A docstring de topo carrega a medicao inteira, porque ela e o que da direito de chamar isto de
constante: censo **completo** a 55 Hz — a soma dos **188 degraus** e **1903 unidades** e a barra
andou exatamente **1903**, com **zero** leituras negativas em **8.555** —, **114** eventos de
abate com media **9,956**, **387** XP por abate sobre **240** linhas de chat, e a conta
`387 / 0,0009956 = 388.700 XP por ponto percentual`. E carrega tambem as **duas** medicoes
independentes com 1,5% de diferenca (383.124 a 0,8 s contra 388.700 a 55 Hz), e o **aglomerado de
~3 unidades que ninguem explica** — 50 eventos, 7,9% do XP da janela —, registrado e nao
escondido, com a razao de ele nao afetar a constante.

**O resultado copia a forma de `Tendencia`**: um `XpAbsoluto` congelado com `valor: int | None`,
`motivo_da_ausencia: str | None` e `procedencia: dict | None`. Quando ha constante, o valor sai e
a procedencia viaja junto — quem exibe consegue dizer que o numero veio de uma medicao de 2,5
minutos com censo completo, e nao de um chute. Quando nao ha, o valor e `None` e o motivo diz
**qual** das tres ausencias aconteceu: sem a chave inteira, sem entrada para o personagem, ou sem
entrada para o **nivel atual**. Tres textos distintos, porque o conserto de cada uma e outro.

**A queda para o nivel anterior e o defeito que este modulo existe para impedir** e vai escrita
com essas letras. A constante do 66 aplicada ao 67 produz um numero com a mesma cara e errado, e
o custo do nivel muda justamente entre eles. A disciplina e a mesma do cambio XM->BRL do
`dashboard`: sem taxa informada, mostra a moeda de origem e **diz que a outra esta indisponivel**.

**A conversao e inteira.** O ganho em decimos de milesimo vira XP por
`ganho * xp_por_ponto / DECIMOS_DE_MILESIMO_POR_PONTO`, com a unidade saindo da constante da Fase
1 e nunca de um literal. `Fraction` quando a divisao nao e exata; nunca ponto flutuante, pela
regra ja escrita na docstring do `LeituraDaRenda`.

**E REND-09 fecha aqui, como funcao pura.** `total_do_xp_da_linha(total, bonus)` devolve o
**total** — o parenteses e a PARTE que veio de bonus e nao um extra a somar —, com a prova na
docstring: `total / (total - bonus)` cai entre 562% e 567% em dez das onze linhas medidas, e a
propria barra exibe 562%. Se fosse soma, o multiplicador seria 10,3x e nada na tela corresponderia
a ele. **Nada nesta fase parseia o chat** — a ferramenta que mede a ponte esta deferida —, e por
isso esta funcao existe: ela e a convencao que a ferramenta futura vai consumir, e sem ela REND-09
fecharia como prosa e a proxima pessoa somaria o parenteses.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_ponte.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_ponte.py -q` termina em 0 com 0 skipped.
    - Existe um teste do caminho COM constante que afirma o XP absoluto com a conta feita a mao no proprio teste, usando `394_380` decimos e `388_700` XP por ponto.
    - Existem os **tres** testes de ausencia — chave inteira ausente, personagem ausente, nivel ausente — e eles afirmam **tres textos de motivo diferentes**.
    - O teste do nivel ausente afirma, na mesma funcao, que a constante do nivel **anterior existia** no arquivo e **nao foi usada**. Sem essa afirmacao ele passaria por vacuidade, que e o Pitfall 5.
    - Existe um teste afirmando que o resultado carrega a procedencia quando ha constante, e que ela e `None` quando nao ha.
    - Existe um teste que converte uma taxa de pontos percentuais por hora em XP por hora, e um irmao afirmando que ela herda o motivo de ausencia quando nao ha ponte.
    - Existe o teste de REND-09: `total_do_xp_da_linha(total=363, bonus=299)` devolve `363`, com o CONTROLE ao lado afirmando que **nao** devolve `662`.
    - Existe o teste da banda do multiplicador sobre as quatro linhas versionadas (`363/64`, `388/69`, `405/72`, `439/78`), afirmando que todas caem entre 5,62 e 5,67; e a mensagem de falha cita os 562% que a barra exibe.
    - Um teste afirma que o resultado e `int` ou `Fraction`, nunca ponto flutuante; e a conferencia de fonte e por arvore: `python -c "import ast;a=ast.parse(open('l2scanner/renda_ponte.py',encoding='utf-8').read());print(sorted({x for x in [getattr(n.func,'id',None) or getattr(n.func,'attr',None) for n in ast.walk(a) if isinstance(n,ast.Call)] if x in {'float','round'}}))"` imprime `[]`.
    - Nem `calibracao` nem `renda_conta` sao importados, conferido por arvore — a docstring **precisa** citar os dois para explicar a separacao: `python -c "import ast;a=ast.parse(open('l2scanner/renda_ponte.py',encoding='utf-8').read());m=[];[m.extend([getattr(n,'module',None) or ''] + [al.name for al in n.names]) for n in ast.walk(a) if isinstance(n,(ast.Import,ast.ImportFrom))];print([x for x in m if 'calibracao' in x or 'renda_conta' in x])"` imprime `[]`.
    - A unidade sai da constante da Fase 1 e nunca de um literal, conferido por arvore: `python -c "import ast;a=ast.parse(open('l2scanner/renda_ponte.py',encoding='utf-8').read());print([n.value for n in ast.walk(a) if isinstance(n,ast.Constant) and n.value==10000])"` imprime `[]`.
    - `python -c "import sys;sys.path.insert(0,'.');b=set(sys.modules);import l2scanner.renda_ponte;a=set(sys.modules);print('cv2' in a,'numpy' in a)"` imprime `False False`.
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo do que a Tarefa 1 deixou.
  </acceptance_criteria>
  <done>O XP absoluto sai com a procedencia junto quando ha constante para o nivel atual, e sai como tres ausencias distinguiveis quando nao ha — nunca convertido com a constante do nivel anterior.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Semear a constante medida — a ferramenta que escreve a ponte pelo caminho que ja existe</name>
  <files>tools/semear_a_ponte_de_xp.py, tests/test_renda_ponte.py</files>
  <read_first>
    - `l2scanner/calibracao.py:900-945` — `salvar` e a escrita atomica por `.tmp` e troca: escrever o arquivo direto deixaria JSON truncado e mataria o scanner de party inteiro.
    - `tests/test_calibrar_renda_nao_apaga_nada.py:440-540` — `fundir_com_a_calibracao_em_disco` e a preservacao por subtracao de `fields(Calibracao)`, com os testes dos tres calibradores.
    - `tools/medir_a_renda.py` e `tools/resgatar_fixturas_da_renda.py` — o molde de ferramenta de bancada desta arvore: mora em `tools/`, roda na mao do executor, e **nao e importada por teste nenhum**.
    - `tests/test_medir_a_renda.py:22-26` — a nota de que o `.venv` de producao nao tem `pytest` e o Python global nao tem as bindings de OCR, e o comando que faz a suite rodar.
    - `.planning/workstreams/renda/REQUIREMENTS.md`, REND-08 — a tabela inteira da medicao, que e o texto que a `observacao` da entrada carrega.
    - `02-CONTEXT.md:79-80` — a fronteira: esta fase **consome** a ponte; medir e ferramenta de calibracao, Fase 3 ou tarefa propria.
  </read_first>
  <action>
Escrever `tools/semear_a_ponte_de_xp.py`: uma ferramenta de bancada, de uso pontual, que poe a
constante medida no `calibration.json` do usuario **pelo caminho que ja existe**.

**Ela nunca escreve o arquivo a mao.** Carrega pelo `Calibracao.carregar`, muda **so**
`renda_ponte_de_xp`, e reemite pelo `Calibracao.salvar` — que faz `.tmp` ao lado e troca atomica.
A razao vai escrita no topo: o `calibration.json` e compartilhado por quatro features, e um
arquivo truncado por Ctrl-C no meio da escrita **mata o scanner de alertas de party inteiro** —
que e o produto, e nao a feature.

**Ela nao apaga nada.** A preservacao das outras chaves e de graca e ja esta provada: o
`salvar` emite todos os campos de `fields(Calibracao)`, e o que a ferramenta carregou volta
inteiro. E ela **nao sobrescreve uma entrada que ja exista** para o mesmo personagem e nivel sem
dizer: se ja houver constante ali, ela mostra a que existe e a que entraria, e exige confirmacao
explicita — uma constante recalibrada com mais tempo de medicao e melhor que a semeada, e a
ferramenta nao pode desfazer isso calada.

**O valor e a procedencia inteira**, e a procedencia e o produto tanto quanto o numero:
personagem Faerlina, nivel 67, **388.700** XP por ponto percentual, medido em **2026-09-02**, a
**55 Hz**, com **240** linhas de chat e **114** eventos de abate na janela; e na `observacao`, o
texto que explica por que este numero e confiavel — **censo completo**: a soma dos 188 degraus e
1903 unidades e a barra andou exatamente 1903, com zero leituras negativas em 8.555 amostras; e a
segunda medicao independente, 383.124 a 0,8 s, 1,5% abaixo. E o aglomerado de ~3 unidades que
ninguem explica vai registrado ali tambem, com a razao de ele nao afetar a constante.

**Todos esses numeros estao versionados no `REQUIREMENTS.md`, REND-08**, e a ferramenta cita o
endereco. Isto e a disciplina que a C-6 cobrou: nenhum numero vira dado sem que exista, em um
arquivo do repositorio, o denominador ao lado.

**A ferramenta nao mede nada.** Medir exige ler o chat em cadencia alta, e isso e ferramenta de
calibracao — Fase 3 ou tarefa propria, e o `02-CONTEXT.md` a defere explicitamente. Esta aqui so
poe no arquivo um numero que ja foi medido, e diz de onde ele veio.

Do lado do teste, o que entra em `tests/test_renda_ponte.py` **nao e a ferramenta** — ela nao e
importada por teste nenhum, como as irmas em `tools/`. O que entra e a **entrada que ela produz**,
como constante de teste, atravessando a ida e volta do `calibration.json` num `tmp_path` e depois
o consumo da Tarefa 2. E o que fecha a C-8: os dois caminhos — com constante e sem — exercitados
com o mesmo numero que esta no arquivo do usuario.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_ponte.py tests/test_calibracao_renda.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_ponte.py tests/test_calibracao_renda.py -q` termina em 0 com 0 skipped.
    - `ls tools/semear_a_ponte_de_xp.py` existe, e `grep -rc "semear_a_ponte_de_xp" tests/ | grep -v ":0" | wc -l` retorna 0 — nenhum teste importa a ferramenta.
    - A escrita passa pelo `salvar` que ja existe, conferido por arvore — o texto do topo **precisa** nomear a escrita direta para explicar por que ela foi recusada: `python -c "import ast;a=ast.parse(open('tools/semear_a_ponte_de_xp.py',encoding='utf-8').read());print(sorted({x for x in [getattr(n.func,'attr',None) or getattr(n.func,'id',None) for n in ast.walk(a) if isinstance(n,ast.Call)] if x in {'write_text','dump','write_bytes'}}))"` imprime `[]`.
    - `grep -c "REQUIREMENTS.md" tools/semear_a_ponte_de_xp.py` retorna ao menos 1 — a procedencia cita o endereco versionado.
    - A ferramenta roda num `calibration.json` de teste em `tmp_path`, e um teste afirma que **todas** as outras chaves sobreviveram, comparando o dicionario antes e depois com a chave da ponte removida dos dois lados.
    - Um teste afirma que rodar a ferramenta duas vezes sobre a mesma entrada **nao** sobrescreve sem confirmacao explicita.
    - `tests/test_renda_ponte.py` tem a entrada semeada como constante de teste com os cinco numeros — `388_700`, `2026-09-02`, `240`, `114` e a janela —, e um teste que a atravessa pela ida e volta do `calibration.json` e depois pelo consumo, afirmando o XP absoluto no fim. Este e o teste que fecha a C-8: o caminho "XP disponivel" rodou contra dado real.
    - `git status --porcelain calibration.json` sai vazio depois da suite — a ferramenta so toca o arquivo real quando um humano a executa de proposito.
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo do que a Tarefa 2 deixou.
  </acceptance_criteria>
  <reversibility rating="reversible">A entrada semeada e um dado no `calibration.json`, e recalibrar por cima e o caminho normal previsto pela CTX-8. O que nao e reversivel de graca e a FORMA da entrada, e ela ja foi travada na Tarefa 1.</reversibility>
  <done>A constante medida esta no `calibration.json` com a procedencia inteira, escrita pelo caminho atomico que ja existe, sem apagar chave alheia e sem sobrescrever calada — e o caminho do XP disponivel roda contra ela num teste.</done>
</task>

</tasks>

## Artifacts que este plano produz

- `l2scanner/calibracao.py` — `renda_ponte_de_xp` nos quatro lugares, o validador de forma e
  procedencia, e o acessor por personagem **e por nivel** que nunca cai para o nivel anterior
- `l2scanner/renda_ponte.py` — o consumo puro: XP absoluto com procedencia, tres ausencias
  distinguiveis, e a convencao do bonus de REND-09 como funcao testada
- `tools/semear_a_ponte_de_xp.py` — a semeadura pelo caminho atomico que ja existe
- `tests/test_calibracao_renda.py` — a entrada em `VALORES_ESPECIAIS` e os casos novos
- `tests/test_renda_ponte.py`

Nada de novo em `requirements.txt`. Nenhum arquivo do workstream `dashboard`. **Este plano nao
toca `renda_conta.py`, `renda_registro.py`, `config.py`, `config.toml` nem `.gitignore`** — eles
sao dos outros tres planos, e e por isso que este roda na onda 1 em paralelo com o `02-01`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> processo | Compartilhado por quatro features, editavel a mao, entrada NAO confiavel no carregamento |
| disco -> `calibration.json` | A gravacao pode ser interrompida; um JSON truncado derruba o scanner de party inteiro |
| constante da ponte -> numero exibido | Uma constante do nivel errado produz um XP absoluto plausivel e errado, e ninguem consegue distingui-lo de um certo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Tampering | conversao com a constante do nivel ANTERIOR produzindo XP plausivel e errado | critical | mitigate | O acessor devolve a entrada daquele nivel ou NADA; teste que afirma que a constante do nivel anterior existia e nao foi usada |
| T-02-22 | Tampering | `xp_por_ponto` booleano ou negativo entrando pelo arquivo editado a mao | medium | mitigate | Validador de forma no arranque, com `bool` recusado antes do teste de inteiro e a mensagem carregando o conserto |
| T-02-23 | Repudiation | constante sem procedencia: ninguem consegue saber se ela veio de 6 minutos ou de 3 horas | high | mitigate | Procedencia **obrigatoria** no validador, e ela viaja dentro do resultado do consumo |
| T-02-24 | Denial of Service | `calibration.json` truncado por Ctrl-C matando o scanner de party inteiro | critical | mitigate | A semeadura passa pelo `salvar` com `.tmp` e troca atomica; portao negando escrita direta na ferramenta |
| T-02-25 | Tampering | chave nova apagada por outro calibrador | high | mitigate | Campo declarado no dataclass E no `salvar`, preso pelo portao estrutural derivado de `fields(Calibracao)`, com os controles positivos nas duas direcoes |
| T-02-26 | Tampering | ferramenta sobrescrevendo calada uma constante melhor, medida por mais tempo | medium | mitigate | A semeadura mostra a que existe e a que entraria, e exige confirmacao explicita; teste afirmando que a segunda execucao nao sobrescreve |
| T-02-SC | Tampering | instalacao de pacote de terceiro | n/a | accept | **Nenhum `pip install` nesta fase.** `json`, `dataclasses` e `fractions` sao stdlib; `git diff --stat requirements.txt` sai vazio como criterio |
</threat_model>

<verification>
- `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_ponte.py tests/test_calibracao_renda.py tests/test_calibrar_renda_nao_apaga_nada.py -q` termina em 0 com 0 skipped.
- `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo de 5355.
- `git diff --stat requirements.txt` sai vazio.
- `git status --porcelain calibration.json` sai vazio — a suite nunca toca o arquivo real.
- `git status --porcelain` nao lista `l2scanner/renda_conta.py`, `l2scanner/renda_registro.py`, `l2scanner/config.py`, `config.toml`, `.gitignore` nem nenhum arquivo de `l2scanner/dashboard*`.
</verification>

<success_criteria>
- `renda_ponte_de_xp` existe nos cinco lugares, valida forma **e** procedencia, e a versao do
  esquema continua em 2.
- O acessor nunca cai para o nivel anterior nem para outro personagem, e ha teste que prova que a
  entrada vizinha **existia** e nao foi usada.
- Os dois caminhos rodam contra dado real: com a constante de 388.700 semeada e sem constante.
- As tres ausencias tem tres motivos distinguiveis, porque o conserto de cada uma e outro.
- O resultado carrega a procedencia, para que quem exibe possa dizer de onde o numero veio.
- REND-09 fecha como funcao testada: o bonus ja esta no total, e a banda de 562-567% esta presa
  sobre as quatro linhas versionadas.
- A semeadura passa pelo `salvar` atomico, nao apaga chave alheia e nao sobrescreve calada.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-04-SUMMARY.md` when done
</output>
