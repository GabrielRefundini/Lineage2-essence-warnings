---
phase: 02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - l2scanner/renda_conta.py
  - tests/test_renda_conta.py
autonomous: true
requirements: [REND-01, REND-02, REND-03, REND-04, REND-05, REND-06, REG-02, LEIT-11]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Subir de nivel registra GANHO POSITIVO: `(100 - anterior) + atual` em decimos de milesimo, e um nivel a mais. O par de campo real da Faerlina sai `394_380` decimos, que sao os 39,438 pontos percentuais medidos na tela, e nunca `-605_620` (REND-03, CTX-5)."
    - "A regra do level up so dispara quando o nivel REALMENTE mudou. Um EXP que cai com o nivel RECUSADO e descontinuidade nomeada e o ganho daquele passo NAO e computado — inventar um level up e pior que perder um, e o LEIT-11 mediu por que: 3 das 4 leituras erradas de nivel passaram POR CONCORDANCIA das duas escalas (CTX-5)."
    - "Uma queda no contador de adena e GASTO, contado a parte e fora da taxa de ganho; o ganho bruto e a soma dos deltas POSITIVOS, e as duas grandezas nunca se somam nem se cancelam (REND-04, CTX-6)."
    - "Um carimbo que anda para tras entre duas amostras e DESCONTINUIDADE NOMEADA e nunca um intervalo negativo: o sinal nao e absorvido por valor absoluto nem por piso em zero, e o passo nao entra no denominador (CTX-10, Pitfall 4)."
    - "A primeira amostra depois do arranque nao tem anterior, logo e ANCORA e nao delta — e e dai, e nao de indice em disco, que sai a garantia de que reiniciar nao inventa nem apaga renda (REG-02, C-7)."
    - "A janela e por TEMPO e o denominador e a soma dos intervalos entre amostras consecutivas ACEITAS: um intervalo maior que o limiar calibrado sai do denominador e e contado a parte como lacuna. Vinte minutos de cegueira nao viram taxa baixa (CTX-1, CTX-2)."
    - "Saem DUAS taxas e nao uma: a da janela movel e a da sessao inteira. Elas respondem perguntas diferentes, e apresentar so uma mente por omissao — medido: 466 mil adena/h na janela curta contra ~232 mil/h na media de 8,5 h, e a diferenca e o tempo parado (CTX-4)."
    - "O tempo ate o proximo nivel sai de `(100 - exp%) / (exp% por hora)` sobre `Fraction`, e com taxa de EXP nao positiva a resposta e MOTIVO NOMEADO — nunca divisao por zero escapando, nunca infinito, nunca um numero negativo com cara de previsao (REND-05)."
    - "A `ContagemDaRenda` separa tres fatos que nao se somam: aceitas, recusadas por motivo e lacunas. E o que responde por que a taxa desta hora tem `n=12` se o scanner rodou quarenta minutos."
    - "Todo numero desta fase e inteiro escalado ou `Fraction`; nenhum `float` participa de diferenca ou de acumulo. O `float` sobrevive so no carimbo, que e epoch e cuja subtracao e exata."
    - "Os motivos nomeados desta fase sao DISTINGUIVEIS entre si e do conjunto da Fase 1, e ha portao derivando a lista do modulo em vez de repeti-la a mao."
  artifacts:
    - l2scanner/renda_conta.py
    - tests/test_renda_conta.py
  key_links:
    - "renda_conta -> renda_leitura.conferir_o_par: a composicao, e nunca as tres irmas soltas. O portao invertido do `02-01` prende que este e o unico chamador de producao da arvore."
    - "renda_conta -> renda_leitura.CamposDaRenda: o caminho do NIVEL RECUSADO, que nao tem par para conferir. As quatro regras exigem `LeituraDaRenda` (os tres inteiros), e com o nivel recusado esse objeto nao existe — entao a decisao de nao adivinhar level up mora aqui, sobre `CamposDaRenda`, e nao dentro de `conferir_o_par`."
    - "renda_conta -> renda_leitura.DECIMOS_DE_MILESIMO_POR_PONTO: os 100 pontos percentuais do nivel sao `10_000 * 100`, derivados da constante da Fase 1 e nunca escritos como `1_000_000` a mao — um literal aqui seria duas verdades sobre a mesma unidade."
    - "renda_conta -> mercado_analise.Evidencia: `n` e piso viajando DENTRO do resultado. A forma vem de la; a aritmetica e nova, porque nao existe uma unica taxa por unidade de tempo em 41 mil linhas."
    - "renda_conta NAO -> sessao.Sessao: `Sessao` e o nucleo do laco de alertas de party, com 20 parametros e a arvore inteira de imports (`cv2`, notificador). Reusa-la puxaria OpenCV e WhatsApp para dentro de um modulo que o `02-CONTEXT.md:18-21` fecha sem laco."
---

<objective>
Escrever a aritmetica que a arvore nao tem. Em 41 mil linhas nao existe **nenhuma** taxa de
grandeza por unidade de tempo — este e o unico trecho da fase sem precedente, e por isso ele
copia a **forma** (`Evidencia` dentro do resultado, motivo no lugar do numero ausente, recencia
separada do valor, `Fraction` na divisao) e inventa so a conta.

Sao tres blocos: os deltas negativos que nao sao prejuizo (level up, gasto, relogio para tras, e
o nivel recusado que NAO vira level up adivinhado); a janela movel por tempo com o denominador
sem lacuna e as duas taxas; e o tempo ate o nivel com a `ContagemDaRenda` que explica de onde
veio cada `n`.

Purpose: a fatia do `02-01` provou que o caminho existe. Este plano e o que faz o caminho estar
**certo** nos quatro casos em que a subtracao ingenua mente — e os quatro sao ordinarios, nao de
borda: subir de nivel e a melhor coisa que acontece numa farmada, gastar adena e o que o usuario
faz toda sessao, ficar cego acontece toda noite, e o relogio desta maquina volta ~3h adiantado
do Linux toda vez que ela reinicia.

Output: `l2scanner/renda_conta.py` completo e `tests/test_renda_conta.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/STATE.md
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-CONTEXT.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-RESEARCH.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-01-SUMMARY.md
</context>

## As decisoes que este plano nao reabre

As dez decisoes travadas (**CTX-1** a **CTX-10**) e as oito contradicoes (**C-1** a **C-8**) estao
tabeladas no `02-01-PLAN.md`, na secao "Como estes quatro planos citam decisao". Este plano e o
dono de **CTX-1, CTX-2, CTX-3, CTX-4, CTX-5, CTX-6, CTX-10** e da **C-7**.

E ele herda tres coisas do `02-01` sem discutir: os cinco limiares chegam por parametro
somente-nomeado e sem valor de fabrica; o tempo e `float` epoch na aritmetica; e a sessao e
delimitada por lacuna e nunca por processo.

## Os quatro casos em que a subtracao ingenua mente, e o que cada um custa

| Caso | O que a subtracao ingenua faz | O que este plano faz | Requisito |
|---|---|---|---|
| Level up | `80_012 - 685_632 = -605_620` — a melhor coisa da noite registra o pior numero | `(1_000_000 - 685_632) + 80_012 = 394_380`, e um nivel a mais | REND-03 / CTX-5 |
| Gasto de adena | O gasto entra negativo na taxa de ganho e a taxa cai sem o usuario saber por que | Delta negativo vai para `gasto`, e a taxa de ganho e a soma dos POSITIVOS | REND-04 / CTX-6 |
| Nivel recusado com EXP caindo | Ou vira renda negativa, ou vira level up **inventado** — e o LEIT-11 mediu que o nivel e o campo onde 3 de 4 erros passaram por concordancia | Descontinuidade nomeada; o ganho daquele passo nao e computado | CTX-5 / LEIT-11 |
| Carimbo andando para tras | `intervalo` negativo entra no denominador e a taxa vira negativa ou infinita, **sem levantar** | Descontinuidade nomeada; o passo sai do denominador | CTX-10 / Pitfall 4 |

**Os quatro tem o mesmo formato de proposito**, e isso e a decisao de desenho deste plano: uma
`RecusaDaRenda` com motivo proprio, no molde de `_recusar` (`renda_leitura.py:211`) e das tres
irmas `MOTIVO_*_PARA_TRAS` (`:140-142`). Reusar o tipo em vez de inventar um quarto e o que o
`02-RESEARCH.md` argumenta na escolha aberta (7): `conferir_o_par` **ja** devolve `RecusaDaRenda`
para as tres irmas, e um tipo novo obrigaria quem consome a tratar duas familias que dizem a
mesma coisa. O que muda e **onde os motivos sao declarados**: eles nascem em `renda_conta.py`,
porque nao sao recusa de LEITURA — sao recusa de PAR dentro da conta.

## Por que o relogio desta maquina merece um caso proprio, e o que `relogio.py` NAO resolve

`relogio.py` resolve o caso principal: depois de ancorado, o avanco vem do monotonico, entao um
pulo de 3h no Windows nao move a hora. **Sobram dois buracos, e os dois tem teste EXIGINDO o
comportamento** — nao sao bugs, sao contratos:

1. **Sem ancora, o pulo aparece inteiro.** `agora_epoch` cai no relogio de parede quando nao ha
   ancora (`relogio.py:147-151`), e `tests/test_relogio.py:120-134` afirma que a diferenca vale as
   tres horas. Sem Chatwoot — offline, Cloudflare barrando, `.env` ausente — a Fase 3 entrega a
   esta conta carimbos que saltam.
2. **A reancoragem periodica pode mover a hora PARA TRAS no meio da sessao.** `sincronizar()`
   troca a ancora sem comparar com a anterior (`relogio.py:121-122`) e ha teste afirmando que a
   segunda ancora vale (`tests/test_relogio.py:90-101`). O laco reancora a cada 1800 s. Se o
   Windows estava adiantado e o Chatwoot corrige, **duas amostras consecutivas podem ter carimbo
   decrescente**.

O unico precedente da casa para tempo que anda para tras e raso e **absorve** em vez de nomear:
`primeira_vez` e MIN e `ultima_vez` e MAX (`mercado_catalogo.py:858-878`). **Esta fase e a
primeira a nomear o evento**, e a CTX-10 acerta ao exigir isso — uma taxa por hora e a unica
coisa nesta arvore cujo resultado o salto corrompe em silencio.

## Numeros medidos que este plano usa como fixtura

| Numero | Valor | Onde esta versionado |
|---|---|---|
| Par de campo do level up | `66 / 685_632 / 10_673_628` -> `67 / 80_012 / 13_160_684` | `tests/test_renda_par.py:99-100` |
| Ganho correto, em decimos | `394_380` (= 39,438 pontos percentuais) | `02-RESEARCH.md`, fato (a).6 |
| Media longa de adena | ~232 mil/h em ~8,5 h, com o delta de 1.974.095 ao lado | `01-MEDICOES-DE-CAMPO.md:299-301` |
| Erros de nivel aceitos por concordancia | 3 de 4 | `REQUIREMENTS.md`, LEIT-11 |
| Salto de relogio do dual boot | 3 h, e `Maquina.pular_parede()` ja o simula | `tests/test_relogio.py:23-53, 120-134` |
| Linha de base da suite | **5355 passed**, com `tests/test_agenda.py` desselecionado | medido no planejamento do `02-01` |

**O que este plano NAO usa como fixtura, e a razao vale ser escrita:** os numeros do
`02-CONTEXT.md:145-153` (466 mil/h, 226 mil/h, 2,41 M XP/h, ~104 abates/min, recusa 21/79/0%)
vieram de um script de bancada e **nao estao em artefato versionado** (achado A1 da pesquisa).
Eles aparecem em **prosa e em comentario**, para dar escala e motivo, e **nunca em assercao** —
um teste construido sobre numero que ninguem reproduz a partir de um clone e um teste que
envelhece calado. As assercoes usam so a tabela acima.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Os quatro deltas negativos que nao sao prejuizo — level up, gasto, relogio para tras, e o nivel que recusou</name>
  <files>l2scanner/renda_conta.py, tests/test_renda_conta.py</files>
  <read_first>
    - `l2scanner/renda_leitura.py:1479-1536` — `o_exp_andou_para_tras`, que **se abstem quando o nivel mudou em qualquer direcao** (`:1498-1499`), e `o_nivel_andou_para_tras`, que **so olha para baixo** (`:1527-1528`). As duas ja fazem metade do trabalho do level up: o que falta e a AFIRMACAO e a CONTA, e nenhuma linha daquele modulo as tem.
    - `l2scanner/renda_leitura.py:1538-1608` — `a_adena_saltou_ordem_de_grandeza`, bidirecional **de proposito**, cuja docstring ja antecipa esta fase (`:1543-1545`: gastar adena e normal e a Fase 2 ja trata renda negativa); o criterio por **razao** com multiplicacao inteira (`:1572`); e a abstencao quando qualquer lado e zero (`:1569-1570`). E `conferir_o_par`, que devolve **tupla vazia** para par coerente.
    - `l2scanner/renda_leitura.py:1268-1298` — `CamposDaRenda`, os tres campos cada um inteiro OU recusa, e a propriedade `por_campo`. E o unico objeto que sobrevive ao nivel recusado; `LeituraDaRenda` exige os tres (`:1300-1319`).
    - `l2scanner/renda_leitura.py:145-160` e `:211-224` — `RecusaDaRenda` e `_recusar`, o molde do motivo nomeado com `campo`, `motivo` e `detalhe`.
    - `l2scanner/renda_leitura.py:112-142` e `:326` — as constantes de motivo ja declaradas (para nao colidir) e `DECIMOS_DE_MILESIMO_POR_PONTO`.
    - `tests/test_renda_par.py:96-200` — a classe do par de campo com a procedencia das duas pontas escrita, e o idioma de nome de teste em portugues com o fato em CAIXA ALTA.
    - `tests/test_renda_par.py:440-531` — o portao da distincao de motivos, que DERIVA a lista do modulo com o prefixo em vez de repeti-la, e a contagem que existe para que fundir dois motivos derrube alguma coisa.
    - `tests/test_relogio.py:23-53` — `Maquina`, `andar()` e `pular_parede()`, com a docstring "o que o dual boot faz: so o relogio de parede salta"; e `:120-134`, o teste que EXIGE que o pulo apareca sem ancora.
    - `l2scanner/mercado_catalogo.py:855-880` — o unico precedente de tempo que anda para tras, e a razao de ele ABSORVER em vez de nomear.
  </read_first>
  <behavior>
    - Level up verdadeiro (`66 -> 67`, EXP `685_632 -> 80_012`): ganho `394_380` decimos, `niveis_ganhos == 1`, `conferir_o_par` devolve tupla vazia, descontinuidade vazia.
    - Level up de dois niveis de uma vez (`66 -> 68`): o ganho conta os niveis atravessados e nao so o ultimo; e o teste diz na mensagem de falha que isso pode acontecer com o scanner cego no meio.
    - Mesmo nivel com EXP subindo: ganho e a diferenca simples, e nao a formula do level up.
    - Mesmo nivel com EXP **caindo** (morte): a regra do level up **nao** dispara, e a recusa de `o_exp_andou_para_tras` chega no passo.
    - Nivel **descendo** (`67 -> 66`): recusa de `o_nivel_andou_para_tras`, ganho nao computado.
    - Adena subindo: entra no ganho bruto. Adena caindo: entra no **gasto**, e o ganho bruto daquele passo e zero.
    - Uma sequencia com um gasto no meio tem ganho bruto igual a soma dos deltas positivos, e o gasto igual a soma dos negativos em modulo — e a soma dos dois **nao** e o delta ponta a ponta, e o teste afirma isso explicitamente.
    - Adena com salto de ordem de grandeza: recusa da regra da Fase 1, sem confundir com gasto.
    - Carimbo do atual menor que o do anterior: descontinuidade `relogio-andou-para-tras`, intervalo nao entra no denominador, ganho nao computado.
    - Carimbos iguais: intervalo zero, e a divisao pelo denominador **nao** e feita — motivo nomeado, nunca infinito.
    - `CamposDaRenda` com o nivel **recusado** e EXP caindo: descontinuidade `nivel-indisponivel-com-exp-caindo`, ganho nao computado, e nenhuma chamada a `conferir_o_par` — nao ha par a conferir.
    - `CamposDaRenda` com o nivel recusado e EXP **subindo**: tambem nao computa ganho de EXP, e a mensagem diz por que — sem o nivel nao da para saber se o EXP subiu dentro do mesmo nivel.
    - `CamposDaRenda` com o nivel recusado e adena OK: a adena continua sendo lida e o passo dela existe. O nivel fragil nao pode derrubar a adena junto.
  </behavior>
  <action>
Escrever, em `l2scanner/renda_conta.py`, os motivos proprios desta fase como constantes de modulo
com o mesmo prefixo da Fase 1, e o `PassoDaRenda` ganhando os campos que a fatia deixou em
esbozo: `niveis_ganhos`, `gasto_de_adena`, e a descontinuidade que passa a ter quatro valores
nomeados alem do vazio.

**O level up (REND-03, CTX-5).** A afirmacao e `atual.nivel > anterior.nivel`, e ela e desta fase
— nenhuma linha de `renda_leitura.py` a faz: aquele modulo apenas **se cala** quando o nivel muda.
A conta em decimos e `(DECIMOS_DE_MILESIMO_POR_PONTO * 100 - anterior.exp) + atual.exp`, e a
constante e **derivada** e nunca escrita a mao: um `1_000_000` literal seria uma segunda verdade
sobre a unidade que a Fase 1 ja declarou. Para mais de um nivel atravessado, somar
`(niveis_ganhos - 1)` niveis inteiros — o caso acontece com o scanner cego por tempo suficiente, e
uma formula que so soma um nivel devolveria um numero menor sem avisar.

**O gasto (REND-04, CTX-6).** Delta de adena negativo vai para `gasto_de_adena`, em modulo, e o
`ganho_de_adena` daquele passo e zero. Os dois sao campos DIFERENTES pela mesma razao que
`duplicadas` e `perdidas` sao campos diferentes em `mercado_modo.Contagem`: sao fatos diferentes e
somar um no outro apaga a pergunta que o usuario tem. **A taxa de ganho consome so os positivos**,
e isso e a metade do requisito que a conta entrega; a outra metade — deixar farm e venda
distinguiveis — e a coluna `origem_do_ganho` do `02-03`, e este fonte escreve o endereco dela para
que ninguem tente inferir a origem por limiar aqui dentro.

**O relogio para tras (CTX-10).** `intervalo = atual.carimbo - anterior.carimbo`, e se ele for
negativo o passo recebe a descontinuidade nomeada e **sai do denominador**. Sem valor absoluto e
sem piso em zero: os dois **absorvem o sinal**, e absorver e o defeito — uma taxa calculada sobre
um intervalo consertado a torto e um numero errado com cara de certo. Intervalo **zero** e outro
fato e tem outro tratamento: nao ha divisao a fazer, e o motivo diz isso.

**O nivel recusado com o EXP caindo (CTX-5, LEIT-11) — o caso mais caro, e ele e estrutural.** As
quatro regras de par exigem `LeituraDaRenda`, que exige os TRES campos inteiros. Com o nivel
recusado esse objeto **nao existe**, entao `conferir_o_par` nao pode ser chamada e a decisao mora
aqui, sobre `CamposDaRenda`. A regra: sem o nivel, **nenhum** ganho de EXP e computado — nem
quando o EXP sobe —, porque sem o nivel nao da para saber se o EXP subiu dentro do mesmo nivel ou
atravessou um. E quando o EXP cai com o nivel recusado, a tentacao e chamar de level up: e
exatamente ai que a alternativa registrada da CTX-5 erra, porque morrer tambem derruba o EXP muito.
O fonte carrega a medicao que fecha o argumento: 3 das 4 leituras erradas de nivel foram aceitas
por **concordancia** das duas escalas, entao o nivel e o campo em que "parece certo" prova menos.

**E a adena sobrevive ao nivel recusado.** O caminho por `CamposDaRenda` existe justamente para
isso: `ler_a_renda` devolveria a primeira recusa em `ORDEM_DOS_CAMPOS`, e `nivel` e o primeiro,
entao uma amostra com EXP e adena perfeitos sumiria por causa do campo mais fragil. Ha um teste
afirmando que a adena continua contando quando o nivel recusou, e o nome dele diz isso.

Os testes montam pares a mao com carimbos escolhidos, sem OCR, sem pixel e sem rede — e para o
salto do relogio, o idioma que ja existe e `Maquina.pular_parede()`, importado ou reescrito em
cinco linhas com o endereco do original no comentario. Um `skip` neste arquivo e falha.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q` termina em 0 com 0 skipped.
    - Existe um teste que afirma `394_380` decimos de ganho para o par de campo, com a procedencia das duas pontas na docstring da classe; e o CONTROLE ao lado afirmando que o resultado **nao** e negativo.
    - Existe um teste do level up de DOIS niveis afirmando que o ganho conta os niveis atravessados; e um CONTROLE de um nivel so, para que uma formula que ignore `niveis_ganhos` nao passe nos dois.
    - Existe um teste que afirma que a formula do level up **nao** dispara quando o nivel e o mesmo, mesmo com o EXP caindo muito — e a mensagem de falha nomeia a alternativa registrada que a CTX-5 recusou.
    - Existe um teste que afirma `gasto_de_adena` preenchido e `ganho_de_adena == 0` no passo do gasto; e um teste de sequencia afirmando que ganho bruto **nao** e igual ao delta ponta a ponta quando houve gasto no meio.
    - Existe um teste com carimbo decrescente afirmando a descontinuidade nomeada; e o CONTROLE afirmando que o intervalo **nao** foi absorvido: `python -c "import ast,sys;src=open('l2scanner/renda_conta.py',encoding='utf-8').read();a=ast.parse(src);print(sum(1 for n in ast.walk(a) if isinstance(n,ast.Call) and getattr(n.func,'id','') in ('abs',)))"` imprime `0`.
    - Existe um teste com carimbos IGUAIS afirmando motivo nomeado e nenhuma divisao — e nao um resultado infinito.
    - Existe um teste que monta `CamposDaRenda` com o nivel recusado e o EXP caindo e afirma a descontinuidade `nivel-indisponivel-com-exp-caindo` com ganho nao computado; e um IRMAO com o EXP subindo afirmando o mesmo tratamento, com a mensagem dizendo por que.
    - Existe um teste que afirma que, com o nivel recusado, **a adena continua contando** — o par de adena existe e o passo dela sai. Sem ele, uma implementacao que descartasse a amostra inteira passaria nos outros.
    - Existe um teste que afirma que, no caminho do nivel recusado, `conferir_o_par` **nao** e chamada — a prova pode ser por injecao de um substituto que registra a chamada, ou por afirmar que a tupla de recusas do passo esta vazia e a descontinuidade preenchida.
    - Os cem pontos percentuais saem da constante da Fase 1 e nunca de um literal, conferido por arvore para que a prosa possa explicar a armadilha: `python -c "import ast;a=ast.parse(open('l2scanner/renda_conta.py',encoding='utf-8').read());print([n.value for n in ast.walk(a) if isinstance(n,ast.Constant) and n.value==1000000])"` imprime `[]`.
    - O portao de distincao de motivos existe em `tests/test_renda_conta.py`, DERIVA a lista do modulo pelo prefixo e afirma que nenhum motivo desta fase colide com um da Fase 1; e ele tem o controle positivo que sintetiza dois motivos com o mesmo texto e afirma que sao acusados.
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo de 5355.
  </acceptance_criteria>
  <done>Os quatro casos em que a subtracao ingenua mente estao presos por teste com o par de campo real, cada um com o seu controle, e o nivel fragil nunca derruba a adena junto nem inventa um level up.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A janela movel por tempo, o denominador sem lacuna, a ancora do reinicio, e as duas taxas</name>
  <files>l2scanner/renda_conta.py, tests/test_renda_conta.py</files>
  <read_first>
    - `l2scanner/mercado_analise.py:145-180` — os tres pisos existentes (`N_MINIMO_PARA_MENOR = 1`, `N_MINIMO_PARA_MEDIANA = 5`, `N_MINIMO_PARA_TENDENCIA = 8`) e `unitario()`, o precedente de `Fraction`.
    - `l2scanner/mercado_analise.py:226-249` — `Evidencia` inteira, com a docstring: ela viaja DENTRO de todo resultado e nao ao lado; abaixo do piso a resposta e o que falta, nunca um numero.
    - `l2scanner/mercado_analise.py:375-398` — `recencia_do_preco` e as quinze linhas explicando por que a recencia e um fato SEPARADO do valor: exibir o minimo de terca ao lado da recencia de hoje seria mentira dentro da mesma linha.
    - `l2scanner/mercado_analise.py:405-425` — `UNIDADE_DA_JANELA`, obrigatoria e cuja escolha E o requisito; e `Tendencia`, com `variacao_percentual: float | None` e `motivo_da_ausencia: str | None`.
    - `l2scanner/mercado_analise.py:426-480` — como a tendencia recusa abaixo do piso e o que ela faz quando a divisao nao pode ser feita; e `:431-443`, a proibicao do carimbo no eixo x, com a razao medida.
    - `l2scanner/dashboard_dados.py:591-650` — `ancora_da_meia_noite` e `baldes()`: o indice inteiro exato por `//` sobre `timedelta`, com a medicao `69713.696` contra `69713`, e o `assert in` por balde como forma executavel do D-02.
    - `.planning/workstreams/renda/ROADMAP.md`, Phase 2, criterio 1 e criterio 4 — o que "se anunciar como ruido" e o que "reiniciar nao inventa nem apaga renda" exigem, com essas palavras.
  </read_first>
  <behavior>
    - Uma sequencia de amostras dentro da janela produz taxa por hora e por minuto sobre `Fraction`, com `n` igual ao numero de PASSOS (e nao de amostras) e a janela farmada em segundos.
    - A mesma sequencia com um intervalo maior que o limiar no meio produz a MESMA taxa por hora, com `lacunas_excluidas == 1` e `segundos_em_lacuna` igual ao intervalo excluido.
    - Duas lacunas produzem `lacunas_excluidas == 2`, e os segundos somam.
    - Uma sequencia inteiramente dentro de uma lacuna (duas amostras separadas por meia hora) produz `por_hora=None` com motivo nomeado, e nunca uma taxa sobre denominador zero.
    - A taxa da JANELA MOVEL e a taxa da SESSAO INTEIRA saem as duas, da mesma sequencia, e sao numeros diferentes quando houve tempo parado — o teste usa a ordem de grandeza medida (janela curta bem acima da media longa) como caso nomeado.
    - A janela movel corta por TEMPO a partir da amostra mais recente, e nao por contagem: dobrar a cadencia dentro da mesma janela **nao** muda a janela coberta, so o `n`.
    - A primeira amostra de uma sequencia produz um passo `ancora` e nao entra em nenhum denominador.
    - Uma sequencia montada como "sessao 1, reinicio, sessao 2" — sem nenhum estado atravessando — produz exatamente a soma dos ganhos das duas, e nunca um delta atravessando o reinicio.
    - Abaixo do piso de amostras: `por_hora=None`, e o motivo nomeia o piso de amostras.
    - Abaixo do piso de janela: `por_hora=None`, e o motivo nomeia o piso de janela — os dois motivos sao textos DIFERENTES.
    - A recencia (`ate`) e o carimbo da amostra mais recente que entrou, e ela sai preenchida **mesmo quando** `por_hora` e `None`.
    - `unidade_da_janela` viaja escrita e vale `minutos farmados`.
  </behavior>
  <action>
Escrever a janela movel, o denominador e as duas taxas.

**A janela e por TEMPO (CTX-1).** Ela corta a sequencia a partir do carimbo da amostra mais
recente para tras, pelo tamanho vindo do `config.toml`. "Ultimos dez minutos" e o que o usuario
entende; "ultimas quarenta amostras" muda de significado quando a cadencia muda ou quando o
scanner fica cego — e a Fase 3 tem cadencia variavel **por natureza**, porque o OCR custa dezenas
de milissegundos e o jogo as vezes some da tela. Isso vai na docstring com a alternativa
registrada ao lado, porque ela e o padrao silencioso e alguem a implementa sem perceber.

**O denominador e a soma dos intervalos entre amostras consecutivas ACEITAS (CTX-2).** Um
intervalo maior que o limiar e **excluido** e contado a parte: os segundos vao para
`segundos_em_lacuna` e a contagem para `lacunas_excluidas`. A razao vai escrita: se o scanner
ficou vinte minutos sem ler, esses vinte minutos nao foram farmados **do ponto de vista da
medicao**, e dividir por eles produz uma taxa artificialmente baixa que o usuario nao tem como
distinguir de "o farm piorou". A alternativa — dividir pelo relogio de parede — vai registrada
como recusada, com o nome, porque ela e o que acontece se ninguem escrever isto.

E a **unidade** vai escrita junto do numero, no molde de `UNIDADE_DA_JANELA`: `minutos farmados`
e um fato diferente de `minutos de relogio`, e a diferenca entre os dois e a medicao inteira desta
decisao. Sem a palavra viajando com o numero, quem desenha a tela le a taxa como se o denominador
fosse o relogio.

**A forma do resultado copia `Evidencia` e `Tendencia`, e nao a aritmetica delas.** A `Evidencia`
viaja **dentro** do resultado, com `n` = numero de PASSOS que entraram no denominador — nao de
amostras, porque um passo e o que tem intervalo — e o piso vindo do `config.toml`. Abaixo do piso,
`por_hora` e `None` e `motivo_da_ausencia` diz **qual** dos dois pisos faltou, com textos
diferentes: um motivo generico obrigaria quem desenha a adivinhar, que e exatamente o que a
docstring de `Evidencia` recusa. E a recencia e um campo **separado** do valor, pela razao de
`recencia_do_preco`: exibir uma taxa de uma hora atras ao lado da recencia de agora seria a mesma
mentira dentro da mesma linha.

**Duas taxas, nao uma (CTX-4).** A da janela movel responde "o que esta acontecendo agora" e a da
sessao inteira responde "o que a noite rendeu". As duas saem da mesma funcao com janelas
diferentes — a da sessao e a sequencia inteira —, e as duas carregam a propria `Evidencia` e a
propria janela farmada. Apresentar so uma **mente por omissao**, e o numero medido esta no fonte
como comentario: a media de 8h45 e a janela curta divergiram por um fator de dois, e a diferenca e
o tempo parado.

**A ancora, e ela e o REG-02 inteiro (C-7).** A primeira amostra de uma sequencia nao tem
anterior, logo nao produz delta: ela e ancora. Isso e regra da **conta**, e nao do registro — o
`ROADMAP.md:39-41` ja tinha dito ("REG-02 e uma regra de taxa vestida de persistencia") e o
`02-CONTEXT.md` nao repete. O fonte escreve isso, e escreve tambem onde a garantia **nao** esta:
no `.mercado/` ela vem do indice em memoria reconstruido do CSV, e essa e justamente a peca que a
C-3 manda **nao** copiar. Um plano que so lesse o CONTEXT procuraria a garantia no lugar errado, e
o comentario existe para que a proxima pessoa nao a procure.

**Tudo em `Fraction` na divisao, inteiro no resto.** A taxa e `ganho * 3600 / segundos` sobre
`Fraction`, nunca `float`: a arvore ja mediu `total_seconds()` devolvendo `69713.696` onde o
inteiro dizia `69713`, e a docstring do `LeituraDaRenda` ja escreveu que diferenca sobre binario
de ponto flutuante acumula erro que ninguem consegue ver depois de gravado. O `float` sobrevive so
no carimbo, que e epoch e cuja subtracao e exata.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q` termina em 0 com 0 skipped.
    - Existe o par janela-com-lacuna / janela-sem-lacuna afirmando a MESMA taxa por hora e `lacunas_excluidas` 1 e 0. Sem os dois, uma implementacao que ignorasse lacunas passaria numa e outra que ignorasse tudo passaria na outra.
    - Existe um teste com DUAS lacunas afirmando `lacunas_excluidas == 2` e a soma dos segundos.
    - Existe um teste que dobra a cadencia dentro da mesma janela e afirma que `janela_farmada_em_segundos` **nao** mudou e que `n` **mudou** — a prova de que a janela e por tempo e nao por contagem.
    - Existe um teste que afirma que a taxa da janela movel e a da sessao inteira saem as duas e sao **diferentes** numa sequencia com tempo parado; e um CONTROLE numa sequencia sem tempo parado afirmando que elas sao **iguais**.
    - Existe um teste afirmando `descontinuidade == "ancora"` para a primeira amostra e que ela nao entra em denominador nenhum.
    - Existe um teste "sessao 1, reinicio, sessao 2" afirmando que o ganho total e a soma dos dois trechos e que **nao existe** delta atravessando o reinicio; a mensagem de falha cita o criterio 4 do roadmap.
    - Existem os dois testes de piso, e eles afirmam **textos de motivo diferentes** para o piso de amostras e o piso de janela. Um teste afirmando so "motivo nao vazio" nos dois casos nao satisfaz este criterio.
    - Existe um teste afirmando que `ate` sai preenchido **mesmo quando** `por_hora` e `None`.
    - Existe um teste afirmando `unidade_da_janela == "minutos farmados"`.
    - `python -c "import sys;sys.path.insert(0,'.');from fractions import Fraction;import l2scanner.renda_conta as c;print(c.__doc__ is not None)"` imprime `True`, e um teste afirma que `por_hora` e `por_minuto` sao instancias de `Fraction` quando nao sao `None`.
    - Nenhuma conversao a ponto flutuante e nenhum `total_seconds`, conferido por arvore: `python -c "import ast;a=ast.parse(open('l2scanner/renda_conta.py',encoding='utf-8').read());print(sorted({x for x in [getattr(n.func,'id',None) or getattr(n.func,'attr',None) for n in ast.walk(a) if isinstance(n,ast.Call)] if x in {'float','total_seconds'}}))"` imprime `[]`.
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo do que a Tarefa 1 deixou.
  </acceptance_criteria>
  <done>A taxa se anuncia: `n`, janela farmada, lacunas contadas a parte, recencia separada e a unidade escrita; as duas taxas saem juntas; e reiniciar produz ancora e nunca um delta atravessando o buraco.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: O tempo ate o nivel, e a contagem que explica de onde veio cada `n`</name>
  <files>l2scanner/renda_conta.py, tests/test_renda_conta.py</files>
  <read_first>
    - `l2scanner/mercado_analise.py:455-480` — o retorno abaixo do piso com `motivo_da_ausencia="evidencia insuficiente"`, e o tratamento do caso em que a divisao nao pode ser feita: motivo nomeado, nunca divisao por zero escapando.
    - `l2scanner/mercado_modo.py:137-151` — `Contagem`, com a docstring que diz que `duplicadas` e `perdidas` sao FATOS DIFERENTES e por isso sao campos diferentes; e `:207-229`, `processar_a_pagina_aceita`, que devolve textos em vez de imprimir e muta a contagem no lugar.
    - `l2scanner/mercado_console.py:660-700` — `resumo_da_sessao`, que separa tres blocos: o que a TELA entregou, o que foi para o DISCO, e os motivos de descarte agregados. A forma, nao o texto — esta fase nao imprime nada.
    - `.planning/workstreams/renda/REQUIREMENTS.md`, REND-05 — a formula e a razao de ela nao precisar de tabela de XP por nivel.
    - `02-CONTEXT.md:90-93` — "por que a taxa desta hora tem `n=12` se o scanner rodou quarenta minutos", que e a pergunta que a contagem responde.
  </read_first>
  <behavior>
    - Com EXP em `80_012` decimos e uma taxa positiva de EXP por hora, o tempo ate o nivel sai em segundos sobre `Fraction`, e ele bate com a conta feita a mao no teste.
    - Com taxa de EXP igual a zero: `segundos=None` e motivo nomeado. Nunca infinito.
    - Com taxa de EXP negativa (o usuario esta morrendo mais do que farmando): `segundos=None` e motivo nomeado, **diferente** do motivo do zero.
    - Com a `Evidencia` da taxa abaixo do piso: `segundos=None` e o motivo herda o da taxa — a previsao nao pode ser mais confiante que o numero de que ela sai.
    - Com EXP em `999_999` decimos (quase no nivel): o tempo e pequeno e positivo, e nao zero nem negativo.
    - A `ContagemDaRenda` separa aceitas, recusadas por motivo e lacunas, e os tres nao se somam.
    - Uma sequencia com quatro aceitas, tres recusas de nivel e uma lacuna produz exatamente esses tres numeros, e o dicionario de recusas tem o motivo como chave.
    - A contagem e mutada no lugar por quem processa, e nao devolvida nova a cada passo — o molde de `mercado_modo`.
  </behavior>
  <action>
**O tempo ate o nivel (REND-05).** `(100 pontos percentuais - exp atual) / (exp por hora)`, tudo em
decimos de milesimo e tudo sobre `Fraction`, com o resultado em segundos. Os cem pontos saem da
constante da Fase 1, nunca de um literal.

**Os tres casos em que nao ha numero a dizer sao TRES casos e nao um**, e cada um tem o seu
motivo, no molde exato de `Tendencia`: taxa **zero** (nao esta ganhando EXP — o painel diz isso e
nao "faltam infinitas horas"), taxa **negativa** (esta perdendo EXP, e o texto e outro porque o
conserto do usuario e outro), e **evidencia insuficiente** (a taxa de que a previsao sairia ainda
nao existe, e a previsao nao pode ser mais confiante que ela). Nenhum deles devolve numero, e
nenhum deixa a divisao acontecer: e a mesma disciplina que `mercado_analise` aplica ao
`intercept == 0`.

Este e o requisito que o usuario pediu com essas palavras — "quanto tempo vai levar" —, e a barra
o entrega **sem tabela de XP por nivel**, que seria dependencia externa, especifica de servidor e
desatualizavel a cada patch. Isso vai na docstring, porque e a razao de a milestone inteira ler a
barra em vez do chat.

**A `ContagemDaRenda`.** Um dataclass mutavel com `aceitas`, `recusadas_por_motivo`, `lacunas` e
`descontinuidades_por_motivo` — quatro fatos que **nao se somam**, no molde literal de
`mercado_modo.Contagem`. Ela e mutada no lugar por quem processa a sequencia, e devolve numeros em
vez de imprimir: esta fase nao tem tela.

Ela existe para responder uma pergunta so, e a pergunta esta escrita no `02-CONTEXT.md`: **por que
a taxa desta hora tem `n=12` se o scanner rodou quarenta minutos.** Sem ela, a taxa de recusa vira
ruido invisivel, e a Fase 1 ja mediu que ela e alta e desigual entre os campos — o nivel recusa
muito mais que a adena, e muito mais que o EXP. A CTX-9 exige que as recusas tambem sejam
gravadas; esta contagem e a metade em memoria do mesmo fato, e o `02-03` escreve a metade em
disco.

**A contagem nao e estado de modulo.** Ela nasce de quem chama e viaja por parametro, exatamente
como a `Contagem` do mercado — e o portao de ausencia de memoria que o `02-01` estendeu a este
modulo continua verde por construcao.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py -q` termina em 0 com 0 skipped.
    - Existe um teste que afirma o tempo ate o nivel com a conta feita a mao no proprio teste, e a mensagem de falha mostra as duas contas lado a lado.
    - Existem **tres** testes de ausencia — taxa zero, taxa negativa e evidencia insuficiente — e eles afirmam **tres textos de motivo diferentes**. Um teste que afirme so "motivo nao vazio" nos tres nao satisfaz este criterio.
    - Existe um teste com EXP em `999_999` afirmando tempo pequeno e **positivo**; sem ele, uma implementacao com o sinal trocado passaria nos outros.
    - O caso do zero e tratado **antes** da divisao e nao capturado depois, conferido por arvore: `python -c "import ast;a=ast.parse(open('l2scanner/renda_conta.py',encoding='utf-8').read());print(sorted({x for x in [getattr(n,'attr',None) or getattr(n,'id',None) for n in ast.walk(a) if isinstance(n,(ast.Attribute,ast.Name))] if x in {'inf','ZeroDivisionError'}}))"` imprime `[]`.
    - Existe um teste da contagem com quatro aceitas, tres recusas de nivel e uma lacuna, afirmando os tres numeros e o motivo como chave do dicionario.
    - Existe um teste afirmando que aceitas + recusadas **nao** e igual a lacunas + aceitas — os campos sao fatos diferentes e o teste afirma que eles nao foram colapsados.
    - Existe um teste afirmando que a contagem e mutada no lugar: a mesma instancia passada duas vezes acumula.
    - O portao de ausencia de memoria do `02-01` continua verde sobre `renda_conta.py` depois desta tarefa: `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_par.py -q` termina em 0.
    - `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo do que a Tarefa 2 deixou.
  </acceptance_criteria>
  <done>"Quanto tempo falta para o proximo nivel" tem resposta ou tem motivo — nunca infinito e nunca negativo —, e a contagem responde por que a taxa desta hora tem o `n` que tem.</done>
</task>

</tasks>

## Artifacts que este plano produz

- `l2scanner/renda_conta.py` completo — os quatro deltas negativos nomeados, a janela movel por
  tempo com denominador sem lacuna, as duas taxas com `Evidencia`, o tempo ate o nivel com os tres
  motivos de ausencia, e a `ContagemDaRenda`
- `tests/test_renda_conta.py` — com o par de campo real, os controles ao lado de cada portao, e o
  idioma de relogio falso que a arvore ja tem

Nenhum arquivo do workstream `dashboard`, nenhum arquivo do `mercado`, nenhuma dependencia nova.
**Este plano nao toca `l2scanner/renda_registro.py`** — ele e do `02-03`, que roda em paralelo.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| carimbo do relogio -> denominador da taxa | O relogio desta maquina anda para tras por dual boot e por reancoragem, e os dois tem teste EXIGINDO o comportamento |
| leitura da barra -> conta | Um numero valido, plausivel e errado que passe pelas guardas da Fase 1 vira taxa errada, e depois de gravado nao ha como distingui-lo de um certo |
| limiares do `config.toml` -> conta | Um limiar de lacuna mal escolhido muda todo denominador em silencio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | nivel lido errado e aceito por concordancia virando level up inventado | critical | mitigate | Nivel recusado com EXP caindo e descontinuidade nomeada, ganho nao computado; a medicao do LEIT-11 (3 de 4 por concordancia) vai escrita no fonte; teste com o caso e o irmao do EXP subindo |
| T-02-10 | Tampering | carimbo decrescente entrando no denominador e produzindo taxa negativa ou infinita **sem levantar** | high | mitigate | Descontinuidade nomeada, passo fora do denominador, e portao de AST afirmando zero chamadas de valor absoluto no modulo |
| T-02-11 | Repudiation | taxa com `n` inexplicavel depois de uma noite de farm | medium | mitigate | `ContagemDaRenda` com aceitas, recusadas por motivo e lacunas como campos separados que nao se somam |
| T-02-12 | Denial of Service | divisao por zero no tempo ate o nivel derrubando o consumidor | medium | mitigate | Os tres casos de ausencia tratados **antes** da divisao, com motivos distintos; portao negando `math.inf` e captura de `ZeroDivisionError` |
| T-02-13 | Tampering | erro binario acumulado ao longo de uma noite tornando a taxa lentamente errada | medium | mitigate | `Fraction` na divisao e inteiro escalado no resto; portao negando `total_seconds()` e conversao a ponto flutuante no modulo |
| T-02-SC | Tampering | instalacao de pacote de terceiro | n/a | accept | **Nenhum `pip install` nesta fase.** `dataclasses` e `fractions` sao stdlib; `git diff --stat requirements.txt` sai vazio como criterio |
</threat_model>

<verification>
- `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_conta.py tests/test_renda_conta_tracer.py tests/test_renda_par.py -q` termina em 0 com 0 skipped.
- `PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --ignore=tests/test_agenda.py -q` termina em 0 e o total de `passed` nao cai abaixo de 5355.
- `git diff --stat requirements.txt` sai vazio.
- `git status --porcelain` nao lista `l2scanner/renda_registro.py` nem nenhum arquivo de `l2scanner/dashboard*`.
</verification>

<success_criteria>
- Subir de nivel registra `394_380` decimos de ganho, com o par de campo real como caso de teste.
- Gastar adena nao entra na taxa de ganho, e o ganho bruto e a soma dos deltas positivos.
- Um nivel recusado nunca vira level up adivinhado, e nunca derruba a adena junto.
- Um carimbo que anda para tras e evento nomeado, e o sinal nao e absorvido.
- Vinte minutos de cegueira nao viram taxa baixa: a lacuna sai do denominador e e contada a parte.
- Saem duas taxas, e as duas carregam `n`, janela farmada, lacunas e recencia separada.
- O tempo ate o nivel tem resposta ou tem um de tres motivos distintos — nunca infinito.
- Reiniciar produz ancora, e a garantia do REG-02 esta na conta com o motivo escrito.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-02-SUMMARY.md` when done
</output>
