---
phase: 03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - vigiar-renda.bat
  - tests/test_vigiar_renda_bat.py
  - .planning/workstreams/renda/ROADMAP.md
autonomous: true
requirements: [CONS-02]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`vigiar-renda.bat` sobe em DOIS CLIQUES, na familia dos lancadores que ja existem, e e um PROCESSO SEPARADO: derrubar o `--renda` nao derruba o `vigiar-party.bat` nem o `vigiar-mercado.bat`, e derrubar qualquer um deles nao derruba o `--renda` (CONS-02, CTX-7)."
    - "Ele e COPIA de `vigiar-mercado.bat` com QUATRO trocas e nada mais: o cabecalho `REM`, `--mercado` -> `--renda` na linha de execucao, o texto dos blocos de erro, e nenhum `--personagem` (o nome sai do titulo, como `renda_modo.py:330` ja faz). A resolucao de `--janela` e IDENTICA e nao se reinventa."
    - "A mira vem da chave `[jogo] personagem` do `config.toml`, por `config.ler_personagem_do_jogo()` mais `cliente.SEPARADOR` e `cliente.NOME_DO_CLIENTE` — as constantes vem do CODIGO e nunca sao reescritas no `.bat`, porque uma segunda copia de `\" - XM Essence\"` envelheceria sozinha."
    - "NENHUM `echo` sai DEPOIS da linha de execucao. Os blocos de erro ficam ANTES dela, e isso e ESTRUTURAL e nao uma guarda: e o defeito medido em campo em 2026-08-28, quando um `.bat` anunciou um desfecho que nao conferiu. O resumo da sessao sai do proprio programa, no `finally` do laco."
    - "Nenhuma mensagem deste lancador manda rodar o lancador de OUTRA feature. E o segundo defeito medido, em 2026-08-31: o usuario rodou o `--mercado` pelo Python global, o modo recusou por falta de OCR, e a mensagem mandou rodar o `vigiar-party.bat` — o lancador da PARTY, para consertar o MERCADO. O conselho certo aqui e `calibrar-renda.bat`."
    - "O `.bat` e ASCII PURO, lido em `cp1252`: acento em `.bat` vira lixo no console do cmd, e este arquivo e literalmente texto para o usuario ler."
    - "Nenhuma flag de DESTINO DE ARQUIVO entra pelo lancador (`--record`, `--replay`, `--pasta`, `--saida`...): a `.renda/` do usuario e dado acumulado e sem desfazer, e o lancador nao pode ser essa porta."
    - "O `ROADMAP.md` deixa de mentir sobre si mesmo (C-9): a tabela de `Progress` para de dizer que as Fases 1 e 2 estao *Not started, 0/?* quando as duas estao mescladas com teste passando, e a `Coverage` ganha os QUATRO requisitos que faltam — LEIT-10, LEIT-11, REND-08 e REND-09 — e para de afirmar \"18 de 18\" sobre uma tabela de 23 linhas."
    - "O `ROADMAP.md` sai desta fase sem gloss que o codigo contradiz: o criterio 4 (`:409-411`) define **parado** como *\"o scanner nao estar vendo\"*, e as quatro plans e o `03-CONTEXT.md` — que prevalece — usam o INVERSO (PARADO = vejo e o valor nao mudou; PAUSADO = nao consigo ver). A substancia esta certa e nao se mexe; o que entra e uma ressalva datada no criterio 4, na mesma disciplina ja aplicada ao criterio 1 quando o `rich.Live` caiu."
    - "Nenhum arquivo de `l2scanner/` e tocado por este plano: ele e o lancador, o teste dele e a documentacao. E por isso ele roda em paralelo com o `03-02`."
  artifacts:
    - vigiar-renda.bat
    - tests/test_vigiar_renda_bat.py
  key_links:
    - "vigiar-renda.bat -> config.ler_personagem_do_jogo + cliente.SEPARADOR + cliente.NOME_DO_CLIENTE: a cadeia da mira, copiada byte a byte de `vigiar-mercado.bat:107-117`. As constantes vem do codigo; o `.bat` nao as reescreve."
    - "vigiar-renda.bat -> `python -m l2scanner --renda --janela \"%JANELA%\" %*`: a unica linha de execucao, e as duas flags do modo tem de estar nela. O `%*` no fim deixa o usuario acrescentar `--intervalo` e `--status-a-cada`, que sao a cadencia do laco e do bloco (C-4, P-4)."
    - "tests/test_vigiar_renda_bat.py -> tests/test_vigiar_mercado_bat.py: molde literal dos onze testes, com `_texto()` em `cp1252` e `_posicao_do_echo` que IGNORA `REM` — a versao ingenua daquela guarda foi enganada pelo proprio comentario que explicava o defeito."
    - "ROADMAP.md -> REQUIREMENTS.md: a `Coverage` e DERIVADA da lista de requisitos e nao escrita a mao. Sao 27 requisitos com `- [ ]` no arquivo e a tabela tem 23 linhas — a diferenca sao os quatro que faltam."
---

<objective>
Dar ao usuario a porta de entrada e parar de mentir na documentacao.

**O lancador**: `vigiar-renda.bat`, na familia dos quatro `.bat` que ja existem, subindo um
**processo separado** — que e literalmente o criterio 2 da fase. Ele nao e trabalho de desenho: e
copia de `vigiar-mercado.bat` com quatro trocas, e o `03-CONTEXT.md:92-96` e o `ROADMAP.md:445-449`
mandam copiar e nao reinventar. O que ele exige de trabalho de verdade sao os **onze testes**, que
nesta casa sao obrigatorios para `.bat` porque os dois defeitos que eles prendem foram medidos em
campo, com data.

**A higiene**: a tabela de `Progress` do `ROADMAP.md` diz que as Fases 1 e 2 estao *Not started,
0/?* — as duas estao mescladas, com cinco e quatro planos e SUMMARY para cada um. A `Coverage`
lista 23 requisitos, afirma **"18 de 18"** e nao inclui o **LEIT-10**, que a propria Fase 3 declara
possuir. Um roadmap que erra a propria contagem e um roadmap que ninguem confere.

Purpose: o produto desta fase so existe para o usuario quando ele consegue subi-lo com dois
cliques, sem lembrar de `--janela` nem de qual Python. E o roadmap e o documento que a proxima
milestone vai ler.

Output: `vigiar-renda.bat`, `tests/test_vigiar_renda_bat.py`, e o `ROADMAP.md` consertado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-CONTEXT.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-01-SUMMARY.md
</context>

> **Os IDs de decisao (CTX-1 a CTX-8, C-1 a C-11, P-1 a P-4) estao definidos no `03-01-PLAN.md`.**
> Este plano cita e nao redefine.

## Por que este plano roda em PARALELO com o `03-02`

Ele nao toca nenhum arquivo de `l2scanner/`. Os arquivos sao `vigiar-renda.bat`,
`tests/test_vigiar_renda_bat.py` e `.planning/workstreams/renda/ROADMAP.md` — zero interseccao com
o `03-02`, que mexe em `renda_estado.py`, `renda_laco.py`, `renda_console.py` e
`captura_janela.py`. A unica dependencia real e o `--renda` existir no `__main__`, e ele nasce no
`03-01`.

**O lancador subindo antes da cegueira nao e um problema**: depois do `03-01` o laco ja captura,
le, conta, grava e mostra. O que falta e a declaracao de pausa — e um lancador que sobe um laco que
ainda nao declara pausa e exatamente o que o `--mercado` faz hoje, de propria escolha.

## As quatro trocas, e so elas

`vigiar-mercado.bat` e `vigiar-party.bat` compartilham, **byte a byte**: `@echo off` / `setlocal` /
`cd /d "%~dp0"`; a busca do Python em cinco lugares guardando o caminho completo; a rejeicao do
stub da Microsoft Store; a criacao do `.venv` na primeira execucao; a sonda de dependencias (que
inclui `winrt.windows.media.ocr`); e os blocos `:erro_venv` / `:erro_deps` **antes** da linha de
execucao. **Nada disso se reescreve.**

| # | O que troca | Por que |
|---|---|---|
| 1 | O cabecalho `REM` | o que ele e, o que **nao** e, e o que precisa: a calibracao da renda por `calibrar-renda.bat`, e que `.renda/` e o produto |
| 2 | `--mercado` -> `--renda` na linha de execucao | e a unica linha funcional que muda |
| 3 | O texto dos blocos de erro e da recusa da mira | o conselho tem de apontar para `calibrar-renda.bat`, e nunca para o lancador de outra feature |
| 4 | **Nada de `--personagem`** | o nome sai do titulo, como `renda_modo.py:330` ja faz. `--personagem` aqui seria a segunda verdade sobre a mesma janela |

## O que o `ROADMAP.md` afirma de errado hoje — C-9, medido

| Onde | O que diz | O que e verdade |
|---|---|---|
| `Progress`, linha da Fase 1 | *Not started, 0/?* | **5 planos, 5 SUMMARY**, mesclada |
| `Progress`, linha da Fase 2 | *Not started, 0/?* | **4 planos, 4 SUMMARY**, mesclada |
| `Coverage` | 23 linhas | faltam **LEIT-10**, **LEIT-11**, **REND-08** e **REND-09** |
| `Coverage`, rodape | *"18 de 18 requisitos mapeados"* | ha **27** requisitos com `- [ ]` no `REQUIREMENTS.md`, e a tabela tem 23 linhas. **Nem o 18 nem o 18** |

> **A linha `**Plans**: TBD` NAO existe mais, e a versao anterior deste plano prometia troca-la.** Medido em 2026-09-03: `ROADMAP.md:450` ja diz `**Plans**: 4 planos escritos`, seguido da lista dos quatro com onda — escrita no proprio planejamento desta fase. A promessa era sobre um estado que o planejamento ja tinha consertado, e o portao que a acompanhava (`'TBD' not in road`) **passa hoje, com a fase inteira por executar**. Os dois sairam. O que a Tarefa 3 faz com a lista e **CONFERI-LA contra o disco**, que e trabalho real e diferente.

> **E entra uma correcao que a versao anterior nao tinha visto:** os ROTULOS do criterio 4 estao invertidos. Ver "O criterio 4 fala ao contrario do codigo" abaixo.

**LEIT-10 e o que mais custa.** O `ROADMAP.md:395-400` acrescentou-o a esta fase por escrito e o
`REQUIREMENTS.md:148` nomeia a Fase 3 como dona — e a `Coverage`, que e a tabela que alguem
consulta para perguntar "isto esta coberto?", nao o tem. Um requisito ausente da tabela de cobertura
e um requisito que a proxima milestone nao sabe que existe.

## O criterio 4 fala ao contrario do codigo — W1, e a substancia esta certa

`ROADMAP.md:409-411`, criterio 4, define os dois rotulos assim:

> *"Valores bit-identicos por N amostras fazem o painel dizer **parado** (...). Renda zero e o
> usuario sentado sem matar nada; **parado e o scanner nao estar vendo**."*

As quatro plans desta fase, seguindo o `03-CONTEXT.md` Area 2 — que **prevalece**, porque e o
documento de decisao travada — usam a atribuicao **inversa**:

| Rotulo | `ROADMAP.md:409-411` | As plans e o `03-CONTEXT.md` (o que o codigo vai fazer) |
|---|---|---|
| **PARADO** | o scanner nao esta vendo | **estou vendo, e o valor nao mudou** — grava |
| **PAUSADO** | (nao nomeado no criterio) | **nao consigo ver** — nao grava |

**A substancia nao muda, e por isso isto e ressalva e nao conserto.** O criterio 4 pede duas
coisas e as duas continuam entregues: que valores bit-identicos produzam um rotulo proprio, e que
ele seja distinguivel de renda zero na tela. O que esta trocado e **qual palavra recebe qual
fato** — e o `03-CONTEXT.md:56-66` decidiu ao contrario do roadmap, com razao: "pausado" e a
palavra que o `REQUIREMENTS.md:307` e o proprio `ROADMAP.md:441` ja usam para *nao estou
enxergando*, e usar "parado" para a mesma coisa daria duas palavras para um fato e nenhuma para o
outro.

**O que nao pode acontecer e a fase FECHAR com o roadmap afirmando o gloss trocado.** Um documento
que descreve o codigo ao contrario e pior que um documento silencioso: o proximo leitor confia
nele e depura na direcao errada. A Tarefa 3 acrescenta a ressalva **datada** no criterio 4, na
forma exata da que ja esta no criterio 1 (a que derrubou o `rich.Live` em 2026-09-03) — e
**nao reescreve o criterio**, pela mesma disciplina: o texto original fica de pe e a correcao fica
ao lado, porque um argumento que some e um argumento que volta.

<tasks>

<task type="auto">
  <name>Tarefa 1: `vigiar-renda.bat` — copia com quatro trocas, e nenhuma quinta</name>
  <files>vigiar-renda.bat</files>
  <read_first>
    - `vigiar-mercado.bat` **inteiro** — e o arquivo a copiar, e a copia e literal fora das quatro trocas.
    - `vigiar-mercado.bat:38-62` — `cd /d "%~dp0"`, a busca do Python em cinco lugares guardando o **caminho completo**, e a rejeicao do stub da Microsoft Store, cada uma com a razao escrita ao lado.
    - `vigiar-mercado.bat:75-105` — a criacao do `.venv` na primeira execucao e a sonda de dependencias, que inclui `winrt.windows.media.ocr`. **A renda precisa da mesma sonda**: o EXP e o nivel saem por OCR do Windows.
    - `vigiar-mercado.bat:107-127` — a resolucao de `--janela` pela chave `[jogo] personagem`, o `for /f` com o `-c` **sem aspas dentro** (o `for /f` recorta o comando entre aspas simples e um apostrofo o encerraria no meio), o `PERSONAGEM_PADRAO` entrando por `sys.argv[1]`, e a recusa quando a mira nao monta.
    - `vigiar-mercado.bat:129-152` — o comentario que explica por que os blocos de erro vem ANTES da execucao, com o incidente de 2026-08-28, e os dois blocos.
    - `vigiar-mercado.bat:154-161` — o `:executar`, a linha unica de execucao com `%*` no fim, e o `pause`.
    - `calibrar-renda.bat` — o irmao da renda que ja existe, para o cabecalho `REM` falar do mesmo produto com o mesmo vocabulario e para o conselho apontar para o lancador certo.
    - `l2scanner/renda_modo.py:325-335` — o nome do personagem saindo do TITULO, e a razao de `--personagem` nao entrar por aqui.
    - `l2scanner/renda_registro.py:248-390` — o `TEXTO_DO_LEIAME` e `escrever_leiame`: o cabecalho `REM` pode dizer que `.renda/` nasce com **dois** arquivos (C-10), para o usuario nao estranhar.
  </read_first>
  <behavior>
    - O arquivo existe na raiz, ao lado dos outros cinco `.bat`, e e **ASCII puro** lido em `cp1252`.
    - A linha de execucao carrega `--renda` e `--janela` **juntas**, executada e nao comentada, e **nenhuma terceira flag**.
    - `--janela` vai **com valor** (`"%JANELA%"`) e nunca pelado.
    - A mira sai da chave `[jogo] personagem`, por `ler_personagem_do_jogo`, e o sufixo do titulo vem de `cliente.SEPARADOR` e `cliente.NOME_DO_CLIENTE` — nao ha `" - XM Essence"` escrito no `.bat`.
    - Quando a mira nao monta, o lancador **recusa** com mensagem, `pause` e `exit /b 1`, e nao roda nada.
    - **Nenhum `echo` executado** aparece depois da linha de execucao — so `REM`, e o teste ignora `REM`.
    - Nenhuma flag de destino de arquivo (`--record`, `--record-janela`, `--replay`, `--pasta`, `--saida`, `--destino`, `--arquivo`, `--out`) aparece no arquivo.
    - Nenhum `echo` manda rodar `vigiar-party.bat`, `vigiar-mercado.bat` nem `calibrar.bat`: quando o conselho e calibrar, ele aponta para `calibrar-renda.bat`.
    - Os sinais de maior e menor estao escapados (`2^>nul`), como no irmao.
    - O cabecalho `REM` diz o que ele e, o que **nao** e (nao vigia a party, nao le o mercado, nao envia alerta), o que precisa antes (`calibrar-renda.bat`), e que o produto e a pasta `.renda/` — que nasce com o CSV **e** o `LEIAME.txt`.
  </behavior>
  <action>
Copiar `vigiar-mercado.bat` para `vigiar-renda.bat` e aplicar **as quatro trocas da tabela acima**.
Nao ha quinta troca, e nao ha melhoria oportunista: qualquer divergencia estrutural entre os dois
lancadores e uma divergencia que envelhece sozinha, e a familia inteira existe para que o usuario
reconheca a forma.

**O cabecalho `REM`**, no molde do irmao, dizendo quatro coisas:

- **o que ele e**: a QUARTA invocacao, ao lado das duas de party e da do mercado. Processo
  separado: derrubar um nao derruba os outros.
- **o que ele NAO e**: nao vigia a party, nao le o mercado, nao envia alerta nenhum. So le a barra
  inferior e grava em `.renda/`.
- **o que precisa antes**: `calibrar-renda.bat` (os tres retangulos e os tres pisos, **por
  personagem**) e, para a adena, `calibrar-renda-moldes.bat`. Sem eles a leitura recusa nomeando o
  que falta, o que **conta como funcionar** — e nao como quebrar.
- **o que ele produz**: a pasta `.renda/`, com um CSV por personagem **e** um `LEIAME.txt` ao lado
  (C-10). Dizer isso aqui evita a duvida de "por que tem um txt no meio dos dados".

**A linha de execucao** e uma so, com `%*` no fim — que e o que deixa o usuario acrescentar
`--intervalo` (a cadencia do laco) e `--status-a-cada` (a do bloco por intervalo) sem que o `.bat`
precise conhece-los. **Nenhuma chave nova de config e nenhum argumento novo**: os dois ja existem
(C-4, P-4).

**A recusa da mira** e o unico texto que muda de conteudo, e ela aponta para `calibrar-renda.bat` e
para a chave `[jogo] personagem` do `config.toml`. Ela **nao** manda rodar o lancador de outra
feature — foi exatamente isso que aconteceu em 2026-08-31 e o teste do irmao existe por causa disso.

**Nada de `echo` depois da execucao.** Nao e estilo: e o que torna **estrutural** a promessa de que
nada e impresso depois de o programa rodar. O resumo da sessao sai do proprio laco, no `finally`
que o `03-01` escreveu — e ele sai **sempre**, inclusive na recusa.
  </action>
  <verify>
    <automated>python -c "d=open('vigiar-renda.bat','rb').read(); assert not [b for b in d if b>127]; t=d.decode('cp1252'); assert '--renda' in t and '--janela' in t"</automated>
  </verify>
  <done>
`vigiar-renda.bat` existe, e ASCII puro, roda `-m l2scanner --renda --janela "%JANELA%" %*` numa
linha so, resolve a mira pela chave do config, recusa quando ela nao monta, e nao imprime nada
depois da execucao.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Os onze testes do lancador — porque `.bat` que anuncia o que nao conferiu ja custou uma sessao</name>
  <files>tests/test_vigiar_renda_bat.py</files>
  <read_first>
    - `tests/test_vigiar_mercado_bat.py` **inteiro** (245 linhas) — os onze testes, a docstring que conta os dois defeitos medidos com data, `_texto()` em `cp1252`, `_linhas_de_echo` que **ignora `REM`**, `_posicao_da_execucao` com a regex e o filtro de `rem`, `FLAGS_DO_MODO` e `FLAGS_DE_DESTINO_DE_ARQUIVO`.
    - `tests/test_calibrar_renda_bat.py:40-63` — `_posicao_do_echo` e a **historia de por que a versao ingenua passava com o `.bat` quebrado**: a primeira guarda foi enganada pelo proprio comentario que explicava o defeito.
    - `tests/test_ponte_bat.py:95-115` — `test_o_bat_chama_o_python_do_venv_por_caminho_literal`, o duodecimo teste desta familia, para decidir se ele vale aqui tambem.
  </read_first>
  <behavior>
    - Os onze testes do irmao existem aqui, adaptados: existencia, ASCII puro, as duas flags do modo na linha executada, **so** as duas flags, `--janela` com valor, a mira vindo da chave do config, a recusa quando a mira nao monta, nenhum `echo` fora de guarda depois da execucao, nenhuma flag de destino de arquivo, nenhum `echo` mandando rodar o lancador de outra feature, e os sinais de maior e menor escapados.
    - O teste do conselho errado e **ampliado**: ele nomeia `vigiar-party.bat`, `vigiar-mercado.bat` e `calibrar.bat`, e afirma que `calibrar-renda.bat` **aparece** — porque um lancador que nao diz como consertar e tao inutil quanto um que diz errado.
    - `_posicao_da_execucao` **ignora linhas `REM`**, e ha um controle positivo: um texto sintetico em que a unica ocorrencia das flags esta num comentario faz o teste **falhar**. Sem esse controle, a guarda passaria sobre um `.bat` quebrado — que foi o que aconteceu com o irmao.
    - `_linhas_de_echo` **ignora `REM`**, com o mesmo controle positivo.
    - Um teste afirma que o `.bat` **nao** carrega `--personagem`: o nome sai do titulo, e uma segunda verdade sobre a mesma janela e o defeito que a Fase 1 mediu (M-F).
  </behavior>
  <action>
Copiar `tests/test_vigiar_mercado_bat.py` e trocar o alvo, as flags do modo e os textos. **Nao
reescrever os dois helpers** — `_linhas_de_echo` e `_posicao_da_execucao` sao onde a inteligencia
mora, e a versao ingenua de cada um ja passou sobre um `.bat` quebrado nesta arvore.

**A docstring do arquivo conta os dois defeitos medidos**, com data, como o irmao faz — porque quem
abrir este arquivo daqui a seis meses precisa saber que os onze testes nao sao cerimonia:

- **2026-08-28**: um `.bat` imprimiu o bloco de conferencia **depois** de a ferramenta ter saido em
  codigo 1, mandando o usuario abrir um arquivo que nunca foi gerado. Por isso os blocos de erro
  ficam antes da execucao e nada e impresso depois dela.
- **2026-08-31**: o usuario rodou o `--mercado` pelo Python **global**, o modo recusou por falta de
  OCR, e a mensagem de recusa mandou rodar o `vigiar-party.bat` — o lancador da **party**, para
  consertar o **mercado**. Por isso existe o teste do conselho.

**Os dois controles positivos sao obrigatorios**, e nesta casa isso e regra e nao preferencia: todo
portao nasce com um controle que prova que ele **acusa**. Um `.bat` sintetico com as flags **so**
dentro de um `REM` tem de fazer `_posicao_da_execucao` devolver `-1`; um com `echo` depois da
execucao tem de ser acusado. Sem os dois, um teste que aceitasse qualquer coisa passaria — e ja
passou.

**O teste novo — `--personagem` fora do lancador.** Ele nao existe no irmao porque o mercado nao
tem esse conceito. Aqui ele existe porque o `03-CONTEXT.md` e o `renda_modo.py:325-335` cravaram que
o nome sai do **titulo**, e um `--personagem` no `.bat` seria a porta pela qual a leitura voltaria a
poder cair no personagem errado.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_vigiar_renda_bat.py tests/test_vigiar_mercado_bat.py -q</automated>
  </verify>
  <done>
Os onze testes passam sobre `vigiar-renda.bat`, os dois helpers ignoram `REM` e tem controle
positivo, e ha um teste a mais afirmando que `--personagem` nao entra pelo lancador.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: O `ROADMAP.md` para de mentir sobre si mesmo (C-9)</name>
  <files>.planning/workstreams/renda/ROADMAP.md</files>
  <read_first>
    - `.planning/workstreams/renda/ROADMAP.md:467-473` — a secao `## Progress` e a tabela com as tres linhas (`:471-473` sao as fases). **Citacoes remedidas em 2026-09-03**: a versao anterior deste plano apontava `:453-460`, `:461-490` e `:444`, e as tres estavam 15 a 25 linhas fora — um `read_first` que manda o executor ler o lugar errado gasta contexto e ensina a desconfiar das citacoes certas.
    - `.planning/workstreams/renda/ROADMAP.md:475-503` — a secao `## Coverage`, a tabela de 23 linhas (`:479-501`) e o rodape de `:503` que afirma *"18 de 18 requisitos mapeados. Nenhum orfao, nenhum em duas fases."*
    - `.planning/workstreams/renda/ROADMAP.md:395-402` — o bloco que acrescentou **LEIT-10** a esta fase, e que a `Coverage` ignora.
    - `.planning/workstreams/renda/ROADMAP.md:409-411` — o **criterio 4**, e a frase *"parado e o scanner nao estar vendo"*, que e o gloss invertido da W1. Ler junto com a secao "O criterio 4 fala ao contrario do codigo" acima.
    - `.planning/workstreams/renda/ROADMAP.md:450-462` — `**Plans**: 4 planos escritos` e a lista dos quatro com onda, **ja escrita no planejamento**. Nao ha `TBD` neste arquivo; esta tarefa **confere** a lista contra o disco e nao a cria.
    - `.planning/workstreams/renda/ROADMAP.md:382-393` — o criterio 1 e a ressalva datada que derrubou o `rich.Live`. **E o molde literal da ressalva do criterio 4** que esta tarefa acrescenta.
    - `.planning/workstreams/renda/REQUIREMENTS.md` inteiro — **a fonte da `Coverage`**. Contar os `- [ ] **XXX-NN**` e derivar a tabela deles, em vez de editar a que esta la.
    - `.planning/workstreams/renda/phases/01-*/` e `02-*/` — os SUMMARY que provam que as duas fases fecharam: cinco na Fase 1 e quatro na Fase 2.
  </read_first>
  <behavior>
    - `Progress` diz **5/5** e **Completed** para a Fase 1 e **4/4** e **Completed** para a Fase 2, com as datas que os SUMMARY carregam — e nunca uma data inventada.
    - `Coverage` tem uma linha para **cada** requisito com `- [ ]` no `REQUIREMENTS.md`, e o executor **conta** em vez de confiar no numero deste plano.
    - **LEIT-10** aparece na `Coverage`, mapeado para a **Phase 3**.
    - **LEIT-11**, **REND-08** e **REND-09** aparecem, mapeados para a **Phase 2**.
    - O rodape diz o numero **contado**, e nao "18 de 18".
    - LEIT-11 traz uma nota de que a Fase 2 e a dona e a Fase 3 carrega a **obrigacao negativa** (o laco chama `renda_conta` e nunca as regras de par) — isso nao e "um requisito em duas fases", e a nota existe justamente para que ninguem conclua que e.
    - A lista de planos da Fase 3 (`:450-462`, escrita no planejamento) **confere com os arquivos que existem no disco**: quatro planos, com as ondas 1/2/3/2. Se um plano tiver sido acrescentado ou renumerado durante a execucao, a lista e corrigida aqui.
    - **Nao ha `TBD` neste arquivo e nao havia**: nem a lista nem o portao `'TBD' not in road` fazem parte desta tarefa. Os dois sairam nesta revisao — um portao que passa hoje, com a fase inteira por executar, nao mede nada.
    - O criterio 1 da Fase 3 **nao e reescrito**: ele ja foi corrigido em 2026-09-03 e a ressalva que derruba o `rich.Live` fica escrita — um numero que caiu precisa dizer que caiu.
    - **O criterio 4 ganha uma ressalva datada e tambem NAO e reescrito** (W1): o roadmap diz *"parado e o scanner nao estar vendo"* e o codigo desta fase faz o inverso (PARADO = vejo e o valor nao mudou; PAUSADO = nao consigo ver), porque o `03-CONTEXT.md:56-66` prevalece. A ressalva nomeia os dois rotulos, diz qual documento prevalece e por que, e fica **ao lado** do texto original — mesma forma da do criterio 1.
    - A ressalva do criterio 4 e **verificada**: um teste ou uma checagem executavel afirma que o paragrafo do criterio 4 e seguido de um bloco de citacao que nomeia PAUSADO. Sem isso a correcao e uma promessa em prosa.
    - Nenhuma outra secao do `ROADMAP.md` e tocada: as edicoes sao pontuais e o arquivo nao e reescrito inteiro.
  </behavior>
  <action>
Tres edicoes **pontuais**. O arquivo tem ~490 linhas e reescreve-lo inteiro para consertar quatro
tabelas e como um escritor monta do zero o que devia ter carregado — a forma exata do incidente de
2026-08-30 que `calibrar_renda.py:7-13` conta. **Use edicao com substituicao de trecho, nunca
reescrita do arquivo.**

<!-- planner-discipline-allow: Not started -->
**(1) `Progress`.** As duas primeiras linhas dizem *Not started, 0/?* sobre fases mescladas com
teste passando. Trocar por `5/5` e `4/4`, `Complete`, e a data que os SUMMARY carregam.
**O rotulo antigo nao pode sobrar nas TRES linhas da tabela** — a checagem e recortada na secao
`## Progress`, entao citar o rotulo em prosa fora dela (num SUMMARY, ou numa nota abaixo da
`Coverage`) e legitimo e nao derruba o portao. **Nao
inventar data**: se um SUMMARY nao a tiver, usar a do commit que o trouxe. A linha da Fase 3 fica
com o numero de planos deste conjunto e o status de execucao, que quem executa atualiza.

**(2) `Coverage`.** **Derivar a tabela do `REQUIREMENTS.md`**, e nao emendar a que esta la: uma
tabela emendada herda o erro que ninguem viu. Varrer os `- [ ] **XXX-NN**`, montar uma linha por
requisito e mapear:

- LEIT-01 a LEIT-09 -> Phase 1
- LEIT-11, REND-01 a REND-06, REND-08, REND-09, REG-01 a REG-04 -> Phase 2
- LEIT-10, CONS-01, CONS-02, CEGO-01, CEGO-02 -> Phase 3

O rodape diz o total **contado**, e a frase *"Nenhum orfao, nenhum em duas fases"* so fica se for
verdade depois da contagem. **LEIT-11 ganha uma nota de rodape**: a dona e a Fase 2 (as regras de
par sao a defesa principal) e a Fase 3 carrega a obrigacao **negativa** de nao chama-las — sao
lados diferentes da mesma fronteira, e nao duas donas.

**(3) A lista de planos da Fase 3** (`:450-462`) ja foi escrita no planejamento, no formato das
fases de cima. **Aqui ela e so CONFERIDA contra o disco** — quatro planos, ondas 1/2/3/2 — e
corrigida se a execucao tiver acrescentado ou renumerado algum.

> **O que esta edicao NAO e.** A versao anterior deste plano dizia que ela substituia uma linha
> `**Plans**: TBD`. **Essa linha nao existe**: `:450` ja diz `4 planos escritos`. A promessa era
> sobre um estado que o proprio planejamento ja tinha consertado, e o portao que a acompanhava
> passava com a fase inteira por executar. Os dois foram removidos nesta revisao. Conferir a lista
> contra o disco e o trabalho que sobra, e ele e real: e a execucao que pode renumerar um plano.

A lista esperada e:

- `03-01-PLAN.md` — o tracador: de um frame ao vivo a uma linha em `.renda/` e uma linha no
  console, mais `--renda` no `__main__` e o painel por intervalo (onda 1)
- `03-02-PLAN.md` — a cegueira declarada e o parado que grava: os quatro estados, `FonteRecuperavel`
  e o primeiro chamador de `esta_minimizada` (onda 2)
- `03-03-PLAN.md` — LEIT-10: `ler_um_campo`, a varredura de pisos vizinhos com o alcance medido, e
  a memoria que nao volta para o `calibration.json` (onda 3)
- `03-04-PLAN.md` — o lancador `vigiar-renda.bat` com os onze testes, e a higiene deste roadmap
  (onda 2)

**(4) A ressalva do criterio 4** (`:409-411`) — a W1, e a unica edicao NOVA desta revisao.
O criterio afirma *"parado e o scanner nao estar vendo"*, e o codigo desta fase faz o inverso:
PARADO e *vejo e o valor nao mudou* (grava) e PAUSADO e *nao consigo ver* (nao grava), porque o
`03-CONTEXT.md:56-66` decidiu assim e ele prevalece. **O criterio NAO e reescrito**: entra um
bloco de citacao logo abaixo dele, datado, no molde literal do que ja esta no criterio 1
(`:386-393`), dizendo duas coisas — (a) que a SUBSTANCIA do criterio esta cumprida, porque
valores bit-identicos ganham rotulo proprio e ele e distinguivel de renda zero na tela; e (b) que
os ROTULOS trocaram de lado, com a tabela dos dois e o documento que prevalece nomeado. Deixar o
roadmap sair desta fase afirmando o gloss trocado e pior que ele calar: o proximo leitor confia
nele e depura na direcao errada.

**O que NAO se toca**: o TEXTO dos criterios 1 e 4. As duas ressalvas ficam **ao lado** deles, e
nao no lugar deles, porque **um numero que caiu precisa dizer que caiu** — e essa e a
disciplina que este workstream aplicou cinco vezes na Fase 1. Um argumento refutado que some e um
argumento que volta.
  </action>
  <verify>
    <automated>python -c "
import re,pathlib
raiz=pathlib.Path('.planning/workstreams/renda')
req=set(re.findall(r'^- \[.\] \*\*([A-Z]+-[0-9]+)\*\*', (raiz/'REQUIREMENTS.md').read_text(encoding='utf-8'), re.M))
road=(raiz/'ROADMAP.md').read_text(encoding='utf-8')
cov=set(re.findall(r'^\| ([A-Z]+-[0-9]+) \|', road, re.M))
faltam=sorted(req-cov)
assert not faltam, f'requisitos fora da Coverage: {faltam}'
assert 'Not started' not in road.split('## Progress')[1].split('## Coverage')[0], 'Progress ainda diz Not started'
m=re.search(r'^4\. Valores bit-id', road, re.M)
assert m, 'o criterio 4 nao foi achado onde a ressalva deve encostar'
depois=road[m.end():m.end()+1400]
assert 'PAUSADO' in depois, 'o criterio 4 saiu sem a ressalva dos rotulos invertidos (W1)'
print('coverage ok:', len(req), 'requisitos; ressalva do criterio 4 presente')
"</automated>
  </verify>
  <done>
`Progress` reflete as cinco e as quatro plans mescladas; `Coverage` tem uma linha por requisito do
`REQUIREMENTS.md`, incluindo LEIT-10, LEIT-11, REND-08 e REND-09; o rodape diz o numero contado; a
lista dos quatro planos da Fase 3 confere com o disco; e o criterio 4 carrega a ressalva datada dos
rotulos invertidos, ao lado do texto original e nao no lugar dele.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| duplo clique -> processo | o `.bat` e a unica superficie que o usuario nao-desenvolvedor toca, e ele roda com os privilegios dele |
| `config.toml` -> `--janela` | a chave `[jogo] personagem` decide **de qual personagem** a renda e lida, e ela atravessa um `for /f` do `cmd` |
| `.bat` -> `.renda/` | o lancador pode, por uma flag descuidada, apontar a escrita para outro lugar ou alimentar o registro com uma gravacao antiga |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Spoofing | lancador subindo a instancia errada e gravando renda do personagem errado | high | mitigate | `--janela` **com valor**, montado da chave `[jogo] personagem` mais as constantes do CODIGO; teste afirmando que `--janela` nao vai pelado e que a mira vem da chave; e **nenhum `--personagem`**, que seria a segunda verdade |
| T-03-24 | Tampering | flag de destino de arquivo entrando pelo lancador e apontando a escrita para fora de `.renda/` | high | mitigate | Teste que varre dez nomes de flag de destino e recusa qualquer um no arquivo; a `.renda/` do usuario e dado acumulado e sem desfazer |
| T-03-25 | Tampering | `--replay` alimentando o registro com uma gravacao antiga como se fosse a tela de agora | high | mitigate | Mesma lista de flags recusadas. Uma noite antiga entrando como a de hoje envenenaria toda taxa dali para a frente |
| T-03-26 | Repudiation | o `.bat` anunciando um desfecho que nao conferiu | medium | mitigate | Blocos de erro **antes** da execucao e **nenhum `echo`** depois dela, com teste que ignora `REM` e tem controle positivo. Defeito medido em 2026-08-28 |
| T-03-27 | Repudiation | mensagem de recusa mandando consertar a feature errada | medium | mitigate | Teste nomeando `vigiar-party.bat`, `vigiar-mercado.bat` e `calibrar.bat` como proibidos e exigindo `calibrar-renda.bat`. Defeito medido em 2026-08-31 |
| T-03-28 | Elevation of Privilege | stub da Microsoft Store ou Python de fora do `.venv` executando o modo | medium | mitigate | A busca em cinco lugares guardando caminho completo e a rejeicao do stub sao copiadas **literais** do irmao, e a execucao usa `".venv\Scripts\python.exe"` por caminho literal |
| T-03-29 | Information Disclosure | `ROADMAP.md` afirmando cobertura que nao tem | low | mitigate | A `Coverage` passa a ser **derivada** do `REQUIREMENTS.md` e ha uma verificacao executavel que compara os dois conjuntos |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Nao se aplica: este plano nao instala pacote nenhum.** O `.bat` roda `pip install -r requirements.txt` no `.venv`, exatamente como os quatro lancadores que ja existem, e `requirements.txt` **nao e alterado** por esta fase |
</threat_model>

<verification>
1. `PYTHONPATH=".;.venv/Lib/site-packages" python -m pytest tests/test_vigiar_renda_bat.py tests/test_vigiar_mercado_bat.py -q` passa.
2. A suite inteira fecha em **>= 5778 passed**, com as mesmas 15 falhas pre-existentes.
3. A verificacao executavel da Tarefa 3 compara `REQUIREMENTS.md` com a `Coverage` e nao acha
   requisito de fora.
4. A checagem executavel da Tarefa 3 confirma que a secao `## Progress` do `ROADMAP.md` nao diz
   mais *Not started* — e a checagem e **recortada naquela secao**, e nao sobre o arquivo
   inteiro. Um `grep` do arquivo todo se auto-invalidaria: esta fase escreve, no proprio roadmap e
   nos SUMMARY, prosa que **cita** o rotulo antigo para dizer que ele caiu, e o portao passaria a
   acusar a documentacao da correcao. O criterio 4
   sai desta fase com a ressalva dos rotulos invertidos encostada nele (W1). **O `grep` por `TBD`
   que ocupava esta linha saiu**: nao ha `TBD` no arquivo e nao havia — ele passava hoje, com a
   fase inteira por executar.
5. `git diff --name-only` lista **tres** arquivos e nenhum deles esta em `l2scanner/`.
</verification>

<success_criteria>
- `vigiar-renda.bat` existe, e ASCII puro, sobe em dois cliques e e um processo separado.
- Os onze testes do lancador passam, com os dois controles positivos.
- Nenhuma mensagem manda rodar o lancador de outra feature, e o conselho aponta para
  `calibrar-renda.bat`.
- O `ROADMAP.md` deixa de dizer *Not started* sobre fases mescladas, a `Coverage` inclui LEIT-10,
  LEIT-11, REND-08 e REND-09, e a lista dos quatro planos da Fase 3 confere com o disco.
- O criterio 4 deixa de afirmar um gloss que o codigo contradiz: a ressalva datada nomeia PARADO e
  PAUSADO com o fato de cada um e diz qual documento prevalece — sem reescrever o criterio.
</success_criteria>

<human-check>
**O criterio 2 da fase nao tem teste automatizado, e nao deveria ter.**

*"Derrubar o `--renda` nao derruba o `vigiar-party.bat` nem o `vigiar-mercado.bat`, e derrubar
qualquer um deles nao derruba o `--renda`. Quatro invocacoes, quatro janelas, nenhuma dependendo da
outra para ficar de pe."*

Isso e uma propriedade do **sistema operacional** com quatro processos vivos e o jogo aberto, e
nenhum numero desta arvore a alcanca — e a mesma fronteira que `resumo_da_sessao` ja declara sobre
contencao entre processos. O portao e humano, e ele e de fim de fase:

1. Subir `vigiar-party.bat` (duas instancias), `vigiar-mercado.bat` e `vigiar-renda.bat`.
2. Fechar a janela do `--renda`. Conferir que as outras tres continuam de pe e continuam
   escrevendo.
3. Subir o `--renda` de novo e fechar a do mercado. Conferir que o `--renda` continua.
4. Conferir no Gerenciador de Tarefas que sao quatro processos `python.exe` distintos.
5. Olhar `.renda/` e conferir que ha o CSV do personagem **e** o `LEIAME.txt`, e que o CSV cresceu.
</human-check>

<output>
Create `.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-04-SUMMARY.md` when done
</output>