---
phase: 03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - l2scanner/renda_leitura.py
  - l2scanner/renda_pisos.py
  - l2scanner/renda_laco.py
  - l2scanner/renda_console.py
  - tests/test_renda_pisos.py
  - tests/test_renda_leit10.py
autonomous: true
requirements: [LEIT-10, LEIT-11, CONS-01]

estimate:
  tokens: 112000
  raw_tokens: 112000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Antes de declarar que um campo parou de sair, o laco tenta os pisos VIZINHOS e usa o primeiro que produzir leitura valida, **registrando qual piso usou** — na tela, no log e no bloco por intervalo (LEIT-10, CTX-6)."
    - "O ALCANCE DA VARREDURA SAI DA MEDICAO DO DESLOCAMENTO, e nao da `largura_da_banda` gravada (C-11). Medido: a banda do EXP da Faerlina andou de `140..170` para `160..180` em 8,5 h — **4 passos de 5**. As larguras gravadas sao 4/3/5, que dao meia-banda de 2/1/2 passos. **Andar so dentro da banda gravada NAO teria salvo o caso real**, e um alcance que nao alcanca o unico caso medido e um requisito cumprido no papel."
    - "A `largura_da_banda` continua sendo usada — como ORIGEM e nunca como limite: o piso gravado e o CENTRO da banda (`calibrar_renda.escolher_o_piso:504-516`) e a grade anda de `PASSO_DA_GRADE_DE_PISOS = 5`, entao os vizinhos legitimos sao `piso +/- 5k`."
    - "A varredura NAO RODA TODO TIQUE. Ela dispara depois de K recusas SEGUIDAS daquele campo, porque a recusa isolada e o estado normal e MEDIDO — 79% no nivel e 21% na adena — e varrer nela triplicaria o custo do tique para redescobrir que o piso esta certo. O modo de falha que LEIT-10 trata e o campo que **parou** de sair, e nao o que recusa de vez em quando."
    - "A varredura e POR CAMPO e nunca pelos tres de uma vez: o campo que recusou paga a varredura, e os outros dois nao pagam nada. E o que `ler_um_campo` existe para tornar possivel."
    - "A MEMORIA DO PISO QUE FUNCIONOU E ESTADO DO LACO, NUNCA DO LEITOR. `tests/test_renda_par.py:601-630` varre a AST de `renda_leitura.py` procurando `global` e literal mutavel de nivel de modulo, com controle positivo — um cache de piso dentro daquele arquivo deixa o portao **vermelho**. A Fase 1 definiu-se pura e continua pura."
    - "O piso que funcionou tenta PRIMEIRO no tique seguinte: depois de uma mudanca de cenario o regime estavel volta a UMA leitura por campo, e nao a nove. Sem essa memoria a varredura seria paga de novo a cada tique pelo resto da noite."
    - "O piso que funcionou **NAO e gravado de volta no `calibration.json`** (CTX-6). Um laco que reescreve a calibracao sozinho tira do usuario a unica ancora que ele tem para saber que a calibracao envelheceu. *Alternativa registrada:* auto-recalibrar — mais confortavel e muito mais dificil de auditar."
    - "`ler_um_campo` e extraida do helper interno `_ler` de `ler_os_tres_campos` e a leitura dos tres campos passa a DELEGAR a ela: nao ha uma segunda verdade sobre como um campo e recortado, mascarado e lido. As 662 leituras de teste da renda que passam hoje continuam passando, byte a byte."
    - "`largura_da_banda` e OPCIONAL no esquema (`calibracao.py:1530`) e calibracoes antigas nao a tem. A varredura tem um alcance de fabrica escrito e testado para esse caso, e ele NAO depende da chave ausente."
    - "O laco continua chamando `renda_conta` e nunca as regras de par (LEIT-11, C-8): a varredura muda o PISO com que um campo e lido, e nao a politica de aceitacao do par. O portao de `tests/test_renda_par.py:704` segue verde com os dois modulos novos na arvore."
    - "O marcador do piso NAO monta na linha do tique: com ele a linha mede **78** colunas e a largura real desta casa e **76** (medido no `03-01`). Ele sai no bloco por intervalo e numa linha de log com LATCH, uma vez por mudanca de piso."
  artifacts:
    - l2scanner/renda_pisos.py
    - tests/test_renda_pisos.py
    - tests/test_renda_leit10.py
  key_links:
    - "renda_laco -> renda_pisos.pisos_vizinhos: o planejador da varredura e PURO e devolve a sequencia de pisos a tentar, na ordem. O laco so itera e para no primeiro que sair. A decisao mora no puro; a execucao e o custo moram na casca."
    - "renda_laco -> renda_leitura.ler_um_campo: a porta de UM campo com UM piso. Ela e a razao de a varredura custar uma leitura de campo por tentativa em vez de tres."
    - "renda_leitura.ler_os_tres_campos -> renda_leitura.ler_um_campo: a delegacao que garante uma verdade so sobre recorte, mascara e piso. Sem ela a varredura teria de reimplementar `_ler`, e duas implementacoes do mesmo recorte e como elas divergem."
    - "renda_pisos NAO -> calibracao / cv2 / ocr: o planejador recebe `piso` e `largura_da_banda` como inteiros e devolve inteiros. Ele e testavel sem jogo, sem OCR e sem `calibration.json`."
    - "renda_laco NAO -> Calibracao.salvar: o piso que funcionou vai para a TELA e para o REGISTRO, nunca de volta para o disco (CTX-6). Ha teste de arvore de sintaxe afirmando que `renda_laco.py` nao chama `salvar`."
---

<objective>
Fechar o LEIT-10, que e desta fase e que o `ROADMAP.md` nao tinha listado: **antes de declarar que
um campo parou de sair, o laco tenta os pisos vizinhos da banda gravada e usa o que produzir
leitura valida, dizendo na tela qual piso usou**.

A razao esta medida tres vezes e ela e cruel com o usuario: a banda util do EXP da Faerlina andou
de `140..170` para `160..180` **em 8,5 horas**, so porque o cenario atras da barra semitransparente
mudou; e no dia seguinte a janela inteira subiu ~8 px e as tres regioes morreram de uma vez, com o
usuario vendo os tres campos recusados. Ele calibra numa area, muda de mapa, e o numero para de
sair **sem que nada tenha quebrado** — nem o calibrador esta errado, nem a leitura.

Na Fase 1 isso seria estado dentro de uma funcao que a fase inteira definiu como pura, e ha portao
de AST prendendo essa pureza. Por isso o dono e o laco: **a memoria do piso que funcionou e estado
do laco, e nunca do leitor**.

Purpose: sem isto, a unica resposta do produto a uma mudanca de area de farm e "recalibre" — e o
usuario so descobre isso de manha, com a noite perdida. Com isto, o laco atravessa a mudanca
sozinho e **diz que atravessou**, o que e diferente de esconder que a calibracao envelheceu.

Output: `ler_um_campo` extraida em `l2scanner/renda_leitura.py`, o planejador puro
`l2scanner/renda_pisos.py`, a varredura e a memoria por campo no laco, e o marcador do piso no
bloco por intervalo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-CONTEXT.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-RESEARCH.md
@.planning/workstreams/renda/phases/01-a-leitura-da-barra-pixel-vira-n-mero-ou-recusa/01-MEDICOES-DE-CAMPO.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-02-SUMMARY.md
</context>

> **Os IDs de decisao (CTX-1 a CTX-8, C-1 a C-11, P-1 a P-4) estao definidos no `03-01-PLAN.md`.**
> Este plano cita e nao redefine.

## O numero que decide o alcance — C-11, e ele contradiz a leitura mais obvia da calibracao

A semantica gravada, confirmada no fonte:

- `BandaUtil.largura` e `len(self.pisos)` (`calibrar_renda.py:457-459`) — **contagem de pisos na
  grade**, e nao amplitude em unidades de brilho.
- A grade anda de `PASSO_DA_GRADE_DE_PISOS = 5` (`calibrar_renda.py:189`), com a historia do passo
  10 que produziu uma conclusao errada escrita em `:179-188`.
- `escolher_o_piso` devolve `banda.pisos[banda.largura // 2]` — **o CENTRO** (`:504-516`), com a
  razao: *"Um piso na borda da banda funciona hoje e esta a uma mudanca de gamma de cair fora
  dela"*.

Logo o mapa dos vizinhos e exato — mas o **alcance** nao sai dele:

| campo | piso gravado | `largura_da_banda` | banda reconstruida | meia-banda |
|---|---|---|---|---|
| EXP (`barra_esquerda`) | 155 | **4** | 145, 150, **155**, 160 | 2 passos |
| adena (`barra_direita`) | 190 | **3** | 185, **190**, 195 | 1 passo |
| nivel (`nivel`) | 210 | **5** | 200, 205, **210**, 215, 220 | 2 passos |

**E o M-S mediu deslocamento de 4 passos.** Andar so dentro da banda gravada alcanca 1 a 2 passos —
**menos do que o unico caso medido**. Um alcance derivado da largura gravada seria um requisito
cumprido no papel e inutil no incidente que o motivou.

**Decisao: o alcance e 4 passos (`+/- 20` unidades de brilho), que e o deslocamento MEDIDO, e a
`largura_da_banda` e a ORIGEM da grade e nunca o limite dela.** A constante do alcance vai nomeada
no planejador com o numero medido e a procedencia (`01-MEDICOES-DE-CAMPO.md:423-435`) escritos ao
lado — nao e escolha, e medicao, e ela tem de dizer de onde veio.

**A ordem e do mais perto para o mais longe, alternando o sinal:** `+5, -5, +10, -10, +15, -15,
+20, -20`. Oito tentativas no pior caso, e ela para na primeira que sair. Do mais perto porque a
banda **anda**, nao salta: o piso certo de amanha e vizinho do de hoje. Alternando porque o M-S
mediu a banda subindo e o M-K/M-I mediram o cenario mudando nos dois sentidos — presumir a direcao
seria adivinhar metade do problema.

> **Incerteza A1 do Assumptions Log, e ela fica escrita no fonte.** A leitura "`largura_da_banda` e
> contagem de passos de 5 e o piso gravado e o centro" foi **derivada** de
> `calibrar_renda.py:457-459` + `:189` + `:504-516`, e **nao ha teste afirmando essa leitura de
> fora**. Se ela estiver errada, a varredura anda no passo errado e nao acha nada — e o sintoma
> seria "a varredura roda e nunca resolve". Por isso o `03-03` deixa o passo e o alcance nomeados e
> testados isoladamente no modulo puro: quando alguem descobrir o numero certo, muda-se um lugar.

## Por que a varredura NAO roda todo tique — e este e o custo real da fase

Medido no `03-CONTEXT.md:97-99`: uma leitura completa dos tres campos custa *"dezenas de ms a
alguns segundos (o OCR domina)"*. E medido na Fase 1: o **nivel recusa em 79% dos tiques**.

Varrer a cada recusa significaria, no campo mais fragil, oito leituras extras em quatro de cada
cinco tiques — para redescobrir, quatro de cada cinco vezes, que o piso gravado esta certo e o
campo simplesmente nao saiu naquele frame. Isso estoura o orcamento de tique e faz o console mentir
sobre a propria cadencia, que e o defeito que a compensacao de `mercado_modo.py:781-783` existe
para impedir.

**O gatilho e uma CORRIDA de recusas seguidas daquele campo.** A recusa isolada e o estado normal e
medido; o que LEIT-10 trata e o campo que **parou** de sair. `RECUSAS_SEGUIDAS_PARA_VARRER` e
constante nomeada com **"ESCOLHA E NAO MEDICAO"** escrito, no molde de
`JANELAS_IGUAIS_PARA_CONGELAR = 3` e de `CONGELADOS_SEGUIDOS_PARA_RELIGAR = 3`.

**E o custo do pior caso e medido e escrito, e nao estimado.** A Tarefa 3 manda o executor
cronometrar uma varredura completa de um campo contra a fixtura de campo e **escrever o numero no
fonte**. `OrcamentoDoTick` ja conta os estouros e o resumo ja os mostra — se a varredura estourar o
intervalo, o usuario ve.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: `ler_um_campo` — a porta de UM campo com UM piso, sem uma segunda verdade sobre o recorte</name>
  <files>l2scanner/renda_leitura.py, tests/test_renda_leit10.py</files>
  <read_first>
    - `l2scanner/renda_leitura.py:1322-1412` — `ler_os_tres_campos` inteira: a docstring (a leitura e POR PERSONAGEM e nunca cai no vizinho; cada regiao usa o SEU piso; os moldes sao desempacotados AQUI e nao dentro de `adena_da_barra`), a recusa por personagem ausente **antes de qualquer OCR**, o desempacotamento dos moldes com o `except ValueError` que degrada para conjunto vazio, e o helper `_ler(campo, subchave, leitor)`. **`_ler` e o que esta tarefa extrai.**
    - `l2scanner/renda_leitura.py:497-560` — `exp_da_barra(recorte, *, piso_de_brilho)`: pura, sem default de piso.
    - `l2scanner/renda_leitura.py:985-1040` — `nivel_da_regiao(recorte, *, piso_de_brilho)`.
    - `l2scanner/renda_leitura.py:1102-1160` — `adena_da_barra(recorte, *, piso_de_brilho, moldes, piso_de_leitura, margem_de_leitura, folga_de_cola)` — a unica das tres que precisa dos moldes.
    - `l2scanner/renda_leitura.py:1268-1300` — `CamposDaRenda`, e a razao de os tres tipos serem diferentes de proposito.
    - `l2scanner/renda_leitura.py` — as constantes `CAMPO_DO_NIVEL`, `CAMPO_DO_EXP`, `CAMPO_DA_ADENA`, `SUBCHAVE_DO_NIVEL`, `SUBCHAVE_DO_EXP`, `SUBCHAVE_DA_ADENA`, `ORDEM_DOS_CAMPOS` e `_recusar`. A funcao nova fala no vocabulario delas.
    - `tests/test_renda_par.py:601-630` — `TestOPortaoDaAusenciaDeMemoria`, que varre a AST de `renda_leitura.py` procurando `global` e literal mutavel de nivel de modulo, **com controle positivo**. A extracao nao pode introduzir nenhum dos dois.
    - `tests/test_renda_completa.py` — as leituras de fixtura que passam hoje. **Elas sao o teste de regressao desta tarefa**: a extracao e refatoracao pura e nenhuma delas pode mudar de resultado.
    - `l2scanner/calibracao.py:1126-1150` — `renda_do_personagem`, a proibicao de queda que mora num lugar so.
  </read_first>
  <behavior>
    - `ler_um_campo(frame, personagem=..., campo=CAMPO_DO_EXP, calibracao=...)` devolve exatamente o mesmo objeto que `ler_os_tres_campos(...).exp` para a mesma fixtura — teste de equivalencia sobre os **tres** campos.
    - `ler_um_campo` com `piso_de_brilho=` explicito usa **aquele** piso e ignora o gravado, e um piso diferente produz um resultado diferente na fixtura em que a banda e estreita.
    - `ler_um_campo` sem `piso_de_brilho` usa o piso gravado do bloco daquele campo — e o default e `None` (o sinal de "sem sobreposicao"), e nunca um numero.
    - `ler_um_campo` com personagem nao calibrado devolve a **mesma** recusa nomeada que os tres campos recebem hoje, **antes de qualquer OCR**.
    - `ler_um_campo` com a sub-chave ausente devolve a recusa que manda rodar o calibrador, identica a de hoje.
    - `ler_um_campo` com um campo desconhecido levanta `ValueError` nomeando os que existem — no molde de `ganho_do_passo` em `renda_conta.py:900-903`.
    - `ler_os_tres_campos` continua desempacotando os moldes **uma vez** e passando-os adiante: um teste conta as chamadas de `glifos_de_calibracao` numa leitura completa e afirma **1**, e nao 3.
    - `ler_um_campo` do campo da adena **sem** moldes passados desempacota por conta propria e produz o mesmo resultado — e o custo desse desempacotamento e medido e escrito num comentario.
    - O portao de ausencia de memoria continua verde: `renda_leitura.py` nao ganhou `global` nem literal mutavel de nivel de modulo.
    - Toda a suite de leitura da renda que passa hoje continua passando, com o mesmo numero de `passed`.
  </behavior>
  <action>
Refatoracao pura, e a unica regra e que **nenhum resultado pode mudar**. Comecar escrevendo o teste
de equivalencia — `ler_um_campo` contra `ler_os_tres_campos` sobre as fixturas de campo, nos tres
campos — e so depois mexer no fonte. Sem ele, a extracao passa e a mudanca silenciosa de
comportamento so aparece numa noite de farm.

**A ASSINATURA:** `ler_um_campo(frame, *, personagem, campo, calibracao, piso_de_brilho=None,
moldes=None)`.

- `piso_de_brilho=None` **nao e constante magica**: `None` e o sinal de "use o gravado", e nao um
  numero por omissao. A distincao e a mesma que `RecusaDaRenda` faz entre "recusou" e "nao mediu".
- `moldes=None` significa "desempacote por conta propria". `ler_os_tres_campos` continua
  desempacotando **uma vez** e passando adiante — o desempacotamento por campo triplicaria o custo
  do caminho de producao para servir a um caminho que roda raramente.

**O CORPO E O `_ler` DE HOJE**, com tres linhas a mais: resolver `entrada` por
`calibracao.renda_do_personagem(personagem)` (com a mesma recusa nomeada quando e `None`), mapear
`campo -> (subchave, leitor)` por uma tabela declarada de modulo, e usar `piso_de_brilho` quando ele
vier. **Nenhuma outra logica muda.** O `recortar` com guarda, a `RecusaDaRenda` que atravessa, o
`int(bloco["piso_de_brilho"])` — tudo igual.

**A tabela `campo -> (subchave, leitor)` e declarada e nao implicita**, pelo mesmo motivo que
`ORDEM_DOS_CAMPOS` e declarada: um `if/elif` de tres ramos e uma quarta verdade sobre quais campos
existem, e a quarta e a que esquece de ser atualizada.

**`ler_os_tres_campos` passa a DELEGAR**, e e isso que impede a segunda verdade. Ela continua sendo
a porta de producao — o `03-01` a chama pelo seam `ler_campos=` e nada disso muda. O que ela deixa
de ter e a implementacao do recorte; o que ela mantem e a ordem da tela, o desempacotamento unico
dos moldes e a recusa antecipada por personagem.

**O que esta tarefa NAO faz, e vai escrito na docstring:** ela nao acrescenta memoria, cache nem
estado a `renda_leitura.py`. O portao de AST de `tests/test_renda_par.py:601-630` varre este arquivo
com controle positivo, e a Fase 1 definiu-se pura por escrito. **A memoria do piso e da Tarefa 3, e
mora no laco.**
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_leit10.py tests/test_renda_completa.py tests/test_renda_par.py -q</automated>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest -k renda -q 2>&1 | tail -3</automated>
  </verify>
  <done>
`ler_um_campo` devolve, para os tres campos, exatamente o mesmo que `ler_os_tres_campos` devolve
hoje; `ler_os_tres_campos` delega e continua desempacotando os moldes uma vez; o portao de ausencia
de memoria segue verde; e nenhuma leitura de fixtura mudou de resultado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: `renda_pisos.py` — o planejador puro da varredura, com o alcance MEDIDO</name>
  <files>l2scanner/renda_pisos.py, tests/test_renda_pisos.py</files>
  <read_first>
    - `l2scanner/calibrar_renda.py:179-230` — `PASSO_DA_GRADE_DE_PISOS = 5`, a historia do passo 10 que produziu uma conclusao errada, e `pisos_da_grade`.
    - `l2scanner/calibrar_renda.py:420-470` — `BandaUtil` e `largura` como `len(self.pisos)`, com `LARGURA_MAXIMA_DE_BANDA_FRAGIL` e o aviso que existe **por medicao** (a banda de largura 1 da adena da Faerlina).
    - `l2scanner/calibrar_renda.py:504-540` — `escolher_o_piso`, que devolve **o centro**, e a razao escrita: *"Um piso na borda da banda funciona hoje e esta a uma mudanca de gamma de cair fora dela"*.
    - `l2scanner/calibracao.py:1520-1540` — a validacao de `largura_da_banda`: **opcional**, inteiro, `>= 1`. Calibracoes antigas nao a tem.
    - `.planning/workstreams/renda/phases/01-.../01-MEDICOES-DE-CAMPO.md:423-435` — o M-S: a banda do EXP da Faerlina de `140..170` para `160..180` em 8,5 h. **E a procedencia do alcance.**
    - `l2scanner/renda_conta.py:290-330` — o molde de constantes nomeadas com a razao ao lado (`GRANDEZA_DO_EXP`, `UNIDADE_DA_JANELA`, `SEGUNDOS_POR_HORA`), para copiar a forma da documentacao.
    - `l2scanner/recaptura.py:55-80` — como esta casa escreve um numero que e **escolha** contra um que e **medicao**.
    - A secao "O numero que decide o alcance" acima, com a tabela dos tres campos.
  </read_first>
  <behavior>
    - `pisos_vizinhos(piso=155, largura_da_banda=4)` devolve, na ordem: `160, 150, 165, 145, 170, 140, 175, 135` — do mais perto para o mais longe, alternando o sinal, com passo 5 e alcance 4.
    - A sequencia **nao inclui** o piso gravado: ele ja foi tentado, e repeti-lo custaria uma leitura para saber o que ja se sabe.
    - `pisos_vizinhos` com `largura_da_banda=None` (calibracao antiga) devolve **a mesma** sequencia: o alcance nao depende da chave ausente.
    - A sequencia e **truncada em 0 e em 255**: um piso negativo ou acima de 255 nao existe em imagem de 8 bits, e pedir a leitura com ele seria uma leitura garantidamente perdida.
    - `pisos_vizinhos(piso=3, ...)` nao devolve nenhum piso negativo, e a sequencia encurta em vez de levantar.
    - O alcance e somente-nomeado e **tem** valor de fabrica — porque ele e **medicao** e nao limiar de usuario, e a medicao mora num lugar so. Um teste afirma que o valor de fabrica e o do M-S e que ele e citado com procedencia num comentario.
    - Chamar com `passo` diferente produz a grade daquele passo — o parametro existe para o dia em que a leitura A1 se provar errada, e ha teste com `passo=10` provando que so um lugar muda.
    - O modulo nao importa `cv2`, `numpy`, `calibracao` nem `ocr`: um teste de fonte afirma isso. Ele recebe inteiros e devolve inteiros.
    - `renda_pisos.py` passa no portao de ausencia de memoria, com controle positivo.
  </behavior>
  <action>
Modulo pequeno, puro e com uma responsabilidade so: **dizer quais pisos tentar, e em que ordem**.
Ele nao le, nao captura, nao mede tempo e nao sabe o que e um campo — recebe um inteiro e devolve
inteiros.

**A funcao:** `pisos_vizinhos(*, piso: int, largura_da_banda: int | None, passo: int = PASSO,
alcance: int = ALCANCE) -> tuple[int, ...]`.

**As duas constantes, e a diferenca entre elas e o conteudo do modulo:**

- `PASSO_DA_GRADE = 5` — **importado** de `calibrar_renda`, e nao redefinido, se o import nao
  arrastar `cv2`. Se arrastar, e copiado com a razao escrita e um teste afirmando que os dois
  valores concordam, no precedente literal de `mercado_registro.py:65` (*"Duas definicoes do mesmo
  separador e como elas divergem"*). **O executor mede o custo do import e escreve o numero de
  modulos.**
- `ALCANCE_EM_PASSOS = 4` — **MEDICAO, e nao escolha**, e o comentario tem de dizer isso com todas
  as letras, citando `01-MEDICOES-DE-CAMPO.md:423-435`: a banda do EXP da Faerlina andou de
  `140..170` para `160..180` em 8,5 h. E logo abaixo, a frase que este numero existe para nao ser
  esquecida: **as `largura_da_banda` gravadas sao 4/3/5 e dao meia-banda de 2/1/2 passos, entao
  andar so dentro da banda gravada NAO teria salvo o caso real** (C-11).

**A `largura_da_banda` entra e nao governa.** Ela e recebida, documentada como **origem** da grade
(o piso gravado e o centro dela), e explicitamente **nao** usada como limite. Recebe-la e nao usa-la
como teto e deliberado: quem ler a assinatura vai perguntar por que, e a docstring responde. Omiti-la
faria a proxima pessoa reintroduzir o limite achando que estava consertando um esquecimento.

**A ordem, e ela e a metade do valor da funcao.** Do mais perto para o mais longe porque a banda
**anda** e nao salta — o piso certo de amanha e vizinho do de hoje. Alternando o sinal porque o
deslocamento foi medido subindo e a janela subindo ~8 px moveu as tres regioes, e presumir a
direcao seria adivinhar metade do problema. **A primeira que sair vence**, e por isso a ordem
decide quantas leituras o pior caso custa.

**O truncamento em `0..255` nao e defensividade: e aritmetica de imagem de 8 bits.** Um piso fora
dessa faixa produz uma mascara vazia ou cheia, ou seja uma leitura garantidamente perdida — e uma
tentativa que nao pode dar certo e tempo de OCR jogado fora num tique que ja esta em apuros.

**Escrever, na docstring do modulo, a incerteza A1** e o que a torna barata de consertar: a leitura
"largura e contagem de passos de 5, piso gravado e centro" foi derivada do fonte do calibrador e
**nao ha teste afirmando-a de fora**. Se ela cair, muda-se `PASSO_DA_GRADE` e o resto continua
valendo — e e por isso que o passo e parametro e nao literal espalhado.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_pisos.py tests/test_renda_par.py -q</automated>
    <automated>test $(grep -v '^\s*#' l2scanner/renda_pisos.py | grep -cE "^\s*(import cv2|import numpy|from \.ocr|from \.calibracao)") -eq 0</automated>
  </verify>
  <done>
`pisos_vizinhos` devolve oito vizinhos na ordem do mais perto ao mais longe, alternando o sinal,
truncados em 0 e 255, sem o piso gravado, e com o alcance de 4 passos citando a medicao que o
produziu; o modulo nao importa nada pesado; e ele passa no portao de ausencia de memoria.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: A varredura no laco — a memoria por campo, o que sai na tela e o que NAO volta ao disco</name>
  <files>l2scanner/renda_laco.py, l2scanner/renda_console.py, tests/test_renda_leit10.py</files>
  <read_first>
    - `l2scanner/renda_laco.py` — o laco inteiro como o `03-01` e o `03-02` o deixaram: o estado entre tiques, a sequencia do tique, o latch de transicao e o bloco por intervalo. **A varredura entra entre a leitura e a classificacao de estado.**
    - `l2scanner/renda_estado.py` — `classificar_a_visao` e o estado SEM LEITURA. **A varredura acontece ANTES dele**: e literalmente o "antes de declarar" do requisito.
    - `l2scanner/mercado_console.py:81-133` — `OrcamentoDoTick`, que ja mede p50/p95/maximo e conta os estouros. **E o instrumento que mede o custo da varredura sem escrever um segundo relogio.**
    - `l2scanner/mercado_pagina.py:789-814` — o LATCH de log, e a razao de ele existir: um aviso por tique vira ruido que o olho aprende a pular.
    - `l2scanner/renda_conta.py:1197-1225` — `ContagemDaRenda`, e o molde de "quatro fatos que nao se somam" que os contadores da varredura copiam.
    - `l2scanner/calibracao.py:940-960` — `Calibracao.salvar` e o que ela reemite. **E o que esta tarefa nao pode chamar** (CTX-6).
    - `l2scanner/calibrar_renda.py:1-30` — o incidente de 2026-08-30, em que um escritor montou do zero o que devia ter carregado e apagou treze moldes, tres ancoras e a grade de negociacao. **E o argumento inteiro de o laco nao escrever calibracao.**
    - A medicao de largura do `03-01`: a linha do tique com o marcador de piso mede **78** colunas e a largura real e **76**. Por isso o marcador **nao** monta na linha do tique.
    - As secoes "O numero que decide o alcance" e "Por que a varredura NAO roda todo tique" acima.
  </read_first>
  <behavior>
    - Um campo que recusa uma vez **nao** dispara varredura: a leitura do tique segue com uma chamada por campo, e o orcamento nao muda.
    - Um campo que recusa `RECUSAS_SEGUIDAS_PARA_VARRER` vezes dispara a varredura **daquele campo** e nao dos outros dois — teste que conta as chamadas de `ler_um_campo` por campo.
    - A varredura para na **primeira** leitura valida: com o piso `+5` funcionando, `ler_um_campo` e chamada duas vezes e nao oito.
    - A varredura que nao acha nada em nenhum dos oito vizinhos deixa a recusa original de pe, com o motivo original, e o estado vira SEM LEITURA — a varredura nao inventa uma recusa nova nem esconde a antiga.
    - O piso que funcionou e **lembrado por campo** e tentado **primeiro** no tique seguinte: dois tiques depois de a varredura vencer, `ler_um_campo` volta a ser chamada **uma** vez por campo.
    - O piso lembrado e **esquecido** quando ele proprio comeca a recusar: a serie recomeca do piso gravado, e nao do lembrado — senao a memoria viraria uma calibracao paralela que envelhece sem ninguem ver.
    - A mudanca de piso loga **uma vez** (latch), dizendo qual campo, qual piso gravado e qual piso em uso.
    - O bloco por intervalo mostra, para cada campo cujo piso saiu do gravado, o piso em uso e o desvio: `nivel: piso 220 (gravado 210, +10)`.
    - **O marcador de piso NAO aparece na linha do tique** — teste que mede a linha do tique no pior caso e afirma **<= 76 colunas**.
    - `renda_laco.py` **nao chama** `Calibracao.salvar` nem escreve em `calibration.json` — teste de arvore de sintaxe, com controle positivo (CTX-6).
    - O resumo da sessao traz quantas varreduras rodaram, quantas venceram e em qual desvio — tres fatos que nao se somam.
    - O laco continua sem chamar as regras de par: `conferir_o_par` e as tres irmas nao aparecem em `renda_laco.py` nem em `renda_pisos.py`, e `tests/test_renda_par.py` segue verde (LEIT-11, C-8).
    - Uma varredura de pior caso e cronometrada contra a fixtura de campo e o numero medido esta escrito num comentario do fonte.
  </behavior>
  <action>
Aqui a fase inteira encosta: a varredura mora **entre** a leitura do tique e a classificacao de
estado do `03-02` — que e literalmente o "antes de declarar cegueira" do requisito.

**O ESTADO NOVO DO LACO, e ele e pequeno:** por campo, `recusas_seguidas: int` e
`piso_em_uso: int | None`. Dois dicionarios de tres chaves, **fora do `while`**, no mesmo lugar
dos outros contadores. **Nao ha nada disto em `renda_leitura.py`** — o portao de AST daquele
arquivo e o que torna esta separacao obrigatoria, e ela tambem e a certa: a Fase 1 e uma funcao de
um frame, e memoria e do laco.

**O CAMINHO DO TIQUE, na ordem:**

1. Para cada campo, se ha `piso_em_uso` lembrado, a leitura do tique ja usa ele — via
   `ler_campos(..., pisos={campo: piso})`, ou chamando `ler_um_campo` por campo quando ha memoria.
   **O executor escolhe a forma medindo**: a chamada unica de `ler_os_tres_campos` continua sendo o
   caminho barato quando nao ha memoria nenhuma, e tres chamadas de `ler_um_campo` so se justificam
   quando ha. Escrever o numero.
2. Campo que saiu: `recusas_seguidas = 0`.
3. Campo que recusou: `recusas_seguidas += 1`. Abaixo do gatilho, **nada acontece** — e este e o
   caminho de 79% dos tiques do nivel.
4. No gatilho: itera `renda_pisos.pisos_vizinhos(piso=gravado, largura_da_banda=...)`, chamando
   `ler_um_campo` com cada um, e **para na primeira valida**. Vencendo, guarda `piso_em_uso`, zera a
   serie e loga uma vez. Perdendo, deixa a recusa original de pe.
5. `classificar_a_visao` recebe os campos **depois** da varredura. E por isso que LEIT-10 diz
   "antes de declarar".

**A MEMORIA TEM DATA DE VALIDADE, e isso e deliberado.** Quando o `piso_em_uso` lembrado comeca a
recusar, a serie recomeca do **gravado** e nao do lembrado. Sem isso a memoria viraria uma
calibracao paralela, invisivel, que envelhece sozinha — e a proxima mudanca de cenario seria varrida
a partir de um ponto que ninguem escolheu.

**O PISO QUE FUNCIONOU NAO VOLTA PARA O `calibration.json` (CTX-6).** Ele vai para a **tela**, para
o **log** e para o **bloco por intervalo**. A razao esta escrita no CONTEXT: *"Um laco que reescreve
a calibracao sozinho tira do usuario a unica ancora que ele tem para saber que a calibracao
envelheceu."* E ha uma segunda, medida nesta arvore: em 2026-08-30 um escritor de calibracao apagou
treze moldes de glifo, tres ancoras e a grade de negociacao, e o `calibration.json` e gitignored —
nao existe `git checkout` que traga nada de volta. Um escritor a mais naquele arquivo, rodando **a
noite inteira sem supervisao**, e a pior versao possivel daquele incidente.
*Alternativa registrada:* auto-recalibrar. Mais confortavel e muito mais dificil de auditar.
**Ha teste de arvore de sintaxe afirmando que `renda_laco.py` nao chama `salvar`.**

**ONDE O MARCADOR APARECE, e a medicao ja decidiu.** A linha do tique com o marcador colado mede
**78** colunas e a largura real desta casa e **76** (medido no `03-01`) — entao ele **nao** monta
ali. Ele sai em dois lugares:

- **uma vez por mudanca**, numa linha de log com latch: *"o campo do nivel voltou a sair com o piso
  220 (gravado 210, +10) — a banda de brilho andou, e a calibracao deste personagem esta
  envelhecendo"*;
- **no bloco por intervalo**, numa secao propria que so aparece quando ha algum campo fora do piso
  gravado. Uma secao vazia todo minuto seria ruido; uma secao que aparece e o aviso.

**A frase importa mais que o numero.** O usuario nao vai deduzir "recalibre" de `piso 220`; a linha
tem de dizer que a banda andou e que a calibracao esta envelhecendo. E o mesmo criterio de
`__main__.py:776-784`: a mensagem tem de dizer o que fazer a respeito.

**MEDIR O CUSTO E ESCREVER O NUMERO.** Cronometrar uma varredura completa (oito tentativas) de um
campo contra `tests/fixtures/renda/montagem_completa.png` e escrever o numero medido num comentario
ao lado de `RECUSAS_SEGUIDAS_PARA_VARRER`. `OrcamentoDoTick(limite=float(args.intervalo))` ja conta
os estouros e o resumo ja os mostra — se a varredura estourar o intervalo, o usuario ve, e o numero
no fonte diz se isso era esperado. Um custo estimado vira folclore na proxima fase; um medido nao.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_leit10.py tests/test_renda_laco.py tests/test_renda_cegueira.py tests/test_renda_par.py -q</automated>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --deselect tests/test_agenda.py -q 2>&1 | tail -3</automated>
    <automated>test $(grep -v '^\s*#' l2scanner/renda_laco.py | grep -c "\.salvar(") -eq 0</automated>
  </verify>
  <done>
Uma recusa isolada nao varre; uma corrida de recusas varre so aquele campo e para na primeira
leitura valida; o piso que venceu e tentado primeiro no tique seguinte e esquecido quando falha; a
mudanca sai uma vez no log e no bloco por intervalo, nunca na linha do tique; nada volta ao
`calibration.json`; o custo do pior caso esta medido e escrito; e a suite fecha em
**>= 5778 passed**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `calibration.json` -> varredura | o piso gravado e a `largura_da_banda` entram como origem da grade; o arquivo e compartilhado por quatro features e ja foi apagado uma vez |
| varredura -> leitura aceita | um piso diferente muda **o numero que sai**, e um numero errado e indistinguivel de um certo depois de gravado |
| laco -> `calibration.json` | a fronteira que este plano **fecha por construcao**: o laco le e nunca escreve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering | varredura aceitando um numero errado que o piso gravado teria recusado | **critical** | mitigate | A varredura muda **o piso** e nao a politica de aceitacao: `numero_valido`, a gramatica com trava, o conjunto de glifos e as recusas nomeadas da Fase 1 valem inteiros em cada tentativa. O piso em uso vai **gravado na tela e no log**, e ha coluna de guarda por campo no CSV desde a Fase 2 — a pergunta "as erradas eram as de piso movido?" continua respondivel depois |
| T-03-18 | Tampering | o laco reescrevendo `calibration.json` e apagando o trabalho de outra feature | **critical** | mitigate | O laco **nao escreve calibracao** (CTX-6), com teste de arvore de sintaxe e controle positivo. O incidente de 2026-08-30 apagou treze moldes, tres ancoras e a grade de negociacao, e o arquivo e gitignored |
| T-03-19 | Denial of Service | varredura por tique estourando o orcamento e afogando os tres processos | high | mitigate | Gatilho por **corrida** de recusas e nao por recusa isolada; varredura **por campo** e nao pelos tres; para na primeira valida; memoria do piso devolve o regime estavel a uma leitura por campo; e `OrcamentoDoTick` mede e mostra os estouros |
| T-03-20 | Repudiation | a leitura mudar de piso em silencio e ninguem saber por que o numero voltou | high | mitigate | Latch de log com a frase acionavel na mudanca, secao propria no bloco por intervalo, e os tres contadores de varredura no resumo da sessao |
| T-03-21 | Spoofing | memoria de piso virando calibracao paralela invisivel que envelhece sozinha | medium | mitigate | A memoria e **esquecida** quando o piso lembrado recusa, e a serie recomeca do **gravado** (T3) |
| T-03-22 | Tampering | refatoracao de `_ler` mudando o resultado de uma leitura em silencio | high | mitigate | Teste de equivalencia `ler_um_campo` x `ler_os_tres_campos` nos tres campos **escrito antes** da extracao, mais a suite de leitura da renda inteira como regressao (T1) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Nao se aplica: esta fase nao instala pacote nenhum.** Nenhuma dependencia nova, e `requirements.txt` nao e tocado |
</threat_model>

<verification>
1. A suite fecha em **>= 5778 passed**, com as mesmas 15 falhas pre-existentes e nenhuma nova.
2. `tests/test_renda_par.py` continua verde com `renda_pisos.py` e a varredura na arvore: o portao
   do chamador unico das regras de par nao os enxerga (LEIT-11).
3. `grep -c "\.salvar(" l2scanner/renda_laco.py` (sem comentarios) devolve 0.
4. Um teste mede a linha do tique no pior caso e afirma `<= 76` colunas.
5. O custo medido de uma varredura completa esta escrito num comentario do fonte, e nao estimado.
</verification>

<success_criteria>
- Antes de declarar que um campo parou de sair, o laco tenta os pisos vizinhos e usa o primeiro que
  produzir leitura valida.
- O alcance e de 4 passos, que e o deslocamento **medido**, e nao a meia-banda gravada de 1 a 2.
- O piso que funcionou aparece na tela e no log, e **nao** volta para o `calibration.json`.
- A varredura custa uma leitura de campo por tentativa, dispara so numa corrida de recusas, e o
  regime estavel volta a uma leitura por campo.
- `renda_leitura.py` continua puro e sem memoria, e nenhuma leitura de fixtura mudou de resultado.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-03-SUMMARY.md` when done
</output>
