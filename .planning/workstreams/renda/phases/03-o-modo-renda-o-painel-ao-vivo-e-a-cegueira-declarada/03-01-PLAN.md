---
phase: 03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/renda_laco.py
  - l2scanner/renda_console.py
  - l2scanner/__main__.py
  - tests/test_renda_laco.py
  - tests/test_renda_console.py
  - tests/test_renda_no_main.py
autonomous: true
requirements: [CONS-01, CONS-02, LEIT-11]

estimate:
  tokens: 118000
  raw_tokens: 118000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uma sequencia de frames falsos atravessa a fase inteira num caminho so: o laco captura, chama `renda_leitura.ler_os_tres_campos`, chama `renda_conta.passo_entre_campos`, soma em `ContagemDaRenda`, apenda UMA linha em `.renda/<apelido>.csv` por tique, emite UMA linha de log por tique e, no `finally`, fecha a fonte e imprime o resumo da sessao."
    - "`.renda/` DEIXA DE ESTAR VAZIO, e nasce com DOIS arquivos: o CSV do personagem e o `LEIAME.txt` que `RegistroDaRenda.__init__` escreve logo depois do `mkdir` (C-10). O criterio de verificacao conta dois, e nao um."
    - "O laco chama `renda_conta.passo_entre_campos` e NUNCA `conferir_o_par`, `o_exp_andou_para_tras`, `o_nivel_andou_para_tras` ou `a_adena_saltou_ordem_de_grandeza` (C-8, LEIT-11). O portao de `tests/test_renda_par.py:704` continua verde com o modulo novo na arvore, e ha teste desta fase afirmando isso pelo nome do modulo novo."
    - "A fonte de producao e `JanelaSource` com `capturar()` — NUNCA `capturar_completo()` (C-5). Ha teste de fonte varrendo `renda_laco.py` e afirmando que `capturar_completo` nao aparece no caminho do tique; sem isso CEGO-01 e CEGO-02 ficam estruturalmente impossiveis no `03-02`."
    - "A tela e `log.info` de texto puro: zero `print`, zero `\\r`, zero clear, zero `rich` (CTX-1, C-1). `renda_console` DEVOLVE texto e nunca imprime, no molde declarado em `mercado_console.py:1-10`, e ha teste de fonte prendendo as tres ausencias."
    - "A linha do tique MEDE 58 a 72 colunas e cabe nas 76 que o fonte desta casa cita como largura real de console (`LARGURA_DO_AVISO`, `mercado_console.py:748`). A linha densa de dez valores mede 87 e foi REFUTADA por medicao (C-2): as quatro taxas, o ETA e a duracao da sessao vao no bloco por intervalo, no precedente de `resumo_da_sessao` e `secao_do_vale_quanto`."
    - "As quatro taxas saem com `n` e recencia COLADOS nos numeros: `2,41 M XP/h (n=142, ha 3s)`. A taxa sem evidencia sai como o MOTIVO e nunca como numero, e as duas taxas (janela e sessao) saem JUNTAS — apresentar so uma mente por omissao, e o tamanho da mentira esta medido em 226 mil contra 466 mil adena/h."
    - "O bloco por intervalo mostra as taxas de recusa POR CAMPO, com os numeros medidos em campo (nivel 79%, adena 21%, EXP 0%). Um painel que as esconde faz o usuario concluir que o scanner travou — foi o que aconteceu em producao em 2026-09-01 com o aviso de layout do mercado."
    - "`--renda` existe em `__main__`, EXIGE `--janela` pelo mesmo motivo que `--mercado` exige, e e despachado ao lado dele, DEPOIS da calibracao e da resolucao do `--janela AUTO` (CONS-02)."
    - "A cadencia e `--intervalo` e a do bloco e `--status-a-cada`, as duas JA EXISTENTES no `__main__` (C-4, P-4). NENHUMA sexta chave entra na secao `[renda]` do `config.toml`."
    - "`montar_registro_da_renda(pasta, personagem)` existe em `__main__.py` no molde literal de `montar_registro_de_mercado`: devolve `None` e NUNCA levanta, porque `RegistroDaRenda.__init__` faz `mkdir` e `open` fora de qualquer `try` e diz por escrito que nao se defende."
    - "Os dois destinos de escrita sao montados ANTES da primeira captura, pela razao escrita em `mercado_modo.py:428-431`: descobrir que a pasta nao abre depois de a janela estar de pe custa uma sessao WGC por nada."
    - "O laco NAO LE O RELOGIO DA MAQUINA por conta propria: o carimbo vem de `relogio.agora_epoch()`, e o `relogio` entra por parametro nomeado com `montar_relogio(args)` como default de producao."
    - "O laco DEVOLVE codigo de saida e nunca levanta: `KeyboardInterrupt` vira log e cai no `finally`; dez excecoes seguidas de captura devolvem 1; portao de arranque devolve 2."
    - "As cinco alavancas de injecao existem e um teste de laco roda SEM jogo aberto e SEM OCR: `fonte=`, `ler_campos=`, `relogio=`, `pasta=` e `ticks_maximos=`. `ler_campos=` e o seam que o mercado nao tem e que a renda precisa (Achado 9), porque `renda_leitura` importa `ler_texto` por nome no topo do modulo."
    - "Nenhum arquivo do workstream `dashboard` foi tocado e nenhuma dependencia nova entrou: `rich` continua ausente do `requirements.txt` e sem import em `l2scanner/`."
  artifacts:
    - l2scanner/renda_laco.py
    - l2scanner/renda_console.py
    - tests/test_renda_laco.py
    - tests/test_renda_console.py
    - tests/test_renda_no_main.py
  key_links:
    - "renda_laco -> renda_leitura.ler_os_tres_campos: a leitura entra pelo parametro `ler_campos=`, com a funcao de producao como default. E o seam que torna o teste de laco possivel sem as bindings WinRT, e ele e um nivel acima do mercado, que injeta as duas leitoras de OCR por construtor de `LeitorDePagina`."
    - "renda_laco -> renda_conta.passo_entre_campos / contar_o_passo / as_duas_taxas / tempo_ate_o_nivel: a UNICA porta para a conta. As regras de par NAO sao chamadas daqui (C-8) — o portao de `tests/test_renda_par.py:704` afirma que o conjunto de chamadores e exatamente `{renda_conta.py}`, e um modulo novo chamando `conferir_o_par` deixa a suite vermelha."
    - "renda_laco -> renda_registro.RegistroDaRenda.registrar: UMA linha por tique, sempre, inclusive com campo recusado. A cegueira do `03-02` age NAO CHAMANDO este metodo (C-6); nada aqui precisa de coluna nova nem de portao novo."
    - "renda_laco -> __main__.montar_registro_da_renda / montar_relogio, por `_modulo_do_arranque()`: o mesmo trilho de `mercado_modo`, que existe para nao re-executar `l2scanner/__main__.py` sob um segundo nome e perder os manipuladores de log que `configurar_log` acabou de instalar."
    - "renda_laco -> captura_janela.JanelaSource com `capturar()`: NUNCA `capturar_completo()`. `renda_modo._frame_de_janela` e o precedente mais obvio e o ERRADO para o laco (C-5) — ele pula a classificacao de saude inteira, e sem `SaudeDoFrame` nao ha como distinguir FALHA de CONGELADO."
    - "renda_console -> console.moldurar / console.LARGURA: `LARGURA = 58` e PISO de `moldurar` (`max(LARGURA, ...)`, console.py:110) e nunca teto da linha ao vivo (C-2). O bloco cresce quando o texto e longo; nada precisa ser aumentado."
    - "renda_laco NAO -> rich: `rich` nao esta instalado, tem zero importacoes em `l2scanner/` e nao entra nesta fase (CTX-1). O `ROADMAP.md` e o `CLAUDE.md` que o citam estao velhos, e o criterio 1 sobrevive inteiro sem ele."
---

<objective>
Provar a fase inteira numa fatia fina e de producao: de um frame ao vivo ate uma linha em
`.renda/` e uma taxa por hora na tela — atravessando TODAS as camadas que a fase vai usar. O
arranque com portoes, os dois destinos de escrita antes da captura, a fonte por janela com
`capturar()`, a leitura da Fase 1, a conta da Fase 2, o disco, a linha do tique, o bloco por
intervalo e o `finally` com o resumo.

`renda_leitura`, `renda_conta` e `renda_registro` existem desde as Fases 1 e 2 **sem chamador de
producao, de proposito**. Este plano e o chamador. O precedente estrutural e literal e
`mercado_modo.py`, cuja propria docstring se descreve como *"o CHAMADOR que faltava"*.

Purpose: se o desenho do laco estiver errado, ele erra aqui, num commit, e nao depois de a
cegueira e a varredura de pisos estarem escritas em cima dele. E a fatia certa e a mais ordinaria
de todas — o jogo aberto, os tres campos saindo, a adena subindo — porque e ela que roda a noite
inteira.

A fatia NAO e prototipo: ela sai daqui com teste de ponta a ponta preso, com o `--renda`
despachado pelo `__main__` de verdade, e o que ela escreve e o esqueleto que o `03-02` e o `03-03`
expandem.

Output: `l2scanner/renda_laco.py`, `l2scanner/renda_console.py`, a flag `--renda` e
`montar_registro_da_renda` em `l2scanner/__main__.py`, e os tres arquivos de teste novos.

**O produto desta fase, do ponto de vista do usuario, e `.renda/` deixando de estar vazio** — e
ele deixa de estar vazio na Tarefa 1 deste plano.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/renda/STATE.md
@.planning/workstreams/renda/ROADMAP.md
@.planning/workstreams/renda/REQUIREMENTS.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-CONTEXT.md
@.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-RESEARCH.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-02-SUMMARY.md
@.planning/workstreams/renda/phases/02-a-conta-e-o-registro-de-duas-amostras-para-uma-taxa-e-uma-li/02-03-SUMMARY.md
</context>

## Como os quatro planos desta fase citam decisao

O `03-CONTEXT.md` organiza as decisoes em **Areas** e as correcoes em **#1 a #10**. A numeracao
`D-NN` pertence ao `PROJECT.md` deste repositorio e colidir com ela criaria duas leituras para o
mesmo simbolo — a mesma razao ja escrita no `02-01-PLAN.md`. Os quatro planos citam:

- **CTX-1 a CTX-8** — as decisoes travadas do `03-CONTEXT.md`, por Area.
- **C-1 a C-11** — as contradicoes, com os numeros que o proprio `<corrections>` lhes deu.
- **P-1 a P-4** — o que foi **medido neste planejamento** e nao estava em documento nenhum.

### As oito decisoes travadas (CTX)

| ID | Decisao | Onde |
|---|---|---|
| **CTX-1** | A tela e construida sobre `console.py`, na forma do `mercado_console`. **Sem dependencia nova**; `rich` nao entra | `03-CONTEXT.md:33-47` |
| **CTX-2** | A forma da tela decide-se **MEDINDO**, com os valores reais | `:48-52` |
| **CTX-3** | Tres estados — *lendo*, *cego*, *parado*. **"Parado" GRAVA e so muda a TELA**; so a cegueira suspende a gravacao | `:56-66` |
| **CTX-4** | Cegueira e **REUSO**: esta fase **classifica e nomeia**, nao detecta de novo | `:67-70` |
| **CTX-5** | "Coberto" **nao** e cegueira (medido); **minimizado continua impossivel** e e DECLARADO | `:71-74` |
| **CTX-6** | LEIT-10: pisos **vizinhos** antes de declarar cegueira, registrando qual usou; o piso que funcionou **nao volta** ao `calibration.json` | `:78-88` |
| **CTX-7** | `vigiar-renda.bat`, **processo separado**, `--janela` obrigatorio herdando `[jogo] personagem` | `:92-96` |
| **CTX-8** | A cadencia e calibrada, nao fixa — **corrigida pela C-4** | `:97-99` |

### As onze contradicoes, e onde cada uma vira tarefa

| ID | O que ela derruba | Plano |
|---|---|---|
| **C-1** | `linha_ao_vivo` **nao e repintada**: nao ha `\r`, nao ha clear, nao ha `print`. O laco faz `log.info` e a linha **ROLA**. "Ao vivo" e uma linha nova por tique | **Este plano, T1 e T3** |
| **C-2** | `console.LARGURA = 58` e **PISO**, nunca governou a linha ao vivo; a largura real citada no fonte e **76** | **Este plano, T1 e T3** |
| **C-3** | Os marcadores de descontinuidade sao **SETE**, e so `lacuna` e `relogio-andou-para-tras` exigem laco ao vivo | **Este plano, T1** |
| **C-4** | A chave de cadencia **nao existe e nao precisa existir**: `--intervalo` ja existe de graca | **Este plano, T2** |
| **C-5** | `renda_modo._frame_de_janela` usa `capturar_completo()` e **pula a saude inteira** | **Este plano, T1** |
| **C-6** | CEGO-01 e mais barato: **basta nao chamar `registrar`** | `03-02` |
| **C-7** | `esta_minimizada` existe e **nao tem chamador** | `03-02` |
| **C-8** | `tests/test_renda_par.py:704` exige que **so** `renda_conta.py` chame as regras de par | **Este plano, T1** (e reafirmado no `03-03`) |
| **C-9** | `Progress` e `Coverage` do `ROADMAP.md` desatualizados; **LEIT-10 falta na Coverage** | `03-04` |
| **C-10** | `.renda/` nasce com **`LEIAME.txt` alem do CSV** | **Este plano, T1** |
| **C-11** | O alcance da varredura do LEIT-10 decide-se pelos **4 passos medidos** e nao pela `largura_da_banda` gravada (3 a 5) | `03-03` |

> **Por que C-11 e nao C-4-de-novo.** O `<corrections>` chama esta de *"Contradicao #4 do meu
> lado, sobre a Area 3"*. Ela e a decima primeira da lista e reusar o numero 4 daria duas leituras
> ao mesmo simbolo.

### O que foi MEDIDO neste planejamento (P), e nao estava em documento nenhum

| ID | Medicao | Consequencia |
|---|---|---|
| **P-1** | **`geometria_da_janela` esta VAZIA (`{}`) no `calibration.json` real, nas duas instancias, HOJE.** O `03-RESEARCH.md` §1.3 afirma `{"largura": 1720, "altura": 1392}` *"conferido no arquivo real"* — **caiu** | O laco **nao pode** derivar a `Regiao` dela. Ver "A geometria da janela" abaixo |
| **P-2** | A linha de base da suite e **5778 passed** (`--deselect tests/test_agenda.py`), com **15 falhas pre-existentes** e 145 desselecionados, em 226 s | O piso e `passed`, nunca `passed + skipped` |
| **P-3** | `JanelaSource` nao expoe o hwnd, e `FonteRecuperavel.__getattr__` **encaminha tudo que nao comeca com `_`** (`recaptura.py:171-177`) | Uma `@property hwnd` em `JanelaSource` atravessa o envelope de graca — e do `03-02` |
| **P-4** | **`--status-a-cada` ja existe** (`__main__.py:3017-3023`, `dest="status_a_cada"`, default 30.0) | O bloco por intervalo herda a cadencia de graca, exatamente como `--intervalo`. Estende a C-4 |

## As quinze falhas pre-existentes, e por que elas nao sao suas

`tests/test_sessao.py`, `tests/test_janela_no_relogio.py` e `tests/test_respawn.py` somam **15
falhas** nesta arvore, no commit base, com a arvore limpa. A causa e uma so: `agenda.py` usa
`date.today()` e as fixturas estao ancoradas em 2026-08-30, entao elas quebraram sozinhas quando o
relogio virou para 09-03.

**Nao conserte, nao conte como suas, e nao as use para justificar nada.** O piso e
**5778 passed** e a comparacao e sobre `passed` — nunca `passed + skipped`. Somar as duas ja
aconteceu tres vezes neste workstream e cada vez escondeu um teste que tinha deixado de rodar.

Comando canonico:

    PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --deselect tests/test_agenda.py -q

## A medicao que decide a forma da tela (CTX-2, C-1, C-2)

Feita agora, com os valores reais do `03-CONTEXT.md:142-143`:

| Linha candidata | Colunas | Veredito |
|---|---|---|
| `renda \| nivel 67 \| EXP 79,2568% \| adena 17.592.060 \| LENDO` | **58** | **cabe** |
| `renda \| nivel 67 \| EXP 79,2568% \| adena 17.592.060 \| PARADO ha 4min12` | **69** | cabe |
| `renda \| nivel 67 \| EXP 79,2568% \| adena 17.592.060 \| SEM LEITURA (nivel)` | **72** | cabe |
| `renda \| PAUSADO: JOGO CAIU - TELA DE LOGIN \| nada gravado desde 03:12` | **69** | cabe |
| `renda \| ... \| LENDO \| piso do nivel +10` | **78** | **NAO cabe** — o marcador de piso do `03-03` **nao** monta na linha do tique |
| `nivel 67 \| EXP 79,2568% \| adena 17.592.060 \| 2,41 M XP/h \| 466 mil adena/h \| falta 3h20` | **87** | **REFUTADA** |

**A referencia e 76 e nao 58.** `LARGURA_DO_AVISO = 76` (`mercado_console.py:748`) diz por escrito
*"a largura em que ela cabe num console padrao de 80"*, e e o unico numero de largura de console
que este fonte cita com razao ao lado. `console.LARGURA = 58` so aparece em `console.py:110`,
dentro de `moldurar`, como `max(LARGURA, ...)` — **piso**. A linha ao vivo do mercado ja roda 84
colunas normalmente e 255 com o aviso de layout colado, e nunca passou por `moldurar`.

**Logo a forma e a que o repositorio ja tem, e ela e duas coisas e nao uma:**

1. **Uma linha por tique** (`log.info`, texto puro, rola) com **o que a tela do jogo AFIRMA** —
   nivel, EXP%, adena — mais o estado. Mede 58 a 72 colunas.
2. **Um bloco por intervalo** (`log.info("\n%s", ...)`, multi-linha) com **o que o programa
   CALCULOU** — as quatro taxas com `n` e recencia, o ETA, a duracao da sessao e as taxas de
   recusa por campo. O precedente e `resumo_da_sessao` (~25 linhas, `mercado_console.py:668`) e
   `secao_do_vale_quanto` (por intervalo, `SEGUNDOS_ENTRE_SECOES = 60.0`), com a razao escrita em
   `mercado_console.py:490-496`: *"Repintar um bloco de dezenas de linhas por segundo afogaria a
   linha ao vivo"*.

A divisao nao e estetica: ela e a mesma do `D-02` do `PROJECT.md`. A linha grava o que a barra
**afirmou**; o bloco mostra o que o programa **derivou**, e derivacao carrega `n` e recencia.

## A geometria da janela — P-1, e ela muda o arranque

O `03-RESEARCH.md` §1.3 manda copiar o `carimbo` do mercado:
`Regiao(0, 0, carimbo["largura"], carimbo["altura"])` a partir de
`renda_por_personagem[<nome>].geometria_da_janela`. **Medido agora: aquela chave esta `{}` nas
duas instancias.** Um `int(carimbo["largura"])` levanta `KeyError` no arranque de producao, e o
usuario ve um traceback em vez de um scanner.

**A geometria e MEDIDA da janela viva, e a gravada e usada como GUARDA e nunca como fonte:**

1. Monta `JanelaSource(titulo, Regiao(0, 0, 1, 1), relativa=True, minimum_update_interval=...)` —
   o molde de `renda_modo._frame_de_janela:263-286`, **e so essa metade dele**.
2. `capturar_completo()` **uma vez, no arranque**, so para medir `frame.shape` → `(altura, largura)`.
3. `fonte.apontar_para(Regiao(0, 0, largura, altura))` — legal porque `relativa=True`
   (`captura_janela.py:336-361` levanta `ValueError` quando nao e).
4. **Dali para a frente, `capturar()` e nunca mais `capturar_completo()`** (C-5).
5. Se `geometria_da_janela` tiver `largura` e `altura` e elas **discordarem** do medido, sai um
   aviso alto **uma vez**, com o texto de `calibrar_renda.avisar_sobre_a_geometria:1240-1271`
   como modelo: *"A JANELA MUDOU DE TAMANHO desde a ultima calibracao deste personagem — os
   retangulos antigos apontam para lugares diferentes nesta janela"*. E a causa numero um de "a
   leitura quebrou do nada", e o usuario nao tem como saber sozinho.

**Alternativa registrada:** exigir a geometria gravada e recusar o arranque com codigo 2 quando
ela falta. Recusada porque o `calibration.json` do usuario **hoje** a tem vazia, e um portao que
recusa a maquina em que a feature foi calibrada e um portao que so o autor consegue passar.

## Auditoria de cobertura das quatro fontes

Nenhum item fica sem plano. As linhas marcadas **FORA** trazem a exclusao escrita.

### GOAL — o objetivo da fase no `ROADMAP.md:364-367`

| Item do Goal | Plano |
|---|---|
| um lancador proprio sobe um **processo separado** | `03-04` (o `.bat`) + `03-01` (a flag e o despacho) |
| mostra ao vivo nivel, EXP%, adena | **`03-01` T1** (a linha do tique) |
| as quatro taxas | **`03-01` T3** (o bloco por intervalo) |
| o tempo ate o nivel | **`03-01` T3** |
| ha quanto tempo a sessao corre | **`03-01` T3** |
| diz **"pausado"** quando nao esta enxergando | `03-02` |
| diz **"parado"** em vez de afirmar renda zero | `03-02` |

### REQ — os requisitos da fase

| ID | Plano dono | Plano de apoio |
|---|---|---|
| CONS-01 | **`03-01` T3** (o painel inteiro) | `03-01` T1 (a linha), `03-02` (os estados na linha), `03-03` (o piso no bloco) |
| CONS-02 | **`03-01` T2** (a flag, o portao e o despacho) | `03-04` (o lancador de dois cliques) |
| CEGO-01 | `03-02` T2 | `03-01` T1 (a fonte com `capturar()`, sem a qual e impossivel) |
| CEGO-02 | `03-02` T1 e T3 | — |
| LEIT-10 | `03-03` | `03-02` (a cegueira que a varredura precede) |
| LEIT-11 | **`03-01` T1** (a fronteira de chamada: o laco chama `renda_conta` e nunca as regras de par) | `03-03` T1 |
| REND-07, ALER-01, CHAT-01/02 | **FORA** — v2, e o `03-CONTEXT.md:156-158` os defere | — |
| A ferramenta que mede a ponte de XP em laco | **FORA** — `03-CONTEXT.md:151-155` a defere explicitamente: *"nenhum requisito a pede e ela nao bloqueia nada"* | — |
| A pagina da renda no navegador | **FORA** — workstream `dashboard`, e nenhum arquivo dele e tocado | — |

### RESEARCH — cada achado da pesquisa vira tarefa

| Achado | Plano |
|---|---|
| Achado 1 — nao ha repintura; a tela e log que rola (C-1) | `03-01` T1 |
| Achado 2 — `LARGURA = 58` e piso; a linha ja roda 84–255 (C-2) | `03-01` T1, T3 |
| Achado 3 — `esta_minimizada` sem chamador (C-7) | `03-02` T2 |
| Achado 4 — `_frame_de_janela` pula a saude (C-5) | `03-01` T1 |
| Achado 5 — nao existe staleness de VALOR nesta arvore | `03-02` T1 |
| Achado 6 — duas behaviours para o jogo sumindo; escolher, nao inventar a terceira | `03-02` T2 |
| Achado 7 — sete marcadores de descontinuidade, dois exigem laco (C-3) | `03-01` T1 |
| Achado 8 — a cegueira nao precisa de coluna nova (C-6) | `03-02` T2 |
| Achado 9 — nao existe seam de injecao de leitura para a renda | **`03-01` T1** (`ler_campos=`) |
| Achado 10 — o portao de chamador do `test_renda_par.py` (C-8) | `03-01` T1 |
| §1.2 — a ordem literal do arranque, com os destinos de escrita antes da captura | `03-01` T1 |
| §1.4 — cadencia compensada, linha depois do processamento, motivos somados todo tique | `03-01` T1 |
| §1.6 — o `finally` com `fechar` + resumo, e o `Ctrl+C` que nao e erro | `03-01` T1 |
| §1.8 — o que e DO MERCADO e nao se copia | `03-01` T1 (declarado) |
| §5.1 e §5.2 — o estado entre tiques e a sequencia exata do tique | `03-01` T1 |
| §6 — o `.bat` e copia com quatro trocas | `03-04` T1 |
| §7 — o piso e parametro; o retry mora no laco | `03-03` |
| §8.1 — o idioma de teste de laco (`FonteFalsa`, `Namespace(intervalo=0.0)`, `ticks_maximos`) | `03-01` T1 |
| A1 do Assumptions Log (a semantica da banda) | `03-03` T2, com a incerteza escrita |
| A2 do Assumptions Log (jogo fechado vira `CONGELADO` em ~30 ticks) | `03-02` T2, com a incerteza escrita |

### CONTEXT — as oito decisoes travadas

| ID | Plano |
|---|---|
| CTX-1, CTX-2 | `03-01` T1 e T3 |
| CTX-3 | `03-02` T1 e T3 |
| CTX-4, CTX-5 | `03-02` T2 |
| CTX-6 | `03-03` |
| CTX-7 | `03-04` T1 |
| CTX-8 (corrigida pela C-4) | `03-01` T2 |

**Zero itens sem plano.**

## O que e DO MERCADO e NAO se copia, declarado antes de o executor abrir o arquivo

`mercado_modo.py` tem 801 linhas e o laco da renda e ~40% disso. O que **fica de fora**, com o
motivo, para nao entrar por inercia:

| Peca | Onde | Por que nao |
|---|---|---|
| `ModeloDeMercado`, `secao_do_vale_quanto`, `secao_da_margem` | `:466-486`, `:599-643` | e analise de preco; a renda nao tem "vale quanto" |
| `TravaDoDestaque`, `destaque_ao_vivo`, `vereditos_da_pagina` | `:648`, `:207-326` | trava de anuncio de oferta barata |
| `watchlist`, `receitas`, `ReceitaInvalida` | `:497-541` | filtros de item |
| `Catalogo` e `PAGINAS_ENTRE_GRAVACOES_DO_CATALOGO = 20` | `:105`, `:725-731` | o catalogo reescreve o arquivo INTEIRO; `RegistroDaRenda.registrar` abre-escreve-fecha por linha (`renda_registro.py:1032-1042`). Nao ha `gravar()` no `finally` da renda |
| `transicao_do_painel` e o latch de painel aberto | `:684-694` | "painel aberto" e estado do World Exchange; a barra de EXP esta **sempre** na tela, entao **a linha do tique da renda sai TODO tique** e nao atras de um portao |
| Portao de layout | `:402-418` | nao existe layout na renda |
| Dedup / indice de chaves | `mercado_registro` | **proibido**: `renda_registro.py:895-903` diz *"uma linha por tique E O PRODUTO, porque ela e o denominador da taxa"*, e ha portao de AST |

E **uma peca que o mercado nao tem e a renda precisa**: o mercado nunca chama
`fonte.estado_do_cliente()`; so o laco da party chama (`sessao.py:468-470`). CEGO-01 exige — e e
do `03-02`.

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: A fatia — de um frame ao vivo a uma linha em `.renda/` e uma linha no console</name>
  <files>l2scanner/renda_laco.py, l2scanner/renda_console.py, tests/test_renda_laco.py, tests/test_renda_console.py</files>
  <read_first>
    - `l2scanner/mercado_modo.py:1-10` — a docstring que se declara *"o CHAMADOR que faltava"*. E a mesma situacao, palavra por palavra.
    - `l2scanner/mercado_modo.py:88-152` — `SAIDA_RECUSADA = 2`, `SAIDA_CEGA = 1`, `MS_ENTRE_FRAMES_DO_MERCADO = 250` com a justificativa inteira em `:107-127` (*"250 E ESCOLHA, E NAO MEDICAO"*, e por que nao 1000: a deriva de fase dispara falso congelamento), `ERROS_SEGUIDOS_PARA_DESISTIR = 10`, e `Contagem` como molde de dataclass mutada no lugar.
    - `l2scanner/mercado_modo.py:154-205` — `_modulo_do_arranque()` e `_garantir_log()`, com as duas razoes inteiras: nao re-executar `l2scanner/__main__.py` sob um segundo nome, e nao instalar um segundo manipulador quando o `caplog` ja instalou o dele.
    - `l2scanner/mercado_modo.py:328-360` — a assinatura de `laco_do_mercado` e a docstring que diz *"OS PARAMETROS NOMEADOS SAO INJECAO DE DEPENDENCIA, e producao nao passa nenhum"* e *"`ticks_maximos=None` significa laco infinito, que e o caso de producao"*.
    - `l2scanner/mercado_modo.py:420-460` — a linha de arranque que conta o orcamento, e **os dois destinos de escrita ANTES da captura**, com o comentario de `:428-431`.
    - `l2scanner/mercado_modo.py:497-600` — config lida **uma vez** no arranque (`:522-524`: *"reler o `config.toml` por secao abriria corrida com o usuario editando o arquivo"*), `montar_relogio(args)`, a fonte, e os contadores/latches **fora** do `while`.
    - `l2scanner/mercado_modo.py:546-564` — o molde da fonte: o import de `captura_janela` **dentro** do `if fonte is None`, que e o que deixa a suite rodar sem as bindings WinRT.
    - `l2scanner/mercado_modo.py:652-801` — o tique inteiro, o `except KeyboardInterrupt` que **nao e erro**, e o `finally` com `fechar()` + resumo. Ler os comentarios de `:733-765` (por que a linha ao vivo sai DEPOIS do bloco de processamento) e de `:778-786` (a cadencia compensada, e por que nao `sleep(intervalo)` puro).
    - `l2scanner/renda_modo.py:263-286` — `_frame_de_janela`, que e o precedente mais obvio e o **ERRADO** para o laco (C-5). Ler para copiar **so** a metade do `Regiao(0,0,1,1)` + `relativa=True`, e **nunca** o `capturar_completo()` como caminho de tique. `calibrar_renda.py:991` importa `_frame_de_imagem` e `_frame_de_janela` deste modulo: **as duas nao podem sumir nem mudar de nome**.
    - `l2scanner/captura_janela.py:232-300` — o construtor de `JanelaSource`, `achar_janela` que levanta `JanelaNaoEncontrada`, e `_esperar_primeiro_frame` que levanta `RuntimeError` depois de 5 s com a mensagem sobre janela minimizada.
    - `l2scanner/captura_janela.py:326-441` — `capturar_completo()` (sem `saude`) contra `capturar()` (com `saude` classificada sobre o recorte), e `apontar_para`, que levanta `ValueError` quando `relativa=False`.
    - `l2scanner/frames.py:28-75` — `SaudeDoFrame` com os tres valores, `LIMIAR_BRILHO_MINIMO = 8.0`, `FRAMES_IDENTICOS_PARA_CONGELADO = 30`, `Frame` com `eq=False` e a propriedade `utilizavel`.
    - `l2scanner/renda_leitura.py:1268-1300` — `CamposDaRenda`, com os tres tipos DIFERENTES de proposito e a propriedade `por_campo` na ordem da tela.
    - `l2scanner/renda_leitura.py:1322-1412` — `ler_os_tres_campos`: a assinatura, o `entrada = calibracao.renda_do_personagem(personagem)` que **nunca cai no vizinho**, o desempacotamento dos moldes **uma vez**, e o helper `_ler` que resolve retangulo e piso.
    - `l2scanner/renda_conta.py:195-290` — os **SETE** marcadores de descontinuidade (C-3), `DESCONTINUIDADES_DO_TEMPO`, `DESCONTINUIDADES_QUE_EXCLUEM`, `DESCONTINUIDADES_DE_PROCEDENCIA` e `SEM_DESCONTINUIDADE`.
    - `l2scanner/renda_conta.py:638-700` — `passo_entre_campos`, somente-nomeados e **sem default**, e a docstring que **recusa por antecipacao** chamar `a_adena_saltou_ordem_de_grandeza` solta daqui (C-8).
    - `l2scanner/renda_conta.py:1197-1254` — `ContagemDaRenda` e `contar_o_passo`, mutada no lugar e devolvendo `None` de proposito.
    - `l2scanner/renda_registro.py:895-1010` — `RegistroDaRenda`: o construtor que **nao se defende**, o `escrever_leiame` logo depois do `mkdir` (C-10), e `registrar` que devolve `bool`, **nunca levanta**, e no primeiro `OSError` desliga `self.ligado` para a sessao inteira.
    - `l2scanner/mercado_console.py:1-10` — *"ELAS DEVOLVEM TEXTO E NUNCA IMPRIMEM (...) Quem imprime e o laco, com `log.info`"*.
    - `l2scanner/mercado_console.py:81-133` — `OrcamentoDoTick` inteiro (`registrar`, `p50`, `p95`, `maximo`, `estouros`), que a renda usa sem uma linha mudada.
    - `l2scanner/mercado_console.py:160-217` — `AVISO_DO_LAYOUT_RECUSADO` (168 colunas numa string so) e `linha_ao_vivo`, que **nao importa `LARGURA`, nao chama `moldurar`, nao trunca e nao quebra** (C-2).
    - `l2scanner/console.py:79-147` — `_pintar`, `moldurar` com o `max(LARGURA, ...)` de `:110`, e `destacar`, cujo `_ESTILO.get(tipo, ...)` aceita `None` e cai no default de ciano (a renda nao tem `TipoDeEvento`).
    - `l2scanner/relogio.py:80-170` — `Relogio`, `agora_epoch()` (epoch float — e este que o carimbo usa), `agora()` (datetime) e `confiavel`.
    - `tests/test_mercado_modo.py:110-240` — o idioma inteiro de teste de laco: `FonteFalsa` com `StopIteration` e `self.fechada`, `argumentos(**extras)` com `intervalo=0.0`, a chamada com as alavancas, e o console conferido por `caplog.at_level(logging.INFO)`.
    - `tests/test_renda_par.py:98-101` e `:704-735` — `O_UNICO_CHAMADOR = "renda_conta.py"`, `A_COMPOSICAO = "conferir_o_par"`, e o portao inteiro (C-8). O modulo novo **nao pode** aparecer naquela lista.
    - `tests/test_renda_conta_tracer.py` — o construtor de `CamposDaRenda` a mao que os 286 casos da Fase 2 ja usam. A sequencia de teste deste plano e montada com ele, e nao com PNG.
    - A secao "A medicao que decide a forma da tela" e "A geometria da janela" acima.
  </read_first>
  <behavior>
    - Quatro frames falsos com EXP e adena subindo produzem **quatro** linhas de dado em `.renda/<apelido>.csv`, alem do cabecalho e do `LEIAME.txt` ao lado.
    - A primeira amostra da sequencia sai marcada `ancora` na coluna `descontinuidade`, e as tres seguintes saem com a coluna vazia.
    - Duas amostras separadas por mais de `lacuna_maxima_segundos` de carimbo produzem `lacuna`; um carimbo que anda para tras produz `relogio-andou-para-tras`. **Sao os dois unicos marcadores que um laco ao vivo produz** (C-3), e os dois tem teste aqui.
    - Um tique com o nivel recusado e o EXP e a adena lidos **grava a linha mesmo assim**, com o motivo na coluna do nivel — a fase nao constroi segundo portao de admissao.
    - `contagem.aceitas` cresce so nos passos aceitos, e `contagem.recusadas_por_motivo` cresce **todo tique** que teve recusa, e nao so nos aceitos.
    - A linha do tique sai **todo tique** (a barra de EXP esta sempre na tela; nao ha analogo do latch de painel aberto), com os tres valores que a tela do jogo afirma, e mede **58 a 72 colunas** com os valores reais.
    - A linha do tique sai **depois** do bloco de processamento: ela mostra o estado DESTE tique, e nunca o do anterior.
    - Um campo recusado aparece na linha do tique como `--` com o motivo abreviado, e nunca como espaco vazio nem como zero.
    - Uma fonte que levanta `StopIteration` encerra o laco **limpo**, com codigo 0, e o `finally` fechou a fonte.
    - Uma fonte que levanta `Exception` dez vezes seguidas devolve **1**; um unico sucesso no meio **zera** o contador e o laco continua.
    - `KeyboardInterrupt` no meio do tique vira `log.info("Encerrado pelo usuario")`, cai no `finally` e devolve **0**.
    - O resumo da sessao sai no `finally` **sempre**, inclusive quando a sessao nao gravou nenhuma linha, e ele nomeia o arquivo de `.renda/`.
    - Nenhum `print`, nenhum `\r`, nenhum `end=`, nenhum `flush=` e nenhum `import rich` aparece em `renda_laco.py` nem em `renda_console.py` — teste de fonte, com controle positivo.
    - `capturar_completo` aparece **no maximo uma vez** em `renda_laco.py`, e ela esta no arranque (medir a janela) e nunca no corpo do tique — teste de arvore de sintaxe, com controle positivo.
    - Nenhuma das quatro regras de par (`conferir_o_par`, `o_exp_andou_para_tras`, `o_nivel_andou_para_tras`, `a_adena_saltou_ordem_de_grandeza`) e chamada de `renda_laco.py` — e o portao de `tests/test_renda_par.py` continua verde com o modulo novo na arvore.
    - `renda_console` nunca importa `logging` para emitir: as funcoes devolvem `str` e quem chama e o laco.
  </behavior>
  <action>
Comecar pelo teste, e comecar pelo teste **do fim**: a sequencia de quatro frames falsos que sai em
quatro linhas no disco. Sem ele, o executor escreve trezentas linhas de arranque antes de descobrir
se as camadas encaixam.

**O SEAM DE LEITURA, e ele e a decisao que torna o teste possivel (Achado 9).**
`renda_leitura.py:66` faz `from .ocr import ler_texto, ler_texto_ampliado` — import de nome no topo
do modulo, chamado direto. Nao ha parametro `ler_texto=` como o `LeitorDePagina` do mercado tem.
Logo o seam sobe **um nivel**: `laco_da_renda` recebe `ler_campos=renda_leitura.ler_os_tres_campos`
como parametro nomeado com default de producao, e o teste passa uma funcao que devolve
`CamposDaRenda` montados a mao — os mesmos que `tests/test_renda_conta_tracer.py` ja constroi. A
alternativa e monkeypatchar `l2scanner.renda_leitura.ler_texto`, que esta casa evita e que faria o
teste de laco depender do formato interno de outro modulo.

**A ASSINATURA**, no molde de `laco_do_mercado:328-340` e com as cinco alavancas nomeadas:
`laco_da_renda(args, cal, *, fonte=None, ler_campos=None, relogio=None, pasta=None,
ticks_maximos=None)`. Producao nao passa nenhuma. `ticks_maximos=None` e o laco infinito.
**Devolve o codigo de saida e nunca levanta**, e a tabela de codigos vai na docstring: `0` fechou,
`1` rodou cego (dez excecoes seguidas), `2` recusa de configuracao.

**A ORDEM DO ARRANQUE, e ela e a mesma ordem do mercado porque cada passo tem uma razao:**

1. `principal = _modulo_do_arranque()` e `_garantir_log(principal)` — copiados literais, com a
   docstring que explica o `hasattr` (pergunta por CAPACIDADE, e nao por nome de arquivo). O
   `hasattr` da renda pergunta por `montar_registro_da_renda`, que a Tarefa 2 cria.
2. **O portao do personagem.** O nome sai do titulo por `cliente.nome_do_personagem(args.janela)`,
   como `renda_modo.py:330` ja faz — nunca de `--personagem`, que seria a segunda verdade sobre a
   mesma janela. Sem nome: recusa nomeada, codigo 2.
3. **O portao da calibracao.** `cal.renda_do_personagem(personagem)` e `None` → recusa nomeada
   dizendo quem esta calibrado e mandando rodar `calibrar-renda.bat`, codigo 2. Reusar o texto de
   `renda_leitura.py:1355-1364`, que ja escreveu essa frase — nao escrever a segunda.
4. **Os ajustes, lidos UMA VEZ:** `config.ler_ajustes_da_renda()`. Reler por tique abriria corrida
   com o usuario editando o arquivo. Os cinco valores viram variaveis locais do laco.
5. **Os destinos de escrita, ANTES da captura:** `registro = principal.montar_registro_da_renda(
   pasta, personagem)`. `None` → recusa, codigo 2. A razao esta em `mercado_modo.py:428-431` e vale
   inteira aqui.
6. **A linha de arranque**, com o que o usuario precisa para saber que subiu certo: o personagem, o
   arquivo de destino, a cadencia e o intervalo do bloco.
7. `relogio = relogio or principal.montar_relogio(args)`.
8. **A fonte**, e ela e a peca em que o executor tem mais chance de errar. Ver "A geometria da
   janela" acima: `Regiao(0,0,1,1)` + `relativa=True` para medir, `apontar_para` para mirar, e
   `capturar()` dali em diante. O import de `captura_janela` mora **dentro** do `if fonte is None`.
   `minimum_update_interval=MS_ENTRE_FRAMES_DA_RENDA`, constante propria deste modulo com a mesma
   razao escrita do mercado — e escrevendo que ela e **escolha e nao medicao**.
9. **Os contadores e o estado entre tiques, FORA do `while`:** `anterior: CamposDaRenda | None`,
   `carimbo_anterior: float | None`, `passos: list[PassoDaRenda]`, `contagem = ContagemDaRenda()`,
   `orcamento = OrcamentoDoTick(limite=float(args.intervalo))`, `erros_seguidos`, `ticks`,
   `proximo_bloco = time.monotonic() + float(args.status_a_cada)`.

**A SEQUENCIA DO TIQUE**, e ela e a de `03-RESEARCH.md` §5.2 sem uma linha inventada:
captura com o mesmo tratamento de `StopIteration` / `Exception` do mercado; `campos =
ler_campos(frame.pixels, personagem=personagem, calibracao=cal)`; `agora =
relogio.agora_epoch()`; `passo = passo_entre_campos(anterior, campos, carimbo_anterior=...,
carimbo=agora, fator_de_salto=..., limiar_de_lacuna_em_segundos=...)`; `contar_o_passo(passo,
contagem)`; `registro.registrar(campos, carimbo=agora, descontinuidade=passo.descontinuidade,
origem_do_ganho=ORIGEM_INDETERMINADA)`; `passos.append(passo)`; `anterior, carimbo_anterior =
campos, agora`; a linha do tique por `log.info("%s", ...)`; o bloco por intervalo; o orcamento; e a
cadencia compensada.

**`registrar` devolve `bool` e nunca levanta.** Quando devolve `False` o laco conta uma perdida e
segue — o disco cheio nao pode derrubar o modo. Somar perdidas com aceitas apagaria a pergunta,
pelo mesmo argumento de `Contagem` no mercado.

**`renda_console.py` nasce nesta tarefa com UMA funcao:** `linha_do_tique(campos, contagem, *,
estado=None)`, que devolve `str` e nunca imprime. A grafia dos tres numeros e a que `renda_modo`
ja escreveu — `_grafia_do_exp`, `_grafia_da_adena`, `_grafia_do_nivel` (`renda_modo.py:177-214`).
**Importar as tres, nao reescrever**: duas grafias do mesmo numero e como o terminal e o monitor
deixam de ser comparaveis, que e o criterio 1 da Fase 1. Se o import arrastar `argparse` e `cv2`
por tabela, mover as tres para `renda_console.py` e reimportar de la em `renda_modo.py` — uma
verdade, num lugar, e o executor escolhe qual lugar **medindo o custo do import** e escrevendo o
numero.

O parametro `estado=None` nasce **nesta** tarefa e fica sem uso ate o `03-02`, com "AINDA NAO FAZ
NADA" escrito ao lado, no precedente literal das cinco chaves que o `02-01` deixou lidas e sem
consumidor. Ele existe agora para que o `03-02` nao precise mexer na assinatura.

**A docstring do modulo abre declarando as tres coisas que ele NAO e**, porque as tres sao a
tentacao: nao e `rich` (CTX-1, e `rich` nao esta instalado); nao e painel repintado (C-1, a linha
rola); e nao usa `capturar_completo` no tique (C-5, e sem `capturar()` a fase seguinte fica
impossivel).
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_laco.py tests/test_renda_console.py tests/test_renda_par.py -q</automated>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --deselect tests/test_agenda.py -q 2>&1 | tail -3</automated>
    <automated>grep -v '^#' l2scanner/renda_laco.py l2scanner/renda_console.py | grep -c "import rich" ; test $(grep -v '^#' l2scanner/renda_laco.py l2scanner/renda_console.py | grep -c "import rich") -eq 0</automated>
  </verify>
  <done>
`.renda/` existe com o CSV do personagem e o `LEIAME.txt`; quatro frames falsos produzem quatro
linhas de dado; o portao de `tests/test_renda_par.py` segue verde com `renda_laco.py` na arvore;
`capturar_completo` nao aparece no corpo do tique; a linha do tique mede 58 a 72 colunas; e a
suite fecha em **>= 5778 passed** com as mesmas 15 falhas pre-existentes e nenhuma nova.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: `--renda` no `__main__` — a terceira invocacao, e o montador que faltava</name>
  <files>l2scanner/__main__.py, tests/test_renda_no_main.py</files>
  <read_first>
    - `l2scanner/__main__.py:3072-3080` — a flag `--mercado`, com o texto que declara o que ela **nao** faz (nao vigia a party, nao envia alerta nenhum). O `--renda` copia essa forma.
    - `l2scanner/__main__.py:3101-3106` — `if args.mercado and not args.janela: parser.error(...)`, com a razao escrita. A razao da renda e **outra** e vai escrita: EXP e adena sao **do personagem**, e com duas instancias na mesma maquina sem mira o scanner le a instancia errada.
    - `l2scanner/__main__.py:3204-3207` — o despacho do `--mercado`, com o comentario que explica por que ele fica **AQUI** e nao junto do `--so-agenda`: *"ao contrario da agenda, este modo OLHA para a tela, e sair antes duplicaria a verdade sobre como a janela e escolhida"*.
    - `l2scanner/__main__.py:3011-3023` — `--intervalo` (com `INTERVALO_PADRAO = 1.0`) e `--status-a-cada` (`dest="status_a_cada"`, default 30.0). **As duas ja existem e o `--renda` as herda de graca** (C-4, P-4).
    - `l2scanner/__main__.py:573-650` — `montar_registro_de_mercado` inteiro: o trilho `try` que devolve `None` e nunca levanta, o `log.error` que diz **por que** a feature desligou, e o parametro `pasta` que existe para o teste apontar `tmp_path` sem tocar o `.mercado/` do usuario.
    - `l2scanner/__main__.py:651-694` — `montar_catalogo_de_mercado`, o segundo exemplar do mesmo trilho, e o comentario que diz que ele e identico e por que.
    - `l2scanner/__main__.py:695-720` — `montar_relogio(args)`.
    - `l2scanner/renda_registro.py:144-152` — `PASTA_DA_RENDA`, `ARQUIVO_DO_LEIAME`, `ORIGEM_INDETERMINADA`.
    - `l2scanner/renda_registro.py:911-940` — a docstring que diz, com todas as letras, que **o construtor nao se defende** e que quem envolve tudo num `try` e a montagem, pelo trilho de `montar_gravador`. E o motivo de esta tarefa existir.
    - `l2scanner/renda_registro.py:514-528` — `arquivo_do_personagem` e `loot.apelido`, o slug por lista de PERMISSAO que torna um nome de personagem seguro para virar nome de arquivo.
    - `tests/test_mira_da_janela.py:300-340` — o idioma de teste que este arquivo copia para provar que a mira e obrigatoria.
  </read_first>
  <behavior>
    - `python -m l2scanner --renda` **sem** `--janela` recusa com mensagem do `argparse` e **nao** sobe; a mensagem diz por que (duas instancias, e renda e do personagem).
    - `--renda` com `--janela` despacha para `renda_laco.laco_da_renda(args, cal)` e devolve o codigo dele.
    - O despacho do `--renda` acontece **depois** da carga da calibracao e **depois** da resolucao de `--janela AUTO`, e ao lado do `--mercado`.
    - `--renda` **nao** liga a vigilancia da party, **nao** monta o notificador e **nao** envia alerta nenhum: um teste afirma que o caminho do `--renda` nao alcanca `laco_principal`.
    - `args.intervalo` chega ao laco com o default `1.0` e `args.status_a_cada` com `30.0`, sem nenhuma chave nova no `config.toml` — teste de fonte afirmando que a secao `[renda]` continua com **cinco** chaves (C-4).
    - `montar_registro_da_renda(pasta, personagem)` devolve um `RegistroDaRenda` no caminho feliz.
    - `montar_registro_da_renda` com uma pasta impossivel (arquivo no lugar do diretorio, ou permissao negada) devolve **`None`**, loga o motivo em `error` e **nunca levanta**.
    - `montar_registro_da_renda` com um arquivo de renda corrompido (`ContratoDaRendaQuebrado`) devolve `None` e o motivo cita o arquivo, para o usuario saber qual mover.
    - `montar_registro_da_renda(None, personagem)` usa `PASTA_DA_RENDA`; com `pasta` explicita usa a pasta dada e **nunca** toca a `.renda/` do usuario.
  </behavior>
  <action>
Tres pecas, e a terceira e a que nao existe em lugar nenhum da arvore.

**A FLAG.** `--renda`, `action="store_true"`, ao lado do `--mercado` em `:3072`. O texto de ajuda
declara o que ela **nao** faz, na forma do irmao: *"roda SO a leitura da renda, como QUARTA
invocacao ao lado das duas de party e da do mercado. NAO vigia a party, NAO envia alerta nenhum e
NAO le o mercado: so le a barra inferior e grava em .renda/. Exige --janela."*

**O PORTAO DA MIRA.** `if args.renda and not args.janela: parser.error(...)`, ao lado do portao do
`--mercado` em `:3101`. **A razao e diferente da do mercado e vai escrita**: o painel do World
Exchange anda dentro da janela; aqui o motivo e que **EXP e adena sao do personagem** e o usuario
roda duas instancias lado a lado. Copiar a frase do mercado seria dar a razao errada para o
portao certo.

**O DESPACHO.** Logo depois do `if args.mercado:` em `:3204`, com import adiado
(`from .renda_laco import laco_da_renda`) pelo mesmo motivo do irmao. O comentario que explica a
POSICAO nao se duplica — ele ja esta escrito em `:3200-3203` e vale para os dois; acrescentar uma
linha dizendo que o `--renda` entra pela mesma porta basta.

**`montar_registro_da_renda(pasta: Path | None = None, personagem: str = ...)`**, no molde
literal de `montar_registro_de_mercado:573-650`. Ela existe porque `RegistroDaRenda.__init__` faz
`mkdir` e `open` **fora de qualquer `try`** e diz por escrito que nao se defende. As tres coisas
que ela captura, e por que:

- `OSError` — pasta que nao abre, disco cheio, permissao. Loga e devolve `None`.
- `ContratoDaRendaQuebrado` — o arquivo existe e o terminador acusou. Loga **citando o arquivo**,
  para o usuario saber qual mover, e devolve `None`.
- E **so essas duas familias**. Um `except Exception` nu esconderia um defeito de programacao com
  a mesma cara de um disco cheio, que e o oposto do que este trilho existe para fazer.

A ordem dos parametros segue o irmao: `pasta` primeiro e com default `None`, porque e o parametro
que o teste sobrescreve. `personagem` e obrigatorio — nao ha personagem default, e cair no
primeiro que estiver no arquivo e exatamente o defeito que `renda_do_personagem` proibe.

**Nada mais entra no `__main__`.** Nenhuma chave nova de config, nenhum argumento novo de
cadencia: `--intervalo` e `--status-a-cada` ja existem (C-4, P-4) e sao o caminho barato. Um teste
de fonte deste arquivo afirma que `SECAO_DA_RENDA` continua com as cinco chaves que o `02-01`
criou.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_no_main.py tests/test_config_da_renda.py -q</automated>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m l2scanner --renda 2>&1 | grep -q "janela"</automated>
  </verify>
  <done>
`--renda` sem `--janela` recusa com a razao certa e sem traceback; com `--janela` despacha para o
laco; `montar_registro_da_renda` devolve `None` e nunca levanta nos tres modos de falha; e a secao
`[renda]` continua com cinco chaves.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: O bloco por intervalo — as quatro taxas, o ETA, a sessao e as recusas medidas (CONS-01)</name>
  <files>l2scanner/renda_console.py, l2scanner/renda_laco.py, tests/test_renda_console.py</files>
  <read_first>
    - `l2scanner/mercado_console.py:490-500` — a razao de o bloco sair por **intervalo** e nunca por tique: *"Repintar um bloco de dezenas de linhas por segundo afogaria a linha ao vivo"*. E `SEGUNDOS_ENTRE_SECOES = 60.0`.
    - `l2scanner/mercado_console.py:599-665` — `secao_do_vale_quanto`: a forma exata de um bloco multi-linha, abrindo com `console.moldurar(...)`, e a linha final que descreve a tendencia com o `n` colado.
    - `l2scanner/mercado_console.py:668-741` — `resumo_da_sessao` inteiro: os quatro blocos, os rotulos alinhados em coluna fixa, os motivos de descarte ordenados por frequencia, e o orcamento de tique. **Este e o molde do resumo da renda.**
    - `l2scanner/mercado_console.py:744-790` — `LARGURA_DO_AVISO = 76` com a razao (*"a largura em que ela cabe num console padrao de 80"*) e a disciplina do aviso que sai **uma vez** e nao por receita.
    - `l2scanner/renda_conta.py:847-885` — `TaxaDaRenda` inteira: `evidencia`, `por_hora`, `por_minuto`, `motivo_da_ausencia`, `janela_farmada_em_segundos`, `unidade_da_janela`, `lacunas_excluidas`, `segundos_em_lacuna` e `ate`. Ler a docstring toda — **e ela que diz o que a tela tem de mostrar** e por que `ate` e separado do valor.
    - `l2scanner/renda_conta.py:1057-1110` — `AsDuasTaxas` e `as_duas_taxas`, com a docstring que diz que apresentar so uma **mente por omissao**, e o tamanho da mentira medido: 226 mil contra 466 mil adena/h.
    - `l2scanner/renda_conta.py:1125-1196` — `MOTIVO_DA_TAXA_DE_EXP_ZERADA`, `MOTIVO_DA_TAXA_DE_EXP_NEGATIVA`, `TempoAteONivel` e `tempo_ate_o_nivel`, com os **tres** casos de ausencia e a razao de serem tres (o conserto do usuario e diferente em cada um).
    - `l2scanner/renda_conta.py:1197-1254` — `ContagemDaRenda` e a razao de os quatro campos nao se somarem. **`recusadas_por_motivo` e a metade de tela dos 79%/21%/0% medidos.**
    - `l2scanner/mercado_analise.py:226-249` — `Evidencia`, com `suficiente` e `faltam`, que e o `n` que vai colado no numero.
    - `l2scanner/console.py:97-147` — `moldurar` (tres linhas, carimbo alinhado a direita, `max(LARGURA, ...)`) e `destacar`.
    - `l2scanner/dashboard_dados.py:620-630` — a medicao de `total_seconds()` devolvendo `69713.696` onde o inteiro dizia `69713`. **A duracao da sessao e formatada a partir de epochs subtraidos, e nunca de `timedelta.total_seconds()`.**
    - A secao "A medicao que decide a forma da tela" acima — em particular que a linha densa de 87 colunas **caiu** e por que.
  </read_first>
  <behavior>
    - O bloco abre com `console.moldurar` e mede, na linha mais longa, **no maximo 76 colunas** — teste que mede cada linha do bloco com os valores reais.
    - As **duas** taxas de cada grandeza saem juntas e rotuladas: a da janela movel e a da sessao inteira. Um bloco que mostre so uma falha o teste.
    - Cada taxa sai com `n` e recencia **colados**: `2,41 M XP/h (n=142, ha 3s)`. Um numero nu falha o teste.
    - Uma taxa sem evidencia sai como o `motivo_da_ausencia` **dela** e nunca como `0` nem como `--`, e o texto nomeia qual piso faltou.
    - `unidade_da_janela` (`minutos farmados`) aparece escrita ao lado do denominador, e nao `minutos de relogio`.
    - `lacunas_excluidas` e `segundos_em_lacuna` aparecem no bloco: e o que impede "a taxa caiu" de ser confundido com "o scanner nao viu".
    - O ETA sai como `falta 3h20` quando ha taxa; e como **um dos tres motivos distintos** quando nao ha — taxa zerada, taxa negativa e sem evidencia produzem **tres textos diferentes**, com o conserto do usuario diferente em cada um.
    - A duracao da sessao sai de subtracao de epochs e e formatada em `h`/`min`, nunca com casas decimais penduradas.
    - As taxas de recusa **por campo** aparecem no bloco, em porcentagem sobre os tiques da sessao. Com os numeros medidos em campo (nivel 79%, adena 21%, EXP 0%) o bloco os mostra sem arredondar para zero.
    - O bloco sai a cada `args.status_a_cada` segundos e **nao** por tique — teste com `ticks_maximos=10` e `status_a_cada` alto afirma que ele saiu **uma vez** (no arranque) e nao dez.
    - O resumo da sessao sai no `finally` **sempre**, inclusive numa sessao de zero linhas gravadas, e traz o orcamento de tique (p50/p95/maximo/estouros) e o caminho do arquivo de `.renda/`.
    - `renda_console` continua devolvendo texto e nunca imprimindo.
  </behavior>
  <action>
Este e o fecho do CONS-01, e ele e o bloco — nao a linha. A medicao ja decidiu isso: os dez valores
numa linha so medem **87** colunas e a largura real desta casa e **76** (C-2).

**Duas funcoes novas em `renda_console.py`:**

`bloco_da_renda(campos, taxas_de_exp, taxas_de_adena, eta, contagem, *, desde, agora)` — o bloco
por intervalo. Estrutura, no molde de `secao_do_vale_quanto` e `resumo_da_sessao`:

    <moldurar: "RENDA — <personagem>", hora>

    O QUE A TELA AFIRMA:
      nivel                            67
      EXP                              79,2568%
      adena                            17.592.060

    O QUE ISSO RENDE (as duas janelas, e elas respondem perguntas diferentes):
      XP/h    janela (10 min)          2,41 M   (n=142, ha 3s)
      XP/h    sessao (4h12 farmadas)   1,88 M   (n=3210, ha 3s)
      adena/h janela (10 min)          466 mil  (n=88, ha 3s)
      adena/h sessao (4h12 farmadas)   226 mil  (n=1980, ha 3s)
      falta para o nivel 68            3h20

    O QUE SAIU DO DENOMINADOR:
      lacunas excluidas                2  (18 min cegos)

    POR QUE O `n` E ESSE (recusa por campo, nesta sessao):
      nivel                            79%
      adena                            21%
      EXP                               0%

**As tres regras que a estrutura acima existe para cumprir, e as tres sao de requisito:**

1. **As duas taxas sempre juntas.** `AsDuasTaxas` traz as duas e a docstring diz que apresentar so
   uma mente por omissao. A janela responde "o que esta acontecendo agora"; a sessao responde "o
   que a noite rendeu". A diferenca entre 466 mil e 226 mil e **tempo parado**, e o usuario as 4 da
   manha precisa das duas para saber qual das duas perguntas ele esta fazendo.
2. **`n` e recencia colados no numero, nunca em rodape.** E o que o CONS-01 pede com todas as
   letras (*"com `n` e recencia colados nos numeros como o `--mercado` ja faz"*). Um numero de
   quarenta minutos atras continua bom; ele so nao e o de agora.
3. **As taxas de recusa aparecem.** Medidas em campo: nivel 79%, adena 21%, EXP 0%. Um painel que
   as esconde faz o usuario concluir que o scanner travou — e isso **ja aconteceu em producao**, em
   2026-09-01, com o aviso de layout do mercado, e virou o comentario de `mercado_modo.py:748-751`.
   A fonte dos numeros e `contagem.recusadas_por_motivo` sobre o total de tiques.

`resumo_da_sessao_da_renda(contagem, orcamento, registro, *, desde, ate)` — o `finally`. Copia a
forma dos quatro blocos de `resumo_da_sessao:668-741`: o que a tela entregou, o que foi para o
disco, por que as linhas foram recusadas (agregado, ordenado por frequencia), e o orcamento de
tique. **Ele sai SEMPRE**, inclusive na recusa e na sessao de zero linhas — o `vigiar-mercado.bat`
depende disso e o `vigiar-renda.bat` do `03-04` vai depender igual: *"Aqui nao ha bloco de
encerramento nenhum: o resumo da sessao sai do proprio programa"*.

**A CADENCIA DO BLOCO E `args.status_a_cada`** (P-4), no molde de `proxima_secao` em
`mercado_modo.py:637-648` e `:767-769`. Nao ha constante nova e nao ha chave nova: o argumento ja
existe com default 30 s e o usuario ja sabe o que ele faz nos outros modos. **O bloco tambem sai
UMA VEZ no arranque**, antes do primeiro tique — sem isso o usuario espera trinta segundos olhando
para linhas de tique sem saber se o calculo esta vivo.

**A FORMATACAO DOS NUMEROS GRANDES E DE SAIDA, e ela e do console.** `2,41 M` e `466 mil` sao
grafia, e `Fraction` e o dado. A conversao acontece **so aqui**, na borda — e a disciplina de
`Fraction` que a Fase 2 impos nao pode ser desfeita um passo antes do fim por um `float` de
conveniencia. A virgula decimal e a que o jogo usa e a que o resto do repositorio ja escreve.

**A duracao da sessao sai de epochs subtraidos.** `dashboard_dados.py:624-628` mediu
`total_seconds()` devolvendo `69713.696` onde o inteiro dizia `69713`; um `timedelta` no meio deste
caminho traz aquele erro de volta.
  </action>
  <verify>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest tests/test_renda_console.py tests/test_renda_laco.py -q</automated>
    <automated>PYTHONPATH=".;C:/Users/refun/Desktop/Lineage2-warnings/.venv/Lib/site-packages" python -m pytest --deselect tests/test_agenda.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>
O bloco sai por `--status-a-cada` e no arranque, cabe em 76 colunas, mostra as duas taxas de cada
grandeza com `n` e recencia colados, o ETA com os tres motivos distintos, o tempo em lacuna e as
tres taxas de recusa medidas; o resumo sai no `finally` mesmo com zero linhas gravadas; e a suite
fecha em **>= 5778 passed**.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| janela do jogo -> `JanelaSource` | pixels de um processo que nao controlamos entram como `ndarray`; o jogo pode fechar, minimizar ou cair no login a qualquer tique |
| `config.toml` / `calibration.json` -> laco | arquivos que o usuario edita a mao atravessam para dentro de um processo que roda a noite inteira sem supervisao |
| laco -> `.renda/*.csv` | o unico artefato DURAVEL e SEM DESFAZER desta fase; uma linha errada e indistinguivel de uma certa depois de gravada |
| `argparse` -> `--janela` | o titulo da janela vem da linha de comando e do `config.toml`, e ele decide **de qual personagem** a renda e lida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | `--janela` apontando para a instancia errada | high | mitigate | `--janela` **obrigatorio** (T2); o personagem sai do TITULO por `nome_do_personagem` e nunca de `--personagem`; `cal.renda_do_personagem` **nunca cai no vizinho** e devolve `None`, que vira recusa nomeada. Medido (M-F): o retangulo do vizinho devolve `349` ou `112`, numeros plausiveis que passam por qualquer validacao |
| T-03-02 | Tampering | linha parseavel e errada em `.renda/` por escrita interrompida | high | transfer | Mitigado na Fase 2 por `conferir_o_terminador` (T-02-01); este plano **nao acrescenta segunda porta de escrita**, que e a fronteira que o `03-CONTEXT.md:17-19` crava |
| T-03-03 | Tampering | nome de personagem virando travessia de diretorio | high | transfer | `loot.apelido()` por lista de PERMISSAO e a pasta saindo de `RAIZ`, ja provados na Fase 2 (T-02-02). Este plano consome `arquivo_do_personagem` e nao monta caminho |
| T-03-04 | Denial of Service | laco derrubado por disco cheio no meio da noite | high | mitigate | `registrar` devolve `bool` e **nunca levanta**; `montar_registro_da_renda` devolve `None` e nunca levanta (T2); dez excecoes seguidas de captura saem com codigo 1 e mensagem, e nao com traceback |
| T-03-05 | Denial of Service | segundo processo (`--renda`) degradando os dois de party | medium | mitigate | `MS_ENTRE_FRAMES_DA_RENDA` limita a entrega da WGC, no molde medido de `MS_ENTRE_FRAMES_DO_MERCADO`; `OrcamentoDoTick` mede p50/p95/maximo e conta os estouros, e o resumo diz por escrito que contencao entre processos e propriedade do SISTEMA e nao deste numero |
| T-03-06 | Repudiation | "por que a taxa desta hora tem `n=12` se o scanner rodou quarenta minutos" | high | mitigate | `ContagemDaRenda` somada **todo tique** e nao so nos aceitos; as taxas de recusa por campo no bloco (T3); `lacunas_excluidas` e `segundos_em_lacuna` visiveis; e o `scanner.log` rotativo recebendo a mesma string do console |
| T-03-07 | Information Disclosure | nome de personagem e caminho de disco no log rotativo | low | accept | O log e local, na maquina do usuario, e o nome do personagem ja esta no titulo da janela e no nome do arquivo. Nenhum segredo atravessa: esta fase nao toca token nem rede |
| T-03-08 | Elevation of Privilege | leitura passiva virando escrita no jogo | critical | mitigate | Nenhuma biblioteca de sintese de input entra; `tests/test_firewall_escopo.py` ja varre a arvore por elas e continua verde. O laco so LE pixels |
| T-03-09 | Tampering | `calibration.json` com `geometria_da_janela` vazia derrubando o arranque | medium | mitigate | P-1: a geometria e **medida da janela viva** e a gravada e so guarda; um `KeyError` no arranque viraria traceback em vez de scanner |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Nao se aplica: esta fase nao instala pacote nenhum.** `rich` esta explicitamente FORA (CTX-1) e `requirements.txt` nao e tocado. Ha teste afirmando que `import rich` nao aparece nos modulos novos |
</threat_model>

<verification>
1. `PYTHONPATH=".;.venv/Lib/site-packages" python -m pytest --deselect tests/test_agenda.py -q` fecha
   em **>= 5778 passed**, com as mesmas 15 falhas pre-existentes e **nenhuma nova**.
2. `tests/test_renda_par.py` continua verde: o portao do chamador unico das regras de par nao
   enxerga `renda_laco.py`.
3. `grep -rn "import rich\|from rich" l2scanner/` devolve zero.
4. `grep -c "capturar_completo" l2scanner/renda_laco.py` devolve no maximo 1, e a ocorrencia esta no
   arranque.
5. `python -m l2scanner --renda` sem `--janela` recusa com mensagem e sem traceback.
6. `git diff --name-only` nao lista nenhum arquivo do workstream `dashboard` nem
   `requirements.txt`.
</verification>

<success_criteria>
- `.renda/` deixou de estar vazio, com **dois** arquivos: o CSV do personagem e o `LEIAME.txt`.
- Um teste de laco roda **sem jogo aberto e sem OCR**, pelas cinco alavancas de injecao.
- A linha do tique sai todo tique, mede 58 a 72 colunas e mostra os tres valores que a tela afirma.
- O bloco por intervalo mostra as quatro taxas com `n` e recencia, o ETA, a duracao da sessao, o
  tempo em lacuna e as tres taxas de recusa medidas.
- `--renda` existe, exige `--janela` pela razao certa, e despacha ao lado do `--mercado`.
- `montar_registro_da_renda` devolve `None` e nunca levanta nos tres modos de falha.
- Nenhuma dependencia nova, nenhuma chave nova de config, nenhum arquivo do `dashboard` tocado.
</success_criteria>

<output>
Create `.planning/workstreams/renda/phases/03-o-modo-renda-o-painel-ao-vivo-e-a-cegueira-declarada/03-01-SUMMARY.md` when done
</output>
