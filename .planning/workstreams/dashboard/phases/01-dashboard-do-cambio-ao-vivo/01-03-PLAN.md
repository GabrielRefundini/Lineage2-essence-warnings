---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - l2scanner/dashboard_cambio.py
  - tests/test_dashboard_cambio.py
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Digitar 0,50 no campo de cambio grava a taxa; fechar e reabrir o navegador mantem o valor, porque ele mora em .mercado/cambio.json e nao no navegador."
    - "Todo valor em R$ e declarado como informado por voce, com a data em que foi informado — nunca apresentado como medido."
    - "Sem taxa informada, a leitura devolve nulo e nenhum valor padrao e chutado."
    - "Um texto que nao seja um numero positivo em forma decimal simples e RECUSADO no servidor, e o cambio anterior continua valendo: notacao de expoente, separador de milhar por sublinhado, sinal explicito e numero absurdamente longo sao recusados um a um, com teste por forma."
    - "A gravacao e atomica: ou fica o arquivo antigo inteiro, ou o novo inteiro, nunca meio — temporario ao lado do destino, nunca no diretorio temporario do sistema."
    - "Cada alteracao e apendada com carimbo e a ultima e a vigente; nada e sobrescrito e nada e apagado, porque nao ha acao destrutiva nesta fase."
  artifacts:
    - l2scanner/dashboard_cambio.py
    - tests/test_dashboard_cambio.py
  key_links:
    - "gravar_o_cambio -> os.replace: a mesma escrita atomica que calibracao.py e loot.py ja usam nesta arvore, com o temporario ao lado do destino."
    - "MOLDE_DO_CAMBIO -> Decimal: o portao de DUAS camadas; a segunda sozinha aceita formas que valem 20x o que o usuario quis dizer."
    - "dashboard_cambio nao importa dashboard_dados e vice-versa: o cambio entra no payload por parametro."
---

<objective>
A taxa XM para BRL: digitada pelo usuario, validada no servidor com falha fechada, persistida
com carimbo em `.mercado/cambio.json` por escrita atomica, e lida de volta entre sessoes.

Purpose: este e o unico arquivo que o processo do dashboard escreve, e o unico numero da tela
que o jogo nao sustenta. Um digito lido errado aqui e uma decisao de dinheiro real errada — a
pesquisa mediu que a validacao ingenua aceita formas que valem vinte vezes o que o usuario quis
dizer, sem nenhum aviso.

Output: `l2scanner/dashboard_cambio.py` e `tests/test_dashboard_cambio.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/REQUIREMENTS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-CONTEXT.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-RESEARCH.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: O portao de duas camadas — a forma antes do valor</name>
  <files>l2scanner/dashboard_cambio.py, tests/test_dashboard_cambio.py</files>
  <read_first>
    - `01-RESEARCH.md:694-724` (Pitfall 5) — a tabela medida de entradas aceitas e recusadas pelas duas abordagens, com o valor que cada uma produz; e a expressao regular ASCII recomendada, com o comentario do que cada parte dela recusa.
    - `01-UI-SPEC.md` secao `Copywriting Contract`, linha `Erro — cambio invalido` (a frase de recusa, travada) e a linha `error | #procedencia (sonda)` das `## UI Considerations`, que diz por extenso que o cambio anterior continua valendo e que o R$ na tela continua sendo o do cambio antigo.
    - `l2scanner/mercado_registro.py:472-479` e `:502-515` — a anatomia das mensagens de falha fechada desta casa: o que aconteceu, que nenhum byte foi alterado, o que fazer, e o que continua funcionando.
    - `l2scanner/mercado_analise.py:152-176` — o molde "ESCOLHA, NAO MEDICAO" para constantes de julgamento.
  </read_first>
  <behavior>
    - Um texto decimal simples com virgula e aceito e vira `Decimal`.
    - Um texto decimal simples com ponto e aceito e vira o mesmo `Decimal`.
    - Zero, negativo, vazio, so letras e duas virgulas sao recusados.
    - Notacao de expoente (`1e3`) e recusada — a segunda camada sozinha a aceitaria como mil.
    - Sublinhado como separador (`1_0`) e recusado — a segunda camada sozinha o leria como dez, vinte vezes o pretendido.
    - Sinal explicito (`+0.5`) e recusado.
    - Um numero com mais digitos que o molde permite e recusado.
    - Digitos Unicode nao-ASCII sao recusados.
    - A excecao levantada carrega a frase exata do UI-SPEC, e nao uma mensagem improvisada.
  </behavior>
  <action>
Criar `l2scanner/dashboard_cambio.py` como modulo folha do lado do dashboard: sem importar
`dashboard_dados` e sem ser importado por ele. Escrever na docstring do modulo por que essa
separacao existe — o cambio entra no payload por parametro, e uma dependencia nos dois sentidos
tornaria a montagem do JSON impossivel de testar sem disco.

Definir `MOLDE_DO_CAMBIO` como expressao regular ASCII compilada, com um comentario acima de
cada coisa que ela recusa e POR QUE, no molde "ESCOLHA, NAO MEDICAO": ancoras nas duas pontas,
teto de digitos inteiros, separador decimal aceitando ponto ou virgula, teto de casas decimais,
e — o ponto inteiro do molde existir — nenhuma notacao de expoente, nenhum sublinhado, nenhum
sinal. Escrever a medicao ao lado, com os valores: a validacao por `Decimal` sozinha, mesmo
conferindo positividade e finitude, ACEITA tres formas que a expressao regular recusa, e uma
delas transforma o que o usuario digitou em dez, o que poe o R$ da tela vinte vezes fora do
lugar sem nenhum aviso.

Definir `MENSAGEM_DE_CAMBIO_INVALIDO` com a frase exata travada no `## Copywriting Contract`, e
`CambioInvalido` como subclasse de `ValueError` carregando essa frase. A mensagem segue a
anatomia da casa: o que aconteceu, que o cambio anterior continua valendo, o que fazer, e um
exemplo.

Definir `interpretar_o_cambio(texto) -> Decimal`: aplicar `MOLDE_DO_CAMBIO` PRIMEIRO e so entao
converter, trocando virgula por ponto, e recusar valor nao positivo. As duas camadas nessa
ordem, e a ordem esta escrita: a expressao regular julga a FORMA, o `Decimal` julga o VALOR, e
inverter a ordem devolveria a permissividade que o molde existe para tirar.

Escrever `tests/test_dashboard_cambio.py` com uma classe `TestOPortaoDeDuasCamadas` e um teste
parametrizado por FORMA — uma linha por entrada da tabela medida na pesquisa, cada uma com o
resultado esperado. As formas que a segunda camada sozinha aceitaria ganham teste NOMEADO
individualmente, e nao so uma linha de parametrizacao: sao elas a razao do molde existir, e um
nome de teste que as cita e o que impede alguem de simplificar o portao numa refatoracao futura.

Acrescentar o CONTROLE NEGATIVO da casa: um teste que alimenta a segunda camada SOZINHA com as
mesmas tres formas e afirma que ela as ACEITA — provando que a primeira camada nao e decorativa.
Sem ele, remover a expressao regular deixaria todos os testes acima verdes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_cambio.py -q -k "portao or forma or invalid"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_cambio.py -q` termina em 0.
    - Existe um teste parametrizado cobrindo todas as entradas da tabela medida na pesquisa, com o veredito de cada uma.
    - Existem testes NOMEADOS individualmente para as tres formas que a validacao por valor sozinha aceitaria, cada um afirmando que `interpretar_o_cambio` levanta `CambioInvalido`.
    - Existe o CONTROLE NEGATIVO que afirma que a segunda camada, chamada sozinha sobre as mesmas tres formas, as aceita — a prova de que a primeira camada faz trabalho.
    - `python -c "from l2scanner.dashboard_cambio import MENSAGEM_DE_CAMBIO_INVALIDO as m; import sys; sys.exit(0 if 'maior que zero' in m and '0,50' in m else 1)"` sai com codigo 0 — a frase e a travada no UI-SPEC, e nao uma improvisada.
    - `python -c "import ast; t=ast.parse(open('l2scanner/dashboard_cambio.py',encoding='utf-8').read()); assert not [n for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module and 'dashboard_dados' in n.module]"` sai com codigo 0 — os dois modulos nao se importam.
  </acceptance_criteria>
  <done>Um texto que nao seja um decimal simples e positivo nao vira cambio, cada forma perigosa tem teste com nome proprio, e o controle negativo prova que o portao da forma nao e enfeite.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A persistencia carimbada e atomica em .mercado/cambio.json</name>
  <files>l2scanner/dashboard_cambio.py, tests/test_dashboard_cambio.py</files>
  <read_first>
    - `l2scanner/calibracao.py:745-768` — o escritor atomico da casa: o temporario ao lado do destino, `os.replace`, e o comentario que explica que entre volumes diferentes a operacao nao e atomica e no Windows nem funciona.
    - `l2scanner/loot.py:263-280` — a variante com o pid no nome do temporario, para duas instancias, e o formato de registro carimbado com `isoformat()`.
    - `l2scanner/mercado_catalogo.py:670` — `PASTA_DO_MERCADO`, a pasta onde o arquivo mora; ela e a mesma do dado do usuario e ja e ignorada pelo git.
    - `01-RESEARCH.md:186` e `:555` — a medicao da escrita atomica: 9,18 ms com sincronizacao, zero temporarios orfaos em 200 gravacoes.
    - `01-UI-SPEC.md` secao `Copywriting Contract`, linhas `Confirmacao de salvo` e `Confirmacao destrutiva` — a segunda diz por extenso que nada e sobrescrito e nada e apagado nesta fase.
  </read_first>
  <behavior>
    - Gravar num diretorio sem o arquivo cria o arquivo com uma lista de um registro.
    - Gravar de novo APENDA um segundo registro e mantem o primeiro; a leitura devolve o ultimo.
    - Cada registro carrega o valor e o carimbo em ISO.
    - Ler de um diretorio sem o arquivo devolve nulo, sem levantar e sem criar o arquivo.
    - Ler um arquivo com JSON corrompido devolve nulo e registra um aviso; nao levanta e nao apaga o arquivo.
    - Depois de 200 gravacoes seguidas nao sobra nenhum arquivo temporario no diretorio.
    - Gravar sobre um destino que ja existe substitui o conteudo inteiro, e nunca deixa o arquivo pela metade.
  </behavior>
  <action>
Definir `ARQUIVO_DO_CAMBIO` como constante de nome de arquivo e `Cambio` como dataclass
congelado com `reais_por_xm` e `informado_em`.

Definir `gravar_o_cambio(pasta, texto, agora) -> Cambio`: chamar `interpretar_o_cambio`, ler a
lista existente (ou lista vazia), APENDAR o novo registro com carimbo, e escrever com o padrao
atomico da casa — temporario ao lado do destino com o pid no nome, escrita, sincronizacao, e
`os.replace`. Escrever a frase operacional que os tres escritores desta arvore ja usam: a troca
e atomica no mesmo volume, ou fica o arquivo antigo inteiro ou o novo inteiro, nunca meio, e o
temporario mora ao lado do destino e nao no diretorio temporario do sistema porque entre volumes
diferentes a operacao perde a atomicidade.

Escrever ao lado, como decisao declarada, por que a estrutura e uma LISTA que cresce e nao um
valor unico sobrescrito: cada alteracao fica com carimbo, a ultima e a vigente, nada e apagado —
e essa e exatamente a estrutura que a coleta automatica do cambio pelo listener dos grupos de
venda vai preencher quando existir. O custo hoje e uma linha; o beneficio e nao haver acao
destrutiva nenhuma nesta fase, que e o que o UI-SPEC afirma na linha de confirmacao destrutiva.

Definir `ler_o_cambio(pasta) -> Cambio | None`: ausencia devolve nulo sem criar nada; JSON
corrompido devolve nulo com aviso no log, no molde do aviso de linha descartada de
`mercado_registro` — nomeando o arquivo e dizendo que o resto continua funcionando, e sem apagar
o arquivo do usuario. Escrever ao lado que devolver nulo aqui e a falha fechada correta: sem
cambio informado a pagina mostra XM normalmente e diz que o R$ esta indisponivel, e um valor
padrao chutado viraria decisao de dinheiro real errada.

Acrescentar a `tests/test_dashboard_cambio.py` a classe `TestAPersistenciaCarimbada` com os sete
casos do bloco `<behavior>`, todos em `tmp_path` e nenhum tocando a pasta real. Incluir o teste
das 200 gravacoes contando arquivos temporarios remanescentes, e o teste que grava duas vezes e
afirma que o primeiro registro CONTINUA no arquivo — a prova de que a estrutura apenda em vez de
sobrescrever.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_cambio.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_cambio.py -q` termina em 0 com pelo menos 14 testes coletados.
    - Existe um teste que grava duas vezes e afirma que o arquivo contem DOIS registros e que a leitura devolve o segundo.
    - Existe um teste que faz 200 gravacoes e afirma que a contagem de entradas do diretorio cujo nome contem o sufixo de temporario e zero.
    - Existe um teste que grava um JSON corrompido a mao no arquivo e afirma que a leitura devolve nulo, que um aviso foi emitido (`caplog`) e que o arquivo CONTINUA existindo com o conteudo original.
    - Existe um teste que afirma que a leitura de uma pasta sem o arquivo devolve nulo E que a pasta continua sem o arquivo depois da chamada.
    - `python -m pytest tests/ -q` termina em 0 e `git status --porcelain .mercado/` sai vazio — nenhum teste escreveu na pasta real.
  </acceptance_criteria>
  <done>A taxa persiste entre sessoes num arquivo que so cresce, a gravacao e atomica e sem lixo, e a ausencia devolve nulo em vez de um valor chutado.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `l2scanner/dashboard_cambio.py` — `ARQUIVO_DO_CAMBIO`, `MOLDE_DO_CAMBIO`,
  `MENSAGEM_DE_CAMBIO_INVALIDO`, `CambioInvalido`, `Cambio`, `interpretar_o_cambio`,
  `ler_o_cambio`, `gravar_o_cambio`
- `tests/test_dashboard_cambio.py`
- Arquivo de dado novo em tempo de execucao: `.mercado/cambio.json` — o UNICO arquivo que o
  processo do dashboard escreve

Nada de novo em `requirements.txt`. Nenhum arquivo do workstream `mercado` e tocado.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto do navegador -> `Decimal` | O texto chega de um `POST` e pode nao ter passado por nenhuma validacao de cliente; um `POST` direto tem de ser recusado igual |
| processo -> disco | O unico arquivo que este processo escreve; uma escrita interrompida no meio nao pode deixar o arquivo do usuario invalido |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | `interpretar_o_cambio` | high | mitigate | Portao de duas camadas com a FORMA julgada antes do valor; teste por forma perigosa; controle negativo provando que a camada da forma faz trabalho. A falha fechada mora no SERVIDOR — a validacao no navegador e conveniencia |
| T-01-11 | Denial of Service | escrita interrompida no `cambio.json` | medium | mitigate | Temporario ao lado do destino, sincronizacao, `os.replace`; medido em 9,18 ms e zero orfaos em 200 gravacoes |
| T-01-12 | Repudiation | qual cambio estava valendo quando | low | mitigate | Estrutura de lista que so cresce, cada registro carimbado em ISO; a ultima e a vigente, nada e apagado |
| T-01-13 | Tampering | `cambio.json` corrompido por edicao a mao | medium | mitigate | Leitura devolve nulo com aviso e NAO apaga o arquivo; a tela cai para R$ indisponivel em vez de exibir numero de origem duvidosa |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_cambio.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- `git status --porcelain .mercado/` sai vazio depois da suite.
- `git diff --stat requirements.txt` sai vazio.
</verification>

<success_criteria>
- Cada forma que a validacao ingenua aceitaria tem um teste com nome proprio recusando-a.
- O controle negativo prova que a camada da forma nao e decorativa.
- A gravacao apenda com carimbo, e atomica e nao deixa lixo.
- A ausencia de cambio devolve nulo, e nenhum valor padrao e chutado em lugar nenhum do modulo.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-03-SUMMARY.md` when done
</output>
