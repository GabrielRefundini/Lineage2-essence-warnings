---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - l2scanner/recursos/dashboard/index.html
  - l2scanner/recursos/dashboard/dashboard.css
  - tests/test_dashboard_pagina.py
autonomous: true
requirements: [DASH-02, DASH-04, DASH-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As tres regioes do UI-SPEC existem no HTML com os identificadores travados, e o formulario do cambio e um formulario de verdade, com rotulo associado ao campo."
    - "O bloco de tokens de cor do UI-SPEC e a UNICA declaracao de cor da arvore: nenhum literal hexadecimal existe fora dele, nem no CSS nem no HTML."
    - "Nenhum valor de espacamento fora da escala de quatro pontos aparece no CSS, com as tres excecoes de traco declaradas no UI-SPEC (larguras de borda, espessura de linha do grafico e anel de foco)."
    - "A tipografia usa quatro tamanhos e dois pesos, tres familias todas do sistema, e nenhum webfont e baixado — zero requisicao de rede na pagina alem das do proprio servidor."
    - "Todo numero da tela usa a familia monoespaçada com largura tabular, para o valor que se atualiza a cada dois segundos nao dancar horizontalmente."
    - "overflow (#destaque, #serie): o numero em destaque usa tamanho elastico entre o piso e o teto declarados na linha Display da escala, com largura tabular — ele encolhe, nao quebra linha e nao vaza do painel; e a densidade de pontos do zoom largo nao vira ruido porque a agregacao ja aconteceu no servidor."
    - "long-text (#serie): o titulo da serie vem de OCR e oscila; ele fica em uma linha com reticencia e o texto completo no atributo de titulo, e nunca invade a area do numero."
    - "long-text (#destaque): o rotulo de unidade vem inteiro do Python e e longo; ele quebra em ate duas linhas dentro do cartao e nunca empurra o numero grande — o cartao cresce em altura, nunca em largura. Encurtar a string no navegador e proibido."
    - "empty (#procedencia): sem nenhum cambio informado, o campo nasce vazio com o exemplo como placeholder, o carimbo de informado em nao aparece, e a linha diz que o R$ esta indisponivel; o placeholder nunca e submetido como valor."
    - "overflow (#procedencia): o campo do cambio tem limite de comprimento e largura fixa em unidades de caractere, e o caminho do arquivo na linha de fonte usa reticencia com o texto completo no atributo de titulo."
    - statement: "long-text (#procedencia): a frase de recencia e a de piso de evidencia vem do Python em ASCII, com comprimento que varia com o estado; elas envolvem em varias linhas sem empurrar o formulario para fora do painel. Sem teste de layout de navegador, isto e verificacao humana declarada."
      verification: backstop
    - "A pagina nao carrega script nem estilo embutido, porque a politica de seguranca de conteudo os proibe — e e por isso que o CSS e o JS sao arquivos separados."
    - "Todo texto da tela fica acima de 4,5:1 de contraste, e a razao de cada par da tabela do UI-SPEC e recalculada e nao estimada."
    - "Com preferencia por movimento reduzido, todo pulso e toda transicao viram nenhum, sem excecao."
  artifacts:
    - l2scanner/recursos/dashboard/index.html
    - l2scanner/recursos/dashboard/dashboard.css
    - tests/test_dashboard_pagina.py
  key_links:
    - "dashboard.css -> o bloco de tokens: o JS le a cor por nome de token para configurar o grafico, entao uma cor sem nome de token e uma cor que o grafico nao consegue pedir."
    - "index.html -> o formulario de verdade: a tecla de confirmacao submete, e o JS intercepta; a diretiva que proibe destino de formulario e a falha fechada correta."
    - "vendor/uPlot.min.css -> dashboard.css: a folha da biblioteca entra por marcacao propria e as sobreposicoes de tema ficam depois dela, num bloco nomeado."
---

<objective>
A superficie: a estrutura das tres regioes e o tema escuro e tematico de Lineage 2 inteiro, em
CSS escrito a mao, sem nenhum byte de rede.

Purpose: o CONTEXT foi explicito — nao e um dashboard generico de analytics, e um painel do jogo
aberto ao lado do cliente, a noite, numa segunda tela. E foi igualmente explicito sobre o limite:
**o tema nunca custa legibilidade do numero**; se um ornamento disputar com o valor em destaque,
o ornamento sai.

Output: `l2scanner/recursos/dashboard/index.html`, `l2scanner/recursos/dashboard/dashboard.css`
e `tests/test_dashboard_pagina.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-CONTEXT.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: A estrutura — tres regioes, um formulario de verdade, zero embutido</name>
  <files>l2scanner/recursos/dashboard/index.html, tests/test_dashboard_pagina.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Layout & Componentes` — o diagrama das tres regioes com os identificadores exatos e o conteudo de cada uma.
    - `01-UI-SPEC.md` secao `Copywriting Contract` — todas as frases da tela, incluindo o rotulo do campo, o texto do botao primario e o rotulo do botao que restaura o alcance total do grafico.
    - `01-UI-SPEC.md` secao `Interacao`, linha do envio do cambio — formulario de verdade, tecla de confirmacao submete.
    - `01-UI-SPEC.md` secao `Registry Safety`, ultimo paragrafo — a diretiva de origem de script proibe embutidos, e e por isso que o CSS e o JS sao arquivos separados; isto e restricao de entrega, nao preferencia.
    - `01-RESEARCH.md:726-740` (Pitfall 6) — a colisao entre a diretiva que proibe destino de formulario e um formulario de verdade, e por que ela e a falha fechada CERTA.
    - `01-PATTERNS.md` secao `Os arquivos web — SEM ANALOGO` — nao ha de onde copiar; o contrato prescritivo e o UI-SPEC.
    - `l2scanner/recursos/dashboard/index.html` — o esqueleto que o tracer (01-01) deixou.
  </read_first>
  <action>
Expandir o esqueleto do tracer para a estrutura completa das tres regioes, com os identificadores
exatos do UI-SPEC.

Na regiao do destaque: o titulo do painel, e dois cartoes lado a lado. Cada cartao tem o numero,
o rotulo de unidade, e a linha de procedencia com o `n` e a recencia. O cartao de R$ nasce
presente na marcacao mas escondido por um atributo de estado, porque a regra ortogonal do
UI-SPEC diz que sem cambio informado ele DESAPARECE da tela e o cartao de XM ocupa a largura
toda — ele nao fica cinza e nao fica zerado.

Na regiao da serie: o titulo, o contentor do grafico, a legenda das duas linhas com o simbolo de
traco solido e o de tracejado ao lado de cada rotulo, e o botao que restaura o alcance total,
com o texto exato travado no UI-SPEC. A legenda fica na marcacao desde o inicio, porque no
estado vazio ela tem de aparecer para o usuario saber o que vai surgir ali.

Na regiao de procedencia: um formulario de verdade com rotulo associado ao campo pelo atributo
de vinculo, o campo com o exemplo como placeholder e o modo de teclado decimal, o botao primario
com o texto travado, e as linhas de procedencia — fonte do dado com a marca de somente leitura,
`n` e recencia, o carimbo do cambio informado, e o aviso do cambio historico.

Acrescentar a placa de estado vazio dentro da regiao da serie, com o titulo, o corpo e a linha de
prova do UI-SPEC — a estrutura existe sempre e o JS so alterna a visibilidade. Um retangulo em
branco seria indistinguivel de "o dashboard quebrou", e e exatamente o erro que esta fase existe
para nao cometer.

Referenciar por marcacao propria, na ordem: a folha de estilo da biblioteca vendorizada, depois
`dashboard.css`, e por fim `dashboard.js` como arquivo. Nenhum script embutido, nenhum estilo
embutido, nenhum atributo de evento em elemento — as tres formas sao proibidas pela diretiva de
origem de script.

Escrever `tests/test_dashboard_pagina.py` com uma classe `TestAEstruturaDaPagina` que faz o parse
do HTML com o analisador da biblioteca padrao e afirma: os tres identificadores de regiao
existem; existe um formulario com um campo e um botao de submissao; o campo tem rotulo associado,
limite de comprimento e o exemplo como placeholder; todo elemento de script tem atributo de
origem; nao ha elemento de estilo embutido; nao ha atributo de evento em nenhum elemento; e a
folha da biblioteca e referenciada ANTES de `dashboard.css`.

Acrescentar `TestAsFrasesSaoAsDoContrato`: para cada frase fixa da tela travada no
`## Copywriting Contract` que aparece no HTML, afirmar presenca literal — e afirmar ausencia das
frases PROIBIDAS. As proibidas ficam numa lista nomeada no proprio teste, com o comentario de que
ela e escrita a mao de proposito.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_pagina.py -q -k "estrutura or frase"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_pagina.py -q` termina em 0.
    - O teste de estrutura afirma a presenca dos tres identificadores de regiao do UI-SPEC, por parse e nao por busca de texto.
    - O teste afirma que todo elemento de script tem atributo de origem, que a contagem de elementos de estilo embutido e zero, e que a contagem de atributos de evento em elementos e zero.
    - O teste afirma que o campo do cambio tem rotulo associado por vinculo, limite de comprimento definido, e placeholder — e que o valor inicial do campo e vazio, para o placeholder nunca ser submetido como valor.
    - O teste afirma que a folha de estilo da biblioteca vendorizada e referenciada antes de `dashboard.css`, para as sobreposicoes de tema virem depois.
    - O teste das frases afirma presenca literal de cada frase fixa e ausencia de cada frase proibida, com a lista das proibidas escrita a mao no teste.
  </acceptance_criteria>
  <done>A estrutura das tres regioes existe, o formulario e de verdade, nada e embutido, e as frases da tela sao as do contrato — tudo afirmado por parse e nao por leitura.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O tema — tokens, tipografia, escala e a receita de relevo</name>
  <files>l2scanner/recursos/dashboard/dashboard.css, tests/test_dashboard_pagina.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Color` — o bloco de tokens completo, a regra de que ele e a UNICA declaracao de cor do projeto, a lista fechada de onde o acento pode aparecer, a hierarquia de cor entre os dois numeros e a razao epistemica dela, e a tabela de contraste medida.
    - `01-UI-SPEC.md` secao `Typography` — as tres familias do sistema, a escala de quatro tamanhos e dois pesos, a regra dura de que o numero em destaque usa a familia monoespaçada e nao a de fantasia, e a guarda de estouro.
    - `01-UI-SPEC.md` secao `Spacing Scale` — a escala de quatro pontos e as tres excecoes declaradas que sao traco e nao espaco.
    - `01-UI-SPEC.md` secao `A receita do relevo` — o CSS pronto do painel, com o gradiente, a borda, o raio anguloso e as tres sombras.
    - `01-UI-SPEC.md` secao `Interacao` — o anel de foco, o pulso de valor mudado e a regra de movimento reduzido.
    - `01-UI-SPEC.md` secao `Ornamento de canto` — a condicao sob a qual ele existe: se encostar no numero grande em qualquer largura, ele sai.
  </read_first>
  <action>
Escrever `dashboard.css` inteiro a partir do contrato, na ordem: o bloco de tokens em raiz (cor,
espacamento, familias), os estilos de base, o papel de cada nivel da escala tipografica, a
receita de relevo do painel, as tres regioes, os cartoes do destaque, o formulario, a legenda, a
placa de estado vazio, os estados ortogonais, o anel de foco, o pulso, e por ultimo um bloco
NOMEADO de sobreposicoes da folha da biblioteca vendorizada.

Aplicar a hierarquia de cor entre os dois numeros exatamente como o UI-SPEC a justifica, e
escrever a razao no proprio CSS: o numero de XM leva o acento porque e derivado SO do que o
scanner leu na tela; o numero de R$ leva a cor de corpo porque e derivado da leitura vezes uma
taxa que o usuario digitou. A diferenca de cor entre os dois e a diferenca epistemica entre eles,
e nao decoracao.

Aplicar a guarda de estouro do numero em destaque com tamanho elastico entre o piso e o teto
declarados na linha Display da escala, sempre com largura tabular. Escrever ao lado que sem
largura tabular um numero que se atualiza a cada dois segundos danca horizontalmente, o que e
ruido puro ao lado do jogo; e que os dois limites sao extremos do MESMO papel da escala, e nao um
quinto tamanho.

Aplicar as regras de texto longo: o titulo da serie em uma linha com reticencia; o rotulo de
unidade do destaque quebrando em ate duas linhas dentro do cartao, com o cartao crescendo em
altura e nunca em largura; o campo do cambio com largura fixa em unidades de caractere; e o
caminho do arquivo na linha de fonte com reticencia. Escrever ao lado de cada uma qual linha das
`## UI Considerations` ela atende.

Aplicar a regra de movimento reduzido zerando todo pulso e toda transicao sob a preferencia do
sistema, sem excecao.

O ornamento de canto e OPCIONAL e entra sob condicao: se em qualquer largura de tela ele encostar
no numero em destaque, ele sai. Se for implementado, sera um unico desenho vetorial embutido no
proprio HTML, replicado nos quatro cantos por transformacao, com opacidade baixa e sem receber
eventos de ponteiro. Registrar no SUMMARY se ele entrou ou nao, e por que.

Acrescentar a `tests/test_dashboard_pagina.py` a classe `TestOTemaObedeceOContrato`, que le o CSS
como texto e afirma, com expressoes regulares nomeadas: que a contagem de literais de cor
hexadecimal FORA do bloco de tokens em raiz e zero; que todo valor de espacamento em pixels usado
em propriedades de espaco pertence a escala declarada, tolerando apenas as tres excecoes de
traco; que a contagem de tamanhos de fonte distintos declarados e no maximo quatro e a de pesos e
no maximo dois; e que nao ha nenhuma referencia a origem externa de fonte ou de imagem.

Acrescentar `TestOContrasteFoiRecalculado`: uma funcao pura no proprio teste que calcula a razao
de contraste a partir de dois valores hexadecimais, e uma assercao por par da tabela do UI-SPEC
exigindo o piso declarado. O teste LE os valores do bloco de tokens do CSS, e nao os repete —
assim, trocar um token e recalcular a razao em vez de estimar no olho, que e o que o UI-SPEC
exige por extenso.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_pagina.py -q -k "tema or contraste"</automated>
  </verify>
  <acceptance_criteria>
    - Existe um teste que afirma que a contagem de literais de cor hexadecimal fora do bloco de tokens em raiz e zero, no CSS e no HTML.
    - Existe um teste que afirma que a contagem de tamanhos de fonte distintos declarados no CSS e no maximo quatro e a de pesos e no maximo dois.
    - Existe um teste que afirma que todo valor de espacamento em pixels em propriedades de espaco pertence a escala de quatro pontos, com as tres excecoes de traco listadas nominalmente no proprio teste.
    - Existe `TestOContrasteFoiRecalculado` com uma funcao pura de razao de contraste e uma assercao por par da tabela do UI-SPEC, LENDO os valores do bloco de tokens do CSS — trocar um token faz o teste recalcular, e falhar se o piso cair.
    - Existe um teste que afirma que nenhuma referencia a origem externa aparece no CSS nem no HTML — a pagina nao faz requisicao de rede alem das do proprio servidor.
    - Existe um teste que afirma que a regra de movimento reduzido esta presente e que ela zera as propriedades de transicao e de animacao.
    - `python -m pytest tests/test_dashboard_pagina.py -q` termina em 0.
  </acceptance_criteria>
  <done>O tema esta escrito a partir do contrato, a paleta mora num lugar so, os pisos de contraste sao recalculados por teste, e nenhum byte vem da rede.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Os estados desenhados — o vazio, o sem-cambio, o velho e o servidor mudo</name>
  <files>l2scanner/recursos/dashboard/dashboard.css, l2scanner/recursos/dashboard/index.html, tests/test_dashboard_pagina.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Estados da tela e sua precedencia` — a ordem fechada de cinco e as tres ortogonais, mais a subsecao `O estado vazio, desenhado`, com os tres elementos que a area do grafico renderiza mesmo sem dado.
    - `01-UI-SPEC.md` secao `## UI Considerations`, linhas `empty | #procedencia`, `overflow | #procedencia`, `long-text | #destaque` e `long-text | #procedencia` — as quatro que esta tarefa desenha.
    - `01-UI-SPEC.md` secao `Copywriting Contract` — as frases de dado velho, de servidor mudo, de R$ indisponivel e de linha parcial.
    - `01-CONTEXT.md` secao `A superficie` — "estado vazio e conteudo, nao ausencia", e a medicao de que hoje o arquivo tem 93 linhas e ZERO da serie da Adena, o que faz do estado vazio a PRIMEIRA tela.
  </read_first>
  <action>
Desenhar em CSS os estados que o JS vai apenas alternar por atributo de estado, para que nenhuma
decisao visual viva no JS.

O estado vazio da area do grafico: a grade e os eixos tematizados desenhados (prova de que a area
existe e esta viva), a placa centralizada com titulo, corpo e a linha de prova com os numeros
reais, e a legenda das duas linhas ja visivel em cor de texto fraco. Escrever ao lado que grafico
vazio mudo esta PROIBIDO, porque um retangulo em branco e indistinguivel de "o dashboard
quebrou".

O estado sem cambio informado: o cartao de R$ sai da tela pela regra ortogonal, o cartao de XM
passa a ocupar a largura toda, e a linha de procedencia mostra a frase de R$ indisponivel logo
acima do campo. O campo nasce vazio com o exemplo como placeholder e sem carimbo de informado
em.

O estado de dado velho: a recencia troca para a cor fria e a frase que recusa afirmar "agora"
aparece acima do destaque. O numero CONTINUA na tela — o que sai e a afirmacao, nao o valor.

O estado de servidor mudo: faixa no topo, com os valores mantidos e marcados como sendo da ultima
resposta recebida.

O estado de primeira pintura: os cartoes desenhados com o rotulo de leitura em curso no lugar do
numero. Nunca um zero com virgula zero zero como espaco reservado — ausencia se escreve com
palavra, e essa frase e uma das proibidas do contrato.

O estado do botao durante o envio do cambio: desabilitado com o rotulo de salvamento em curso,
sem virar indicador giratorio, e com o campo preservando o valor digitado.

Acrescentar a `tests/test_dashboard_pagina.py` a classe `TestOsEstadosTemDesenho`: para cada
estado, afirmar que existe no CSS uma regra ancorada no atributo de estado correspondente, e que
a frase da copia daquele estado existe no HTML ou esta listada como vinda do servidor. Afirmar
tambem que a placa de estado vazio esta na marcacao mesmo quando escondida — ela nao e criada
pelo JS.

Acrescentar `TestOEstadoVazioEConteudo`: afirmar que a regiao da serie contem, mesmo escondida, o
titulo da placa, o corpo e o lugar reservado para a linha de prova com os numeros reais; e que a
legenda das duas linhas esta na marcacao. Esta e a prova estrutural de que a primeira tela que o
usuario vai ver — que hoje e o estado vazio, medido — nao e um retangulo branco.
  </action>
  <acceptance_criteria>
    - Existe uma regra de CSS ancorada em atributo de estado para cada um dos seis estados desenhados nesta tarefa, afirmada por teste.
    - Existe um teste que afirma que a placa de estado vazio, a legenda das duas linhas e o lugar da linha de prova estao na MARCACAO — e nao criados pelo JS.
    - Existe um teste que afirma que a frase proibida de espaco reservado com zeros nao aparece em lugar nenhum do HTML nem do CSS.
    - Existe um teste que afirma que o cartao de R$ tem o atributo de estado que o retira da tela, e que a regra correspondente existe no CSS.
    - Existe um teste que afirma que a regra de dado velho troca a COR da recencia e nao esconde o numero.
    - `python -m pytest tests/test_dashboard_pagina.py -q` termina em 0 com pelo menos 20 testes coletados.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_dashboard_pagina.py -q</automated>
    <human-check>Abrir o dashboard e olhar: o numero em destaque e a primeira coisa que o olho encontra; nenhum ornamento de canto encosta nele em nenhuma largura de janela; o texto sobre o relevo do painel continua legivel; e o cartao de R$ realmente SOME quando nao ha cambio informado, em vez de ficar cinza ou zerado.</human-check>
  </verify>
  <done>Cada estado tem desenho proprio no CSS, o estado vazio e conteudo e nao ausencia, e nenhuma decisao visual mora no JS.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `l2scanner/recursos/dashboard/index.html` — as tres regioes com os identificadores travados, o
  formulario de verdade, a legenda, a placa de estado vazio, e as referencias por arquivo
- `l2scanner/recursos/dashboard/dashboard.css` — o bloco de tokens (unica declaracao de cor da
  arvore), a escala tipografica, a escala de espacamento, a receita de relevo, os seis estados
  desenhados e o bloco nomeado de sobreposicoes da folha da biblioteca
- `tests/test_dashboard_pagina.py` — estrutura por parse, frases do contrato, tema conferido por
  expressao regular, contraste RECALCULADO a partir dos tokens, e os estados desenhados

Nenhum arquivo Python do pacote e tocado por este plano. Nenhum byte de rede entra na pagina.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pagina -> rede externa | Qualquer referencia externa no HTML ou no CSS seria uma requisicao de rede que a doutrina de zero-install e a CSP existem para nao ter |
| conteudo do CSV -> texto da tela | O nome exibido da serie vem de OCR e e conteudo nao confiavel que acaba dentro do HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Information Disclosure | referencia externa de fonte ou imagem | medium | mitigate | Somente familias do sistema; teste afirmando ausencia de qualquer origem externa no HTML e no CSS; a CSP com origem propria barra o resto |
| T-01-22 | Tampering | nome exibido vindo de OCR injetado no HTML | medium | mitigate | O JS insere texto como TEXTO e nunca como marcacao (prendido no plano 01-07); aqui, o titulo tem largura limitada com reticencia e o texto completo vai no atributo de titulo |
| T-01-23 | Tampering | estilo ou script embutido | high | mitigate | Teste por parse afirmando zero elementos de estilo embutido, zero atributos de evento, e atributo de origem em todo script — as tres formas que a diretiva de origem de script proibe |
| T-01-24 | Denial of Service | numero longo estourando o painel | low | mitigate | Tamanho elastico entre piso e teto com largura tabular; limite de comprimento no campo; reticencia nos textos longos |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_pagina.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- `git diff --stat requirements.txt` sai vazio.
- Nenhum arquivo `.py` do pacote aparece em `git diff --name-only` para este plano.
</verification>

<success_criteria>
- As tres regioes existem com os identificadores do contrato, afirmadas por parse.
- A paleta mora num lugar so, e nao ha literal de cor fora dela.
- Os pisos de contraste sao recalculados por teste a partir dos tokens, e nao repetidos.
- Cada estado tem desenho proprio, e o estado vazio e conteudo.
- Nenhuma requisicao de rede sai da pagina.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-06-SUMMARY.md` when done
</output>
