---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 08
type: execute
wave: 5
depends_on: ["01-05", "01-07"]
files_modified:
  - dashboard.bat
  - tests/test_dashboard_bat.py
  - tests/test_dashboard_serie_generica.py
autonomous: true
requirements: [DASH-05, DASH-06]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "dashboard.bat sobe a interface em dois cliques e abre o navegador sozinho, sem o usuario digitar comando nenhum."
    - "O dashboard roda em PROCESSO SEPARADO: derrubar o dashboard nao interrompe a coleta da noite, e derrubar o scanner deixa o dashboard vivo mostrando o ultimo dado com a recencia na cara."
    - "O comportamento do --mercado fica identico: nenhuma linha de l2scanner/__main__.py, mercado_modo.py, rastreador.py, visao.py, console.py, mercado_registro.py, mercado_analise.py ou mercado_console.py muda."
    - "TRES arquivos preexistentes mudam nesta fase, e sao exatamente estes tres: mercado_catalogo.py (uma linha de import, aprovada pelo usuario em CTX-2), config.py (uma linha: RAIZ passa a ser re-exportado do modulo folha) e tests/test_mercado_firewall_de_fase.py (o tripwire que o corte tornou falso). Qualquer quarto arquivo preexistente alterado e uma quebra do DASH-06."
    - "A mudanca em config.py e uma re-exportacao e nada mais: a superficie publica do modulo continua a mesma, e o diff dele remove exatamente UMA linha — a definicao antiga de RAIZ."
    - "Os blocos de erro do lancador vem ANTES da linha de execucao, com desvio por cima — o defeito medido em campo em 2026-08-28 foi exatamente um bloco de encerramento saindo depois de a ferramenta ter saido em codigo de erro."
    - "O lancador e ASCII puro e recusa o atalho da loja da Microsoft, como o irmao dele ja faz."
    - "A sonda de dependencias do lancador cobre exatamente o que o processo do dashboard precisa, MEDIDO, e nao o que o lancador do mercado sonda: nem captura de tela, nem OCR entram neste processo."
    - statement: "zero-one-many (componente de serie, DASH-05): zero e um estao desenhados; MUITAS series nao tem tela no v1 por decisao travada, e a generalidade e provada por teste instanciando uma segunda serie sobre o payload servido, sem codigo de grafico novo. Sem evidencia de que a segunda instancia RENDERIZA, o desenho de duas series fica como verificacao humana declarada."
      verification: backstop
  artifacts:
    - dashboard.bat
    - tests/test_dashboard_bat.py
    - tests/test_dashboard_serie_generica.py
  key_links:
    - "dashboard.bat -> python -m l2scanner.dashboard: o lancador chama o modulo direto, e por isso l2scanner/__main__.py nao muda uma linha (DASH-06)."
    - "a sonda do lancador -> a cadeia de import medida: mercado_console arrasta OpenCV e numpy, e nada alem disso; a sonda diz a verdade sobre este processo, e nao copia a do mercado."
    - "test_dashboard_serie_generica -> o payload servido: a prova do DASH-05 acontece sobre o JSON real, sem navegador."
---

<objective>
O lancador proprio, a prova de que ele obedece ao molde da casa, e a prova sem navegador de que
o componente de serie e generico.

Purpose: o usuario nao digita comando. O `.bat` e a interface real do programa, e esta arvore ja
pagou em campo o preco de um lancador que anuncia um desfecho que nao conferiu. E o DASH-05
exige que a generalidade seja PROVADA, e nao afirmada — com a tela do v1 mostrando so a Adena.

Output: `dashboard.bat`, `tests/test_dashboard_bat.py` e `tests/test_dashboard_serie_generica.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/REQUIREMENTS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-RESEARCH.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-01-SUMMARY.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-05-SUMMARY.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: dashboard.bat — o molde do irmao, sem as partes que este processo nao tem</name>
  <files>dashboard.bat</files>
  <read_first>
    - `vigiar-mercado.bat` inteiro — o molde: o cabecalho que explica o que ele E e o que ele NAO E, a ida para a pasta do proprio arquivo, a descoberta do Python guardando o caminho completo, a recusa do atalho da loja por substituicao de string nativa, a montagem do ambiente na primeira execucao, a sonda de dependencias, e — o coracao do molde — os blocos de erro ANTES da linha de execucao com desvio por cima, com o comentario que registra o defeito medido em campo em 2026-08-28.
    - `01-PATTERNS.md` secao `dashboard.bat (launcher)` — o que copiar (a estrutura) e o que sai (a mira de janela e a sonda de OCR).
    - `01-01-SUMMARY.md` — a medicao registrada da cadeia de import: o que o processo do dashboard efetivamente carrega depois do corte de RAIZ.
    - `01-RESEARCH.md:666-692` (Pitfall 4) — as faixas de porta que o Windows reserva e que mudam a cada reinicializacao, e a recomendacao de sondar antes de subir.
    - `l2scanner/dashboard.py` — a assinatura de `main`, a opcao de porta e a `MENSAGEM_DE_PORTA_OCUPADA`, entregues pelo plano 01-05.
  </read_first>
  <action>
Escrever `dashboard.bat` copiando a ESTRUTURA do irmao, nao o conteudo.

O cabecalho explica, no mesmo tom: o que ele e (a janela do cambio, aberta ao lado do jogo), o
que ele NAO e (ele nao coleta nada, nao mira janela, nao le pixel, nao manda alerta; derrubar
esta janela preta nao interrompe a coleta da noite), e o que ele precisa (que o
`vigiar-mercado.bat` ja tenha rodado ao menos uma vez, para haver arquivo a ler — e se nao
houver, a propria pagina explica isso com todas as letras).

Copiar a ida para a pasta do proprio arquivo e a descoberta do Python guardando o CAMINHO
completo e nunca o nome do comando, incluindo a recusa do atalho da loja por substituicao de
string nativa e os caminhos alternativos de instalacao.

Manter o bloco de ambiente proprio e a sonda de dependencias, mas com a sonda MEDIDA para este
processo, e nao copiada do irmao: o dashboard nao captura tela e nao le nome nenhum, entao nada
de captura, de janela nem de OCR entra na sonda. Escrever ao lado a medicao registrada no plano
01-01: o processo do dashboard carrega OpenCV e numpy porque o formatador do mercado os arrasta
pela cadeia do console, e essa aresta nao foi cortada nesta fase por decisao travada — logo a
sonda cobre exatamente esses dois, e nada mais. Registrar tambem, no comentario, que a pesquisa
antecipava um lancador sem ambiente virtual nenhum e que a medicao derrubou essa expectativa: um
numero que caiu precisa dizer que caiu, inclusive num arquivo de lote.

Manter a regra estrutural do molde, que e o coracao dele: TODOS os blocos de erro vem ANTES da
linha de execucao, com um desvio por cima, e nao ha bloco de encerramento nenhum depois dela.
Copiar o comentario que registra o defeito medido em campo, adaptando a referencia.

A linha de execucao chama o modulo do dashboard diretamente pelo interpretador do ambiente,
repassando os argumentos extras — inclusive a opcao de porta, para o usuario poder mudar a porta
sem editar o fonte se a dela estiver ocupada. Escrever ao lado que o modulo e chamado direto e
NAO pelo ponto de entrada do pacote, e que e assim que o caminho do `--mercado` fica intocado
(DASH-06).

Nao abrir o navegador pelo `.bat`: quem abre e o `main` do Python, depois de o bind ter dado
certo. Escrever ao lado por que — abrir antes do bind mostraria uma aba de erro de conexao ao
usuario quando a porta estivesse ocupada, exatamente no caso em que ele mais precisa ler a
mensagem na janela preta.

O arquivo inteiro em ASCII puro, como todo texto que este projeto poe na frente do usuario:
o console do Windows abre em uma pagina de codigo antiga e a mesma frase acaba colada num log,
numa mensagem de erro e num editor qualquer.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_bat.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard.bat` existe na raiz do repositorio, irmao de `vigiar-mercado.bat`.
    - A linha de execucao chama `-m l2scanner.dashboard` e repassa os argumentos extras.
    - A sonda de dependencias do lancador nomeia exatamente os dois pacotes que a medicao mostrou serem carregados, e nenhum pacote de captura, de janela ou de OCR.
    - `git diff --stat l2scanner/__main__.py` sai vazio.
    - O arquivo nao contem nenhum byte fora da faixa ASCII (conferido pelo teste da Tarefa 2).
    - Rodar `dashboard.bat` a partir de outra pasta de trabalho funciona, porque ele vai para a pasta do proprio arquivo antes de qualquer coisa — registrar a saida do teste manual no SUMMARY.
  </acceptance_criteria>
  <done>Dois cliques sobem o dashboard e abrem o navegador, os erros saem antes da execucao, e a sonda diz a verdade medida sobre este processo.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O teste do lancador — a estrutura, o ASCII e a ordem dos blocos</name>
  <files>tests/test_dashboard_bat.py</files>
  <read_first>
    - `tests/test_vigiar_mercado_bat.py:53-78` — os auxiliares do molde: a extracao das linhas de mensagem, a localizacao da posicao da linha de execucao, e a leitura do arquivo na pagina de codigo antiga; mais o teste que afirma que o lancador e ASCII puro.
    - `tests/test_calibrar_mercado_bat.py` e `tests/test_ponte_bat.py` — as outras duas instancias do mesmo molde nesta arvore, para o novo teste nao inventar um terceiro dialeto.
    - `vigiar-mercado.bat:129-136` — o comentario que explica por que a ordem dos blocos e estrutural e nao estilistica.
  </read_first>
  <behavior>
    - O arquivo e ASCII puro: nenhum byte acima de 127.
    - Toda linha de mensagem ao usuario aparece ANTES da linha de execucao, e nao existe nenhuma depois dela.
    - Existe pelo menos um rotulo de erro alcancado por desvio, e o desvio por cima da linha de execucao esta presente.
    - A linha de execucao chama o modulo do dashboard e nao o ponto de entrada do pacote.
    - A sonda de dependencias nomeia apenas os pacotes que este processo carrega, e nenhum pacote de captura, de janela ou de OCR.
    - O lancador recusa o atalho da loja da Microsoft, pela mesma substituicao de string do irmao.
    - O lancador vai para a pasta do proprio arquivo antes de qualquer outra instrucao efetiva.
  </behavior>
  <action>
Escrever `tests/test_dashboard_bat.py` reusando o molde e os auxiliares dos testes de lancador ja
existentes — mesma leitura na pagina de codigo antiga, mesma extracao de linhas de mensagem,
mesma localizacao da posicao da linha de execucao. Se os auxiliares estiverem escritos em um dos
arquivos de teste existentes e nao num modulo compartilhado, copiar a implementacao com um
comentario dizendo de onde veio e por que nao foi importada — um teste importando outro teste e
um acoplamento que a suite desta casa nao usa.

Escrever uma classe por invariante do bloco `<behavior>`, com nomes em portugues e o fato em
caixa alta no ponto que importa.

Acrescentar o CONTROLE NEGATIVO do auxiliar de ordem: alimentar a funcao que localiza a posicao
da linha de execucao com um texto fabricado no proprio teste que tem uma linha de mensagem
DEPOIS da execucao, e exigir que a checagem acuse. Sem isso, um erro no reconhecimento das linhas
de mensagem deixaria o teste de ordem verde sobre qualquer arquivo — e a promessa estrutural que
o molde existe para garantir viraria decorativa.

Acrescentar tambem o teste que afirma que a sonda do lancador NAO nomeia nenhum dos pacotes de
captura, de janela ou de OCR: a lista desses pacotes fica escrita a mao no proprio teste, com o
comentario de que deriva-la do lancador do mercado faria o teste concordar com qualquer coisa
que alguem copiasse de la.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_bat.py tests/test_vigiar_mercado_bat.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_bat.py -q` termina em 0 com pelo menos 8 testes coletados.
    - Existe o teste de ASCII puro afirmando que a decodificacao estrita em ASCII do arquivo nao levanta.
    - Existe o teste de ordem afirmando que a posicao da ultima linha de mensagem e menor que a posicao da linha de execucao.
    - Existe o CONTROLE NEGATIVO que alimenta o auxiliar de ordem com um texto fabricado tendo uma mensagem depois da execucao e exige que ele acuse.
    - Existe o teste que afirma que a sonda do lancador nao nomeia nenhum pacote da lista escrita a mao de captura, janela e OCR.
    - Existe o teste que afirma que a linha de execucao chama o modulo do dashboard, e nao o ponto de entrada do pacote.
  </acceptance_criteria>
  <done>O lancador tem as mesmas garantias estruturais que os irmaos dele, e o auxiliar que as verifica tem controle negativo provando que ele acusa.</done>
</task>

<task type="auto">
  <name>Tarefa 3: A prova do DASH-05 — instanciar uma SEGUNDA serie sem codigo de grafico novo</name>
  <files>tests/test_dashboard_serie_generica.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `O componente de serie e generico (DASH-05)` — o contrato de props e a regra de que a escolha da cor da segunda instancia e do chamador.
    - `01-UI-SPEC.md` `## UI Considerations`, linha `zero-one-many` — o que exatamente fica como reserva de verificacao e por que.
    - `REQUIREMENTS.md`, DASH-05 — "o v1 so mostra Adena na tela; a generalidade e provada em teste".
    - `l2scanner/dashboard_dados.py` — `serie_para_o_grafico` e a forma da lista de series no payload, entregues pelo plano 01-02.
    - `l2scanner/recursos/dashboard/dashboard.js` — a funcao de instanciacao de serie, entregue pelo plano 01-07, e os nomes de propriedade que ela consome.
    - `tests/test_mercado_analise.py:283-300` — o construtor de observacao a mao, para montar a segunda serie sem disco.
  </read_first>
  <behavior>
    - Um CSV de fixture com observacoes de DUAS series (a da Adena e um item qualquer da aba de negociacao) produz um payload com dois elementos na lista de series.
    - Os dois elementos tem exatamente o mesmo conjunto de chaves — nenhuma chave existe so para a Adena e nenhuma so para a outra.
    - O formatador de cada serie e escolhido pelo ponto de decisao unico do mercado: a serie da Adena recebe o formatador de taxa, a outra recebe o de unitario comum — e os textos resultantes sao diferentes em FORMA, provando que a escolha aconteceu.
    - A segunda serie atravessa o servidor: um pedido ao endpoint devolve os dois elementos.
    - A funcao de instanciacao de serie do JS consome apenas nomes de propriedade que existem nos DOIS elementos.
  </behavior>
  <action>
Escrever `tests/test_dashboard_serie_generica.py` como a prova do DASH-05 sem navegador.

Montar em `tmp_path` um CSV de fixture com observacoes de duas series distintas: a da Adena, pela
chave-sentinela do catalogo, e um item comum da aba de negociacao. Nenhum disco real, nenhum
frame, nenhum OCR.

Afirmar sobre o payload: dois elementos na lista de series, e conjuntos de chaves IDENTICOS entre
eles. Escrever na docstring da classe por que essa e a forma correta de provar generalidade: se
existisse uma chave so da Adena, instanciar a segunda serie exigiria codigo novo — e o requisito
proibe exatamente isso.

Afirmar que o formatador foi escolhido pelo ponto de decisao unico: comparar o texto do menor da
serie da Adena com o texto do menor da outra serie e exigir que eles difiram em forma, e nao so
em digito. Escrever ao lado que chamar o formatador errado imprimiria um numero zerado com toda
a confianca do mundo — o defeito que o ponto de decisao unico do mercado existe para nao ter.

Afirmar a travessia pelo servidor: subir o servidor na fixture de porta efemera, pedir o
endpoint e afirmar que os dois elementos chegaram com o mesmo contrato.

Afirmar do lado do navegador, por leitura de fonte: extrair do `dashboard.js` os nomes de
propriedade que a funcao de instanciacao de serie consome e afirmar que TODOS existem nos dois
elementos do payload. Este e o elo que fecha a prova — sem ele, o teste provaria que o servidor e
generico e nao que o desenho e.

Escrever, no cabecalho do arquivo, a fronteira honesta desta prova: ela mostra que o DADO e o
CONTRATO sao genericos e que o codigo do grafico nao precisa mudar; ela NAO mostra que duas
series desenhadas ao mesmo tempo ficam legiveis na tela, porque isso exige navegador e a casa
recusou instalador pesado de automacao de navegador. Essa metade e verificacao humana declarada,
e esta registrada como tal.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_serie_generica.py -q</automated>
    <human-check>Instanciar temporariamente a segunda serie na tela (uma linha no console do navegador, chamando a funcao de instanciacao com o segundo elemento do payload) e conferir que ela desenha duas linhas legiveis, com o titulo da outra serie e sem o codigo do grafico ter mudado. Desfazer em seguida — o v1 mostra so a Adena por decisao travada.</human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_serie_generica.py -q` termina em 0 com pelo menos 6 testes coletados.
    - Existe um teste que afirma que os dois elementos da lista de series tem conjuntos de chaves IDENTICOS.
    - Existe um teste que afirma que os textos formatados das duas series diferem em FORMA, provando que o ponto de decisao unico do mercado foi consultado.
    - Existe um teste que sobe o servidor e afirma que os dois elementos atravessam o endpoint.
    - Existe um teste que extrai do `dashboard.js` os nomes de propriedade consumidos pela funcao de instanciacao e afirma, NA MESMA FUNCAO DE TESTE e ANTES da comparacao, que o conjunto extraido NAO ESTA VAZIO — sem essa assercao, um `dashboard.js` sem a funcao (ou uma extracao que falhasse em silencio) faria o teste passar por vacuidade, que e o modo de falha que 01-01 Tarefa 3 existe para eliminar.
    - Existe o CONTROLE NEGATIVO da extracao: alimentar a funcao de extracao com um texto de JS fabricado no proprio teste, contendo uma funcao de serie com propriedades conhecidas, e afirmar que ela devolve exatamente essas propriedades; e alimentar com um texto sem funcao de serie nenhuma e afirmar conjunto vazio. E o que prova que a extracao mede o que diz medir.
    - O cabecalho do arquivo declara por extenso o que esta prova NAO cobre, e a metade que fica como verificacao humana esta registrada.
    - `python -m pytest tests/ -q` termina em 0.
  </acceptance_criteria>
  <done>Uma segunda serie atravessa dado, contrato e endpoint sem uma linha de codigo de grafico nova, e a metade que so o olho ve esta declarada como tal em vez de omitida.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `dashboard.bat` — o lancador proprio, irmao do `vigiar-mercado.bat`, com a sonda medida para
  este processo e os blocos de erro antes da execucao
- `tests/test_dashboard_bat.py` — ASCII puro, ordem dos blocos com controle negativo, a linha de
  execucao, e a sonda que nao copia a do mercado
- `tests/test_dashboard_serie_generica.py` — a prova do DASH-05 em dado, contrato, endpoint e
  nomes consumidos pelo navegador

`l2scanner/__main__.py` continua intocado, e por isso o caminho do `--mercado` fica identico
(DASH-06).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lancador -> interpretador | O `.bat` escolhe qual Python roda; um atalho errado ou um caminho nao citado leva a um erro que o usuario nao consegue diagnosticar |
| lancador -> porta TCP | A porta e o unico recurso do sistema operacional que este processo toma, e as faixas reservadas pelo Windows mudam a cada reinicializacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27 | Spoofing | atalho da loja da Microsoft no lugar do Python | medium | mitigate | Recusa por substituicao de string nativa, copiada do molde do irmao, e prendida por teste |
| T-01-28 | Repudiation | lancador anunciando desfecho que nao conferiu | high | mitigate | Blocos de erro antes da linha de execucao com desvio por cima; teste de ordem com CONTROLE NEGATIVO provando que o auxiliar acusa |
| T-01-18 | Tampering | dois dashboards na mesma porta | high | mitigate | Ja mitigado no plano 01-05 pelo bind exclusivo; aqui, a opcao de porta repassada pelo lancador da ao usuario a saida sem editar fonte |
| T-01-29 | Information Disclosure | lancador expondo o servidor na rede | high | mitigate | O lancador nao passa endereco nenhum; o bind local esta travado no proprio modulo e prendido por teste de fonte no plano 01-05 |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_bat.py tests/test_dashboard_serie_generica.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- `git diff --stat <base-da-fase> -- l2scanner/__main__.py l2scanner/mercado_modo.py l2scanner/rastreador.py l2scanner/visao.py l2scanner/console.py l2scanner/mercado_registro.py l2scanner/mercado_analise.py l2scanner/mercado_console.py requirements.txt` sai VAZIO.
- `git diff --numstat <base-da-fase> -- l2scanner/config.py` mostra **exatamente uma linha removida** — a definicao antiga de `RAIZ`. As linhas acrescentadas podem ser mais de uma (o import mais o comentario que explica a re-exportacao), mas nenhuma linha do diff pode mencionar `ARQUIVO_ENV`, `ARQUIVO_CONFIG`, `ARQUIVO_CONFIG_LOCAL`, `def ` ou `class `: o corte toca a constante e nada mais.
- `git diff --name-only <base-da-fase> -- l2scanner/ tests/ requirements.txt` nao lista nenhum arquivo PREEXISTENTE alem de `l2scanner/config.py`, `l2scanner/mercado_catalogo.py` e `tests/test_mercado_firewall_de_fase.py`. Um quarto arquivo preexistente na lista e quebra do DASH-06 e para a fase.
- Teste manual do lancador registrado no SUMMARY: dois cliques sobem o servidor e abrem o
  navegador; um segundo clique com o primeiro aberto mostra a mensagem de porta ocupada e nao um
  traceback.
</verification>

<success_criteria>
- Dois cliques sobem a interface e abrem o navegador.
- Os blocos de erro saem antes da execucao, e o controle negativo prova que o teste que verifica
  isso acusa quando a ordem quebra.
- A sonda de dependencias diz a verdade medida sobre este processo.
- Uma segunda serie atravessa toda a pilha sem codigo de grafico novo, e o que a prova nao cobre
  esta declarado.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-08-SUMMARY.md` when done
</output>
