---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 07
type: execute
wave: 4
depends_on: ["01-02", "01-04", "01-05", "01-06"]
files_modified:
  - l2scanner/recursos/dashboard/dashboard.js
  - tests/test_dashboard_js.py
autonomous: true
requirements: [DASH-02, DASH-03, DASH-04, DASH-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uma pagina nova lida pelo scanner aparece na tela sem recarregar a mao: o navegador consulta o endpoint a cada dois segundos e repinta quando o dado muda."
    - "O navegador EXIBE COMO RECEBEU toda string vinda do Python: nao reacentua, nao reformata, nao arredonda e nao encurta. Um segundo formatador e exatamente o que DASH-03 recusa."
    - "O grafico mostra duas linhas — menor pedido visivel em traco solido e mediana em tracejado — distinguiveis sem depender de cor."
    - "O zoom vai das horas do dia ate dias atras, por arrasto e por roda do mouse, e o botao de restaurar o alcance total devolve o periodo inteiro."
    - "O componente de serie e generico: ele recebe titulo, unidade, pontos, formatador e os dois rotulos, e a palavra que nomeia a serie da Adena nao aparece nem no CSS nem no modulo do grafico."
    - "As cores do grafico sao lidas do CSS por nome de token; nenhum literal hexadecimal existe no JS."
    - "empty (#destaque, #serie): sem linha da serie, a tela renderiza a placa de estado vazio com a contagem real de linhas do arquivo e a grade do grafico desenhada; grafico vazio mudo e proibido."
    - "loading (#destaque, #serie): a primeira pintura mostra o rotulo de leitura em curso no lugar do numero, e o polling seguinte nao reintroduz estado de carregamento nem indicador giratorio."
    - "error (pagina inteira): tres erros distintos, cada um com uma causa e um proximo passo — arquivo ausente, cabecalho quebrado e servidor mudo. Falha fechada, nunca adivinhar coluna."
    - "partial (#destaque, #serie): abaixo do piso de evidencia a tela mostra a frase de falta vinda do Python, nunca um numero; e a nota de linha final incompleta aparece no rodape."
    - "error (#procedencia): texto que nao seja um numero positivo nao e salvo, o formulario recusa com a frase que diz o que se espera, o cambio anterior continua valendo, e o R$ na tela continua sendo o do cambio antigo — nunca um valor calculado a partir do texto recusado."
    - "loading (#procedencia): enquanto o envio esta em voo o botao fica desabilitado com o rotulo de salvamento; ele nao vira indicador giratorio, o campo nao e limpo, e se a resposta falhar o valor digitado PERMANECE no campo."
    - "partial (#procedencia): o rodape mostra cada peca de procedencia que existe e NOMEIA a que falta, em vez de sumir com a linha."
    - statement: "stale (#destaque, #procedencia): com o scanner parado o valor permanece e a recencia vira a cor fria com a frase de dado velho; a tela nunca afirma 'agora'. A regra de precedencia e testavel no endpoint, mas o DESENHO do estado velho e verificacao humana declarada no fim da fase."
      verification: backstop
  artifacts:
    - l2scanner/recursos/dashboard/dashboard.js
    - tests/test_dashboard_js.py
  key_links:
    - "dashboard.js -> GET /dados a cada dois segundos: o intervalo vem DENTRO do payload, para nao existir um segundo numero no navegador."
    - "dashboard.js -> getPropertyValue dos tokens: a paleta mora so no CSS, e uma cor sem nome de token e uma cor que o grafico nao consegue pedir."
    - "o componente de serie -> um elemento da lista de series do payload: instanciar a segunda serie e passar outro elemento, e nao escrever codigo de grafico novo."
    - "o envio do formulario -> preventDefault e fetch: a diretiva que proibe destino de formulario torna o submit nativo impossivel, e interceptar e obrigatorio, nao opcional."
---

<objective>
O codigo do navegador: o polling, a maquina de estados da tela, o componente de serie generico
que instancia a biblioteca de grafico, o zoom que a medicao provou que teriamos de escrever nos
mesmos, e o envio do cambio sem recarregar.

Purpose: e aqui que a tela vira viva. E e aqui que mora a regra mais facil de quebrar sem
perceber — reformatar no navegador um numero que ja veio pronto do Python. Toda a disciplina do
console de mercado atravessa a fronteira HTTP como string; o JS e um exibidor, nao um segundo
formatador.

Output: `l2scanner/recursos/dashboard/dashboard.js` e `tests/test_dashboard_js.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-RESEARCH.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-02-SUMMARY.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-05-SUMMARY.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: O polling e a maquina de estados — exibir como recebeu</name>
  <files>l2scanner/recursos/dashboard/dashboard.js, tests/test_dashboard_js.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Estados da tela e sua precedencia` — a ordem fechada e as tres ortogonais; o JS OBEDECE a precedencia que o servidor ja decidiu, e nao a recalcula.
    - `01-UI-SPEC.md` secao `Copywriting Contract`, especialmente a REGRA DURA de que a string vinda do Python nao e reescrita no navegador, e a lista de frases proibidas na tela.
    - `01-UI-SPEC.md` secao `Interacao` — sem indicador giratorio no polling, pulso de fundo curto quando o valor muda, e movimento reduzido zerando tudo.
    - `01-UI-SPEC.md` secao `## UI Considerations`, as linhas `loading`, `error`, `partial` e `stale`.
    - O contrato do payload entregue pelo plano 01-02 (`01-02-SUMMARY.md`): as chaves de estado, fonte, avisos, destaque e series, e o intervalo de polling servido dentro dele.
    - `l2scanner/recursos/dashboard/dashboard.js` — o `fetch` unico que o tracer deixou, e o comentario de topo sobre o segundo formatador.
  </read_first>
  <action>
Transformar o `fetch` unico do tracer num laco de polling com o intervalo LIDO DO PAYLOAD, e nao
escrito no JS. Escrever ao lado que um intervalo escrito em dois lugares envelhece em um deles.

Escrever a funcao de pintura que recebe o payload e alterna os atributos de estado que o CSS do
plano 01-06 ja desenha. O JS NAO decide precedencia: ele le a chave de estado que o servidor
mandou e aplica. Escrever isso como comentario, porque a tentacao de recalcular no cliente e o
comeco de duas verdades sobre o mesmo fato.

Toda insercao de texto vindo do servidor acontece por propriedade de TEXTO, e nunca por
propriedade de marcacao — o nome exibido da serie vem de OCR, que e conteudo nao confiavel.
Escrever a razao ao lado.

Nenhuma string do servidor e transformada: nada de reacentuar, nada de arredondar, nada de
encurtar, nada de formatar numero. Escrever no topo do arquivo o paragrafo completo: a divergencia
de acentuacao entre a moldura em UTF-8 (nossa) e o valor em ASCII (do Python) e INTENCIONAL,
porque reacentuar significaria um segundo formatador, que e exatamente o que DASH-03 recusa; e a
mesma logica vale para encurtar o rotulo de unidade longo, que o UI-SPEC proibe por extenso.

Implementar o estado de servidor mudo: se uma consulta falhar ou estourar o tempo, manter os
valores da ultima resposta na tela, ligar o atributo de estado da faixa do topo, e mostrar a
frase do contrato com o tempo desde a ultima resposta. Nunca limpar a tela por causa de uma
consulta que falhou.

Implementar o pulso de valor mudado: comparar o texto novo com o anterior e, so quando mudar,
ligar por um instante curto o atributo que o CSS anima. Nada se move e nada pisca; e um pulso de
fundo. A preferencia por movimento reduzido ja e respeitada pelo CSS, entao o JS nao precisa de
um segundo caminho — escrever isso ao lado, para ninguem acrescentar um.

Implementar a primeira pintura com o rotulo de leitura em curso no lugar do numero, e garantir
que ela nao volte nos ciclos seguintes: polling nao e carregamento.

Comecar `tests/test_dashboard_js.py`, que le o JS como TEXTO e afirma invariantes de fonte — a
casa ja usa leitura de fonte como tripwire de arquitetura, e sem navegador e o que da para
afirmar. Classe `TestOJSNaoEUmSegundoFormatador`: afirmar que a contagem de chamadas de
arredondamento com casas fixas, de conversao para numero local, e de qualquer substituicao de
virgula por ponto sobre texto vindo do payload e zero; e afirmar que o paragrafo de refutacao
esta no topo do arquivo. Classe `TestOJSInsereTEXTOeNaoMARCACAO`: afirmar que a contagem de
atribuicoes a propriedade de marcacao interna e zero.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_js.py -q -k "formatador or TEXTO or polling"</automated>
  </verify>
  <acceptance_criteria>
    - Existe um teste que afirma que o intervalo de polling NAO aparece como numero literal no JS e que o codigo o le de uma chave do payload — se o servidor mudar o intervalo, o navegador acompanha sozinho.
    - Existe um teste que afirma que a contagem de atribuicoes a propriedade de marcacao interna no JS e zero — todo texto do servidor entra como texto.
    - Existe um teste que afirma que o topo do arquivo contem o paragrafo de refutacao sobre o segundo formatador, conferido pela presenca das palavras que o nomeiam.
    - Existe um teste que afirma que a chave de estado do payload e apenas LIDA e nunca recalculada no JS — conferido pela ausencia de qualquer comparacao de contagem de observacoes com um piso dentro do JS.
    - `python -m pytest tests/test_dashboard_js.py -q` termina em 0.
  </acceptance_criteria>
  <done>A tela se atualiza sozinha a cada dois segundos, obedece a precedencia que o servidor decidiu, exibe cada string como recebeu, e nao limpa nada quando o servidor cala.</done>
</task>

<task type="auto">
  <name>Tarefa 2: O componente de serie generico e a instanciacao do grafico</name>
  <files>l2scanner/recursos/dashboard/dashboard.js, tests/test_dashboard_js.py</files>
  <read_first>
    - `01-UI-SPEC.md` secao `O componente de serie e generico (DASH-05)` — o contrato de props exato, os seletores permitidos, a proibicao da palavra que nomeia a serie da Adena no CSS e no modulo do grafico, e a regra de que a cor da segunda instancia e escolha do chamador.
    - `01-UI-SPEC.md` secao `O grafico e tematizado pela superficie da propria biblioteca` — a leitura das cores por nome de token, a tabela dos elementos do grafico, e a regra de nenhum literal hexadecimal no JS.
    - `01-UI-SPEC.md` secao `O grafico nao pode distinguir so por cor` — traco solido de dois pixels para o menor, tracejado de um e meio para a mediana.
    - `01-UI-SPEC.md` subsecao `O estado vazio, desenhado` — os tres elementos que a area do grafico renderiza sem dado.
    - `01-RESEARCH.md:857-872` — a fronteira do `float`: o numero no payload e o PIXEL, a string e a VERDADE, e o texto da dica e da legenda vem da string.
    - O contrato de serie entregue pelo plano 01-02: `chave`, `titulo`, `unidade`, `rotulo_principal`, `rotulo_tipico`, `pontos` e `baldes` por nivel de zoom.
  </read_first>
  <action>
Escrever a funcao que instancia UMA serie, recebendo exatamente o objeto que o payload entrega e
mais nada. Ela nao conhece a Adena: a palavra que nomeia essa serie nao pode aparecer no modulo
do grafico nem no CSS, e a instancia da Adena e apenas a primeira chamada.

Ler as cores do CSS por nome de token — a cor de acento para a linha principal, a cor da serie
tipica para a mediana, a cor da grade e a de texto fraco para eixos e rotulos. Escrever ao lado a
razao que o UI-SPEC da: uma cor sem nome de token e uma cor que o grafico nao consegue pedir, e
foi por isso que a cor da mediana ganhou token proprio em vez de continuar sendo um literal
solto.

Configurar as duas linhas com traco solido e traco tracejado, alem da diferenca de cor. Escrever
ao lado que solido contra tracejado sobrevive a daltonismo, a monitor mal calibrado e ao gama do
cliente, e que cor sozinha nao sobreviveria.

Escolher qual conjunto de pontos desenhar — a serie crua por instante ou um dos conjuntos de
balde — conforme o alcance visivel: a janela do zoom e decisao do navegador, mas a CONTA de
agregacao ja aconteceu no servidor. Escrever ao lado por que a conta nao pode migrar para ca: a
mediana inferior sobre fracao exata so existe no Python, e no navegador viraria ordenacao de
ponto flutuante — e um numero exibido tem de ter existido.

Montar o texto da dica sob o cursor a partir das STRINGS do ponto, e nunca do numero. Escrever a
regra ao lado: o numero no payload e o pixel, a string e a verdade.

Renderizar o estado vazio dentro da area do grafico: os eixos e a grade tematizados desenhados, a
placa central visivel, e a legenda das duas linhas ja aparecendo. Escrever ao lado que grafico
vazio mudo esta proibido porque seria indistinguivel de um dashboard quebrado.

Ligar o botao que restaura o alcance total ao metodo de escala da biblioteca, devolvendo o
periodo inteiro. Sem ele o usuario fica preso no zoom, e o UI-SPEC ja travou o texto do rotulo.

Acrescentar a `tests/test_dashboard_js.py` a classe `TestOComponenteDeSerieEGenerico`: afirmar
que a palavra que nomeia a serie da Adena nao aparece em `dashboard.js` nem em `dashboard.css`;
afirmar que a contagem de literais hexadecimais de cor no JS e zero; afirmar que a funcao de
serie recebe um unico parametro de objeto e que os nomes das propriedades usadas dentro dela sao
exatamente os do contrato do UI-SPEC; e afirmar que os seletores usados sao os da familia de
serie e nunca um seletor com o nome da Adena.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_js.py -q -k "generico or serie or cor"</automated>
  </verify>
  <acceptance_criteria>
    - Existe um teste que afirma que a palavra que nomeia a serie da Adena nao aparece em `dashboard.js` nem em `dashboard.css` — e que ela APARECE no payload servido pelo Python, para provar que a genericidade e do desenho e nao um efeito de a palavra nao existir em lugar nenhum.
    - Existe um teste que afirma que a contagem de literais hexadecimais de cor em `dashboard.js` e zero e que ha ao menos quatro leituras de valor de propriedade customizada do CSS.
    - Existe um teste que afirma que a funcao de instanciacao de serie usa exatamente os nomes de propriedade do contrato do UI-SPEC, e nenhum a mais.
    - Existe um teste que afirma que a configuracao das duas linhas difere em TRACO alem de diferir em cor — conferido pela presenca da configuracao de tracejado.
    - Existe um teste que afirma que o texto da dica e montado a partir das chaves de texto do ponto, e nao das chaves numericas.
    - `python -m pytest tests/test_dashboard_js.py -q` termina em 0.
  </acceptance_criteria>
  <done>Uma serie e desenhada por uma funcao que nao conhece a Adena, com as cores vindas do CSS por nome de token, duas linhas distinguiveis sem cor, e o estado vazio com a area do grafico viva.</done>
</task>

<task type="auto">
  <name>Tarefa 3: O zoom escrito por nos, e o envio do cambio sem recarregar</name>
  <files>l2scanner/recursos/dashboard/dashboard.js, tests/test_dashboard_js.py</files>
  <read_first>
    - `01-RESEARCH.md:1188-1225` — a refutacao medida: a suposicao do UI-SPEC de que toda biblioteca dessa classe faz zoom por roda com configuracao e FALSA para a escolhida; a listagem dos eventos que ela de fato registra; a citacao do repositorio oficial dizendo que nao ha arrasto de rolagem embutido e que o zoom por roda se acrescenta por fora, pela API de plugins e hooks; e a consequencia dita por extenso para o plano.
    - `01-RESEARCH.md:726-740` (Pitfall 6) — a colisao entre a diretiva que proibe destino de formulario e um formulario de verdade: sem interceptar, a tecla de confirmacao dispara um envio nativo que o navegador BLOQUEIA, e nada aparece alem de um erro no console. Isto e a falha fechada CERTA, mas tem de estar escrito.
    - `01-UI-SPEC.md` secao `Interacao`, linhas de zoom e de envio do cambio; e as linhas `loading | #procedencia` e `error | #procedencia` das `## UI Considerations`.
    - `vendor/README.md` — a nota de revisao do plano 01-04, que ja registra esta mesma refutacao do lado do artefato.
    - O contrato de resposta do `POST /cambio` entregue pelo plano 01-05.
  </read_first>
  <action>
Escrever o zoom por roda do mouse e o arrasto de deslocamento pela API de hooks da biblioteca,
sobre o elemento de sobreposicao do grafico: a roda recalcula a escala do eixo temporal em torno
da posicao do cursor e chama o metodo de definicao de escala; o arrasto com o botao pressionado
desloca a janela. O zoom por selecao retangular ja e nativo e nao se reescreve.

Escrever imediatamente acima desse bloco a REFUTACAO completa, com a medicao: o UI-SPEC afirmava
que roda e arrasto viriam da biblioteca porque "toda biblioteca dessa classe faz isso com
config"; a medicao sobre o arquivo minificado mostrou ZERO registradores do evento de roda na
escolhida e zero na segunda candidata, com apenas a terceira tendo o evento — e a documentacao
oficial da escolhida diz textualmente que nao ha arrasto de rolagem embutido e que o zoom por
roda se acrescenta por fora. Fechar dizendo que estas linhas sao consequencia dessa medicao e
nao capricho, e que a alternativa (a biblioteca que trazia o evento pronto) foi recusada por
outros tres motivos: veredito de legitimidade suspeito por ser nova demais, quase quatro vezes o
tamanho, e o risco nao medido de ela ser feita para grade temporal regular enquanto a nossa
serie e esparsa e irregular.

Escrever o envio do cambio: interceptar o envio do formulario com a prevencao do comportamento
padrao e enviar por consulta assincrona ao endpoint. Escrever ao lado a razao inteira do Pitfall
6 — a diretiva que proibe destino de formulario torna o envio nativo impossivel, e isso e a
falha fechada certa (se o JS morrer, o formulario nao envia, em vez de recarregar a pagina para
lugar nenhum), mas o interceptador e OBRIGATORIO e nao opcional.

Durante o envio: desabilitar o botao e trocar o rotulo para o de salvamento em curso, sem virar
indicador giratorio, e sem limpar o campo. Em caso de recusa, voltar o botao, mostrar a frase de
erro que veio do servidor, e MANTER o valor digitado no campo para o usuario nao redigitar. Em
caso de sucesso, mostrar a confirmacao com o carimbo e repintar com o payload novo — o R$ aparece
sem recarregar.

Escrever ao lado que a validacao no navegador e conveniencia e que a falha fechada mora no
servidor: um envio direto, feito por fora da pagina, e recusado igual, e ha teste do lado do
Python provando isso.

Acrescentar a `tests/test_dashboard_js.py` a classe `TestOZoomEDeNosEDizPorQue`: afirmar que o JS
registra o evento de roda sobre o elemento de sobreposicao; afirmar que o bloco de refutacao esta
presente e cita a medicao (a contagem zero e a frase da documentacao oficial), e nao apenas a
conclusao; e afirmar que o botao de restaurar alcance esta ligado ao metodo de escala.
Acrescentar `TestOEnvioDoCambioEInterceptado`: afirmar que ha prevencao do comportamento padrao
no manipulador de envio, que o rotulo de salvamento existe, e que nao ha nenhuma limpeza do campo
no caminho de erro.
  </action>
  <acceptance_criteria>
    - Existe um teste que afirma que o JS registra o evento de roda sobre o elemento de sobreposicao do grafico e chama o metodo de definicao de escala.
    - Existe um teste que afirma que o bloco de refutacao do zoom por roda esta no JS e contem a MEDICAO — a contagem zero de registradores e a citacao da documentacao oficial — e nao so a conclusao.
    - Existe um teste que afirma que o manipulador de envio do formulario chama a prevencao do comportamento padrao; sem isso a diretiva de destino de formulario bloquearia o envio silenciosamente.
    - Existe um teste que afirma que no caminho de erro do envio nao ha atribuicao de valor vazio ao campo — o valor digitado permanece.
    - Existe um teste que afirma que o rotulo de salvamento em curso e o texto do botao primario sao os travados no contrato de copia.
    - `python -m pytest tests/test_dashboard_js.py -q` termina em 0 com pelo menos 18 testes coletados.
    - `python -m pytest tests/ -q` termina em 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_dashboard_js.py tests/test_dashboard_pagina.py -q</automated>
    <human-check>Com o dashboard aberto: a roda do mouse sobre o grafico aproxima e afasta em torno do cursor; o arrasto desloca a janela; o botao de restaurar devolve o periodo inteiro. No campo do cambio, digitar um valor e apertar a tecla de confirmacao SALVA sem recarregar a pagina, e o R$ aparece; digitar um texto invalido mostra a frase de recusa e o R$ da tela continua sendo o do cambio antigo. Com o scanner parado por alguns minutos, a recencia muda de cor e a frase de dado velho aparece, com o numero ainda na tela — a tela nao afirma "agora".</human-check>
  </verify>
  <done>O zoom funciona por roda e por arrasto com codigo nosso e a medicao que o justifica escrita ao lado, e o cambio e salvo sem recarregar com o valor preservado em caso de erro.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `l2scanner/recursos/dashboard/dashboard.js` — o laco de polling com o intervalo lido do payload,
  a pintura por atributo de estado, o estado de servidor mudo, o pulso de valor mudado, a funcao
  generica de instanciacao de serie, a leitura de cores por nome de token, o zoom por roda e
  arrasto pelos hooks com a refutacao medida ao lado, o botao de restaurar alcance, e o envio do
  cambio interceptado
- `tests/test_dashboard_js.py` — os tripwires de fonte: nenhum segundo formatador, nenhuma
  insercao de marcacao, nenhum literal de cor, nenhuma mencao da Adena no codigo do grafico, a
  refutacao presente com a medicao, e a prevencao do comportamento padrao no envio

Nenhum arquivo Python do pacote e tocado por este plano.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload -> DOM | O nome exibido da serie vem de OCR sobre a tela do jogo: conteudo nao confiavel que termina dentro da pagina |
| pagina -> endpoint local | O envio do cambio parte da pagina, mas o endpoint tambem e alcancavel por qualquer outra aba |
| biblioteca vendorizada -> pagina | Codigo de terceiro executando no mesmo contexto da nossa pagina |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Tampering | nome exibido vindo de OCR inserido na pagina | medium | mitigate | Insercao por propriedade de TEXTO em todo o JS, prendida por teste que afirma zero atribuicoes a propriedade de marcacao interna |
| T-01-17 | Elevation of Privilege | envio do cambio | high | mitigate | A defesa real e do lado do servidor (portao de origem, plano 01-05); aqui, o envio interceptado apenas garante que o caminho normal funcione — e o comentario diz por extenso que a validacao do navegador e conveniencia |
| T-01-03 | Tampering | biblioteca vendorizada | high | mitigate | A CSP com origem propria ja barra qualquer conexao de saida; VEND-1..3 no plano 01-04; nenhuma configuracao passada a biblioteca aceita URL como fonte de dado |
| T-01-25 | Information Disclosure | mensagem de erro do servidor exibida na pagina | low | accept | As mensagens sao as do contrato de copia e nomeiam apenas caminhos do proprio projeto; sem elas o usuario nao sabe o que consertar |
| T-01-26 | Denial of Service | polling continuando com o servidor morto | low | mitigate | O estado de servidor mudo mantem os valores e marca a faixa, sem acumular consultas nem limpar a tela |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_js.py tests/test_dashboard_pagina.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- Verificacao humana declarada no fim da fase, com o roteiro do `<human-check>` da Tarefa 3.
- `git diff --stat requirements.txt` sai vazio.
</verification>

<success_criteria>
- A tela se atualiza sozinha e nunca reintroduz estado de carregamento.
- Nenhuma string do Python e reescrita no navegador, provado por tripwire de fonte.
- O grafico distingue as duas linhas sem depender de cor, com as cores vindas dos tokens.
- O zoom por roda e arrasto funciona, com a refutacao medida escrita ao lado do codigo.
- O cambio e salvo sem recarregar, e um texto recusado nao apaga o cambio bom nem o campo.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-07-SUMMARY.md` when done
</output>
