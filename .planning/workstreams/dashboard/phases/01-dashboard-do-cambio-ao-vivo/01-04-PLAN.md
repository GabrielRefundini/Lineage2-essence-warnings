---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - l2scanner/recursos/dashboard/vendor/uPlot.iife.min.js
  - l2scanner/recursos/dashboard/vendor/uPlot.min.css
  - l2scanner/recursos/dashboard/vendor/uPlot.LICENSE
  - l2scanner/recursos/dashboard/vendor/README.md
  - tests/test_firewall_dashboard.py
autonomous: true
requirements: [DASH-03]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "VEND-1: vendor/README.md registra nome, versao exata, licenca, URL de origem, data e SHA-256, e o SHA-256 registrado bate com o do arquivo em disco — recalculado sobre a arvore, nunca copiado da pesquisa."
    - "VEND-2: o fonte da biblioteca foi lido procurando primitivas de rede e de execucao dinamica, e a nota de revisao esta no README com a contagem por primitiva e arquivo:linha para qualquer ocorrencia."
    - "VEND-3: tests/test_firewall_dashboard.py e verde na suite e QUEBRA quando uma primitiva de rede aparece num arquivo de codigo vendorizado — provado por controle negativo sobre a funcao pura de decisao, e nao por uma varredura que ficaria verde sobre uma pasta vazia."
    - "requirements.txt nao ganhou linha nesta fase, e ha teste com a lista de distribuicoes escrita a mao prendendo isso."
    - "A licenca vendorizada e permissiva e o texto dela acompanha o codigo na arvore; copyleft foi recusado pelo UI-SPEC."
    - "A varredura do teste cobre apenas os arquivos de codigo do vendor; o README, que CITA as primitivas na nota de revisao, fica fora do alcance dela por construcao."
  artifacts:
    - l2scanner/recursos/dashboard/vendor/uPlot.iife.min.js
    - l2scanner/recursos/dashboard/vendor/uPlot.min.css
    - l2scanner/recursos/dashboard/vendor/uPlot.LICENSE
    - l2scanner/recursos/dashboard/vendor/README.md
    - tests/test_firewall_dashboard.py
  key_links:
    - "vendor/README.md -> o arquivo em disco: o SHA-256 e o que torna a linha conferivel em vez de declaratoria."
    - "test_firewall_dashboard -> a funcao pura de decisao: o unico ponto de decisao do modulo, e por isso o alvo da mutacao do controle negativo."
---

<objective>
Trazer a biblioteca de grafico para a arvore com as tres provas que o UI-SPEC exige antes do
merge: proveniencia conferivel (VEND-1), revisao de fonte registrada (VEND-2) e um teste que
quebra (VEND-3). A quarta prova, a CSP no cabecalho, ja viaja desde o tracer e e recobrada no
plano 01-05.

Purpose: um arquivo de terceiro minificado, executando no navegador do usuario, na mesma
maquina que roda o jogo e na mesma arvore em que mora o `.env` com o token do Chatwoot, nao
entra sem as quatro provas. O UI-SPEC diz por extenso que incompletas no merge sao BLOCK no
portao de verificacao da fase.

Output: `vendor/uPlot.iife.min.js`, `vendor/uPlot.min.css`, `vendor/uPlot.LICENSE`,
`vendor/README.md` e `tests/test_firewall_dashboard.py`.
</objective>

## A escolha da biblioteca, e por que ela nao pede checkpoint de legitimidade

CTX-3 travou **uPlot 1.6.32**. O veredito do seam de legitimidade de pacote foi **OK** — npm,
publicado em 2025-03-14, 560.268 downloads por semana, repositorio conhecido, `postinstall`
nulo. Nao ha pacote marcado como suposto nem suspeito nesta fase, e portanto **nenhum checkpoint
humano de legitimidade e devido**. A candidata que teria exigido um (`lightweight-charts` 5.2.1,
marcada suspeita por ser nova demais) NAO foi escolhida, e `dygraphs` foi descartada por
reprovar o proprio VEND-2 com duas ocorrencias de primitiva de rede no fonte.

O preco medido da escolha: uPlot **nao** tem zoom por roda do mouse — zero registradores desse
evento no minificado, e o repositorio oficial diz textualmente que nao ha arrasto de rolagem
embutido. Isso vira cerca de 30 linhas nossas pela API de hooks no plano 01-07, e a refutacao da
suposicao do UI-SPEC ("toda biblioteca dessa classe faz isso com config") vai escrita ao lado
delas.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-RESEARCH.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: VEND-1 — vendorizar uPlot 1.6.32 e registrar proveniencia conferivel</name>
  <files>l2scanner/recursos/dashboard/vendor/uPlot.iife.min.js, l2scanner/recursos/dashboard/vendor/uPlot.min.css, l2scanner/recursos/dashboard/vendor/uPlot.LICENSE, l2scanner/recursos/dashboard/vendor/README.md</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Registry Safety`, tabela `As quatro tarefas` — a linha VEND-1 define os campos que o README tem de carregar, e a secao seguinte traz a condicao de bloqueio dita por extenso.
    - `01-RESEARCH.md:229-243` — a tabela de proveniencia ja medida (bytes, SHA-256, licenca, URL) e, logo abaixo dela, o AVISO de que o SHA-256 tem de ser recalculado sobre o arquivo em disco e nao copiado, porque outra origem pode mudar o byte legitimamente.
    - `01-RESEARCH.md:1162-1186` — a tabela comparativa das tres candidatas, incluindo que uPlot precisa de DOIS arquivos (codigo e folha de estilo), e nao de um so.
    - `01-RESEARCH.md:1251-1253` — a validade declarada da pesquisa: sete dias para hashes e versoes das bibliotecas.
  </read_first>
  <precondition>Ha acesso a rede para baixar o pacote uma unica vez, a partir da URL de origem registrada na tabela de proveniencia da pesquisa. Sem rede, a tarefa para e pede o download manual ao usuario — nao inventa o arquivo nem copia hash de documento.</precondition>
  <action>
Baixar da URL de origem registrada na pesquisa os dois artefatos da versao 1.6.32 — o pacote de
codigo em formato de execucao imediata e a folha de estilo — mais o texto da licenca do
repositorio oficial. Gravar os tres em `l2scanner/recursos/dashboard/vendor/`.

Recalcular o SHA-256 dos arquivos EM DISCO e usar esses valores no README, e nao os da tabela da
pesquisa. A pesquisa avisa por que: o hash registrado la e o do artefato de uma CDN especifica, e
um download de outra origem pode diferir legitimamente por convencao de fim de linha. Se o hash
recalculado divergir do registrado na pesquisa, isso nao e um erro a esconder — registrar os
dois no README, com a diferenca explicada, e seguir com o recalculado como autoritativo.

Escrever `vendor/README.md` com os campos do VEND-1 para CADA arquivo vendorizado: nome, versao
exata, licenca, URL de origem, data do download, SHA-256 recalculado e tamanho em bytes. O
tamanho e o que permite conferir a olho que nao veio o arquivo errado.

Acrescentar ao README uma secao curta dizendo por que uPlot e nao as outras duas, com os numeros
da comparacao: o veredito de legitimidade de cada candidata, a contagem de primitivas de rede no
fonte de cada uma, e o total de bytes vendorizados. A decisao fica escrita onde quem abre a
pasta a encontra, e nao so num documento de planejamento.

Escrever tambem que a validade dos hashes e versoes da pesquisa era de sete dias e que o
recalculo desta tarefa e o que passa a valer, para o documento de pesquisa nao envelhecer
mentindo.
  </action>
  <verify>
    <automated>python -m pytest tests/test_firewall_dashboard.py -q -k "proveniencia or sha"</automated>
  </verify>
  <acceptance_criteria>
    - Os quatro arquivos existem em `l2scanner/recursos/dashboard/vendor/`: o codigo, a folha de estilo, o texto da licenca e o README.
    - Existe em `tests/test_firewall_dashboard.py` um teste `test_o_sha256_do_README_bate_com_o_arquivo_EM_DISCO` que, para cada arquivo de codigo e de estilo do vendor, calcula o SHA-256 e afirma que essa string aparece no README. Ele e o que torna VEND-1 conferivel em vez de declaratorio.
    - Existe um teste que afirma que o README nomeia a versao `1.6.32` e a licenca `MIT` — e que a licenca registrada esta na lista permissiva do UI-SPEC (MIT, Apache-2.0 ou ISC).
    - Existe um teste que afirma que o arquivo de texto da licenca nao esta vazio e contem a palavra `Copyright`.
    - `git status --porcelain l2scanner/recursos/dashboard/vendor/` mostra os quatro arquivos como adicionados — eles sao versionados, nao ignorados.
  </acceptance_criteria>
  <done>A biblioteca esta na arvore com licenca ao lado, e o SHA-256 do README bate com o do arquivo em disco, provado por teste.</done>
</task>

<task type="auto">
  <name>Tarefa 2: VEND-2 — ler o fonte e registrar a revisao com contagem por primitiva</name>
  <files>l2scanner/recursos/dashboard/vendor/README.md</files>
  <read_first>
    - `01-UI-SPEC.md` secao `Registry Safety`, linha VEND-2 — a lista das primitivas a procurar e a regra: zero ocorrencias aprova; qualquer ocorrencia exige revisao humana explicita e registrada antes de seguir.
    - `01-RESEARCH.md:245-271` — a varredura ja executada sobre as tres candidatas, com a contagem por primitiva e o trecho exato do contexto da ocorrencia que reprovou a `dygraphs`.
    - `01-RESEARCH.md:1188-1214` — a refutacao medida do wheel-zoom, com a listagem dos eventos que uPlot de fato registra.
  </read_first>
  <action>
Rodar a varredura sobre os arquivos vendorizados EM DISCO, procurando cada uma das primitivas
que o VEND-2 nomeia: busca de recurso por rede na forma moderna e na forma antiga, envio de
sinal em segundo plano, avaliacao dinamica de codigo, construcao de funcao a partir de string,
importacao dinamica com URL externa, criacao de elemento de script, e conexao de soquete
bidirecional. Contar cada uma separadamente.

Registrar no `vendor/README.md` uma secao de revisao com a contagem POR PRIMITIVA e por arquivo.
Zero ocorrencias aprova; qualquer ocorrencia vira uma linha com `arquivo:linha` e um paragrafo de
revisao humana explicita, e nesse caso a tarefa PARA e pede decisao ao usuario antes de seguir —
o UI-SPEC diz que uma biblioteca de grafico nao precisa de nenhuma dessas primitivas.

Registrar na mesma secao a comparacao medida que sustentou a escolha: a candidata descartada por
duas ocorrencias de busca de recurso por rede aceitava uma URL como fonte de dados, o que e uma
funcionalidade e nao telemetria — e ainda assim o portao a reprova, porque o portao e sobre o
fonte e nao sobre a contencao. Escrever tambem que a CSP conteria o estrago mas nao substitui a
revisao, e que as tres camadas (a leitura do fonte, o teste que quebra e o cabecalho) existem
porque cada uma cobre o que as outras nao cobrem.

Registrar por fim, na secao de revisao, a refutacao do wheel-zoom com a medicao: a biblioteca
escolhida registra os eventos de clique, clique duplo, os quatro de mouse, redimensionamento e
rolagem, e NENHUM de roda; a documentacao oficial diz que nao ha arrasto de rolagem embutido e
que o zoom por roda se acrescenta por fora, pela API de plugins e hooks. Isso e o que justifica
as linhas de codigo nosso no plano 01-07 — e a suposicao do UI-SPEC que caiu precisa dizer que
caiu, no lugar onde quem for mexer no grafico vai ler.
  </action>
  <verify>
    <automated>python -m pytest tests/test_firewall_dashboard.py -q -k "revisao or vend2"</automated>
  </verify>
  <acceptance_criteria>
    - O `vendor/README.md` contem uma secao de revisao com uma linha por primitiva do VEND-2 e a contagem encontrada em cada arquivo de codigo vendorizado.
    - Existe em `tests/test_firewall_dashboard.py` um teste `test_o_README_registra_a_revisao_de_TODAS_as_primitivas` que afirma que a secao de revisao do README menciona cada item da lista de primitivas usada pelo proprio modulo de teste — se alguem acrescentar uma primitiva a lista e esquecer de revisar, o teste cai.
    - A secao de revisao registra a refutacao do wheel-zoom com a lista dos eventos efetivamente registrados pela biblioteca, e nao apenas a conclusao.
    - Se alguma primitiva tiver contagem diferente de zero, existe no README o paragrafo de revisao humana com `arquivo:linha` e a tarefa foi interrompida para decisao do usuario antes de seguir — registrar no SUMMARY qual dos dois caminhos ocorreu.
  </acceptance_criteria>
  <done>A leitura do fonte esta registrada com contagem por primitiva, e a suposicao do UI-SPEC sobre zoom por roda esta refutada no lugar onde ela sera lida.</done>
</task>

<task type="auto">
  <name>Tarefa 3: VEND-3 — o teste que quebra, com o controle negativo que prova que ele quebra</name>
  <files>tests/test_firewall_dashboard.py</files>
  <read_first>
    - `tests/test_firewall_escopo.py:60-131` — o molde do FIRE-01 inteiro: a banlist ampla e nomeada com a razao de ser ampla, o normalizador, a funcao pura de decisao com a docstring que diz por que ela e o alvo da mutacao, e a mensagem de falha que explica o porque e aponta o documento.
    - `tests/test_firewall_escopo.py:261-276` — `test_o_detector_acusa_uma_distribuicao_banida_injetada`: o CONTROLE NEGATIVO, com a docstring que explica que sem ele um bug no normalizador ou uma banlist vazia deixaria tudo verde para sempre.
    - `tests/test_mercado_firewall_de_fase.py:400-440` — `test_o_requirements_nao_ganhou_linha_nesta_fase` e o comentario de que a lista esta escrita a mao DE PROPOSITO, porque deriva-la do proprio arquivo faria o teste concordar com qualquer coisa que alguem acrescentasse.
    - `tests/test_mercado_firewall_de_fase.py:449-470` — a doutrina: ao lado da afirmacao, o controle negativo que prova que o guarda reprova quando o fato acontece.
    - `01-PATTERNS.md` secao `tests/test_firewall_dashboard.py (VEND-3)` — as quatro pecas do molde, com o aviso de que a quarta e a que nao pode faltar.
  </read_first>
  <behavior>
    - A varredura sobre os arquivos de codigo vendorizados atuais devolve conjunto vazio e o teste passa.
    - A funcao pura de decisao, alimentada com um trecho fabricado que contem uma primitiva de rede, devolve essa primitiva — o guarda acusa.
    - A funcao pura, alimentada com um trecho fabricado sem nenhuma primitiva, devolve conjunto vazio — o guarda nao acusa a esmo.
    - A varredura falha com uma mensagem que nomeia a primitiva, o arquivo e o documento onde a regra mora.
    - A varredura enumera pelo menos um arquivo; uma pasta vazia FALHA em vez de passar.
    - A lista de distribuicoes declaradas em `requirements.txt` e identica a lista escrita a mao no teste.
  </behavior>
  <action>
Escrever `tests/test_firewall_dashboard.py` nas quatro pecas do molde do FIRE-01.

Primeira peca: a lista de primitivas do VEND-2 como constante nomeada de modulo, com o
comentario dizendo por que ela e ampla — quem procurar uma forma de "so buscar uma coisinha" vai
encontrar uma delas, e listar hoje custa menos que descobrir a proxima em producao.

Segunda peca: a funcao pura `_primitivas_presentes(texto) -> set[str]`, o UNICO ponto de decisao
do modulo, com a docstring dizendo que manter a decisao numa funcao pura e o que permite provar
que o detector acusa sem baixar nada de verdade.

Terceira peca: a mensagem de falha com a anatomia da casa — o que foi encontrado, em qual
arquivo, por que isso importa aqui (o `.env` na mesma arvore, o navegador do usuario na mesma
maquina do jogo), e o apontamento para a secao `Registry Safety` do UI-SPEC.

Quarta peca, a que nao pode faltar: o CONTROLE NEGATIVO. Alimentar `_primitivas_presentes` com
um trecho fabricado no proprio teste contendo uma primitiva de rede e exigir que ela seja
acusada; e alimentar com um trecho inocente e exigir conjunto vazio. Escrever na docstring por
que: sem isso, uma varredura sobre uma pasta de vendor vazia fica verde para sempre, e o
firewall vira teatro de controle — que e o padrao de defeito que esta arvore ja nomeou oito
vezes.

Acrescentar o GUARDA DE ALCANCE, que e onde este teste especificamente pode se auto-anular: a
varredura tem de enumerar **apenas os arquivos de codigo e de estilo** do diretorio do vendor, e
NUNCA o README — porque o README, por exigencia do VEND-2, cita as primitivas pelo nome na nota
de revisao, e uma varredura ingenua sobre o diretorio inteiro acusaria o proprio documento que a
regra manda escrever. Escrever isso como comentario no ponto do filtro, e prender com um teste
que afirma que o conjunto de arquivos varridos contem o arquivo de codigo e NAO contem o README.

Acrescentar tambem o teste que afirma que a varredura enumerou pelo menos um arquivo — uma pasta
vazia tem de FALHAR, e nao passar por vacuidade.

Acrescentar, por fim, a classe `TestNenhumaDependenciaNova` com a lista de distribuicoes de
`requirements.txt` escrita A MAO, no molde de `test_mercado_firewall_de_fase.py:400-440`, e o
comentario de que deriva-la do proprio arquivo faria o teste concordar com qualquer coisa que
alguem acrescentasse. Reusar o extrator de nome de distribuicao ja escrito em
`tests/test_firewall_escopo.py` em vez de escrever um segundo.
  </action>
  <verify>
    <automated>python -m pytest tests/test_firewall_dashboard.py tests/test_firewall_escopo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_firewall_dashboard.py -q` termina em 0 com pelo menos 8 testes coletados.
    - Existe o controle negativo que alimenta `_primitivas_presentes` com um trecho fabricado contendo uma primitiva e afirma que ela e acusada, e um par que afirma conjunto vazio sobre um trecho inocente.
    - Existe um teste que afirma que a lista de arquivos varridos CONTEM o arquivo de codigo do vendor e NAO CONTEM `README.md` — o guarda de alcance que impede o teste de acusar o proprio documento de revisao.
    - Existe um teste que afirma que a varredura enumerou ao menos um arquivo, para que uma pasta vazia falhe em vez de passar por vacuidade.
    - Existe `TestNenhumaDependenciaNova` afirmando igualdade entre as distribuicoes declaradas em `requirements.txt` e uma lista escrita a mao no proprio teste.
    - Prova de que o guarda reprova de verdade: gravar um arquivo temporario de codigo com uma primitiva dentro do diretorio do vendor, rodar `python -m pytest tests/test_firewall_dashboard.py -q` e observar FALHA; apagar o arquivo e observar VERDE. Registrar as duas saidas no SUMMARY.
    - `python -m pytest tests/ -q` termina em 0 e `git diff --stat requirements.txt` sai vazio.
  </acceptance_criteria>
  <done>O firewall do vendor esta verde, quebra quando uma primitiva aparece, nao se auto-acusa pelo README, nao passa por vacuidade numa pasta vazia, e prende o `requirements.txt` contra linha nova.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `l2scanner/recursos/dashboard/vendor/uPlot.iife.min.js` — o codigo da biblioteca, versao 1.6.32
- `l2scanner/recursos/dashboard/vendor/uPlot.min.css` — a folha de estilo que ela exige
- `l2scanner/recursos/dashboard/vendor/uPlot.LICENSE` — o texto da licenca permissiva
- `l2scanner/recursos/dashboard/vendor/README.md` — VEND-1 (proveniencia com SHA-256 recalculado)
  e VEND-2 (nota de revisao com contagem por primitiva e a refutacao do zoom por roda)
- `tests/test_firewall_dashboard.py` — VEND-3, com `_primitivas_presentes`, o controle negativo,
  o guarda de alcance, o guarda contra vacuidade e `TestNenhumaDependenciaNova`

Nada de novo em `requirements.txt` — e agora ha teste prendendo isso.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terceiro -> arvore do repo | Primeiro artefato de terceiro vendorizado deste projeto; ele passa a ser versionado e a rodar no navegador do usuario |
| arquivo vendorizado -> navegador | Codigo minificado que ninguem vai reler a cada atualizacao; a defesa tem de ser estrutural, e nao uma leitura feita uma vez |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | biblioteca vendorizada executando no navegador | high | mitigate | Tres camadas: VEND-2 (leitura do fonte, registrada), VEND-3 (teste que quebra, com controle negativo) e VEND-4 (CSP com `connect-src 'self'` no cabecalho, ja no tracer) |
| T-01-14 | Tampering | troca silenciosa do arquivo vendorizado | high | mitigate | SHA-256 registrado no README e conferido por teste contra o arquivo em disco; uma substituicao muda o hash e derruba a suite |
| T-01-15 | Repudiation | de onde veio este arquivo, e quando | medium | mitigate | VEND-1 registra URL de origem, versao, data e tamanho, alem do hash |
| T-01-16 | Tampering | o proprio guarda ficando verde por vacuidade | high | mitigate | Guarda de alcance (o README nao e varrido) e guarda contra pasta vazia (a varredura tem de enumerar ao menos um arquivo), mais o controle negativo sobre a funcao pura |
| T-01-SC | Tampering | instalacoes de pacote | high | mitigate | Veredito de legitimidade **OK** para a biblioteca escolhida; nenhum pacote suposto ou suspeito, portanto nenhum checkpoint devido; `TestNenhumaDependenciaNova` prende o `requirements.txt` |
</threat_model>

<verification>
- `python -m pytest tests/test_firewall_dashboard.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- `git diff --stat requirements.txt` sai vazio.
- As quatro provas VEND-1..4 estao cobertas: 1 e 2 pelo `vendor/README.md` conferido por teste,
  3 por `tests/test_firewall_dashboard.py`, e 4 pelo teste de cabecalho do plano 01-05.
</verification>

<success_criteria>
- Os quatro arquivos do vendor estao versionados, com licenca ao lado.
- O SHA-256 do README bate com o do arquivo em disco, provado por teste e recalculado nesta
  arvore.
- A nota de revisao registra a contagem por primitiva e a refutacao do zoom por roda.
- O firewall quebra quando uma primitiva aparece e nao passa por vacuidade — as duas metades
  demonstradas e registradas no SUMMARY.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-04-SUMMARY.md` when done
</output>
