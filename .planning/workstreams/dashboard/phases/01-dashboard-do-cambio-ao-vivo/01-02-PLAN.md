---
phase: 01-dashboard-do-cambio-ao-vivo
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - l2scanner/dashboard_dados.py
  - tests/test_dashboard_leitura.py
  - tests/test_dashboard_dados.py
autonomous: true
requirements: [DASH-01, DASH-03, DASH-04, DASH-05]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rodar o dashboard por uma sessao inteira nao muda tamanho, conteudo nem mtime do .mercado/observacoes.csv — provado por impressao digital (tamanho, mtime_ns, sha256) antes e depois, e nao por intencao."
    - "Com o scanner apendando durante a leitura, a serie nao some e nenhuma linha parcial aparece: a leitura degrada para a ultima linha completa e o payload carrega a nota de linha ignorada."
    - "Uma linha ruim no meio do arquivo cai sozinha, com o numero dela nomeado no log, e as demais linhas carregam normalmente."
    - "Um ponto do grafico e UM INSTANTE DE LEITURA (primeira_vez), literalmente: o menor e a mediana daquele instante, com a Evidencia do instante dentro do proprio ponto."
    - "Onde a mediana nao alcanca o piso de 5 ofertas distintas, o ponto carrega a frase de piso vinda do Python e valor nulo — nunca um numero inventado. Esse e o caso NORMAL, e nao um erro."
    - "A agregacao de zoom largo usa median_low sobre Fraction e indice de balde inteiro; o valor de qualquer balde e um valor que existiu na tela."
    - "Os baldes de um dia comecam a meia-noite local, e nao no instante da primeira observacao."
    - "O payload declara qual dos cinco estados esta valendo, e a precedencia e fechada: erro de contrato vence arquivo ausente, que vence sem leitura da Adena, que vence abaixo do piso, que vence serie presente."
    - "Todo numero do payload viaja com n e recencia ao lado, e o que e derivado diz que e derivado."
    - "A serie no payload e um ELEMENTO de uma lista com contrato fixo: instanciar uma segunda serie e acrescentar um elemento, sem chave nova e sem caso especial da Adena."
    - "populated: dois numeros lado a lado, cada um com n e recencia; duas linhas no grafico; rodape com fonte, n, recencia e o carimbo do cambio."
  artifacts:
    - l2scanner/dashboard_dados.py
    - tests/test_dashboard_leitura.py
    - tests/test_dashboard_dados.py
  key_links:
    - "observacoes_ao_vivo -> mercado_registro.observacoes_do_arquivo pelo ArquivoRecortado: um parser so, os dois portoes chamados, a mensagem nomeando o arquivo real."
    - "pontos_por_instante -> mercado_analise.menor_pedido_visivel/mediana_dos_unitarios: as contas do console, aplicadas ao subconjunto de UM instante (CTX-1)."
    - "payload -> mercado_console.formatador_do_unitario: o unico ponto de decisao entre taxa e unitario comum, chamado e nunca replicado."
    - "payload(cambio=...) : o cambio entra por parametro, e nao por import — dashboard_dados fica puro e testavel sem disco."
---

<objective>
Fechar a camada de dado do dashboard: a leitura ao vivo com as provas medidas que o DASH-01
exige, a agregacao que respeita CTX-1 e o D-02 do projeto, e a montagem do JSON com a
precedencia de estados fechada do UI-SPEC.

Purpose: e aqui que mora a promessa central da fase — que os numeros do dashboard sao os mesmos
do console, vindos da mesma fonte pela mesma conta. Toda esta camada e pura ou somente-leitura,
e portanto inteiramente provavel sem navegador.

Output: `l2scanner/dashboard_dados.py` completo, `tests/test_dashboard_leitura.py` e
`tests/test_dashboard_dados.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/REQUIREMENTS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-CONTEXT.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-RESEARCH.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-PATTERNS.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: A leitura ao vivo, endurecida — e as tres provas que o DASH-01 cobra</name>
  <files>l2scanner/dashboard_dados.py, tests/test_dashboard_leitura.py</files>
  <read_first>
    - `l2scanner/dashboard_dados.py` — o `ArquivoRecortado` e o `observacoes_ao_vivo` que o tracer (01-01) deixou; esta tarefa os endurece, nao os reescreve.
    - `l2scanner/mercado_registro.py:516-605` — o corpo de `observacoes_do_arquivo`, incluindo `:577-582`, o `log.warning` que NOMEIA o numero da linha descartada e diz que uma linha ruim nunca condena o arquivo inteiro. O dashboard reusa esse aviso por heranca de chamada; nao inventa outro nivel de log.
    - `l2scanner/mercado_registro.py:428-471` — a docstring de `conferir_o_terminador`: o molde de "as saidas recusadas, cada uma com o motivo, e o custo aceito dito em voz alta".
    - `tests/test_mercado_firewall_de_fase.py:481-495` — `_impressao_do_arquivo(caminho) -> (st_size, st_mtime_ns, sha256)` e a docstring que explica por que os TRES juntos, e nao so o sha256.
    - `tests/test_mercado_analise.py:261-268` — o tripwire de arquitetura por leitura de fonte (`inspect.getsource` + ausencia dos modos de escrita), que e o molde exato para provar "o dashboard nunca abre o CSV para escrita".
    - `tests/test_mercado_registro.py:1-10` — "NADA AQUI TOCA A `.mercado/` REAL": `tmp_path` sempre.
    - `01-RESEARCH.md:428-480` (Padrao 2) e `:757-796` (as medicoes da leitura concorrente, incluindo o controle positivo).
  </read_first>
  <behavior>
    - Arquivo ausente: devolve leitura vazia, nao levanta, e NAO cria o arquivo.
    - Arquivo de zero bytes: devolve leitura vazia sem levantar.
    - Arquivo sem nenhuma quebra de linha: devolve leitura vazia com `cauda_incompleta` verdadeiro — nao ha nem cabecalho a afirmar.
    - Arquivo cortado no meio da ultima linha: devolve as observacoes das linhas completas e `cauda_incompleta` verdadeiro.
    - Arquivo cortado exatamente no fim de uma linha: devolve todas as observacoes e `cauda_incompleta` falso.
    - Cabecalho trocado: levanta `ContratoDoArquivoQuebrado` com o caminho real na mensagem — o portao do cabecalho continua desligando alto.
    - Uma linha com quantidade nao-numerica no meio do arquivo: as demais carregam, e o aviso emitido nomeia o numero da linha.
    - 300 leituras seguidas deixam `(st_size, st_mtime_ns, sha256)` inalterados.
  </behavior>
  <action>
Completar `observacoes_ao_vivo` para devolver `LeituraAoVivo` com os campos `observacoes`,
`cauda_incompleta`, `arquivo_ausente` e `linhas_completas` (a contagem de linhas do trecho
cortado, sem o cabecalho — e ela que alimenta a frase de prova do estado vazio,
"o arquivo foi lido: N linhas, 0 da serie Adena").

Garantir que o unico modo de abertura no modulo inteiro seja o de leitura, e que nao exista
`mkdir` em lugar nenhum: a leitura nunca cria a pasta nem o arquivo. Escrever isso como
invariante na docstring do modulo, com a frase de que a criacao seria o programa escrevendo num
caminho de LEITURA — o mesmo argumento que `conferir_o_terminador` usa para recusar truncar a
cauda no disco.

Acrescentar ao modulo a constante nomeada `NOTA_DE_LINHA_PARCIAL` com a frase exata do
`## Copywriting Contract` do UI-SPEC para a linha ignorada, e faze-la viajar no payload quando
`cauda_incompleta` for verdadeiro. A frase e do Python e chega pronta ao JS; escrever ao lado
que reacentua-la ou reescreve-la no navegador seria o segundo formatador que o DASH-03 recusa.

Escrever `tests/test_dashboard_leitura.py` no idioma da casa: classes `Test...` por invariante,
sem heranca, nomes em portugues com o fato em CAIXA ALTA, `tmp_path` sempre, e um construtor de
CSV de fixture a mao. As classes: `TestOPortaoDeContratoEOMESMO` (cabecalho trocado levanta e
nomeia o arquivo real; arquivo ausente devolve vazio sem levantar; a leitura NAO cria o arquivo
ausente), `TestACaudaIncompletaCaiSOZINHA` (os quatro casos de corte do bloco `<behavior>`),
`TestUmaLinhaRuimCaiSOZINHA` (o aviso nomeia o numero da linha, via `caplog`), e
`TestALeituraNaoEscreveNADA` (a impressao digital em 300 leituras, mais o tripwire por
`inspect.getsource` sobre `observacoes_ao_vivo` e sobre `ArquivoRecortado.open`).

Escrever tambem, dentro do teste da cauda, um CONTROLE NEGATIVO: alimentar `observacoes_ao_vivo`
com um texto que TERMINA em quebra de linha e afirmar que `cauda_incompleta` e falso. Sem ele,
um bug que retornasse `True` sempre deixaria os quatro casos de corte verdes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_leitura.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_leitura.py -q` termina em 0 com pelo menos 10 testes coletados.
    - O teste de impressao digital compara as TRES componentes `(st_size, st_mtime_ns, sha256)` antes e depois de 300 leituras e falha se qualquer uma divergir.
    - O tripwire por `inspect.getsource` cobre `observacoes_ao_vivo` E `ArquivoRecortado.open`, e afirma ausencia dos modos de escrita e de `mkdir` no fonte das duas.
    - Existe um teste que corta o CSV de fixture no meio da ultima linha e um CONTROLE NEGATIVO que alimenta um texto terminado em quebra de linha e exige `cauda_incompleta` falso — os dois no mesmo arquivo.
    - `python -m pytest tests/test_dashboard_leitura.py -q -k "NOMEIA"` passa: o aviso da linha descartada carrega o numero da linha, capturado por `caplog`.
    - `grep -c "NOTA_DE_LINHA_PARCIAL" l2scanner/dashboard_dados.py` retorna ao menos 2 — a constante e definida e usada, e nao apenas declarada.
  </acceptance_criteria>
  <done>A leitura ao vivo tem as tres provas do DASH-01 presas em teste: somente leitura por impressao digital, degradacao para a ultima linha completa com controle negativo, e uma linha ruim caindo sozinha com o numero nomeado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A agregacao — um ponto e um instante (CTX-1), e o balde nunca inventa valor</name>
  <files>l2scanner/dashboard_dados.py, tests/test_dashboard_dados.py</files>
  <read_first>
    - `l2scanner/mercado_analise.py:341-373` — `mediana_dos_unitarios`, o corpo que estabelece as tres regras herdadas: piso, `Fraction`, `median_low`.
    - `l2scanner/mercado_analise.py:226-254` — `Evidencia`, com `suficiente` e `faltam`, e a regra de que ela viaja DENTRO do resultado e nunca ao lado.
    - `l2scanner/mercado_analise.py:152/162/170` — `N_MINIMO_PARA_MENOR = 1`, `N_MINIMO_PARA_MEDIANA = 5`, `N_MINIMO_PARA_TENDENCIA = 8`, cada um com o comentario "ESCOLHA, NAO MEDICAO": o molde de como qualquer constante nova desta fase se escreve.
    - `l2scanner/mercado_analise.py:178-207` — `unitario(total_em_centesimos, quantidade) -> Fraction`, o unico comparavel valido.
    - `l2scanner/mercado_analise.py:411-420` — a docstring que diz por extenso "nao existe serie temporal de preco neste CSV; o `n` conta anuncios, nao instantes". Ela e a razao de CTX-1 doer, e a razao de a refutacao precisar ficar escrita.
    - `01-RESEARCH.md:645-664` (Pitfall 3) — as tres medicoes: `median` devolvendo `13/42` sobre uma lista que nao o contem, `total_seconds()` devolvendo `69713.696` contra `69713` inteiro, e o balde de um dia comecando as 15:00 quando a ancora e arbitraria.
    - `01-RESEARCH.md:828-872` — o bucketizador sem float e a fronteira do `float` medida (pior erro 1,9e-11).
    - `01-RESEARCH.md:905-941` — a `Open Question 1` inteira, incluindo a recomendacao (b) que o usuario RECUSOU.
  </read_first>
  <behavior>
    - `pontos_por_instante` sobre observacoes de 3 instantes distintos devolve 3 pontos, ordenados por instante.
    - Um instante com 2 ofertas distintas produz ponto com menor preenchido (piso 1) e mediana nula com a frase de piso — e nao com um numero.
    - Um instante com 5 ofertas distintas produz ponto com mediana preenchida, e o valor esta na lista de unitarios daquele instante.
    - `baldes` com largura de uma hora agrupa por indice inteiro; o valor de cada balde e `median_low` dos valores dos instantes dele e esta entre eles.
    - `baldes` com largura de um dia e ancora de meia-noite local coloca duas observacoes do mesmo dia civil no mesmo balde, mesmo que a primeira observacao tenha ocorrido as 15:00.
    - Nenhuma funcao desta camada devolve `float`; a conversao para `float` acontece so na fronteira do JSON e e declarada.
  </behavior>
  <action>
Definir `PontoDaSerie` como dataclass congelado com `instante`, `menor` (`Fraction | None`),
`menor_evidencia`, `tipica` (`Fraction | None`), `tipica_evidencia` e `n`. `Evidencia` viaja
DENTRO do ponto, no molde de `mercado_analise:226-254` — nunca num campo paralelo.

Definir `pontos_por_instante(observacoes)`: agrupar por `primeira_vez` e, para cada grupo,
chamar `mercado_analise.menor_pedido_visivel` e `mercado_analise.mediana_dos_unitarios` sobre o
SUBCONJUNTO daquele instante. Devolver ordenado por instante.

Na docstring de `pontos_por_instante`, escrever a decisao e a refutacao inteiras: que a
pesquisa mediu 92 observacoes com apenas 13 valores distintos de `primeira_vez` e a maior serie
com 15 observacoes em 2 instantes; que com `N_MINIMO_PARA_MEDIANA` valendo 5 isso deixa a linha
da mediana AUSENTE na maior parte do grafico; que a pesquisa por isso RECOMENDOU o acumulado
ate o instante (a alternativa que bate literalmente com o console e sempre tem mediana); e que o
usuario, confrontado com essa medicao em 2026-09-01, MANTEVE a leitura literal (CTX-1). Fechar
dizendo que a ausencia da mediana e portanto o caso NORMAL desta tela e nao um defeito, e que
onde ela falta o que aparece e a frase de piso do Python.

Definir `LARGURAS_DE_BALDE` como constante nomeada com a razao escrita ao lado, no molde de
`N_MINIMO_PARA_MEDIANA`: tres larguras (cinco minutos, uma hora, um dia) porque sao as tres
resolucoes que a pergunta do usuario tem — "as horas do dia" e "dias atras" — mais uma
intermediaria; e que a escolha e ESCOLHA e nao MEDICAO.

Definir `ancora_da_meia_noite(instante)` devolvendo a meia-noite local do dia do instante, com
a medicao escrita ao lado: com ancora arbitraria de 15:00, os baldes de um dia comecam as 15:00,
e "dias atras" deixa de querer dizer o que um humano acha que quer dizer.

Definir `baldes(pontos, largura, ancora)`: indice do balde por divisao inteira da diferenca de
`datetime` pela largura — nunca por `total_seconds()`, e escrever ao lado que ele devolveria
`float`, medido em `69713.696` contra `69713` inteiro, e que `float` e como um centavo aparece
do nada. O valor de cada balde e `statistics.median_low` dos valores dos instantes dele, com a
refutacao ao lado: a mediana comum inventa valor, medida devolvendo `13/42` sobre uma lista de
quatro `Fraction` que nao o contem, o que viola o D-02 do projeto; `median_low` devolve um
ELEMENTO da lista, e cada elemento ja e um unitario que esteve na tela, entao a composicao
preserva a disciplina.

Escrever os testes de agregacao em `tests/test_dashboard_dados.py`, com construtor de
`ObservacaoLida` a mao no molde de `tests/test_mercado_analise.py:283-300`. Incluir o CONTROLE
que prova que a agregacao nao inventa: para cada balde produzido, afirmar que o valor esta
CONTIDO na lista dos valores dos instantes daquele balde. Esse `assert in` e a forma executavel
do D-02, e sem ele trocar a mediana pela media passaria despercebido.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_dados.py -q -k "agrega or balde or instante or ponto"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_dados.py -q` termina em 0.
    - Existe um teste que, para cada balde produzido, afirma que o valor do balde esta CONTIDO na lista dos valores dos instantes dele — a forma executavel do D-02.
    - Existe um teste com duas observacoes do mesmo dia civil, uma antes e outra depois das 15:00, afirmando que caem no MESMO balde de um dia.
    - Existe um teste que afirma que o tipo do valor de todo ponto e de todo balde e `Fraction`, e nunca `float`.
    - Existe um teste com um instante de 2 ofertas distintas afirmando que `tipica` e nulo E que a evidencia daquele ponto reporta `faltam` igual a 3.
    - `grep -c "13/42" l2scanner/dashboard_dados.py` retorna ao menos 1 e `grep -c "69713" l2scanner/dashboard_dados.py` retorna ao menos 1 — as duas medicoes que justificam as duas escolhas estao no fonte, com o numero.
    - `grep -c -E "CTX-1|manteve a leitura literal" l2scanner/dashboard_dados.py` retorna ao menos 1 — a decisao do usuario contra a recomendacao da pesquisa esta registrada no fonte.
  </acceptance_criteria>
  <done>Um ponto e um instante, a mediana falta onde tem de faltar e diz por que, o balde nunca inventa valor, e as tres medicoes que sustentam essas escolhas estao escritas ao lado do codigo que as executa.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: O payload — a precedencia fechada dos estados e a serie como elemento de lista</name>
  <files>l2scanner/dashboard_dados.py, tests/test_dashboard_dados.py</files>
  <read_first>
    - `l2scanner/mercado_console.py:522-560` `_linha_do_menor`, `:561-598` `_linha_da_mediana` e `:588-593` a frase de piso literal — o endpoint devolve AS MESMAS strings, montadas pelas mesmas funcoes.
    - `l2scanner/mercado_console.py:305-322` `formatador_do_unitario` — UM ponto de decisao, e so um; a docstring explica o que quatro `if` espalhados fariam.
    - `l2scanner/mercado_console.py:219-229` `formatar_centesimos`, `:265` `UNIDADE_DA_TAXA`, `:268-304` `formatar_taxa_derivada`, `:500-521` `_recencia_em_duas_formas`.
    - `l2scanner/mercado_analise.py:375-400` `recencia_do_preco` e a distincao, escrita la, entre a recencia do PRECO e o `ultima_vez` do catalogo.
    - `01-UI-SPEC.md` secao `Estados da tela e sua precedencia` (a ordem fechada de cinco, mais as tres ortogonais) e secao `Copywriting Contract` (as frases exatas, incluindo as proibidas).
    - `01-UI-SPEC.md` secao `O componente de serie e generico (DASH-05)` — o contrato de props que o payload tem de alimentar.
    - `01-RESEARCH.md:857-872` — a fronteira do `float`, com o pior erro medido (1,9e-11) e a regra que sai dela.
  </read_first>
  <behavior>
    - Cabecalho quebrado: `payload` devolve estado de erro de contrato, sem destaque e sem serie, com a frase do UI-SPEC; nenhuma coluna e adivinhada.
    - Arquivo ausente: estado de arquivo ausente, com a frase que diz o que fazer.
    - Arquivo presente com zero linhas da serie da Adena: estado sem leitura, com a contagem real de linhas do arquivo na frase de prova.
    - Serie com 2 ofertas distintas: estado abaixo do piso — o menor aparece, a mediana aparece como frase de piso.
    - Serie com 6 ofertas distintas: estado de serie presente, com os dois numeros do destaque.
    - Sem cambio informado: o sub-objeto de R$ do destaque e nulo e o payload carrega a frase de R$ indisponivel; nenhum valor padrao e chutado.
    - Com cambio informado: o R$ aparece, marcado como derivado e com o carimbo de quando foi informado, mais o aviso de que o cambio de hoje foi aplicado a toda a serie.
    - Recencia acima do limiar de frescor: o payload marca o dado como velho e carrega a frase que recusa afirmar "agora"; o numero continua presente.
    - A chave de series do payload e uma LISTA; com uma segunda serie nas observacoes, ela tem dois elementos com o mesmo contrato e nenhuma chave nova.
  </behavior>
  <action>
Definir `ESTADOS` como tupla nomeada de cinco valores na ordem de precedencia do UI-SPEC, com o
comentario dizendo que a ordem e FECHADA e que o primeiro que casar manda no destaque e no
grafico — e que varios podem ser verdade ao mesmo tempo, que e exatamente por que a ordem
precisa estar escrita num lugar so.

Definir `LIMIAR_DE_FRESCOR` como constante nomeada, no molde "ESCOLHA, NAO MEDICAO", com a razao
escrita: acima dele a tela para de afirmar "agora" sobre o numero, porque a frase proibida do
UI-SPEC e exatamente "agora" colado num numero cuja recencia passou do limiar.

Definir `serie_para_o_grafico(chave, observacoes, agora)` devolvendo o dicionario de UMA serie
com o contrato de props do UI-SPEC: `chave`, `titulo` (de `ModeloDeMercado.nome_exibido_de`),
`unidade`, `rotulo_principal`, `rotulo_tipico`, `pontos` e `baldes`. Cada ponto carrega
`instante` em ISO, `menor_texto` e `tipica_texto` (as strings prontas do Python, ou a frase de
piso), `menor_pixel` e `tipica_pixel` (os `float` para o canvas, ou nulo), e `n`. Escrever ali,
no ponto exato da conversao, a declaracao da fronteira do `float`: que JSON nao tem `Fraction`,
que o `float` no payload e o PIXEL e a string e a VERDADE, que o texto do tooltip e da legenda
vem da string e nunca de uma reformatacao no navegador, e que o pior erro medido dessa conversao
foi de 1,9e-11 centesimos por milhao.

Definir `payload(pasta_do_mercado, agora, cambio=None)`: chamar `observacoes_ao_vivo`, capturar
`ContratoDoArquivoQuebrado` para produzir o estado de erro de contrato com a mensagem da
excecao, montar `ModeloDeMercado`, decidir o estado pela ordem fechada, e montar as chaves
`estado`, `gerado_em`, `fonte`, `avisos`, `destaque` e `series`. `series` e sempre uma LISTA,
mesmo com um elemento so — escrever ao lado que e isso que faz "instanciar uma segunda serie
nao exige codigo de grafico novo" (DASH-05) ser estrutural e nao uma intencao: uma serie nova e
um elemento a mais, e nao uma chave nova nem um `if` da Adena.

O sub-objeto de R$ do destaque so existe quando `cambio` nao for nulo. Quando ele existe, marcar
`derivado` verdadeiro e carregar o carimbo de quando o cambio foi informado, mais o aviso de que
o cambio de hoje foi aplicado a toda a serie. Quando nao existe, o payload carrega a frase de R$
indisponivel do UI-SPEC. Escrever ao lado que um cambio chutado vira decisao de dinheiro real
errada, e por isso a ausencia se escreve com palavra e nunca com zero.

Todas as frases da tela vem de constantes nomeadas neste modulo, copiadas do
`## Copywriting Contract` do UI-SPEC, e nao de literais espalhados pelo corpo das funcoes.
Escrever um teste que afirma que as frases proibidas do UI-SPEC nao aparecem em nenhum valor de
string do payload em nenhum dos cinco estados.

Escrever os testes de payload em `tests/test_dashboard_dados.py`, uma classe por estado, mais
`TestASerieEUmElementoDeLista` (com duas series nas observacoes, afirmando dois elementos com as
MESMAS chaves) e `TestNenhumValorPadraoEChutado` (sem cambio, nenhuma chave de R$ com numero).
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_dados.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_dados.py -q` termina em 0 com pelo menos 15 testes coletados.
    - Existe um teste por estado dos cinco, cada um afirmando o valor exato de `payload["estado"]`.
    - Existe um teste de PRECEDENCIA que monta um caso em que dois estados sao verdade ao mesmo tempo (arquivo com cabecalho quebrado E sem serie da Adena) e afirma que vence o erro de contrato.
    - Existe um teste que monta duas series nas observacoes e afirma `len(payload["series"]) == 2` E que o conjunto de chaves dos dois elementos e identico — a prova estrutural do DASH-05 no lado do dado.
    - Existe um teste que percorre recursivamente todos os valores de string do payload nos cinco estados e afirma que nenhuma das frases proibidas do UI-SPEC aparece.
    - Existe um teste que, sem cambio, afirma que o sub-objeto de R$ do destaque e nulo E que a string de R$ indisponivel esta nos avisos.
    - Existe um teste com recencia acima do limiar afirmando que o payload marca o dado como velho E que o numero continua presente — o que sai e a afirmacao de "agora", nao o valor.
    - `grep -c "1,9e-11\|1.9e-11" l2scanner/dashboard_dados.py` retorna ao menos 1 — a fronteira do `float` esta declarada com o erro medido, no ponto da conversao.
  </acceptance_criteria>
  <done>O payload declara o estado pela ordem fechada, todo numero viaja com `n` e recencia, o R$ some quando nao ha cambio informado, e a serie e um elemento de lista com contrato fixo.</done>
</task>

</tasks>

## Artifacts this phase produces

Deste plano:

- `l2scanner/dashboard_dados.py` — completa com `LeituraAoVivo.linhas_completas`,
  `NOTA_DE_LINHA_PARCIAL`, `PontoDaSerie`, `pontos_por_instante`, `LARGURAS_DE_BALDE`,
  `ancora_da_meia_noite`, `baldes`, `ESTADOS`, `LIMIAR_DE_FRESCOR`, `serie_para_o_grafico`,
  `payload`, e as constantes de frase copiadas do `## Copywriting Contract`
- `tests/test_dashboard_leitura.py` — as tres provas do DASH-01
- `tests/test_dashboard_dados.py` — agregacao, precedencia de estados, DASH-05 no lado do dado

Nada de novo em `requirements.txt`. Nenhum arquivo do workstream `mercado` e tocado por este
plano.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco -> processo do dashboard | O CSV e conteudo do usuario: pode chegar truncado, com cabecalho trocado, ou com uma linha editada a mao no Sheets |
| dado lido -> numero na tela | Um numero errado aqui vira decisao de compra errada com dinheiro real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `.mercado/observacoes.csv` | high | mitigate | Modo de abertura de leitura e nada mais; tripwire por `inspect.getsource` sobre `observacoes_ao_vivo` e `ArquivoRecortado.open`; impressao digital em 300 leituras |
| T-01-05 | Spoofing | cabecalho do CSV trocado | medium | mitigate | `conferir_o_cabecalho` continua sendo chamado e desligando alto; o payload vira estado de erro de contrato e nao adivinha coluna |
| T-01-07 | Repudiation | linha descartada em silencio | medium | mitigate | O `log.warning` herdado de `observacoes_do_arquivo` NOMEIA o numero da linha; teste com `caplog` prende isso |
| T-01-08 | Tampering | numero exibido que nunca existiu | high | mitigate | `median_low` sobre `Fraction`, indice de balde inteiro, e o teste que afirma que o valor de cada balde esta CONTIDO na lista dos valores dele |
| T-01-09 | Information Disclosure | mensagem de contrato quebrado exposta ao navegador | low | accept | A mensagem nomeia apenas `.mercado/observacoes.csv` e as colunas esperadas; nao ha segredo nela, e sem ela o usuario nao sabe o que consertar |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_leitura.py tests/test_dashboard_dados.py -q` termina em 0.
- `python -m pytest tests/ -q` termina em 0.
- `git diff --stat requirements.txt` sai vazio.
- `git status --porcelain .mercado/` sai vazio depois de rodar a suite — nenhum teste tocou a pasta real.
</verification>

<success_criteria>
- As tres provas do DASH-01 estao presas em teste, cada uma com o seu controle.
- Um ponto e um instante, e a ausencia da mediana e o caso normal, dito no fonte e no payload.
- Nenhum balde inventa valor, provado por `assert in`.
- A precedencia dos cinco estados e testada uma a uma, mais um caso de empate.
- A serie e um elemento de lista, e duas series produzem dois elementos com o mesmo contrato.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-02-SUMMARY.md` when done
</output>
