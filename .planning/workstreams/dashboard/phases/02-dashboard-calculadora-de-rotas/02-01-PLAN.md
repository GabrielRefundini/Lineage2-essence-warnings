---
phase: 02-dashboard-calculadora-de-rotas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/config.py
  - config.toml
  - l2scanner/dashboard_rotas.py
  - l2scanner/dashboard_dados.py
  - l2scanner/dashboard.py
  - l2scanner/recursos/dashboard/index.html
  - l2scanner/recursos/dashboard/dashboard.css
  - l2scanner/recursos/dashboard/dashboard.js
  - tests/test_dashboard_itens_config.py
  - tests/test_dashboard_rotas_tracer.py
autonomous: true
requirements: [CALC-01, CALC-02]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Com um bloco [[dashboard.item]] valido no config.toml e a serie do item presente no CSV, um GET /dados devolve, dentro de calculadora.itens, as duas rotas com seus textos formatados e a vencedora marcada — e a comparacao que elegeu a vencedora aconteceu em Fraction, nunca em float."
    - "Um bloco [[dashboard.item]] torto DERRUBA O ARRANQUE do dashboard nomeando o item e o campo, e a porta nunca chega a ser aberta — precedente ReceitaInvalida (D-06 do 02-CONTEXT)."
    - "Sem nenhum bloco configurado, a quarta regiao aparece com a frase do Python dizendo o que escrever no config.toml — ela nunca fica um retangulo mudo."
    - "Sem taxa da Adena lida, a quarta regiao diz que nao da para converter, e a frase deriva do MESMO objeto MenorPedidoVisivel que o destaque ja usa — uma autoridade so sobre a taxa."
    - "Um item configurado que nunca apareceu no CSV aparece na tela dizendo isso, com o nome que o usuario escreveu (D-07 do 02-CONTEXT)."
    - "A quarta regiao e um .painel e por isso os dois estados de falha fechada da Fase 1 a retiram da tela sem uma regra nova de CSS."
    - "Nenhuma frase da lista avisos do payload mudou de posicao: o acoplamento por posicao que o 01-07 prendeu com quatro testes continua verde."
  artifacts:
    - l2scanner/dashboard_rotas.py
    - l2scanner/config.py
    - config.toml
    - l2scanner/recursos/dashboard/index.html
    - tests/test_dashboard_itens_config.py
    - tests/test_dashboard_rotas_tracer.py
  key_links:
    - "config.ler_itens_de_rota -> dashboard.main: a leitura acontece ANTES do bind da porta, e ItemDeRotaInvalido devolve codigo de saida nao-zero."
    - "dashboard_dados.payload -> dashboard_rotas.vereditos_das_rotas: a taxa entregue e a unitario do MESMO menor_pedido_visivel que o destaque exibe, nunca uma segunda leitura."
    - "dashboard_rotas -> mercado_analise.resolver_o_nome: o casamento do item com a serie e o EXATO que ja existe, com ambiguidade QUEBRANDO e listando candidatas."
    - "dashboard_rotas -> mercado_console.formatar_unitario_derivado: o texto das duas rotas sai do formatador que ja existe — nenhum segundo formatador nasce nesta fase."
    - "index.html #molde-da-rota -> dashboard.js: as linhas nascem de um <template> clonado, com textContent; nenhuma copia de interface nasce no JS."
---

<objective>
Provar a arquitetura inteira desta fase numa fatia fina e de producao: de um bloco
`[[dashboard.item]]` no `config.toml` ate um veredito desenhado na quarta regiao da pagina,
atravessando TODAS as camadas que a fase vai usar — leitor TOML com recusa que derruba o
arranque, calculo puro em `Fraction`, bloco novo no payload, servidor lendo a configuracao no
arranque, e a quarta regiao desenhada a partir de um `<template>` da marcacao.

Purpose: esta fase e composicao, nao construcao. Se a arquitetura estiver errada — se o
casamento do nome nao servir, se o payload nao couber, se a quarta regiao brigar com a
precedencia de estados da Fase 1 — e melhor descobrir depois de UM commit do que depois de
quatro.

Output: `l2scanner/dashboard_rotas.py` (novo), o leitor `ler_itens_de_rota` em `config.py`, o
bloco `calculadora` no payload, a quarta regiao no `index.html`/`dashboard.css`/`dashboard.js`,
e dois arquivos de teste.

**O que esta fatia NAO faz, e e por isso que ela e fina:** ela nao aplica piso de evidencia ao
veredito (CALC-04, plano 02), nao calcula a diferenca em R$ (CALC-03, plano 02), nao mostra a
mediana como contexto, e nao guarda contra o unitario que arredonda para zero. Cada uma dessas
e uma linha a mais no MESMO caminho, e nenhuma delas e uma camada nova.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/ROADMAP.md
@.planning/workstreams/dashboard/REQUIREMENTS.md
@.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-CONTEXT.md
@.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md
@.claude/CLAUDE.md
</context>

## Fatos MEDIDOS nesta arvore em 2026-09-03 — nao re-derive nenhum

Cada numero abaixo foi medido antes deste plano existir. Eles estao aqui para que ninguem gaste
uma volta redescobrindo-os. **Nenhum deles e autoridade: cada um vira asserção de teste nas
tarefas abaixo, e se algum estiver errado o teste e quem diz.**

| Fato | Valor medido | Como |
|---|---|---|
| Series no `.mercado/observacoes.csv` real | duas: `adena#` (n=50) e `common-fafurion-doll#` (n=8) | leitura das seis colunas que `mercado_registro` grava |
| Menor unitario da Adena | `Fraction(9, 10000)` centesimos de XM por adena — 45,00 XM por 5 milhoes | `min` sobre `Fraction(total, quantidade)`, que e o corpo de `mercado_analise.unitario` |
| Menor unitario do `common-fafurion-doll#` | `5900` centesimos de XM por unidade — 59,00 por unidade | idem |
| `[[dashboard.item]]` no `tomllib` | `{"dashboard": {"item": [ {...}, {...} ]}}` — o acesso e ANINHADO | `tomllib.loads` |
| `[dashboard]` com `item = "solto"` | devolve a string `'solto'`, e nao uma lista | `tomllib.loads` |
| `import l2scanner.dashboard` | 341 modulos, com `cv2` e `numpy` JA presentes | subprocesso limpo |
| `import l2scanner.dashboard` + `l2scanner.config` | 366 modulos — **+25, e nenhum deles e `cv2` ou `numpy`** | subprocesso limpo |
| Preco de NPC abaixo do qual o unitario arredonda para zero centesimos, na taxa de hoje | ~555,6 adena por unidade | `round(Fraction(preco) * Fraction(9,10000))` |

**A ultima linha e uma armadilha real e ela NAO e desta fatia.** Ela e do plano 02-02, e esta
escrita aqui para que ninguem a "conserte" por conta propria com um formatador novo — o
formatador novo e que seria o defeito.

## Artifacts this phase produces (plano 02-01)

**Modulo novo:** `l2scanner/dashboard_rotas.py`

| Simbolo | Forma |
|---|---|
| `MARGEM_DE_EMPATE_PERCENTUAL` | `Fraction` — a faixa de empate, ESCOLHA declarada |
| `VENCEDORA_NPC`, `VENCEDORA_MERCADO` | `str` — os dois nomes de vencedora |
| `ROTA_DECIDIDA`, `ROTA_EMPATADA`, `ROTA_NUNCA_VISTA`, `ROTA_NOME_AMBIGUO` | `str` — os estados de UMA linha |
| `ESTADOS_DA_ROTA` | tupla fechada, na ordem de precedencia |
| `CustoDaRota` | dataclass frozen: `unitario`, `total_do_pacote`, `quantidade` |
| `VereditoDeRota` | dataclass frozen: `estado`, `item`, `chave`, `nome_exibido`, `npc`, `mercado`, `vencedora`, `diferenca_por_unidade`, `diferenca_percentual`, `evidencia`, `recencia`, `candidatas` |
| `custo_da_rota_do_npc(item, taxa)` | `CustoDaRota` |
| `custo_da_rota_do_mercado(menor)` | `CustoDaRota` |
| `veredito_de_uma_rota(item, modelo, taxa, agora)` | `VereditoDeRota` |
| `vereditos_das_rotas(itens, modelo, taxa, agora)` | `tuple[VereditoDeRota, ...]` |

**Em `l2scanner/config.py`:** `SECAO_DO_DASHBOARD`, `CHAVE_DOS_ITENS`, `TETO_DO_PRECO_DO_NPC`,
`TETO_DA_QUANTIDADE_DO_PACOTE`, `_EXEMPLO_DO_ITEM`, `ItemDeRotaInvalido`, `ItemDeRota`,
`ler_itens_de_rota`, `_blocos_de_item`, `_item_de_dict`, `_inteiro_positivo_do_item`.

**Em `l2scanner/dashboard_dados.py`:** `CALCULADORA_COM_ITENS`, `CALCULADORA_SEM_ITENS`,
`CALCULADORA_SEM_TAXA`, `FRASE_DE_SEM_ITENS_CONFIGURADOS`, `FRASE_DE_SEM_TAXA_DA_ADENA`,
`MOLDE_DE_ITEM_NUNCA_VISTO`, `MOLDE_DE_NOME_AMBIGUO`, `MOLDE_DO_PACOTE_DO_NPC`,
`MOLDE_DA_LEITURA_DA_CONFIGURACAO`, `_bloco_da_calculadora`; e os parametros novos `itens` e
`itens_lidos_em` em `payload`.

**Em `l2scanner/dashboard.py`:** os parametros `itens` e `itens_lidos_em` em `montar_servidor`, em
`Manipulador` e em `CacheDaLeitura.pronto`; a leitura da configuracao em `main` antes do bind, com
o instante dessa leitura capturado no mesmo ponto.

**Chaves novas no JSON de `GET /dados`:**
`calculadora` (ou `null` na falha fechada) → `estado`, `aviso`, `itens_lidos_em` (**string ja
formatada pelo Python**, nunca um carimbo cru para o navegador montar), `itens[]`; e cada item →
`estado`, `item`, `nome_exibido`, `npc.texto`, `npc.pacote_texto`, `mercado.texto`, `vencedora`,
`diferenca.xm`, `diferenca.percentual`, `n`, `recencia`, `velho`, `aviso`.

**Ids e classes novos no DOM:** `#rotas`, `#rotas-aviso`, **`#rotas-lidos-em`**, `#rotas-lista`,
`#molde-da-rota`, e as
classes `rota`, `rota__item`, `rota__aviso`, `rota__lados`, `rota__lado`, `rota__lado--npc`,
`rota__lado--mercado`, `rota__rotulo`, `rota__valor`, `rota__pacote`, `rota__marca`,
`rota__diferenca`, `rota__procedencia`, `rota__n`, `rota__recencia`. Atributos de estado da
linha: `data-estado` e `data-vencedora`.

**Chave nova no `config.toml`:** a secao `[[dashboard.item]]`, entrando COMENTADA.

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: O leitor de [[dashboard.item]] que DERRUBA o arranque</name>
  <files>l2scanner/config.py, config.toml, tests/test_dashboard_itens_config.py</files>

  <read_first>
    - `l2scanner/config.py` linhas 1140-1460 — `SECAO_DA_RECEITA`, `_EXEMPLO_DA_RECEITA`,
      `ReceitaInvalida`, `ComponenteDaReceita`, `Receita`, `ler_receitas`, `_blocos_de_receita`,
      `_receita_de_dict`, `_componente_de_dict`, `_inteiro_positivo_da_receita`. **Este e o
      analogo a imitar linha por linha**: mesma forma de recusa, mesma regra de
      `config.local.toml` vencendo com aviso nomeando o vencedor, mesma ordem de guarda de
      booleano ANTES do teste numerico.
    - `l2scanner/config.py` linhas 429-470 — `_recusar_bosses_repetidos`, e 736-766 —
      `_recusar_nicks_repetidos`. O analogo da recusa de nome repetido.
    - `config.toml` linhas 440-470 — o bloco `[[receita]]` COMENTADO. O analogo de como uma
      secao opcional entra no arquivo do usuario.
    - `l2scanner/dashboard_cambio.py` linhas 52-100 — `TETO_DE_DIGITOS_INTEIROS` e
      `TETO_DE_CASAS_DECIMAIS`: o analogo de por que um numero de dinheiro ganha teto, e de
      como o teto se escreve como ESCOLHA.
    - `tests/test_mercado_receitas.py` — a forma dos testes de recusa desta arvore.
  </read_first>

  <behavior>
    - Arquivo ausente devolve lista vazia; secao ausente devolve lista vazia. Nenhum dos dois e erro.
    - TOML quebrado levanta `ItemDeRotaInvalido` citando o NOME do arquivo.
    - `[dashboard]` presente mas nao sendo tabela levanta, em vez de estourar num acesso de atributo.
    - `item` presente mas nao sendo lista levanta, com a razao "um texto solto seria lido letra por letra" — medido: `item = "solto"` devolve a string.
    - Bloco sem `nome`, ou com `nome` vazio, levanta nomeando a posicao do bloco.
    - `preco_npc_adena` e `quantidade_do_pacote` ausentes, booleanos, fracionarios, zero ou negativos levantam, cada um com o campo nomeado.
    - Booleano e recusado ANTES do teste de inteiro — `isinstance(True, int)` e verdadeiro, e `preco_npc_adena = true` viraria "1 adena".
    - Acima do teto, cada um dos dois levanta.
    - Dois blocos com o mesmo `nome` (comparado por `mercado_analise.nome_normalizado`) levantam listando os dois.
    - Com `config.toml` e `config.local.toml` os dois trazendo blocos, vence o local, com aviso no log nomeando o vencedor; e SO o vencedor e validado.
    - A ordem da lista devolvida e a ordem escrita no arquivo.
  </behavior>

  <action>
    Acrescente ao fim de `l2scanner/config.py`, depois da secao da renda, a secao dos itens de
    rota do dashboard, seguindo `ler_receitas` como molde literal.

    Declare `SECAO_DO_DASHBOARD` com o valor da secao e `CHAVE_DOS_ITENS` com a chave da lista de
    tabelas, e escreva ao lado que o acesso e ANINHADO em dois niveis — medido: `tomllib` devolve
    um dicionario com a secao por fora e a lista por dentro, ao contrario do `[[receita]]`, que e
    de nivel unico. Essa e a unica diferenca estrutural entre este leitor e o analogo, e ela
    precisa estar escrita porque quem copiar `_blocos_de_receita` sem ler vai chamar `get` uma vez
    so e receber `None` para sempre.

    Declare `_EXEMPLO_DO_ITEM` no molde de `_EXEMPLO_DA_RECEITA`: o bloco pronto para o usuario
    copiar, com Gemstone C como o exemplo, os tres campos e nada mais. Ele viaja em TODA recusa
    desta secao.

    Declare `TETO_DO_PRECO_DO_NPC` e `TETO_DA_QUANTIDADE_DO_PACOTE` como constantes nomeadas, cada
    uma com a razao por extenso e a marca de ESCOLHA e nao medicao, no molde de
    `TETO_DE_DIGITOS_INTEIROS` do `dashboard_cambio`. A razao a escrever: este numero multiplica
    uma decisao de dinheiro real, e um numero colado por acidente de outra janela — um id de
    conversa, um telefone — tem de ser recusado em vez de virar um veredito absurdo
    perfeitamente formatado. Escreva tambem que ninguem mediu quanto um item de NPC pode custar, e
    que se um dia um preco legitimo bater no teto, o numero sobe e e uma linha.

    Declare `ItemDeRotaInvalido` como classe PROPRIA, e nao reuso de `ReceitaInvalida`. Escreva a
    razao no molde da docstring de `ReceitaInvalida`: o que separa as duas e o destino do `except`.
    A receita e capturada pelo laco do mercado, que segue coletando; esta aqui e lida pelo arranque
    do dashboard, que PARA. Uma classe separada e o que permite os dois chamadores tratarem cada
    caso sem inspecionar texto de mensagem.

    Declare `ItemDeRota` como dataclass congelada com `nome`, `preco_npc_adena` e
    `quantidade_do_pacote`. Escreva na docstring que `nome` e o nome COMO O USUARIO LE NA TELA, e
    que a resolucao dele para uma chave de serie do CSV acontece na analise e nunca aqui — este
    modulo le TOML e nao sabe o que e uma serie. E a mesma frase que `ComponenteDaReceita` ja
    carrega, e ela e verdadeira pelo mesmo motivo.

    Escreva `ler_itens_de_rota`, `_blocos_de_item`, `_item_de_dict` e `_inteiro_positivo_do_item`
    espelhando as quatro funcoes irmas da receita. As diferencas, todas deliberadas: o acesso
    aninhado; a guarda de que a secao e uma tabela antes de procurar a chave dentro dela; os dois
    tetos; e a recusa de nome repetido, que a receita nao tem e esta aqui porque duas entradas do
    mesmo item produziriam duas linhas na tela com dois vereditos e nenhuma forma de saber qual
    vale. A comparacao de nome repetido usa `mercado_analise.nome_normalizado`, e nao igualdade
    crua, pelo mesmo motivo que o casamento com a serie usa — caixa e espaco nao podem fazer duas
    entradas parecerem distintas.

    Na docstring de `ler_itens_de_rota` escreva por extenso, como decisao e nao como detalhe, POR
    QUE este numero mora no `config.toml` e nao no `cambio.json`: o `cambio.json` e escrito pelo
    NAVEGADOR e por isso e JSON de maquina; o preco do NPC e escrito A MAO, uma vez, e TOML tem
    comentario — que e onde o usuario anota de qual NPC aquele preco veio. Esse e o criterio, e
    nao gosto.

    No `config.toml`, acrescente a secao nova COMENTADA, no molde do bloco `[[receita]]` que ja
    esta la: o cabecalho explicando o que a secao faz, os tres campos com o que cada um significa,
    a instrucao de que o `nome` tem de ser o nome exato como aparece no jogo, a nota de que um
    bloco torto derruba o arranque de proposito, e dois blocos comentados — Gemstone C e Gemstone
    B, as duas primeiras instancias que o ROADMAP nomeia.

    **Os precos dos dois blocos entram como um valor de exemplo evidente e o comentario diz que
    e exemplo, porque ninguem nesta arvore sabe quanto o NPC cobra.** Escrever um numero plausivel
    no arquivo que o usuario vai usar seria fabricar um numero de dinheiro — o oposto do que esta
    fase existe para fazer. O comentario tem de mandar o usuario conferir no NPC antes de
    descomentar.

    O arquivo inteiro do `config.toml` e as mensagens de recusa ficam SEM ACENTO, como todo texto
    que este projeto poe na frente do usuario em terminal.
  </action>

  <verify>
    <automated>python -m pytest tests/test_dashboard_itens_config.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_itens_config.py -q` coleta **>= 18** testes e termina em 0 falhas.
    - Existe um teste por forma de recusa, e cada um afirma que a mensagem contem o NOME do item (quando ha nome) e o NOME do campo. Um teste que so afirme `pytest.raises` sem olhar a mensagem nao conta para o piso.
    - **Controle da guarda de booleano:** um teste passa `preco_npc_adena = true` e afirma `ItemDeRotaInvalido`; um segundo teste afirma que `_inteiro_positivo_do_item` chamado com o mesmo bloco mas com a guarda de booleano REMOVIDA da ordem devolveria `1` — escrito como asserção sobre `isinstance(True, int)` sendo verdadeiro, para a razao da ordem ficar medida e nao afirmada.
    - **Controle do acesso aninhado:** um teste afirma que `tomllib.loads` sobre um `[[dashboard.item]]` produz a lista SOMENTE por acesso de dois niveis, e que o acesso de um nivel devolve `None`. Sem ele, um leitor que sempre devolve lista vazia passaria em todos os testes de "secao ausente".
    - Um teste afirma que `config.local.toml` vence e que o bloco TORTO do `config.toml` perdedor **nao** derruba nada — validar o perdedor e o defeito que o analogo ja documenta.
    - Um teste afirma que dois blocos com `nome` diferindo so em caixa e espaco duplo sao recusados como repetidos.
    - `python -c "import l2scanner.config as c; print(c.TETO_DO_PRECO_DO_NPC, c.TETO_DA_QUANTIDADE_DO_PACOTE)"` imprime os dois numeros.
    - `grep -v '^#' config.toml | grep -c 'dashboard.item'` devolve **0** — o bloco entra comentado, e a contagem ignora comentario justamente porque o bloco E comentario.
    - `git diff --numstat requirements.txt` sai vazio.
  </acceptance_criteria>

  <done>
    Um `[[dashboard.item]]` valido vira `ItemDeRota`; qualquer forma torta levanta
    `ItemDeRotaInvalido` nomeando o item e o campo; o `config.toml` tem a secao comentada com as
    duas primeiras instancias e a instrucao de conferir o preco no NPC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A conta em Fraction, o bloco no payload, e o arranque que para</name>
  <files>l2scanner/dashboard_rotas.py, l2scanner/dashboard_dados.py, l2scanner/dashboard.py, tests/test_dashboard_rotas_tracer.py</files>

  <read_first>
    - `l2scanner/mercado_analise.py` linhas 140-410 — `N_MINIMO_PARA_MENOR`,
      `N_MINIMO_PARA_MEDIANA`, `unitario`, `Evidencia`, `MenorPedidoVisivel`,
      `menor_pedido_visivel`, `mediana_dos_unitarios`, `recencia_do_preco`.
    - `l2scanner/mercado_analise.py` linhas 760-1060 — `nome_normalizado`, `ResolucaoDeNome`,
      `resolver_o_nome`, `MARGEM_CALCULADA`/`MARGEM_SEM_SERIE`/`MARGEM_AMBIGUA`, `LinhaDaMargem`,
      `MargemDeCraft`, `margem_de_craft`. **`margem_de_craft` e o analogo direto**: uma conta
      composta sobre o modelo, com estados nomeados, `Fraction`, evidencia e idade viajando
      dentro do resultado, e um `motivo` para cada quebra.
    - `l2scanner/mercado_console.py` linhas 219-360 — `formatar_centesimos`,
      `formatar_unitario_derivado`, `UNIDADE_DA_TAXA`, `formatar_taxa_derivada`,
      `formatador_do_unitario`, `descrever_a_quantidade`.
    - `l2scanner/dashboard_dados.py` linhas 36-80 (os imports e a razao do underscore),
      100-275 (as frases e os cinco estados), 999-1209 (`_fonte`, `_falha_fechada`,
      `_valor_em_reais`, `payload`).
    - `l2scanner/dashboard_cambio.py` linhas 1-30 — a razao de o cambio entrar por PARAMETRO e
      nao por import. **O mesmo contrato por FORMA vale para os itens.**
    - `l2scanner/dashboard.py` linhas 326-500 (`RESOLUCAO_DO_CACHE`, `_truncado`,
      `CacheDaLeitura`, `Manipulador.__init__`) e 730-838 (`montar_servidor`, `main`).
  </read_first>

  <behavior>
    - `custo_da_rota_do_npc` sobre um item de 15.000 adena por pacote de 1, com taxa `Fraction(9,10000)`, devolve unitario `Fraction(27,2)` centesimos de XM — exato, nao 13,5 em ponto flutuante.
    - `custo_da_rota_do_mercado` sobre um `MenorPedidoVisivel` de total 5900 e quantidade 1 devolve unitario `Fraction(5900,1)`.
    - Com esses dois, a vencedora e a do NPC, e `diferenca_por_unidade` e a subtracao exata.
    - Duas rotas dentro da faixa de empate devolvem `ROTA_EMPATADA` e `vencedora` nulo, mesmo quando uma e estritamente menor que a outra.
    - Duas rotas fora da faixa por uma margem minima acima dela devolvem `ROTA_DECIDIDA` — o par de casos vizinhos da borda e um teste, e nao um comentario.
    - Nome que nao casa com serie nenhuma devolve `ROTA_NUNCA_VISTA` com o nome que o usuario escreveu.
    - Nome que casa com duas series devolve `ROTA_NOME_AMBIGUO` com as candidatas ORDENADAS.
    - `payload` sem itens devolve o bloco `calculadora` no estado sem itens, com a frase do Python e a lista vazia.
    - `payload` com itens mas sem taxa da Adena devolve o estado sem taxa, e a decisao vem do MESMO `MenorPedidoVisivel` que o destaque usa.
    - `payload` nos dois estados de falha fechada devolve `calculadora` nulo.
    - A lista `avisos` do payload tem exatamente o mesmo conteudo e a mesma ordem que tinha antes desta tarefa, em todos os cinco estados.
  </behavior>

  <action>
    Crie `l2scanner/dashboard_rotas.py`. Ele e PURO: sem disco, sem relogio proprio (o instante
    entra por parametro), sem impressao — a mesma disciplina que o cabecalho de `mercado_analise`
    declara e que `dashboard_dados` repete. Escreva isso no cabecalho, junto com a razao: e o que
    permite a suite inteira exercitar a conta sem montar servidor nem escrever arquivo.

    O contrato com o item configurado e por FORMA e nao por import: `dashboard_rotas` nao importa
    `config`. Ele consome qualquer objeto com os tres atributos, sob `TYPE_CHECKING`, no molde
    literal com que `mercado_analise` consome `ObservacaoLida` e com que `dashboard_dados.payload`
    consome o cambio. Escreva a razao: uma dependencia entre o calculo e o leitor de arquivo
    tornaria a conta nao-testavel sem disco, e nenhum dos dois lados pediu isso.

    Escreva no cabecalho, por extenso, a UNIDADE em que a conta acontece, porque ela e a coisa que
    mais facilmente se erra por um fator de dez aqui: as duas rotas terminam em **centesimos de XM
    por UMA unidade do item**, e as duas chegam la por caminhos diferentes. A do mercado ja e isso
    — `mercado_analise.unitario` sobre a serie do item devolve centesimos de XM por unidade. A do
    NPC sai de adena: o preco em adena multiplicado pela taxa (que e centesimos de XM por adena,
    exatamente o numero que o destaque da pagina ja exibe), dividido pela quantidade do pacote.
    Escreva a derivacao com os numeros reais medidos hoje, no molde da docstring de
    `formatar_taxa_derivada`, que documenta a propria conta assim porque a pesquisa dela errou por
    um fator de dez.

    Escreva tambem, no cabecalho, **o achado que desenha o modulo**: as duas rotas terminam
    multiplicadas pelo mesmo `reais_por_xm`, entao ele CANCELA na comparacao. Por isso a conta
    inteira deste modulo e em XM e nao existe nenhum parametro de cambio aqui. O R$ e uma
    multiplicacao de EXIBICAO, e ela mora do lado de quem exibe. Uma versao deste modulo que
    comparasse em BRL perderia o veredito exatamente no dia em que o usuario esquecesse de
    atualizar o cambio, que e o dia em que ele mais precisa dele.

    Declare `MARGEM_DE_EMPATE_PERCENTUAL` como constante nomeada, em `Fraction`, com a razao por
    extenso e a marca **ESCOLHA, NAO MEDICAO**, no molde de `N_MINIMO_PARA_MEDIANA`. A razao a
    escrever: o veredito repousa no MENOR PEDIDO VISIVEL, que e UMA oferta, e eleger um vencedor
    por tres decimos por cento afirmaria uma precisao que o dado nao tem. Escreva tambem o que FOI
    medido, com o rotulo de que isso nao mede a faixa: no CSV real de hoje, a segunda oferta mais
    barata do `common-fafurion-doll#` esta 9,2% acima da mais barata pelo mesmo denominador que a
    tela usa — o menor pedido visivel se move em dezenas por cento quando alguem compra a oferta
    de baixo. Isso diz que existe imprecisao a respeitar; **nao** diz que ela vale o numero
    escolhido. E a escapatoria da casa: se na pratica a tela empatar demais, ou de menos, este
    numero muda, e e uma linha.

    Declare os dois nomes de vencedora e os quatro estados de linha como constantes nomeadas, e
    `ESTADOS_DA_ROTA` como tupla fechada na ordem de precedencia — nome ambiguo primeiro, depois
    nunca visto, depois empatado, depois decidido. Escreva a razao da ordem no molde do bloco de
    `ESTADOS` do `dashboard_dados`: varios podem ser verdade ao mesmo tempo, e a ordem escrita num
    lugar so e o que impede quatro `if` em telas diferentes darem quatro respostas para o mesmo
    item.

    Escreva `CustoDaRota` e `VereditoDeRota` como dataclasses congeladas. A `Evidencia` e a
    recencia viajam DENTRO do veredito, e nao ao lado — a mesma regra que `LinhaDaMargem` ja
    aplica a idade, pela mesma razao: quem desenha nao pode ter como esquecer de pedir.

    Escreva `custo_da_rota_do_npc`, `custo_da_rota_do_mercado`, `veredito_de_uma_rota` e
    `vereditos_das_rotas`. O casamento do nome e `mercado_analise.resolver_o_nome`, chamado e nao
    reimplementado. Escreva ao lado da chamada a **refutacao ja medida no workstream `mercado`**,
    com o numero: o corte de similaridade de 0,8947 foi calibrado para agrupar duas leituras de
    OCR dos MESMOS pixels; aplicado a texto digitado por humano ele junta um item com o mesmo item
    de encanto diferente, com semelhanca de 0,9286. Aplicado aqui, ele juntaria Gemstone B com
    Gemstone C — e o casamento exato com quebra por ambiguidade e precisamente o que impede isso.
    A refutacao mora no fonte, ao lado do codigo que ela justifica, como manda a casa.

    A diferenca percentual e a diferenca absoluta dividida pela rota MAIS CARA. Escreva a escolha e
    a razao: dividir pela mais cara responde "quanto eu economizo", que e a pergunta que o usuario
    faz; dividir pela mais barata infla o numero — uma rota que custa o dobro da outra le 50% por
    um denominador e 100% pelo outro, e o numero maior e o que agrada. Escreva os dois numeros
    como exemplo. E declare que a faixa de empate compara contra ESTE MESMO denominador, porque
    uma tela que dissesse empatado ao lado de um percentual acima da faixa pareceria defeito.

    Guarde o caso degenerado: se a rota mais cara custa zero, nao ha percentual a calcular e o
    estado e empatado — zero contra zero e empate, e a divisao nao acontece. Escreva a guarda com
    a razao, e nao so o `if`.

    Em `l2scanner/dashboard_dados.py`, acrescente as tres constantes de estado do bloco e as
    frases novas, todas como constantes nomeadas no bloco de frases que ja existe, com acento
    (a pagina e HTML em UTF-8 e a moldura e nossa) e no estilo do `## Copywriting Contract`. A
    frase de nenhum item configurado diz O QUE FAZER — o nome da secao e os tres campos —, no
    molde de anatomia de mensagem da casa. A de item nunca visto diz que o preco do NPC ja esta
    configurado e que falta o scanner ver o item, porque sumir da tela seria indistinguivel de
    "esqueci de configurar". A de nome ambiguo lista as candidatas e diz que o dashboard nao vai
    escolher por ele.

    `MOLDE_DA_LEITURA_DA_CONFIGURACAO` monta a frase do instante da leitura, e ela sai do Python
    JA PRONTA, usando o mesmo `_recencia_em_duas_formas` que o resto da pagina usa — a relativa e a
    absoluta juntas, porque o navegador nao tem o direito de compor uma segunda forma de tempo.
    **A frase diz que o arquivo foi LIDO naquele instante, e nunca que o valor foi informado
    naquele instante** — as duas coisas sao diferentes e a segunda seria falsa, porque o numero
    pode estar no arquivo ha meses. A escolha da palavra e o requisito aqui, e nao um detalhe de
    redacao; o plano 02-03 traz a razao inteira, com a alternativa recusada.

    Escreva `_bloco_da_calculadora` e ligue-a em `payload`, que ganha o parametro `itens` com
    default de tupla vazia — a assinatura antiga continua valendo, exatamente como aconteceu
    quando o cambio entrou. A taxa entregue ao calculo e o `unitario` do MESMO objeto
    `MenorPedidoVisivel` que o destaque ja exibe. **Escreva por que ela tem de ser esse objeto e
    nao uma segunda chamada:** duas leituras da mesma serie no mesmo payload poderiam divergir se
    alguem mudasse uma delas, e a pagina passaria a mostrar dois valores para a mesma adena, um no
    destaque e outro no veredito.

    O criterio do estado de taxa ausente, NESTA fatia, e "nao ha menor da Adena" — o piso do menor,
    que vale um. **Escreva ao lado que esse criterio esta em aberto e que quem o fecha e o plano
    02-02**, que decide se o veredito herda tambem a evidencia da taxa. Deixar a pergunta escrita e
    diferente de deixa-la calada: o proximo plano tem de encontra-la, e nao redescobri-la.

    Acrescente `calculadora` com valor nulo em `_falha_fechada`, ao lado de `destaque` nulo e
    `series` vazia, e escreva que isso e a mesma falha fechada: quando o programa acabou de dizer
    que nao entende o arquivo, ele nao pode oferecer um veredito sobre dinheiro tirado dele.

    **REGRA DURA desta tarefa, e ela nao e negociavel:** nada e acrescentado, removido ou
    reordenado dentro da lista `avisos`. O `01-07-SUMMARY.md` registra que a ORDEM dessa lista
    virou contrato — o `dashboard.js` acha tres frases por POSICAO, e quatro testes prendem isso
    sobre payloads reais. As frases desta fase viajam DENTRO do bloco `calculadora`, cada uma no
    campo `aviso` do seu dono. Escreva essa restricao como comentario ao lado do bloco novo, para
    que a proxima pessoa nao a descubra pelo teste vermelho.

    Em `l2scanner/dashboard.py`, faca `montar_servidor` e `Manipulador` receberem os itens **e o
    instante em que eles foram lidos**, e passa-los adiante ate `payload`, pelo caminho que o
    `pasta_do_mercado` ja percorre. Em `main`, leia a configuracao ANTES de montar o servidor: uma
    recusa imprime a mensagem e devolve codigo de saida nao-zero, e a porta nunca chega a ser
    aberta.

    **O instante da LEITURA viaja junto, e ele nao e o instante do informe.** Ele existe porque
    esta fase cria um modo de falha que a Fase 1 nao tinha: a configuracao e lida UMA vez, no
    arranque, entao o usuario que corrigir um preco no `config.toml` e recarregar o navegador ve o
    numero VELHO, sem nenhum sinal de que o processo nao releu o arquivo. Com o instante da leitura
    na tela, essa pergunta — "o dashboard que esta rodando ja conhece a minha correcao?" — tem
    resposta. Ele e capturado no mesmo ponto da leitura, com o mesmo relogio que o resto do
    servidor usa, e nunca dentro de `payload`, que continua puro. Escreva a razao no molde da decisao do
    `02-CONTEXT`: a watchlist do mercado podia degradar porque so promovia series no console, mas
    uma CONTA torta que degradasse para "sem resposta" sairia calada, e esta conta e sobre dinheiro
    real.

    Os itens **nao** entram na chave do `CacheDaLeitura`. Escreva a razao ao lado da chave: eles
    sao lidos uma vez no arranque e nao podem mudar sem reinicio, justamente porque um bloco torto
    tem de derrubar o arranque; reler a cada pedido moveria essa derrubada do arranque para uma
    volta de polling qualquer, no meio da noite.

    Ao lado do import de `config` em `dashboard.py`, escreva a medicao: sozinho, importar o
    dashboard traz 341 modulos, **ja com `cv2` e `numpy`** pela segunda aresta que o `raiz.py`
    documenta; com `config` junto sao 366 — **+25 modulos, e nenhum deles e `cv2` ou `numpy`**.
    Medido em 2026-09-03. Sem essa linha, a proxima pessoa vai ler este import como o retorno da
    cadeia pesada que o corte de `RAIZ` cortou, e vai desfaze-lo.
  </action>

  <verify>
    <automated>python -m pytest tests/test_dashboard_rotas_tracer.py tests/test_dashboard_dados.py tests/test_dashboard_js.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_rotas_tracer.py -q` coleta **>= 20** testes, 0 falhas.
    - `python -m pytest tests/test_dashboard_dados.py tests/test_dashboard_js.py tests/test_dashboard_servidor.py -q` termina em 0 falhas — **os quatro testes de ordem de `avisos` do 01-07 continuam verdes**. Este e o criterio que reprova a rota mais tentadora desta tarefa.
    - Um teste afirma, sobre um payload REAL montado a partir de uma fixture, que `dados["avisos"]` e IDENTICO (mesma lista, mesma ordem) ao produzido pela mesma fixture sem itens configurados. Um `assert` sobre o comprimento nao basta: a comparacao e da lista inteira.
    - Um teste afirma que `custo_da_rota_do_npc(item_de_15000_por_1, Fraction(9,10000)).unitario == Fraction(27, 2)` — **igualdade exata contra uma `Fraction`, nunca `pytest.approx`**.
    - **Controle do `Fraction`, em duas saidas e nenhuma delas e fabricar o par.** Existe um teste que exercita a diferenca entre a conta exata e a mesma conta em ponto flutuante, e ele fecha por UMA das duas:
      **(a)** demonstra um par de precos em que o `float` faz o veredito cair do lado ERRADO e a `Fraction` do lado certo — o par tem de ser DERIVADO, nunca escolhido a dedo ate dar certo; ou
      **(b)** registra no cabecalho do teste **o que foi tentado e por que o par nao apareceu**, e prende no lugar a propriedade mais forte que se conseguir medir — por exemplo, que a diferenca exata entre as duas rotas e nao nula enquanto a diferenca em ponto flutuante e zero.
      **A forma fraca continua proibida nas duas saidas:** um teste que apenas afirme que a implementacao usa `Fraction`, sem exercitar diferenca nenhuma, NAO satisfaz este criterio.
      A razao de as duas saidas existirem esta na regra 6 do `CLAUDE.md`: um criterio que so aceita o par obriga quem nao o encontrar a escolher entre reprovar e fabricar, e fabricar e o desfecho que ninguem ve. O plano 02-02 cobra a mesma propriedade com a mesma forma de duas saidas, de proposito — os dois criterios sao IRMAOS e nao podem divergir.
    - **Par de borda da faixa de empate:** dois testes vizinhos, um com a diferenca percentual logo ABAIXO da margem afirmando `ROTA_EMPATADA` e `vencedora is None`, e outro logo ACIMA afirmando `ROTA_DECIDIDA` com a vencedora nomeada. Os dois valores sao derivados da constante, e nao numeros escritos a mao — assim a mudanca da constante move os dois juntos.
    - Um teste afirma que `resolver_o_nome` e chamado de fato: com duas series cujo `nome_exibido` normaliza igual, o estado e `ROTA_NOME_AMBIGUO` e `candidatas` sai ORDENADA.
    - Um teste sobe o servidor numa porta efemera com um item configurado e uma fixture de CSV, faz `GET /dados` e afirma que `calculadora["itens"][0]["vencedora"]` e o esperado e que `calculadora["itens"][0]["npc"]["texto"]` e byte-identico a `mercado_console.formatar_unitario_derivado` sobre a mesma `Fraction`. **Esta e a prova de ponta a ponta do tracer.**
    - Um teste afirma que `main` com um `config.toml` de bloco torto devolve codigo de saida nao-zero e que a mensagem nomeia o item e o campo; e um segundo afirma que **nenhuma porta foi aberta** nesse caminho (a porta continua livre para um bind subsequente).
    - `python -c "import l2scanner.dashboard_rotas as r; print(sorted(m for m in __import__('sys').modules if m.startswith('l2scanner')))"` **nao** lista `l2scanner.config` — o contrato por forma e verificado, e nao so afirmado.
    - `git diff --numstat requirements.txt` sai vazio.
  </acceptance_criteria>

  <done>
    A conta existe em `Fraction`, o payload carrega o bloco `calculadora` nos tres estados de
    bloco e nos quatro de linha, o servidor le a configuracao no arranque e para quando ela esta
    torta, e a lista `avisos` continua byte a byte como estava.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: A quarta regiao — o molde na marcacao, o tema, e a linha desenhada</name>
  <files>l2scanner/recursos/dashboard/index.html, l2scanner/recursos/dashboard/dashboard.css, l2scanner/recursos/dashboard/dashboard.js, tests/test_dashboard_rotas_tracer.py</files>

  <read_first>
    - `l2scanner/recursos/dashboard/index.html` inteiro (279 linhas) — as tres regioes, os
      atributos de estado no `<body>`, e o comentario que explica por que nada e embutido.
    - `l2scanner/recursos/dashboard/dashboard.css` linhas 30-200 (tokens, `.pagina`, `.painel`,
      `.painel__titulo`, `.faixa`) e 675-830 (o bloco de estados, incluindo as regras que retiram
      `.painel` da tela nos dois estados de falha fechada).
    - `l2scanner/recursos/dashboard/dashboard.js` linhas 60-135 (as constantes e a razao de cada
      uma), 136-230 (as primitivas de escrita e `avisosPorPosicao`), 259-380 (`pintarUmCartao` e
      `pintar`).
    - `tests/test_dashboard_js.py` linhas 140-160 e 300-350 — `SONDAS_DE_MARCACAO`,
      `CONTROLE_DA_MARCACAO`, `SONDAS_DE_PISO_RECALCULADO`, `_so_o_codigo` e o teste que exige
      que **nenhuma linha tenha codigo antes de uma barra dupla**.
    - `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-UI-SPEC.md`
      secoes `Copywriting Contract`, `A receita do relevo` e `Estados da tela e sua precedencia`.
  </read_first>

  <action>
    No `index.html`, acrescente a quarta regiao DEPOIS de `#procedencia`, como uma `<section>` com
    o id da calculadora e a classe de painel. **Ela tem de ser um painel, e isso e o desenho:** as
    duas regras de falha fechada que ja existem no CSS retiram `.painel` da tela, entao a quarta
    regiao entra na precedencia da Fase 1 sem uma linha de CSS nova. Escreva isso no comentario da
    secao — quem tirar a classe depois vai reintroduzir o defeito sem quebrar teste nenhum.

    A regiao tem o titulo no molde dos outros tres, um paragrafo para o aviso de bloco (a frase do
    Python quando nao ha item ou nao ha taxa), **um paragrafo para o instante da leitura da
    configuracao**, e uma lista vazia que o JS preenche.

    **O instante da leitura fica em NIVEL DE REGIAO, e nunca dentro da linha.** Ele e um fato do
    bloco — a configuracao inteira foi lida uma vez, no arranque — e o payload o entrega em nivel de
    bloco, ao lado de `estado` e `aviso`. Repeti-lo dentro de cada linha imprimiria a mesma frase N
    vezes na tela e criaria a impressao falsa de que cada item foi lido num instante proprio.
    **O que E de cada linha e outra coisa:** o rotulo de que aquele preco foi informado por voce no
    `config.toml`, e esse fica na linha, no lado do NPC — os dois nao se confundem porque respondem
    a perguntas diferentes. O plano 02-03 escreve a razao inteira desse par, com as candidatas
    recusadas; aqui basta o vao existir e ser preenchido.

    Esta tarefa **ja escreve** o instante da leitura no vao, e nao deixa o campo pendurado esperando
    um plano futuro: um campo que atravessa duas ondas sem chegar a tela e um campo que ninguem
    percebe estar quebrado.

    Acrescente um elemento de molde com a estrutura de UMA linha: o nome do item, um paragrafo de
    aviso da linha, os dois lados (NPC pagando em adena, Mercado pagando em XM) cada um com seu
    rotulo, seu valor, e a marca de mais barata; o lado do NPC tambem com a linha do pacote; e no
    fim a diferenca e a procedencia com `n` e recencia. **Toda a copia de interface nasce aqui, na
    marcacao**, incluindo os dois rotulos de lado e a palavra da marca de vencedora — e o motivo
    e o que o `01-06` ja registrou: copia que nasce no JS e copia que nenhum teste de marcacao ve.

    Os rotulos dos dois lados sao o lugar onde a MOEDA de cada rota se escreve. O texto que vem do
    Python e o unitario formatado por `formatar_unitario_derivado`, que diz "por unidade
    (derivado)" e nao nomeia moeda — porque no console dele a moeda e implicita. Aqui as duas rotas
    aparecem lado a lado e a ambiguidade seria real, entao a moeda vira **cabecalho de coluna na
    marcacao**, conferivel por teste sobre o HTML. Escreva no comentario que a alternativa —
    remontar a string no navegador para enfiar a palavra dentro dela — seria o segundo formatador
    que o DASH-03 proibe.

    No `dashboard.css`, escreva as regras da quarta regiao usando SOMENTE os tokens que ja existem
    em `:root`. Nenhum hexadecimal novo, nenhuma escala de espacamento nova, nenhum quinto tamanho
    de tipo. A marca de mais barata aparece pelo atributo de vencedora na linha: com a vencedora
    sendo o NPC, so a marca do lado do NPC aparece; com empate, nenhuma das duas. **As duas rotas
    ficam SEMPRE visiveis**, e a perdedora nao e escondida nem apagada — esconder a perdedora
    impede conferir a conta, e esta conta e sobre dinheiro real (decisao travada do `02-CONTEXT`).
    Escreva essa razao ao lado da regra, porque a tentacao de "limpar a tela" escondendo a
    perdedora vai aparecer.

    O acento dourado e uma lista fechada de posicoes no UI-SPEC. A marca de vencedora entra na
    lista ou usa outro recurso — decida, e escreva a decisao com a razao ao lado da regra. Se
    entrar, o `01-UI-SPEC.md` continua sendo o documento e a lista dele passa a ter uma posicao a
    mais, dito por extenso no CSS.

    O valor de cada lado usa a mesma familia de fonte numerica e o mesmo alinhamento tabular que
    os cartoes do destaque ja usam, porque duas colunas de numero que nao alinham sao duas colunas
    que o olho nao compara.

    No `dashboard.js`, escreva a funcao que pinta a quarta regiao e chame-a de `pintar`, ao lado
    das outras. Ela le o bloco novo do payload e:

    - escreve o aviso de bloco por propriedade de TEXTO, e deixa a lista vazia quando nao ha item;
    - para cada item, clona o molde da marcacao pelo conteudo do elemento de molde, preenche cada
      vao por propriedade de TEXTO, e poe os dois atributos de estado na linha.

    **Nenhuma marcacao e montada em string.** O clone do molde nao passa por nenhuma das quatro
    sondas que `tests/test_dashboard_js.py` ja roda, e e por isso que ele e a rota certa: o
    conteudo nao confiavel — o nome do item vem de OCR sobre a tela do jogo e do arquivo que o
    usuario edita a mao — entra como texto e nunca como marcacao.

    **Nenhuma decisao visual mora no JS.** A linha recebe atributos; o CSS decide o que aparece.
    Uma condicao de tela escrita em JavaScript e uma condicao que nao aparece na folha de estilo.

    **Nenhuma contagem e comparada com um piso no JS.** O estado de cada linha chega PRONTO do
    Python. As sondas de piso recalculado do `01-07` acusariam a comparacao, e elas estao certas:
    duas autoridades sobre o mesmo piso divergem na primeira vez que alguem mudar uma delas.

    A lista so e reconstruida quando o conteudo mudou. Guarde a assinatura da ultima pintura — a
    concatenacao dos textos que o Python ja formatou, e nao um recalculo de coisa nenhuma — e
    compare antes de reconstruir. Escreva a razao: reconstruir a cada volta de dois segundos
    apagaria a selecao de texto do usuario no meio de uma conferencia, e esta e uma conta que o
    usuario vai conferir com o dedo na tela.

    Respeite a premissa do removedor de comentarios da suite: nenhuma linha deste arquivo tem
    codigo antes de uma barra dupla.
  </action>

  <verify>
    <automated>python -m pytest tests/test_dashboard_rotas_tracer.py tests/test_dashboard_js.py tests/test_dashboard_pagina.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_js.py tests/test_dashboard_pagina.py -q` termina em 0 falhas — **as 41 sondas do `01-07` e as da pagina continuam verdes com o codigo novo dentro do arquivo**.
    - `node --check l2scanner/recursos/dashboard/dashboard.js` sai 0. (Se `node` nao existir nesta maquina, o teste da suite pula com a razao dita, como ja faz.)
    - Um teste afirma, sobre o HTML, que a quarta regiao existe, que ela carrega a classe de painel, e que o elemento de molde contem os dois rotulos de lado e a palavra da marca de vencedora. **Se a copia migrar para o JS, este teste cai.**
    - Um teste afirma que o vao do instante da leitura existe **em nivel de regiao e NAO dentro do elemento de molde da linha** — as duas metades sao asseridas, porque so a primeira passaria tambem sobre um vao duplicado dentro da linha. E um teste afirma que o JS **escreve** nele: um vao que existe e ninguem preenche e copia morta com cara de campo.
    - Um teste afirma, sobre o CSS, que existe regra ligando o atributo de vencedora a exibicao da marca, e um **controle negativo** afirmando que a sonda acusa a ausencia dessa regra num CSS de mentira.
    - Um teste afirma que **nenhuma regra do CSS esconde o lado perdedor** — a busca e por uma regra que retire da tela um dos dois lados, e o teste tem controle: uma folha de mentira contendo essa regra tem de ser acusada.
    - **As tres sondas de CSS acima sao a FORMA MINIMA, e o plano 02-03 as ESTENDE — ele nao as reescreve.** Escreva cada uma como funcao chamavel de fora do arquivo de teste, com o cabecalho dizendo que ela sera reusada e que uma segunda copia divergiria. O 02-03 amplia o conjunto de formas reconhecidas e acrescenta um controle por forma; se a sonda nascer embutida num teste, aquele plano so tera a saida de copia-la, que e o defeito.
    - Um teste afirma que a quarta regiao NAO tem regra propria de falha fechada, e que ela desaparece pelas duas regras de `.painel` que ja existiam. A prova e por leitura do CSS: a lista de seletores das duas regras de falha fechada continua sendo a mesma de antes desta fase.
    - Um teste afirma que o `dashboard.js` contem escrita por propriedade de texto **e** clonagem de molde, e que a contagem das quatro sondas de marcacao continua zero — com o controle negativo que ja existe.
    - `grep -c 'painel' l2scanner/recursos/dashboard/index.html` sobe em relacao ao valor anterior ao plano, e o valor anterior fica registrado no SUMMARY.
    - `git diff --numstat requirements.txt` sai vazio.
  </acceptance_criteria>

  <done>
    Com um item configurado e a serie dele no CSV, a quarta regiao mostra as duas rotas lado a
    lado com a vencedora marcada, o pacote do NPC ao lado, `n` e recencia; sem item configurado
    ela mostra a frase do Python; e nos dois estados de falha fechada ela some junto com as
    outras tres.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` → processo do dashboard | numero de dinheiro escrito a mao pelo usuario, lido no arranque |
| `.mercado/observacoes.csv` → processo do dashboard | nome de item vindo de OCR sobre a tela do jogo, e arquivo que o usuario edita no Sheets |
| processo do dashboard → navegador local | JSON servido em `127.0.0.1`, sob a CSP e o portao de origem que a Fase 1 ja montou |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `config.ler_itens_de_rota` | medium | mitigate | Entrada torta derruba o arranque nomeando item e campo; booleano recusado ANTES do teste de inteiro; teto nomeado em cada um dos dois numeros |
| T-02-02 | Spoofing | `dashboard_rotas` ↔ serie do CSV | medium | mitigate | Casamento EXATO por `resolver_o_nome`; ambiguidade QUEBRA listando candidatas; nenhuma similaridade fuzzy, com a refutacao medida escrita no fonte |
| T-02-03 | Information disclosure | `#rotas` no DOM | low | mitigate | Nome do item entra por propriedade de texto sobre um molde clonado; as quatro sondas de marcacao do `01-07` cobrem a regressao; CSP `script-src 'self'` ja servida |
| T-02-04 | Denial of service | `preco_npc_adena` gigante | low | mitigate | `TETO_DO_PRECO_DO_NPC` e `TETO_DA_QUANTIDADE_DO_PACOTE` recusam antes de a conta acontecer |
| T-02-05 | Repudiation | o veredito na tela | medium | mitigate | `n`, recencia e a marca de derivado viajam dentro de cada numero; as duas rotas ficam sempre visiveis para a conta poder ser conferida |
| T-02-06 | Denial of service | releitura da configuracao a cada pedido | low | accept | Os itens sao lidos uma vez no arranque e nao entram na chave do cache; a decisao esta escrita ao lado da chave |
| T-02-SC | Tampering | cadeia de suprimentos | low | mitigate | Esta fase nao instala pacote nenhum. `git diff --numstat requirements.txt` sai vazio e e criterio de aceitacao em cada uma das tres tarefas |
</threat_model>

<verification>
1. `python -m pytest tests/test_dashboard_itens_config.py tests/test_dashboard_rotas_tracer.py -q` — 0 falhas, e os dois pisos de contagem atingidos.
2. `python -m pytest -q --ignore=tests/test_agenda.py` — rodada unica, saida inteira, sem `tail`. Termina em 0 falhas.
3. `git diff --numstat requirements.txt` sai vazio.
4. A cerca dura intacta: `git diff --name-only` nao lista `rastreador.py`, `visao.py`, `console.py`, `mercado_registro.py`, `mercado_analise.py`, `mercado_console.py` nem `mercado_catalogo.py`.
5. `git status --porcelain .mercado/` sai vazio depois da suite — nenhum teste tocou a pasta real.
6. Medicao manual a registrar no SUMMARY: rodar `dashboard_dados.payload` contra o `.mercado/` REAL com um item configurado apontando para `Common Fafurion Doll`, e anotar o veredito e os dois textos. Comparar com os numeros medidos na tabela de fatos deste plano — **se divergirem, o numero errado e o da tabela, e a correcao vai para o SUMMARY com os dois valores**.
</verification>

<success_criteria>
- Um `[[dashboard.item]]` valido no `config.toml` produz, num `GET /dados` sobre um servidor de pe, uma linha de calculadora com as duas rotas e a vencedora marcada.
- Um `[[dashboard.item]]` torto derruba o arranque nomeando item e campo, e nenhuma porta e aberta.
- Sem item configurado, e sem taxa da Adena, a quarta regiao diz o que houve com frase do Python.
- A comparacao acontece em `Fraction`, provada por igualdade exata e por um controle que mostra o `float` caindo do lado errado.
- A lista `avisos` do payload nao mudou, e os quatro testes de ordem do `01-07` continuam verdes.
- `requirements.txt` sem uma linha de diferenca.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-01-SUMMARY.md` when done.
</output>
