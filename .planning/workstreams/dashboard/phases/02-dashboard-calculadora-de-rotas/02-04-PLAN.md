---
phase: 02-dashboard-calculadora-de-rotas
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - tests/test_dashboard_rotas_generica.py
autonomous: true
requirements: [CALC-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um TERCEIRO item, que nao aparece como literal em nenhum arquivo de producao, atravessa a configuracao, o calculo, o payload, o servidor e os vaos da marcacao produzindo um veredito de forma IDENTICA a dos dois primeiros — e nenhuma linha de codigo de producao foi escrita para isso acontecer."
    - "Nenhum nome de item aparece como literal no codigo de calculo nem no codigo de tela. Provado por sonda literal com controle negativo, e nem o comentario do teste escreve os nomes procurados."
    - "Os commits deste plano nao tocam NENHUM arquivo de producao — a cerca e l2scanner/**, config.toml e requirements.txt; tests/** e o alcance e .planning/** esta fora da conta, porque o SUMMARY deste plano mora la. Se um arquivo de producao precisar mudar para o terceiro item funcionar, a generalidade era falsa — e o desfecho e registrar isso, nao remendar."
    - "A cerca dura do workstream mercado esta intacta, atribuida commit a commit e nao por diff cru contra a base, porque a branch e compartilhada com outros workstreams."
  artifacts:
    - tests/test_dashboard_rotas_generica.py
  key_links:
    - "config.ler_itens_de_rota -> dashboard_rotas.vereditos_das_rotas -> dashboard_dados.payload -> GET /dados -> os vaos do #molde-da-rota: os cinco elos que o terceiro item atravessa sem codigo novo."
---

<objective>
Provar o CALC-05: acrescentar um terceiro item e **uma entrada de configuracao**, sem uma linha de
codigo de calculo nem de tela.

Purpose: a genericidade e a diferenca entre uma calculadora e dois `if`. Ela nao se prova
afirmando — ela se prova instanciando algo que o codigo nunca viu e mostrando que ele atravessa
tudo. E ela so vale se a prova puder FALHAR: por isso este plano nao tem permissao de tocar em
arquivo de producao. Se o terceiro item exigir uma linha la, a resposta e registrar que a
generalidade nao existia, e nao escrever a linha.

Output: `tests/test_dashboard_rotas_generica.py`, no molde de cinco elos com controle negativo que
o `tests/test_dashboard_serie_generica.py` estabeleceu para o DASH-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/dashboard/REQUIREMENTS.md
@.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-CONTEXT.md
@.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-02-SUMMARY.md
@.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

## O precedente, e a armadilha que ele ja documentou

O `01-08` provou o DASH-05 em **cinco elos**, e o `01-VERIFICATION.md` registra que o quinto —
"toda propriedade que a funcao de desenho consome existe nos DOIS" — **passaria por vacuidade sem
um controle negativo no extrator**. Um extrator que nao encontra propriedade nenhuma devolve
conjunto vazio, e conjunto vazio esta contido em qualquer coisa.

A mesma armadilha existe aqui, em todos os cinco elos, e a regra deste plano e curta: **cada
asserção positiva vem acompanhada da prova de que a sonda dela DISCRIMINA**. Um guarda que passa
sobre uma implementacao vazia e pior que nenhum guarda — e a doutrina desta arvore, e o verificador
da fase procura por ela.

## A regra dura deste plano

`files_modified` tem **um** arquivo, e ele e de teste. Nenhum arquivo de producao entra.

Se, ao escrever a prova, ficar claro que o terceiro item **nao** atravessa alguma camada sem uma
mudanca de producao, o desfecho correto e:

1. escrever o teste que FALHA, com o nome dizendo o que ele encontrou;
2. registrar no SUMMARY, como `deviation`, exatamente qual camada tem o `if` e qual linha o
   remove;
3. **nao** escrever essa linha aqui.

O CALC-05 e uma afirmacao sobre o codigo que existe. Consertar o codigo dentro do plano que o
audita transformaria a auditoria numa formalidade.

## Artifacts this phase produces (plano 02-04)

**Arquivo novo:** `tests/test_dashboard_rotas_generica.py`, com cinco classes de elo, cada uma com
o teste positivo e o controle, e uma classe de gate de escopo.

<tasks>

<task type="auto">
  <name>Tarefa 1: Os cinco elos do terceiro item, cada um com o controle que o discrimina</name>
  <files>tests/test_dashboard_rotas_generica.py</files>

  <read_first>
    - `tests/test_dashboard_serie_generica.py` inteiro — os cinco elos, o controle negativo do
      extrator, e o cabecalho que declara por extenso o que a prova alcanca e o que ela nao
      alcanca. **Este e o molde a seguir, elo por elo.**
    - `l2scanner/config.py`, a secao dos itens de rota — a forma de `ItemDeRota` e de
      `ler_itens_de_rota`.
    - `l2scanner/dashboard_rotas.py` inteiro.
    - `l2scanner/dashboard_dados.py`, a funcao que monta o bloco da calculadora.
    - `l2scanner/recursos/dashboard/index.html`, o elemento de molde da linha.
    - `l2scanner/recursos/dashboard/dashboard.js`, a funcao que pinta a quarta regiao.
    - `tests/test_dashboard_calculadora_pagina.py` — os elos que o plano 03 ja escreveu, para
      **nao repetir** e sim estender para o terceiro item.
  </read_first>

  <action>
    Escreva o arquivo com o cabecalho no molde do analogo: o que esta prova afirma, o que ela nao
    alcanca, e por que cada controle existe.

    Escolha um terceiro item que **nao** e nenhum dos dois que o ROADMAP nomeia e que **nao**
    aparece em nenhum arquivo desta arvore. Escreva um `config.toml` de teste em pasta temporaria
    com os tres blocos, e uma serie de fixture para o terceiro no formato de seis colunas que o
    `mercado_registro` grava.

    **Elo 1 — configuracao.** `ler_itens_de_rota` sobre esse arquivo devolve os tres na ordem
    escrita, e o terceiro tem exatamente os mesmos campos que os dois primeiros.
    *Controle:* um arquivo com o terceiro bloco torto derruba, e a mensagem nomeia o terceiro — se
    o leitor so validasse os dois primeiros, o controle o pegaria.

    **Elo 2 — calculo.** `vereditos_das_rotas` sobre os tres devolve tres vereditos, e o conjunto
    de campos do terceiro e IDENTICO ao dos dois primeiros, seja qual for o estado em que cada um
    caia.
    *Controle:* a mesma comparacao rodada contra um veredito ao qual se removeu um campo tem de
    ACUSAR. Sem isso, comparar conjuntos vazios passaria.

    **Elo 3 — payload.** `payload` com os tres itens traz tres entradas em `calculadora.itens`, e
    as chaves de cada entrada sao identicas entre si.
    *Controle:* o extrator de chaves, rodado sobre um dicionario vazio, tem de devolver conjunto
    vazio **e o teste tem de reprovar nesse caso** — este e literalmente o controle que o
    `01-VERIFICATION.md` registra como imprescindivel.

    **Elo 4 — servidor.** Com um servidor de pe numa porta efemera, `GET /dados` entrega as tres
    entradas, e nenhum campo se perde na serializacao. O terceiro atravessa igual aos dois
    primeiros.
    *Controle:* a comparacao e entre o que o `payload` produziu e o que voltou do HTTP, campo a
    campo — assim uma serializacao que perdesse um campo seria vista, em vez de o teste conferir a
    resposta consigo mesma.

    **Elo 5 — a tela, por leitura de fonte.** Extraia do `dashboard.js` as propriedades do bloco
    que a funcao de pintura le, e afirme que todas existem em **cada uma das tres** entradas do
    payload real. E extraia do `index.html` os vaos do molde da linha, afirmando que a funcao de
    pintura escreve em todos e so neles.
    *Controle:* um payload de mentira sem uma das propriedades tem de ser ACUSADO, e o extrator
    rodado sobre um texto sem propriedade nenhuma tem de fazer o teste reprovar.

    **A sonda literal, que e o elo transversal.** Varra `l2scanner/dashboard_rotas.py`, a parte de
    `l2scanner/dashboard_dados.py` que monta o bloco, o `dashboard.js` e o `dashboard.css`
    procurando **qualquer nome de item como literal**, e afirme zero acusacoes. A lista do que
    procurar sai da configuracao de teste, e nao de nomes escritos dentro do arquivo de teste — e
    escreva no cabecalho que nem os comentarios deste arquivo escrevem os nomes procurados, pela
    mesma razao que o `01-07` registrou: uma sonda que pune o arquivo por NOMEAR o defeito que ele
    evita ensina a apagar a explicacao.
    *Controle:* um texto de mentira com um `if` por nome tem de ser acusado.

    Escreva na secao final do cabecalho **a fronteira desta prova, por extenso**, no molde do que o
    `01-07-SUMMARY.md` fez: o que estas asserções garantem e o piso — a configuracao chega, a conta
    roda, o payload carrega, o servidor entrega e os vaos existem. O que elas **nao** garantem e o
    desenho: que a terceira linha apareca bonita e legivel na tela e verificacao humana declarada,
    e nao cobertura automatica.
  </action>

  <verify>
    <automated>python -m pytest tests/test_dashboard_rotas_generica.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_rotas_generica.py -q` coleta **>= 14** testes, 0 falhas.
    - Os cinco elos existem como cinco classes nomeadas, **e cada uma tem ao menos um teste de controle**. Um elo sem controle nao satisfaz este criterio.
    - A sonda literal devolve zero acusacoes sobre os quatro arquivos de producao varridos, **e** o controle acusa o texto de mentira.
    - `grep -c "Gemstone" tests/test_dashboard_rotas_generica.py` devolve **0** — nem o teste que procura os nomes os escreve; a lista sai da configuracao de teste.
    - Um teste afirma que o conjunto de chaves do veredito do terceiro item e igual ao dos dois primeiros por **igualdade de conjunto**, e nao por conteudo — e o controle prova que a comparacao acusa um campo faltando.
    - O teste do elo 4 compara o dicionario do `payload` com o que voltou do HTTP campo a campo. Uma asserção de que a resposta tem status 200 nao satisfaz este criterio.
  </acceptance_criteria>

  <done>
    Um terceiro item que o codigo nunca viu atravessa as cinco camadas produzindo um veredito de
    forma identica aos dois primeiros, e cada elo dessa afirmacao tem uma prova de que a sonda dele
    discrimina.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: O gate de escopo — atribuido por commit, porque a branch e compartilhada</name>
  <files>tests/test_dashboard_rotas_generica.py</files>

  <read_first>
    - `.planning/workstreams/dashboard/phases/01-dashboard-do-cambio-ao-vivo/01-08-SUMMARY.md`,
      a secao **Gates de escopo da fase — o que foi MEDIDO**. Ela registra que a branch e
      compartilhada com outros workstreams e que **um `git diff` cru contra a base nao mede o que
      esta fase fez** — a atribuicao correta e por commit. Ela registra tambem dois gates que
      triparam e por que nenhum dos dois era a quebra que descreviam.
    - `tests/test_mercado_firewall_de_fase.py` — a forma de um guarda de escopo nesta arvore.
    - `l2scanner/dashboard_rotas.py` e `l2scanner/config.py` — para a lista de simbolos que o gate
      afirma existirem.
  </read_first>

  <action>
    Acrescente ao mesmo arquivo uma classe de gate de escopo, com tres afirmacoes que podem falhar:

    **Gate 1 — a cerca dura do workstream `mercado`.** Os sete arquivos que este workstream nao
    pode alterar (`rastreador.py`, `visao.py`, `console.py`, `mercado_registro.py`,
    `mercado_analise.py`, `mercado_console.py`, `mercado_catalogo.py`) continuam sem uma linha
    alterada por esta fase. **A medida e por atribuicao de commit, e nao por diff cru contra a
    base** — a branch e compartilhada, e o `01-08` ja mediu que o diff cru acusa commits de outros
    workstreams. O teste que couber na suite afirma o que da para afirmar sem git (por exemplo, que
    os modulos do dashboard importam desses arquivos e nunca escrevem neles); a atribuicao por
    commit vai para o SUMMARY, com o comando e a saida.

    **Gate 2 — `requirements.txt` sem uma linha.** Nenhuma dependencia entrou nesta fase.
    *Controle:* o guarda tem de REPROVAR quando uma linha nova e declarada, no molde do teste que
    o `tests/test_firewall_dashboard.py` ja tem para o mesmo fim.

    **Gate 3 — o dashboard nunca escreve no CSV.** A promessa central do DASH-01 continua valendo
    depois desta fase: rodar a calculadora nao muda tamanho, conteudo nem carimbo do
    `observacoes.csv`. A prova e a impressao de tres componentes antes e depois, sobre uma copia em
    pasta temporaria, no molde do que a Fase 1 ja faz — **e nunca sobre o arquivo real**.

    Escreva no cabecalho da classe por que os tres gates estao num arquivo de teste e nao so no
    SUMMARY: um gate que so existe no documento e um gate que ninguem roda de novo.
  </action>

  <verify>
    <automated>python -m pytest tests/test_dashboard_rotas_generica.py tests/test_firewall_dashboard.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_dashboard_rotas_generica.py -q` coleta **>= 20** testes no total do plano, 0 falhas.
    - O gate do `requirements.txt` tem controle: um teste afirma que o guarda REPROVA uma linha nova declarada.
    - O gate do CSV mede a impressao de tres componentes antes e depois, sobre copia em pasta temporaria, e `git status --porcelain .mercado/` sai vazio depois da suite.
    - **`git diff --name-only` para os commits deste plano nao lista NENHUM arquivo de producao.** A cerca e `l2scanner/**` mais `config.toml` mais `requirements.txt`; `tests/**` e o alcance do plano, e `.planning/**` esta **explicitamente fora da conta** porque o proprio `<output>` deste plano escreve o SUMMARY ali. Redigido como "somente `tests/`", o gate seria literalmente insatisfazivel — e um gate impossivel e afrouxado na primeira execucao, e um gate afrouxado uma vez para de discriminar para sempre. Se qualquer caminho da cerca aparecer, o plano falhou na sua unica premissa e o SUMMARY registra qual camada exigiu a linha, pelo caminho de desvio do cabecalho.
    - O SUMMARY traz a atribuicao por commit dos sete arquivos da cerca, com o comando e a saida — e nao a conclusao sozinha.
  </acceptance_criteria>

  <done>
    Os tres gates existem como teste, dois deles com controle, e a atribuicao por commit da cerca
    dura esta registrada com a saida do comando.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de teste ↔ arvore de producao | um teste que reimplementa o que audita prova apenas a copia |
| suite de teste ↔ `.mercado/` real | um teste que escreva na pasta do usuario quebraria a promessa central da Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Repudiation | prova de generalidade vazia | high | mitigate | Cada um dos cinco elos carrega o controle que prova que a sonda discrimina; o extrator vazio tem de reprovar |
| T-02-16 | Tampering | plano consertando o que audita | medium | mitigate | `files_modified` tem so arquivo de teste; `git diff --name-only` dos commits e criterio de aceitacao |
| T-02-17 | Tampering | teste escrevendo no `.mercado/` real | medium | mitigate | Toda medicao acontece sobre copia em pasta temporaria; `git status --porcelain .mercado/` vazio e criterio |
| T-02-SC | Tampering | cadeia de suprimentos | low | mitigate | Nenhum pacote novo; o gate do `requirements.txt` tem controle que reprova uma linha declarada |
</threat_model>

<verification>
1. `python -m pytest tests/test_dashboard_rotas_generica.py -q` — >= 20 testes, 0 falhas.
2. `python -m pytest -q --ignore=tests/test_agenda.py` — rodada unica, saida inteira. 0 falhas.
3. `git diff --name-only` dos commits deste plano nao lista nenhum caminho da cerca de producao
   (`l2scanner/**`, `config.toml`, `requirements.txt`). `tests/**` e o alcance; `.planning/**` esta
   fora da conta, porque o SUMMARY deste plano mora la.
4. Atribuicao por commit dos sete arquivos da cerca dura, registrada no SUMMARY com a saida.
5. `git status --porcelain .mercado/` vazio depois da suite.
6. `git diff --numstat requirements.txt` sai vazio.
</verification>

<success_criteria>
- Um terceiro item atravessa as cinco camadas sem uma linha de producao nova.
- Cada elo tem o controle que prova que ele discrimina.
- Nenhum nome de item aparece como literal no calculo nem na tela.
- Os tres gates de escopo existem como teste, e a atribuicao por commit esta medida e registrada.
</success_criteria>

<output>
Create `.planning/workstreams/dashboard/phases/02-dashboard-calculadora-de-rotas/02-04-SUMMARY.md` when done.
</output>
