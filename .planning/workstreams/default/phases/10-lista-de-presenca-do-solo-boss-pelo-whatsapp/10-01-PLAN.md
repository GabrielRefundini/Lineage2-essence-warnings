---
phase: 10-lista-de-presenca-do-solo-boss-pelo-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/comandos.py
  - l2scanner/config.py
  - l2scanner/__main__.py
  - config.toml
  - tests/test_comandos.py
autonomous: true
requirements: [PRES-03, PRES-04]

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Um telefone declarado em [[membro]] manda `.join` e o pedido atravessa as cinco travas (D-05)"
    - "Esse mesmo telefone e RECUSADO em `.cancelar`, `.solo`, `.party`, `.status`, `.loot-<nick>`, `.loot-`, `.<nick>`, `.corrigir-<nick>`, `.pegou` e `.help` (D-05)"
    - "Um telefone de CHATWOOT_TELEFONES_COMANDO continua alcancando TODOS os comandos, inclusive `.join` e `.leave` — o nivel de membro e aditivo (D-05)"
    - "O nick que acompanha o pedido vem do mapa [[membro]], nunca de `sender.name` do Chatwoot (D-10)"
    - "Dois telefones configurados com os mesmos 8 digitos finais viram aviso alto no arranque, nomeando os dois"
    - "O config.toml do repositorio nao carrega telefone nenhum — so exemplo comentado"
  artifacts:
    - l2scanner/comandos.py
    - l2scanner/config.py
    - config.toml
    - tests/test_comandos.py
  key_links:
    - "config.toml [[membro]] -> config.ler_membros -> LeitorDeComandos.membros -> comandos_novos(membros=) -> autorizado_para"
    - "comandos.telefone_equivalente reusado pelos DOIS niveis — uma implementacao so do corte de 8 digitos"
    - "COMANDOS_DE_MEMBRO derivado do enum Comando: comando novo nasce FORA do alcance de membro"
---

<objective>
Fazer a autorizacao de comando ter DOIS NIVEIS, e nada mais. Hoje
`autor_autorizado` e global e binaria: um telefone na allowlist pode TUDO,
inclusive `.corrigir` e `.pegou`, que reescrevem estatistica permanente em
`.loot/` — pasta que nunca e podada e nao tem backup. Por os telefones de
quatro a oito party-mates nessa lista para eles poderem dar `.join` daria a
todos eles esse poder.

Este plano vem PRIMEIRO e SOZINHO por ordem bloqueante do ROADMAP: e a unica
parte da fase que mexe numa trava de seguranca, e misturar isso com a
funcionalidade de presenca esconderia o risco dentro do recurso. Ao fim deste
plano o `.join` e o `.leave` ja EXISTEM, ja sao reconhecidos pelo caminho real
de leitura e ja tem a autorizacao certa — o que eles FAZEM e o plano 10-03.

Purpose: nenhum party-mate ganha comando destrutivo de carona numa
funcionalidade de presenca.
Output: `[[membro]]` no `config.toml`, `Membro`, `COMANDOS_DE_MEMBRO`,
`nick_do_membro`, `autorizado_para`, `colisoes_de_telefone`, `Comando.JOIN`,
`Comando.LEAVE`, e a visibilidade do nivel novo no arranque.

## Legenda das decisoes (10-CONTEXT.md, na ordem em que aparecem)

| ID | Decisao |
|----|---------|
| D-01 | Um terceiro `TipoDeAviso.CHAMADA`, nao uma lista de antecedencias |
| D-02 | Opt-in por evento (`chamar_minutos_antes`) |
| D-03 | Mecanismo generico, configuracao especifica |
| D-04 | O aviso de 10 minutos continua intocado |
| D-05 | Autorizacao em DOIS NIVEIS |
| D-06 | A descoberta da conversa NAO muda (etiqueta `CP`) |
| D-07 | `.join` fora da janela e aceito; a resposta diz QUAL horario |
| D-08 | `.join` repetido nao repete no grupo |
| D-09 | A confirmacao sai nos DOIS lugares, com redacoes diferentes |
| D-10 | O nick vem do mapa, nunca do WhatsApp |
| D-11 | A lista mora no `.agenda/`, prefixo proprio, `O_CREAT|O_EXCL`, poda de 3 dias |
| D-12 | Fecha no horario do boss, e so fala se alguem joinou |
| D-13 | A lista SUGERE ao loot, nao manda nele |
| D-14 | `.leave` depois do fechamento e recusado |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-CONTEXT.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-PATTERNS.md
</context>

<interface_context>
Direcao de importacao ja existente no pacote, medida e imutavel nesta fase:

    agenda   <- (nada)
    loot     <- agenda
    comandos <- loot
    config   <- agenda, notificador

`comandos.py` NAO importa `config.py`. Por isso `Membro` nasce em
`comandos.py` (o modulo cuja docstring e sobre a superficie de seguranca) e
`config.ler_membros` o importa de la: `config -> comandos -> loot -> agenda`,
sem ciclo. O caminho inverso (`Membro` em `config.py`) faria `comandos`
importar `config` e fecharia o ciclo no primeiro uso.

Assinaturas que este plano cria ou muda:

```
comandos.Membro(nick: str, telefone: str)                     # frozen dataclass
comandos.COMANDOS_DE_MEMBRO: frozenset[Comando]
comandos.nick_do_membro(remetente: dict, membros) -> str | None
comandos.autorizado_para(comando, remetente, telefones, membros) -> bool
comandos.colisoes_de_telefone(telefones, membros) -> list[tuple[str, str]]
comandos.comandos_novos(mensagens, ja_obedecidos, telefones=None,
                        nicks_conhecidos=frozenset(), membros=()) -> list[...]
comandos.MensagemDeComando.nick: str | None = None
comandos.LeitorDeComandos(..., membros=())                     # + self.membros
config.ler_membros(caminho: Path | None = None) -> list[Membro]
```

Toda chamada existente continua valida: os parametros novos tem default vazio,
exatamente como `nicks_conhecidos: frozenset[str] = frozenset()` fez.
</interface_context>

<tasks>

<task type="tracer">
  <name>Tarefa 1: Um telefone de membro atravessa `.join` e para em tudo o mais</name>
  <files>l2scanner/comandos.py, l2scanner/config.py, config.toml, tests/test_comandos.py</files>
  <read_first>
    - `l2scanner/comandos.py` INTEIRO. E o arquivo cuja docstring de topo enumera as travas; o nivel novo e a sexta coisa que ele passa a saber e precisa ser escrito na mesma lingua.
    - `l2scanner/config.py` linhas 108-228 — `ler_agenda` e `_evento_de_dict` sao o analogo literal de leitura e validacao de bloco TOML, com o padrao de mensagem de erro que cita o NOME e nao o indice.
    - `config.toml` inteiro (55 linhas) — o cabecalho "Campos de cada [[evento]]" e o bloco comentado do Solo Boss sao o analogo de documentacao para nao-programador.
    - `tests/test_comandos.py` linhas 140-230 — os dois tripwires da ajuda e o `TELEFONE` de allowlist que eles usam.
    - `.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-PATTERNS.md`, secao `l2scanner/comandos.py`.
  </read_first>
  <action>
Fatia vertical unica, ponta a ponta: um bloco `[[membro]]` num arquivo TOML
vira uma decisao de autorizacao dentro de `comandos_novos`. Sem ramo de
despacho, sem lista de presenca, sem agenda — so o caminho da trava.

Em `l2scanner/comandos.py`:

1. `Membro`, `@dataclass(frozen=True)` com `nick: str` e `telefone: str`,
   mesma forma de `EventoAgendado`. Docstring explicando que o nick vem daqui
   e nunca de `sender.name` do Chatwoot (D-10), com a razao: o nome do contato
   e escrito pelo dono do telefone e o nick do jogo nao e.

2. Dois membros novos no enum `Comando`: `JOIN = "join"` e `LEAVE = "leave"`,
   com comentario dizendo que sao os primeiros comandos alcancaveis por um
   SEGUNDO nivel de autorizacao, e que essa e a razao de o enum precisar de
   `COMANDOS_DE_MEMBRO` logo abaixo.

3. `_VOCABULARIO` ganha quatro formas escritas: `join` e `entrar` para
   `Comando.JOIN`, `leave` e `sair` para `Comando.LEAVE`. Sem argumento, entao
   vocabulario fixo — nao `interpretar_dinamico`. Registrar em comentario o
   preco ja aceito noutros pontos do arquivo: uma palavra no `_VOCABULARIO`
   deixa de poder ser consultada como nick (o ramo `.{nick}` recusa o que esta
   no vocabulario), entao um personagem chamado "Sair" nao e consultavel por
   `.sair` — mesma natureza do preco de `_PALAVRAS_DE_CANCELAMENTO`. Manter a
   lista de apelidos CURTA justamente por isso.

4. `_AJUDA` ganha as duas linhas, numa familia nova chamada `Presenca`,
   inseridas ENTRE a familia `Silencio` e a familia `Loot do Solo Boss` (a
   ordem de insercao do dict e a ordem da resposta). Sintaxes `.join` e
   `.leave`, apelidos `(".entrar",)` e `(".sair",)`. Isto satisfaz o tripwire
   `set(Comando) == set(_AJUDA)` que vai quebrar assim que o enum crescer.

5. `COMANDOS_DE_MEMBRO = frozenset({Comando.JOIN, Comando.LEAVE})`, com
   docstring dizendo que a lista e de INCLUSAO e nao de exclusao de proposito:
   um comando novo no enum nasce FORA do alcance de membro sem ninguem
   precisar lembrar de exclui-lo.

6. `nick_do_membro(remetente, membros) -> str | None` — percorre os membros
   comparando com `telefone_equivalente`, que ja existe e ja resolve o nono
   digito brasileiro. NAO reimplementar o corte de 8 digitos.

7. `autorizado_para(comando, remetente, telefones, membros) -> bool`, e a
   ORDEM aqui e a coisa mais importante do plano inteiro: chama
   `autor_autorizado(remetente, telefones)` PRIMEIRO e devolve True na hora se
   ele aceitar. O nivel de dono e ADITIVO, nunca exclusivo — ele continua
   alcancando todo comando do enum, inclusive `JOIN` e `LEAVE` (D-05). So
   depois disso, se o comando estiver em `COMANDOS_DE_MEMBRO`, consulta
   `nick_do_membro`. Escrever na docstring que inverter essa ordem, ou
   restringir `JOIN` ao nivel de membro, quebra
   `test_toda_sintaxe_anunciada_volta_como_o_comando_certo`, que roda a tabela
   de ajuda inteira com o telefone de dono — e que o teste esta certo.

8. `comandos_novos` ganha `membros=()` com default vazio (mesma tecnica de
   `nicks_conhecidos`). A TRAVA 5 passa a chamar `autorizado_para(comando,
   remetente, ...)` em vez de `autor_autorizado(remetente, ...)`; note que ela
   ja roda DEPOIS da interpretacao, entao o comando ja e conhecido nesse
   ponto. `MensagemDeComando` ganha `nick: str | None = None`, preenchido com
   `nick_do_membro(remetente, membros)` — para o dono que nao esta em
   `[[membro]]` isso e `None`, e quem consome decide o que fazer (plano
   10-03).

9. `LeitorDeComandos.__init__` ganha `membros=()` e guarda `self.membros`
   como lista, ao lado de `self.telefones`.

Em `l2scanner/config.py`:

10. `ler_membros(caminho=None) -> list[Membro]`, copiando a forma de
    `ler_agenda`: arquivo ausente devolve `[]` e nao e erro; TOML mal formado
    levanta `AgendaInvalida`; le `dados.get("membro", [])`. Validar cada bloco
    com o padrao de `onde` de `_evento_de_dict` (citar o nick quando existir,
    senao `[[membro]] #N`): `nick` obrigatorio e casando `NICK_VALIDO`
    importado do `loot`, `telefone` obrigatorio e com pelo menos um digito
    depois de `so_digitos`. Reusar `AgendaInvalida` em vez de criar excecao
    nova, e dizer por que num comentario: ela ja e a excecao de "o config.toml
    nao faz sentido", ja derruba o scanner no arranque com o usuario olhando
    para o console, e uma segunda classe duplicaria esse tratamento sem ganho.
    Comentar a assimetria proposital: `CHATWOOT_TELEFONES_COMANDO` mora no
    `.env` porque acompanha o token; o telefone de um party-mate nao e
    segredo, e o `config.toml` e o arquivo feito para ser aberto e editado a
    mao.

Em `config.toml`:

11. Um bloco `[[membro]]` COMENTADO ao fim do arquivo, com nick e telefone de
    exemplo, e comentario explicando em duas frases o que esse nivel alcanca
    (`.join` e `.leave`) e o que ele nao alcanca. O arquivo versionado nao
    carrega telefone de ninguem — quem preenche e o usuario, na maquina dele.
  </action>
  <behavior>
    - Telefone em `[[membro]]`, texto `.join`: `comandos_novos` devolve um pedido com `comando is Comando.JOIN` e `nick` igual ao nick configurado.
    - Mesmo telefone, texto `.corrigir-Kaus` (com o nick conhecido): devolve lista vazia.
    - Telefone de `CHATWOOT_TELEFONES_COMANDO`, texto `.join`: devolve um pedido `Comando.JOIN`.
    - Telefone desconhecido, texto `.join`: devolve lista vazia.
    - `ler_membros` sobre um TOML com dois `[[membro]]` devolve dois `Membro` na ordem do arquivo.
    - `ler_membros` sobre um arquivo inexistente devolve `[]`.
  </behavior>
  <verify>
    <automated>python -m pytest tests/test_comandos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Comando.JOIN` e `Comando.LEAVE` existem no enum e tem linha em `_AJUDA` — o tripwire `set(Comando) - set(_AJUDA)` passa sem ser afrouxado.
    - `autorizado_para` avalia o nivel de dono ANTES do de membro e devolve True para qualquer comando quando o dono e reconhecido (D-05).
    - Um telefone que so esta em `[[membro]]` produz pedido para `.join`/`.leave` e para NENHUM outro membro do enum.
    - `MensagemDeComando.nick` sai do mapa `[[membro]]`; nenhuma linha nova le `remetente.get("name")` para produzir nick (D-10).
    - `nick_do_membro` chama `telefone_equivalente`; nenhuma segunda implementacao do corte de 8 digitos aparece no arquivo.
    - `comandos_novos(...)` sem o parametro `membros` continua compilando e devolvendo o mesmo de antes — todas as chamadas existentes em `__main__.py` e nos testes seguem intactas.
    - `ler_membros` sobre o `config.toml` do repositorio devolve lista vazia.
  </acceptance_criteria>
  <reversibility rating="costly">
    E uma fronteira de autorizacao num programa que o usuario ja roda em duas
    instancias contra a conta real de Chatwoot dele. Mudar a forma depois de
    party-mates ja terem os telefones no arquivo exige avisar todo mundo e
    reeditar config em outra maquina.
  </reversibility>
  <done>Um telefone de membro atravessa `.join` pelo caminho real de leitura e e recusado em todo o resto da superficie; o telefone de dono continua alcancando tudo.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A fronteira provada nos dois sentidos, com tripwire</name>
  <files>tests/test_comandos.py</files>
  <read_first>
    - `tests/test_comandos.py` linhas 1-60 (os imports e `_FAMILIAS_ESPERADAS`) e 130-260 (a classe do tripwire da ajuda). O `_FAMILIAS_ESPERADAS` da linha 28 e o segundo tripwire que esta fase quebra de proposito.
    - `l2scanner/comandos.py` depois da Tarefa 1.
  </read_first>
  <behavior>
    - Familia nova na ordem: `_FAMILIAS_ESPERADAS` vira `("Vigilancia", "Silencio", "Presenca", "Loot do Solo Boss", "Ajuda")` e `test_as_familias_saem_na_ordem_combinada` volta a passar sem ser afrouxado.
    - Aditividade do dono: para CADA membro de `Comando`, a sintaxe anunciada em `_AJUDA` mandada pelo telefone de dono volta como o comando certo — inclusive `.join` e `.leave`. E o teste que ja existe (`test_toda_sintaxe_anunciada_volta_como_o_comando_certo`); ele nao pode ser alterado para excluir os comandos novos.
    - Recusa derivada do enum: para cada comando em `set(Comando) - COMANDOS_DE_MEMBRO`, a sintaxe anunciada mandada pelo telefone de MEMBRO devolve lista vazia. Derivado do enum, e nao escrito a mao, para que um comando novo nasca coberto.
    - Alcance de membro: para cada comando em `COMANDOS_DE_MEMBRO`, o telefone de membro devolve exatamente um pedido, com o `nick` configurado.
    - Compatibilidade: com `telefones=[]` (allowlist vazia), qualquer telefone continua alcancando qualquer comando, exatamente como antes de existir `[[membro]]`.
    - Telefone que nao esta em lugar nenhum, com allowlist NAO vazia: devolve vazio para todo comando.
    - Nono digito: um membro configurado como `+5544997077001` e um remetente que chega como `554497077001` sao a mesma pessoa.
    - Eco do bot: a mesma confirmacao devolvida como `message_type` de saida nao vira comando nenhum.
  </behavior>
  <action>
Escrever a classe de testes da fronteira em `tests/test_comandos.py`, ao lado
da classe do tripwire da ajuda. Dois telefones distintos como constantes de
classe: o de dono (o `+5544997077000` que os testes existentes ja usam) e um
de membro que NAO colide com ele nos 8 digitos finais.

As duas travas derivadas do enum sao o coracao desta tarefa e precisam de
docstring dizendo POR QUE sao derivadas: escrever a lista de recusa a mao
significaria que o proximo comando destrutivo do projeto nasce alcancavel por
membro e ninguem descobre. A mensagem de falha precisa NOMEAR o comando, como
o tripwire da ajuda ja faz.

Ajustar `_FAMILIAS_ESPERADAS` para a ordem combinada, com comentario dizendo
que `Presenca` vem antes de `Loot do Solo Boss` porque a lista de presenca
acontece antes do loot no ciclo do boss.

Acrescentar tambem o teste de ECO: montar uma mensagem com a redacao de uma
confirmacao de grupo, marcada como saida do bot, e afirmar que
`comandos_novos` devolve vazio. A trava ja existe (TRAVA 3); o teste existe
porque esta fase e a primeira em que o bot passa a escrever no grupo textos
que se parecem com comandos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_comandos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A lista de comandos recusados para o nivel de membro e computada como `set(Comando) - COMANDOS_DE_MEMBRO` dentro do teste, nunca digitada.
    - `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` continua rodando a tabela `_AJUDA` inteira com o telefone de dono e passa sem exclusoes.
    - `_FAMILIAS_ESPERADAS` contem `Presenca` e o teste de ordem passa.
    - Um remetente sem telefone nenhum (`phone_number` ausente) e recusado quando a allowlist nao esta vazia.
    - A suite inteira de `tests/test_comandos.py` passa.
  </acceptance_criteria>
  <done>A fronteira tem prova nos dois sentidos, e a prova cresce sozinha quando o enum crescer.</done>
</task>

<task type="auto">
  <name>Tarefa 3: A colisao de 8 digitos deixa de ser invisivel</name>
  <files>l2scanner/comandos.py, l2scanner/__main__.py, tests/test_comandos.py</files>
  <read_first>
    - `l2scanner/comandos.py` linhas 50-66 — o comentario que documenta o preco da comparacao por 8 digitos e o tamanho de allowlist para o qual ele foi aceito.
    - `l2scanner/__main__.py` linhas 455-510 — `montar_leitor_de_comandos` e o aviso `COMANDOS ABERTOS`, que e o precedente literal de "um estado de permissao nao pode ser descoberto por acidente".
  </read_first>
  <action>
O comentario do `DIGITOS_FINAIS_DO_TELEFONE` aceita a colisao explicitamente
"numa allowlist de duas a cinco pessoas". Esta fase acrescenta de quatro a
oito telefones a superficie comparada, e a colisao deixa de ser teorica. Pior:
ela e uma escalada de privilegio SILENCIOSA — se os 8 digitos finais de um
membro coincidirem com os de um telefone de dono, `autor_autorizado` casa
primeiro e aquele membro passa a alcancar `.corrigir` e `.pegou` sem que nada
no sistema diga uma palavra.

Em `l2scanner/comandos.py`: `colisoes_de_telefone(telefones, membros) ->
list[tuple[str, str]]`, funcao pura que devolve os pares de entradas
configuradas cujos 8 digitos finais sao iguais, comparando dono contra dono,
membro contra membro e dono contra membro. Cada par sai como os dois textos
COMO FORAM CONFIGURADOS, para o usuario achar as duas linhas no arquivo.
Determinismo na ordem dos pares, para o teste poder afirmar. A docstring cita
o comentario do `DIGITOS_FINAIS_DO_TELEFONE` e diz qual dos tres tipos de par
e o perigoso.

Em `l2scanner/__main__.py`, `montar_leitor_de_comandos`: ler `ler_membros()`,
passar ao `LeitorDeComandos`, e logar no arranque quantos membros podem dar
`.join`/`.leave` — pelo mesmo motivo do aviso `COMANDOS ABERTOS` que ja esta
ali. Para cada colisao devolvida, um `log.warning` nomeando as duas entradas e
dizendo a consequencia em uma frase. Nao derrubar o scanner: um aviso alto e
proporcional, e recusar a subir por causa de uma colisao entre dois membros
(que nao escala privilegio nenhum) seria desproporcional. Atualizar a
mensagem existente que ensina a restringir para citar tambem o `[[membro]]`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_comandos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `colisoes_de_telefone` acha colisao dono-contra-membro, membro-contra-membro e dono-contra-dono.
    - Dois telefones que diferem apenas pelo nono digito brasileiro (`+5544997077000` e `554497077000`) sao a MESMA pessoa e nao contam como colisao.
    - Sem `[[membro]]` configurado, `colisoes_de_telefone` devolve lista vazia e o arranque loga exatamente como hoje.
    - `montar_leitor_de_comandos` passa os membros lidos ao `LeitorDeComandos` e o `leitor.membros` reflete o arquivo.
    - `python -m pytest tests/ -q` passa inteiro — nenhum teste existente precisou ser afrouxado. (Linha de base medida em 2026-08-26: `pytest --collect-only` coleta **785** testes. O numero so pode SUBIR nesta fase.)
  </acceptance_criteria>
  <done>Uma colisao de 8 digitos entre um membro e o dono aparece no console no arranque, nomeando as duas entradas.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces — parte do plano 10-01

Simbolos criados aqui. A lista completa da fase esta espalhada pelos cinco
planos; o proximo plano depende so do que esta abaixo.

**`l2scanner/comandos.py`**
| Simbolo | Tipo |
|---|---|
| `Membro` | dataclass frozen (`nick: str`, `telefone: str`) |
| `Comando.JOIN` | membro de enum, valor `"join"` |
| `Comando.LEAVE` | membro de enum, valor `"leave"` |
| `COMANDOS_DE_MEMBRO` | `frozenset[Comando]` |
| `nick_do_membro(remetente, membros)` | funcao -> `str \| None` |
| `autorizado_para(comando, remetente, telefones, membros)` | funcao -> `bool` |
| `colisoes_de_telefone(telefones, membros)` | funcao -> `list[tuple[str, str]]` |
| `MensagemDeComando.nick` | campo novo, `str \| None = None` |
| `comandos_novos(..., membros=())` | parametro novo, default vazio |
| `LeitorDeComandos(..., membros=())` | parametro novo + `self.membros` |
| chaves de `_VOCABULARIO` | `"join"`, `"entrar"`, `"leave"`, `"sair"` |
| entradas de `_AJUDA` | `Comando.JOIN`, `Comando.LEAVE`, familia `"Presenca"` |

**`l2scanner/config.py`**
| Simbolo | Tipo |
|---|---|
| `ler_membros(caminho=None)` | funcao -> `list[Membro]` |

**`config.toml`**
| Simbolo | Tipo |
|---|---|
| `[[membro]]` | bloco de config novo (nick + telefone), no repositorio so como exemplo COMENTADO |

**`tests/test_comandos.py`** — classe nova da fronteira de autorizacao;
`_FAMILIAS_ESPERADAS` cresce com `"Presenca"`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp -> ponte Baileys -> Chatwoot -> `LeitorDeComandos` | Texto e telefone escritos por terceiros atravessam aqui |
| `comandos_novos` -> `autorizado_para` | Onde "quem mandou" vira "pode mandar" |
| `config.toml` -> `ler_membros` | Dado de identidade entra por arquivo editado a mao |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | `comandos.autorizado_para` | high | mitigate | Nivel de dono avaliado primeiro e aditivo; nivel de membro limitado a `COMANDOS_DE_MEMBRO`, e a lista de recusa do teste e derivada de `set(Comando) - COMANDOS_DE_MEMBRO`, entao comando novo nasce fora do alcance de membro (Tarefas 1 e 2) |
| T-10-02 | Spoofing | `comandos.telefone_equivalente` (corte de 8 digitos) | high | mitigate | `colisoes_de_telefone` no arranque, com aviso nomeando as duas entradas configuradas; o par dono-contra-membro e a escalada silenciosa e e citado por nome no aviso (Tarefa 3) |
| T-10-03 | Information Disclosure | `[[membro]]` num `config.toml` versionado | medium | mitigate | O arquivo do repositorio leva so exemplo comentado; criterio de aceite afirma `ler_membros(config.toml do repo) == []` (Tarefa 1) |
| T-10-05 | Spoofing | eco das proprias mensagens do bot voltando como comando | medium | mitigate | TRAVA 3 (`message_type` de entrada) e TRAVA 4 (`comando_<id>`) ja existentes; teste novo manda a redacao de uma confirmacao de grupo de volta como saida do bot e afirma zero comandos (Tarefa 2) |
| T-10-06 | Elevation of Privilege | conversa sem a etiqueta `CP` chegando ao caminho de comando | medium | accept | `LeitorDeComandos._por_etiqueta` ja e a unica fonte alem das conversas fixas e ja falha em silencio devolvendo vazio (ouvir MENOS, nunca mais). D-06: a descoberta nao muda nesta fase |
| T-10-SC | Tampering | instalacao de pacote (npm/pip/cargo) | high | mitigate | Zero dependencia nova: tudo e stdlib mais modulos ja no pacote. Criterio de aceite: nenhum import novo fora de `l2scanner.*` e da stdlib, e nenhuma mudanca em arquivo de dependencia |
</threat_model>

<verification>
1. `python -m pytest tests/ -q` — a suite inteira passa, sem teste afrouxado.
2. `python -c "import l2scanner.__main__"` — o pacote importa (prova de que `config -> comandos` nao fechou ciclo).
3. `git diff --stat` nao toca `pyproject.toml`, `requirements*.txt` nem nenhum arquivo de dependencia.
4. `python -m pytest tests/test_comandos.py -q -k "membro or presenca or colisao"` — os testes novos rodam e passam.
</verification>

<success_criteria>
- PRES-03 e PRES-04 demonstrados por teste automatico, sem jogo aberto e sem rede.
- Nenhum teste existente precisou ser afrouxado; os dois que precisaram MUDAR (`_FAMILIAS_ESPERADAS` e a ordem das familias) cresceram, nao encolheram. Linha de base coletada em 2026-08-26: **785** testes.
- O arranque diz quantos membros existem e grita quando dois telefones colidem.
</success_criteria>

<output>
Create `.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-01-SUMMARY.md` when done
</output>
