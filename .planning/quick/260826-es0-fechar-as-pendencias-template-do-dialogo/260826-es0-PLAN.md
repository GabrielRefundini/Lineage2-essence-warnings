---
phase: quick-260826-es0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cliente.py
  - tests/fixtures/gameplay/faixa_com_inventario.png
  - .planning/WINDOWS.md
  - .planning/todos/pending/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md
  - .planning/todos/completed/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md
  - .planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260826-es0]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O template do dialogo, medido contra gameplay normal REAL, esta travado por teste permanente: `cv2.matchTemplate` sobre `tests/fixtures/gameplay/faixa_com_inventario.png` fica abaixo de 0.70 — o criterio de aceite que a propria pendencia escreveu — e a suite quebra no dia em que o template ou a faixa mudarem para pior (D-01, D-02)."
    - "A fixture guardada e PROVADAMENTE a faixa de busca, nao um recorte qualquer: um tripwire recalcula `FAIXA_DO_DIALOGO` sobre a janela real de 1720x1392 e exige (696, 1032) — as dimensoes exatas do arquivo. Alargar `FAIXA_DO_DIALOGO` sem remedir passa a quebrar a suite (D-02)."
    - "A armadilha da faixa-da-faixa esta documentada NO TESTE, nao so em prosa: `casar_dialogo` recorta a faixa A PARTIR do que recebe, entao entregar a faixa ja recortada mede uma regiao MENOR (0.3586 medido, contra 0.4618 do `matchTemplate` direto). O teste afirma os dois numeros e a relacao entre eles (D-03)."
    - "A divergencia 1 do ledger (`.planning/WINDOWS.md`, `momento_desejado`) foi JULGADA contra o codigo lido, nao carimbada: o veredito cita `l2scanner/loot.py:641` e os testes nominais que prendem a regra, e o ledger sai com `open_count: 0` (D-04)."
    - "A ideia REFUTADA do anel de chrome fora da regiao calibrada esta registrada no corpo do todo do terreno escuro, com os numeros medidos (livre 70.7; coberta 36.4..63.8; separacao pior que a atual, 25 pontos), e o passo 4 da Solution — que propunha exatamente essa ideia — esta marcado como refutado no lugar onde ele e lido (D-05)."
    - "Nenhuma linha de `l2scanner/visao.py` foi tocada: o portao da barra propria continua identico ao que a quick 260826-dxm entregou (D-05)."
    - "A pendencia A saiu de `.planning/todos/pending/` com a medicao escrita no corpo e chegou em `.planning/todos/completed/`, e o `STATE.md` deixou de anuncia-la como pendente (D-06)."
    - "A suite inteira segue verde sem o jogo aberto e sem rede: 780 passando + 2 skipped hoje, 783 + 2 depois deste plano, e nenhum teste existente muda de veredito."
  artifacts:
    - "tests/fixtures/gameplay/faixa_com_inventario.png — 1032x696, a faixa de busca recortada do frame real com inventario E mercado abertos; o negativo mais dificil que o jogo produz naturalmente"
    - "tests/test_cliente.py — `TestOTemplateContraGameplayNormal`: a medicao travada, o tripwire da geometria e a armadilha da faixa-da-faixa"
    - ".planning/WINDOWS.md — entrada 1 fora de `open`, com a razao escrita como raciocinio"
    - ".planning/todos/completed/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md — a pendencia fechada com o numero medido no corpo"
    - ".planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md — a pendencia que CONTINUA aberta, agora com uma saida a menos para tentar"
  key_links:
    - "`FAIXA_DO_DIALOGO` <-> a geometria da fixture — e o unico elo entre 'o numero medido' e 'o numero que o scanner mede em producao'. Sem o tripwire, alargar a faixa deixa a medicao valendo para uma regiao que nao existe mais, e o teste continua verde mentindo"
    - "`casar_dialogo(faixa)` <-> `cv2.matchTemplate(faixa)` — recortar duas vezes mede MENOS, nunca mais (as posicoes candidatas viram um subconjunto). Um teste que passasse a fixture para `casar_dialogo` e chamasse aquilo de 'a medicao' estaria travando um numero otimista por acidente"
    - "`momento_desejado` <-> `test_data_no_FUTURO_ou_IMPOSSIVEL_recusa_e_nao_grava` <-> `test_a_virada_do_ANO` — os dois testes puxam a regra em sentidos OPOSTOS. E o par que decide se a divergencia do ledger e defeito ou acerto; ler so um deles produz o veredito errado"
    - "`.loot/` nunca e podado e `.corrigir-<nick>` so alcanca o registro MAIS RECENTE — e por isso que um registro gravado no ano errado seria permanente e inalcancavel. Este e o custo que o julgamento da entrada 1 tem que pesar"
---

<objective>
Fechar DUAS das tres pendencias abertas do projeto: a medicao do template do
dialogo contra gameplay normal, e a divergencia 1 do ledger de janelas quebradas.
De quebra, registrar uma ideia REFUTADA na terceira pendencia — a que fica aberta
esperando uma gravacao que so o usuario pode produzir.

Purpose: as duas pendencias sao do mesmo tipo — uma afirmacao que o projeto vinha
carregando SEM medicao por tras. A do template dizia "o risco e baixo" por
inferencia ("terreno de jogo com textura nao se parece com uma caixa cinza"); a
do ledger dizia "o teste do futuro venceu" sem que ninguem tivesse voltado ao
codigo para dizer se venceu com razao. A regra do projeto e explicita e vale para
as duas: nada de limiar no chute.

A medicao do template JA FOI FEITA — 10 frames reais da tela do usuario, capturados
hoje, incluindo o caso mais dificil que existe (inventario e mercado abertos, que
sao literalmente caixas cinzas com botoes):

    gameplay normal, n=10 frames de 1720x1392
    score minimo   0.3068
    score maximo   0.4662     (limiar de deteccao = 0.90)
    margem         0.4338     (criterio de aceite da pendencia: < 0.70)

O que falta nao e medir de novo: e TRAVAR o numero com um teste permanente, julgar
a divergencia do ledger com o codigo aberto na frente, e deixar escrito o que ja
foi tentado e falhou.

Output: um teste de regressao com a fixture real, um veredito com evidencia no
ledger, a pendencia A arquivada, e uma saida a menos para quem for atacar a
pendencia do terreno escuro.
</objective>

<decisoes_recebidas>
Este plano nao teve `/gsd-discuss-phase`; as decisoes abaixo vieram travadas no
briefing da tarefa e sao NAO-NEGOCIAVEIS.

- **D-01** — A medicao dos 10 frames e DADO DE ENTRADA. Nao recapturar, nao pedir
  o jogo aberto, nao regravar frames. Os numeros acima entram nos artefatos como
  medicao, nao como estimativa.
- **D-02** — A fixture guardada e a FAIXA recortada (1032x696, 464 KB), nao a
  janela inteira. Recortar a faixa preserva o score exato (0.4618 na faixa,
  identico ao da janela inteira), e a janela custava 3,6 MB contra as fixtures do
  projeto, que ficam entre 76 KB e 192 KB. O recorte tambem cumpre a regra de
  privacidade que a propria pendencia escreveu no passo 4: o chat com nomes e
  mensagens de outros jogadores nao vai para o repositorio.
- **D-03** — `casar_dialogo(pixels, template)` calcula a faixa A PARTIR do frame
  que recebe. Passar a faixa ja recortada faz ele recortar uma faixa DA FAIXA e
  medir outra coisa. O teste chama `cv2.matchTemplate` direto sobre a fixture, e
  a diferenca fica REGISTRADA com o porque.
- **D-04** — A entrada 1 do ledger e uma DIVERGENCIA registrada por outra sessao,
  nao um defeito conhecido. Ler o codigo de verdade antes de julgar. Se o
  comportamento implementado estiver certo, arquivar com razao raciocinada; se
  estiver errado, corrigir com teste ANTES. Arquivar sem ter lido e a decisao mais
  facil de carimbar errado desta tarefa.
- **D-05** — A ideia refutada do anel de chrome entra no CORPO do todo do terreno
  escuro. O codigo do portao (`l2scanner/visao.py`) nao e tocado.
- **D-06** — Fechar a pendencia A significa as tres coisas juntas: teste de
  regressao permanente, numero medido registrado no todo, e o arquivo movido para
  o destino de resolvidos do projeto — que existe e e `.planning/todos/completed/`
  (dir ja criado, hoje vazio; a ferramenta que move e `gsd-tools todo complete`).
- **D-07** — Codigo, comentarios e docstrings em portugues, no estilo do projeto.
  As MEDICOES vao nos comentarios e nos artefatos, nao so no commit.
</decisoes_recebidas>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/todos/pending/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md
@.planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md
@l2scanner/cliente.py
@l2scanner/loot.py
@tests/test_cliente.py
</context>

<interface>
O que ja existe e este plano usa, medido no repositorio de hoje:

    l2scanner/cliente.py:119   carregar_template(caminho=None) -> np.ndarray | None
    l2scanner/cliente.py:140   FAIXA_DO_DIALOGO = (0.20, 0.30, 0.80, 0.80)   # x0, y0, x1, y1
    l2scanner/cliente.py:143   casar_dialogo(pixels, template) -> float | None
    l2scanner/cliente.py:186   estado_do_cliente(..., limiar_do_dialogo=0.90)
    l2scanner/recursos/dialogo_desconexao.png                 320x62 (o template)

    tests/test_cliente.py:32   FIXTURES = Path(__file__).parent / "fixtures" / "cliente"
    tests/test_cliente.py:76   class TestTemplateDoDialogo   (o positivo, 0.9997)

    l2scanner/loot.py:641      momento_desejado(pedido, agora) -> datetime | None
    l2scanner/loot.py:580      interpretar_pegou(argumento) -> PedidoDePegou | None
    tests/test_loot.py:1095    test_data_explicita_alcanca_um_dia_ANTIGO
    tests/test_loot.py:1120    test_data_no_FUTURO_ou_IMPOSSIVEL_recusa_e_nao_grava
    tests/test_loot.py:1135    test_a_virada_do_ANO

Geometria ja conferida (nao precisa redescobrir):

    janela real          1720 x 1392
    faixa de busca       y int(1392*0.30)=417 .. int(1392*0.80)=1113   -> 696
                         x int(1720*0.20)=344 .. int(1720*0.80)=1376   -> 1032
    fixture no disco     (696, 1032, 3)   <- bate exatamente

Comando de teste do projeto (README:384): `python -m pytest tests/ -q`.
`gsd-tools` resolve em `$HOME/.claude/gsd-core/bin/gsd-tools.cjs` (invocar com `node`).
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Travar a medicao do template contra gameplay normal</name>
  <files>tests/test_cliente.py, tests/fixtures/gameplay/faixa_com_inventario.png</files>
  <precondition>`tests/fixtures/gameplay/faixa_com_inventario.png` existe no disco (1032x696) e ainda nao esta versionada — `git status --short` a mostra como nao rastreada. Se o arquivo nao existir, PARE: ele e a medicao inteira desta tarefa e nao pode ser reconstruido sem o jogo aberto.</precondition>
  <behavior>
    - `cv2.matchTemplate(fixture, template, cv2.TM_CCOEFF_NORMED).max()` fica abaixo de 0.70 (criterio de aceite da pendencia) e, medido, vale 0.4618.
    - A geometria da fixture e exatamente `FAIXA_DO_DIALOGO` aplicada a uma janela de 1720x1392: shape (696, 1032).
    - `casar_dialogo(fixture, template)` mede uma regiao MENOR e devolve um valor MENOR ou igual ao do `matchTemplate` direto — medido 0.3586 — e tambem fica abaixo de 0.70.
    - O positivo real de `TestTemplateDoDialogo` continua acima de 0.99: o negativo novo nao pode ter custo do lado que nao pode falhar.
  </behavior>
  <action>
Acrescentar `class TestOTemplateContraGameplayNormal` em `tests/test_cliente.py`,
logo depois de `TestTemplateDoDialogo` (o positivo e o negativo lado a lado), e
versionar a fixture (D-01, D-02, D-03).

Antes de qualquer codigo, ABRIR a fixture e olhar (Read da imagem). Confirmar que
nao ha chat, nomes de outros jogadores nem nada pessoal dentro dela — e a regra
que o passo 4 da propria pendencia impos, e a faixa central existe justamente
porque o chat fica embaixo a esquerda, fora dela. Se houver qualquer coisa
identificavel, PARE e reporte antes de commitar: imagem no git e permanente.

Constante do diretorio ao lado da que ja existe na linha 32:

    FIXTURES_DE_GAMEPLAY = Path(__file__).parent / "fixtures" / "gameplay"

A docstring da classe carrega a MEDICAO (D-07) — ela e o artefato principal desta
task, e sem ela o teste vira um numero magico:

  - o que faltava: o template so tinha sido medido contra um positivo (0.9997) e
    contra a tela de login (0.5051). Nunca contra o frame com o jogo rodando
    normal, que e o frame contra o qual o scanner roda 99% do tempo;
  - o que foi medido em 2026-08-26, n=10 frames de janela inteira 1720x1392:
    minimo 0.3068, maximo 0.4662, limiar 0.90, margem 0.4338;
  - que os 10 incluem o pior caso possivel — inventario E mercado abertos, que
    sao exatamente caixas cinzas com botoes, a coisa na tela que mais se parece
    com o dialogo — e mesmo esse ficou na METADE do limiar;
  - por que so a faixa foi guardada: 464 KB contra 3,6 MB da janela inteira, com
    o score preservado (0.4618 na faixa, identico ao da janela), e o chat fora do
    recorte.

Tres testes:

1. `test_gameplay_normal_com_inventario_aberto_nao_casa` — le a fixture, le
   `carregar_template()`, chama `cv2.matchTemplate(..., cv2.TM_CCOEFF_NORMED)` e
   afirma `.max() < 0.70`, com a mensagem de falha carregando o valor medido
   (`f"casou {pontuacao:.4f}"`). Afirmar tambem a folga ate o limiar de producao:
   `0.90 - pontuacao > 0.40`. O comentario diz por que o `matchTemplate` e
   chamado DIRETO em vez de `casar_dialogo` (D-03): a fixture JA E a faixa, e
   `casar_dialogo` recortaria uma faixa da faixa.

2. `test_a_fixture_e_exatamente_a_faixa_de_busca` — o tripwire. Recalcula a
   geometria a partir de `FAIXA_DO_DIALOGO` e da janela real registrada
   (`JANELA_MEDIDA = (1392, 1720)`, altura x largura) e exige que bata com
   `fixture.shape[:2]`. Escrever a conta no teste, nao os numeros prontos:
   `int(altura * fy1) - int(altura * fy0)` e o par em x. Este teste existe porque
   a medicao do teste 1 so vale enquanto a faixa for esta — alargar
   `FAIXA_DO_DIALOGO` sem remedir deixaria o teste 1 verde medindo uma regiao que
   o scanner nao usa mais.

3. `test_casar_dialogo_sobre_a_faixa_mede_uma_regiao_menor` — a armadilha
   registrada como teste. Afirma `casar_dialogo(fixture, template) <= matchTemplate
   direto` e que ambos ficam abaixo de 0.70, com o comentario explicando o
   motivo estrutural: recortar duas vezes reduz o conjunto de posicoes
   candidatas, entao o maximo so pode cair — a faixa-da-faixa mede MENOS, nunca
   mais, e por isso ela nao serve como medicao (seria um numero otimista por
   acidente). Valores medidos hoje: 0.3586 contra 0.4618.

Depois de verde, PROVAR o tripwire por mutacao, no estilo do projeto: mudar
`FAIXA_DO_DIALOGO` para `(0.10, 0.30, 0.80, 0.80)`, rodar
`python -m pytest tests/test_cliente.py -q` e confirmar que o teste 2 FALHA
dizendo as dimensoes; reverter em seguida e confirmar verde. Registrar o
resultado da mutacao no SUMMARY — um tripwire que nunca falhou nao foi provado.

Versionar a fixture: `.gitignore` ignora `*.png` mas reabre
`!tests/fixtures/**/*.png`, entao `git add tests/fixtures/gameplay/` basta, sem
`-f`. Conferir com `git check-ignore -v` no arquivo antes de commitar.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cliente.py -q</automated>
    <automated>git check-ignore -v tests/fixtures/gameplay/faixa_com_inventario.png; test $? -eq 1</automated>
  </verify>
  <done>Os tres testes novos passam junto com os de `TestTemplateDoDialogo` (o positivo continua >= 0.99); a fixture esta rastreada pelo git; o tripwire foi provado por mutacao e o resultado esta anotado para o SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Julgar a divergencia 1 do ledger com o codigo aberto</name>
  <files>.planning/WINDOWS.md, l2scanner/loot.py, tests/test_loot.py</files>
  <precondition>`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` mostra a entrada 1 com status `open` e `open_count: 1`. Se ela ja estiver resolvida, outra sessao chegou primeiro — reportar e nao mexer.</precondition>
  <action>
A entrada 1 diz:

    quick-260825-pik | deviation | l2scanner/loot.py | status: open
    "momento_desejado: data sem ano escolhe a leitura de calendario mais proxima
     em vez do recuo de ano irrestrito que o plano descrevia — o plano se
     contradizia e o teste do futuro venceu"

Isso veio de OUTRA sessao (o comando `.pegou`), e e uma DIVERGENCIA registrada,
nao um defeito conhecido. LER antes de julgar (D-04). Nao arquivar de cadeira.

Ler, nesta ordem:

  - `l2scanner/loot.py:641` `momento_desejado` inteira, docstring incluida — ela
    ja argumenta a escolha com dois exemplos datados;
  - `l2scanner/loot.py:580` `interpretar_pegou`, so o trecho da data, para ver o
    que chega em `pedido.dia/mes/ano`;
  - os tres testes que prendem a regra: `tests/test_loot.py:1095`, `:1120`
    (`test_data_no_FUTURO_ou_IMPOSSIVEL_recusa_e_nao_grava`) e `:1135`
    (`test_a_virada_do_ANO`);
  - a linha de `.planning/STATE.md` em `## Decisions` que comeca com
    `[Phase 4]: [loot]: Data sem ano resolve para a leitura de calendario MAIS
    PROXIMA` — se a decisao ja foi ratificada no estado do projeto, o ledger esta
    registrando uma divergencia contra um plano que o proprio projeto ja
    substituiu, e isso e parte do veredito.

O criterio do julgamento — e ele nao e "o codigo esta bonito", e a pergunta que
o projeto sempre faz: qual das duas regras pode ESCREVER ESTADO PERMANENTE que o
usuario nao quis?

  - o recuo de ano irrestrito (o que o plano descrevia): em 25/08/2026, `.pegou
    30/12 18:00 Korzis` gravaria 30/12/2025. `.loot/` nunca e podado e
    `.corrigir-<nick>` so alcanca o registro MAIS RECENTE — logo esse registro
    seria permanente e inalcancavel;
  - a leitura de calendario mais proxima (o implementado): a mesma digitacao em
    agosto RECUSA, e em 03/01/2027 o mesmo `30/12` aceita como dezembro de 2026,
    que e a semana em que o comando mais e usado.

Verificar tambem que a recusa nao vira silencio: `responder_atribuicao` devolve
um texto com "ja passou" nos dois casos (futuro e data impossivel).

Entao decidir:

  - **Se o implementado estiver CERTO** — arquivar com
    `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows waive 1 "<razao>"`.
    A razao e o produto desta task: uma linha unica, em portugues, SEM o
    caractere de barra vertical (o ledger e uma tabela markdown), nomeando (a) o
    que o recuo irrestrito teria gravado num exemplo datado, (b) por que aquele
    registro seria permanente e inalcancavel, e (c) os dois testes que prendem os
    dois sentidos da regra. "Ok, esta certo" nao e razao.
  - **Se estiver ERRADO** — teste que falha PRIMEIRO, nomeando o caso concreto que
    o codigo erra; so entao a correcao; depois
    `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 1`. Neste
    caminho, checar antes se algum dos tres testes existentes precisa mudar de
    veredito — se precisar, isso e parte do achado e vai para o SUMMARY, porque
    significa que um teste verde estava prendendo o comportamento errado.

Nos dois caminhos, o veredito e o raciocinio que o sustenta vao para o SUMMARY,
com os numeros de linha lidos.
  </action>
  <verify>
    <automated>python -m pytest tests/test_loot.py -q</automated>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
  </verify>
  <done>A entrada 1 saiu de `open` (por `waive` com razao raciocinada, ou por `fixed` depois de teste + correcao); `windows status` mostra `open_count: 0`; `tests/test_loot.py` verde; o SUMMARY carrega o veredito com as evidencias que o produziram.</done>
</task>

<task type="auto">
  <name>Task 3: Arquivar a pendencia A e registrar a ideia refutada na do terreno escuro</name>
  <files>.planning/todos/pending/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md, .planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md, .planning/STATE.md</files>
  <action>
Tres registros, nenhuma linha de codigo de producao (D-05, D-06, D-07).

**(a) Fechar a pendencia A.** No corpo de
`.planning/todos/pending/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md`,
acrescentar uma secao `## Resolution` no fim, com:

  - a tabela da medicao, no formato que o `## Problem` ja usa, agora com a linha
    que faltava: gameplay normal n=10, minimo 0.3068, maximo 0.4662, limiar 0.90;
  - a nota de que os 10 frames incluem inventario e mercado abertos — o pior
    negativo que o jogo produz — e que mesmo ele ficou na metade do limiar;
  - o criterio de aceite que o proprio todo escreveu (`< 0.70`) e o veredito:
    atendido com folga de 0.2338 no pior frame;
  - a fixture (`tests/fixtures/gameplay/faixa_com_inventario.png`, 1032x696,
    464 KB) e por que so a faixa foi guardada — score preservado (0.4618), 3,6 MB
    a menos, e o chat fora do recorte, cumprindo o passo 4 do proprio todo;
  - os nomes dos tres testes que agora prendem isso, e a nota de que
    `casar_dialogo` NAO e quem mede na fixture, com o motivo em uma linha.

Depois mover com a ferramenta do projeto, nao com `mv`:

    node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" todo complete 2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md

O destino e `.planning/todos/completed/`, que ja existe (hoje vazio) — e a
convencao do proprio `.planning/`, entao nao inventar pasta nova. Se o comando
falhar, mover a mao para `.planning/todos/completed/` e registrar a falha no
SUMMARY.

**(b) Registrar a ideia REFUTADA** em
`.planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md`.
O passo 4 da `## Solution` daquele todo propoe EXATAMENTE a ideia que falhou
("olhar chrome que existe FORA do campo da barra... o caminho e o mesmo que a
party ja usa"). Entao o registro tem que aparecer onde o passo 4 e lido, senao
o proximo leitor tenta de novo. Marcar o passo 4 como refutado no lugar, e
acrescentar o bloco medido logo abaixo dele:

    IDEIA REFUTADA — amostrar o chrome FORA da regiao calibrada, como
    `_bordas_da_barra_intactas` faz para as barras da party.
    Medido num anel de 3 px em volta da regiao `hp_proprio`:
      barra livre    media 70.7
      coberta (n=9)  media 36.4 .. 63.8
    Separa PIOR que o teste atual (73.7 vs 48.9, 25 pontos). Motivo: quando uma
    janela grande do jogo cobre a barra, ela cobre a vizinhanca junto — o anel
    de fora nao e refugio nenhum.

A pendencia CONTINUA aberta e continua major: ela espera uma gravacao em ambiente
escuro que so o usuario pode produzir, e essa gravacao esta sendo tratada em
paralelo. Nao alterar o `## Problem`, nao alterar os passos 1-3 e 5, e nao tocar
em `l2scanner/visao.py` — nem uma linha (D-05).

**(c) `STATE.md`.** Na tabela `## Pending Todos`, trocar a linha da pendencia
fechada pela que segue aberta:

    | major | [A moldura da barra propria em terreno escuro](todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md) | detection |

Ela nunca chegou a ser listada ali, e sair com a tabela vazia anunciaria que o
projeto nao tem pendencia nenhuma — que e o oposto da verdade. Nao mexer em
`## Quick Tasks Completed`: essa linha e do encerramento do `/gsd-quick`
(`gsd-tools quick-tasks-append`), e escrever a mao duplicaria.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-08-24-medir-o-template-do-dialogo-contra-gameplay-normal.md && test "$(ls .planning/todos/pending/ | wc -l)" = "1" && grep -c "IDEIA REFUTADA" .planning/todos/pending/2026-08-26-a-moldura-da-barra-propria-em-terreno-escuro.md && grep -c "terreno-escuro\|terreno escuro" .planning/STATE.md</automated>
    <automated>git diff --stat -- l2scanner/visao.py; test -z "$(git diff --name-only -- l2scanner/visao.py)"</automated>
  </verify>
  <done>A pendencia A esta em `.planning/todos/completed/` com a medicao no corpo; sobra exatamente um arquivo em `pending/`, o do terreno escuro, agora com a ideia refutada registrada ao lado do passo 4 que a propunha; `STATE.md` lista a pendencia que continua aberta; `l2scanner/visao.py` intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela do usuario -> repositorio git | A fixture e um recorte da TELA REAL do usuario. O que entra no git e permanente e publico se o repo for compartilhado. |
| ledger de defeitos -> portao de entrega | `windows_enforce` bloqueia `/gsd-ship` enquanto houver entrada `open`. Arquivar sem evidencia converte um portao de qualidade em carimbo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-es0-01 | Information Disclosure | tests/fixtures/gameplay/faixa_com_inventario.png | medium | mitigate | O recorte e a faixa central (y 0.30-0.80), e o chat do jogo fica embaixo a esquerda, fora dela — a mesma regra que o positivo ja seguiu. Task 1 exige ABRIR e olhar a imagem antes do `git add`, e PARAR se houver nome ou mensagem de terceiro. |
| T-es0-02 | Repudiation | .planning/WINDOWS.md entrada 1 | medium | mitigate | O `waive` so e permitido depois de ler `loot.py:641` e os tres testes nominais, e a razao gravada tem que citar o exemplo datado e os testes. Uma razao vazia deixa o ledger dizendo "alguem decidiu" sem dizer com base em que. |
| T-es0-03 | Tampering | l2scanner/visao.py | low | mitigate | D-05 proibe tocar no portao da barra propria; a verificacao da Task 3 falha se `git diff` mostrar qualquer linha nesse arquivo. |
| T-es0-SC | Tampering | instalacao de pacotes | low | accept | Nenhuma instalacao neste plano — sem `pip install`, sem dependencia nova. A suite roda com o que ja esta no `requirements.txt`. |
</threat_model>

<verification>
1. `python -m pytest tests/ -q` — 783 passando + 2 skipped (780 + 2 hoje, mais os
   tres testes da Task 1). Sem o jogo aberto e sem rede, como toda a suite.
2. `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` — `open_count: 0`.
3. `ls .planning/todos/pending/` — exatamente um arquivo, o do terreno escuro.
4. `ls .planning/todos/completed/` — exatamente um arquivo, a pendencia do template.
5. `git status --short` — a fixture rastreada; `l2scanner/` sem diferenca alguma.
6. O tripwire da geometria foi provado por MUTACAO, e o resultado esta no SUMMARY.
</verification>

<success_criteria>
- O template do dialogo tem, pela primeira vez, um negativo de gameplay REAL
  travado por teste, com o numero medido escrito ao lado.
- Alargar `FAIXA_DO_DIALOGO` sem remedir passa a quebrar a suite.
- A divergencia 1 saiu do ledger com um veredito que cita codigo e testes, nao
  com um carimbo.
- Quem for atacar a pendencia do terreno escuro le, no passo que iria seguir, que
  aquele caminho ja foi medido e falhou — com os numeros.
- Nenhuma linha de codigo de producao mudou, a menos que a Task 2 tenha
  encontrado um defeito de verdade — e nesse caso, com teste antes.
</success_criteria>

<output>
Criar `.planning/quick/260826-es0-fechar-as-pendencias-template-do-dialogo/260826-es0-SUMMARY.md` ao terminar.
</output>
