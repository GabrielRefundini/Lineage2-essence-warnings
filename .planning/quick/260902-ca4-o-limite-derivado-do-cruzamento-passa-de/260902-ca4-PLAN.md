---
phase: quick-260902-ca4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/mercado_leitura.py
  - tools/medir_leitura_de_glifo.py
  - tests/test_mercado_adena.py
  - tests/test_mercado_leitura.py
  - tests/test_medir_leitura_de_glifo.py
  - .planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-DRY-RUN.txt
autonomous: true
requirements: [QUICK-260902-ca4]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O LIMITE ESTAVA PELA METADE, E ISSO ESTA MEDIDO CONTRA UM GABARITO DECLARADO ANTES DA LEITURA. Em 2026-09-02 o CSV foi zerado, as dez linhas da tela foram escritas a mao ANTES de o scanner rodar (`260902-ca4-GABARITO-DA-PROVA-REAL.txt`), a pagina nao mudou durante a leitura (grid diff 8.463 sobre 450.000 pixels) e o leitor acertou 10 de 10 em nome, quantidade e total. Sobre essa MESMA pagina verificada, o fechamento e 7 de 10 com meio centesimo por unidade e 10 de 10 com um centesimo por unidade. Depois desta tarefa `LIMITE_DERIVADO_POR_UNIDADE` vale 1,0."
    - "A TELA TRUNCA, E QUATRO LINHAS DISCRIMINAM ISSO — AS QUATRO. Das dez linhas do gabarito, seis dividem exato e nada dizem; as outras quatro tem unitario diferente sob truncamento e sob arredondamento, e as QUATRO mostram o truncado: 1199/4 = 299,75 exibido 299 (arredondado seria 300), 2200/6 = 366,67 exibido 366 (367), 3100/8 = 387,5 exibido 387 (388) e 3500/9 = 388,89 exibido 388 (389). ZERO linhas arredondam. Isso e o que promove a suspeita escrita na docstring de `limite_derivado_do_cruzamento` de observacao sobre uma fixtura para fato de campo."
    - "AS TRES QUE CAIAM SAO AS TRES QUE A ARITMETICA PREVE, E ELAS ESTAO NOMEADAS NO FONTE: 1199/4 residuo 3 contra limite 2,0; 2200/6 residuo 4 contra 3,0; 3500/9 residuo 8 contra 4,5. Nenhuma e leitura errada — o CSV preservado guarda exatamente esses residuos (`260902-ca4-CSV-DA-PROVA-REAL.csv`, linha 2 com residuo 3). Com 1,0 as tres cabem e o fechamento vai a 10 de 10."
    - "A FRASE QUE CAIU DIZ QUE CAIU. O paragrafo `O QUE FALTA PARA REMEDIR` de `mercado_leitura.py` termina afirmando que o veredito do 02-02 e robusto; a medicao de campo mostra que boa parte da queda era o limite valendo metade, e o fonte passa a dizer isso com data e numero. Os numeros do 02-02 (1273, 0,6525, 0,0164 e a linha `1273.0000 centesimos por unidade`) CONTINUAM no fonte — a refutacao se acrescenta, nunca apaga o registro anterior, e os dois testes que os prendem seguem verdes sem edicao."
    - "A GUARDA NAO AFROUXA POR EFEITO COLATERAL, E ISSO E UMA DECISAO ESCRITA. `FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` cai de 2,0 para 1,0 no mesmo commit da constante, porque o 2x existia justamente para deixar espaco para a hipotese do truncamento — com o truncamento virando a propria derivacao, manter o 2x empilharia a mesma folga duas vezes. O TETO ABSOLUTO fica onde estava: `LIMITE_POR_UNIDADE x FATOR` vale 1,0 centesimo por unidade antes e depois, e a linha de veredito continua dizendo maximo 1.0. A alternativa (manter 2,0, teto virando 2,0) fica recusada por escrito ao lado."
    - "O CUSTO DA DOBRA E MEDIDO E ESTA NO FONTE, NAO SUAVIZADO. Uma troca `0`<->`8` mexe o total em no MINIMO 8 centesimos, entao a guarda da Adena continua pegando enquanto a escala for de ate 7 incrementos, contra ate 15 antes. Preso por teste nos dois lados com numero: `quantidade_de_adena(1000, 144)` (n=7, residuo 8 contra limite 7,0) RECUSA, e `quantidade_de_adena(1000, 126)` (n=8, residuo 8 contra limite 8,0) ACEITA — e com `LIMITE_DERIVADO_POR_UNIDADE` injetado de volta em 0,5 esse mesmo caso era recusado. As ofertas reais da aba sao de 1 a 3 incrementos (5M/10M/15M), entao o caso de campo do usuario continua coberto."
    - "A LINHA 5 DA FIXTURA CONTINUA SENDO RECUSADA — a justificativa da Fase 5 nao evapora com o limite dobrado. `quantidade_de_adena(13588, 6750)` da n=2, residuo 88 contra limite 2,0 e devolve `None`; a taxa `135,88` continua fora do CSV. O caso-bandeira `133,33/66,66` continua aceito, mas agora com folga (residuo 1 contra 2,0) e nao mais por igualdade — e a testemunha do sinal `<=` passa a ser exclusivamente `TestOSinalDaComparacao`, que injeta o limite 1,0 e nao depende da constante. Isso esta dito nas docstrings que afirmavam o contrario."
    - "O PORTAO DE LAYOUT QUE ENTRA NA VARREDURA E O DE PRODUCAO, E ISSO E PROVADO POR CONTAGEM DE CHAMADAS. A ferramenta constroi um `LeitorDePagina` de verdade e chama `_casamento_do_layout`; o teste embrulha esse metodo com um contador e afirma que ele foi chamado uma vez por frame com painel aberto, sobre duas fixturas REAIS (`janela_negociacao_f005.png` e `janela_adena_f014.png`), com os vereditos saindo `negociacao` e `adena`. Nenhum criterio desta tarefa afirma que uma funcao existe."
    - "E A RESPOSTA DO PORTAO E CONSUMIDA, provado pelo controle negativo: com o portao forcado a responder `adena` para TODOS os frames, a populacao do cruzamento vai a ZERO enquanto a populacao de glifo (`resultado.amostras`) fica intacta e maior que zero. Esse par separa `o portao foi chamado` de `o portao decide alguma coisa`, e separa tambem as duas populacoes de proposito."
    - "O ESCOPO DO PORTAO E UMA ESCOLHA COM RAZAO ESCRITA: ele filtra a populacao do CRUZAMENTO e nao a de score/margem. Producao le `Total Price` e `5 mln increment` da aba Adena com os MESMOS retangulos de negociacao (medido no 05-01), entao a populacao de glifo e a mesma nos dois layouts; o que depende do layout e a identidade `total = unitario x quantidade`, que na Adena e normalizada por cinco milhoes. Filtrar a varredura inteira tiraria da medicao do piso glifos que a producao realmente le — recusado por escrito."
    - "A REMEDICAO E RODADA E O NUMERO VAI PARA O FONTE, seja ele qual for. O dry-run das 8 gravacoes do censo fica preservado em `260902-ca4-DRY-RUN.txt`, e o bloco de refutacao passa a carregar o fechamento no limite derivado NOVO, a deteccao, o veredito e a quebra por layout — inclusive se o portao nao tiver removido nenhuma linha, que tambem e resposta e desmontaria a hipotese de contaminacao pela aba Adena."
    - "A GUARDA NAO E LIGADA POR ESTA TAREFA, EM NENHUM DESFECHO. A ferramenta roda SEM `--gravar`, entao `mercado_tolerancia_do_cruzamento` continua `None` no disco por construcao. Se o veredito sair APROVADA, isso e ACHADO entregue ao usuario, e nao permissao para ligar: uma guarda que passa a descartar dado exige a decisao dele. Se sair REPROVADA, o relatorio diz QUAL criterio caiu com o numero."
    - "NADA DE MAQUINA E TOCADO. `calibration.json` sai com sha256 comecando em `c41e665b8eac1669`, identico ao do inicio, e `.mercado/observacoes.csv` sai com sha256 comecando em `d107dbc1ab462ba3` — o CSV da prova preservada, byte a byte. `VERSAO_DO_ESQUEMA` continua em 2 (nenhuma chave nova de calibracao nasce aqui: a constante e o fator vivem no fonte) e nenhuma chave de calibracao muda de valor. Os testes novos usam `tmp_path` e fixturas versionadas, e `recordings/` so e lido pela logica de 8 pastas nomeadas que a ferramenta ja tinha."
    - "A suite nao regride: `python -m pytest -q --ignore=tests/test_agenda.py` sai com codigo 0 e com `passed` ACIMA do baseline de 4698 passed, 2 skipped. Roda no python GLOBAL; a ferramenta roda no `.venv/Scripts/python.exe`. `tests/test_agenda.py:1231` levanta `KeyboardInterrupt` de proposito e e area de outro agente."
    - "Zero dependencia nova e nenhuma biblioteca de sintese de input. `l2scanner/rastreador.py`, `l2scanner/visao.py` e os arquivos do agente paralelo (`respawn.py`, `bosses.py`, `agenda.py`, `sessao.py`, `test_bosses.py`, `identidade.py`, `discord/`, `dashboard.py`) ficam fora do diff."
  artifacts:
    - "l2scanner/mercado_leitura.py — `LIMITE_DERIVADO_POR_UNIDADE = 1.0` com a medicao de campo de 2026-09-02 escrita ao lado, o bloco de refutacao do 02-02 com a frase final marcada como refutada e com a remedicao, e as docstrings de `limite_derivado_do_cruzamento` e `quantidade_de_adena` recontadas para o truncamento"
    - "tools/medir_leitura_de_glifo.py — `FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO = 1.0` com a decisao escrita, o `LeitorDePagina` de producao construido na varredura, `layout_por_frame`, o filtro de layout dentro de `linhas_do_cruzamento` e a quebra por layout no relatorio"
    - "tests/test_mercado_adena.py — as contas explicitas recomputadas com o motivo, e a classe nova que MEDE a fronteira de deteccao da dobra (n=7 pega, n=8 escapa, e o controle com 0,5 injetado)"
    - "tests/test_mercado_leitura.py e tests/test_medir_leitura_de_glifo.py — as expectativas do limite derivado atualizadas COM A RAZAO em cada docstring"
    - "tests/test_medir_leitura_de_glifo.py — a classe que prova por CONTAGEM DE CHAMADAS que o portao de layout da PRODUCAO foi chamado, com o controle negativo que zera a populacao do cruzamento"
    - ".planning/quick/260902-ca4-.../260902-ca4-DRY-RUN.txt — a saida integral do dry-run sobre as 8 gravacoes do censo"
  key_links:
    - "`LIMITE_DERIVADO_POR_UNIDADE` -> `limite_derivado_do_cruzamento` -> `quantidade_de_adena` (GUARDA, descarta linha) e -> `_observar_o_cruzamento` (OBSERVACAO, so loga): o mesmo numero decide descarte na Adena e limiar de log na negociacao"
    - "`LIMITE_DERIVADO_POR_UNIDADE` (producao) -> `LIMITE_POR_UNIDADE` (apelido na ferramenta) -> `LIMITE_POR_UNIDADE x FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` = o teto da tolerancia proposta: mexer no primeiro sem decidir o segundo move o teto sem ninguem decidir"
    - "`LeitorDePagina._casamento_do_layout` (producao, `mercado_pagina.py:892`) -> `varrer` -> `linhas_do_cruzamento` -> `veredito_da_guarda`: o veredito passa a ser julgado so sobre a negociacao"
    - "`tests/fixtures/mercado/calibracao_de_fixture.json` carrega `mercado_layouts` com o bloco `adena` e o `mercado_cabecalho_de_coluna` da negociacao — sem esses dois o portao devolveria `None` para tudo e o teste do portao mediria vacuo"
---

<objective>
O limite derivado do cruzamento passa de meio centesimo por unidade para um centesimo,
porque a tela do jogo TRUNCA o unitario em vez de arredondar — medido em campo em
2026-09-02 contra um gabarito declarado antes da leitura. Junto com ele, o portao de
layout de PRODUCAO entra na varredura que julga a guarda, e a ferramenta e rodada de novo
em dry-run para dizer, com numero, o que sobra da reprovacao do 02-02.

Purpose: a guarda de cruzamento e a UNICA conferencia que vem de outro lugar da tela, e ela
foi reprovada em cima de um limite que valia metade do que a aritmetica da tela permite.
Enquanto o limite estiver errado, toda remedicao futura comeca torta — e a Adena, onde o
cruzamento JA e guarda e descarta linha, esta descartando com a regua errada.

Output: a constante corrigida com a medicao escrita ao lado, a frase refutada dizendo que
caiu, o custo da dobra medido e preso em teste, o portao de layout chamado (e nao copiado)
dentro da ferramenta, e o veredito do dry-run preservado. A guarda continua DESLIGADA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/workstreams/mercado/STATE.md
@.planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-GABARITO-DA-PROVA-REAL.txt
@.planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-CSV-DA-PROVA-REAL.csv

A PROVA LIMPA DE 2026-09-02, ja feita e preservada nos dois arquivos acima:
o vigia foi parado, `.mercado/observacoes.csv` foi truncado no cabecalho, as dez linhas da
tela foram escritas a mao ANTES de o scanner rodar, a pagina nao mudou durante a leitura
(grid diff 8.463 sobre um recorte de 450.000 pixels) e o resultado foi 10 de 10 exatos em
nome, quantidade e total, com o residuo guardado batendo com a conta feita a partir do
unitario da propria tela. O leitor esta CERTO nessa pagina; o que esta errado e o limite.

A CONTA, sobre essa mesma pagina verificada (as dez linhas do gabarito):

    q=8  t=2000 u=250  residuo 0   lim 0,5 -> 4,0   ok      lim 1,0 -> 8,0   ok
    q=4  t=1199 u=299  residuo 3   lim 0,5 -> 2,0   FALHA   lim 1,0 -> 4,0   ok
    q=6  t=1800 u=300  residuo 0   lim 0,5 -> 3,0   ok      lim 1,0 -> 6,0   ok
    q=3  t=1000 u=333  residuo 1   lim 0,5 -> 1,5   ok      lim 1,0 -> 3,0   ok
    q=4  t=1360 u=340  residuo 0   lim 0,5 -> 2,0   ok      lim 1,0 -> 4,0   ok
    q=3  t=1050 u=350  residuo 0   lim 0,5 -> 1,5   ok      lim 1,0 -> 3,0   ok
    q=6  t=2200 u=366  residuo 4   lim 0,5 -> 3,0   FALHA   lim 1,0 -> 6,0   ok
    q=8  t=3100 u=387  residuo 4   lim 0,5 -> 4,0   ok      lim 1,0 -> 8,0   ok
    q=9  t=3500 u=388  residuo 8   lim 0,5 -> 4,5   FALHA   lim 1,0 -> 9,0   ok
    q=5  t=1980 u=396  residuo 0   lim 0,5 -> 2,5   ok      lim 1,0 -> 5,0   ok

    fechamento a 0,5: 7 de 10 (70%)      fechamento a 1,0: 10 de 10 (100%)

AS QUATRO LINHAS QUE DISCRIMINAM truncamento de arredondamento sao a 2, a 7, a 8 e a 9, e
as quatro truncam: 299,75 exibido 299 (arredondado daria 300), 366,67 exibido 366 (367),
387,5 exibido 387 (388) e 388,89 exibido 388 (389). Nenhuma arredonda. As outras seis
dividem exato e nao opinam. O usuario tambem leu na tela, na mesma sessao, 6,74/5 = 1,348
exibido 1,34 e 15,00/8 = 1,875 exibido 1,87 — o mesmo sentido, de outra pagina.

@l2scanner/mercado_leitura.py
@tools/medir_leitura_de_glifo.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: a derivacao vira 1,0 — a constante, a frase refutada, o teto que NAO se move e o custo medido</name>
  <files>l2scanner/mercado_leitura.py, tools/medir_leitura_de_glifo.py, tests/test_mercado_adena.py, tests/test_mercado_leitura.py, tests/test_medir_leitura_de_glifo.py</files>
  <read_first>
    l2scanner/mercado_leitura.py linhas 1169-1250 (o bloco de refutacao do 02-02, a
    constante e a docstring da derivacao), 1330-1385 (o criterio da Adena e o paragrafo do
    sinal `<=`), 2212-2250 (`_observar_o_cruzamento`); tools/medir_leitura_de_glifo.py
    linhas 130-160 (os criterios) e 900-930 (a linha do relatorio que escreve o limite);
    tests/test_mercado_adena.py linhas 1-52 e 213-345; tests/test_mercado_leitura.py
    linhas 1688-1706 e 1808-1820 e 1948-1965; tests/test_medir_leitura_de_glifo.py
    linhas 289-364. `l2scanner/ocr.py:34-52` e o padrao de como este projeto escreve um
    numero que caiu.
  </read_first>
  <behavior>
    - `limite_derivado_do_cruzamento(1)` vale 1,0; `(10)` vale 10; `(48)` vale 48.
    - As dez linhas do gabarito de 2026-09-02 fecham TODAS contra o limite novo, e tres
      delas (1199/4, 2200/6, 3500/9) NAO fechavam contra o antigo.
    - `quantidade_de_adena(13588, 6750)` continua devolvendo `None`: residuo 88 contra
      limite 2,0. A justificativa da Fase 5 sobrevive a dobra.
    - `quantidade_de_adena(13333, 6666)` continua devolvendo `(10_000_000, 2)`, agora com
      folga (residuo 1 contra 2,0) em vez de por igualdade.
    - A FRONTEIRA DE DETECCAO, nos dois lados: `quantidade_de_adena(1000, 144)` da n=7,
      residuo 8 contra limite 7,0 e RECUSA; `quantidade_de_adena(1000, 126)` da n=8,
      residuo 8 contra limite 8,0 e ACEITA — a troca escapa. Com
      `LIMITE_DERIVADO_POR_UNIDADE` injetado de volta em 0,5, esse mesmo n=8 era recusado
      (limite 4,0). E o custo exato da dobra, medido.
    - `LIMITE_POR_UNIDADE x FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` na ferramenta vale 1,0
      antes e depois da mudanca: o teto absoluto da tolerancia proposta NAO se move.
    - `veredito_da_guarda` continua aprovando o conjunto de cinco linhas do teste que ja
      aprovava, e continua reprovando por tolerancia o conjunto de `100,00` por 10 com
      unitario `5,00`.
  </behavior>
  <action>
    Em `l2scanner/mercado_leitura.py`, trocar `LIMITE_DERIVADO_POR_UNIDADE` de meio para um
    centesimo por unidade e reescrever o comentario de uma linha que fica acima dele: a
    derivacao agora sai do TRUNCAMENTO e nao do arredondamento, e o comentario tem de
    carregar a procedencia (prova de campo de 2026-09-02, gabarito declarado antes da
    leitura, 10 de 10 exatos, pagina imovel com grid diff 8.463 sobre 450.000 pixels).

    Reescrever a docstring de `limite_derivado_do_cruzamento` inteira, no tom que ela ja
    tem. Ela precisa dizer: que o unitario exibido e `total / quantidade` TRUNCADO a duas
    casas, que cada unidade carrega ate um centesimo inteiro de erro e que `quantidade`
    unidades carregam `quantidade`; recomputar o exemplo do spike (`40,00` por 48 unidades
    exibe `0,83` porque 0,8333 truncado da 0,83, o residuo continua sendo 16 e o limite
    derivado passa de 24 para 48); e transformar o paragrafo que hoje chama o truncamento
    de SUSPEITO em paragrafo CONFIRMADO, mantendo a fixtura `janela_negociacao_f005.png`
    linha 5 como o primeiro indicio e acrescentando as QUATRO linhas do gabarito que
    discriminam, com as quatro contas (299,75 exibido 299; 366,67 exibido 366; 387,5
    exibido 387; 388,89 exibido 388) e a frase de que nenhuma linha da pagina arredonda.
    Manter a distincao que ja esta la entre REFERENCIA e PENEIRA.

    No bloco de comentario da guarda (o que comeca em `POR QUE ELA EXISTE`), NAO apagar
    nada: os numeros do 02-02 sao registro e dois testes os prendem no fonte. Acrescentar
    ao final do paragrafo `O QUE FALTA PARA REMEDIR` uma marcacao datada de 2026-09-02
    dizendo que a afirmacao final daquele paragrafo — a de que o veredito e robusto — esta
    REFUTADA POR MEDICAO, com os numeros: sobre a pagina verificada o fechamento e 7 de 10
    com o limite antigo e 10 de 10 com o novo, e a ferramenta re-rodada hoje devolve 0,6525
    no limite antigo sobre o censo, ou seja os dois lados concordam e o que o 02-02 leu como
    populacao cheia de leitura errada era, em boa parte, o limite valendo metade. Deixar
    escrito que a remedicao com o portao de layout entra na Tarefa 3 deste mesmo plano.
    Corrigir tambem, no mesmo bloco, a linha da tabela que descreve o limite derivado como
    meio por unidade, e a frase que chama 1273 de 2.546 vezes o limite derivado (com 1,0 o
    fator vira 1.273 vezes) — um numero derivado que continua escrito errado e uma segunda
    verdade.

    Ajustar as duas docstrings que repetem a derivacao antiga: em `quantidade_de_adena`, o
    paragrafo (c) que diz que cada incremento carrega no maximo meio centesimo passa a
    dizer um centesimo, e o paragrafo do sinal `<=` passa a dizer que o caso-bandeira
    `133,33 / 66,66` deixou de ser caso de igualdade (residuo 1 contra limite 2,0, agora com
    folga) e que a testemunha viva do sinal passa a ser `TestOSinalDaComparacao`, que injeta
    o limite e nao depende da constante; em `_observar_o_cruzamento` e no texto da
    `TravaDaObservacao`, conferir se alguma frase afirma meio centesimo e corrigir.

    ESCREVER O CUSTO DA DOBRA AO LADO DA CONSTANTE, com numero e sem suavizar: uma troca
    `0`<->`8` mexe o total em no minimo 8 centesimos (o digito na ultima casa), entao a
    guarda so a pega enquanto o limite for menor que 8 — ou seja ate 7 unidades ou
    incrementos, contra ate 15 antes. Dizer que na Adena as ofertas reais sao de 1 a 3
    incrementos (5M/10M/15M), entao o caso de campo continua coberto, e que na negociacao a
    guarda esta desligada e o efeito e so no limiar do log de observacao. Um limite mais
    correto que pega menos e uma troca, e ela tem de estar escrita.

    Em `tools/medir_leitura_de_glifo.py`, DECIDIR E ESCREVER O EFEITO COLATERAL: baixar
    `FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` de 2,0 para 1,0 e reescrever o comentario de tres
    linhas que fica acima dele. A razao a escrever: o 2x existia para deixar espaco para a
    possibilidade do truncamento, e com o truncamento virando a propria derivacao manter o
    2x empilharia a mesma folga duas vezes e afrouxaria a guarda como efeito colateral de
    um conserto, que e exatamente o modo de falha que este projeto existe para evitar.
    Registrar ao lado que o TETO ABSOLUTO nao se move — o produto continua valendo 1,0
    centesimo por unidade — e que a alternativa de manter 2,0 foi RECUSADA porque levaria o
    teto a 2,0 sem ninguem ter decidido isso. Corrigir tambem a linha do relatorio que
    escreve o limite derivado como 0,5 por unidade para que ela derive de `LIMITE_POR_UNIDADE`
    em vez de repetir o numero a mao, e a frase da docstring do modulo que descreve a
    derivacao como meio centesimo por unidade.

    Atualizar as expectativas que caem, CADA UMA COM A RAZAO NA DOCSTRING, nunca apagando
    teste para ficar verde: em `tests/test_medir_leitura_de_glifo.py`, a classe
    `TestOLimiteDerivadoDoCruzamento` (o spike vira limite 48; o crescimento vira 10 e 1,0;
    a docstring da classe passa a falar de truncamento) e a docstring do teste da tolerancia
    que fala em caber em 2x o limite; em `tests/test_mercado_leitura.py`, o comentario do
    bloco do spike e a assercao de limite 24 que vira 48, e a docstring de
    `tolerancia_de_ensaio` que descreve a derivacao; em `tests/test_mercado_adena.py`, a
    tabela do docstring do modulo, o docstring da classe `TestOsDoisCasosDificeis`, as duas
    contas explicitas que afirmam limite 1,0 para n=2 (viram 2,0) e as duas docstrings que
    dizem que o caso bom passa raspando — ele passa com folga agora, e dizer o contrario
    seria deixar no teste uma afirmacao que a constante desmentiu.

    Acrescentar em `tests/test_mercado_adena.py` uma classe nova que MEDE a fronteira de
    deteccao descrita no bloco `behavior`, com a aritmetica escrita na docstring e com o
    controle que injeta `LIMITE_DERIVADO_POR_UNIDADE` de volta em 0,5 para mostrar o que a
    dobra custou. Acrescentar em `tests/test_medir_leitura_de_glifo.py` um teste que afirma
    o TETO ABSOLUTO da ferramenta pelo produto dos dois fatores, e nao pelo valor de
    qualquer um deles isolado — e o teto que nao pode se mover, e afirmar so o fator deixaria
    a proxima mudanca de constante move-lo em silencio.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mercado_adena.py tests/test_mercado_leitura.py tests/test_medir_leitura_de_glifo.py -q</automated>
    <automated>python -c "import l2scanner.mercado_leitura as m; print([m.limite_derivado_do_cruzamento(n) for n in (1,2,4,6,9,48)])"</automated>
    <automated>python -c "
import l2scanner.mercado_leitura as m
g=[(8,2000,250),(4,1199,299),(6,1800,300),(3,1000,333),(4,1360,340),(3,1050,350),(6,2200,366),(8,3100,387),(9,3500,388),(5,1980,396)]
ok=sum(1 for q,t,u in g if m.residuo_do_cruzamento(t,u,q)<=m.limite_derivado_do_cruzamento(q))
print('fechamento do gabarito:',ok,'de',len(g)); assert ok==10"</automated>
    <mutation-proof>
      OBRIGATORIO, e o resultado vai para o SUMMARY. Depois de verde: injetar
      `LIMITE_DERIVADO_POR_UNIDADE = 0.5` de volta, rodar
      `python -m pytest tests/test_mercado_adena.py -q` e CONFIRMAR que a classe nova da
      fronteira de deteccao e as contas explicitas FALHAM. Copiar a linha de resumo do
      pytest e a linha do assert que quebrou. Desfazer e rodar de novo. Um teste que ninguem
      viu falhando nao foi visto testando nada.
    </mutation-proof>
  </verify>
  <done>
    `limite_derivado_do_cruzamento` devolve 1,0 para uma unidade e 48 para 48; as dez linhas
    do gabarito fecham; `quantidade_de_adena(13588, 6750)` continua `None` e
    `quantidade_de_adena(13333, 6666)` continua `(10_000_000, 2)`; a fronteira de deteccao
    esta presa nos dois lados com o controle de 0,5 injetado; o produto
    `LIMITE_POR_UNIDADE x FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` vale 1,0; a frase refutada
    do bloco do 02-02 diz que caiu, com data e numero, e os numeros 1273 / 0,6525 / 0,0164
    continuam no fonte com os dois testes que os prendem verdes. A mutacao foi aplicada, a
    falha foi medida e copiada, e a mutacao foi desfeita.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: o portao de layout da PRODUCAO entra na varredura — chamado, nunca copiado</name>
  <files>tools/medir_leitura_de_glifo.py, tests/test_medir_leitura_de_glifo.py</files>
  <read_first>
    l2scanner/mercado_pagina.py linhas 445-470 (a assinatura de `LeitorDePagina`), 552-602
    (a montagem de `self._layouts`) e 892-955 (`_casamento_do_layout`, com a tabela medida
    no 05-02); tools/medir_leitura_de_glifo.py linhas 395-500 (`varrer`) e 500-540
    (`linhas_do_cruzamento`) e 895-930 (o paragrafo do relatorio que hoje diz que o portao
    ainda nao existe); tests/test_medir_leitura_de_glifo.py linhas 40-100 (como a ferramenta
    e carregada) e 372-400 (a guarda de que ferramenta nao escreve em recordings);
    tests/test_mercado_adena.py linhas 120-160 (como um `RastreioDoPainel` e montado sobre
    `calibracao_de_fixture.json`).
  </read_first>
  <behavior>
    - Sobre uma gravacao sintetica em `tmp_path` com dois frames REAIS — o primeiro copiado
      de `tests/fixtures/mercado/janela_negociacao_f005.png` e o segundo de
      `janela_adena_f014.png` — `varrer` chama `LeitorDePagina._casamento_do_layout`
      exatamente uma vez por frame em que o painel abriu, e esse numero e maior que zero.
    - Os vereditos gravados sao `negociacao` para o primeiro frame e `adena` para o segundo.
    - Com o portao real, `linhas_do_cruzamento` devolve linhas completas do frame de
      negociacao, e mais que zero.
    - CONTROLE NEGATIVO: com o portao forcado a responder `adena` para todo frame, as linhas
      completas do cruzamento vao a ZERO e `resultado.amostras` continua igual ao do caso
      real e maior que zero — a populacao de glifo NAO e filtrada pelo layout, de proposito.
    - Se nenhum frame vencer como `negociacao`, o relatorio diz isso alto e o veredito nao
      se confunde com uma guarda reprovada.
  </behavior>
  <action>
    Importar `LeitorDePagina` de `l2scanner.mercado_pagina` no topo de
    `tools/medir_leitura_de_glifo.py`, junto dos outros imports de producao, e escrever ao
    lado — no tom do comentario que ja explica por que `limite_derivado_do_cruzamento` foi
    promovida — por que o portao e CHAMADO e nao reimplementado: uma copia do portao mede
    outra coisa que o que a producao decide, e uma copia medida no lugar do original e
    exatamente o defeito que o DEBT-07 fechou.

    Em `varrer`, construir UM `LeitorDePagina` por gravacao, com o `RastreioDoPainel` daquela
    gravacao, catalogo vazio e as duas leitoras de texto como `None` — a varredura nunca
    chama OCR, e `_casamento_do_layout` so olha a banda do cabecalho. Para cada frame em que
    o painel abriu, chamar `_casamento_do_layout` com o frame e a origem que a varredura ja
    tem, e guardar a resposta em um campo novo `layout_por_frame` do
    `ResultadoDaVarredura`, indexado por gravacao e nome de arquivo, mais um contador
    `abertos_por_layout` que conta tambem a resposta `None` (nenhum layout casou ou houve
    empate).

    Em `linhas_do_cruzamento`, descartar as celulas cujo frame nao venceu como `negociacao`,
    e contabilizar quantas linhas COMPLETAS foram tiradas por layout, para o relatorio poder
    dizer o tamanho do efeito. Escrever no fonte, com a razao, que o portao filtra a
    populacao do CRUZAMENTO e NAO a de score/margem: producao le `Total Price` e
    `5 mln increment` da aba Adena com os MESMOS retangulos de negociacao (medido no 05-01),
    entao a populacao de glifo e a mesma nos dois layouts, e o que depende do layout e a
    identidade `total = unitario x quantidade`, que na Adena e normalizada por cinco milhoes
    de adena. Registrar a alternativa RECUSADA — filtrar a varredura inteira — com o custo:
    tiraria da medicao do piso e da margem glifos que a producao realmente le.

    No relatorio, substituir o paragrafo que hoje avisa que o portao de layout ainda nao
    existe pela quebra MEDIDA: frames com painel aberto por layout, linhas completas por
    layout, e quantas linhas o portao tirou da populacao do cruzamento. Se nenhum frame
    vencer como negociacao, imprimir uma linha alta explicando que a medicao ficou sem
    populacao e que as causas provaveis sao calibracao sem molde de cabecalho ou sem o bloco
    de layouts — sem isso, uma calibracao torta sairia do relatorio parecendo guarda
    reprovada.

    Escrever os testes do bloco `behavior` em `tests/test_medir_leitura_de_glifo.py`, numa
    classe propria. Eles montam a gravacao sintetica em `tmp_path`, apontam
    `GRAVACOES_DO_CENSO` da ferramenta para essa unica pasta via monkeypatch, carregam
    `calibracao_de_fixture.json` e embrulham `LeitorDePagina._casamento_do_layout` com um
    contador que delega ao metodo original. O controle negativo troca o metodo por um que
    responde sempre `adena`. Se alguma das duas fixturas nao abrir o painel sob esse
    rastreio, PARAR e relatar o numero medido em vez de afrouxar o teste — um teste que mede
    zero frames aberto passaria por acidente.

    Nao acrescentar escrita nenhuma na ferramenta: a guarda `TestAsFerramentasNAO_ESCREVEM_EM_RECORDINGS`
    tem de continuar verde sem edicao, inclusive a varredura linha a linha que ela faz sobre
    as mencoes a recordings no fonte.
  </action>
  <verify>
    <automated>python -m pytest tests/test_medir_leitura_de_glifo.py -q</automated>
    <automated>python -m pytest tests/test_mercado_pagina.py -q</automated>
    <mutation-proof>
      OBRIGATORIO, e o resultado vai para o SUMMARY. Depois de verde: remover do
      `linhas_do_cruzamento` o descarte por layout (so ele), rodar
      `python -m pytest tests/test_medir_leitura_de_glifo.py -q` e CONFIRMAR que o controle
      negativo (portao forcado a `adena`) FALHA. Em seguida, desfazer isso e trocar a chamada
      de `_casamento_do_layout` por uma constante local `"negociacao"`, e CONFIRMAR que o
      teste de contagem de chamadas FALHA. Copiar as duas linhas de resumo do pytest e os
      dois asserts que quebraram. Desfazer as duas mutacoes e rodar de novo.
    </mutation-proof>
  </verify>
  <done>
    O contador de chamadas de `LeitorDePagina._casamento_do_layout` sai igual ao numero de
    frames com painel aberto e maior que zero, com vereditos `negociacao` e `adena` sobre as
    duas fixturas reais; com o portao forcado a `adena` a populacao do cruzamento vai a zero
    e a de glifo nao muda; o relatorio imprime a quebra por layout. As duas mutacoes foram
    aplicadas, as duas falhas foram medidas e copiadas, e as duas foram desfeitas.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: o dry-run sobre o censo, o veredito escrito no fonte, e a guarda que continua desligada</name>
  <files>tools/medir_leitura_de_glifo.py, l2scanner/mercado_leitura.py, .planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-DRY-RUN.txt</files>
  <precondition>
    `.venv/Scripts/python.exe` existe e as 8 gravacoes NOMEADAS do censo estao em
    `recordings/` no checkout principal (a ferramenta sai com codigo 3 se faltar qualquer
    uma). Conferir antes de rodar; se faltar, PARAR e relatar — medir sobre um conjunto
    diferente do censo produz um numero que nao se compara com nenhum outro deste projeto.
  </precondition>
  <action>
    Rodar a ferramenta com `.venv/Scripts/python.exe tools/medir_leitura_de_glifo.py` SEM
    `--gravar`, jamais com ele, e preservar a saida INTEGRAL em
    `.planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-DRY-RUN.txt`.
    Antes de rodar, anotar o sha256 de `calibration.json` e o de `.mercado/observacoes.csv`;
    depois de rodar, conferir que os dois nao mudaram.

    Ler do relatorio: a linha do veredito, o fechamento no limite derivado NOVO, a tolerancia
    proposta, a deteccao, a quebra por layout (quantas linhas o portao tirou) e o par
    (piso, margem) que a ferramenta PROPORIA. Registrar tudo isso no bloco de refutacao de
    `l2scanner/mercado_leitura.py`, embaixo da marcacao escrita na Tarefa 1, como a
    REMEDICAO de 2026-09-02: os numeros como sairam, e a conclusao que eles sustentam sobre
    a hipotese de contaminacao pela aba Adena — inclusive se o portao tiver tirado ZERO
    linhas, porque isso tambem e resposta e desmontaria a hipotese.

    NAO GRAVAR NADA no `calibration.json`, em nenhum desfecho.
    `mercado_tolerancia_do_cruzamento` continua `None` no disco por construcao, porque a
    ferramenta so escreve com `--gravar`. Se o veredito sair REPROVADA, escrever QUAL
    criterio caiu com o numero. Se sair APROVADA — o que exigiria fechamento de pelo menos
    0,99 E deteccao de pelo menos 0,90 —, isso e ACHADO para o usuario decidir, e o texto no
    fonte tem de dizer que a guarda segue desligada aguardando essa decisao, nunca que ela
    foi ligada.

    O par (piso, margem) proposto vai mudar, porque a populacao de linhas confirmadas muda
    com o limite e com o portao. Ele e apenas RELATADO, nunca gravado, e o par que vive no
    `calibration.json` (0,469831 e 0,036984) fica intocado. Se o piso proposto ficar ABAIXO
    do gravado, dizer isso no registro com os dois numeros — e um afrouxamento potencial e
    tem de ser visivel; se ficar acima, idem. Nao mexer em `FECHAMENTO_MINIMO`, em
    `DETECCAO_MINIMA` nem no piso ou na margem de leitura para fazer qualquer coisa passar:
    se a analise concluir que um deles precisa se mover, PARAR e relatar em vez de mover.

    Rodar a suite inteira e comparar com o baseline. Conferir por fim que o diff nao encosta
    em `l2scanner/rastreador.py`, `l2scanner/visao.py`, nem nos arquivos do agente paralelo.
  </action>
  <verify>
    <automated>grep -c "GUARDA " .planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-DRY-RUN.txt</automated>
    <automated>sha256sum calibration.json | cut -c1-16 | grep -x c41e665b8eac1669</automated>
    <automated>sha256sum .mercado/observacoes.csv | cut -c1-16 | grep -x d107dbc1ab462ba3</automated>
    <automated>python -c "
import json;d=json.load(open('calibration.json',encoding='utf-8'))
print('tolerancia:',d['mercado_tolerancia_do_cruzamento'],'piso:',d['mercado_limiar_de_leitura_de_glifo'])
assert d['mercado_tolerancia_do_cruzamento'] is None
assert d['mercado_limiar_de_leitura_de_glifo'] == 0.4698309302330017
assert d['mercado_margem_de_leitura_de_glifo'] == 0.03698354959487915"</automated>
    <automated>git status --porcelain -- l2scanner/rastreador.py l2scanner/visao.py l2scanner/respawn.py l2scanner/bosses.py l2scanner/agenda.py l2scanner/sessao.py l2scanner/identidade.py l2scanner/dashboard.py</automated>
    <automated>python -m pytest -q --ignore=tests/test_agenda.py</automated>
  </verify>
  <done>
    O dry-run rodou sobre as 8 gravacoes do censo, a saida integral esta preservada no
    arquivo do quick, e o bloco de refutacao do fonte carrega a remedicao de 2026-09-02 com
    o fechamento no limite novo, a deteccao, o veredito e a quebra por layout. `calibration.json`
    e `.mercado/observacoes.csv` saem com os mesmos sha256 do inicio,
    `mercado_tolerancia_do_cruzamento` continua `None` e o piso gravado continua 0,469831.
    A suite sai com codigo 0 e com mais `passed` que o baseline de 4698. O comando de status
    dos arquivos proibidos sai vazio.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixels da tela -> numero no CSV | entrada NAO confiavel: OCR e casamento de molde erram, e o par `0`x`8` tem margem 0,0370 |
| ferramenta de medicao -> `calibration.json` | uma escrita aqui muda o comportamento de producao em todo arranque futuro |
| plano -> `.mercado/` e `recordings/` | dados de campo do usuario, insubstituiveis (o CSV desta prova, 1502 PNGs em pre-voo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ca4-01 | Tampering | `limite_derivado_do_cruzamento` -> `quantidade_de_adena` | high | mitigate | dobrar o limite reduz a faixa em que a troca `0`<->`8` e pega (ate 7 incrementos, contra 15): o custo esta escrito ao lado da constante e preso por teste nos dois lados, e o caso de campo (1 a 3 incrementos) continua coberto |
| T-ca4-02 | Elevation of Privilege | `FATOR_MAXIMO_SOBRE_O_LIMITE_DERIVADO` | high | mitigate | o teto da tolerancia proposta subiria de 1,0 para 2,0 como efeito colateral silencioso; o fator cai para 1,0 no MESMO commit e o produto e afirmado por teste |
| T-ca4-03 | Tampering | `calibration.json` (13+13 moldes, ja perdido uma vez) | critical | mitigate | dry-run apenas, `--gravar` proibido nesta tarefa, sha256 conferido no fim (`c41e665b8eac1669`) |
| T-ca4-04 | Repudiation | o registro da medicao do 02-02 no fonte | medium | mitigate | a refutacao ACRESCENTA e nao apaga: 1273 / 0,6525 / 0,0164 continuam no fonte, presos por dois testes de inspecao |
| T-ca4-05 | Information Disclosure | `.mercado/observacoes.csv` (prova preservada) | high | mitigate | nenhum teste escreve fora de `tmp_path`; sha256 conferido no fim (`d107dbc1ab462ba3`) |
| T-ca4-06 | Denial of Service | varredura de `recordings/` | medium | accept | a ferramenta varre 8 pastas NOMEADAS, logica dela e ja existente; nenhum glob amplo e acrescentado |
| T-ca4-SC | Tampering | instalacoes npm/pip/cargo | high | accept | nenhuma dependencia nova entra; nao ha instalacao de pacote nesta tarefa |
</threat_model>

<verification>
1. `python -m pytest -q --ignore=tests/test_agenda.py` sai com codigo 0 e com `passed`
   acima de 4698 (baseline: 4698 passed, 2 skipped). Abortar em `tests/test_agenda.py` nao
   e falhar — o arquivo esta ignorado de proposito e e area de outro agente.
2. `limite_derivado_do_cruzamento` devolve 1,0 / 2,0 / 4,0 / 6,0 / 9,0 / 48 para 1, 2, 4, 6,
   9 e 48 unidades, e as dez linhas do gabarito preservado fecham contra ele.
3. As duas mutacoes obrigatorias (constante de volta em 0,5; portao de layout trocado por
   constante local) foram aplicadas, as falhas foram MEDIDAS, copiadas para o SUMMARY, e
   desfeitas.
4. `sha256sum calibration.json` comeca em `c41e665b8eac1669` e
   `sha256sum .mercado/observacoes.csv` comeca em `d107dbc1ab462ba3`.
5. `mercado_tolerancia_do_cruzamento` continua `None` e `mercado_limiar_de_leitura_de_glifo`
   continua 0,4698309302330017 no `calibration.json`.
6. O dry-run esta preservado em `260902-ca4-DRY-RUN.txt` e seus numeros estao transcritos no
   bloco de refutacao do fonte.
7. `git diff --name-only` nao lista `l2scanner/rastreador.py`, `l2scanner/visao.py` nem
   nenhum arquivo do agente paralelo.
</verification>

<success_criteria>
- O limite derivado vale um centesimo por unidade, com a prova de campo de 2026-09-02
  escrita no fonte: gabarito declarado antes, 10 de 10 exatos, 7 de 10 a 0,5 contra 10 de 10
  a 1,0, e as quatro linhas que discriminam truncando as quatro.
- A afirmacao do 02-02 de que o veredito era robusto esta marcada como REFUTADA com data e
  numero, sem apagar um unico numero do registro anterior.
- O teto da tolerancia proposta na ferramenta continua valendo 1,0 centesimo por unidade,
  por decisao escrita, e o produto dos dois fatores esta preso por teste.
- O custo da dobra (deteccao da troca `0`<->`8` cai de ate 15 para ate 7 de escala) esta
  escrito e medido nos dois lados, com o controle que injeta 0,5 de volta.
- O portao de layout que a varredura usa e o de PRODUCAO, provado por contagem de chamadas e
  por controle negativo que zera a populacao do cruzamento sem tocar a de glifo.
- A guarda continua DESLIGADA: dry-run apenas, `mercado_tolerancia_do_cruzamento` intocada, e
  o criterio que caiu (ou o achado, se tiver passado) escrito com o numero.
</success_criteria>

<output>
Create `.planning/quick/260902-ca4-o-limite-derivado-do-cruzamento-passa-de/260902-ca4-SUMMARY.md` when done.
</output>
