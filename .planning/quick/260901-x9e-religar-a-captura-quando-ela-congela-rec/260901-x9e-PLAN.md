---
phase: quick-260901-x9e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/recaptura.py
  - l2scanner/__main__.py
  - tests/test_recaptura.py
autonomous: true
requirements: [QUICK-260901-x9e]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O DEFEITO MEDIDO EM CAMPO FECHA. Em 2026-09-01, 23:18:39, o scanner logou `Imagem congelada` e ficou 33 minutos em `[SEM VISAO]` sem nunca voltar, com o jogo VIVO: a janela existia (hwnd 30216936, nao minimizada, em (1713,0)-(3447,1399)), a geometria batia com `cal[\"geometria_da_tela\"]` (dx=0 dy=0), o recorte da party mostrava 4 membros legiveis, `hp_proprio` lia `HP 5915/5915`, o `mss` via a tela MUDANDO (diffs 174.087 e 120.828) e uma `JanelaSource` NOVA construida na hora funcionava (diffs 150.453 e 149.983, brilho medio 58,2). So o objeto de captura preso no processo estava morto. Depois desta tarefa, N frames CONGELADO seguidos RECONSTROEM a fonte."
    - "A PROVA E CONTAGEM DE CONSTRUCOES, NAO EXISTENCIA DE FUNCAO. O teste central usa uma fonte falsa cujo `__init__` incrementa um contador, empurra o estado congelado e afirma que o contador SUBIU. Esse mesmo teste foi rodado contra a producao MUTILADA (chamada de reconstrucao removida) e FALHOU — a saida medida do pytest fica escrita no `<verify>` da Tarefa 1 e no SUMMARY. Doze defeitos desta sessao nasceram de criterios que afirmavam que a funcao EXISTE em vez de que ela foi CHAMADA; este nao e um deles."
    - "O TETO E MEDIDO, NAO PROMETIDO. Com uma fabrica que nunca cura, o numero total de construcoes para em `1 + TENTATIVAS_DE_RELIGACAO` e nao anda mais, por mais ticks que o teste rode. Sem teto, WGC apanharia uma tentativa por tick para sempre."
    - "UMA RELIGACAO BEM-SUCEDIDA DEVOLVE O ORCAMENTO INTEIRO. Depois que um frame volta com saude diferente de CONGELADO, um congelamento novo horas depois compra um orcamento cheio de novo. O reset e ancorado no FRAME SAUDAVEL, e nao no `__init__` ter retornado: a WGC entrega sessoes que constroem lindamente e continuam mortas, e um reset ancorado na construcao nunca esgotaria o teto nesse caso — que e exatamente o caso de campo."
    - "UMA RECONSTRUCAO QUE EXPLODE NAO DERRUBA O SCANNER. A excecao morre dentro do envelope, conta como tentativa gasta, e `capturar()` devolve um `Frame` normal. O braco `except Exception` do laco (`erros_seguidos`, 10 e sai com codigo 1) NAO e acionado por falha de religacao."
    - "ESGOTADO O TETO, O SCANNER DIZ UMA VEZ, ALTO, E CONTINUA CEGO. Um unico `log.error` — nao um por tick — e o laco segue rodando. Nao sai, nao silencia. `CONGELADO` ja cai no portao `PortaoGlobal.CEGO` que imprime `SEM VISAO` (`__main__.py:700`), entao o modo cego e honesto e ja existe: nada de novo precisa ser inventado para ele."
    - "OS DOIS BACKENDS PASSAM PELO MESMO CAMINHO. `JanelaSource` (com `--janela`) e `MssSource` (sem) sao ambos embrulhados pela MESMA `FonteRecuperavel`, construida em `montar_fonte`. `ReplaySource` fica de fora de proposito: reconstruir um replay reiniciaria a gravacao, e o fim dele ja e `StopIteration` no laco."
    - "A LISTA DE ARGUMENTOS DA CONSTRUCAO EXISTE UMA VEZ SO. A reconstrucao chama a MESMA fabrica que construiu a primeira fonte — nao ha um segundo local com a lista de parametros repetida, que e como os dois sites divergem em silencio. Em particular `minimum_update_interval` continua ausente da chamada da party, byte-identico a hoje, e `tests/test_mercado_firewall_de_fase.py` (linhas 176-185) continua verde."
    - "OS TRES DONOS DE REFERENCIA CONTINUAM VENDO A FONTE VIVA. `Sessao.fonte` (`sessao.py:218`, usa `estado_do_cliente`), `Gravador(fonte_completa=fonte.completo_do_frame_atual)` (`__main__.py:290`, guarda um METODO LIGADO na construcao) e `Reancorador(fonte=fonte)` (`reancoragem.py:345`, chama `capturar_completo` e `apontar_para`) guardam o ENVELOPE, cuja identidade nao muda. Sem o envelope, religar rebindaria so a variavel local e os tres ficariam apontando para o objeto morto — o defeito de hoje, agora em tres lugares."
    - "`hasattr(fonte, \"estado_do_cliente\")` continua respondendo pelo INTERIOR. `MssSource` nao tem esse metodo e `sessao.py:453` decide por `hasattr`; o envelope delega por `__getattr__` em vez de declarar o metodo, senao o caminho de desktop passaria a prometer um estado de cliente que nao existe e estouraria dentro do `try` de analise."
    - "A GEOMETRIA REANCORADA SOBREVIVE A RELIGACAO. Se o `Reancorador` ja adotou uma regiao nova em memoria via `apontar_para`, a fonte reconstruida e reapontada para ELA, nao para a regiao do arranque. Sem isso, um congelamento depois de um reancoramento devolveria o scanner a ler o lugar antigo — ler um lugar com a regua de outro, que o proprio `reancoragem.py:359` chama de pior que nao reancorar."
    - "AS TRES CONSTANTES TEM O PORQUE DO NUMERO ESCRITO AO LADO e vivem no codigo, nao no `calibration.json` — mesma razao ja registrada para `PISO_DO_RESTO` (`mercado_catalogo.py:195-201`): uma chave nova obrigatoria deixaria o scanner morto no proximo arranque ate o usuario recalibrar."
    - "A suite nao regride: `python -m pytest tests/ -q` sai com codigo 0 e a contagem de `passed` fica ACIMA do baseline de 4404 passed, 2 skipped. Roda no python GLOBAL, nao no `.venv`. `tests/test_agenda.py:1141` levanta `KeyboardInterrupt` DE PROPOSITO — abortar ali nao e falhar."
    - "Zero dependencia nova, e NENHUMA biblioteca de sintese de input entra no ambiente. `pyproject.toml` nao muda."
  artifacts:
    - "l2scanner/recaptura.py — modulo novo com `CONGELADOS_SEGUIDOS_PARA_RELIGAR`, `TENTATIVAS_DE_RELIGACAO`, `SEGUNDOS_ENTRE_TENTATIVAS` e a classe `FonteRecuperavel`"
    - "l2scanner/__main__.py — a construcao da fonte fatorada em `montar_fonte`, com os dois caminhos vivos embrulhados"
    - "tests/test_recaptura.py — a fonte falsa que conta construcoes, o teste de religacao (com mutacao medida), o de teto, o de reset de orcamento, o de reconstrucao que explode, os de delegacao e o de fiacao dos dois backends"
  key_links:
    - "`FonteRecuperavel.capturar()` -> `self._construir()` (a fabrica de `montar_fonte`) — o unico ponto onde a fonte nasce, no arranque e na religacao"
    - "`montar_fonte` (`__main__.py`) -> `FonteRecuperavel` -> `Sessao(fonte=...)`, `montar_gravador(args, fonte)`, `Reancorador(fonte=...)`, `avisar_no_arranque(cal, fonte)` — os quatro consumidores recebem o envelope, nunca o interior"
    - "`SaudeDoFrame.CONGELADO` (`frames.py:30`, apos 30 frames identicos) -> contador do envelope -> reconstrucao. O detector NAO muda: ele ja acertou em campo."
---

<objective>
Religar a captura quando ela congela: apos N frames `CONGELADO` seguidos, reconstruir a
fonte de frames e continuar, com teto de tentativas e espera entre elas.

Purpose: em 2026-09-01 o scanner ficou 33 minutos cego com o jogo vivo, a janela no
lugar, a calibracao certa e uma fonte NOVA funcionando quando construida na hora. O
detector de congelamento acertou; o laco so escreveu um `log.warning` e seguiu chamando
`capturar()` num objeto morto para sempre.

Output: `l2scanner/recaptura.py` com o envelope `FonteRecuperavel`, a construcao da fonte
fatorada em `montar_fonte` dentro de `__main__.py`, e `tests/test_recaptura.py` provando
por CONTAGEM DE CONSTRUCOES que a fonte foi reconstruida — com a mutacao medida escrita
no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@l2scanner/frames.py
@l2scanner/captura_janela.py
</context>

<interface_context>
O que ja existe e nao muda:

- `l2scanner/frames.py:110` — `FrameSource` Protocol: `capturar() -> Frame` e
  `fechar() -> None`. E a porta que o envelope precisa implementar.
- `l2scanner/frames.py:30` — `FRAMES_IDENTICOS_PARA_CONGELADO = 30`. A 1 Hz, `CONGELADO`
  ja e um veredito com meio minuto de debounce embutido.
- `l2scanner/frames.py:117` — `_ClassificadorDeSaude` guarda hash e contagem POR
  INSTANCIA. Uma fonte recem-construida comeca com historico limpo e volta a reportar
  `OK` no primeiro frame.
- `l2scanner/frames.py:154` — `MssSource(regiao, extras=None)`, com `fechar()` em 186.
- `l2scanner/captura_janela.py:207` — `JanelaSource(titulo_da_janela, regiao,
  relativa=False, extras=None, minimum_update_interval=None)`. Metodos publicos:
  `capturar`, `capturar_completo`, `completo_do_frame_atual`, `apontar_para`,
  `estado_do_cliente` (462), `fechar` (490, seta `_parar` e da join de 2 s na thread).
- `l2scanner/__main__.py:2284-2320` — o bloco `if args.replay / elif args.janela / else`
  que constroi a fonte hoje. `__main__.py:2081` constroi OUTRA `JanelaSource` num
  subcomando de diagnostico do banner — NAO e o laco e NAO deve ser tocado.
- `l2scanner/__main__.py:2612-2640` — o laco, com o braco `except Exception` que conta
  `erros_seguidos` e devolve 1 aos 10. E o precedente de estilo a seguir.

Os quatro consumidores da fonte, todos depois da construcao:
`avisar_no_arranque(cal, fonte)` (usa `capturar_completo`), `montar_gravador(args, fonte)`
(guarda o metodo ligado `fonte.completo_do_frame_atual`), `Reancorador(fonte=fonte)`
(usa `capturar_completo` e `apontar_para`), `Sessao(fonte=fonte)` (usa
`hasattr(fonte, "estado_do_cliente")` e o chama).
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: a fatia fina — N congelados seguidos reconstroem a fonte, ponta a ponta</name>
  <files>l2scanner/recaptura.py, l2scanner/__main__.py, tests/test_recaptura.py</files>
  <read_first>
    l2scanner/frames.py (linhas 20-45 e 105-190), l2scanner/__main__.py (linhas 2284-2320
    e 2610-2645), l2scanner/mercado_catalogo.py (linhas 193-201, o precedente de por que a
    constante nao vai para o calibration.json).
  </read_first>
  <behavior>
    - Uma fonte falsa cujo `__init__` incrementa um contador de modulo, construida pelo
      envelope no arranque: contador vale 1.
    - A falsa devolve frames com saude `CONGELADO`. Depois de
      `CONGELADOS_SEGUIDOS_PARA_RELIGAR` chamadas de `capturar()` no envelope, o contador
      vale 2 — a fonte FOI RECONSTRUIDA.
    - Enquanto a falsa devolve `OK`, o contador fica em 1 por muitos ticks: nao ha
      religacao sem congelamento.
    - Quando o envelope reconstroi, ele chama `fechar()` na fonte antiga exatamente uma
      vez, e a nova instancia passa a ser quem responde a `capturar()`.
    - `capturar()` continua devolvendo um `Frame` em toda chamada, inclusive na chamada em
      que a reconstrucao acontece.
  </behavior>
  <action>
    Criar `l2scanner/recaptura.py` com as tres constantes nomeadas, cada uma com o motivo
    do numero escrito ao lado em portugues, no tom do modulo vizinho: quantos frames
    `CONGELADO` seguidos disparam a religacao (poucos bastam, porque `CONGELADO` ja carrega
    os 30 frames identicos de `FRAMES_IDENTICOS_PARA_CONGELADO`, ou seja meio minuto a
    1 Hz); o teto de tentativas; e a espera minima entre tentativas. Escrever ao lado
    delas, citando `PISO_DO_RESTO` em `mercado_catalogo.py`, por que nenhuma das tres mora
    no `calibration.json` — chave nova obrigatoria mata o scanner no proximo arranque.

    Nesta tarefa implementar SO a fatia fina do envelope `FonteRecuperavel`: recebe uma
    fabrica sem argumentos que devolve uma `FrameSource` e a chama uma vez na propria
    construcao; expoe `capturar()` que delega ao interior, conta frames `CONGELADO`
    seguidos, zera essa contagem em qualquer outra saude, e ao atingir o limite constroi
    uma fonte nova pela MESMA fabrica, fecha a antiga e adota a nova. O teto, a espera e o
    tratamento de excecao entram na Tarefa 2 — nao antecipar. Registrar no log a tentativa
    e o sucesso, em portugues, no tom do laco.

    Em `l2scanner/__main__.py`, fatorar o bloco que hoje escolhe entre `ReplaySource`,
    `JanelaSource` e `MssSource` numa funcao `montar_fonte` que devolve a fonte pronta.
    Dentro dela, cada caminho vivo vira uma fabrica sem argumentos que carrega a lista de
    argumentos EXATA de hoje — a de `JanelaSource` sem mencionar `minimum_update_interval`,
    para o padrao `None` continuar valendo e o teste de firewall continuar verde — e o
    retorno passa a ser `FonteRecuperavel` embrulhando essa fabrica. O caminho de replay
    devolve a `ReplaySource` crua, sem envelope, e o comentario deve dizer por que.
    Preservar os `log.info`/`log.warning` que ja existem em cada braco, palavra por palavra.
    Nao mexer no subcomando do banner na linha 2081. Nao mexer no laco: a cura acontece
    debaixo dele e o `log.warning` de imagem congelada continua como esta.

    Escrever `tests/test_recaptura.py` com a fonte falsa que conta construcoes e os testes
    do bloco `behavior`. A falsa nao pode reimplementar nada da producao: ela so devolve
    frames com a saude que o teste mandar.
  </action>
  <verify>
    <automated>python -m pytest tests/test_recaptura.py -q</automated>
    <automated>python -m pytest tests/test_mercado_firewall_de_fase.py -q</automated>
    <mutation-proof>
      OBRIGATORIO, e o resultado vai literalmente para o SUMMARY. Depois dos testes
      passarem: comentar a chamada de reconstrucao dentro de `capturar()` em
      `l2scanner/recaptura.py` (so ela; nada mais), rodar
      `python -m pytest tests/test_recaptura.py -q`, e CONFIRMAR que o teste de contagem
      de construcoes FALHA. Copiar a linha de resumo do pytest e a linha do `assert` que
      quebrou. Desfazer a mutacao e rodar de novo para voltar ao verde. Um teste que nao
      foi visto falhando nao foi visto testando nada.
    </mutation-proof>
  </verify>
  <done>
    O contador de construcoes da fonte falsa sai de 1 para 2 depois de
    `CONGELADOS_SEGUIDOS_PARA_RELIGAR` frames `CONGELADO`, e fica em 1 sob frames `OK`. A
    mutacao foi aplicada, a falha foi MEDIDA, a saida foi copiada, e a mutacao foi
    desfeita. `montar_fonte` existe e e o unico lugar do laco onde `JanelaSource` e
    `MssSource` sao construidas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: limitada e pausada — teto, espera, orcamento devolvido e religacao que explode</name>
  <files>l2scanner/recaptura.py, tests/test_recaptura.py</files>
  <behavior>
    - Fabrica que nunca cura (toda fonte nova ja nasce devolvendo `CONGELADO`): o total de
      construcoes para em `1 + TENTATIVAS_DE_RELIGACAO` e nao sobe mais, nem depois de dez
      vezes mais ticks que o necessario.
    - Esgotado o teto, sai UM unico `log.error` — a contagem de registros nesse nivel e 1,
      nao um por tick — e `capturar()` continua devolvendo `Frame` normalmente.
    - Espera: com um relogio injetado que nao anda, duas tentativas nao acontecem no mesmo
      instante; avancando o relogio em `SEGUNDOS_ENTRE_TENTATIVAS` a proxima tentativa
      acontece. O envelope NAO chama `time.sleep`.
    - Fabrica que levanta excecao na reconstrucao: `capturar()` devolve `Frame` sem
      propagar, a tentativa conta como gasta, o interior continua sendo a fonte antiga, e o
      teto ainda para em `TENTATIVAS_DE_RELIGACAO` tentativas.
    - Orcamento devolvido: uma fonte que congela, religa e volta com saude diferente de
      `CONGELADO` zera tentativas gastas; congelando de novo depois disso, o total de
      construcoes cresce por um orcamento CHEIO outra vez.
  </behavior>
  <action>
    Acrescentar ao `FonteRecuperavel` o contador de tentativas gastas, o teto, e o portao
    de espera baseado num relogio injetavel com padrao `time.monotonic` — comparacao de
    instantes, nunca `time.sleep`, porque o laco ja dorme `args.intervalo` por tick e
    bloquear aqui atrasaria os comandos e a agenda.

    Envolver a construcao da fonte nova e o fechamento da antiga em tratamento de excecao
    proprio: falhar em religar conta tentativa e mantem o interior anterior; falhar em
    fechar o antigo nao pode impedir a adocao do novo. Construir o novo ANTES de fechar o
    velho, para que uma fabrica que explode deixe o scanner com a fonte antiga em vez de
    sem fonte nenhuma.

    Ancorar o reset do orcamento no FRAME SAUDAVEL, e nao no `__init__` ter retornado, e
    escrever o motivo em comentario: a WGC devolve sessoes que constroem sem erro e
    continuam mortas — foi o caso medido em campo — e um reset ancorado na construcao nunca
    esgotaria o teto justamente nesse caso.

    Ao esgotar o teto, emitir um unico `log.error` dizendo que a captura nao voltou, que o
    scanner segue rodando e que nada sera detectado ate a imagem mudar; usar uma marca de
    ja-avisado para nao repetir por tick. Nao sair, nao levantar, nao silenciar. Estender
    `tests/test_recaptura.py` com os cinco casos do bloco `behavior`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_recaptura.py -q</automated>
  </verify>
  <done>
    O total de construcoes com fabrica que nunca cura e exatamente
    `1 + TENTATIVAS_DE_RELIGACAO` e nao cresce com mais ticks. Um unico registro de nivel
    error e emitido no esgotamento. Uma fabrica que levanta excecao nao propaga e nao
    impede o teto de ser respeitado. Um frame saudavel devolve o orcamento inteiro.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: os donos de referencia sobrevivem a troca, e os dois backends passam pelo mesmo cano</name>
  <files>l2scanner/recaptura.py, tests/test_recaptura.py</files>
  <behavior>
    - Metodo ligado colhido ANTES da religacao: guardar `envelope.completo_do_frame_atual`
      numa variavel, forcar a religacao, chamar a variavel e receber o dado da fonte NOVA.
      E o padrao exato do `Gravador`, que colhe esse metodo na construcao.
    - `capturar_completo()` e `apontar_para()` chamados no envelope chegam a fonte VIVA
      depois de uma religacao. E o padrao do `Reancorador`.
    - `hasattr(envelope, "estado_do_cliente")` e False quando o interior e uma falsa sem
      esse metodo, e True quando o interior o tem — e nesse caso a chamada devolve o valor
      do interior. E a decisao de `sessao.py:453`.
    - Geometria reancorada sobrevive: chamar `apontar_para` com uma regiao nova, depois
      forcar a religacao, e a fonte reconstruida recebe ESSA regiao, nao a do arranque.
    - Fiacao: com `JanelaSource` e `MssSource` substituidas por falsas no espaco de nomes
      de `__main__`, `montar_fonte` devolve uma `FonteRecuperavel` nos DOIS caminhos vivos,
      e a fonte falsa foi construida exatamente uma vez em cada. No caminho de replay
      devolve a `ReplaySource` crua.
  </behavior>
  <action>
    Completar a delegacao do envelope. Declarar EXPLICITAMENTE em `FonteRecuperavel` so os
    metodos cujos donos guardam a referencia ou o metodo ligado atravessando a religacao —
    `capturar`, `capturar_completo`, `completo_do_frame_atual`, `apontar_para` e `fechar` —
    cada um repassando para o interior CORRENTE no momento da chamada. Todo o resto vai por
    `__getattr__` delegando ao interior, e o comentario deve dizer por que `__getattr__` e
    obrigatorio aqui em vez de metodos declarados: `sessao.py` decide por
    `hasattr(fonte, "estado_do_cliente")`, `MssSource` nao tem esse metodo, e um metodo
    declarado no envelope faria o caminho de desktop prometer um estado de cliente que nao
    existe.

    Guardar no envelope a ultima regiao passada a `apontar_para` e reaplica-la na fonte
    recem-construida quando houver uma, para o reancoramento em memoria sobreviver a
    religacao. Reaplicar so quando o interior aceitar — `apontar_para` recusa fonte de
    coordenadas de desktop com `ValueError`, e essa recusa nao pode virar crash de
    religacao.

    Estender `tests/test_recaptura.py` com os cinco casos do bloco `behavior`, incluindo os
    dois testes de fiacao que trocam `JanelaSource` e `MssSource` por falsas no modulo
    `l2scanner.__main__` e conferem o tipo devolvido por `montar_fonte`. Rodar a suite
    inteira e conferir que a contagem de `passed` ficou acima do baseline de 4404.
  </action>
  <verify>
    <automated>python -m pytest tests/test_recaptura.py -q</automated>
    <automated>python -m pytest tests/ -q</automated>
    <automated>python -c "import importlib.util as u; proibidas=['pyautogui','pydirectinput','pynput','keyboard','mouse']; achadas=[p for p in proibidas if u.find_spec(p)]; print('SINTESE DE INPUT:', achadas); raise SystemExit(1 if achadas else 0)"</automated>
  </verify>
  <done>
    Um metodo ligado colhido antes da religacao entrega dados da fonte nova. `hasattr` do
    estado do cliente responde pelo interior nos dois casos. A regiao reancorada sobrevive
    a religacao. `montar_fonte` devolve `FonteRecuperavel` nos dois caminhos vivos e
    `ReplaySource` crua no replay. `python -m pytest tests/ -q` sai com codigo 0 com mais
    de 4404 aprovados, e nenhuma biblioteca de sintese de input esta no ambiente.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WGC/DWM -> processo do scanner | A sessao de captura e recurso do SO e pode morrer sem avisar. Foi o que aconteceu em campo. |
| laco do scanner -> WhatsApp (Chatwoot) | Todo alerta emitido a partir de um frame mal julgado vira mensagem para a party. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x9e-01 | Denial of Service | `FonteRecuperavel` -> WGC | high | mitigate | Teto `TENTATIVAS_DE_RELIGACAO` mais portao de espera: sem eles, uma religacao por tick martelaria a WGC para sempre. Teste de teto na Tarefa 2. |
| T-x9e-02 | Denial of Service | thread da `JanelaSource` | medium | mitigate | Cada religacao fecha a fonte antiga (`fechar()` seta `_parar` e da join). Sem isso, cada tentativa vazaria uma thread de captura e um buffer da janela inteira. |
| T-x9e-03 | Spoofing (dado falso vira alerta) | frame apos religacao | high | mitigate | A fonte nova traz `_ClassificadorDeSaude` limpo e o primeiro frame volta `OK`; o julgamento de HP continua em `visao.py`/`rastreador.py`, que este plano NAO toca. Nenhum alerta novo nasce daqui. |
| T-x9e-04 | Tampering | dependencias | high | mitigate | Zero pacote novo; `pyproject.toml` fora do diff. O portao de sintese de input roda na Tarefa 3. |
| T-x9e-05 | Information disclosure | `calibration.json` | medium | mitigate | As tres constantes ficam no codigo; nada e lido nem escrito na calibracao, que segue gitignored e intocada. |
</threat_model>

<verification>
1. `python -m pytest tests/test_recaptura.py -q` — verde.
2. A MUTACAO MEDIDA da Tarefa 1 — a saida do pytest com o teste de contagem FALHANDO,
   copiada literalmente para o SUMMARY. Sem ela a tarefa nao esta feita.
3. `python -m pytest tests/test_mercado_firewall_de_fase.py -q` — verde, provando que
   `minimum_update_interval` continua ausente da construcao da party.
4. `python -m pytest tests/ -q` — codigo 0, `passed` acima de 4404.
5. `git diff --stat` mostra apenas `l2scanner/recaptura.py`, `l2scanner/__main__.py` e
   `tests/test_recaptura.py`. `rastreador.py`, `visao.py`, `sessao.py`, `respawn.py`,
   `bosses.py`, `agenda.py`, `identidade.py`, `discord/`, `pyproject.toml` e
   `calibration.json` fora do diff.
</verification>

<success_criteria>
- N frames `CONGELADO` seguidos RECONSTROEM a fonte, provado por contagem de construcoes
  numa fonte falsa, com a falha sob mutacao MEDIDA e escrita.
- O numero de reconstrucoes e limitado, espacado no tempo, e imune a uma fabrica que
  levanta excecao.
- Um frame saudavel devolve o orcamento inteiro.
- Esgotado o teto: um unico erro no log, o scanner continua rodando cego, nao sai.
- `JanelaSource` e `MssSource` passam pelo mesmo envelope; `ReplaySource` fica de fora com
  o motivo escrito.
- Os quatro consumidores da fonte continuam funcionando atraves da troca.
- Suite verde acima do baseline; nenhuma dependencia nova.
</success_criteria>

<output>
Create `.planning/quick/260901-x9e-religar-a-captura-quando-ela-congela-rec/260901-x9e-SUMMARY.md` when done.

O SUMMARY DEVE CONTER, literalmente: a saida do pytest com o teste de contagem de
construcoes FALHANDO sob a mutacao (chamada de reconstrucao removida), e a saida dele
passando depois de desfeita. Os valores finais das tres constantes com o porque de cada
numero. A contagem final da suite.
</output>
