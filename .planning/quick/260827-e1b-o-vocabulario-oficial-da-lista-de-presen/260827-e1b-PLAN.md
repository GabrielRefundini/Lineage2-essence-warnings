---
phase: quick-260827-e1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/comandos.py
  - l2scanner/agenda.py
  - l2scanner/presenca.py
  - l2scanner/__main__.py
  - config.toml
  - README.md
  - tests/test_comandos.py
  - tests/test_agenda.py
  - .planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260827-e1b]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O vocabulario ANUNCIADO da lista de presenca e portugues: `_AJUDA[Comando.JOIN].sintaxe == '/entrar'` e `_AJUDA[Comando.LEAVE].sintaxe == '/sair'`, e o texto que sai no celular (`texto_de_ajuda()`) traz os dois como sintaxe principal (D-01)."
    - "OS QUATRO NOMES CONTINUAM FUNCIONANDO. `_VOCABULARIO` nao perde uma unica entrada: `entrar`, `join`, `sair` e `leave` seguem mapeados, e os quatro atravessam o caminho REAL (`comandos_novos` com as cinco travas ligadas) nos dois prefixos aceitos. Ninguem que ja decorou o nome ingles fica sem resposta (D-02)."
    - "O ingles fica ANUNCIADO COMO APELIDO, nao apagado: ele aparece na tupla `apelidos` das duas linhas de Presenca, entao o proprio `/help` continua ensinando que ele vale — e ninguem descobre por acidente que decorou a forma demovida (D-02)."
    - "As mensagens operacionais de UMA LINHA falam SO portugues: a chamada de 1h50 no grupo (`agenda.py`), o erro de disco do registro (`presenca.py`) e as tres linhas de arranque no console (`__main__.py`) nao citam mais o nome ingles em lugar nenhum (D-03)."
    - "As superficies de REFERENCIA mostram os dois, com o portugues na frente: a tabela `_AJUDA`, o bloco de comentario `[[membro]]` do `config.toml` e a tabela de comandos do `README.md` trazem o portugues como principal e o ingles entre parenteses (D-03)."
    - "A tabela do README deixa de estar em divida com o bot: ela passa a listar as duas linhas de Presenca, que nunca entraram la — a lacuna registrada como Deferred Item da quick `260827-b82` (D-04)."
    - "O tripwire cresceu, nao foi trocado: `set(_AJUDA) == set(Comando)` e `toda sintaxe comeca por PREFIXO` continuam valendo, e agora TODO APELIDO ANUNCIADO tambem volta como o comando certo pelo caminho real — a prova que hoje so existe para a `sintaxe` e que, sem ela, o ingles demovido a apelido ficaria anunciado sem prova nenhuma (C-03)."
    - "Uma reversao silenciosa para o ingles quebra a suite: existe asercao direta de que a sintaxe anunciada das duas linhas de Presenca e a portuguesa (C-03)."
    - "Os testes que fixavam o texto antigo foram atualizados NO MESMO COMMIT da mudanca que os quebra — `tests/test_agenda.py` (a chamada do Solo Boss) e `tests/test_comandos.py` (a linha de arranque da presenca) (C-05)."
    - "Zero dependencia nova e zero mudanca de comportamento do parser: `pyproject.toml` fora do diff, e `_VOCABULARIO`, `autorizado_para` e `COMANDOS_DE_MEMBRO` intocados (C-01, C-02)."
    - "A suite nao regrediu: `python -m pytest tests/ -q` sai com pelo menos `1121 passed, 2 skipped` e com MAIS testes do que comecou — cobertura acrescentada, nunca trocada (C-04)."
    - "Os itens 3 e 4 do `10-UAT.md` citam os textos NOVOS e voltaram para pendente de re-teste, porque o texto que eles julgam mudou (D-05)."
  artifacts:
    - "l2scanner/comandos.py — `_AJUDA` com `/entrar` e `/sair` como sintaxe e o ingles como apelido, com o porque escrito ao lado das quatro entradas do `_VOCABULARIO`"
    - "tests/test_comandos.py — os quatro nomes provados pelo caminho real nos dois prefixos, o tripwire de apelidos anunciados, e a asercao direta da sintaxe portuguesa"
    - "tests/test_agenda.py — a chamada do Solo Boss afirmada em portugues e negando o ingles"
    - "config.toml e README.md — a referencia mostra portugues na frente, ingles entre parenteses; o README ganha as duas linhas de Presenca que faltavam"
    - ".planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md — itens 3 e 4 com os textos novos e de volta para re-teste"
    - ".planning/quick/260827-e1b-o-vocabulario-oficial-da-lista-de-presen/260827-e1b-SUMMARY.md"
  key_links:
    - "`_AJUDA[Comando.JOIN].sintaxe` -> `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` (tests/test_comandos.py:277): esse teste ja existe e roda a tabela inteira por `comandos_novos` com as cinco travas ligadas. Inverter a sintaxe o CONVERTE, de graca, em prova ponta-a-ponta de que `/entrar` e `/sair` funcionam de verdade. E o motivo de a tabela ser o primeiro lugar a mudar."
    - "`_AJUDA[...].apelidos` -> NENHUM teste hoje: `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` percorre so o campo `sintaxe`. No momento em que o ingles vira APELIDO, ele sai da cobertura ponta-a-ponta e passa a ser anunciado sem prova. Esta e a lacuna que a Tarefa 1 fecha, e e o unico defeito real que esta tarefa pode introduzir."
    - "`l2scanner/agenda.py:274-275` -> `tests/test_agenda.py:365-368`: o teste afirma o nome ingles no texto MAIS LIDO do recurso (sai duas vezes por ocorrencia de boss, para o grupo todo). Ele QUEBRA no instante em que a chamada mudar, e essa quebra e o alarme de que a Tarefa 2 chegou no lugar certo — por isso os dois andam no mesmo commit."
    - "`l2scanner/__main__.py:551` -> `tests/test_comandos.py:2402` (`assert '/join' in caplog.text`): a segunda asercao que quebra. O `caplog` e a unica prova de que a linha de arranque da presenca existe; convertida no mesmo commit, ela continua provando a mesma coisa com a palavra nova."
    - "`_VOCABULARIO` (comandos.py:213-216) NAO e tocado, e essa e a metade mais importante do pedido. Se o diff remover uma unica das quatro entradas, o comando some EM SILENCIO — nao existe resposta de recusa, entao quem digitou o nome antigo recebe NADA e conclui que o bot caiu. `git diff l2scanner/comandos.py` nao pode conter uma linha de delecao dentro desse bloco."
    - "`.join(` e METODO DE STRING e aparece 10 vezes so no `__main__.py`, alem de `.loot/`, `.env` e `.agenda/` que sao CAMINHOS. Nenhum portao usa `.join` — todos usam a forma com barra, que nao casa nem o metodo nem os caminhos. `sed` de arquivo inteiro esta proibido nesta tarefa."
---

<objective>
O vocabulario oficial da lista de presenca do Solo Boss passa a ser `/entrar` e
`/sair`, em portugues — mas so no que o produto ANUNCIA. Os quatro nomes
continuam aceitos.

Purpose: a party e brasileira e a mao digita portugues sozinha. O gap 1 do
`10-UAT.md` nao e defeito — `/entrar` e `/sair` JA funcionam hoje; o que esta
errado e a vitrine, que anuncia o ingles como principal e relega o portugues a
apelido. E cortar o ingles seria trocar uma inconveniencia por uma falha muda,
pela mesma razao ja medida na quick `260827-b82`: comando nao reconhecido morre
no `continue` do laco de autorizacao, sem resposta nenhuma.

Output: a tabela de ajuda invertida e provada; as tres mensagens operacionais em
portugues; a referencia (`config.toml`, `README.md`) mostrando os dois com o
portugues na frente; e os itens 3 e 4 do UAT da Fase 10 de volta para re-teste.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/quick/260827-b82-comandos-passam-a-usar-a-barra-do-lineag/260827-b82-SUMMARY.md
@.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md

@l2scanner/comandos.py
@l2scanner/agenda.py
@l2scanner/presenca.py
@config.toml
@README.md
</context>

<terreno_confirmado>
Medido agora, antes de planejar. Onde diverge do briefing, vale isto:

- **Suite: `1121 passed, 2 skipped` em 26s** (`python -m pytest tests/ -q`). O
  briefing dizia 1077 — esse era o piso ANTES da quick `260827-b82`, que
  acrescentou 44 testes. **1121 e o piso real.**
- `_VOCABULARIO` (comandos.py:213-216) mapeia os quatro nomes. Confirmado.
- `_AJUDA` (comandos.py:274-285): `JOIN` traz `"/join"` com `("/entrar",)`,
  `LEAVE` traz `"/leave"` com `("/sair",)`. Confirmado.
- **Os testes da ajuda sao quase todos DERIVADOS da tabela** e por isso NAO
  quebram com a inversao: `set(_AJUDA) == set(Comando)` (230), `toda sintaxe
  comeca por PREFIXO` (241), `o texto nao anuncia o prefixo legado` (263, que
  calcula `"." + sintaxe[1:]`), `toda sintaxe volta como o comando certo` (277),
  `o texto cita todas as sintaxes` (301). Todos continuam verdes.
- **LACUNA ENCONTRADA, e e o risco desta tarefa:** o teste da linha 277 percorre
  SO o campo `sintaxe`. **Nenhum teste exercita `apelidos`.** Demover o ingles a
  apelido o tira da cobertura ponta-a-ponta em silencio.
- `texto_de_ajuda` (comandos.py:326-337) monta `sintaxe (apelidos) — descricao`
  sem alinhamento de coluna: `/entrar` (7 chars) no lugar de `/join` (5) nao
  quebra nenhuma conta de largura. `test_a_ajuda_nao_sai_moldurada` (330) so
  procura linha de asteriscos.
- **Os DOIS testes que quebram, e so eles:**
  `tests/test_agenda.py:365-368` (`"/join" in texto`, `"/leave" in texto`) e
  `tests/test_comandos.py:2402` (`assert "/join" in caplog.text`).
- **Nao ha teste** afirmando o texto do erro de disco do `presenca.py`, nem as
  linhas de arranque 529 e 592 do `__main__.py`. Confirmado por grep.
- Contagens de partida, com a forma COM BARRA (a forma com ponto nao e tocada
  por nenhum portao desta tarefa):

  | arquivo | ingles hoje | alvo |
  |---|---|---|
  | `l2scanner/agenda.py` | 2 | **0** |
  | `l2scanner/presenca.py` | 1 | **0** |
  | `l2scanner/__main__.py` | 5 | **0** |
  | `l2scanner/comandos.py` | 2 | **2** (viram apelidos, continuam la) |
  | `config.toml` | 4 | 2 (so a listinha, entre parenteses) |
  | `README.md` | 2 | 2 (a frase de apelidos) |

- Armadilhas confirmadas: `.join(` e metodo de string, **10 ocorrencias so no
  `__main__.py`**; `.loot/`, `.env`, `.agenda/` sao caminhos. Nenhum portao
  desta tarefa usa a forma com ponto, entao nenhum deles os enxerga.
- Armadilha herdada do b82, ainda valendo: **o `README.md` tem um TAB VERTICAL
  (`\x0b`)**. `str.splitlines()` quebra nele e o `grep -n` nao; a numeracao das
  duas visoes diverge em 1 dali para baixo. Quem editar o README por numero de
  linha tem que usar `split("\n")`.
- O README **nao lista** as duas linhas de Presenca na tabela de comandos —
  a lacuna registrada como Deferred Item do b82. Confirmada.
</terreno_confirmado>

<decisoes>
Travadas pelo usuario no `10-UAT.md`, verbatim — nao reabrir:
*"ah se ja funciona pode manter, so use em portugues nas mensagens como padrao
/entrar /sair"*

- **D-01 — O portugues e o vocabulario ANUNCIADO.** `/entrar` e `/sair` viram a
  `sintaxe` das duas linhas de Presenca da `_AJUDA`.
- **D-02 — Os quatro nomes continuam valendo, e o ingles fica ANUNCIADO COMO
  APELIDO.** Nenhuma entrada sai do `_VOCABULARIO`. O ingles entra na tupla
  `apelidos`, entao o proprio `/help` continua ensinando que ele funciona: isto
  e mais forte que "aceito em silencio" e e o que a palavra *manter* do usuario
  pede. E o motivo de nao cortar e o mesmo ja medido no b82 — comando nao
  reconhecido morre no `continue` do laco de autorizacao, sem resposta.

Decididas aqui, porque o briefing manda decidir e documentar:

- **D-03 — Mensagem operacional de uma linha fala SO portugues; superficie de
  referencia mostra os dois.** A chamada de 1h50, o erro de disco e as tres
  linhas de arranque nomeiam UMA acao e sao lidas de relance no meio do farm:
  listar dois nomes ali dobra o comprimento e ensina duas coisas onde so uma
  precisa ser feita. Ja a tabela `_AJUDA`, o bloco `[[membro]]` do
  `config.toml` e a tabela do `README.md` sao o lugar onde a pessoa vai
  PROCURAR o que existe — e e exatamente la que o apelido tem que estar, senao
  quem decorou o ingles descobre por acidente que ele foi demovido.
- **D-04 — A tabela do README ganha as duas linhas de Presenca.** Elas nunca
  entraram la (Deferred Item do b82). Custam duas linhas, o arquivo ja esta
  aberto nesta tarefa, e uma tabela de comandos que omite justamente o comando
  cujo nome estamos trocando seria uma vitrine mentindo por omissao.
- **D-05 — Os itens 3 e 4 do `10-UAT.md` voltam para pendente.** O item 3 ja
  tinha passado e o item 4 e o proprio gap; os dois julgam TEXTO, e o texto
  mudou. Deixar o item 3 marcado como aprovado seria carimbar uma redacao que
  ninguem leu.
- **D-06 — Os nomes do ENUM (`Comando.JOIN`, `Comando.LEAVE`) NAO mudam.**
  Sao identificadores internos, invisiveis ao usuario. Renomea-los espalharia o
  diff por seis arquivos e por centenas de linhas de teste sem entregar um
  caractere de valor a quem le o WhatsApp — e o pedido e sobre o que o produto
  ANUNCIA.
</decisoes>

<restricoes>
- **C-01** — Zero dependencia nova. `pyproject.toml` fora do diff.
- **C-02** — **NAO REMOVER NENHUMA ENTRADA DO `_VOCABULARIO`.** As quatro
  linhas 213-216 ficam como estao. `autorizado_para` e `COMANDOS_DE_MEMBRO`
  tambem nao sao tocados: esta tarefa nao muda uma virgula do que o parser
  aceita nem do que um membro alcanca.
- **C-03** — Preservar as propriedades dos tripwires que ja existem e cobrir a
  lacuna nova: apelido anunciado sem prova ponta-a-ponta e o unico defeito que
  esta tarefa pode introduzir.
- **C-04** — Suite verde ao final, sem regressao. **Piso medido: `1121 passed,
  2 skipped`**, e com mais testes do que comecou.
- **C-05** — Teste que fixa texto antigo e atualizado **no mesmo commit** da
  mudanca que o quebra. Nunca um commit vermelho no meio.
- Nunca `sed` de arquivo inteiro, nunca regex de arquivo inteiro: por
  ocorrencia, com numero de linha, com asercao previa do texto esperado.
- Portugues SEM ACENTOS no codigo e nos assuntos de commit. Assunto no estilo
  do log: `feat(quick-260827-e1b): ...`. Testes rodam com `python -m pytest`
  (o `.venv` nao tem pytest). `git add` sempre por caminho explicito — `.gsd/`
  nao esta rastreado nem ignorado e um `git add -A` o varreria para dentro.
- Branch: continuar em `feat/solo-boss-join`. Nao criar branch.
</restricoes>

<!-- planner-discipline-allow: /join -->
<!-- planner-discipline-allow: /leave -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A vitrine inverte — o portugues vira a sintaxe anunciada, e o apelido ganha a prova que nunca teve</name>
  <files>l2scanner/comandos.py, tests/test_comandos.py</files>

  <read_first>
    l2scanner/comandos.py — o bloco de comentario e as quatro entradas do
    `_VOCABULARIO` em 202-216, as duas linhas de Presenca da `_AJUDA` em
    274-285, e `texto_de_ajuda` em 313-337.
    tests/test_comandos.py:200-312 — a classe de ajuda inteira: os dois
    tripwires (230 e 241), o teste do texto entregue (263) e o teste do caminho
    real (277), que e o que percorre so o campo `sintaxe`.
  </read_first>

  <behavior>
    - `_AJUDA[Comando.JOIN].sintaxe` e a forma portuguesa de entrar;
      `_AJUDA[Comando.LEAVE].sintaxe` e a forma portuguesa de sair. Asercao
      direta, e nao derivada: e esta a decisao, e o que a impede de reverter
      em silencio no proximo ajuste de texto.
    - A forma inglesa de cada uma aparece na tupla `apelidos` da mesma linha —
      anunciada, nao apagada.
    - **Os quatro nomes voltam como o comando certo pelo CAMINHO REAL**
      (`comandos_novos`, com as cinco travas ligadas), parametrizado sobre os
      dois prefixos de `comandos.PREFIXOS`: oito casos, e nenhum deles monta
      parser proprio no teste.
    - **Todo APELIDO anunciado na tabela inteira volta como o comando certo
      pelo mesmo caminho real** — a generalizacao do teste que hoje so olha
      `sintaxe`.
    - `set(_AJUDA) == set(Comando)` continua valendo, toda sintaxe e todo
      apelido continuam comecando por `PREFIXO`, e `texto_de_ajuda()` continua
      sem anunciar o prefixo legado.
  </behavior>

  <action>
    Esta e a fatia vertical: uma unica mudanca que atravessa tabela -> texto
    montado -> caminho real de leitura, e cuja prova ponta-a-ponta ja existe na
    suite e passa a valer para a palavra nova de graca.

    **Primeiro, escrever os testes e ve-los falhar** (RED), depois a inversao
    (GREEN). Sao dois commits.

    Em `tests/test_comandos.py`, dentro da classe de ajuda que ja existe,
    acrescentar:

    1. A asercao direta das duas sintaxes de Presenca, com a razao na
       docstring: os outros tripwires sao derivados da tabela de proposito e
       por isso NAO enxergam uma reversao de vocabulario — inverter os dois
       campos de volta deixaria todos eles verdes. Esta e a unica prova que
       quebra nesse caso, e e o registro executavel da decisao do usuario.
       Afirmar tambem que a forma demovida esta presente entre os `apelidos`,
       senao "inverter" viraria "apagar" sem nenhum alarme.
    2. O teste dos quatro nomes pelo caminho real, parametrizado sobre
       `comandos.PREFIXOS` — nunca escrito duas vezes, para que um terceiro
       prefixo, se um dia existir, ja nasca provado. Montar a mensagem no
       formato que `comandos_novos` recebe, com telefone de dono autorizado,
       no estilo do `_mensagem` que a classe ja usa. A docstring registra por
       que os quatro convivem: comando nao reconhecido e descartado no
       `continue` do laco de autorizacao, sem resposta de recusa, entao quem
       digitou o nome demovido receberia NADA — indistinguivel, do lado dele,
       de o bot ter caido.
    3. A generalizacao do tripwire do caminho real para os `apelidos`: no
       estilo do teste vizinho que percorre `sintaxe`, percorrer agora cada
       apelido de cada linha, aplicando as mesmas substituicoes de
       marcador de nick e de hora. A docstring diz o que ele existe para
       impedir: um apelido anunciado que nao funciona ensina sintaxe morta
       exatamente como uma sintaxe anunciada que nao funciona — a tabela nao
       tem dois niveis de veracidade, e ate hoje ela tinha dois niveis de
       prova.

    Rodar e CONFERIR QUE FALHAM antes de tocar em `comandos.py`. O item 3, em
    particular, tem que passar ja hoje (os apelidos atuais todos funcionam);
    se ele falhar antes da inversao, achamos um defeito pre-existente e ele
    merece ser registrado no SUMMARY em vez de escondido.

    Em `l2scanner/comandos.py`, inverter as duas linhas de Presenca da `_AJUDA`
    (274-285), per D-01 e D-02: a forma portuguesa passa para o campo
    `sintaxe`, a inglesa passa para a tupla `apelidos`. **Nao mexer em
    `familia` nem em `descricao`** — a familia Presenca continua vindo antes de
    Loot, que e a ordem do ciclo do boss, e a ordem de insercao do dict e a
    ordem da resposta.

    Registrar o porque no comentario que ja existe logo acima das quatro
    entradas do `_VOCABULARIO` (202-212). Aquele comentario ja explica por que
    a lista de apelidos e curta e qual o preco de cada palavra registrada;
    acrescentar a ele, per D-02, que a party e brasileira e que o portugues
    passou a ser o ANUNCIADO, que os quatro nomes continuam aceitos, e que a
    forma inglesa nao pode sair dali porque o descarte de comando desconhecido
    e SILENCIOSO — quem decorou o nome antigo nao receberia recusa, receberia
    nada. **Escrever esse comentario sem citar as formas com barra**: os
    portoes da Tarefa 2 procuram o literal com barra dentro dos arquivos, e uma
    mencao aqui e legitima mas a proxima pessoa que copiar o padrao para outro
    arquivo reprovaria o portao com razao. Nomear as palavras sem o prefixo.

    **NAO TOCAR nas quatro entradas do `_VOCABULARIO` (213-216)** (C-02). Elas
    sao a metade mais importante do pedido. Tambem nao tocar em
    `autorizado_para`, `COMANDOS_DE_MEMBRO`, `_PALAVRA_HUMANA`,
    `interpretar`, `interpretar_dinamico` nem `comandos_novos`: esta tarefa nao
    muda uma virgula do que o parser aceita.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && python -m pytest tests/test_comandos.py -q 2>&1 | tail -3 && python -c "
import l2scanner.comandos as c
from l2scanner.comandos import Comando, _AJUDA, _VOCABULARIO, interpretar
j, l = _AJUDA[Comando.JOIN], _AJUDA[Comando.LEAVE]
assert j.sintaxe == c.PREFIXO + 'entrar', j.sintaxe
assert l.sintaxe == c.PREFIXO + 'sair', l.sintaxe
assert c.PREFIXO + 'join' in j.apelidos, j.apelidos
assert c.PREFIXO + 'leave' in l.apelidos, l.apelidos
for nome in ('entrar', 'join'):
    for p in c.PREFIXOS: assert interpretar(p + nome) is Comando.JOIN, p + nome
for nome in ('sair', 'leave'):
    for p in c.PREFIXOS: assert interpretar(p + nome) is Comando.LEAVE, p + nome
assert {'entrar','join','sair','leave'} <= set(_VOCABULARIO)
assert set(_AJUDA) == set(Comando)
t = c.texto_de_ajuda()
assert (c.PREFIXO + 'entrar') in t and (c.PREFIXO + 'sair') in t
print('OK vitrine')" && git diff -U0 l2scanner/comandos.py | grep -c '^-.*Comando\.\(JOIN\|LEAVE\),$' | grep -qx 0 && echo "OK vocabulario intacto"</automated>
  </verify>

  <done>
    `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` — que ja existia e
    nao foi tocado — passa a rodar a sintaxe PORTUGUESA pelo caminho real com
    as cinco travas ligadas, e passa. O ingles, agora apelido, tem prova
    ponta-a-ponta propria pela primeira vez. `_VOCABULARIO` sem uma unica linha
    de delecao no `git diff`. `tests/test_comandos.py` verde inteiro, com pelo
    menos tres testes a mais que antes.
  </done>
</task>

<task type="auto">
  <name>Task 2: As mensagens falam portugues, e os dois testes que fixavam o texto antigo andam junto</name>
  <files>l2scanner/agenda.py, l2scanner/presenca.py, l2scanner/__main__.py, config.toml, README.md, tests/test_agenda.py, tests/test_comandos.py</files>

  <precondition>A Tarefa 1 esta commitada e a suite esta verde — o texto so pode anunciar a palavra depois que a tabela a anuncia.</precondition>

  <read_first>
    l2scanner/agenda.py:267-277 (a chamada de 1h50, com o comentario que explica
    o "no PRIVADO"), l2scanner/presenca.py:280-285 (o erro de disco),
    l2scanner/__main__.py:526-532, 548-556 e 588-596 (as tres linhas de
    arranque), config.toml:68-80, README.md:287-305 (a tabela de comandos e a
    frase de apelidos) e 349-352 (o paragrafo dos party-mates),
    tests/test_agenda.py:355-370, tests/test_comandos.py:2395-2403.
  </read_first>

  <behavior>
    - A chamada do Solo Boss (`texto_do_aviso` com `TipoDeAviso.CHAMADA`)
      anuncia as duas palavras portuguesas e nao contem mais nenhuma das duas
      formas inglesas.
    - `tests/test_agenda.py:365-368`, que hoje afirma o ingles nesse texto,
      passa a afirmar o portugues e a negar o ingles — e mantem as duas
      negativas do prefixo legado que a quick anterior deixou la.
    - `tests/test_comandos.py:2402`, que afirma a linha de arranque da presenca
      pelo `caplog`, passa a afirmar a palavra portuguesa.
  </behavior>

  <action>
    POR OCORRENCIA, COM NUMERO DE LINHA, conferindo o texto esperado antes de
    cada troca. Proibido `sed` e regex de arquivo inteiro: o metodo de string
    de juntar lista aparece dez vezes so no `__main__.py`, e `.loot/`, `.env` e
    `.agenda/` sao caminhos.

    **Mensagens operacionais — so portugues (D-03):**

    - `l2scanner/agenda.py` 274 e 275, a chamada de 1h50. **E o texto mais lido
      do recurso inteiro**: sai duas vezes por ocorrencia de boss, para o grupo
      todo, e ja passou pelo julgamento humano do item 3 do UAT. Trocar SO os
      dois tokens de comando — nenhuma outra palavra da frase muda. A frase
      aprovada tem a forma "Mande X no PRIVADO do bot para entrar na lista, ou
      Y para sair", e com os nomes em portugues o verbo do comando passa a
      casar com o verbo da explicacao, que e justamente o que o usuario pediu;
      reescrever a redacao alem disso reabriria um julgamento que ja foi feito.
      **Nao mexer no comentario logo acima** (267-271): ele explica por que a
      chamada diz "no PRIVADO" e continua verdadeiro.
    - `l2scanner/presenca.py` 283, o erro de disco. Uma linha, um comando.
    - `l2scanner/__main__.py` 529, 551 e 592, as tres linhas de arranque no
      console. **Nao encostar** em 118, 540, 575 e 609 nem em nenhuma mencao ao
      arquivo de ambiente ou ao diretorio de loot: sao caminhos e comentarios
      internos.

    **Superficie de referencia — os dois, portugues na frente (D-03):**

    - `config.toml` 70, 75 e 76. A linha 70 e o titulo do bloco e nomeia a
      capacidade: so portugues. As linhas 75-76 sao a listinha de duas colunas
      e passam a trazer a forma portuguesa como principal com a inglesa entre
      parenteses, **na mesma ordem e no mesmo desenho que a `_AJUDA` agora
      tem** — este bloco e lido por um leigo em paralelo com a resposta do
      `/help`, e as duas divergindo e como ele aprende a sintaxe errada.
      Realinhar as duas colunas depois da troca (a palavra nova tem outro
      comprimento).
    - `README.md`, o paragrafo dos party-mates (349-351): as duas palavras
      passam a portugues. **Nao encostar** nas mencoes ao arquivo de ambiente,
      ao gitignore, ao `config.local.toml` nem ao interpretador dentro do
      ambiente virtual.
    - `README.md`, a tabela de comandos (291-300), per D-04: acrescentar as
      duas linhas de Presenca que nunca entraram la, **entre a linha de voltar
      a vigiar a party e a primeira linha de loot** — que e a posicao
      correspondente a ordem das familias na `_AJUDA`. As descricoes saem da
      propria tabela `_AJUDA`, para as duas nao divergirem. Logo abaixo da
      tabela ja existe um paragrafo que anuncia os apelidos do comando de
      ajuda; acrescentar ali uma frase dizendo que as formas inglesas continuam
      valendo no lugar das portuguesas — mesmo lugar, mesmo tom, uma frase.
      **Cuidado com o tab vertical do README**: se editar por numero de linha,
      usar `split("\n")` e nunca `splitlines()`, senao a edicao atinge a linha
      vizinha (aconteceu na quick anterior e comeu tres pontos finais).

    **Os dois testes que quebram, no MESMO commit (C-05):**

    - `tests/test_agenda.py` 365-368: converter as duas positivas para as
      palavras portuguesas e as duas negativas para as inglesas. **Manter** as
      negativas do prefixo legado que ja estao la — elas custam nada e
      preservam a prova da quick anterior. Atualizar o comentario de 360-364
      para explicar a negativa nova: a forma inglesa continua ACEITA pelo
      parser, entao so a ausencia dela AQUI prova que a chamada nao ensina mais
      a forma demovida a quem nunca usou o bot.
    - `tests/test_comandos.py` 2402: converter a asercao do `caplog` para a
      palavra portuguesa. Deixar o nome do metodo de teste como esta — ele
      nomeia a capacidade e nao a sintaxe, e renomea-lo so aumentaria o diff.

    Nao acrescentar teste novo de redacao aqui. Os portoes por contagem, com a
    contagem de partida medida, sao a prova desta tarefa; teste que amarra
    frase quebra em toda melhoria de texto e vira a rede que se aprende a
    ignorar.

    **Ao escrever qualquer comentario nesta tarefa, nao citar as formas
    inglesas com barra**: os portoes abaixo procuram esses literais dentro dos
    tres arquivos operacionais, e uma mencao ressuscitada num comentario
    reprova a tarefa com razao — o texto continuaria no arquivo.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && echo "-- operacionais, alvo 0 --" && for f in l2scanner/agenda.py l2scanner/presenca.py l2scanner/__main__.py; do echo -n "$f="; grep -c '/join\|/leave' $f || true; done && echo "-- caminhos e metodo intactos, alvo >0 --" && grep -c '\.join(' l2scanner/__main__.py && grep -c '\.loot/\|\.env' l2scanner/__main__.py && echo "-- referencia mostra os dois --" && grep -c '/entrar' config.toml && grep -c '/entrar' README.md && grep -c '/sair' README.md && python -c "
from datetime import datetime as d
from l2scanner.agenda import Aviso, TipoDeAviso, texto_do_aviso
t = texto_do_aviso(Aviso('Solo Boss', TipoDeAviso.CHAMADA, d(2026,8,27,20,0), d(2026,8,27,18,10)))
assert '/entrar' in t and '/sair' in t, t
assert '/join' not in t and '/leave' not in t, t
assert '.join' not in t and '.leave' not in t, t
assert 'privado' in t.lower(), t
print('OK chamada:', t)" && python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>

  <done>
    Os tres arquivos operacionais saem em ZERO na forma inglesa (eram 2, 1 e
    5); o metodo de string e os caminhos seguem intactos (contagem maior que
    zero); `config.toml` e `README.md` mostram as duas palavras portuguesas; a
    tabela do README lista as duas linhas de Presenca; os dois testes que
    fixavam o texto antigo foram convertidos no mesmo commit; e a suite inteira
    sai com pelo menos `1121 passed, 2 skipped` e com mais testes do que
    comecou.
  </done>
</task>

<task type="auto">
  <name>Task 3: Os itens 3 e 4 do UAT da Fase 10 citam o texto novo e voltam para a fila</name>
  <files>.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md</files>

  <precondition>A Tarefa 2 esta commitada — as citacoes so podem ser refeitas a partir da fonte viva ja alterada.</precondition>

  <action>
    Refazer as citacoes **lendo a fonte viva**, nunca copiando do plano: o texto
    da chamada sai de `l2scanner/agenda.py`, e os seis desfechos do item 4 saem
    de `l2scanner/presenca.py`. Foi essa disciplina que o proprio arquivo adotou
    no bloco "CORRECAO DE PREFIXO" quando a quick anterior mudou o texto por
    baixo dele, e ela existe porque um UAT que julga uma citacao envelhecida
    julga um produto que nao existe.

    1. **Item 3** (a chamada): trocar o bloco citado pelo texto novo e devolver
       o item para pendente, per D-05 — desmarcar a caixa, tirar a data de
       conferido, `result: [pending]`. Registrar em uma linha que ele ja tinha
       passado em 2026-08-27 e voltou porque o texto que ele julga mudou; sem
       isso, a proxima leitura conclui que o item nunca foi olhado.
    2. **Item 4** (os seis desfechos): trocar as citacoes pelos textos novos —
       so a do erro de disco muda de fato, e as outras cinco tem que ser
       CONFERIDAS contra a fonte, nao presumidas. O item sai de `issue` para
       `[pending]`, e o `reported` original fica registrado como a origem do
       conserto em vez de ser apagado: e a decisao do usuario que gerou esta
       tarefa.
    3. **O bloco `## Gaps`**: o gap 1 passa de `failed` para resolvido, citando
       a quick `260827-e1b`. Nao apagar o gap — o `root_cause` dele e o registro
       de que `/entrar` e `/sair` ja funcionavam e o defeito estava na vitrine,
       que e a coisa mais facil de reaprender errado depois.
    4. **O bloco `## Current Test` e o `## Summary`**: refazer a contagem. Com
       o item 3 voltando para pendente e o item 4 saindo de issue, os numeros
       de `passed`, `issues` e `pending` mudam os tres — e o `Summary` e o unico
       lugar do arquivo onde a conta aparece de uma vez. Atualizar tambem a
       ordem escolhida e a nota de pausa para refletir que o gap 1 foi fechado.
    5. Acrescentar um bloco curto de correcao, no formato do "CORRECAO DE
       PREFIXO" que ja existe, dizendo o que mudou, quando, e de quais linhas de
       quais arquivos as citacoes foram refeitas. E o que permite conferir a
       citacao contra a fonte sem reler o diff inteiro.

    Nao tocar nos itens 1, 2, 5 e 6: o item 6 nao cita comando nenhum (o proprio
    arquivo ja registra isso), o item 2 julga o bloco `[[membro]]` como
    LEGIBILIDADE PARA LEIGO e continua valendo — o desenho do bloco nao mudou,
    so as duas palavras dentro dele —, e os itens 1 e 5 seguem bloqueados em
    campo.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && U=.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md && echo "-- o UAT nao cita mais a forma demovida, alvo 0 --" && grep -c '/join\|/leave' $U || true && echo "-- e cita a nova, alvo >0 --" && grep -c '/entrar' $U && grep -c '/sair' $U && python -c "
import re, pathlib
u = pathlib.Path('.planning/phases/10-lista-de-presenca-do-solo-boss-pelo-whatsapp/10-UAT.md').read_text(encoding='utf-8')
from l2scanner.agenda import Aviso, TipoDeAviso, texto_do_aviso
from datetime import datetime as d
t = texto_do_aviso(Aviso('Solo Boss', TipoDeAviso.CHAMADA, d(2026,8,27,22,0), d(2026,8,27,20,10)))
assert t in u, 'a citacao do item 3 nao bate com a fonte viva:\n' + t
s = re.search(r'^total: (\d+)\npassed: (\d+)\nissues: (\d+)\npending: (\d+)', u, re.M)
assert s, 'bloco Summary nao encontrado ou fora de formato'
tot, ok, iss, pend = (int(g) for g in s.groups())
assert tot == 6 and iss == 0 and ok + iss + pend <= tot, s.group(0)
print('OK uat', s.group(0).replace(chr(10), ' | '))" && python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>

  <done>
    O `10-UAT.md` nao cita mais nenhuma das formas inglesas, a citacao do item 3
    bate caractere a caractere com o que `agenda.py` produz hoje, os itens 3 e 4
    estao pendentes de re-teste com a razao registrada, o gap 1 esta fechado
    citando esta quick, e o `Summary` fecha a conta. A suite continua no piso.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp -> Chatwoot -> `comandos_novos` | Texto de terceiro entra e vira acao. Esta tarefa **nao mexe** nessa fronteira: `_VOCABULARIO` fica identico, entao o conjunto de entradas aceitas nao muda em um caractere. |
| `[[membro]]` vs `CHATWOOT_TELEFONES_COMANDO` | Os dois niveis de autorizacao. Nao tocados; a interpretacao continua entregando os mesmos `Comando` na mesma fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e1b-01 | Denial of Service | `_VOCABULARIO` (comandos.py:213-216) | high | mitigate | Remover uma entrada por engano ao "inverter" tiraria do ar um comando que cinco pessoas ja decoraram, **em silencio** — nao existe resposta de recusa. Mitigado por asercao direta dos quatro nomes pelo caminho real nos dois prefixos (Tarefa 1) e por portao de `git diff` sem linha de delecao no bloco. |
| T-e1b-02 | Spoofing | superficie anunciada vs superficie aceita | medium | mitigate | Anunciar um apelido que nao funciona ensina sintaxe morta e faz quem digitou concluir que o bot caiu. Ate hoje so o campo `sintaxe` tinha prova ponta-a-ponta; a Tarefa 1 estende a prova a **todo apelido anunciado**, o que e exatamente a lacuna aberta por demover o ingles a apelido. |
| T-e1b-03 | Elevation of Privilege | `autorizado_para` / `COMANDOS_DE_MEMBRO` | high | mitigate | Nao tocados. A trava de autorizacao roda DEPOIS da interpretacao e recebe o mesmo `Comando` de sempre; a troca de vitrine nao amplia o que um `[[membro]]` alcanca. Diff que encoste em qualquer um dos dois esta fora de escopo. |
| T-e1b-04 | Tampering | edicao por numero de linha em `README.md` e `__main__.py` | medium | mitigate | O tab vertical do README desalinha `splitlines()` do `grep -n` e ja comeu pontuacao uma vez; `.loot/`, `.env` e o metodo de string de juntar lista sao alvos falsos de qualquer varredura cega. Mitigado por edicao ocorrencia a ocorrencia com asercao previa, proibicao explicita de `sed`, e portoes que so casam a forma com barra. |
| T-e1b-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalacao de pacote nesta tarefa (C-01, zero dependencia nova). Sem superficie de cadeia de suprimentos. |
</threat_model>

<verification>
1. `python -m pytest tests/ -q` sai com pelo menos `1121 passed, 2 skipped` e com
   mais testes do que comecou (C-04).
2. Os quatro nomes atravessam `comandos_novos` com as cinco travas ligadas, nos
   dois prefixos aceitos.
3. `git diff l2scanner/comandos.py` nao tem nenhuma linha de delecao dentro do
   bloco 213-216 do `_VOCABULARIO` (C-02).
4. Os tres arquivos operacionais saem em zero na forma inglesa; o metodo de
   string e os caminhos seguem intactos.
5. `git diff --stat` nao lista `pyproject.toml` (C-01), e `git diff` filtrado por
   `autorizado_para` e `COMANDOS_DE_MEMBRO` sai vazio.
6. `python -m ruff check` limpo nos arquivos tocados (os erros pre-existentes do
   repo, em outros arquivos, continuam la de proposito).
7. `git add` por caminho explicito em todos os commits: `.gsd/` nao esta
   rastreado nem ignorado e um `git add -A` o varreria para dentro.
</verification>

<success_criteria>
- O que o produto ANUNCIA para entrar e sair da lista e portugues, no bot, no
  `config.toml` e no README.
- Os quatro nomes continuam funcionando, e o ingles continua ANUNCIADO como
  apelido — provado pelo caminho real, nao por leitura.
- Os dois testes que fixavam o texto antigo foram atualizados no mesmo commit da
  mudanca que os quebra.
- Os itens 3 e 4 do `10-UAT.md` citam o texto novo e estao de volta na fila de
  re-teste; o gap 1 esta fechado.
- Suite verde, sem regressao, com mais testes do que comecou.
</success_criteria>

<output>
Create `.planning/quick/260827-e1b-o-vocabulario-oficial-da-lista-de-presen/260827-e1b-SUMMARY.md` when done
</output>
