---
phase: quick-260825-t1n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/comandos.py
  - l2scanner/__main__.py
  - tests/test_comandos.py
  - README.md
autonomous: true
requirements: [QUICK-260825-t1n]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.help` mandado de um telefone autorizado responde com a lista de TODOS os comandos que o scanner obedece hoje (D-01)."
    - "O texto da ajuda e DERIVADO de uma tabela chaveada pelo enum `Comando`, nunca escrito a mao num literal solto: acrescentar um membro ao enum sem documenta-lo QUEBRA a suite (D-02, D-06)."
    - "Cada sintaxe canonica anunciada pela ajuda, passada de volta pelo caminho real de leitura de comando, devolve exatamente o `Comando` que a ajuda diz — a ajuda nao tem como ensinar sintaxe que nao funciona (D-02)."
    - "Os comandos DINAMICOS (`.loot-<nick>`, `.loot-`, `.<nick>`, `.corrigir-<nick>`, `.pegou <hora> <nick>`), que nao moram no `_VOCABULARIO`, aparecem na ajuda pelo membro do enum (D-06)."
    - "A resposta sai SO na conversa de origem: `avisar_o_grupo=False`, mesmo racional ja documentado no ramo do `.status` (D-03)."
    - "A ajuda NAO e moldurada em NENHUM dos dois destinos — nem no WhatsApp nem no console — porque `moldurar` tem largura casada com a linha mais longa e o texto tem ~11 linhas (D-04)."
    - "`.help` de telefone FORA da allowlist e ignorado como qualquer outro comando: ele nao ganha portao proprio nem etiqueta nova, atravessa as cinco travas que ja existem (D-07)."
    - "Os comandos aparecem agrupados por familia na ordem vigilancia, silencio, loot, ajuda, com UMA forma canonica cada (D-05)."
    - "A suite inteira segue verde (730 testes hoje), sem o jogo aberto e sem rede."
  artifacts:
    - "l2scanner/comandos.py — `Comando.AJUDA`, entrada no `_VOCABULARIO`, a tabela `_AJUDA` chaveada pelo enum e `texto_de_ajuda()`"
    - "l2scanner/__main__.py — ramo `Comando.AJUDA` em `atender_comandos` e o desvio de moldura para resposta de varias linhas"
    - "tests/test_comandos.py — tripwire de cobertura da tabela, round-trip de sintaxe, destino unico, allowlist e ausencia de moldura"
    - "README.md — linhas de `.help`, `.solo` e `.party` na tabela de comandos"
  key_links:
    - "`set(_AJUDA) == set(Comando)` — o tripwire e o unico mecanismo que impede a ajuda de envelhecer; sem ele a tabela e so mais um literal para esquecer"
    - "`comandos_novos` -> `interpretar` -> `interpretar_dinamico` — o round-trip precisa atravessar ESTE caminho, nao um parser paralelo do teste, senao ele prova a coisa errada"
    - "`atender_comandos` -> `log.info(destacar(resposta))` — `destacar` chama `moldurar`, entao o console e o SEGUNDO destino que a decisao (d) alcanca"
---

<objective>
Acrescentar o comando `.help`, que responde com a lista de todos os comandos que o
scanner obedece — e faze-lo de um jeito que nao possa envelhecer.

Purpose: o projeto ganhou CINCO comandos em UM dia (`.loot-`, `.<nick>`,
`.loot-cancelar`, `.corrigir`, `.pegou`). Uma ajuda escrita a mao estaria
desatualizada antes do fim da semana, e ajuda desatualizada e PIOR que ajuda
nenhuma: ela ensina sintaxe que nao funciona e faz o usuario concluir que o bot
esta quebrado. Por isso o texto e derivado de uma tabela com tripwire, e nao um
literal.

Output: `Comando.AJUDA` + tabela `_AJUDA` + `texto_de_ajuda()` em `comandos.py`,
o ramo correspondente em `atender_comandos`, os testes de tripwire e de costura,
e a linha do README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@l2scanner/comandos.py
@l2scanner/__main__.py
@tests/test_comandos.py
@l2scanner/console.py
</context>

<decisoes_travadas>
Vindas da conversa com o usuario. NAO reabrir nenhuma delas.

- **D-01** — `.help` entra no `_VOCABULARIO` fixo como `Comando.AJUDA`, com as
  formas `help`, `ajuda` e `comandos`.
- **D-02** — **O TEXTO DA AJUDA E DERIVADO, NAO ESCRITO A MAO.** Uma tabela em
  `comandos.py` mapeando cada membro do enum `Comando` para (sintaxe canonica,
  descricao curta), mais um TESTE-TRIPWIRE no estilo do
  `test_o_vocabulario_e_fechado` que ja existe, afirmando que TODO membro de
  `Comando` tem entrada na tabela.
- **D-03** — A resposta sai SO na conversa de origem, `avisar_o_grupo=False`,
  mesma razao ja documentada no ramo do `.status`: e pergunta pessoal, e ecoar
  no grupo e ruido para quem nao perguntou.
- **D-04** — A ajuda NAO passa por `console.moldurar()`. Sao ~11 linhas e a
  moldura tem largura fixa casada com a linha mais longa; no celular isso
  quebraria feio.
- **D-05** — Agrupar por familia, nesta ordem: vigilancia (`.status`, `.solo`,
  `.party`), silencio (`.cancelar`), loot (`.loot-<nick>`, `.loot-`, `.<nick>`,
  `.corrigir-<nick>`, `.pegou`), e a propria ajuda. UMA forma canonica por
  comando, com apelidos entre parenteses so quando ajudarem — nao despejar o
  vocabulario inteiro, que tem 11 chaves para 4 comandos fixos.
- **D-06** — Os comandos DINAMICOS nao estao no `_VOCABULARIO`; eles vivem em
  `interpretar_dinamico`. Eles entram na tabela de ajuda pelo membro do enum
  correspondente, e e por isso que a tabela e chaveada pelo ENUM e nao pelo
  `_VOCABULARIO`.
- **D-07** — **NAO existe portao novo.** O scanner ja tem CINCO travas em serie
  que todo comando atravessa: conversa configurada ou etiquetada, `message_type`
  incoming, nunca nota privada, dedupe por id, e allowlist de telefone. Quem nao
  passa por elas nunca e atendido, em comando nenhum. Logo `.help` sendo um
  comando comum JA e "so para quem tem a tag admin". Nao inventar uma segunda
  etiqueta nem um segundo portao — seria mais uma trava para manter e para o
  usuario configurar errado.
</decisoes_travadas>

<achados_da_leitura_do_codigo>
Ja apurado nesta sessao, contra o codigo real. Confira, mas nao redescubra do zero.

1. **`.?` CABE no parser, sem gambiarra.** `interpretar` faz
   `miolo = palavra[len(PREFIXO):].lower()` e depois
   `_VOCABULARIO.get(miolo.replace("-", "").replace("_", ""))` — uma consulta de
   dicionario, nao uma validacao de charset. `"?"` atravessa os dois `replace`
   intacto e nao casa `_NICK_VALIDO`, entao nao ha colisao com o ramo de
   consulta por nick. Nenhum teste faz afirmacao sobre as chaves do
   `_VOCABULARIO` (verificado: os unicos usos sao as tres linhas de
   `comandos.py` mais a linha 344). **Incluir `"?"`**, com teste. Se, ao
   executar, algo desmentir isto, deixe `.?` de fora e registre o porque num
   comentario ao lado do `_VOCABULARIO` — nunca force.

2. **O inventario do enum bate com o da conversa.** Hoje: `CANCELAR_SILENCIO`,
   `STATUS`, `SOLO`, `PARTY`, `LOOT_DESIGNAR`, `LOOT_CONSULTA`, `LOOT_CANCELAR`,
   `LOOT_CORRIGIR`, `LOOT_ATRIBUIR`. Confira de novo antes de escrever a tabela
   — outra sessao commitou neste mesmo branch hoje.

3. **Sintaxe real dos dinamicos, lida em `interpretar_dinamico`:**
   `.loot-<nick>` (tambem `.loot <nick>`); `.loot-` (tambem `.loot cancelar`,
   `ninguem`, `nenhum`, `limpar`; `.loot` sozinho NAO faz nada, de proposito);
   `.corrigir-<nick>` (tambem `.corrigir <nick>`); `.pegou <hora> <nick>` com
   data opcional na frente e a forma `.pegou-<hora> <nick>`, gramatica validada
   por `interpretar_pegou` no `loot.py`; `.<nick>` sozinho, so para nick
   conhecido.

4. **A decisao (d) alcanca DOIS destinos, nao um.** `moldurar` nao e aplicado
   pelo `Despachante` — as respostas de comando saem cruas no WhatsApp, entao o
   lado do celular ja esta certo por construcao e o teste ali e guarda de
   regressao. Mas `atender_comandos` faz `log.info(destacar(resposta))`, e
   `destacar` chama `moldurar`. Com texto de varias linhas,
   `largura = max(LARGURA, len(miolo) + len(carimbo))` conta a string INTEIRA
   com os `\n` dentro: o console e o `scanner.log` sairiam com uma borda de
   centenas de asteriscos. Isso e defeito, e o conserto pertence a esta tarefa.
</achados_da_leitura_do_codigo>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: a tabela de ajuda, o tripwire, e o texto que sai dela</name>
  <files>l2scanner/comandos.py, tests/test_comandos.py</files>
  <behavior>
    Testes primeiro, em `tests/test_comandos.py`, numa classe nova
    `TestAjuda` (docstring explicando o PORQUE: cinco comandos em um dia).

    - TRIPWIRE DE COBERTURA (o teste central de D-02/D-06):
      `set(_AJUDA) == set(Comando)`, com mensagem de falha que diz o que fazer
      ("comando novo sem entrada na tabela de ajuda: {faltando}"). Ele falha
      quando alguem acrescentar um membro ao enum e esquecer de documenta-lo.
    - ROUND-TRIP DE SINTAXE (a prova de que a ajuda nao ensina o que nao
      funciona): para CADA entrada da tabela, tome a `sintaxe`, troque
      `<nick>` por `J4guar` e `<hora>` por `18:00`, monte uma mensagem falsa
      (`message_type` 0, `private` False, id unico, `sender` com o telefone
      autorizado) e passe por `comandos_novos(..., nicks_conhecidos=...)`
      — o CAMINHO REAL, com as cinco travas, nao um parser paralelo do teste.
      Afirme que sai exatamente o `Comando` que chaveia aquela entrada.
      Use `apelido("J4guar")` para montar `nicks_conhecidos`, senao o
      `.<nick>` morre no portao de nick conhecido.
    - O texto de `texto_de_ajuda()` CITA cada `sintaxe` da tabela (pega o caso
      em que uma familia inteira deixa de ser emitida pelo agrupamento).
    - `interpretar(".help")`, `".ajuda"`, `".comandos"` e `".HELP"` devolvem
      `Comando.AJUDA`; e `".?"` tambem, conforme o achado 1.
    - `interpretar("help")` sem ponto devolve None — a ajuda nao escapa da
      regra do prefixo so por ser inofensiva.
    - `texto_de_ajuda()` nao contem nenhuma linha feita so de `*` (guarda de
      D-04 no proprio produtor do texto).

    Depois o codigo, em `l2scanner/comandos.py`.
  </behavior>
  <action>
    Acrescente `AJUDA = "ajuda"` ao enum `Comando`, com comentario curto no
    estilo do arquivo dizendo o que ele e: o unico comando que nao muda estado
    nenhum, e o unico cujo conteudo e DERIVADO dos outros.

    Acrescente ao `_VOCABULARIO` as chaves `"help"`, `"ajuda"`, `"comandos"` e
    `"?"` apontando para `Comando.AJUDA`.

    Crie, logo abaixo do `_VOCABULARIO`, um dataclass frozen `LinhaDeAjuda` com
    os campos `familia: str`, `sintaxe: str`, `descricao: str` e
    `apelidos: tuple[str, ...] = ()`, e a tabela
    `_AJUDA: dict[Comando, LinhaDeAjuda]` chaveada pelo ENUM e escrita NA ORDEM
    DE EXIBICAO de D-05 (dict preserva ordem de insercao, entao a ordem da
    tabela e a ordem da resposta — uma fonte so).

    Familias e conteudo, nesta ordem:
    - "Vigilancia": STATUS `.status`; SOLO `.solo`; PARTY `.party`
      (apelido `.pt`).
    - "Silencio": CANCELAR_SILENCIO `.cancelar`.
    - "Loot do Solo Boss": LOOT_DESIGNAR `.loot-<nick>`; LOOT_CANCELAR
      `.loot-`; LOOT_CONSULTA `.<nick>`; LOOT_CORRIGIR `.corrigir-<nick>`;
      LOOT_ATRIBUIR `.pegou <hora> <nick>`.
    - "Ajuda": AJUDA `.help` (apelidos `.ajuda`, `.comandos`).
    Descricoes curtas, uma linha, no espirito da tabela do README que ja
    existe. A do LOOT_ATRIBUIR pode citar um exemplo dentro da propria
    descricao, ja que a sintaxe sozinha nao ensina o formato do horario.

    Escreva `texto_de_ajuda() -> str`: percorre `_AJUDA` na ordem de insercao,
    abre com uma linha de titulo, emite o nome da familia quando ela muda, e
    uma linha por comando no formato `sintaxe` + separador + `descricao`, com
    os apelidos entre parenteses depois da sintaxe quando houver. Use o mesmo
    travessao das outras mensagens de saida do projeto. Sem moldura, sem ANSI,
    sem `rich` — este texto vai para o celular.

    A docstring de `texto_de_ajuda` e da tabela precisa carregar o PORQUE de
    D-02, com o dado concreto (cinco comandos novos em um dia) e a frase que
    justifica o tripwire: ajuda desatualizada e pior que ajuda nenhuma, porque
    ensina sintaxe que nao funciona. A docstring da tabela explica tambem
    por que a chave e o ENUM e nao o `_VOCABULARIO` (D-06: os dinamicos nao
    moram no vocabulario).

    Se a verificacao do `.?` desmentir o achado 1, tire a chave `"?"` e deixe
    um comentario de uma linha ao lado do `_VOCABULARIO` dizendo o que
    impediu; nao invente caso especial no parser para acomoda-lo.
  </action>
  <verify>
    <automated>.venv\Scripts\python -m pytest tests/test_comandos.py -q</automated>
  </verify>
  <done>
    `set(_AJUDA) == set(Comando)` e verificado por teste; toda sintaxe da tabela
    volta pelo `comandos_novos` como o comando certo; `.help`, `.ajuda`,
    `.comandos` e `.?` resolvem para `Comando.AJUDA`; `texto_de_ajuda()` cita
    todas as sintaxes, agrupadas na ordem de D-05, e nao tem linha de moldura.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a costura — responder onde perguntaram, sem moldura, e a linha do README</name>
  <files>l2scanner/__main__.py, tests/test_comandos.py, README.md</files>
  <behavior>
    Testes primeiro, reaproveitando os helpers `_atender` que ja existem em
    `TestRespondeOndePerguntaram` (mesmo formato de `LeitorFalso`,
    `NotificadorEmMemoria` e `Despachante`):

    - `.help` vindo da conversa "1" produz resposta e
      `[alvo for _, alvo in destinos] == ["1"]` — SO a origem, o grupo nao ve
      (D-03).
    - O texto despachado cita as sintaxes da tabela (a costura entrega mesmo o
      produto de `texto_de_ajuda()`, nao um resumo).
    - O texto despachado NAO e moldurado: nenhuma linha feita so de `*`
      (D-04).
    - O que vai para o LOG tambem nao e moldurado: com `caplog` em INFO,
      nenhuma linha do registro da resposta e uma borda de asteriscos. E o
      achado 4 — `destacar` chama `moldurar`, e a conta de largura de
      `moldurar` conta a string inteira com os `\n` dentro.
    - `.help` de um telefone FORA da allowlist e IGNORADO: `LeitorFalso` com
      `telefones = ["+5544997077000"]` e `sender` com outro numero produz zero
      destinos (D-07 — a ajuda nao tem portao proprio; ela usa os que ja
      existem).
    - Guarda direta do desvio de moldura, como funcao pura: texto de UMA linha
      sai moldurado, texto de VARIAS linhas sai cru.

    Depois o codigo.
  </behavior>
  <action>
    Em `l2scanner/__main__.py`, acrescente o ramo `Comando.AJUDA` em
    `atender_comandos`, entre os `elif` existentes:
    `resposta = texto_de_ajuda()` e `avisar_o_grupo = False`, com o comentario
    no estilo dos vizinhos explicando o PORQUE (D-03: pergunta pessoal, mesmo
    racional ja escrito no ramo do `.status`; ecoar a lista inteira no grupo
    seria ruido para quem nao perguntou). Importe `texto_de_ajuda` junto dos
    outros nomes que ja vem de `.comandos`.

    Conserte a moldura do console: extraia uma funcao pequena — sugestao,
    `_para_o_console(resposta: str) -> str` — que devolve `destacar(resposta)`
    quando a resposta cabe numa linha e a resposta CRUA quando ela tem `\n`, e
    troque `log.info(destacar(resposta))` por ela. A docstring explica a conta:
    `moldurar` monta `largura = max(LARGURA, len(miolo) + len(carimbo))` sobre
    a string inteira, entao um texto de varias linhas vira uma borda de
    centenas de asteriscos no console e no `scanner.log`. E a mesma razao de
    D-04, aplicada ao segundo destino — uma regra so para os dois, e nao um
    caso especial para o `.help`.

    No `README.md`, na tabela "Comandos, sempre com ponto na frente": acrescente
    a linha do `.help` e aproveite para incluir `.solo` e `.party`, que existem
    desde a quick de 24/08 e nunca entraram na tabela — e exatamente o
    envelhecimento que D-02 previne do lado do bot, e vale consertar do lado do
    README. Uma frase curta abaixo da tabela pode dizer que `.help` responde
    com esta mesma lista direto no WhatsApp, para quem estiver no jogo.
  </action>
  <verify>
    <automated>.venv\Scripts\python -m pytest -q</automated>
  </verify>
  <done>
    A suite inteira verde (730+ testes). `.help` responde so na conversa de
    origem, sem moldura no WhatsApp e sem moldura no console, e e ignorado
    quando o telefone nao esta na allowlist. README com `.help`, `.solo` e
    `.party` na tabela.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp -> Chatwoot -> `LeitorDeComandos` | Texto de terceiro entra no processo e vira acao |
| `atender_comandos` -> `Despachante` | Texto do scanner sai para o grupo / para o autor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-t1n-01 | Information Disclosure | `texto_de_ajuda` | medium | mitigate | A lista de comandos e um mapa da superficie de ataque. Ela sai pelas MESMAS cinco travas dos outros comandos (conversa, incoming, nao-privada, dedupe, allowlist de telefone) e `avisar_o_grupo=False` impede o eco para quem nao pediu (D-03, D-07). Teste dedicado: telefone fora da allowlist recebe zero destinos. |
| T-t1n-02 | Information Disclosure | tabela `_AJUDA` | low | mitigate | A tabela guarda so sintaxe e descricao. Nunca colocar ali nome de conversa, id, telefone, caminho de disco ou qualquer coisa vinda do `.env`. |
| T-t1n-03 | Denial of Service | resposta multilinha | low | mitigate | Uma resposta de ~11 linhas por comando, atras do dedupe por id de mensagem, que ja impede repeticao em laco. Sem novo caminho de rede e sem novo estado duravel. |
| T-t1n-04 | Tampering | dependencias | low | accept | Zero pacote novo — so stdlib e o que ja esta no `pyproject`. Nao ha instalacao de pacote nesta tarefa, logo nao ha portao de legitimidade a atravessar. |
</threat_model>

<verification>
- `.venv\Scripts\python -m pytest -q` — suite inteira verde, sem jogo aberto e
  sem rede.
- `.venv\Scripts\python -m ruff check l2scanner tests` e
  `.venv\Scripts\python -m ruff format --check l2scanner tests`.
- Prova do tripwire por MUTACAO, e ela nao e opcional: acrescente um membro
  descartavel ao enum `Comando` sem toca-lo na tabela, rode
  `pytest tests/test_comandos.py -q`, confirme que o tripwire FALHA nomeando o
  membro, e desfaca. Um tripwire que nao foi visto falhando e so mais um teste
  verde.
- Olhar o texto uma vez, como ele chega no celular:
  `.venv\Scripts\python -c "from l2scanner.comandos import texto_de_ajuda; print(texto_de_ajuda())"`
  — conferir que as familias saem na ordem de D-05 e que nenhuma linha e longa
  a ponto de quebrar feio numa tela de telefone.
</verification>

<success_criteria>
- `.help` (e `.ajuda`, `.comandos`, `.?`) responde com a lista completa dos
  comandos, derivada da tabela.
- Comando novo sem entrada na tabela de ajuda quebra a suite — verificado por
  mutacao, nao so por leitura.
- Toda sintaxe anunciada volta pelo `comandos_novos` como o comando certo.
- Resposta so na conversa de origem, sem moldura no WhatsApp nem no console.
- Telefone fora da allowlist continua sem resposta nenhuma.
- README com `.help`, `.solo` e `.party` na tabela de comandos.
</success_criteria>

<output>
Create `.planning/quick/260825-t1n-comando-help-listando-todos-os-comandos-/260825-t1n-SUMMARY.md` when done
</output>
