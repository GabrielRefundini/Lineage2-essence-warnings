---
phase: quick-260825-c6g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/relogio.py
  - l2scanner/__main__.py
  - tests/test_relogio.py
  - tests/test_relogio_no_laco.py
autonomous: true
requirements: [QUICK-260825-c6g]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Depois de ancorar no servidor, mudar o relogio do Windows em 3 horas NAO move a hora que o scanner usa para a agenda."
    - "Sem rede, sem .env e sem Chatwoot, o scanner SOBE e funciona — cai no relogio do Windows e diz isso em WARNING, uma vez, no arranque."
    - "Quando o relogio do Windows esta errado, o arranque diz de quanto e o erro e em que direcao, citando o dual boot como causa provavel."
    - "A hora que alimenta a agenda (avisos de TvT/Prime e janela de silencio) vem do relogio ancorado, nao de datetime.now()."
    - "O replay continua usando os horarios GRAVADOS: frame.momento tem precedencia sobre o relogio ancorado."
    - "Nenhuma chamada de time.monotonic() do caminho de deteccao (debounce, cooldown, staleness, ritmo do laco) foi tocada."
    - "Zero dependencia nova: relogio.py importa somente stdlib."
  artifacts:
    - "l2scanner/relogio.py — Relogio (ancora + monotonico), epoch_do_cabecalho_date, fonte_chatwoot"
    - "tests/test_relogio.py — ancoragem, fallback com aviso, e a imunidade a pulo do relogio de parede"
    - "tests/test_relogio_no_laco.py — a agenda le a hora ancorada, e montar_relogio nao levanta sem rede"
  key_links:
    - "fonte_chatwoot -> cabecalho Date da resposta HTTP (inclusive de HTTPError) -> Relogio.ancorar"
    - "Relogio.agora_epoch -> momento em laco_principal -> Sessao.tick -> datetime.fromtimestamp -> avisos_devidos/silencio_ativo"
    - "Relogio.agora -> laco_da_agenda, _anunciar_proximo, comando_teste_de_agenda, comando_cancelar_silencio"
    - "montar_relogio -> config_do_chatwoot (URL); ConfigAusente NAO pode derrubar o scanner"
---

<objective>
Fazer o scanner parar de perguntar as horas para um relogio que mente.

O PC do usuario e dual boot. O Linux grava o relogio do hardware em UTC, o Windows le o
mesmo valor como hora local, e ao voltar do Linux o Windows fica ~3h adiantado ate
sincronizar sozinho. Palavras dele: "as vezes ele muda pois eu logo no linux e volta um
horario aleatorio".

A DETECCAO ESTA SEGURA e nao entra neste plano: debounce, cooldown, staleness e o ritmo
do laco usam `time.monotonic()` (`__main__.py:811,843,868,878`, `notificador.py:436`).
Monotonico nao pula. Nao mexer nisso e um requisito, nao uma cortesia.

QUEM QUEBRA E A AGENDA. Ela le `datetime.now()` a cada tick e com 3h de erro o silencio de
TvT/Prime entra e sai na hora errada e o aviso do evento provavelmente NAO SAI NENHUMA VEZ:
`agenda.py` so dispara dentro de `TOLERANCIA_MINUTOS = 5` depois do alvo, e um pulo de 3h
atravessa a janela inteira sem tocar nela. Falha em silencio — o pior modo de falha que
este projeto tem.

Purpose: um unico lugar decide que horas sao, e ele nao depende do relogio da maquina.
Output: `l2scanner/relogio.py` e a fiacao dos 6 lugares de `__main__.py` que hoje chamam
`datetime.now()` / `time.time()` para fins de agenda — 7 chamadas ao todo, nas linhas
499, 580, 583, 631, 635, 654 e 770, mais o `time.time()` de 835.
</objective>

<decisoes_ja_tomadas>
Fechadas pelo usuario. Nao reabrir, nao pesquisar alternativa, nao propor melhoria.

- **A hora vem do cabecalho `Date` da resposta HTTP do Chatwoot.** Zero dependencia nova,
  zero porta nova, stdlib pura. Reusa o servidor e a porta 443 que o scanner ja usa e ja
  sabe que atravessam o firewall. O acoplamento e de graca: se o Chatwoot estiver fora, o
  scanner ja esta mudo de qualquer jeito.
- **NTP esta descartado.** Nada de `ntplib`, nada de porta UDP 123.
- **Ancorar + contar pelo monotonico.** Pega a hora do servidor junto com um
  `time.monotonic()` do mesmo instante; dai em diante `agora = ancora + (monotonic() -
  monotonic_da_ancora)`. Isso da um relogio certo E imune a pulo, inclusive a uma
  re-sincronizacao do Windows no meio da sessao.
- **Falhou a fonte externa: cai no relogio do Windows e DIZ isso alto.** Nunca fingir
  precisao que nao tem. O scanner NUNCA pode deixar de subir por causa disso.
- **O fuso continua vindo do Windows, e esta certo.** O dual boot estraga o RELOGIO, nao a
  configuracao de fuso. Hora certa de fora + fuso intacto = hora local certa, entao
  `datetime.fromtimestamp()` normal resolve a conversao.
</decisoes_ja_tomadas>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@l2scanner/agenda.py
@l2scanner/sessao.py
@l2scanner/notificador.py
@l2scanner/__main__.py
@tests/test_notificador.py
</context>

<convencoes>
- Codigo, comentarios, docstrings, nomes de teste e textos ao usuario em **portugues SEM
  ACENTOS no codigo**.
- Comentario explica o **PORQUE**, citando a falha real que motivou o codigo. Comentario
  que descreve o que a linha faz nao entra.
- Testes em `tests/`, docstring do teste explica o porque.
- Rodar com `python -m pytest` (o **`.venv` NAO tem pytest**; o `python` do sistema tem,
  9.1.1 em CPython 3.12.10). `uv` NAO esta no PATH.
- O tempo entra por parametro. `agenda.py` nao tem relogio proprio e isso e o que torna
  esta mudanca barata — preserve a disciplina: `relogio.py` tambem recebe suas fontes por
  parametro, para que os testes nao precisem de rede nem de `monkeypatch`.
</convencoes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: o relogio ancorado — modulo novo, com as tres propriedades testadas</name>
  <files>l2scanner/relogio.py, tests/test_relogio.py</files>
  <read_first>
    - `l2scanner/agenda.py` (docstring do modulo: "nao ha relogio proprio, o tempo entra
      por parametro" — e a disciplina a preservar)
    - `l2scanner/notificador.py:275-302` (`_postar`: como o projeto ja fala HTTP com
      `urllib.request`, o `USER_AGENT` de :43 e por que ele existe)
    - `tests/test_notificador.py:1-55` (estilo: docstring do teste explica o porque)
  </read_first>

  <behavior>
    Escreva `tests/test_relogio.py` PRIMEIRO, com estes casos. Os tres primeiros sao os
    exigidos pelo usuario; os demais fecham as bordas.

    - **Ancoragem bem-sucedida**: com `fonte` devolvendo um epoch conhecido, `agora_epoch()`
      volta esse epoch (nao o da maquina) e `confiavel` e True.
    - **Fonte indisponivel**: `fonte` levantando `OSError` -> `sincronizar()` devolve False,
      `confiavel` e False, `agora_epoch()` devolve o relogio de parede injetado, e NADA e
      levantado para fora.
    - **A PROPRIEDADE CENTRAL — pulo do relogio do sistema**: ancora; depois o relogio de
      parede injetado PULA +3h (o dual boot voltando do Linux) enquanto o monotonico anda
      so 10s; `agora_epoch()` andou 10s, nao 3h. Este e o teste que representa o bug.
    - **O contrario tambem**: sem ancora, o pulo de +3h APARECE (o fallback e honesto, nao
      finge estabilidade que nao tem).
    - **`epoch_do_cabecalho_date`**: `"Mon, 25 Aug 2026 11:46:09 GMT"` vira o epoch UTC
      correto; lixo (`""`, `"nao e data"`, `None`) vira `None` sem levantar; data absurda
      (ano < 2020) vira `None` — um proxy quebrado nao pode reancorar o scanner em 1970.
    - **Compensacao de latencia**: a ancora e presa ao PONTO MEDIO da requisicao
      (`(mono_antes + mono_depois) / 2`), nao ao fim. Com uma fonte que consome 2s de
      monotonico, o erro resultante e <= 1s.
    - **Desvio medido**: `desvio_do_windows()` devolve, em segundos com sinal, o quanto o
      relogio da maquina esta adiantado em relacao a ancora — e `None` quando nao ancorado.
      E o numero que o arranque vai imprimir para o usuario.
    - **Re-ancorar nao anda para tras**: uma segunda `sincronizar()` com um epoch novo troca
      a ancora e a contagem continua a partir dela.
  </behavior>

  <action>
Criar `l2scanner/relogio.py`, somente stdlib (`time`, `datetime`, `email.utils`, `logging`,
`threading`, `urllib.request`, `urllib.error`).

**`epoch_do_cabecalho_date(valor: str | None) -> float | None`**
Le o formato RFC 7231 (`Mon, 25 Aug 2026 11:46:09 GMT`) com
`email.utils.parsedate_to_datetime`, que devolve um datetime AWARE em UTC, e converte com
`.timestamp()`. Devolve `None` em qualquer entrada que nao case, e tambem quando o ano for
menor que 2020 — uma resposta de intermediario quebrado nao pode virar ancora. Nunca
levanta.

**`class Relogio`**
Construtor recebe TUDO por parametro, para o teste nao precisar de rede nem de
`monkeypatch`: `fonte` (chamavel que devolve `float | None`, o epoch do servidor),
`monotonico` (default `time.monotonic`) e `parede` (default `time.time`). Estado interno:
`_ancora_epoch: float | None` e `_ancora_mono: float`.

- `sincronizar() -> bool` — marca o monotonico ANTES, chama `fonte()`, marca DEPOIS, e
  ancora no PONTO MEDIO dos dois. O cabecalho `Date` e do instante em que o servidor
  respondeu; prender a ancora ao meio da viagem e a aproximacao classica e custa duas
  linhas. Qualquer excecao da fonte vira `False` — a fonte e rede, e rede falha. Devolve
  `True` so quando ancorou de verdade.
- `agora_epoch() -> float` — ancorado: `_ancora_epoch + (monotonico() - _ancora_mono)`. Nao
  ancorado: `parede()`. Comente o porque nesta linha: e ela que faz o pulo do Windows nao
  mover a hora do scanner.
- `agora() -> datetime` — `datetime.fromtimestamp(self.agora_epoch())`. Ingenuo, hora local,
  de proposito: a agenda inteira trabalha com datetime ingenuo e o fuso do Windows esta
  correto mesmo quando o relogio nao esta.
- `confiavel -> bool` (property) e `desvio_do_windows() -> float | None`.
- `iniciar_sincronizacao_periodica(intervalo=1800)` — thread daemon que so chama
  `sincronizar()` de tempos em tempos. Daemon e separada porque o laco de captura NUNCA
  pode bloquear em rede: e a mesma razao pela qual o `Despachante` existe. O porque de
  re-ancorar nao e deriva de cristal (irrelevante em minutos) — e que o comportamento do
  monotonico atravessando suspensao do sistema e dependente de plataforma e nao e coisa em
  que apostar; re-ancorar limita a divergencia seja qual for o comportamento.

**`fonte_chatwoot(url: str, timeout: float = TIMEOUT_ANCORA) -> Callable[[], float | None]`**
Devolve um chamavel que faz um `GET` na URL base do Chatwoot e le o cabecalho `Date` da
resposta. Tres detalhes que sao o desenho, nao acaso:
1. **Sem token.** Qualquer resposta HTTP carrega `Date`, inclusive 401/403/404. Nao mandar
   o token reduz a exposicao dele a zero neste caminho e nos livra de escolher endpoint.
2. **`HTTPError` tambem serve.** Ele expoe `.headers`; capturar e ler o `Date` dele conta
   como sucesso. So `URLError`/`TimeoutError`/`OSError` (DNS, conexao, timeout) e falha.
3. **`USER_AGENT` do notificador**, importado de `l2scanner.notificador` — o Chatwoot do
   usuario esta atras do Cloudflare e o UA padrao do urllib leva erro 1010.
`TIMEOUT_ANCORA = 3.0`: isto roda no arranque e um usuario offline nao pode esperar 10s
para o scanner subir.
  </action>

  <verify>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -m pytest tests/test_relogio.py -q</automated>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -c "import ast; a=ast.parse(open('l2scanner/relogio.py',encoding='utf-8').read()); m={(n.module or '').split('.')[0] if isinstance(n,ast.ImportFrom) else n.names[0].name.split('.')[0] for n in ast.walk(a) if isinstance(n,(ast.Import,ast.ImportFrom))}; permitido={'__future__','time','datetime','email','logging','threading','urllib','typing','collections','dataclasses','l2scanner'}; assert m<=permitido, sorted(m-permitido); print('imports ok:',sorted(m))"</automated>
  </verify>

  <done>
`tests/test_relogio.py` passa, incluindo o caso do pulo de +3h no relogio de parede depois
da ancora. `relogio.py` importa apenas stdlib (mais `l2scanner.notificador` pelo USER_AGENT).
Nenhum arquivo de deteccao foi tocado.
  </done>

  <reversibility rating="reversible">
    Modulo novo e isolado; apagar o arquivo devolve o comportamento anterior.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: ligar o relogio nos 6 lugares que hoje perguntam as horas ao Windows</name>

  <precondition>Task 1 concluida: `l2scanner/relogio.py` existe e `tests/test_relogio.py` passa.</precondition>
  <files>l2scanner/__main__.py</files>
  <read_first>
    - `l2scanner/__main__.py:125-152` (`montar_despachante` — o molde a espelhar: monta,
      degrada com WARNING, nunca levanta)
    - `l2scanner/sessao.py:130-160` (`tick`: `agora = datetime.fromtimestamp(momento)` — e
      por aqui que a agenda recebe a hora, e e por isso que `sessao.py` NAO precisa mudar)
  </read_first>

  <action>
Acrescentar `montar_relogio(args) -> Relogio` em `__main__.py`, espelhando
`montar_despachante`: monta, degrada com WARNING, e **nunca levanta**.

- Le a URL com `config_do_chatwoot()`. `ConfigAusente` -> `Relogio` sem fonte, WARNING de
  uma linha, e o scanner segue. Rodar com `--dry-run`, sem `.env` e sem rede tem que subir.
- Com config: chama `sincronizar()` UMA VEZ, sincrono, ANTES do laco. Tem que ser antes:
  se a primeira volta rodar com o relogio torto, ela pode gravar um marcador de
  "ja avisei" com a chave errada e envenenar o aviso de verdade — e o marcador e duravel.
- Ancorou: se `abs(desvio_do_windows()) > 60`, logar em WARNING de quanto e em que direcao
  o relogio da maquina esta errado, dizendo que a hora usada e a do servidor e citando o
  dual boot (o Linux grava o relogio do hardware em UTC) como causa provavel. Abaixo disso,
  uma linha de INFO discreta.
- Nao ancorou: WARNING dizendo que a hora do servidor nao veio, que o relogio do Windows
  esta sendo usado, e que quem acabou de voltar do Linux pode ver horario de TvT/Prime
  errado. Alto, uma vez, no arranque.
- Depois disso, `iniciar_sincronizacao_periodica()`.

Fiacao, ponto a ponto (linhas do arquivo de hoje):

1. **`laco_principal:835`** — `momento = frame.momento if frame.momento is not None else
   time.time()` vira `... else relogio.agora_epoch()`. **E a linha mais importante do
   plano**: esse `momento` desce para `Sessao.tick`, que faz `datetime.fromtimestamp` e
   alimenta `avisos_devidos` e `silencio_ativo`. Uma linha conserta a agenda inteira, e de
   quebra o horario dentro das mensagens de WhatsApp (`notificador.py:59` le
   `evento.momento`) e o do console (`__main__.py:687`).
   **A precedencia de `frame.momento` fica exatamente como esta**: no replay o horario e o
   GRAVADO, senao uma sessao de uma hora reproduzida em trinta segundos mediria tudo
   errado — decisao da Fase 4, nao mexer.
2. **`laco_principal:770`** — `proxima_ocorrencia(datetime.now(), ...)` -> `relogio.agora()`.
3. **`laco_da_agenda:580,583`** — o `ultimo_anuncio` e o `agora` de cada volta passam a vir
   de `relogio.agora()`. Este e o modo `--so-agenda`, que existe justamente para quem NAO
   esta com o jogo aberto; e o modo em que o relogio torto e 100% do produto.
4. **`_anunciar_proximo:631,635`** — ganha um parametro `relogio` e usa `relogio.agora()`
   nas duas chamadas. Atualizar os call sites.
5. **`comando_teste_de_agenda:654`** e **`comando_cancelar_silencio:499`** — cada um monta o
   proprio relogio e usa `relogio.agora()`. Sao processos separados; sem isso,
   `--cancelar-silencio` escolheria a ocorrencia errada com o relogio adiantado.
6. **`destacar(texto)` sem hora** (fim de silencio e avisos de agenda, em `laco_da_agenda` e
   no `for texto in resultado.avisos` de `laco_principal`) — passar
   `hora=relogio.agora().strftime("%H:%M")` por palavra-chave. `console.py` NAO e alterado:
   a assinatura ja e `destacar(texto, tipo=None, hora=None)` e mante-la pura e melhor do que
   dar um relogio a ela.

**Nao tocar**, e cada um tem dono:
- Todo `time.monotonic()` de `__main__.py`, `notificador.py` e `captura_janela.py` — e o que
  mantem morte e saida de party corretos com o relogio torto.
- `notificador.py:383` (`time.time()` no carimbo do `outbox.jsonl`) — registro de disco de
  quando a linha foi escrita, nao de quando o evento aconteceu; o evento ja carrega a hora
  ancorada dentro do texto.
- `gravador.py:29` — nome de arquivo de gravacao, cosmetico.
- `agenda.py:320` (`date.today()` em `podar`) — a poda apaga marcador com mais de 3 DIAS;
  um erro de 3h nao chega perto do limite.
  </action>

  <verify>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -m pytest -q</automated>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -c "import ast; s=open('l2scanner/__main__.py',encoding='utf-8').read(); a=ast.parse(s); nows=[n for n in ast.walk(a) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='now']; assert not nows, 'ainda ha .now() nas linhas '+str([n.lineno for n in nows]); assert 'relogio.agora_epoch()' in s, 'o seam do momento nao foi ligado'; assert s.count('frame.momento if frame.momento is not None')==1, 'a precedencia do replay mudou'; print('fiacao ok')"</automated>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && TOCADOS=$(git diff --name-only -- l2scanner/rastreador.py l2scanner/captura_janela.py l2scanner/notificador.py l2scanner/sessao.py l2scanner/agenda.py l2scanner/console.py); if [ -n "$TOCADOS" ]; then echo "ARQUIVO PROIBIDO ALTERADO: $TOCADOS"; exit 1; fi; echo "deteccao intacta"</automated>
  </verify>

  <done>
Os 446 testes existentes continuam passando. Nenhum `datetime.now()` sobra no corpo de
`__main__.py`. O seam do `momento` usa `relogio.agora_epoch()` e a precedencia de
`frame.momento` (replay) esta intacta. Nenhum arquivo do caminho de deteccao aparece no
diff.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: a prova de que a agenda pegou a hora certa, e de que o arranque sem rede sobe</name>
  <files>tests/test_relogio_no_laco.py</files>

  <behavior>
    - **A agenda le a hora ancorada**: com um `Relogio` ancorado num epoch escolhido e o
      relogio de parede injetado 3h adiantado, montar uma `Sessao` com um evento de agenda
      cujo aviso vence NO horario do servidor, chamar
      `sessao.tick(frame, relogio.agora_epoch())` e afirmar que o aviso saiu. Com a hora do
      Windows (3h adiantada) nao sairia: os 3h atravessam a janela de 5 minutos inteira e o
      aviso morre em silencio. E exatamente o bug relatado, escrito como teste.
    - **O contrario, para o teste nao passar por acaso**: o MESMO cenario alimentado com o
      epoch do relogio de parede torto NAO produz aviso nenhum.
    - **Arranque sem rede nao derruba nada**: `montar_relogio` com uma fonte que sempre
      falha (ou com `ConfigAusente`) devolve um `Relogio` utilizavel, `confiavel` False, e
      `agora()` responde. Nada e levantado. E o requisito de que o scanner suba em
      `--dry-run`, offline, sem `.env`.
    - Usar `tmp_path` para o `RegistroEmDisco` — os marcadores nao podem cair no `.agenda/`
      de verdade do usuario e cancelar um aviso real.
  </behavior>

  <action>
Escrever `tests/test_relogio_no_laco.py` exercitando o contrato de ponta a ponta pelo
nucleo testavel (`Sessao.tick`), nao pelo `while` — o laco precisa de jogo aberto e a
Fase "o tick sai do laco e vira testavel" existiu justamente para isto. Reaproveitar os
utilitarios de frame de `tests/test_silenciamento.py` ou `tests/test_agenda.py` em vez de
inventar fixture nova; se `montar_relogio` exigir isolamento de `.env`, injetar o caminho
em vez de mexer no arquivo real do usuario.
  </action>

  <verify>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -m pytest tests/test_relogio.py tests/test_relogio_no_laco.py -q</automated>
    <automated>cd "C:/Users/refun/Desktop/Lineage2-warnings" && python -m pytest -q</automated>
  </verify>

  <done>
Existe um teste que falha se o `momento` voltar a sair do relogio do Windows, e outro que
falha se o arranque sem rede levantar. Suite inteira verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor Chatwoot -> scanner | O cabecalho `Date` de uma resposta HTTP passa a DIRIGIR a agenda do scanner: quando ele cala e quando ele avisa. |
| relogio do Windows -> scanner | Fonte hoje confiada e comprovadamente errada apos dual boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-c6g-01 | Tampering | `fonte_chatwoot` / cabecalho `Date` | medium | mitigate | A URL vem do `.env` do usuario e e HTTPS na instalacao dele; `epoch_do_cabecalho_date` rejeita ano < 2020, entao um intermediario quebrado nao ancora o scanner em 1970. Acima disso, o estrago maximo e agenda em horario errado — a **deteccao continua em `time.monotonic()` e nao e alcancavel por esta superficie**. |
| T-c6g-02 | Information Disclosure | token do Chatwoot | low | mitigate | A requisicao de ancoragem NAO manda `api_access_token`. Qualquer resposta HTTP traz `Date`, inclusive 401/404 — nao ha motivo para expor o token neste caminho. |
| T-c6g-03 | Denial of Service | arranque do scanner | medium | mitigate | `TIMEOUT_ANCORA = 3.0` e falha -> fallback com WARNING. A ancoragem nunca pode impedir o scanner de subir; a re-ancoragem roda em thread daemon separada para nunca bloquear o laco de captura. |
| T-c6g-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Nenhum pacote instalado.** Somente stdlib (`time`, `datetime`, `email.utils`, `urllib`, `threading`, `logging`) — o gate de imports na Task 1 e automatizado e falha o plano se algo de fora entrar. |
</threat_model>

<verification>
1. `python -m pytest -q` — suite inteira verde (os 446 de hoje + os novos).
2. Sem rede: desligar o Wi-Fi e rodar `python -m l2scanner --so-agenda --dry-run`. Tem que
   SUBIR, imprimir o WARNING de que a hora do servidor nao veio, e anunciar o proximo
   evento normalmente.
3. Com rede: rodar o mesmo comando e conferir a linha de arranque. Se o relogio do Windows
   estiver certo, ela e discreta; se estiver torto (logo depois de voltar do Linux), ela
   diz de quantas horas e o erro.
4. `git diff --stat` — os unicos arquivos de producao tocados sao `l2scanner/relogio.py` e
   `l2scanner/__main__.py`.
</verification>

<success_criteria>
- Um pulo do relogio do Windows depois do arranque nao move a hora que a agenda usa —
  provado por teste, nao por inspecao.
- Sem rede, sem `.env` e sem Chatwoot, o scanner sobe, funciona, e diz em WARNING que esta
  no relogio da maquina.
- Nenhuma dependencia nova. Nenhuma porta nova.
- Nenhuma chamada de `time.monotonic()` do caminho de deteccao alterada.
- O replay continua usando os horarios gravados.
</success_criteria>

<output>
Create `.planning/quick/260825-c6g-hora-certa-do-servidor-nao-depender-do-r/260825-c6g-SUMMARY.md` when done
</output>
