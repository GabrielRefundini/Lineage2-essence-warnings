---
phase: quick-260827-b82
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/comandos.py
  - l2scanner/agenda.py
  - l2scanner/presenca.py
  - l2scanner/loot.py
  - l2scanner/__main__.py
  - tools/check_whatsapp.py
  - README.md
  - config.toml
  - tests/test_comandos.py
  - tests/test_agenda.py
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260827-b82]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/join` funciona: `interpretar('/join')` devolve `Comando.JOIN`, e o caminho REAL (`comandos_novos`, com as cinco travas ligadas) tambem — a barra e o prefixo oficial, pela convencao do proprio Lineage (`/target`, `/invite`) (D-01)."
    - "O ponto continua funcionando: `interpretar('.join')` continua devolvendo `Comando.JOIN` e continua atravessando `comandos_novos` inteiro. Nenhum comando do enum perdeu a forma antiga (D-02)."
    - "Toda a superficie de TEXTO mostra somente a barra: cada `sintaxe` e cada apelido da tabela `_AJUDA` comeca por `PREFIXO`, o cabecalho de `texto_de_ajuda()` fala em barra, e as respostas ao usuario, o README e os comentarios do `config.toml` nao anunciam mais o ponto em lugar nenhum (D-03)."
    - "O porque de aceitar os dois esta escrito na constante do prefixo, e cita a falha real: comando nao reconhecido morre EM SILENCIO no `continue` de `comandos_novos` (nao existe resposta de recusa), entao num corte seco os party-mates ja treinados no ponto nao receberiam NADA e concluiriam que o bot caiu (D-04)."
    - "`/offline` e recusado, exatamente como `.offline`: a protecao `_PALAVRA_HUMANA` e AGNOSTICA de prefixo. Com a barra o risco humano some, mas manter a protecao custa so um personagem hipotetico chamado Offline e evita que os dois prefixos divirjam de comportamento (D-05)."
    - "`/<nick>` consulta, igual a `.<nick>` — e isso nao e escolha solta: a tabela `_AJUDA` passa a anunciar `/<nick>`, e `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` roda a tabela INTEIRA pelo caminho real, entao anunciar a barra e nao aceita-la quebraria a suite (D-06)."
    - "O parser tem UM ponto de decisao sobre prefixo: `sem_prefixo()`. `interpretar` e `interpretar_dinamico` chamam a mesma funcao, e a fronteira que existe entre eles (o dinamico olha a primeira palavra CRUA, com hifens; o fixo remove hifens do miolo, e e por isso que `.loot-j4guar` passa ileso por ele) continua intacta (D-01, D-02)."
    - "Zero dependencia nova: nenhuma linha entrou no `pyproject.toml`, e o diff nao importa nada que ja nao fosse importado (C-01)."
    - "A propriedade do tripwire foi preservada E ampliada: `set(_AJUDA) == set(Comando)` continua valendo, e agora um comando novo anunciado com ponto tambem quebra a suite (C-03)."
    - "A suite nao regrediu: `python -m pytest -q` sai com pelo menos `1077 passed, 2 skipped`, e o numero de testes SOBE — a cobertura da barra foi ACRESCENTADA, nao trocada pela do ponto (C-02, C-04)."
  artifacts:
    - "l2scanner/comandos.py — `PREFIXO = \"/\"`, `PREFIXOS`, `sem_prefixo()`, tabela `_AJUDA` na barra"
    - "tests/test_comandos.py — cobertura da barra acrescentada, cobertura do ponto preservada, tripwire de texto novo"
    - "tests/test_agenda.py — a asercao da chamada passa a afirmar a barra"
    - "README.md e config.toml — a sintaxe documentada ao usuario e so a barra"
    - ".planning/quick/260827-b82-comandos-passam-a-usar-a-barra-do-lineag/260827-b82-SUMMARY.md"
  key_links:
    - "`sem_prefixo()` -> `interpretar` e `interpretar_dinamico`: e o unico lugar onde a barra entra no parser. Duas copias do `startswith` divergiriam no primeiro ajuste e o vocabulario fixo passaria a aceitar uma barra que a superficie dinamica recusa — em SILENCIO, que e o pior modo de falha desta trava."
    - "A tabela `_AJUDA` -> `test_toda_sintaxe_anunciada_volta_como_o_comando_certo`: virar a tabela para a barra CONVERTE aquele teste em prova ponta-a-ponta da barra, atravessando `comandos_novos` com as cinco travas ligadas. E o motivo de a tabela e o parser mudarem na MESMA tarefa."
    - "`tests/test_agenda.py:360` afirma `\".join\" in texto` sobre a chamada do Solo Boss — o texto mais lido do recurso inteiro. Ele QUEBRA quando `agenda.py:274` virar barra, e essa quebra e o alarme de que a tarefa 3 chegou no lugar certo."
    - "`l2scanner/__main__.py:609` e `:118` falam de `.loot/`, e varias linhas falam de `.env` e `.agenda/`: sao NOMES DE DIRETORIO E DE ARQUIVO, nunca comandos. Uma varredura por regex os transformaria em `/loot/` e `/env` e quebraria o projeto — por isso a tarefa 3 e por ocorrencia, com numero de linha, e nunca `sed`."
    - "`autorizado_para` e `COMANDOS_DE_MEMBRO` NAO sao tocados: a trava 5 roda DEPOIS da interpretacao, entao o prefixo novo entrega o mesmo `Comando` na mesma fronteira. Se o diff encostar em qualquer um dos dois, a mudanca saiu do escopo."
---

<objective>
Os comandos do WhatsApp passam a usar a barra, seguindo a convencao do proprio
Lineage (`/target`, `/invite`), com o ponto continuando a funcionar em silencio.

Purpose: a mao que joga ja digita barra. Mas cortar o ponto de vez seria trocar
uma inconveniencia por uma falha muda: comando nao reconhecido e descartado sem
resposta nenhuma, entao os cinco party-mates ja treinados no ponto digitariam o
comando antigo, nao receberiam NADA, e concluiriam que o bot caiu. Aceitar os
dois custa um caractere numa tupla.

Output: parser com dois prefixos e um ponto de decisao so; toda a superficie de
texto (ajuda, respostas, README, config.toml) falando somente a barra; o ponto
coberto por teste como legado vivo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@l2scanner/comandos.py
@l2scanner/presenca.py
@l2scanner/loot.py
@l2scanner/__main__.py
@README.md
@config.toml
@tests/test_comandos.py
</context>

<terreno_confirmado>
Medido agora, antes de planejar. Onde diverge do briefing, vale isto:

- Suite atual: **1077 passed, 2 skipped** em 22s (`python -m pytest -q`). O
  briefing dizia 491 — a base andou. 1077 e o piso.
- `PREFIXO = "."` em `l2scanner/comandos.py:64`, usado em **4 lugares**, todos
  no mesmo arquivo: linhas 669 e 674 (`interpretar`), 727 e 729
  (`interpretar_dinamico`). Confirmado.
- **O briefing nao lista o texto mais importante:** `l2scanner/agenda.py:274-275`
  e a chamada do Solo Boss ("Quem vai? Mande .join no PRIVADO do bot..."). E o
  unico texto deste recurso que o GRUPO INTEIRO le, e sai duas vezes por
  ocorrencia de boss.
- **E nao lista o teste que quebra:** `tests/test_agenda.py:360` afirma
  `".join" in texto` sobre essa chamada. Ele falha assim que o texto virar
  barra — de proposito, e e a rede da tarefa 3.
- Armadilha confirmada: `__main__.py:118` (`PASTA_LOOT`), `:575` e `:609` falam
  de `.loot/`; varias linhas falam de `.env` e `.agenda/`. Sao caminhos.
- Armadilha nova, nao listada: `", ".join(...)` e metodo de string e aparece
  ~15 vezes so no `__main__.py`. Qualquer portao por `grep '\.join'` de arquivo
  inteiro da falso positivo — os portoes abaixo sao por FRASE ou por TESTE.
- Contagens de partida dos portoes: README **20**, config.toml **6**,
  `__main__.py` **6**, `check_whatsapp.py` **1**.
</terreno_confirmado>

<decisoes>
Travadas pelo usuario, nao reabrir:

- **D-01** — A barra e o prefixo OFICIAL, pela convencao do Lineage.
- **D-02** — O ponto continua funcionando. Os DOIS valem.
- **D-03** — Toda a superficie de TEXTO mostra somente a barra: `_AJUDA`,
  respostas, README, comentarios do `config.toml`.
- **D-04** — O raciocinio do descarte silencioso vira comentario no codigo, na
  constante do prefixo.

Decididas aqui, porque o briefing mandou decidir e documentar:

- **D-05 — `/offline` tambem e recusado.** A protecao `_PALAVRA_HUMANA` fica
  AGNOSTICA de prefixo. Com a barra o risco humano some (ninguem digita
  `/offline` para avisar gente), mas o ponto continua aceito e a protecao nao
  pode sair. Torna-la especifica de prefixo criaria uma diferenca de
  comportamento entre as duas formas — exatamente o contrario de "os dois
  valem" — e o unico preco de mante-la e um personagem hipotetico chamado
  Offline, que a propria docstring de hoje ja aceitou perder.
- **D-06 — `/<nick>` consulta, igual a `.<nick>`.** Coerente, e na verdade
  FORCADO: a tabela `_AJUDA` passa a anunciar `/<nick>`, e
  `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` roda a tabela
  inteira pelo caminho real de leitura. Anunciar a barra sem aceita-la quebra a
  suite. As fronteiras que precisam crescer estao na tarefa 2.
</decisoes>

<restricoes>
- **C-01** — Zero dependencia nova.
- **C-02** — Nao reescrever para barra os testes que exercitam o ponto. O ponto
  continua valendo e precisa continuar coberto. ACRESCENTAR cobertura da barra;
  CONVERTER so o que afirma a sintaxe OFICIAL (ajuda, respostas, README).
- **C-03** — Preservar a propriedade do tripwire do `_AJUDA`: crescer o enum sem
  crescer a tabela tem que continuar quebrando a suite.
- **C-04** — Suite verde ao final, sem regressao. Piso: 1077 passed, 2 skipped.
- Portugues SEM ACENTOS no codigo; comentario explica o PORQUE citando a falha
  real. Testes rodam com `python -m pytest` (o `.venv` nao tem pytest).
</restricoes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A barra atravessa o parser inteiro, e a ajuda passa a anuncia-la</name>
  <files>l2scanner/comandos.py, tests/test_comandos.py</files>

  <read_first>
    l2scanner/comandos.py — cabecalho inteiro (linhas 1-48: as cinco travas e os
    dois niveis de autorizacao), a constante em 64, a tabela `_AJUDA` em 237-291,
    `texto_de_ajuda` em 294-318, `interpretar` em 654-680, `_PALAVRA_HUMANA` em
    689-692, `interpretar_dinamico` em 704-812.
    tests/test_comandos.py:190-280 — o tripwire e os testes de ajuda ja existentes.
  </read_first>

  <behavior>
    - `interpretar("/join")` devolve `Comando.JOIN`; `interpretar(".join")`
      continua devolvendo `Comando.JOIN`.
    - `interpretar("/cancelar silencio")` devolve `CANCELAR_SILENCIO` — a forma
      de duas palavras vale nos dois prefixos.
    - `interpretar("join")` sem prefixo nenhum continua devolvendo `None`.
    - Todo valor de `_AJUDA` tem `sintaxe` e todos os `apelidos` comecando por
      `PREFIXO` — tripwire novo, no estilo do que ja existe.
    - `texto_de_ajuda()` nao anuncia nenhuma sintaxe com o prefixo legado.
    - `set(_AJUDA) == set(Comando)` continua valendo (C-03).
  </behavior>

  <action>
    Esta e a fatia vertical: uma unica mudanca que atravessa constante ->
    parser fixo -> parser dinamico -> tabela de ajuda -> texto entregue, e cuja
    prova ponta-a-ponta ja existe na suite.

    Em `l2scanner/comandos.py`, linha 64: trocar a constante unica por um par.
    `PREFIXO` (singular) passa a ser a barra e continua sendo o OFICIAL — o
    unico que aparece em texto. `PREFIXOS` (tupla) e o que o parser ACEITA, com
    a barra primeiro e o legado depois. O comentario dessa constante e onde
    mora o porque, per D-04: dizer que a barra segue a convencao do proprio
    Lineage, e dizer que o legado nao foi cortado porque comando nao
    reconhecido morre em silencio no `continue` do laco de autorizacao de
    `comandos_novos` — nao existe resposta de recusa —, entao um corte seco
    deixaria os cinco party-mates ja treinados sem resposta nenhuma, concluindo
    que o bot caiu. Deixar explicito qual das duas constantes serve a texto e
    qual serve a parsing, senao a proxima pessoa usa a errada.

    Criar `sem_prefixo(palavra: str) -> str | None`: devolve o que sobra depois
    do primeiro prefixo que casar, ou `None` quando nao casa nenhum. Usar
    `is None` nos chamadores, nunca teste de verdade — um prefixo sozinho
    devolve string vazia, que e um caso legitimo e ja morre sozinho mais
    adiante. A docstring dela explica por que ela existe: com dois prefixos
    vivos, duas copias do `startswith` divergiriam no primeiro ajuste e o
    vocabulario fixo passaria a aceitar uma forma que a superficie dinamica
    recusa, em silencio.

    Trocar os dois pares de linhas pelo helper: 669/674 em `interpretar`, e
    727/729 em `interpretar_dinamico`. PRESERVAR A FRONTEIRA ENTRE OS DOIS: o
    dinamico continua olhando a primeira palavra CRUA, com hifens; o fixo
    continua removendo hifen e sublinhado do miolo. Nao reaproveitar o miolo de
    um no outro — e essa separacao que faz o comando de loot com hifen passar
    ileso pelo parser fixo.

    Virar a tabela `_AJUDA` (237-291) para a barra: cada campo `sintaxe` e cada
    entrada de `apelidos`, os doze comandos. Nao mexer em `familia` nem em
    `descricao`. Ajustar o cabecalho montado por `texto_de_ajuda` (linha 307)
    para falar em barra em vez de ponto.

    Atualizar a TRAVA 2 do cabecalho do modulo (linhas 14-17), que hoje declara
    o prefixo antigo como a regra: ela passa a dizer que o oficial e a barra e
    que a forma antiga segue aceita e nao anunciada. O restante do cabecalho e
    das docstrings do arquivo NAO entra nesta tarefa — sao texto de
    desenvolvedor, e varre-los inflaria o diff sem beneficio para o usuario.

    No comentario de `_PALAVRA_HUMANA` (689-692), registrar D-05: a exclusao
    continua valendo para OS DOIS prefixos; com a barra o risco de confundir
    convencao humana com consulta some, mas a forma antiga continua aceita e
    torna a protecao especifica de prefixo criaria uma divergencia de
    comportamento entre duas formas que devem ser a mesma coisa.

    Em `tests/test_comandos.py`, na classe de ajuda que ja existe, acrescentar
    o tripwire de texto: para cada entrada de `_AJUDA`, afirmar que `sintaxe` e
    cada apelido comecam por `comandos.PREFIXO`. Derivar de `PREFIXO`, nunca de
    um literal — assim a prova acompanha a constante. A mensagem de falha diz o
    nome do comando culpado, no estilo do tripwire vizinho.

    NAO TOCAR: `autorizado_para`, `COMANDOS_DE_MEMBRO`, `_VOCABULARIO`,
    `_PALAVRAS_DE_CANCELAMENTO`, `comandos_novos`. A trava de autorizacao roda
    DEPOIS da interpretacao e recebe o mesmo `Comando` que sempre recebeu.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && python -m pytest tests/test_comandos.py -q 2>&1 | tail -3 && python -c "import l2scanner.comandos as c; assert c.PREFIXO == '/'; assert '.' in c.PREFIXOS; assert c.interpretar('/join') is c.Comando.JOIN; assert c.interpretar('.join') is c.Comando.JOIN; assert c.interpretar('join') is None; assert all(l.sintaxe.startswith(c.PREFIXO) and all(a.startswith(c.PREFIXO) for a in l.apelidos) for l in c._AJUDA.values()); assert set(c._AJUDA) == set(c.Comando); print('OK tracer')"</automated>
  </verify>

  <done>
    `test_toda_sintaxe_anunciada_volta_como_o_comando_certo` — que ja existia e
    nao foi tocado — passa a rodar a tabela inteira NA BARRA pelo caminho real
    (`comandos_novos` -> `interpretar` -> `interpretar_dinamico`) com as cinco
    travas ligadas, e passa. O legado continua interpretado. `tests/test_comandos.py`
    verde inteiro, com pelo menos um teste a mais que antes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: As fronteiras dos dois prefixos, provadas — e as duas decisoes, registradas</name>
  <files>tests/test_comandos.py</files>

  <read_first>
    tests/test_comandos.py:280-460 — a classe `TestInterpretarDinamico`, para
    copiar o estilo e nao duplicar caso ja coberto.
  </read_first>

  <behavior>
    - Paridade: para uma amostra representativa do vocabulario fixo, as duas
      formas devolvem o MESMO `Comando`, parametrizado sobre os dois prefixos.
    - D-05: a palavra humana e recusada nas DUAS formas.
    - D-06: nick conhecido consulta pelas duas formas; palavra que mora no
      vocabulario e recusada como consulta pelas duas formas.
    - Superficie dinamica na barra: designacao com hifen, designacao com duas
      palavras, cancelamento com hifen, cancelamento por palavra reservada, e o
      comando de loot SOZINHO continuando a nao fazer nada (D-02 do codigo:
      comando sem argumento nao pode ser destrutivo).
    - Prefixo duplicado (`//join`) e prefixo sozinho devolvem `None`.
    - Legado ponta-a-ponta: uma mensagem com o prefixo antigo continua saindo de
      `comandos_novos` com as travas ligadas — nao so de `interpretar`.
  </behavior>

  <action>
    Acrescentar uma classe nova a `tests/test_comandos.py` cobrindo a barra e a
    convivencia. ACRESCENTAR, nunca converter: nenhum teste que hoje exercita a
    forma antiga muda de prefixo nesta tarefa (C-02). A forma antiga e legado
    ACEITO, e legado sem teste e legado que morre no proximo refactor sem
    ninguem perceber.

    A paridade e parametrizada sobre `comandos.PREFIXOS`, e nao escrita duas
    vezes: assim um terceiro prefixo, se um dia existir, ja nasce provado. A
    docstring da classe registra por que os dois convivem — o descarte
    silencioso — e por que a prova e de paridade e nao de igualdade de texto.

    Escrever o caso de D-05 com a razao na docstring: a protecao existe porque
    a palavra e convencao HUMANA do grupo e nao pode virar consulta de nick, e
    ela e agnostica de prefixo de proposito, para as duas formas nunca
    divergirem de comportamento.

    Escrever os casos de D-06 cobrindo os dois lados da fronteira: nick
    conhecido consulta, e palavra que mora no vocabulario NAO consulta (senao
    um personagem homonimo de comando sombrearia o comando). Registrar na
    docstring que a barra ganhou a consulta por consequencia de a ajuda
    anuncia-la, e que o tripwire de sintaxe anunciada e quem forca isso.

    Cobrir a superficie dinamica na barra caso a caso, e em especial o
    cancelamento por palavra reservada em DUAS palavras — foi exatamente essa
    forma que, tratada so com hifen, ja fez o scanner designar um personagem
    inexistente chamado "cancelar" numa tarefa anterior deste projeto. A forma
    de duas palavras nunca e opcional num comando que mexe em estado duravel.

    Fechar com o caso ponta-a-ponta do legado: montar a mensagem no formato que
    `comandos_novos` recebe, com telefone de dono autorizado, e afirmar que a
    forma antiga sai reconhecida. Sem isto, so temos prova no `interpretar`, e a
    promessa feita aos party-mates ja treinados e sobre o caminho inteiro.

    NAO TOCAR `tests/test_presenca.py`. A classe `TestDestinoDosComandosAntigos`
    (linha ~1370) roda a tabela de comandos antigos pelo despacho INTEIRO e
    afirma para onde cada resposta vai; deixa-la na forma antiga entrega de
    graca a regressao de rota do legado, que e exatamente o que precisamos
    provar e o que uma conversao apagaria.
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && python -m pytest tests/test_comandos.py tests/test_presenca.py -q 2>&1 | tail -3</automated>
  </verify>

  <done>
    Os dois prefixos tem cobertura de fronteira; D-05 e D-06 estao afirmados por
    teste com a razao escrita na docstring; nenhum teste da forma antiga foi
    convertido nem removido; `tests/test_presenca.py` intocado e verde.
  </done>
</task>

<task type="auto">
  <name>Task 3: Toda a superficie de texto passa a falar somente a barra</name>
  <files>l2scanner/agenda.py, l2scanner/presenca.py, l2scanner/loot.py, l2scanner/__main__.py, tools/check_whatsapp.py, README.md, config.toml, tests/test_agenda.py</files>

  <precondition>As tarefas 1 e 2 estao commitadas e a suite esta verde — o texto so pode anunciar a barra depois que a barra funciona.</precondition>

  <behavior>
    - A chamada do Solo Boss (`agenda.texto_do_aviso` com `TipoDeAviso.CHAMADA`)
      anuncia a barra e nao contem mais a forma antiga.
    - `tests/test_agenda.py:360`, que hoje afirma a forma antiga nesse texto,
      passa a afirmar a barra E a negar a forma antiga.
  </behavior>

  <action>
    POR OCORRENCIA, COM NUMERO DE LINHA. Nunca `sed`, nunca regex de arquivo
    inteiro: `.loot/`, `.env`, `.agenda/`, `.venv`, `.gitignore` e o metodo de
    string `.join(` sao nomes de caminho e chamadas de metodo, e uma varredura
    cega os quebraria. O `__main__.py` sozinho tem cerca de quinze chamadas do
    metodo de string.

    Trocar o prefixo para barra nestas ocorrencias, e so nestas:

    - `l2scanner/agenda.py` 274 e 275 — a chamada do Solo Boss. **E o texto mais
      lido do recurso inteiro**: sai duas vezes por ocorrencia de boss, para o
      grupo todo. Se so uma linha desta tarefa for feita, e esta.
    - `l2scanner/presenca.py` 283 — a resposta de erro de disco, que pede para
      mandar o comando de novo.
    - `l2scanner/loot.py` 1089, 1090 e 1108 — as respostas de gramatica recusada
      e de horario sem boss por perto. Sao os unicos textos do arquivo que
      ENSINAM sintaxe ao usuario; as docstrings do mesmo arquivo ficam como
      estao.
    - `l2scanner/__main__.py` 518, 529, 551, 560, 592 e 608 — as mensagens de
      arranque no console. **Nao encostar em 118, 575 nem 609**, que falam do
      diretorio de loot, nem em nenhuma mencao ao arquivo de ambiente.
    - `tools/check_whatsapp.py` 429 — a dica final do diagnostico de entrada.
    - `README.md` — o cabecalho da secao de comandos (289), a tabela inteira
      (291-300), a frase dos apelidos da ajuda (302), a prosa que compara os
      dois comandos de loot (308-326) e o paragrafo do bloco de membro
      (350-351). **Nao encostar** nas mencoes ao arquivo de ambiente, ao
      gitignore, ao interpretador dentro do ambiente virtual nem ao
      `config.local.toml`.
    - `config.toml` 70-86 — os comentarios que dizem o que um membro alcanca.

    Depois, `tests/test_agenda.py:360`: converter a asercao para a barra e
    ACRESCENTAR a negativa da forma antiga na mesma linha de raciocinio. Essa
    asercao afirma a sintaxe OFICIAL no texto mais publico do produto, entao ela
    e exatamente o caso que C-02 manda converter em vez de duplicar.

    Ao escrever comentario de codigo nesta tarefa, nao reintroduzir as frases
    antigas: os portoes abaixo procuram por frase, e uma frase ressuscitada num
    comentario reprova a tarefa com razao — o texto continuaria no arquivo.

    Nao acrescentar teste novo aqui alem da conversao acima. Os portoes por
    frase, com contagem de partida medida, sao a prova desta tarefa; teste novo
    de redacao amarraria a frase e quebraria em toda melhoria de texto, que e a
    rede que se aprende a ignorar (ver a docstring de
    `TestDestinoDosComandosAntigos`).
  </action>

  <verify>
    <automated>cd /c/Users/refun/Desktop/Lineage2-warnings && echo "README (era 20):" && grep -cE '`\.(join|leave|entrar|sair|loot|corrigir|pegou|status|solo|party|cancelar|help|ajuda|comandos|pt|<nick>|\?)' README.md; echo "config.toml (era 6):" && grep -cE '(^|[^A-Za-z0-9_/])\.(join|leave|entrar|sair|loot|corrigir|pegou|status|solo|party|cancelar|help|ajuda|comandos)\b' config.toml; echo "__main__ (era 6):" && grep -cE 'Mande \.status|so \.join/\.leave|dar \.join/\.leave|inclusive \.corrigir|um \.join pode ser|alcancaria \.corrigir' l2scanner/__main__.py; echo "check_whatsapp (era 1):" && grep -c 'Mande \.cancelar' tools/check_whatsapp.py; echo "caminhos intactos (tem que ser >0):" && grep -c '\.loot/\|\.env' l2scanner/__main__.py; python -c "from l2scanner.agenda import Aviso, TipoDeAviso, texto_do_aviso; from datetime import datetime as d; t=texto_do_aviso(Aviso('Solo Boss', TipoDeAviso.CHAMADA, d(2026,8,27,20,0), d(2026,8,27,18,10))); assert '/join' in t and '/leave' in t, t; assert '.join' not in t and '.leave' not in t, t; print('OK chamada')"; python -m pytest -q 2>&1 | tail -3</automated>
  </verify>

  <done>
    Os quatro portoes por frase saem em ZERO (eram 20, 6, 6 e 1), os caminhos
    de diretorio e de arquivo de ambiente seguem intactos (contagem maior que
    zero), a chamada do Solo Boss anuncia a barra e nega a forma antiga, e a
    suite inteira sai com pelo menos `1077 passed, 2 skipped` e com mais testes
    do que comecou.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp -> Chatwoot -> `comandos_novos` | Texto de terceiro entra e vira acao. Esta tarefa ALARGA o vocabulario de entrada aceito, entao a fronteira e o assunto. |
| `[[membro]]` vs `CHATWOOT_TELEFONES_COMANDO` | Os dois niveis de autorizacao. Nao tocados, mas o alargamento do parser passa por cima deles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-b82-01 | Elevation of Privilege | `sem_prefixo` -> `interpretar` / `interpretar_dinamico` | high | mitigate | Um so ponto de decisao sobre prefixo, chamado pelos dois leitores. Duas copias divergiriam e abririam uma forma que so um dos parsers valida. Provado pela paridade parametrizada da tarefa 2. |
| T-b82-02 | Elevation of Privilege | `autorizado_para` / `COMANDOS_DE_MEMBRO` | high | mitigate | Nao tocados. A trava 5 roda DEPOIS da interpretacao e recebe o mesmo `Comando`; a barra nao amplia o que um party-mate alcanca. O teste de fronteira ja deriva a lista de recusa de `set(Comando) - COMANDOS_DE_MEMBRO` e cresce sozinho. Diff que encoste em qualquer um dos dois esta fora de escopo. |
| T-b82-03 | Tampering | superficie dinamica com barra | low | accept | Mensagem comecando com barra e comum em chat (caminho de arquivo, comando de outro bot). Contida pelo mesmo desenho de hoje: charset fechado de nick (a barra nao pertence a ele), consulta so de nick CONHECIDO, vocabulario fixo com precedencia. Um `/palavra` arbitrario continua morrendo em silencio. |
| T-b82-04 | Repudiation | descarte silencioso | medium | accept | E o proprio motivo de D-02: como o parser nao responde recusa, cortar o prefixo antigo seria indistinguivel de "o bot caiu". Aceito com mitigacao por compatibilidade, nao por resposta de erro — acrescentar resposta de recusa abriria eco a qualquer texto de terceiro. |
| T-b82-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalacao de pacote nesta tarefa (C-01, zero dependencia nova). Sem superficie de cadeia de suprimentos. |
</threat_model>

<verification>
1. `python -m pytest -q` sai com pelo menos `1077 passed, 2 skipped` e com mais
   testes do que comecou (cobertura ACRESCENTADA, C-02/C-04).
2. As duas formas atravessam `comandos_novos` com as cinco travas ligadas.
3. Os quatro portoes por frase saem em zero; os caminhos de diretorio e o metodo
   de string seguem intactos.
4. `git diff --stat` nao lista `pyproject.toml` (C-01) e nao lista mudanca em
   `autorizado_para` nem em `COMANDOS_DE_MEMBRO`.
5. `git add` por caminho explicito: `.gsd/` nao esta rastreado nem ignorado, e um
   `git add -A` o varreria para dentro do commit.
</verification>

<success_criteria>
- `/join` funciona; `.join` funciona; a ajuda, as respostas, o README e o
  `config.toml` anunciam somente `/`.
- O porque de aceitar os dois esta na constante do prefixo, citando o descarte
  silencioso.
- D-05 e D-06 decididos, escritos no codigo e provados por teste.
- Suite verde, sem regressao, com o legado ainda coberto.
</success_criteria>

<output>
Create `.planning/quick/260827-b82-comandos-passam-a-usar-a-barra-do-lineag/260827-b82-SUMMARY.md` when done
</output>

