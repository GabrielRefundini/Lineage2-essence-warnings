---
phase: quick-260902-i4o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - l2scanner/manutencao.py
  - l2scanner/sessao.py
  - tests/test_manutencao.py
  - tests/test_sessao.py
autonomous: true
requirements: [QUICK-260902-i4o]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O DEFEITO MEDIDO EM CAMPO FECHA (D-03). Em 2026-09-02, entre 11:58 e 12:52, o grupo de WhatsApp do usuario recebeu ~19 mensagens `MANUTENCAO DO SERVIDOR em X (as HH:MM). Nao entre em instance.` para UMA unica manutencao, com o horario-alvo pulando entre 17 leituras: 12:59, 12:56, 12:59, 13:05, 12:18, 12:11, 12:26, 12:29, 12:30, 12:34, 12:35, 12:37, 12:38, 12:40, 13:05, 12:43, 12:56 — um espalhamento de 54 minutos (12:11 ate 13:05). Depois desta tarefa, replayar essa mesma sequencia produz EXATAMENTE UM aviso `ANUNCIADA`."
    - "A PROVA E MUTACAO MEDIDA, NAO LEITURA (D-07). O teste de replay de campo e rodado contra a producao MUTILADA (o `self._emitidos.clear()` do ramo de remarcacao restaurado) e FALHA, e a saida do pytest com a contagem que ele mede nesse estado vai LITERALMENTE para o SUMMARY, ao lado da saida passando. Sem os dois numeros a tarefa nao esta feita."
    - "A ANCORA CONTINUA SE MOVENDO — a intencao original do `_emitidos.clear()` foi SEPARADA, nao apagada (D-03). Uma manutencao genuinamente remarcada, confirmada por duas leituras concordantes fora da tolerancia, TROCA `self._ancora` exatamente como hoje: o comentario existente diz que a ancora velha nao pode prender o vigia num horario que nao existe mais, e isso continua verdade. O que deixou de acontecer e o ANUNCIADA voltar a parecer novo."
    - "SO `_expirar` ENCERRA O EPISODIO (D-03). `_emitidos.clear()` passa a existir em UM lugar so, dentro de `_expirar` (`manutencao.py:761`), que roda quando `agora > ancora + FOLGA_APOS_A_MANUTENCAO`. `tests/test_manutencao.py::TestExpiracao::test_depois_de_expirar_uma_manutencao_nova_pode_ser_anunciada` continua VERDE sem edicao — e ele que prova que o remedio contra o spam nao criou o pior modo de falha do projeto (a manutencao seguinte detectada, ancorada e nunca anunciada)."
    - "O SEGUNDO AVISO SAI AOS 10 MINUTOS (D-01). `ANTECEDENCIA` passa de `timedelta(minutes=5)` para `timedelta(minutes=10)` em `manutencao.py:73`. Provado por um teste que ancora uma manutencao acima de 10 minutos, afirma que NADA sai enquanto o restante e maior que 10 minutos, e afirma que o aviso sai no primeiro tick em que o restante cai a 10 minutos ou menos — com o texto dizendo os minutos REAIS, nunca a palavra fixa."
    - "O SEGUNDO AVISO NAO RE-ARMA POR DESLIZE (D-04). Depois de emitido, uma reancoragem DENTRO da tolerancia de 60 s nao o re-emite (o ramo de tolerancia nao encosta em `_emitidos`, hoje e depois). Ele so volta a ficar armado quando uma remarcacao confirmada traz `duracao > ANTECEDENCIA` — manutencao genuinamente adiada para mais de 10 minutos. `duracao` E o tempo restante lido, porque `implicado = agora + duracao`, entao a regra e pura e nao precisa de relogio."
    - "O RESIDUO ACEITO ESTA MEDIDO E ESCRITO, nao escondido (D-04). Com a regra de re-armar, o segundo aviso ainda pode sair mais de uma vez se o OCR deslizar a ancora repetidamente para ACIMA de 10 minutos — em campo o segundo tipo saiu 3 vezes (12:11, 12:41, 12:52). O teste de replay afirma o numero EXATO que a sequencia de campo produz depois do conserto, e a docstring dele diz que esse numero e custo aceito e de onde ele vem."
    - "O VALOR `faltam5` SOBREVIVE A RENOMEACAO, COM TRIPWIRE (D-02). O membro do enum passa a se chamar `ANTES` (o mesmo nome que `agenda.TipoDeAviso.ANTES` ja usa para o aviso de antecedencia), e `.value` continua sendo a string `faltam5`. Um teste novo afirma o valor e explica por que ele nao acompanha o nome: o valor entra em `chave_do_marcador` e vira nome de arquivo em `.agenda/`, e troca-lo faria um aviso ja enviado voltar a parecer novo — a mesma lei ja escrita em `agenda.py:76-79` e no proprio enum."
    - "OS NUMEROS DE CAMPO ENTRAM NA DOCUMENTACAO (D-06). O cabecalho de `l2scanner/manutencao.py` ganha uma secao no formato das duas que ja existem, nomeando: a data (2026-09-02), a janela (11:58-12:52), a contagem (~19 mensagens de um tipo e 3 do outro), o espalhamento dos 17 horarios-alvo (12:11 a 13:05), e a mecanica — leitura fora da tolerancia vira `_candidata`, a leitura seguinte 5 s depois repete o MESMO erro sistematico de OCR e portanto cai dentro dos 60 s de `TOLERANCIA_DO_CONSENSO` entre si, reancora, e o `_emitidos.clear()` re-armava o ANUNCIADA."
    - "POR QUE A DEDUP EM DISCO NAO SEGUROU, escrito onde ela mora (D-06). `_processar_manutencao` (`sessao.py:1186`) ganha o paragrafo: `chave_do_marcador` deriva do MOMENTO DA ANCORA arredondado ao minuto, entao cada deslize de minuto produz uma chave inedita e o `registro.marcar` de `sessao.py:1203` cria um arquivo novo em vez de barrar. A guarda de uma-vez-por-episodio tem de viver no vigia; o disco protege contra as DUAS INSTANCIAS do usuario, nunca contra a ancora escorregando. O comportamento de `sessao.py` NAO muda — a edicao e so de docstring."
    - "A SUITE INTEIRA PASSA. `python -m pytest -q` sai com codigo 0 e a contagem de `passed` fica ACIMA do baseline medido no inicio da Tarefa 1 (4919 testes coletados hoje). Nenhum teste existente muda de veredito por acidente: as edicoes em testes ja existentes sao SO as listadas na Tarefa 2, cada uma com a razao aritmetica escrita."
    - "Zero dependencia nova. `pyproject.toml` e `calibration.json` ficam fora do diff, e o diff inteiro cabe em quatro arquivos."
  artifacts:
    - "l2scanner/manutencao.py — `ANTECEDENCIA = timedelta(minutes=10)`, `TipoDeAvisoDeManutencao.ANTES` com `.value` inalterado, `texto_de_antecedencia`, e o ramo de remarcacao de `_registrar` sem `_emitidos.clear()` e com o re-armar condicionado a `duracao > ANTECEDENCIA`"
    - "l2scanner/sessao.py — SOMENTE a docstring de `_processar_manutencao`, explicando por que a dedup em disco nao segura a ancora deslizante"
    - "tests/test_manutencao.py — o replay da sequencia de campo, o teste do limiar de 10 minutos, os dois testes da regra de re-armar, o tripwire do valor `faltam5`, e as correcoes aritmeticas dos testes cujas duracoes ficaram abaixo do limiar novo"
    - "tests/test_sessao.py — a costura ponta-a-ponta com a ancora deslizando: vigia -> marcador em disco -> resultado -> despacho, com UM despacho de anuncio"
  key_links:
    - "`VigiaDeManutencao._registrar` (`manutencao.py:871`, ramo da candidata concordante) -> `self._ancora` troca / `self._emitidos` sobrevive — e o ponto exato do conserto"
    - "`VigiaDeManutencao._expirar` (`manutencao.py:761`) -> `self._emitidos.clear()` — o UNICO lugar que encerra o episodio, depois desta tarefa"
    - "`ANTECEDENCIA` (`manutencao.py:73`) -> `_avisos_devidos` (`manutencao.py:937`, o limiar do segundo aviso) E `_registrar` (a condicao de re-armar). O mesmo numero decide os dois, e por isso o membro do enum passa a se chamar ANTES em vez de carregar um numero"
    - "`TipoDeAvisoDeManutencao.ANTES.value` -> `chave_do_marcador` (`manutencao.py:584`) -> `registro.marcar` (`sessao.py:1203`) -> nome de arquivo em `.agenda/` — a cadeia que obriga o valor a nao mudar"
---

<objective>
Parar o spam do aviso de manutencao no WhatsApp e mover o segundo aviso de 5 para 10
minutos.

Purpose: em 2026-09-02, entre 11:58 e 12:52, o grupo recebeu ~19 vezes a mesma mensagem
de anuncio de manutencao, para UMA manutencao so, com o horario-alvo pulando por uma
faixa de 54 minutos. O usuario: "Funcionou perfeitamente mas esta spammando muito, avise
apenas quando aparece o anuncio e quando faltar 10m." O recurso esta certo no essencial
— ele leu o banner, ancorou e entregou. O que esta errado e que uma reancoragem dentro
do mesmo episodio re-arma o anuncio, e a dedup em disco nao segura porque a chave dela
deriva da propria ancora que escorregou.

Output: o ANUNCIADA vira UMA VEZ POR EPISODIO (so `_expirar` libera um novo), o segundo
aviso passa a sair aos 10 minutos, e os numeros de campo ficam escritos no codigo — que
neste repositorio e parte do conserto, nao enfeite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@l2scanner/manutencao.py
</context>

<interface_context>
O que ja existe, com as coordenadas conferidas hoje:

- `l2scanner/manutencao.py:73` — `ANTECEDENCIA = timedelta(minutes=5)`, com o comentario
  `# Quando sai o SEGUNDO aviso (GOAL-02).`
- `l2scanner/manutencao.py:64` — `SEGUNDOS_ENTRE_LEITURAS = 5.0` (a cadencia do OCR).
- `l2scanner/manutencao.py:70` — `TOLERANCIA_DO_CONSENSO = timedelta(seconds=60)`.
- `l2scanner/manutencao.py:77` — `FOLGA_APOS_A_MANUTENCAO = timedelta(minutes=10)`.
- `l2scanner/manutencao.py:572-581` — `class TipoDeAvisoDeManutencao(Enum)` com
  `ANUNCIADA = "anunciada"` e `FALTAM5 = "faltam5"`. A docstring avisa em maiusculas que
  OS VALORES ENTRAM NA CHAVE.
- `l2scanner/manutencao.py:584` — `chave_do_marcador(momento, tipo)` monta
  `{YYYY-MM-DD}_manutencao-{HHMM}_{tipo.value}` a partir do momento da manutencao
  ARREDONDADO ao minuto (`+30 s`, depois `second=0`).
- `l2scanner/manutencao.py:644` — `texto_de_5_minutos(momento, restante)`. Ele ja diz os
  minutos REAIS calculados da ancora; nunca imprimiu a palavra cinco.
- `l2scanner/manutencao.py:734` — `avaliar`: ordem deliberada expirar -> ler ->
  registrar -> montar.
- `l2scanner/manutencao.py:761` — `_expirar`: zera ancora, candidata, duracao e
  `_emitidos`. A docstring ja explica por que limpar ali e OBRIGATORIO.
- `l2scanner/manutencao.py:871` — `_registrar(implicado, duracao)`, tres situacoes. O
  segundo ramo (`self._candidata is not None and self._bate(implicado, self._candidata)`
  com ancora ja posta) e o caminho da remarcacao, e e o unico lugar fora de `_expirar`
  que hoje chama `self._emitidos.clear()`.
- `l2scanner/manutencao.py:913` — `_avisos_devidos(agora)`: monta ANUNCIADA quando ha
  ancora e o tipo nao esta em `_emitidos`; monta o segundo aviso quando
  `restante <= ANTECEDENCIA` e o tipo nao esta em `_emitidos`.
- `l2scanner/sessao.py:1186` — `_processar_manutencao`, com `registro.marcar(aviso.chave)`
  em `sessao.py:1203` como A decisao de despachar.

Invariante aritmetica que o conserto usa: em `_ler_com_as_duas_escalas` o retorno e
`(agora + veredito.duracao, veredito.duracao)`, logo em `_registrar` vale sempre
`implicado - agora == duracao`. `duracao` E o tempo restante no instante da leitura, e e
por isso que a condicao de re-armar pode ser pura, sem receber `agora`.

Testes que tocam esta area (coordenadas de hoje):

- `tests/test_manutencao.py:302` — `test_a_chave_sai_do_momento_da_manutencao_nao_do_aviso`
  (usa `TipoDeAvisoDeManutencao.FALTAM5`).
- `tests/test_manutencao.py:373` — `novo_vigia(texto)` e `rodar(vigia, obter, inicio,
  segundos, passo=1)`, os dois utilitarios que todo teste do vigia usa.
- `tests/test_manutencao.py:489` — `test_uma_manutencao_remarcada_re_ancora_com_duas_
  leituras`, que hoje afirma `[a.tipo for a in saiu] == [ANUNCIADA]`. E O TESTE QUE
  CODIFICA O DEFEITO.
- `tests/test_manutencao.py:736` — `test_o_bloco_das_18h16_...` (campo de 04:12, abaixo
  dos dois limiares — NAO e afetado pelo D-01).
- `tests/test_manutencao.py:912, 935, 949` — os tres de `TestAncoraSobreviveACegueira`.
- `tests/test_manutencao.py:962-1010` — `TestExpiracao`, incluindo o de linha 991 que
  prova que a manutencao seguinte volta a ser anunciada.
- `tests/test_sessao.py:930` — `LeitorDoBanner(texto, leituras=2)`: le duas vezes e fica
  cego; `conferir` NAO consome leitura.
- `tests/test_sessao.py:1067` — `test_o_faltam5_tambem_atravessa_a_costura`, com
  `"Server Maintence 6 minutes"` e o laco `[0, 6] + list(range(7, 131))`.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarefa 1: a fatia fina — a ancora desliza, o anuncio sai UMA vez, ponta a ponta</name>
  <files>l2scanner/manutencao.py, tests/test_manutencao.py, tests/test_sessao.py</files>
  <read_first>
    l2scanner/manutencao.py (linhas 60-80, 570-610, 730-800 e 860-951),
    l2scanner/sessao.py (linhas 1186-1215),
    tests/test_manutencao.py (linhas 312-400 para os utilitarios, e 455-506),
    tests/test_sessao.py (linhas 930-1000 e 1067-1100).
  </read_first>
  <behavior>
    Antes de escrever producao, MEÇA o baseline: `python -m pytest -q` e anote a linha
    final de contagem. Ela e o piso do criterio de aceite da Tarefa 3.

    Depois escreva os testes, veja-os falhar, e so entao mexa na producao (D-07).

    - **O replay da sequencia de campo, em `tests/test_manutencao.py`** — o teste
      central. Reproduza a leitura deslizante de 2026-09-02 com um relogio injetado:
      comece em 11:58 e, para cada um dos 17 horarios-alvo registrados no print
      (12:59, 12:56, 12:59, 13:05, 12:18, 12:11, 12:26, 12:29, 12:30, 12:34, 12:35,
      12:37, 12:38, 12:40, 13:05, 12:43, 12:56), alimente o leitor com um banner cuja
      duracao seja `alvo - agora` no instante da leitura, repetido em DUAS cadencias
      consecutivas — porque em campo a leitura seguinte, 5 s depois, repetia o MESMO
      erro sistematico de OCR e caia dentro dos 60 s da candidata. Rode `avaliar` tick a
      tick ao longo dos ~54 minutos. Afirme que a contagem de `ANUNCIADA` e
      EXATAMENTE 1. Afirme tambem que a ancora se MOVEU ao longo da corrida (o valor
      final difere do primeiro), senao o teste passaria de graca num vigia que parou de
      reancorar. A docstring carrega os numeros de campo e a mecanica.
    - **A costura ponta-a-ponta, em `tests/test_sessao.py`** — a MESMA sequencia
      deslizante entrando por `Sessao.tick`, com o `_frame_com_banner` e o `tmp_path` que
      a classe `TestManutencaoNoTick` ja usa. Nao reuse `LeitorDoBanner` (ele fica cego
      apos duas leituras): escreva um leitor que responde a partir do relogio, com
      `conferir` espelhando sem consumir leitura, como o existente ja faz. Afirme que os
      despachos com "manuten" no texto contem exatamente UM anuncio, e que o `.agenda/`
      do `tmp_path` NAO impediu nada sozinho — ou seja, que a prova vem da guarda no
      vigia e nao do marcador. E o teste que fecha o fio vigia -> marcador -> resultado
      -> despacho.
    - **A remarcacao continua movendo a ancora** — reescreva
      `tests/test_manutencao.py:489`. O que ele afirma sobre a ANCORA (que ela troca, e
      para o horario novo, com tolerancia de 1 s) fica INTACTO: e a intencao original do
      `_emitidos.clear()` e ela nao pode ser apagada junto. So a linha
      `assert [a.tipo for a in saiu] == [ANUNCIADA]` inverte, passando a afirmar que
      NENHUM aviso sai. A docstring passa a dizer as duas coisas — a ancora velha nao
      pode prender o vigia num horario que sumiu, E o grupo nao pode receber o anuncio de
      novo — e cita o episodio de campo.
  </behavior>
  <action>
    No ramo da remarcacao de `_registrar` (`manutencao.py:871`, o bloco
    `if self._candidata is not None and self._bate(implicado, self._candidata)` que roda
    com ancora ja posta), a movimentacao da ancora, da duracao confirmada e a limpeza da
    candidata ficam BYTE-IDENTICAS. Retire dali a chamada que zera o conjunto de emitidos
    inteiro e ponha, no lugar, o descarte APENAS do tipo do segundo aviso, condicionado a
    `duracao > ANTECEDENCIA` (D-03, D-04). Nesta tarefa `ANTECEDENCIA` continua valendo
    cinco minutos e o membro do enum continua com o nome atual — o D-01 e o D-02 sao da
    Tarefa 2, e separa-los e o que deixa a prova de mutacao apontar para uma coisa so.

    O primeiro ramo (reancoragem DENTRO da tolerancia) NAO e tocado: ele ja nao encosta
    em `_emitidos` hoje, e e exatamente o caso "a ancora deslizou alguns segundos" que
    nunca pode re-emitir nada (D-04).

    Escreva na docstring de `_registrar`, no item da remarcacao, o PORQUE com os numeros:
    a leitura fora da tolerancia vira candidata; a leitura seguinte chega 5 s depois
    (`SEGUNDOS_ENTRE_LEITURAS`) repetindo o mesmo erro sistematico de OCR, portanto cai
    dentro dos 60 s de `TOLERANCIA_DO_CONSENSO` em relacao a candidata; a ancora troca — e
    ate 2026-09-02 isso re-armava o anuncio, rendendo ~19 mensagens entre 11:58 e 12:52
    para uma manutencao so. Diga que a ancora precisa mesmo se mover e que o episodio so
    termina em `_expirar`.

    Nao invente teto de mensagens, contador de repeticoes nem estado durável novo. O
    conserto e a separacao entre "mover a ancora" e "re-armar o anuncio", e nada alem
    disso.
  </action>
  <verify>
    <automated>python -m pytest tests/test_manutencao.py tests/test_sessao.py -q</automated>
  </verify>
  <done>
    O replay da sequencia de campo produz exatamente um ANUNCIADA, no teste do vigia e na
    costura pelo `Sessao`. A ancora continua trocando numa remarcacao confirmada. A
    MUTACAO esta MEDIDA: com o zeramento do conjunto de emitidos restaurado no ramo da
    remarcacao, o teste de replay falha, e a saida do pytest com a contagem que ele
    reporta nesse estado esta copiada para o SUMMARY ao lado da saida passando.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: o segundo aviso passa para 10 minutos, com o nome e o valor decididos por escrito</name>
  <files>l2scanner/manutencao.py, tests/test_manutencao.py, tests/test_sessao.py</files>
  <read_first>
    l2scanner/manutencao.py (linhas 70-80, 570-660 e 913-951),
    l2scanner/agenda.py (linhas 70-80 — o precedente `TipoDeAviso.ANTES` e a lei do valor
    que nao muda),
    tests/test_manutencao.py (linhas 281-311, 736-760 e 903-1010),
    tests/test_sessao.py (linhas 1067-1100).
  </read_first>
  <behavior>
    - **O limiar novo, provado nas duas bordas.** Ancore uma manutencao de 15 minutos.
      Afirme que na ancoragem sai SO o anuncio; que nada mais sai enquanto o restante e
      maior que 10 minutos; que o segundo aviso sai no primeiro tick em que o restante
      cai a 10 minutos ou menos; e que o texto dele diz os minutos REAIS calculados da
      ancora. Um segundo caso ancora com 4 minutos e afirma que os dois avisos saem
      juntos, com o texto dizendo quatro e nao dez — o aviso nunca mente sobre o tempo.
    - **A regra de re-armar, nos dois lados (D-04).** (a) Depois do segundo aviso ter
      saido, uma reancoragem DENTRO da tolerancia (deslize de ~15 s) nao o re-emite, e
      tambem nao re-emite o anuncio. (b) Depois do segundo aviso ter saido, uma
      remarcacao confirmada por duas leituras concordantes que traz o alvo de volta para
      MAIS de 10 minutos re-arma o segundo aviso — ele sai de novo quando o novo restante
      cair a 10 minutos — e o ANUNCIADA continua sem sair. Este par e a regra inteira.
    - **O tripwire do valor durável (D-02).** Um teste afirma que o membro renomeado
      carrega a string do valor antigo, e a docstring dele explica: o valor entra em
      `chave_do_marcador`, vira nome de arquivo em `.agenda/`, e troca-lo faria um aviso
      ja enviado voltar a parecer novo para um scanner reiniciado no meio de uma
      manutencao em curso. Cite `agenda.py:76-79`, que ja declara a mesma lei para
      `TipoDeAviso.CHAMADA`.
    - **O replay da Tarefa 1 continua verde** com o limiar novo, e a contagem do SEGUNDO
      tipo de aviso que ele produz passa a ser afirmada com o numero EXATO medido. A
      docstring diz que esse numero e o residuo aceito: o segundo aviso pode repetir
      quando o OCR joga a ancora de volta para acima de 10 minutos, e em campo o segundo
      tipo saiu 3 vezes (12:11, 12:41, 12:52).
  </behavior>
  <action>
    Aplique D-01: o valor de `ANTECEDENCIA` (`manutencao.py:73`) passa a ser dez minutos.
    O comentario acima dele ganha a razao: o usuario pediu, em 2026-09-02, ser avisado
    quando aparece o anuncio e quando faltar dez.

    Aplique D-02, e escreva a decisao inteira na docstring do enum
    (`manutencao.py:572`). O membro que hoje carrega o numero cinco no NOME passa a se
    chamar `ANTES`, o mesmo nome que `agenda.TipoDeAviso` ja usa para o aviso de
    antecedencia — vocabulario que este repositorio ja tem, e que nao envelhece quando o
    limiar muda. O `.value` NAO acompanha: continua sendo a string `faltam5`. Diga
    explicitamente que o descasamento entre o nome e o valor e DELIBERADO, que o valor e
    identidade duravel e nao descricao, e qual e o preco de troca-lo (uma mensagem
    duplicada no grupo, para uma manutencao ja em curso, num scanner reiniciado com o
    codigo novo) contra o preco de mante-lo (um nome de arquivo em `.agenda/` que so faz
    sentido com esta docstring ao lado). A decisao e por manter, seguindo a lei que o
    proprio modulo e o `agenda.py` ja escreveram.

    Renomeie `texto_de_5_minutos` (`manutencao.py:644`) para `texto_de_antecedencia` pela
    mesma razao: o numero no nome deixou de ser verdade e a funcao nunca imprimiu numero
    fixo nenhum. Ela nao e importada em lugar nenhum fora deste modulo — confirme com uma
    busca antes de renomear, e ajuste a unica chamada em `_avisos_devidos`.

    Ajuste os testes existentes cuja aritmetica o limiar novo invalida, e SO esses, cada
    um com a conta escrita na docstring:

    - `tests/test_manutencao.py:912` — a manutencao de 8 minutos passaria a disparar os
      dois avisos no mesmo tick da ancoragem, e o teste que existe para provar que o
      segundo aviso sai COM O OCR CEGO deixaria de provar isso. Suba a duracao acima do
      limiar novo e estenda a corrida o bastante para atravessa-lo; capture o retorno da
      ancoragem e afirme que ali saiu so o anuncio.
    - `tests/test_manutencao.py:935` — a manutencao de 3 minutos continua valendo; so a
      docstring e a assercao de texto que citam o numero cinco passam a citar dez.
    - `tests/test_manutencao.py:949` — a manutencao de 6 minutos continua passando, mas
      passaria a emitir os dois avisos no MESMO tick e o teste perderia a forca. Suba a
      duracao acima do limiar novo e estenda a corrida, para que os dois tipos continuem
      saindo em ticks distintos e a contagem de um-de-cada continue medindo algo.
    - `tests/test_sessao.py:1067` — mesma correcao aritmetica: a duracao do banner sobe
      acima do limiar novo e o laco de ticks se estende o bastante para o segundo aviso
      cair dentro dele, com a conta escrita. O nome do teste e a docstring param de citar
      o numero cinco.

    Os demais testes de manutencao NAO sao editados. Em particular o de 04:12
    (`tests/test_manutencao.py:736`) esta abaixo dos dois limiares e passa nos dois
    mundos, e `TestExpiracao` inteiro (`tests/test_manutencao.py:962-1010`) tem de
    continuar verde SEM UMA LINHA de edicao — e ele que garante que a manutencao seguinte
    volta a ser anunciada.

    Renomeie as referencias ao membro do enum nos testes junto com o membro. Onde o nome
    do TESTE cita o numero cinco, renomeie o teste tambem; nao renomeie a fixture nem os
    textos de campo.
  </action>
  <verify>
    <automated>python -m pytest tests/test_manutencao.py tests/test_sessao.py -q</automated>
  </verify>
  <done>
    O segundo aviso sai quando faltam dez minutos, provado nas duas bordas, e o texto diz
    os minutos reais. Re-armar so acontece com remarcacao acima de dez minutos, nunca com
    deslize dentro da tolerancia, e o anuncio nao volta em nenhum dos dois casos. O
    membro renomeado carrega o valor antigo, com tripwire e com a decisao escrita. Os
    quatro testes cuja aritmetica virou estao corrigidos com a conta na docstring, e
    `TestExpiracao` continua verde sem edicao.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: os numeros de campo no cabecalho, o porque da dedup em disco, e a suite inteira</name>
  <files>l2scanner/manutencao.py, l2scanner/sessao.py</files>
  <read_first>
    l2scanner/manutencao.py (linhas 1-48 — as duas secoes historicas que servem de
    formato), l2scanner/sessao.py (linhas 1186-1215).
  </read_first>
  <action>
    Acrescente ao cabecalho de `l2scanner/manutencao.py` uma terceira secao historica, no
    mesmo formato das duas que ja existem (D-06). Ela nomeia: a data 2026-09-02; a janela
    11:58 a 12:52; as ~19 mensagens de anuncio e as 3 do segundo tipo (12:11, 12:41,
    12:52) para UMA manutencao; os 17 horarios-alvo lidos, espalhados por 54 minutos
    (12:11 a 13:05); e a mecanica em uma frase — leitura fora da tolerancia vira
    candidata, a seguinte chega 5 s depois com o mesmo erro sistematico de OCR e portanto
    dentro dos 60 s da candidata, a ancora troca, e o zeramento do conjunto de emitidos
    re-armava o anuncio. Feche dizendo o que mudou: mover a ancora e re-armar o anuncio
    eram a mesma linha e viraram duas, e o episodio agora so termina em `_expirar`.
    Registre tambem, na mesma secao, que a antecedencia passou de cinco para dez minutos
    a pedido do usuario e que o valor duravel do marcador nao acompanhou o nome.

    Na docstring de `_processar_manutencao` (`l2scanner/sessao.py:1186`), acrescente o
    paragrafo que explica por que o `marcar` desta funcao nao segurou o spam: a chave
    deriva do MOMENTO DA ANCORA arredondado ao minuto, entao uma ancora que escorrega
    produz chave inedita a cada minuto novo e o marcador cria arquivo em vez de barrar.
    Diga o que ele protege de verdade — as duas instancias do usuario mandando a mesma
    mensagem — e que a guarda de uma-vez-por-episodio tem de morar no vigia. NENHUMA
    linha de codigo desta funcao muda.

    Rode a suite inteira e compare com o baseline medido na Tarefa 1. Rode o `ruff check`
    apenas nos arquivos tocados e nao conserte os erros pre-existentes do resto do
    repositorio. Confira o escopo do diff.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>
    A suite inteira sai com codigo 0 e a contagem de `passed` esta acima do baseline
    medido na Tarefa 1. O cabecalho do modulo carrega os numeros de campo e a docstring
    do `_processar_manutencao` explica a dedup em disco. `git diff --stat` mostra
    exatamente `l2scanner/manutencao.py`, `l2scanner/sessao.py`,
    `tests/test_manutencao.py` e `tests/test_sessao.py` — nada mais.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR do banner -> ancora no relogio | Texto lido da tela vira um horario duravel que decide quando o grupo e avisado. O erro sistematico de OCR atravessa o consenso temporal porque duas leituras do mesmo metodo concordam no mesmo erro. |
| vigia -> `.agenda/` -> WhatsApp (Chatwoot) | Todo aviso emitido vira mensagem no grupo do usuario. O custo do excesso e medido: ~19 mensagens em 54 minutos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-i4o-01 | Denial of Service (ruido) | `VigiaDeManutencao._registrar` -> grupo de WhatsApp | high | mitigate | E o defeito desta tarefa. O anuncio vira uma-vez-por-episodio (D-03) e o segundo aviso so re-arma com remarcacao acima do limiar (D-04). Provado por replay da sequencia de campo mais mutacao medida (D-07). |
| T-i4o-02 | Denial of Service (silencio) | `_expirar` continuar sendo o unico a encerrar o episodio | high | mitigate | O risco espelhado: uma guarda forte demais faria a manutencao SEGUINTE nunca ser anunciada — o pior modo de falha do projeto, porque de fora parece funcionar. `TestExpiracao` inteiro continua verde sem edicao, e isso e criterio de aceite explicito. |
| T-i4o-03 | Tampering (identidade duravel) | `TipoDeAvisoDeManutencao.<membro>.value` -> nome de arquivo em `.agenda/` | medium | mitigate | O valor NAO muda com o nome (D-02), e um tripwire novo trava a string. Se um dia mudar, o preco esta escrito ao lado: uma mensagem duplicada por manutencao ja em curso num scanner reiniciado. |
| T-i4o-04 | Repudiation (forense pos-farm) | `_registrar_veredito` e o log rotativo | low | accept | O log continua sem limitacao de repeticao, de proposito e ja documentado. Esta tarefa reduz MENSAGENS no grupo, nunca linhas de log — sao as linhas que respondem "por que nao avisou". |
| T-i4o-05 | Tampering | dependencias | high | mitigate | Zero pacote novo; `pyproject.toml` fora do diff. Nenhuma biblioteca de sintese de input entra no ambiente. |
</threat_model>

<verification>
1. `python -m pytest tests/test_manutencao.py tests/test_sessao.py -q` — verde.
2. A MUTACAO MEDIDA da Tarefa 1: a saida do pytest com o teste de replay FALHANDO sob a
   producao mutilada (zeramento do conjunto de emitidos restaurado no ramo da
   remarcacao), com a contagem de anuncios que ele reporta nesse estado, e a saida dele
   passando depois de desfeita. As duas literalmente no SUMMARY.
3. `python -m pytest tests/test_manutencao.py::TestExpiracao -q` — verde, e
   `git diff tests/test_manutencao.py` nao mostra edicao dentro dessa classe.
4. `python -m pytest -q` — codigo 0, `passed` acima do baseline medido na Tarefa 1.
5. `ruff check l2scanner/manutencao.py l2scanner/sessao.py tests/test_manutencao.py tests/test_sessao.py`
   — sem erro NOVO em relacao ao mesmo comando rodado antes do primeiro commit.
6. `git diff --stat` lista somente os quatro arquivos do `files_modified`.
</verification>

<success_criteria>
- Replayar a sequencia deslizante de 2026-09-02 produz UM anuncio, no vigia e pela
  costura do `Sessao`, com a falha sob mutacao medida e escrita.
- A ancora continua se movendo numa remarcacao confirmada — a intencao original foi
  separada, nao apagada.
- So `_expirar` encerra o episodio, e a manutencao seguinte continua sendo anunciada.
- O segundo aviso sai aos dez minutos, dizendo os minutos reais, e so re-arma com
  remarcacao acima do limiar.
- O membro do enum perdeu o numero do nome e manteve o valor, com tripwire e com a
  decisao escrita nos dois sentidos.
- Os numeros de campo estao no cabecalho do modulo, e o porque de a dedup em disco nao
  ter segurado esta na docstring de `_processar_manutencao`.
- Suite inteira verde acima do baseline; zero dependencia nova; diff em quatro arquivos.
</success_criteria>

<output>
Create `.planning/quick/260902-i4o-parar-o-spam-do-aviso-de-manutencao-e-mo/260902-i4o-SUMMARY.md` when done.

O SUMMARY DEVE CONTER, literalmente: a saida do pytest com o teste de replay de campo
FALHANDO sob a mutacao e a saida dele passando; a contagem de anuncios que a sequencia de
campo produz antes e depois; o numero exato de segundos-avisos que ela produz depois do
conserto, com a frase que o declara residuo aceito; a decisao sobre o nome e o valor do
membro do enum, com o preco de cada lado; e a contagem final da suite contra o baseline
medido.
</output>
