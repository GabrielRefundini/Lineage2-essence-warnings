# Captura de tela. Escolhido em vez de dxcam porque dxcam.grab() devolve None
# quando o frame nao mudou — e uma party window parada em AFK e exatamente esse
# caso, que um laco ingenuo leria como "perdi a visao".
mss>=10.2.0

# Analise das barras (HSV, medicao de preenchimento) e as ferramentas visuais de
# calibracao. Precisa ser o pacote completo, NAO o -headless: selectROI,
# createTrackbar e imshow sao o coracao da calibragem.
# Preso abaixo da 5 porque a 5.0 trouxe quebras deliberadas de API.
opencv-python>=4.10,<5

numpy>=2.0

# Captura da janela do jogo mesmo coberta por outras (--janela).
# Validado no spike 001: o XM Essence continua renderizando sem foco.
# NAO resolve janela minimizada — nada resolve, o Windows para de
# produzir frames e nao ha API que contorne.
windows-capture>=1.4

# OCR do banner de manutencao do servidor (l2scanner/manutencao.py).
#
# O motor de OCR JA VEM NO WINDOWS 11 — zero instalador, zero modelo para
# baixar, tudo offline. O que falta e so a ponte para o Python.
#
# SAO SEIS PACOTES porque as bindings do WinRT sao MODULARES desde set/2023:
# cada namespace e um pacote separado, e faltar UM nao quebra no import — quebra
# no meio da cadeia de chamada, que e muito pior de diagnosticar.
#
# Sem elas o scanner sobe exatamente igual e SO o aviso de manutencao se
# desliga, com o arranque dizendo o motivo e o conserto. Ver l2scanner/ocr.py.
# Versoes: as que o spike instalou e exercitou com sucesso.
winrt-Windows.Media.Ocr>=3.2.1
winrt-Windows.Graphics.Imaging>=3.2.1
winrt-Windows.Storage.Streams>=3.2.1
winrt-Windows.Globalization>=3.2.1
winrt-Windows.Foundation>=3.2.1
winrt-Windows.Foundation.Collections>=3.2.1

# NAO ADICIONE AQUI, POR DECISAO DE PROJETO:
#   pyautogui, pydirectinput, keyboard, pynput ou qualquer lib de input.
# O scanner e somente leitura. Manter essas libs fora da arvore de dependencias
# torna a violacao do invariante estruturalmente impossivel, em vez de apenas
# proibida por escrito. Ver l2scanner/__init__.py.
